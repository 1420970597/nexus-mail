# Nexus-Mail 完整开发计划（todo.md）

> **执行模式已切换：** 本文档不再只是规划稿，而是当前仓库的**执行中路线图**。后续提交必须对照阶段目标更新状态，并在阶段完成后进入下一阶段。

## 执行状态看板（持续维护）

### 当前总体进度
- Phase 0：**已完成**（开发底座、Docker、多服务骨架已提交）
- Phase 1：**已完成**（身份权限、共享控制台骨架已提交）
- Phase 2：**已完成**（项目/库存/激活订单主链路、管理员项目配置、供应商资源录入已提交）
- Phase 3：**已完成**（已完成 SMTP 会话落盘、metadata 入库、RabbitMQ 解析任务入队、MinIO 原始对象上传，并打通验证码/link 提取回退增强、订单 READY/FINISHED 自动迁移、真实 OAuth2 刷新接入、授权码/App Password/密码型凭证录入与健康状态落库、`env://` secret_ref 解析、IMAP/POP3 实际登录校验、官方邮箱端点限制、Proton Bridge 接入策略与 Postfix 转发样例）
- Phase 4：进行中（已完成用户钱包、冻结余额、成功扣费、超时退款、供应商待结算余额、管理员调账能力与基础结算页面；已完成供应商报表筛选、供货规则维护、管理员争议单显式筛选、供应商域名运营页、管理员供应商运营页与共享控制台深色单壳导航收敛；已完成 Webhooks / 风控 / 审计真实页面收敛与对应真实 API 回放验证；当前前端后续重点继续围绕 shared-console focused 用例去脆弱化、语义锚点收敛与 new-api 风格单壳一致性补强，再回到 Phase 4/5 余下后端能力）
- Phase 5：进行中（已完成管理员高危操作二次确认、OpenAPI + `/docs`、管理员审计查询、管理员概览/风控真实页、API Key 默认 scopes / 白名单 / Redis 全局限流 / 审计、Webhook 异步投递与 delivery 查询、风控规则 CRUD 等里程碑；后续在“前端先行、真实 API 验证、五维评审后提交”的约束下继续推进 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片）
- Phase 6：未开始

### 本机环境现状（2026-04-13 更新）
- `go version`：可用
- `docker version`：可用
- `docker compose version`：可用
- `pnpm -v`：可用
- 本机已验证可绑定 **25 端口**

### 本地参考资料
- 已在项目目录放置竞品调研参考：`docs/reference-local/luckyous-reference.md`
- 该目录已加入本地 Git 排除规则，**仅作为本机开发参考，不推送远程仓库**

### 最新执行检查点（2026-05-07，Profile capability bridge canonical heading 收敛）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 Phase 4/5 契约的前提下，仅把 `web/src/pages/ProfilePage.tsx` 中个人资料页 capability bridge 的 API 接入卡片文案从泛化的 `API 接入控制面 / 前往 API Keys` 收敛到与共享路由目录一致的 `API Keys / 打开 API Keys`，并同步把 `web/src/pages/ProfilePage.test.tsx` 中该桥接链路的 focused proof 收紧为“按钮仍作用域限定在 `profile-capability-bridge` 内，且 Webhooks / Docs 两条 shared-console 路由继续命中 canonical heading”；其中 API Keys 继续保持 route-stub 导航命中断言，以避免把跨页目标文案耦合到 Profile 页自身测试合同里，同时保持“个人资料页只作为 shared-console 单壳桥接中枢，不派生独立接入后台”的产品叙事。
- 控制器已按 TDD 先修改 `web/src/pages/ProfilePage.test.tsx` 让 focused 用例在 RED 阶段显式要求 capability bridge 内出现 `打开 API Keys`，并在点击 Webhook / Docs CTA 后命中 `开发者 Webhook 接入工作台`、`API 文档与接入控制台` 两个 canonical heading；随后最小修改 `web/src/pages/ProfilePage.tsx` 的 API 卡片标题与 CTA 文案完成 GREEN，并重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx`（7 tests 全绿）、`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/App.test.tsx`（2 files / 33 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`。
- 真实 API 方面新增本地回放脚本 `.hermes/local-output/run_profile_capability_canonical_heading_real_api.sh`，最新产物目录 `.hermes/local-output/profile-capability-canonical-heading/20260507-080547` 已验证：`GET /healthz` 返回 `status=ok` 且 `redis=ok`；普通用户 `POST /api/v1/auth/register` 成功，随后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200，且菜单继续暴露 `/api-keys`、`/webhooks`、`/docs`、`/profile` 并未泄露 supplier/admin 路径；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；前端 `/`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/settings`、`/admin/risk`、`/supplier/domains` 的 SPA shell 均返回 200，证明本轮仅收敛 Profile shared-console 桥接命名与合同，未破坏真实注册、角色菜单与关键前端/后端入口。
- 本轮五维评审结论：产品/规格 PASS（首轮评审指出 API Keys / Webhooks / Docs canonical heading 需避免假阳性后，控制器已把 API Keys 回退为 route-stub 导航命中、保留 Webhooks / Docs 的真实 canonical heading 合同，并保持 shared-console 单壳叙事一致）；代码质量 APPROVED（首轮评审提出不应把 Profile 页测试过度耦合到跨页 API Keys 文案，控制器已修正为仅在当前页收敛 API Keys 按钮文案，并把跨页 heading 强校验限制在 Webhooks / Docs 两条本轮能真实对齐的目标页上；最终 diff 保持 focused scope 稳定）；安全/集成 SAFE（静态扫描 live diff 新增行未发现 hardcoded secret / shell / eval / SQL 注入问题，且 route path、权限门控与真实 API 回放均保持健康；API Keys 路由仍复用共享目录真值 `/api-keys`）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅前端静态文案与测试合同调整，不引入新请求、状态分支、部署步骤或运行时开销）。

### 最新执行检查点（2026-05-07，Webhooks shared integration route-stub canonical heading 收敛）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 Phase 4/5 契约与 Webhook 真实投递链路的前提下，仅把 `web/src/pages/WebhooksPage.test.tsx` 中 shared integration loop 两条落点 proof 从低信号的纯 route-stub 存在性检查，收紧为 `webhooks-route-stub-api-keys` / `webhooks-route-stub-docs` + canonical heading 双合同；同时把测试夹具里旧的 `<div data-testid="api-keys-route-stub">开发者 API 接入工作台</div>`、`<div data-testid="api-docs-route-stub">API 文档</div>` 统一升级为 `<section data-testid="..."><h1>...</h1></section>` 结构，继续保持“API Keys → Webhooks → Docs 全部属于同一登录后 shared-console 单壳接入链路”的产品叙事，而不是退回到弱化的占位页文本命中。
- 控制器已按 TDD 先修改 `web/src/pages/WebhooksPage.test.tsx`，让 focused 用例在 RED 阶段显式要求点击 `先配置 API Keys` 后同时出现 `webhooks-route-stub-api-keys` 与 heading `开发者 API 接入工作台`，点击 `查看 API 文档` 后同时出现 `webhooks-route-stub-docs` 与 heading `API 文档`；随后保持运行时代码零改动，仅收敛测试夹具与断言完成 GREEN，并重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx`（10 tests 全绿）、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/WebhooksPage.test.tsx`（2 files / 36 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_webhooks_integration_scope_real_api.sh`，最新产物目录 `.hermes/local-output/webhooks-integration-scope/20260507-074214` 已验证：`GET /healthz` 返回 `status=ok` 且 `redis=ok`；普通用户 `POST /api/v1/auth/register` 成功，`GET /api/v1/auth/menu` 继续暴露 `/api-keys`、`/webhooks`、`/docs` 且不泄露 supplier/admin 路径，`GET /api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；前端 `/`、`/api-keys`、`/webhooks`、`/docs`、`/admin/audit` SPA 壳均返回 200，证明本轮仅收紧 Webhooks shared integration route-stub 合同，未破坏真实注册、角色菜单、Webhook/API Keys/Docs 单壳入口与管理员运营链路。
- 本轮五维评审结论：产品/规格 PASS（diff 仅把 Webhooks integration loop 的两条目标页 proof 从低信号 route-stub 存在性收紧为 route-stub + canonical heading 双合同，更贴近 shared-console 单壳真实用户可见结果，不改变产品叙事）；代码质量 APPROVED（测试夹具统一改用 `<section> + <h1>` 语义结构，断言从纯 testid 存在性升级为语义 heading 合同，没有退化为实现细节快照；一份跨仓库漂移的 delegate 评审已按 fail-closed 丢弃）；安全/集成 SAFE（控制器静态扫描当前 live diff 新增行未发现 hardcoded secret / shell / eval / SQL 注入问题，且本轮仅改测试层；真实 user/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯测试合同与夹具语义收敛，不引入运行时查询、部署步骤或构建复杂度增量）。

### 最新执行检查点（2026-05-07，SupplierOfferings mission-step 中文标签对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 Phase 4/5 契约与供应商供货逻辑的前提下，仅把 `web/src/pages/SupplierOfferingsPage.tsx` 中供应商主任务流仍残留的 3 个英文步骤标签 `Resources` / `Settlement` / `Shared Console` 收敛为中文运行时合同 `资源准备` / `结算反馈` / `统一控制台`，并同步把 `web/src/pages/SupplierOfferingsPage.test.tsx` 的 focused proof 收紧到 mission-flow 区域内对这 3 个新标签的显式断言，继续保持“供应商资源 → 供货规则 → 供应商结算 → API Keys/Webhooks/Docs 全部位于同一登录后深色 shared-console 单壳内”的产品叙事，不引入第二套供应商后台。
- 控制器已按 TDD 先修改 `web/src/pages/SupplierOfferingsPage.test.tsx`，让 focused 用例在 RED 阶段显式要求 mission-flow 内出现 `资源准备` / `结算反馈` / `统一控制台`；首轮 RED 失败证明运行时仍输出英文标签。随后仅最小修改 `web/src/pages/SupplierOfferingsPage.tsx` 的 `missionSteps` 三处 `tag` 文案完成 GREEN，并重新通过 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx`（6 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_supplier_offerings_route_stub_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-offerings-route-stub-real-api/20260507-071915` 已验证：`GET /healthz` 返回 `status=ok` 且 `redis=ok`；普通用户 `POST /api/v1/auth/register` 与 `GET /api/v1/auth/menu` 正常，菜单继续暴露 `/api-keys`、`/webhooks`、`/docs` 且未泄露 supplier/admin 路径；供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/resources/offerings`、`GET /api/v1/supplier/settlements` 正常；管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 正常；前端 `/`、`/supplier/offerings`、`/api-keys`、`/webhooks`、`/docs` SPA 壳均返回 200，证明本轮仅前端中文标签收口，未破坏真实注册、角色菜单、供应商供货页与共享控制台单壳入口。
- 本轮五维评审结论：产品/规格 PASS（diff 仅把 SupplierOfferings mission-step 三个运行时标签从英文收敛为中文合同，并同步补强 focused proof，不改变单一登录后控制台、角色差异菜单与后端契约）；代码质量 APPROVED（运行时代码仅 3 处字面标签替换，测试继续锚定真实 mission-flow 区域与行为入口，没有退化为纯 testid 存在性检查；一次污染性子代理评审漂移到错误仓库后已丢弃，最终以控制器 live diff/测试结果为准）；安全/集成 SAFE（静态扫描当前 live diff 新增行未发现 hardcoded secret / shell / eval / SQL 注入问题，且未改 auth/menu/route/API 语义，真实 user/supplier/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；Semi UI `findDOMNode`、chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅静态文案替换，不增加查询、状态分支或部署复杂度，web/api 容器重建后健康）。

### 最新执行检查点（2026-05-07，SupplierDomains metrics / mission / capability scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 Phase 4/5 接口契约的前提下，仅把 `web/src/pages/SupplierDomainsPage.test.tsx` 中仍残留的 page-global 指标/能力矩阵文本命中，收紧为真实运行时卡片/区块语义锚点 scoped 合同；具体把 4 张顶部指标卡补齐最小 `supplier-domains-metric-*` 锚点，把“供应商主任务流”标题收紧到新的 `supplier-domains-mission-section` 容器，把“控制台能力矩阵”收紧到 `supplier-domains-capability-matrix`，并继续保持“同一登录后 shared-console 深色单壳内完成域名运营 → 资源映射 → 供货编排 → API Keys/Webhooks/Docs 联动”的产品叙事，不引入第二套供应商后台。
- 控制器已按 TDD 先修改 `web/src/pages/SupplierDomainsPage.test.tsx` 让 focused 用例在 RED 阶段显式要求 metric card / mission section / capability matrix 的 scoped 合同；首轮测试/可靠性评审指出同步断言异步加载后的指标值会引入竞态，控制器随后把 `expectMetricCard(...)` 修正为 `await within(card).findByRole(...)` 后再做 GREEN，并最小修改 `web/src/pages/SupplierDomainsPage.tsx`：仅为 `MetricCard` 增加可选 `testId`、为任务区块与能力矩阵容器补齐 2 个最小 `data-testid`。随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx`（3 files / 23 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。
- 真实 API 方面新增本地回放脚本 `.hermes/local-output/run_supplier_domains_metric_scope_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-domains-metric-scope/20260507-064954` 已验证：`GET /healthz` 返回 `status=ok` 且 `redis=ok`；普通用户 `POST /api/v1/auth/register` 与 `GET /api/v1/auth/menu` 正常；供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`POST /api/v1/supplier/resources/domains`、`GET /api/v1/supplier/settlements` 正常，且新建域名已在后续 overview 中可见；管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 正常；前端 `/`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs`、`/admin/audit` SPA 壳返回 200，证明本轮 scoped contract 收紧未破坏真实注册、角色菜单、供应商域名运营链路与共享控制台单壳入口。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 SupplierDomains focused proof 从 page-global 文本收紧为真实 metric/section/card 合同，不改变 shared-console 单壳、角色差异菜单或后端契约）；代码质量 APPROVED（运行时代码仅新增 1 个可选 `testId` 透传与 6 个最小语义锚点，测试仍校验真实标题/值/说明与导航行为，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（静态扫描 live diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且真实 supplier menu/overview/domain-create/settlements 与 admin overview/risk/audit 回放继续健康）；测试/可靠性 PASS（已根据评审修复异步指标竞态，focused Vitest、build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode`、chunk 体积告警仍为历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（新增锚点仅为静态属性，不引入新请求、状态分支或部署负担）。下一前端优先项继续清理 shared-console focused 用例中仍残留的 page-global 指标/说明命中与异步断言脆弱点，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，shared-console 英文残留标签中文化对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录行为、不改菜单权限、不改 Phase 4/5 后端接口契约的前提下，仅收敛 3 处仍残留在运行时壳层里的英文 shared-console 标签，使登录入口、Dashboard 总览与余额页 fallback 文案进一步统一到中文共享控制台术语；具体把 `DashboardPage.tsx` hero tag `Shared Console Entry` 收敛为 `共享控制台入口`，把 `BalancePage.tsx` fallback tag `Shared Console fallback` 收敛为 `共享控制台回退`，并把 `AppSidebar.tsx` 顶部 brand pill 从 `Nexus-Mail · Shared Console` 收敛为 `Nexus-Mail · 统一控制台`，继续保持“注册后进入同一控制台、角色菜单驱动差异能力、无冗余独立后台”的产品叙事不变。
- 控制器已按 TDD 先修改 `DashboardPage.test.tsx`、`BalancePage.test.tsx`、`AppSidebar.test.tsx`，让 focused 用例先显式要求 `共享控制台入口` / `共享控制台回退` / `Nexus-Mail · 统一控制台` 这 3 处新合同，再最小修改对应运行时代码 `DashboardPage.tsx`、`BalancePage.tsx`、`AppSidebar.tsx` 完成 GREEN；随后重新通过 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/pages/BalancePage.test.tsx src/components/AppSidebar.test.tsx`（3 files / 16 tests 全绿）、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/DashboardPage.test.tsx src/pages/BalancePage.test.tsx src/components/AppSidebar.test.tsx`（4 files / 42 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`。真实 API 方面新增本地回放脚本 `.hermes/local-output/run_frontend_shared_console_copy_alignment_real_api.sh`，最新产物目录为 `.hermes/local-output/frontend-shared-console-copy-alignment-real-api/20260507-054951`：其中 `/healthz` 返回 `status=ok` / `redis=ok`，新注册普通用户 `/auth/menu` 暴露 `/`、`/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs`、`/settings`、`/profile`，管理员 `/auth/menu` 暴露 `/admin/users`、`/admin/suppliers`、`/admin/pricing`、`/admin/risk`、`/admin/audit` 等扩展路径，供应商 `/auth/menu` 暴露 `/supplier/domains`、`/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`，并且 `/login`、`/`、`/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs` 的 SPA shell 全部 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 仅收敛 shared-console 壳层英文残留标签到统一中文术语，不改变单一登录后控制台、角色差异菜单或前后端合同）；代码质量 APPROVED（运行时代码仅 3 处字面文案替换，测试同步补上 focused/integration 语义 proof，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（控制器静态扫描当前 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且真实 auth/register/menu/admin overview/supplier menu 回放继续健康，未引入权限或路由漂移）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check`、浏览器 fresh-load `/login` 复核与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅静态文案与测试合同调整，不增加查询、轮询、构建步骤或部署复杂度，同时顺手清理 `.git/info/exclude` 中重复/绝对路径规则，保留 `.hermes/local-output/` 与 `docs/reference-local/` 两条本地忽略真值）。

### 最新执行检查点（2026-05-07，Supplier shared-console menu label / fallback route-stub contract alignment）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改运行时代码、不改注册/菜单权限/后端 Phase 4/5 契约的前提下，仅把 `SupplierOfferingsPage.test.tsx`、`SupplierResourcesPage.test.tsx`、`SupplierSettlementsPage.test.tsx` 中仍残留的旧 shared-console 菜单夹具与低信号 fallback 断言收敛到当前 canonical 合同；具体把 supplier menu fixture 中的 `共享控制台首页` 统一对齐为 `仪表盘`，把 `Webhook` / `Docs` 对齐为运行时真实标题 `Webhook 设置` / `API 文档`，并将 `SupplierOfferingsPage.test.tsx` 的两处 fallback proof 从宽泛 `findByText('控制台总览')` 收紧为 `supplier-offerings-route-stub-shared-home` + heading 双合同，继续保持“供应商资源 / 供货规则 / 结算 / API Keys / Webhooks / Docs 全部仍处于同一登录后共享控制台内”的产品叙事，而不是派生独立供应商后台。
- 控制器已按 TDD 先从 focused 测试合同收紧开始，仅修改 3 个 supplier page test 文件；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx`（3 files / 23 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`。真实 API 方面：`bash .hermes/local-output/run_supplier_offerings_route_stub_real_api.sh` 通过，最新产物目录 `.hermes/local-output/supplier-offerings-route-stub-real-api/20260507-051419`；`bash .hermes/local-output/run_supplier_settlements_metric_scope_real_api.sh` 通过，最新产物目录 `.hermes/local-output/supplier-settlements-metric-scope-real-api/20260507-051424`；`run_supplier_resources_card_scope_real_api.sh` 首次因历史脚本未显式传 `address` 导致 `mailbox_pool_address_key` 唯一键冲突而 fail，控制器按“先定位根因再修复脚本”原则核对 live overview 与仓库约束后，仅修正本地忽略脚本以显式传入唯一 `address`，随后复跑通过，最新产物目录 `.hermes/local-output/supplier-resources-card-scope-real-api/20260507-051911`，确认 supplier `/auth/menu` 暴露 `/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs`，且新建 domain/account/mailbox 均在 live overview 中可见，管理员 `/admin/overview`、`/admin/risk` 继续健康。
- 本轮五维评审结论：产品/规格 PASS（diff 仅把 supplier focused menu fixture 与 fallback route proof 对齐到 shared-console canonical 标题，不改变单壳、角色差异菜单与后端契约）；代码质量 APPROVED（测试改动局部且更贴近 `consoleNavigation.tsx` 真值，`SupplierOfferingsPage` 的 fallback proof 由 page-global 文本收紧为 route-stub + heading 双合同，没有弱化行为覆盖）；安全/集成 SAFE（静态扫描当前 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且未改 auth/menu/route/API 逻辑；真实 supplier/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、前端 build、Go tests、compose config、`git diff --check` 与三组 supplier 真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅测试夹具标题与 fallback 断言收敛，不增加运行时查询或部署复杂度，同时修复了本地忽略的 supplier-resources replay 脚本地址构造问题，降低后续 autonomous real-API 验证误报率）。

### 最新执行检查点（2026-05-07，ConsoleLayout quick-action canonical route contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册、菜单权限、后端 Phase 4/5 契约与 quick-action 计算逻辑的前提下，仅把 `web/src/components/ConsoleLayout.test.tsx` 中仍残留的低信号 quick-action 顺序 / 导航 proof，从 `getAllByRole(...).filter(textContent)` 与点击后 `findAllByText('项目市场')` 这类宽泛命中，收紧为真实 quick-action 按钮级语义锚点 + canonical route-stub 合同；运行时代码仅为 `web/src/layouts/ConsoleLayout.tsx` 的前三个 quick-action 按钮补齐最小 `data-testid`，继续保持 shared-console 单壳、server-menu truth 过滤与角色差异菜单不变，而不是引入独立快捷入口后台或新的导航分叉。
- 控制器已按 TDD 先修改 `web/src/components/ConsoleLayout.test.tsx`，让 focused 用例在 RED 阶段显式要求 `console-layout-quick-action-projects` / `-balance` / `-docs` 与 `console-layout-route-stub-*` 双合同，再最小修改 `web/src/layouts/ConsoleLayout.tsx` 仅为真实可点击 quick-action 按钮补齐 button-level `data-testid`。随后重新通过 `pnpm --dir web exec vitest run src/components/ConsoleLayout.test.tsx`、`pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx src/utils/consoleNavigation.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`；真实 API 方面本轮新增产物目录 `.hermes/local-output/console-layout-quick-actions-real-api/20260507-044201`，其中 `summary.json` 已确认新注册普通用户 `/auth/menu` 暴露 `/`、`/projects`、`/orders`、`/balance`、`/api-keys`、`/webhooks`、`/settings`、`/docs`，管理员菜单继续包含 `/admin/risk`、`/admin/audit` 等角色扩展路径，且 `/`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk` 的 SPA shell 全部 200，`GET /healthz` 返回 `status=ok` / `redis=ok`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 ConsoleLayout quick-action proof 从 page-global 文本/集合过滤收紧为 button-level quick-action + canonical route-stub 合同，不改变 shared-console 单壳、server-menu truth 或角色边界叙事）；代码质量 APPROVED（运行时代码仅新增 1 处 button-level `data-testid` 生成，测试继续保留真实点击导航与顺序断言，没有退化为纯 testid 存在性检查，也避免继续依赖 `textContent` 过滤）；安全/集成 SAFE（控制器静态扫描当前 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且 quick-action 仍复用现有 `visibleQuickActionPaths(menu, currentPath, role)` 逻辑，真实 user/admin `/auth/menu` 与关键页面 shell 回放继续健康）；测试/可靠性 PASS（focused Vitest、AppSidebar+ConsoleLayout 组合 Vitest、App 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode`、chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅补最小按钮锚点与测试合同收敛，不增加运行时查询、轮询或部署复杂度，`web` 重建后健康）。

### 最新执行检查点（2026-05-07，ApiKeys create/list/whitelist scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 API Key 契约与 Phase 4/5 运行逻辑的前提下，仅把 `web/src/pages/ApiKeysPage.test.tsx` 中仍残留的低信号 page-global 列表加载、创建、白名单编辑与 fallback 入口 proof，收紧为真实运行时卡片作用域合同；具体把 `默认密钥` / `已撤销密钥` 的初始就绪 proof 统一收敛到 `api-keys-current-keys-card`，把 `创建新密钥` 的点击目标收敛到 `api-keys-create-card`，把白名单编辑输入与保存动作收敛到 `api-keys-whitelist-editor-card`，并保持 bridge / fallback 仍然位于同一个共享控制台壳内，不引入独立接入后台语义。
- 控制器已按 TDD 先在 `web/src/pages/ApiKeysPage.test.tsx` 写出更严格的 scoped 合同，再最小修改 `web/src/pages/ApiKeysPage.tsx`：仅为创建卡、当前密钥卡、白名单编辑卡补齐 3 个最小 `data-testid`，随后通过 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 观察 RED→GREEN 收敛，并继续通过 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。真实 API 方面新增本地回放脚本 `.hermes/local-output/run_api_keys_card_scope_real_api.sh`（最新产物目录 `.hermes/local-output/api-keys-card-scope-real-api/20260507-041808`），确认 `GET /healthz` 返回 `status=ok` / `redis=ok`，新注册普通用户 `/auth/menu` 暴露 `/api-keys`、`/webhooks`、`/docs`，真实 `POST /api/v1/auth/api-keys` 成功返回 201 与 `plaintext_key`，`PATCH /api/v1/auth/api-keys/:id/whitelist` 返回 200 且白名单更新为 `172.18.0.1`，`POST /api/v1/auth/api-keys/:id/revoke` 返回 200 且状态为 `revoked`，`GET /api/v1/auth/api-keys/audit` 可见 `create` / `update_whitelist` / `revoke`，同时管理员 `/admin/overview`、`/admin/risk`、`/admin/audit` 与供应商 `/supplier/resources/overview`、`/supplier/settlements` 均继续返回 200，前端 `/api-keys`、`/webhooks`、`/docs`、`/profile`、`/settings` SPA shell 全部 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 API Keys 页的 create/list/whitelist/fallback focused proof 从 page-global 命中收紧为真实卡片语义区域合同，shared-console 单壳、角色扩展菜单与“同一登录后控制台内完成接入链路”的产品叙事保持不变）；代码质量 APPROVED（运行时代码仅新增 3 个最小卡片锚点，测试继续保留真实行为断言，包括 whitelist 更新后的行内数据变化、bridge/fallback 跳转与创建后联动，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（控制器静态扫描当前 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且真实 API Key 创建、白名单更新、撤销与审计链路均按 live contract 成功回放）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅补卡片级语义锚点与 scoped proof，不新增运行时查询、轮询或部署复杂度，`web`/`api` 重建后健康）。

### 最新执行检查点（2026-05-07，SupplierResources metric/table scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册、菜单权限、供应商资源接口契约与 Phase 4/5 后端逻辑的前提下，仅把 `web/src/pages/SupplierResourcesPage.test.tsx` 中仍残留的 page-global 指标/表格文本命中，收紧为运行时真实卡片语义锚点 scoped 合同；具体把 `健康账号`、`可用邮箱池` 两张顶部指标卡从整页 `screen.getByText(...)` 收敛为 `supplier-resources-metric-healthy-accounts` / `supplier-resources-metric-available-mailboxes` 作用域断言，并把供应商资源概览数据 proof 从 page-global 命中收敛为 `supplier-resources-domains-table-card`、`supplier-resources-accounts-table-card`、`supplier-resources-mailboxes-table-card` 三个真实表格卡片内断言，继续保持“供应商资源、API Keys、Webhook、Docs 仍属于同一登录后共享控制台，而不是拆成独立供应商后台”的产品叙事。
- 控制器已按 TDD 先在 `web/src/pages/SupplierResourcesPage.test.tsx` 写出更严格的 scoped 合同，再通过 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx` 观察 RED 阶段因缺少 `supplier-resources-metric-healthy-accounts`、`supplier-resources-domains-table-card` 等运行时锚点而失败；随后仅最小修改 `web/src/pages/SupplierResourcesPage.tsx`：为 `MetricCard` 增加可选 `testId` 透传、给 4 张顶部指标卡补齐最小语义锚点，并为域名池 / 第三方邮箱账号池 / 邮箱池表格卡片补齐最小 `data-testid`。GREEN 后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。真实 API 方面新增本地回放脚本 `.hermes/local-output/run_supplier_resources_card_scope_real_api.sh`（最新产物目录 `.hermes/local-output/supplier-resources-card-scope-real-api/20260507-034544`），确认 `GET /healthz` 返回 `status=ok` / `redis=ok`，供应商 `/auth/menu` 暴露 `/supplier/resources`、`/api-keys`、`/webhooks`、`/docs`，`GET /api/v1/supplier/resources/overview` 返回 200，且真实新增 domain/account/mailbox 后再次读取 overview 可见 `created_domain_visible=true`、`created_account_visible=true`、`created_mailbox_visible=true`；管理员 `/api/v1/admin/overview`、`/api/v1/admin/risk` 也继续返回 200，前端 `/supplier/resources` SPA shell 返回 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 SupplierResources focused proof 从 page-global 文本收紧为真实 metric/table card 作用域合同，不改变 shared-console 单壳、角色扩展菜单或供应商资源页定位）；代码质量 APPROVED（运行时代码仅增加最小 `testId` 透传与 7 个卡片级锚点，测试继续保留真实文案与数据断言，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（控制器静态扫描新增 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且未改 auth/menu/route/API 行为；真实 supplier/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、前端 build、Go tests、compose config、容器重建、真实 API 回放全部通过；既有 React `findDOMNode` / act 噪声仍属历史非阻塞问题，本轮未恶化）；性能/运维 PASS（仅补语义锚点与 scoped proof，不引入新的运行时查询、轮询或部署复杂度；`web` 重建后健康，前端 chunk 体积告警维持既有状态）。本轮曾尝试 delegate 专家评审，但子代理错误漂移到无关仓库，已按 fail-closed 规则全部丢弃，最终五维结论仅以控制器在 `/root/nexus-mail` 的 live diff、测试与真实 API 结果为准。

### 最新执行检查点（2026-05-07，shared-console shell wording + canonical route-stub sweep）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 Phase 4/5 接口契约的前提下，仅推进一组小而完整的 shared-console 质量收口——把 `AppSidebar.tsx` 顶部副标题从英文 `Single login, multi-role workspace` 收敛为中文 `单一登录 · 按角色切换工作区`，同时把 `web/src/components/AppSidebar.test.tsx`、`web/src/pages/LoginPage.test.tsx`、`web/src/pages/DashboardPage.test.tsx` 中仍残留的低信号 route stub 占位文案（如 `项目市场页面`、`API Keys 页面`、`Webhook 设置页面`、`API 文档页面`、`设置中心页面`）统一升级为显式 `<section data-testid="..."> + <h1>` 的 canonical heading 合同，继续保持“注册后进入同一套共享控制台、角色只影响菜单与页面，不拆独立后台”的产品叙事。
- 控制器已按 RED→GREEN 先修改 3 个 focused 测试文件与 `AppSidebar` 文案，再重新通过 `pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/pages/LoginPage.test.tsx src/pages/DashboardPage.test.tsx`（3 files / 14 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`；同时执行真实 API 回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`（最新产物目录 `.hermes/local-output/app-login-register-scope/20260507-031153`）与 `.hermes/local-output/run_sidebar_layout_contract_real_api.sh`，确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，新注册普通用户 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均为 200，管理员 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 均为 200，且 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` 的 SPA shell 全部 200；浏览器 fresh-load + vision 复核也确认登录页继续维持深色 shared-console/new-api 风格，无新增重叠、裁切或白底突兀块。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只收敛 shared-console 顶部单一登录文案与 route-stub canonical heading 合同，不引入独立后台语义，登录→注册→API Keys/Webhooks/Docs 的统一壳叙事保持一致）；代码质量 APPROVED（运行时代码仅 1 处中文副标题更新，测试改为更稳定的 route-stub `<section> + <h1>` 合同，没有退化为纯 testid 存在性断言，也减少了 page-global 占位文本耦合）；安全/集成 SAFE（未改 auth/menu/route/API 逻辑与密钥处理，静态扫描新增 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，真实 user/admin API 与 SPA 壳回放保持健康）；测试/可靠性 PASS（focused Vitest、前端 build、Go tests、compose config、容器重建、真实 API 回放、浏览器 fresh-load/vision 复核全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅文案与测试合同收敛，不新增运行时请求、轮询、状态分支或部署复杂度，`web`/`api` 容器重建后均健康）。

### 最新执行检查点（2026-05-07，AdminPricing scoped route + card contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 Phase 4/5 接口契约的前提下，仅把 `web/src/pages/AdminProjectsPage.test.tsx` 中管理员价格策略页残留的低信号全页文本与旧占位落点 proof 收紧为 canonical route-stub + scoped card 合同；其中把风控/审计/API Keys 三条 mission-flow 落点从 `风控中心页面`、`审计日志页面`、`API Keys 页面` 升级为 `admin-pricing-route-stub-risk` / `admin-pricing-route-stub-audit` / `admin-pricing-route-stub-api-keys` + `<h1>` 标题双合同，并把“控制台能力矩阵”“项目列表”“编辑项目”三块 proof 从 page-global 命中改为 `admin-pricing-capability-matrix`、`admin-pricing-project-list-card`、`admin-pricing-project-editor-card` 语义区域内断言，继续保持管理员价格策略页只是统一共享控制台中的运营中枢，而不是独立后台或额外壳层。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/AdminProjectsPage.test.tsx` 令 focused 用例显式要求新的 route-stub / scoped-card 合同，再最小修改 `web/src/pages/AdminProjectsPage.tsx` 仅为真实运行时卡片补齐 3 个最小 `data-testid`；随后重新通过 `pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx`、`pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`docker compose up -d --build web`、`git diff --check`，并执行真实 API 回放脚本 `.hermes/local-output/admin_pricing_real_api.sh`，确认管理员真实菜单包含 `/admin/pricing`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`，`GET /api/v1/admin/projects` / `GET /api/v1/admin/projects/offerings` / `GET /api/v1/admin/risk` / `GET /api/v1/admin/audit?limit=5` / `GET /api/v1/auth/api-keys` / `GET /api/v1/webhooks/endpoints` / `GET /docs` 均成功，且一次 `PATCH /api/v1/admin/projects/:id` 真改真回滚通过；浏览器 fresh-load 复核本轮重建后的登录页结构正常，本 diff 未引入新的可视布局破坏。
- 本轮五维评审结论：产品/规格 PASS（仅把 AdminPricing 页的导航落点与卡片 proof 从低信号整页文本收紧为 canonical heading + scoped card 合同，shared-console 单壳、角色分区与 new-api 风格叙事保持一致，不引入独立后台语义）；代码质量 APPROVED（运行时代码只新增 3 个最小 `data-testid`，测试改为 `within(...)` 作用域断言与 route-stub heading 双合同，避免重复文本误报且未退化为纯 testid 存在性检查）；安全/集成 SAFE（未改鉴权、菜单、路由常量、真实接口或密钥处理，管理员风控/审计/API Keys 目标路径与 `App.tsx`、真实 `/auth/menu`、真实 API 回放保持一致）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode`、`FormApi.setValues()` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅补充语义锚点与测试合同收敛，不引入新请求、轮询、状态分支或部署复杂度；浏览器视觉复核也未发现本轮 diff 造成的新布局回归）。

### 最新执行检查点（2026-05-06，shared-console hero tag 中文化收口）
- 本轮继续按“前端优先”清理 shared-console / new-api 单壳里剩余的低信号英文 `Mission Control` hero 标签：在不改注册/菜单/权限/真实 API 契约、继续保持单一登录后控制台的前提下，仅把管理员风控/审计/用户运营/价格策略/供应商运营，以及供应商供货/资金争议/域名运营/资源运营、个人资料、设置页等 hero tag 收敛为对应中文中枢标签（如 `风控中枢`、`审计中枢`、`用户运营中枢`、`价格策略中枢`、`供应商运营中枢`、`供货规则中枢`、`资金争议中枢`、`域名运营中枢`、`资源运营中枢`、`个人资料中枢`、`设置中枢`），同步为缺少稳定锚点的页面补齐最小 `*-hero-card` testid，并把 focused proof 从脆弱的 `.closest('.semi-card')` 收敛为 hero-card 作用域内的中文标签合同。
- 控制器已按 RED→GREEN 先更新对应 page tests，再最小修改运行时代码；随后重新通过 `pnpm --dir web exec vitest run src/pages/AdminRiskPage.test.tsx src/pages/AdminAuditPage.test.tsx src/pages/AdminUsersPage.test.tsx src/pages/AdminProjectsPage.test.tsx src/pages/AdminSuppliersPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/ProfilePage.test.tsx src/pages/SettingsPage.test.tsx`（11 files / 66 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；同时执行真实 API / SPA 回放脚本 `.hermes/local-output/mission-tag-real-api/run.sh`（最新产物目录 `.hermes/local-output/mission-tag-real-api/20260506-234737`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户注册后 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/wallet/overview`、`/wallet/transactions`、`/projects/inventory`、`/orders/activations`、`/auth/api-keys`、`/webhooks/endpoints` 全部 200；管理员 `/auth/me`、`/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/admin/wallet-users` 全部 200；供应商 `/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements` 全部 200；且 `/`、`/login`、`/profile`、`/settings`、`/projects`、`/orders`、`/balance`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit`、`/admin/pricing`、`/admin/suppliers`、`/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/supplier/domains` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（中文中枢标签与各页中文 heading / shared-console 单壳叙事一致，不引入独立后台语义）、代码质量 APPROVED（首轮评审指出 `.closest('.semi-card')` 过于脆弱后，控制器已补最小 `*-hero-card` 锚点并改为显式区域断言；最终 diff 可维护性通过）、安全/集成 SAFE（未改动 auth/menu/route/API 契约，仅收敛展示标签与 focused proof，真实 API 回放保持健康）、测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` / Form warning、chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（仅前端静态标签与最小语义锚点调整，不引入新运行时分支或部署负担）。下一前端优先项继续清理其余 shared-console focused 用例里残留的低信号 page-global 文案/断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，SupplierSettlements metrics scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改权限、菜单、后端财务/争议接口合同的前提下，仅把 `SupplierSettlementsPage.test.tsx` 首个 mission-control focused 用例中 4 个顶部资金指标（`待结算余额`、`活跃成本模型`、`开放争议`、`当前列表流水`）从低信号 page-global `screen.getByText(...)` 命中，收紧为 `supplier-settlements-metric-*` 语义锚点 scoped 合同；运行时代码仅为 `SupplierSettlementsPage.tsx` 的 `MetricCard` 增加可选 `testId` 透传，并给 4 张指标卡补齐最小 testid，避免后续供应商资金页或别的 shared-console 卡片复用同名指标时让 focused proof 误报，同时继续保持供应商财务闭环与共享接入桥接属于同一控制台壳，而不是派生新的独立后台。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/SupplierSettlementsPage.test.tsx` 让用例显式要求 `supplier-settlements-metric-pending` / `supplier-settlements-metric-active-profiles` / `supplier-settlements-metric-open-disputes` / `supplier-settlements-metric-entry-total` 四个锚点，再最小修改 `web/src/pages/SupplierSettlementsPage.tsx` 为 `MetricCard` 增加 `testId` 并落到 4 张指标卡；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`、`docker compose up -d --build web gateway`，并执行真实 API 回放脚本 `.hermes/local-output/run_supplier_settlements_metric_scope_real_api.sh`（最新产物目录 `.hermes/local-output/supplier-settlements-metric-scope-real-api/20260507-001312`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，新注册普通用户 `/auth/me`、`/auth/menu`、`/wallet/overview`、`/wallet/transactions` 全部 200，供应商 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements`、`/supplier/reports?limit=5` 全部 200，管理员 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 200，且 `/`、`/balance`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/settlements` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（本次 diff 仅把供应商资金页 4 张指标卡的测试合同从 page-global 文本收紧为卡片级语义锚点，不改变单壳叙事、角色路径或财务页面定位）、代码质量 APPROVED（`MetricCard` 仅增加可选 `testId` 透传，改动局部且与 test contract 一一对应，没有引入新的 DOM 包装耦合）、安全/集成 SAFE（未改鉴权、菜单、接口、密钥、结算或争议逻辑；真实 user/supplier/admin API 与 SPA 壳均保持健康）、测试/可靠性 PASS（focused Vitest、前端 build、Go tests、compose config、git diff --check、容器重建与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（仅补最小 testid 与 focused proof 收敛，不增加运行时查询、分支或部署复杂度）。下一前端优先项继续优先清理 `SupplierSettlementsPage.test.tsx` 其余 report/争议表格中的 page-global 金额/文本命中，再推进其它 shared-console focused 脆弱点，之后再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，SupplierSettlements records scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改权限、菜单、后端财务/争议接口合同的前提下，仅把 `SupplierSettlementsPage.test.tsx` 中“真实结算/报表/争议数据已渲染”这一 focused 用例，从 page-global `screen.getByText(...)` 命中收紧为 `supplier-settlements-entries-card`、`supplier-settlements-reports-card`、`supplier-settlements-cost-profiles-card`、`supplier-settlements-disputes-card` 四个语义区域内的 scoped 合同；运行时代码只为 `SupplierSettlementsPage.tsx` 的成本模型、争议单、结算流水三张卡片补齐最小 `data-testid`，避免后续供应商资金页继续因重复金额/备注/争议文本产生误报，同时保持供应商财务闭环、共享接入桥接与单壳控制台叙事不变，不派生独立后台。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/SupplierSettlementsPage.test.tsx` 让用例显式要求 `supplier-settlements-entries-card` / `supplier-settlements-cost-profiles-card` / `supplier-settlements-disputes-card` 等锚点与作用域内断言，再最小修改 `web/src/pages/SupplierSettlementsPage.tsx` 为三张运行时卡片补齐 testid；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行真实 API 回放脚本 `.hermes/local-output/run_supplier_settlements_metric_scope_real_api.sh`（本轮最新产物目录 `.hermes/local-output/supplier-settlements-metric-scope-real-api/20260507-015114`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，新注册普通用户 `/auth/me`、`/auth/menu`、`/wallet/overview`、`/wallet/transactions` 全部 200，供应商 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements`、`/supplier/reports?limit=5` 全部 200，管理员 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 200，且 `/`、`/balance`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/settlements` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 SupplierSettlements 数据展示 proof 从 page-global 文本收紧为报表/流水/争议/成本四个真实卡片作用域合同，shared-console 单壳、角色路径与财务页面定位不变）、代码质量 APPROVED（新增锚点均落在真实运行时卡片层，避免 `.closest('.semi-card')` 一类结构耦合，也没有把 proof 弱化为纯 testid 存在性）、安全/集成 SAFE（未改 auth/menu/route/API 语义；静态扫描无 hardcoded secret / shell / eval / SQL 注入问题，真实 user/supplier/admin API 回放保持健康）、测试/可靠性 PASS（focused Vitest、supplier pair Vitest、前端 build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（仅补最小语义锚点并收紧测试合同，不增加运行时查询、分支或部署复杂度）。下一前端优先项继续优先清理 `SupplierResourcesPage.test.tsx` / `SupplierSettlementsPage.test.tsx` 其余残留的 page-global copy/金额命中，再推进其它 shared-console focused 脆弱点，之后再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，SupplierSettlements form scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改权限、菜单、后端财务/争议接口合同的前提下，仅把 `SupplierSettlementsPage.test.tsx` 中“提交成本模型 / 提交争议并刷新数据”这一 focused 用例，从 page-global 表单控件与动作按钮命中收紧为 `supplier-settlements-cost-profile-form`、`supplier-settlements-dispute-form` 两个真实表单语义区域内的 scoped 合同；运行时代码仅为 `SupplierSettlementsPage.tsx` 的成本模型表单与争议表单补齐最小 `data-testid`，继续保持供应商财务闭环、共享接入桥接与单壳控制台叙事不变，不派生独立后台。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/SupplierSettlementsPage.test.tsx`，让用例显式要求 scoped form contract，再最小修改 `web/src/pages/SupplierSettlementsPage.tsx` 为两个真实表单补齐语义锚点；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。真实 API 方面重跑本地回放脚本 `.hermes/local-output/run_supplier_settlements_metric_scope_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-settlements-metric-scope-real-api/20260507-062041`：`GET /healthz` 返回 `status=ok` / `redis=ok`，新注册普通用户 `/auth/menu`、`/auth/me`、`/wallet/overview` 返回 200，供应商 `/supplier/settlements`、`/supplier/reports?limit=5` 返回 200，管理员 `/admin/audit?limit=5` 返回 200，且 `/balance`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/settlements` 的 SPA shell 均保持 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 仅把 SupplierSettlements 表单提交 proof 从 page-global 控件命中收紧为真实成本模型/争议表单作用域合同，不改变 shared-console 单壳、角色路径与财务页面定位）；代码质量 APPROVED（运行时代码仅新增两个最小表单级 `data-testid`，测试继续保留真实输入、提交、reload 与 toast 行为断言，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（控制器静态扫描当前 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且未改鉴权、菜单、接口或资金逻辑；真实 user/supplier/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、supplier pair Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check`、容器重建与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍为历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅补最小表单语义锚点并收紧测试合同，不增加运行时查询、分支或部署复杂度）。下一前端优先项继续优先清理其余 shared-console focused 用例里残留的 page-global 表单/按钮命中，再推进下一个前端统一控制台切片，之后再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，route stub canonical heading 对齐收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改运行时代码、不改注册/菜单/权限/后端 API 契约的前提下，仅把 `SettingsPage.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 中残留的旧 route stub 占位文案（如 `项目市场页面`、`API Keys 页面`、`Webhook 设置页面`、`API 文档页面`）统一收敛为与共享控制台 canonical heading 一致的 route stub / heading 合同；其中 `SettingsPage.test.tsx` 进一步把首页、项目市场、订单中心、API Keys、API 文档、个人资料、Webhook、供应商资源、供应商结算 9 个测试路由桩全部升级为显式 `settings-route-stub-*` 语义锚点 + `<h1>` 标题，`ApiDocsPage.test.tsx` 与 `WebhooksPage.test.tsx` 则把 API Keys / Webhook / Docs 落点文案对齐为 `开发者 API 接入工作台`、`Webhook 设置`、`API 文档`，避免继续依赖旧占位页文案或 page-global 弱文本匹配，同时继续保持 shared-console/new-api 单壳叙事，不派生独立后台。
- 控制器已按 RED→GREEN 先修改 3 个 focused 测试文件，再重新通过 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx`（3 files / 18 tests 全绿）与 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/SettingsPage.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx`（4 files / 44 tests 全绿）；随后完成 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`。真实 API 方面新增本地复放脚本 `.hermes/local-output/run_front_route_heading_stub_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/front-route-heading-stub-scope/20260507-003541`：`GET /healthz` 返回 200 且 `status=ok`、`redis=ok`；新注册普通用户 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 200；管理员 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 200；供应商 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 全部 200；且前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs` 的 SPA shell 均返回 200。该产物摘要同时确认 3 个角色菜单路径仍与 shared-console 单壳模型一致：普通用户不泄露 supplier/admin 路由，supplier/admin 继续在同一壳内扩展。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 3 个 focused 测试里的旧 route stub 占位文案收敛为 canonical heading 合同，shared-console 单壳、角色扩展菜单与 new-api 风格叙事保持不变）、代码质量 APPROVED（改动局限于测试层，`SettingsPage.test.tsx` 通过最小语义锚点显式化 9 个 route stub，避免再依赖 page-global 占位文案；`ApiDocsPage.test.tsx` / `WebhooksPage.test.tsx` 仅把落点文案对齐真实标题，没有引入新结构耦合）、安全/集成 SAFE（控制器静态扫描新增 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题；真实 user/admin/supplier API 回放与 `/auth/menu` 路径摘要均证明权限、菜单、接口合同未漂移）、测试/可靠性 PASS（focused Vitest、App 组合 Vitest、build、Go tests、compose config、git diff --check 全部通过，并新增本地 front-route-heading-stub-scope 真实 API 回放工件确认 `/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/supplier/domains` 壳路由均保持 200 与既有菜单真值）；性能/运维 PASS（仅前端测试合同收敛，不增加运行时查询或部署复杂度；历史 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警维持非阻塞状态）。
### 最新执行检查点（2026-05-07，Login shared-console wording alignment）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、菜单权限、角色路由与后端 Phase 4/5 能力的前提下，仅把登录页登录/注册主标题、左侧 brand tag、hero signals 与注册后接入 runway 的残留英文/中英混杂文案收敛为统一中文 shared-console 术语（如 `登录并进入统一控制台`、`创建账号并进入统一控制台`、`统一登录入口`、`权限菜单真值`、`真实 API 联调`、`注册后配置 API Keys`、`Webhook 回调联调`、`文档回放校验`），继续保持“注册成功后进入同一套共享控制台，而不是独立新手页或多后台分叉”的产品叙事，并让 Login focused proof / App 联动断言同步对齐新合同。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/LoginPage.test.tsx` 与 `web/src/App.test.tsx`，让 focused / integrated 用例先显式要求新的中文 heading、hero tag 与 runway copy，再最小更新 `web/src/pages/LoginPage.tsx` 的运行时文案；随后重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行真实 API 回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`（最新产物目录 `.hermes/local-output/app-login-register-scope/20260507-012911`），确认 `GET /healthz` 返回 `status=ok` / `redis=ok`，新注册普通用户可继续成功访问 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，管理员 `/admin/overview`、`/admin/risk`、`/admin/audit` 也保持 200，且 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（登录/注册入口与注册后接入跑道统一收敛到 shared-console 中文术语，单壳叙事更一致，没有引入独立后台语义）、代码质量 APPROVED（diff 仅涉及 3 个前端文件的文案与合同对齐，测试仍保留行为级 register-mode 切换与注册提交流程，没有弱化为纯静态文本存在性断言）、安全/集成 SAFE（静态扫描无 hardcoded secret / shell / eval / SQL 注入问题，且 auth/menu/route/API 契约未漂移，真实 user/admin API 回放继续健康）、测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、git diff --check、容器重建与真实 API 回放全部通过；浏览器 fresh-load 快照已确认中文文案实际生效）、性能/运维 PASS（仅前端静态文案与测试合同调整，不增加新查询、分支或部署复杂度；浏览器视觉检查提示 auth card 低对比度仍是既有非阻塞债务，本轮未恶化）。


### 最新执行检查点（2026-05-07，Projects / Orders shared-console contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单/权限/后端 Phase 4/5 业务逻辑的前提下，仅把 `web/src/pages/ProjectsPage.test.tsx`、`web/src/pages/OrdersPage.test.tsx` 中残留的低信号 route stub 占位文案（如 `订单中心页面`、`项目市场页面`、`API Keys 页面`、`控制台总览页面`）统一收敛为 route-stub `data-testid` + canonical heading 双合同，并把 `OrdersPage` 的“履约说明”区域补齐最小父级语义锚点 `orders-fulfillment-guidance-card`，让 focused proof 改为在该作用域内断言 `READY 后完成订单`、`异常时看结果面板`、`订单为空时的下一步` 三个真实用户可见 heading，同时继续保持“采购 → 履约 → API 接入”全部属于同一登录后共享控制台，而不是拆成独立订单后台或接入后台。
- 控制器已按 RED→GREEN 先收紧 `ProjectsPage.test.tsx` / `OrdersPage.test.tsx` 的目标合同，再最小修改 `web/src/pages/OrdersPage.tsx` 仅补父级 guidance anchor；期间首轮代码质量评审指出不应新增 3 个子卡片 testid 并把测试弱化为纯 testid 存在性断言，控制器已同轮回退这些子级锚点，改为保留单个父级 `orders-fulfillment-guidance-card` 并在其内部断言 3 个真实 heading。随后重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`（2 files / 21 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`。真实 API 方面新增本地复放脚本 `.hermes/local-output/run_projects_orders_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/projects-orders-scope/20260507-021702`：`GET /healthz` 返回 200 且 `status=ok`、`redis=ok`；新注册普通用户 `/auth/menu` 返回 `/projects`、`/orders`、`/api-keys` 等 shared 路径；`/projects/inventory` 返回 200 且非空；`/orders/activations` 返回 200 并观测到当前真实合同为 `{"items": null}`；`/auth/api-keys` 返回 200；前端 `/`、`/projects`、`/orders` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 仅把 Projects / Orders focused 导航落点 proof 从旧占位文案收敛为 canonical heading 合同，并把订单说明 proof 收紧到父级 guidance card 作用域，没有引入独立后台语义，shared-console 单壳叙事保持一致）、代码质量 APPROVED（最终 runtime 仅保留一个最小父级语义锚点；测试继续验证真实 heading 与导航结果，没有退化为纯 testid 存在性断言）、安全/集成 SAFE（未改鉴权、菜单、角色边界或接口路径；真实 API 回放也如实记录 `/orders/activations` 当前 `items` 可能为 `null` 的观测合同，未夸大为前端尚未验证的数组语义）、测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（仅测试合同收敛与单个父级语义锚点补充，不增加运行时查询、状态或部署复杂度；本轮同时把真实 API 观测到的 `orders.items=null` 明确沉淀为后续后端/前端契约收口线索）。

### 最新执行检查点（2026-05-07，Profile route-stub canonical heading tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单/权限/后端 API 契约、也不引入新的运行时代码分叉的前提下，仅把 `web/src/pages/ProfilePage.test.tsx` 中残留的低信号 route stub 占位文案从纯 `<div>` 文本收敛为带 `data-testid` + `<h1>` 的 canonical route-stub 合同；其中项目市场、订单中心、开发者 API 接入工作台、Webhook 设置、API 文档、设置中心、域名池运营中枢、风控中心与控制台总览 9 个落点全部升级为语义化 heading，继续保持“个人资料页只是同一共享控制台里的身份中枢与角色桥接，而非独立后台入口”的产品叙事。
- 控制器已按测试先行执行最小垂直切片：先修改 `web/src/pages/ProfilePage.test.tsx` 的路由桩结构，再复跑 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx`（7 tests 全绿）；随后完成 `pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；真实 API 方面复用并重跑 `.hermes/local-output/run_profile_route_stub_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/profile-route-stub-scope/20260507-005831`，其中 `summary.json` 已确认 `GET /healthz` 返回 `status=ok` / `redis=ok`，新注册普通用户菜单包含 `/`、`/projects`、`/orders`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`，同时 `/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/supplier/domains` 前端壳路由全部返回 200。
- 本轮五维评审结论：产品/规格 PASS（controller-side 复核发现本仓现有多处 focused route stub 本就使用 `开发者 API 接入工作台`、`Webhook 设置`、`API 文档`、`域名池运营中枢`、`控制台总览` 这组 heading 合同，当前 diff 与既有单壳测试真值一致，并未新引入 heading 体系漂移）、代码质量 APPROVED（仅单文件测试 harness 从纯文本 `<div>` 收敛为 `<section>` + `<h1>`，可访问结构和 route-stub 语义更清晰；后续若需要可再统一抽 helper，但当前不构成阻塞）、安全/集成 SAFE（静态扫描新增 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且本轮为测试层变更；真实 API 回放继续证明 auth/menu/route 合同未漂移）、测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 告警仍为历史非阻塞噪声）、性能/运维 PASS（仅测试桩结构升级，不增加任何运行时分支、查询或部署成本）。

### 最新执行检查点（2026-05-06，Balance heading alignment / docs bridge wording sync）
- 本轮继续按“前端优先”收敛 shared-console/new-api 单壳中的低信号英文 hero 文案：在不改注册/菜单/权限/真实 API 契约、继续保持单一登录后控制台的前提下，仅把 `BalancePage.tsx` hero 标签从英文 `Finance Mission Control` 收敛为中文 `余额任务总览`，同步把 `BalancePage.test.tsx` 的 hero focused 合同与用例命名改为与当前页面术语一致，并把 `ApiDocsPage.tsx` 中返回余额页的 bridge 描述从旧的 `Finance Mission Control` 指代同步为 `余额任务总览`，再由 `ApiDocsPage.test.tsx` 补齐菜单前置条件与更新后的 bridge 文案断言，避免余额页与文档桥接对同一工作台继续使用中英混杂旧称谓。
- 控制器已按最小垂直切片完成验证：先修正 Balance hero / docs bridge 文案，再重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/pages/BalancePage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/BalancePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；同时执行真实 API 回放脚本 `.hermes/local-output/balance-heading-alignment-real-api/run.sh`（最新产物目录 `.hermes/local-output/balance-heading-alignment-real-api/20260506-231331`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 后 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/wallet/overview`、`/wallet/transactions` 全部 200，管理员 `POST /api/v1/auth/login` 后 `/auth/me`、`/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 200，供应商 `POST /api/v1/auth/login` 后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 全部 200，且 `/`、`/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（仅把余额页 hero 与 docs→balance bridge 对同一工作台的命名收敛到中文 `余额任务总览`，不改变 shared-console/new-api 单壳叙事）、代码质量 APPROVED（补齐了 `ApiDocsPage` 对应 bridge 文案断言并同步测试命名，无新的命名漂移或跨页称谓分叉）、安全/集成 SAFE（未改动任何权限判断、菜单 gating、路由常量或前后端契约，真实 user/admin/supplier API 与 SPA 路由均保持健康）、测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（纯静态文案与测试合同对齐，不引入新的运行时分支、构建配置或部署负担）。下一前端优先项继续清理其余 shared-console focused 用例里残留的低信号英文 mission-control 标签 / page-global 文案断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Balance scoped hero / capability contract tightening）
- 本轮继续按“前端优先”收敛 `BalancePage.test.tsx` 中仍残留的低信号 page-global 页面文案断言：在不改注册/菜单/权限/真实 API 契约、继续保持 new-api 风格单一登录后控制台的前提下，仅把余额页首个 shared-console focused 用例从 page-global `资金任务流` / `控制台能力矩阵` / `角色差异仍共用单壳` / `资金观察与售后同层` / `本次会话新提交的争议` 等散落文本命中，收紧为 `balance-hero-card`、`balance-mission-cards`、`balance-capability-matrix` 与新增 `balance-session-disputes-card` 语义区域内的 scoped 合同，同时为运行时代码最小补充 `balance-hero-card` 与 `balance-session-disputes-card` testid，避免后续 shared-console 文案润色或同页重复文案导致 focused 误报。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/BalancePage.test.tsx`，让首个 focused 用例转向 hero / mission / capability / disputes 四个语义区域，再以最小运行时代码为 `web/src/pages/BalancePage.tsx` 补齐 `balance-hero-card` 与 `balance-session-disputes-card`；随后重新通过 `pnpm --dir web exec vitest run src/pages/BalancePage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/BalancePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`，并执行真实 API 回放脚本 `.hermes/local-output/balance-scoped-contract-real-api/run.sh`（最新产物目录 `.hermes/local-output/balance-scoped-contract-real-api/20260506-224148`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，新注册普通用户 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/wallet/overview`、`/wallet/transactions` 全部 200，管理员 `POST /api/v1/auth/login` 后 `/auth/me`、`/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 200，供应商 `POST /api/v1/auth/login` 后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 全部 200，且 `/`、`/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（仅把 Balance 首屏 proof 从 page-global 文本收敛为更贴近 shared-console 运行时结构的 hero / mission / capability / disputes 区域合同，不改变单壳资金页叙事）、代码质量 APPROVED（改动局限于单个 focused 文件与两个最小 testid，保留用户层 heading 与 Banner 文本合同，没有退化为纯 testid 存在性断言）、安全/集成 SAFE（无新权限、菜单、鉴权、路由或接口改动，真实 user/admin/supplier 菜单与资金/运营接口保持健康）、测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（仅前端语义锚点与测试合同收敛，不引入新的运行时路径或部署负担）。下一前端优先项继续清理其余 shared-console focused 用例中仍残留的低信号 page-global 文案 / 指标断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，AdminUsers/AdminRisk route-stub + canonical heading contract tightening）
- 本轮继续按“前端优先”收敛管理员共享控制台 focused 用例中仍残留的低信号 page-global 路由落点断言：在不改运行时代码、不改注册/菜单/权限/真实 API 契约、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `AdminUsersPage.shared-console.test.tsx` 与 `AdminRiskPage.test.tsx` 中管理员主任务流 / fallback 导航 proof 从 `风控中心页面`、`审计日志页面`、`资金工作台页面`、`API Keys 页面`、`控制台总览` 这类历史占位或 page-global 文本，统一收紧为 `admin-users-route-stub-*` / `admin-risk-route-stub-*` 语义锚点 + canonical heading 双合同，并把 `ADMIN_USERS_ROUTE` 落点明确对齐真实共享控制台标题 `用户管理`，避免继续把旧占位文案固化进 focused proof。
- 控制器已按最小垂直切片完成验证：先复核 live diff 与路由标题真值，再通过 `pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx src/pages/AdminRiskPage.test.tsx`、`pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx src/pages/AdminRiskPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；同时执行真实 API 回放脚本 `.hermes/local-output/run_admin_users_admin_risk_route_proof_real_api.sh`（最新产物目录 `.hermes/local-output/admin-users-admin-risk-route-proof/20260506-222107`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，新注册普通用户 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 200，管理员 `POST /api/v1/auth/login` 后 `/auth/me`、`/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 200，且 `/admin/users`、`/admin/risk`、`/api-keys`、`/` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（仅把管理员资金/风控两页的导航落点 proof 从旧 page-global 文本收敛为更贴近 shared-console 运行时结构的 route-stub + canonical heading 合同，并把“资金工作台”落点准确对齐实际页面标题 `用户管理`）、代码质量 APPROVED（改动局限于两份 focused 测试文件，保留点击行为断言并新增 route-stub + heading 双合同，没有退化为文本弱断言）、安全/集成 SAFE（无新权限、菜单、鉴权或接口改动，canonical heading 与 `consoleNavigation` 真值及真实 API 菜单保持一致）、测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` / Form warning、chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（纯测试合同收敛，不引入运行时代码路径或部署开销变化）。下一前端优先项继续清理其余 shared-console focused 用例里残留的低信号 page-global 落点 / 标题断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，SupplierOfferings route-stub + canonical heading alignment）
- 本轮继续按“前端优先”收敛 shared-console focused 用例里残留的低信号 page-global 路由落点断言：在不改运行时代码、不改注册/菜单/权限/真实 API 契约、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `SupplierOfferingsPage.test.tsx` 中供应商主任务流三条导航 proof 从 `供应商资源页` / `供应商结算页` / `API Keys 页面` 这类历史 route stub 文本，统一收紧为 `supplier-offerings-route-stub-*` testid + canonical heading 双合同，并把测试内各个路由桩标题对齐真实共享控制台页面标题（如 `供应商资源`、`供应商结算`、`开发者 API 接入工作台`），避免继续把低信号占位文案固化进 focused proof。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/SupplierOfferingsPage.test.tsx`：新增 route-stub 语义锚点，并把 mission-flow 导航用例改为 `findByTestId(...) + heading` 双合同；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx`、`pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`，并执行真实 API 回放脚本 `.hermes/local-output/run_supplier_offerings_route_stub_real_api.sh`（最新产物目录 `.hermes/local-output/supplier-offerings-route-stub-real-api/20260506-220206`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`；普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/menu` 保持只暴露 shared shell 路径；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements` 返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `http://127.0.0.1:5173/`、`/supplier/offerings`、`/api-keys`、`/webhooks`、`/docs` 均返回 SPA 壳 200。
- 本轮五维评审以 controller-side live diff 与实测为准：产品/规格 PASS（仅把 SupplierOfferings 导航落点 proof 从旧 route 文本收敛为更贴近 shared-console 运行时结构的 route-stub + canonical heading 合同）、代码质量 APPROVED（改动局限于单个 focused 文件，路由桩命名与实际目标页标题对齐）、安全/集成 SAFE（未引入新权限、菜单、接口或凭据处理变化，供应商/管理员真实菜单与接口健康）、测试/可靠性 PASS（focused Vitest、grouped App+SupplierOfferings Vitest、build、Go tests、compose config、真实 API 回放全部通过；既有 Semi UI `findDOMNode`、React `act(...)` 与 chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（仅测试合同与路由桩语义收敛，不引入运行时开销）。下一前端优先项继续清理其余 focused 用例中仍残留的旧 route stub / page-global 落点断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，ApiDocs semantic heading contract tightening）
- 本轮继续按“前端优先”收敛 shared-console focused 用例中仍残留的装饰性英文 docs 标题依赖：在不改注册/菜单/权限/真实 API 契约、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `ApiDocsPage.tsx` 三种角色视图的装饰性 `Docs Mission Control` 标题统一收敛为语义化中文页面标题 `API 文档与接入控制台`，并同步把 `ApiDocsPage.test.tsx` 与 `App.test.tsx` 中 docs fallback / bridge / loop proof 从低信号装饰性标题依赖收紧为语义 heading + bridge/loop 容器 + iframe surface 合同，确保单壳 docs 页面在用户/供应商/管理员视角下都以共享控制台语义呈现，而不是继续依赖易漂移的英文视觉标签。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/ApiDocsPage.test.tsx` 与 `web/src/App.test.tsx`，通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/App.test.tsx` 观察到 RED 阶段明确报错 `Unable to find an accessible element with the role "heading" and name "API 文档与接入控制台"`，随后仅最小修改 `web/src/pages/ApiDocsPage.tsx` 的角色标题文案，再重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；并执行真实 API 回放脚本 `.hermes/local-output/api-docs-semantic-heading-real-api/run.sh`（最新产物目录 `.hermes/local-output/api-docs-semantic-heading-real-api/20260506-214129`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me`、`/auth/menu`、`/auth/api-keys`、`/webhooks/endpoints` 返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `http://127.0.0.1:5173/`、`/docs`、`/api-keys`、`/webhooks` 均返回 SPA 壳 200。
- 本轮五维评审由控制器基于 live diff 与实测直接完成并作为最终准入：产品/规格 PASS（确认本轮仅把 docs 页面标题从装饰性英文标签收敛为共享控制台语义中文标题，并补强 fallback/bridge/loop 合同，没有改变单壳权限叙事）、代码质量 APPROVED（改动仅 3 个文件、13 行新增/6 行删除，测试命名与断言范围与运行时语义一致）、安全/集成 SAFE（无新凭据/权限/路由漂移，真实 API 菜单与 docs/bridge 容器合同保持成立）、测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 仍为历史非阻塞噪声）、性能/运维 PASS（仅前端静态文案与测试合同收敛，不引入新运行时开销；前端 chunk 体积告警维持既有状态）。期间 3 个 delegate reviewer 明显漂移到其他仓库/无关 diff，已按 fail-closed 规则全部丢弃，未用于本仓库决策。下一前端优先项继续清理 `WebhooksPage.test.tsx`、`App.test.tsx` 中残留的低信号 page-global/长说明断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，AdminSuppliers / SupplierDomains / ApiKeys route heading alignment sweep）
- 本轮继续按“前端优先”收敛 shared-console focused 用例里残留的低信号 page-global route stub 文案：在不改运行时代码、不动注册/菜单/权限/真实 API 合同、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `AdminSuppliersPage.test.tsx`、`SupplierDomainsPage.test.tsx`、`ApiKeysPage.test.tsx` 中导航落点 proof 从 `结算与争议页面`、`风控中心页面`、`审计日志页面`、`供应商资源页`、`供应商供货页`、`供应商结算页`、`API Keys 页面`、`Webhook 设置页面`、`API 文档页面`、`项目市场页面` 等历史占位/低信号 stub 文本，统一收紧为 route-stub `data-testid` + canonical heading 双合同，并把 shared-home fallback 标题继续对齐运行时 dashboard 真值 `控制台总览`。
- 控制器已按 RED→GREEN 先修改三份 focused 用例中的路由桩与 heading proof，再重新通过 `pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/ApiKeysPage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/AdminSuppliersPage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/ApiKeysPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；并执行真实 API 回放脚本 `.hermes/local-output/front-route-heading-alignment-real-api/run.sh`（最新产物目录 `.hermes/local-output/front-route-heading-alignment-real-api/20260506-211614`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me`、`/auth/menu`、`/auth/api-keys` 返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview`、`/admin/wallet-users`、`/admin/disputes?limit=5`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/supplier/resources/overview`、`/supplier/settlements` 返回 200；前端 `http://127.0.0.1:5173/`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/domains`、`/admin/suppliers` 均返回 SPA 壳 200。
- 本轮五维评审以 controller-side live diff 与实测为准：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；其中一次 delegate 安全/集成评审明显漂移到无关仓库，已按 fail-closed 规则丢弃，改由控制器基于 live diff、静态安全扫描、focused/grouped Vitest、build、Go tests、compose config 与真实 API 回放完成 repo-scoped 五维复核。评审一致确认当前 diff 仅把三组 focused 导航/回退 proof 从历史占位 route 文本收敛为更贴近 shared-console 运行时结构的 route-stub + heading 合同，不改变权限、导航、菜单或真实 API 契约；非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警。下一前端优先项继续清理其余 focused 用例里仍残留的低信号 page-global route/documentation 文本断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Balance route-stub + shared-shell heading contract tightening）
- 本轮继续按“前端优先”收敛 `BalancePage.test.tsx` 中仍残留的低信号 page-global 路由落点断言：在不改运行时代码、不改注册/菜单/权限/真实 API 合同、继续保持 new-api 风格单一登录后控制台的前提下，仅把余额页 5 条共享控制台导航 proof（项目市场、订单中心、API Keys、Webhook、Docs）从 `项目市场页面` / `订单中心页面` / `API Keys 页面` / `Webhook 设置页面` / `API 文档页面` 这类 page-global stub 文本，收紧为显式 `balance-route-stub-*` testid + shared-shell canonical heading 双合同；其中 heading 对齐 `resolveRouteTitle()` / `consoleNavigation.tsx` 中的壳内路由标题真值（如 `Webhook 设置`、`API 文档`），避免继续把历史占位文案固化进 focused proof，同时不把页面内部更长 hero 标题与共享壳 breadcrumb/header 标题混为一谈。
- 控制器已按 RED→GREEN 先修改 `BalancePage.test.tsx` 的 focused 用例与路由桩，再重新通过 `pnpm --dir web exec vitest run src/pages/BalancePage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/BalancePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；并执行真实 API 回放脚本 `.hermes/local-output/balance-route-stub-real-api/run.sh`（最新产物目录 `.hermes/local-output/balance-route-stub-real-api/20260506-205004`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me`、`/auth/menu`、`/wallet/overview`、`/wallet/transactions` 返回 200，且前端 `http://127.0.0.1:5173/`、`/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs` 均返回 SPA 壳 200。
- 本轮五维评审以 controller-side 事实为准：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；其中首轮产品评审一度把 Webhooks/Docs 的页面 hero 标题误当成 shared-shell route title，控制器已据 `consoleNavigation.tsx`、`resolveRouteTitle()`、`ConsoleLayout.tsx` 与 grouped App/Balance Vitest 复核，确认本轮合同应对齐共享壳路由标题而非页面 hero copy。评审一致确认当前 diff 仅把 Balance focused 导航落点 proof 从低信号 page-global 文本收敛为更贴近 shared-console 运行时结构的 route-stub + heading 合同，不改变权限、导航、菜单、真实 API 或部署流程；非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警。下一前端优先项继续清理其余 focused 用例中仍残留的 page-global 落点/说明断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Webhooks/AppSidebar shared-home route-stub heading alignment）
- 本轮继续按“前端优先”收敛 shared-console focused 用例里残留的旧 shared-home / page-global 落点文案：在不改运行时代码、不动注册/菜单/权限/真实 API 契约、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `WebhooksPage.test.tsx` 中空 endpoint fallback 的共享首页 stub 从旧文案 `共享控制台首页` 收紧为 `shared-console-home-route-stub` + `控制台总览` heading 双合同，并把 `AppSidebar.test.tsx` 中点击 `Webhook 设置` 后的落点 proof 从 page-global `Webhook 页面` 文本收紧为 `app-sidebar-route-stub-webhooks` + `Webhook 设置` heading 双合同，避免继续把历史占位文案和低信号整页文本固化进 focused proof。
- 控制器已按最小垂直切片完成验证：`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/components/AppSidebar.test.tsx` 通过（2 files / 15 tests）；随后 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 均通过，并执行真实 API 回放脚本 `.hermes/local-output/webhooks-home-stub-real-api/run.sh`（最新产物目录 `.hermes/local-output/webhooks-home-stub-real-api/20260506-203041`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me`、`/auth/menu`、`/webhooks/endpoints` 返回 200，前端 `http://127.0.0.1:5173/webhooks` 返回 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅把 Webhooks/AppSidebar 两个 focused 导航/回退 proof 从旧 shared-home 文案或低信号 page-global 文本收敛为更贴近运行时结构的 route-stub + heading 合同，不改变权限、导航、菜单、真实 API 或部署流程。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理其余 shared-console focused 用例中残留的旧 shared-home / page-global 落点断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，route-stub shared-home canonical heading follow-up）
- 本轮继续按“前端优先”收敛 shared-console focused 用例里仍残留的低信号 shared-home stub 文案：在不改运行时代码、不动注册/菜单/权限/真实 API 合同、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `AdminProjectsPage.test.tsx`、`SupplierDomainsPage.test.tsx`、`BalancePage.test.tsx` 中 fallback / shared-home route stub 的旧文案 `共享控制台首页` 统一对齐为 canonical dashboard heading `控制台总览`，并在管理员价格页、供应商域名页把回退落点 proof 收紧为 route-stub testid + heading 双合同，避免继续把历史首页占位文案固化进 focused proof。
- 控制器已按最小垂直切片完成验证：`pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/BalancePage.test.tsx` 通过（3 files / 18 tests）；随后 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 均通过，并执行真实 API 回放脚本 `.hermes/local-output/front-route-stub-heading-real-api/run.sh`（最新产物目录 `.hermes/local-output/front-route-stub-heading-real-api/20260506-200051`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201 且 `GET /api/v1/auth/me`、`/auth/menu`、`/wallet/overview`、`/wallet/transactions` 返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/risk`、`/admin/audit?limit=5`、`/admin/projects` 返回 200；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/supplier/resources/overview` 返回 200；前端 `http://127.0.0.1:5173/`、`/balance`、`/supplier/domains`、`/admin/pricing` 均返回 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅把 3 个 focused 测试中的 shared-home route stub / fallback 证明从旧文案 `共享控制台首页` 收敛到与运行时 dashboard 真值一致的 `控制台总览`，未改变权限、导航、真实 API 或部署流程。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、局部 `FormApi.setValues()` 与前端 chunk 体积告警；下一前端优先项继续清理其余残留旧 shared-home stub / page-global route 落点文本，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，shared-home canonical title alignment sweep）
- 本轮继续按“前端优先”收敛 shared-console focused 用例里残留的共享首页旧文案：在不改注册/登录真实 API 契约、不改权限/菜单/后端逻辑、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `ProtectedRoute.test.tsx`、`ProfilePage.test.tsx`、`ApiKeysPage.test.tsx`、`ApiDocsPage.test.tsx`、`LoginPage.test.tsx`、`SettingsPage.test.tsx`、`AdminUsersPage.shared-console.test.tsx`、`AdminRiskPage.test.tsx`、`SupplierOfferingsPage.test.tsx` 中 shared-home route stub / fallback 断言的标题，从旧的 `共享控制台首页` 统一对齐为运行时 canonical dashboard heading `控制台总览`，避免继续把历史首页占位文案固化进 focused proof。
- 控制器已按最小垂直切片完成验证：`pnpm --dir web exec vitest run src/components/ProtectedRoute.test.tsx src/pages/ProfilePage.test.tsx src/pages/ApiKeysPage.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/LoginPage.test.tsx src/pages/SettingsPage.test.tsx src/pages/AdminUsersPage.shared-console.test.tsx src/pages/AdminRiskPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx` 通过（9 files / 56 tests）；随后 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 均通过，并执行真实 API 回放脚本 `.hermes/local-output/console-home-title-alignment-real-api/run.sh`（最新产物目录 `.hermes/local-output/console-home-title-alignment-real-api/20260506-193531`），确认 `GET /healthz` 返回 200，普通用户 `POST /api/v1/auth/register` 返回 201 且 `GET /api/v1/auth/me` `/auth/menu` 返回 200、菜单不泄露 supplier/admin 路径；管理员与供应商 `POST /api/v1/auth/login` 后各自 `GET /api/v1/auth/menu` 返回 200；前端 `http://127.0.0.1:5173/` 返回 200 且 SPA 壳存在 `Nexus-Mail` 标识。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅统一 focused 测试中的 shared-home canonical title，使其与 `DashboardPage` / `resolveRouteTitle(DASHBOARD_ROUTE)` 的真实 `控制台总览` 合同对齐，不改变运行时权限、导航、菜单或真实 API 契约。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理其余未触达 focused 用例中残留的 `共享控制台首页` / 低信号 page-global 落点断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Supplier/Admin route-stub heading alignment tightening）
- 本轮继续按“前端优先”清理 shared-console focused 用例中仍残留的低信号 page-global 落点断言：在不改运行时代码、不改注册/菜单/权限/真实 API 合同、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `SupplierResourcesPage.test.tsx`、`SupplierSettlementsPage.test.tsx`、`AdminAuditPage.test.tsx` 的 mission-flow / shared-console fallback 导航 proof 从 `供应商资源页`、`供应商供货页`、`API Keys 页面`、`共享控制台首页` 这类 page-global stub 文本，收紧为 route-scoped `data-testid` + 与真实页面一致的 heading 双合同；同时把 shared-home stub 标题按当前真实 Dashboard 页对齐为 `控制台总览`，避免把旧的首页占位文案继续固化进测试。
- 控制器已按 RED→GREEN 先让三份 focused 测试暴露 route 落点和 shared-home heading 的低信号问题，再最小收敛测试 harness；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx src/pages/AdminAuditPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/front-route-stub-hardening/run.sh`（最新产物目录 `.hermes/local-output/front-route-stub-hardening/20260506-190617`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，新注册普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me` `/auth/menu` `/dashboard/overview` 返回 200 且菜单不暴露 supplier/admin 路径；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu` `/admin/audit?limit=5` `/admin/risk` 返回 200；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu` `/supplier/resources/overview` `/supplier/settlements` 返回 200；前端 `/supplier/resources` `/supplier/settlements` `/admin/audit` `/api-keys` `/webhooks` `/docs` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS（同轮已把 shared-home stub heading 从 `共享控制台首页` 修正为与真实 Dashboard 一致的 `控制台总览`）、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅把 Supplier/Admin 三个 focused 导航/回退 proof 从低信号 page-global 落点文本收敛为更贴近运行时结构的 route-stub + heading 合同，不改变权限、导航或真实 API 契约。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理其余 shared-console focused 用例中仍残留的 page-global 落点/说明断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Projects/Orders guidance scoped contract tightening）
- 本轮继续按“前端优先”推进 shared-console focused 去脆弱化：在不改真实注册/菜单/API 契约、不拆分独立后台、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `ProjectsPage` / `OrdersPage` 中采购与履约任务流的 proof 从 page-global copy 收紧为更贴近运行时结构的 scoped guidance / continuation 合同；其中为 `ProjectsPage.tsx` 的采购动作区补充最小 `projects-action-guidance-card`、`projects-guidance-next-step-card`、`projects-guidance-fallback-card` 语义锚点，并把 `ProjectsPage.test.tsx` / `OrdersPage.test.tsx` 分别改为在对应 scoped 卡片内验证 `下单后下一步`、`共享控制台回退路径`、`订单结果 → API 接入 → 再次采购` 及相关 CTA，而不再依赖易重复的整页说明文案。
- 控制器已按 RED→GREEN 先让新的 scoped focused 断言失败，再以最小运行时代码补齐缺失锚点并复验通过；随后重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_projects_orders_hero_scope_real_api.sh`（本轮最新产物目录 `.hermes/local-output/projects-orders-hero-scope/20260506-183800`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` `/auth/me` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` `/projects/inventory` `/orders/activations` 返回 200，管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu` `/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200，且前端 `/` `/projects` `/orders` `/api-keys` `/webhooks` `/docs` `/admin/suppliers` 均保持 SPA 壳 200。
- 本轮五维评审以 controller-side 事实为准：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；其中 code quality / reliability 仅继续记录既有 Semi UI `findDOMNode`、React `act(...)` 与前端 chunk 体积告警为非阻塞债务。评审一致确认当前 diff 仅把采购/履约任务流 proof 收紧到 scoped 语义区域，不改变权限、导航或真实 API 合同；下一前端优先项继续清理其他 shared-console focused 用例里仍残留的 page-global copy / route-stub 落点断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Login auth shell embedded-tab refinement）
- 本轮继续按“前端优先”推进登录/注册入口的 shared-console / new-api 风格统一：在不改注册/登录真实 API 契约、不拆分独立后台、继续保持“注册成功后进入同一套共享控制台”的前提下，仅收敛 `LoginPage.tsx` 右侧 auth shell 的内部层级与可访问语义 —— 为登录 / 注册切换补齐 `tablist` / `tab` + `aria-selected` 合同，把浅色 segmented control 改成嵌入式深色切换条，引导 banner 压暗到与壳同层的嵌入态，表单输入框同步改为深色高对比输入面，并把注册 guidance 明确收敛到真实共享菜单术语 `项目市场 → 订单中心 → API Keys`，继续保持单一登录后共享控制台叙事而不是独立新手页或多后台分叉。
- 控制器已按 RED→GREEN 先扩展 `LoginPage.test.tsx` 与联动 `App.test.tsx`，让 focused 用例先要求 `login-auth-mode-switch` 的 `tablist` / `tab` 可访问语义、登录/注册 guidance 切换、`统一控制台认证` 壳内 eyebrow 文案，以及 App 层注册入口从 mode switch 触发的真实 register bootstrap 路径；随后以最小运行时代码补齐 auth shell 语义与样式，再重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行真实 API 回放脚本 `.hermes/local-output/login-auth-shell-refine-real-api/run.sh`（最新产物目录 `.hermes/local-output/login-auth-shell-refine-real-api/latest`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，本地 SPA `http://127.0.0.1:5173/login` 在一次启动瞬态 `connection reset` 重试后返回 200 且包含 `Nexus-Mail` 壳标识，普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me` `/auth/menu` `/dashboard/overview` `/auth/api-keys` 均返回 200。
- 本轮五维评审以 controller-side 事实为准：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间两轮 reviewer 曾分别 fail-closed 提醒（1）认证模式切换不应只停留在 `aria-pressed`，应升级为 mutually-exclusive `tablist` / `tab` 语义；（2）注册 guidance 不应使用泛化的 `API 接入` 术语而应与真实共享菜单 `API Keys` 对齐；控制器已在同轮完成整改并复跑 focused Vitest / build / Go tests / compose config / 真实 API 全部通过。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、前端 chunk 体积告警，以及浏览器视觉评审指出 auth shell 文案/输入框仍可继续做一轮对比度微调；下一前端优先项继续围绕登录壳剩余可读性微调，或转向其他 shared-console focused 用例/页面的低信号断言收敛，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Login shared-console auth shell darkening）
- 本轮继续按“前端优先”推进登录/注册入口的 shared-console / new-api 风格统一：在不改注册/登录真实 API 契约、不拆分独立后台、继续保持“注册成功后进入同一套共享控制台”的前提下，仅把 `LoginPage.tsx` 右侧认证卡从高亮白卡收敛为深色 auth shell，并把登录/注册标题与主 CTA 文案统一改为 `登录并进入 Shared Console` / `创建账号并进入 Shared Console`、`登录并进入统一控制台` / `注册并进入统一控制台`，同时补充最小 `login-auth-shell` 语义锚点，确保登录入口视觉与 shared-console 单壳叙事一致，不再像独立营销页弹出的浅色表单。
- 控制器已按 RED→GREEN 先修改 `LoginPage.test.tsx`，让 focused 用例先要求新的 shared-console auth shell 文案与 `login-auth-shell` 锚点，再最小更新运行时代码与 `App.test.tsx` 的联动断言；随后重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`docker compose up -d --build web` 与 `git diff --check`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_login_console_refresh_real_api.sh`（最新产物目录见 `.hermes/local-output/login-console-refresh-real-api/latest.txt`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，前端 `/login` `/` `/api-keys` `/docs` 返回 200，普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me` `/auth/menu` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 均返回 200。
- 本轮五维评审以 controller-side 事实为准：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间已丢弃 5 份明显跨仓库漂移、错误审阅到其他仓库的 delegate 结果，并按 fail-closed 规则改用 live diff、focused Vitest、build、Go tests、compose config、静态安全扫描、真实 API 回放与浏览器视觉检查作为最终判定依据。评审一致确认当前 diff 仅收敛登录入口文案与 auth shell 视觉层级，不改变权限、注册/登录契约或 shared-console 单壳导航真值；非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、前端 chunk 体积告警，以及浏览器视觉评审指出的 auth shell 内部分层/提示条仍可继续压暗与 segmented control 仍可进一步精修，下一前端优先项继续围绕登录卡片内部层级、提示条暗色嵌入式样式与 segmented control 精致度收敛，再回到 Phase 4/5 余下能力。

### 最新执行检查点（2026-05-06，AdminSuppliers route-stub scoped proof）
- 本轮继续按“前端优先”收敛 `AdminSuppliersPage.test.tsx` 中仍残留的低信号 page-global 路由落点断言：在不改 `AdminSuppliersPage.tsx` 运行时代码、单一登录后 shared-console / new-api 单壳导航合同、管理员角色菜单 gating 与真实接口合同的前提下，仅把管理员供应商运营台 mission-flow 三条导航 proof（结算 / 争议、风控中心、审计日志）以及共享回退 proof，从 `结算与争议页面`、`风控中心页面`、`审计日志页面`、`共享控制台首页` 这类 page-global stub 文本，收紧为统一命名的 `admin-suppliers-route-stub-*` 语义锚点合同，避免继续依赖整页重复落点文案，同时保持“管理员供应商运营页仍属于同一共享控制台壳，而非独立后台”的产品语义不变。
- 控制器已按 RED→GREEN 先让 `pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx` 验证 focused 用例，再重新通过 `pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；同时执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_suppliers_route_stub_scope_real_api.sh`（最新产物目录见 `.hermes/local-output/admin-suppliers-route-stub-scope/latest.txt`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` `/auth/me` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 返回 200、管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu` `/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200，且前端 `/` `/admin/suppliers` `/api-keys` `/webhooks` `/docs` `/admin/risk` `/admin/audit` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间根据代码质量/运维评审的非阻塞建议，把 fallback 路由桩命名从不规则的 shared-home 特例统一收敛为 `admin-suppliers-route-stub-shared-home`，随后重新复跑 focused / grouped Vitest、build、Go tests、compose config 与真实 API 回放全部通过。评审一致确认当前 diff 仅把 AdminSuppliers focused 导航落点 proof 从低信号 page-global stub 文本收敛为更贴近运行时结构的 route-stub 合同，不改变权限、导航或真实 API 合同；非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警，下一前端优先项继续清理其余 admin/shared-console focused 用例里的低信号 page-global 落点 / 文本断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Settings capability-matrix scoped proof）
- 本轮继续按“前端优先”收敛 `SettingsPage.test.tsx` 与联动 helper `App.test.tsx` 中仍残留的低信号 page-global 文案断言：在不改 shared-console/new-api 单壳运行时合同的前提下，仅为 `SettingsPage.tsx` 的“控制台能力矩阵”卡片补充最小 `settings-capability-matrix` 语义锚点，把设置页 focused proof 从 `深色共享工作台`、`Docs / Webhooks / API Keys 已统一到单一壳内导航`、普通用户首轮提示整句 copy 等 page-global 文本，收紧为 checklist / mission-cards / capability-matrix scoped 合同，同时删除 `App.test.tsx` 中 `expectDefaultUserFirstRunLane()` 对 `欢迎进入共享控制台` 的 page-global heading 依赖，继续保持注册后统一壳、角色菜单 gating、设置页 → Docs/Webhooks 导航与普通用户首轮回流语义真值不变，而不是继续依赖易变整页文案。
- 控制器已按 RED→GREEN 先让 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 暴露 scoped 锚点缺失，再以最小运行时改动补上 `settings-capability-matrix` 后复验通过；随后重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/SettingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_settings_front_scope_real_api.sh`（最新产物目录 `.hermes/local-output/settings-front-scope/20260506-164205`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` `/auth/me` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 返回 200、管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu` `/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200，且前端 `/` `/settings` `/profile` `/api-keys` `/webhooks` `/docs` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅为 Settings focused proof 补最小语义锚点并把断言收敛到更贴近运行时结构的 scoped 合同，不改变权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、局部 `async-validator`/Semi Form warning 与前端 chunk 体积告警；下一前端优先项继续清理其余 shared-console focused 用例里仍残留的 page-global copy / route-stub 落点断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Projects/Orders route-stub scoped proof）
- 本轮继续按“前端优先”收敛 `ProjectsPage.test.tsx` 与 `OrdersPage.test.tsx` 中仍残留的低信号 page-global 路由落点断言：在不改运行时代码与真实接口合同的前提下，仅把采购页与订单页内 4 条共享控制台导航 proof（项目市场 → 订单中心、项目市场 → API Keys、订单中心 → 项目市场、两个 empty-state → 推荐工作台回流）从 `订单中心页面` / `开发者 API 接入工作台` / `控制台总览页面` 这类 page-global 文本，收紧为显式 `*-route-stub` / `*-shared-console-home-route` `data-testid` 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Projects ↔ Orders ↔ API Keys 单壳衔接与共享回退语义真值不变，而不是继续依赖易重复的整页落点文案。
- 控制器已按 RED→GREEN 先完成 focused 用例修正，再重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_projects_orders_hero_scope_real_api.sh`（最新产物目录 `.hermes/local-output/projects-orders-hero-scope/20260506-161820`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` `/auth/me` `/dashboard/overview` `/projects/inventory` `/orders/activations` 返回 200、管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200，且前端 `/` `/projects` `/orders` `/api-keys` `/webhooks` `/docs` `/admin/suppliers` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间已丢弃 5 份明显跨仓库漂移、错误审阅到 `/root/.hermes/hermes-agent` 的 delegate 结果，并按 fail-closed 规则改用 controller-side live diff、静态安全扫描、focused Vitest、build、`go test ./...`、`docker compose config`、真实 API 回放作为最终判定依据。评审一致确认当前 diff 仅把 Projects / Orders focused 导航 proof 从低信号 page-global 落点文本收敛为更贴近运行时结构的 scoped route-stub 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理其余 shared-console focused 用例里仍残留的 page-global 落点 / 说明断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，ApiKeys route-stub scoped proof）
- 本轮继续按“前端优先”收敛 `ApiKeysPage.test.tsx` 中仍残留的低信号共享接入落点断言：在不改运行时代码与真实接口合同的前提下，仅把 bridge / fallback 导航 proof 从 `Webhook 设置页面` / `API 文档页面` / `项目市场页面` / `共享控制台首页` 这类 page-global 文本，收紧为 `api-keys-shared-console-bridge`、`api-keys-shared-console-fallback` 作用域内 CTA 交互 + `route-stub-*` `data-testid` 合同，并把创建后桥接验证表述收敛为“创建前后均可通过 scoped bridge 导航”，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、API Keys → Webhooks / Docs / Projects 单壳桥接与共享回退语义真值不变，而不是继续依赖易重复的整页落点文案。
- 控制器已按 RED→GREEN 先让 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 验证 focused 用例，再重新通过 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_keys_focused_hardening_real_api.sh`（最新产物目录 `api-keys-focused-hardening-20260506-151730`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/me` `/auth/menu` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 返回 200，且前端 `/api-keys` `/webhooks` `/docs` `/settings` 均保持 SPA 壳 200。期间 controller-side grouped Vitest 曾一次性命中与本 diff 无关的 `App.test.tsx` 历史异步波动（管理员 Webhook endpoint skeleton 先渲染导致文本断言瞬时失败），随后按 fail-closed 规则单独隔离 `renders webhook settings page for authenticated admin` 并重跑完整分组后恢复 38/38 通过，因此该波动记录为既有非阻塞 flake 债务而非本轮回归。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅把 `ApiKeysPage.test.tsx` 中共享接入 bridge / fallback 导航 proof 从低信号 page-global 落点文本收敛为更贴近运行时结构的 scoped CTA + route-stub 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、前端 chunk 体积告警，以及 grouped App/Webhooks 异步骨架偶发波动；下一前端优先项继续清理其余 shared-console focused 用例里仍残留的低信号 page-global 落点/说明断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Dashboard route-stub + async webhook readiness proof）
- 本轮继续按“前端优先”收敛 shared-console focused 用例中的低信号 page-global 落点断言：在不改运行时代码与真实接口合同的前提下，仅把 `DashboardPage.test.tsx` 中 shared journey lane 的四条导航 proof，从 `余额中心页面` / `项目市场页面` / `订单中心页面` / `开发者 API 接入工作台` 这类 page-global stub 文本，收紧为 route-local `dashboard-*-route-stub` 语义锚点 + 与真实页面对齐的 heading 合同；同时顺手把 `App.test.tsx` 中管理员 Webhook 首屏对当前 endpoint 的同步文本命中改为 `findBy*` 异步就绪 proof，避免 grouped App/Dashboard 回归里受首屏 skeleton 时序影响而出现假阴性，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Dashboard 下一步旅程与 Webhook 管理页真实 API 菜单真值不变。
- 控制器已按 RED→GREEN 先让 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx` 暴露 navigation proof 的 page-global 文本脆弱点，再补齐 route-stub + heading 合同，并在系统化调试后修复 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx` 暴露的管理员 Webhook current-endpoint skeleton 时序问题；随后重新通过 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/dashboard-route-stub-scope/run_dashboard_route_stub_scope_real_api.sh`（最新产物目录见 `.hermes/local-output/dashboard-route-stub-scope/latest.txt`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` `/auth/me` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 返回 200、管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200，且前端 `/` `/api-keys` `/webhooks` `/docs` `/settings` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；其中产品评审一度 fail-closed 指出 route-stub heading 应与真实目标页标题一致，代码质量评审提醒 route-stub testid 不应单独替代行为级合同，控制器已在同轮把 Dashboard 三个目标页 heading 改为 `余额中心` / `项目市场` / `订单中心` 并保留 route-stub + heading 双合同后复验通过。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理其余 shared-console focused 用例中仍残留的低信号 page-global 落点 / 文本断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Profile route-stub scoped proof）
- 本轮继续按“前端优先”收敛 `ProfilePage.test.tsx` 中仍残留的低信号 route 落点文本断言：在不改运行时代码与真实接口合同的前提下，仅把 `profile-capability-bridge` 与 `profile-shared-console-return` 两组导航 proof 的目标页判断，从 `API Keys 页面` / `Webhook 设置页面` / `API 文档页面` / `设置中心页面` / `共享控制台首页` 这类 page-global 文本，收紧为显式 `*-route-stub` `data-testid` 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Profile→API Keys/Webhooks/Docs/Settings/shared-home 单壳回流语义真值不变，而不是继续依赖易重复的整页文案节点。
- 控制器已按 RED→GREEN 先让 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 验证 Profile focused 用例，再重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_profile_route_stub_scope_real_api.sh`（最新产物目录 `profile-route-stub-scope/20260506-142821`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` `/auth/me` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 返回 200、管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200，且前端 `/` `/profile` `/settings` `/api-keys` `/webhooks` `/docs` `/admin/risk` `/supplier/domains` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；同轮已丢弃 3 份明显跨仓库漂移、错误审阅到 `/root/.hermes/hermes-agent` 的 delegate 结果，并以 controller-side live diff、focused tests、build、`go test ./...`、`docker compose config`、静态安全扫描与真实 API 回放作为最终判定依据。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、前端 chunk 体积告警，以及当前仓库早已存在且与本次 diff 无关的 `pnpm --dir web lint` 类型错误；下一前端优先项继续清理 `ApiKeysPage.test.tsx` 等仍依赖 route-stub 页面文本落点的 focused 用例，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，route-stub scope hardening）
- 本轮继续按“前端优先”收敛 `ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 与 `App.test.tsx` 中仍残留的低信号 page-global 导航落点断言：在不改运行时代码与真实接口合同的前提下，仅把 Docs / Webhooks 多跳 CTA 的目标页证明从宽泛页面文本收紧为显式 route-stub `data-testid`，并把 App 层 supplier profile / dashboard 跳转后的落点从普通文本收紧为 `域名池运营中枢` heading；同时保留 `WebhooksPage.test.tsx` 首屏 `loads endpoints and auto-loads first endpoint deliveries` 对自动拉取首个 endpoint delivery 的真实断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Docs ↔ API Keys ↔ Webhooks 单壳桥接与 supplier 域名运营路径真值不变，而不是继续依赖易重复的整页文案节点。
- 控制器已按 RED→GREEN 先将 route 落点 proof 改为 stub/heading 合同，再根据产品评审补回 `mockedGetWebhookEndpoints` 与 `mockedGetWebhookDeliveries` 断言以维持测试名与证明内容一致；随后重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_route_stub_scope_real_api.sh`（产物目录 `route-stub-scope-real-api/20260506-124759`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints` 全部返回 200、管理员 `POST /api/v1/auth/login` 后 `/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200、供应商 `/supplier/resources/overview` `/supplier/settlements` 返回 200，且前端 `/` `/login` `/profile` `/settings` `/api-keys` `/webhooks` `/docs` `/supplier/domains` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；产品评审曾 fail-closed 指出 `loads endpoints and auto-loads first endpoint deliveries` 被过度弱化，控制器已在同轮补回自动拉取 delivery 的关键断言后复验通过。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理 `ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx`、`App.test.tsx` 之外其余 shared-console focused 用例里仍残留的低信号 page-global 落点/说明断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Webhooks role-guidance scoped proof）
- 本轮继续按"前端优先"收敛 `WebhooksPage.test.tsx` 中仍残留的低信号 page-global 角色引导 / 空态文案断言：在不改运行时代码与真实接口合同的前提下，仅把 supplier/admin 角色视图对 `供给事件回调工作台`、`Webhook 运维与回调观测`、`供应商视角`、`管理员视角` 的证明收紧到 `webhooks-role-guidance` 卡片内，并把空 endpoint 场景对 `当前还没有 Webhook endpoint，先创建第一个回调地址。` 的断言收紧到 `webhooks-current-endpoints-card` 作用域内；同时保留管理员视角对 `最近回调`、`失败 / 排队中`、`重点关注 failed / pending 重试链路与 last_error` 的真实运维信号合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Webhook → API Keys / Docs 接入闭环与真实 API 菜单真值不变，而不是继续依赖易重复的整页文案节点。
- 控制器已按 RED→GREEN 先把 `WebhooksPage.test.tsx` 中 supplier/admin guidance 与 empty-state proof 改为 card-scoped 合同，再依据产品评审把 badge 断言从 `getByLabelText('Tag: ...')` 回调为 `webhooks-role-guidance` 区域内真实可见文案，随后重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/verify_webhooks_role_guidance_scope.sh`（最新产物目录 `webhooks-role-guidance-card-scope/20260506-132538`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` / `GET /api/v1/webhooks/endpoints` 返回 200、管理员 `GET /api/v1/admin/risk` / `GET /api/v1/admin/audit?limit=5` 返回 200、供应商 `GET /api/v1/supplier/resources/overview` / `GET /api/v1/supplier/settlements` 返回 200，且前端 `/` `/login` `/webhooks` `/api-keys` `/docs` `/admin/audit` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；产品评审曾 fail-closed 指出把 badge 断言降级为 `Tag:` 可访问名前缀会弱化用户可见合同，控制器已在同轮回调为 `webhooks-role-guidance` 区域内真实文案断言后复验通过。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理 `ProfilePage.test.tsx`、`ApiKeysPage.test.tsx` 等仍依赖 route-stub 页面文本落点的 focused 用例，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Login shared-console hero densification）
- 前端优先推进登录/注册页垂直切片：将左侧信息架构从多组冗余旅程/说明/接入卡压缩为更接近 new-api / Linear 风格的深色 hero signals + 单一 registration runway，保留单一登录后共享控制台叙事，并继续维持注册仅 `email/password`、成功后进入同一控制台的真实行为合同。
- 本轮新增 `login-hero-signal-grid` 与更紧凑的 `login-register-journey` 结构，删除重复的 `journeyCards` / `capabilityHighlights` / `firstRunChecklist` / `integrationStages` 大块文案；`LoginPage.test.tsx` 先以 RED→GREEN 方式新增 compact hero / runway focused proof，确认新结构存在且旧冗余区块不再渲染，然后再完成运行时代码收敛。
- 验证结果：`pnpm exec vitest run src/pages/LoginPage.test.tsx` 通过；`pnpm exec vitest run src/App.test.tsx src/pages/LoginPage.test.tsx` 通过；`pnpm build` 通过；`go test ./...` 通过；`docker compose up -d --build web` 成功；真实 API 回放脚本 `/root/nexus-mail/.hermes/local-output/login-milestone/verify_login_milestone.sh` 完成 `POST /auth/register`、`GET /auth/menu`、`GET /auth/me`、`GET /dashboard/overview` 以及 `/login`、`/`、`/projects`、`/api-keys`、`/webhooks`、`/docs` 的 SPA shell 200 检查并全部通过。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 真实推进的是登录入口的信息密度与单壳注册跑道，而非后端认证合同变更。非阻塞债务仍仅包括既有 React `act(...)` 告警与前端 chunk 体积告警；下一前端优先项继续转向 shared-console 首屏 / 仪表盘 remaining focused proof 去脆弱化与视觉收敛，随后再回到 Phase 4/5 的 API Key / Webhook / 风控 / 审计后续切片。

### 最新执行检查点（2026-05-06，SupplierOfferings scoped contract hardening）
- 本轮继续按"前端优先"收敛 `SupplierOfferingsPage.test.tsx` 中供货规则页首个 mission-shell focused 用例仍残留的低信号 page-global 文本/指标命中：在不改运行时代码与真实接口合同的前提下，仅为 `SupplierOfferingsPage.tsx` 的 hero 区补充 `supplier-offerings-hero-card`、为四张汇总指标卡补充 `supplier-offerings-metric-*`、为 shared-console pillar 卡补充 `supplier-offerings-console-pillar-*`、并为当前可售规则表补充 `supplier-offerings-table` / `aria-label` 最小语义锚点；同时把对应 focused proof 从整页标题/文案/按钮命中收紧为 hero / mission-flow / shared-console-bridge / offerings-table scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、供应商任务流与共享接入桥接真值不变，而不是继续依赖整页重复 copy、数字或按钮永远唯一这一过宽假设。
- 控制器已按 RED→GREEN 先让 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx` 暴露多重命中与按钮可访问名问题，再补齐最小锚点后复跑通过；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/App.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_supplier_offerings_real_api.sh`（最新产物目录 `supplier-offerings-real-api/20260506-121800`），确认 `GET /healthz` 返回 200 且 `redis=ok`、新注册普通用户 `/auth/menu` 暴露共享壳入口、供应商 `/auth/menu` 包含 `/supplier/resources` `/supplier/offerings` `/supplier/settlements` `/api-keys` `/webhooks` `/docs`、供应商真实接口 `/supplier/resources/overview` `/supplier/resources/offerings` `/supplier/settlements` 均返回 200、管理员 `/admin/risk` 与 `/admin/audit?limit=5` 返回 200，且前端 `/` `/supplier/offerings` `/api-keys` `/webhooks` `/docs` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；测试/可靠性评审一度指出若只保留两张指标卡会削弱对 `覆盖项目` 与 `可挂接域名池` 的合同保护，控制器已在同轮补回这两项 scoped metric proof 后复验通过。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、局部表单校验 warning 与前端 chunk 体积告警；下一前端优先项继续清理 `SupplierOfferingsPage.test.tsx` 之外其余 shared-console focused 用例中的低信号 page-global 命中，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，shared shell semantic region hardening）
- 本轮继续按“前端优先”收敛 shared-shell focused 用例中仍残留的 page-global 角色/头部摘要断言：在不改运行时代码与真实接口合同的前提下，仅为 `AppSidebar.tsx` 的角色标签 + 摘要说明补充最小 `app-sidebar-role-summary` 语义区域，并为 `ConsoleLayout.tsx` 的 breadcrumb / 当前页标题 / 角色标签 / 单一登录后控制台提示补充最小 `console-layout-header-summary` 语义区域；同时把 `AppSidebar.test.tsx` 与 `ConsoleLayout.test.tsx` 中对供应商/管理员角色摘要、共享头部标题与单壳提示的 proof 从 page-global 文本命中收紧为对应语义区域 scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、统一布局骨架、角色菜单 gating 与单壳导航真值不变，而不是继续依赖整页同名文本永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API 回放脚本 `.hermes/local-output/run_shared_shell_semantic_region_real_api.sh`（产物目录 `shared-shell-semantic-region-real-api/20260506-103637`），确认 `GET /healthz` 返回 200 且 `redis=ok`、新注册普通用户 `/auth/menu` 仅暴露共享工作台路径并排除 `/admin/risk` 与 `/supplier/domains`、普通用户 `/auth/me` `/dashboard/overview` `/projects/inventory` `/orders/activations` `/auth/api-keys` `/webhooks/endpoints` 全部返回 200、管理员 `/auth/menu` 含 `/admin/risk` 与 `/admin/audit`，且 `/admin/overview` `/admin/risk` `/admin/audit?limit=5` 均返回 200，证明单一登录后共享壳的角色菜单与管理面真实 API 合同未被此次语义锚点收敛破坏。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间已丢弃 3 组明显跨仓库漂移、错误审阅到 `/root/.hermes/hermes-agent` 的 delegate 结果，并以 controller-side live diff、focused tests、build、`go test ./...`、真实 API 回放与静态安全扫描为最终判定依据。评审一致确认当前 diff 仅为 shared shell 角色摘要区与头部摘要区补充最小语义锚点，并把 focused proof 收敛为更贴近运行时结构的 scoped 合同，不改变运行时权限、导航或真实 API 合同；非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警，下一前端优先项继续清理 `App.test.tsx` 及部分 shared-shell/page focused 用例里仍残留的低信号 page-global 文本/落点断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，App webhook send-action scoped proof）
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中管理员 Webhook 页 `creates a webhook test delivery and refreshes the delivery feed` 用例仍残留的低信号 page-global 同名按钮命中：在不改运行时代码与真实接口合同的前提下，仅把点击目标从 page-global `findByRole('button', { name: '发送测试投递' })` 收紧为 `webhooks-current-endpoints-card` 区域内绑定真实 endpoint 行动作的 `webhooks-send-test-button-11` 语义锚点，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Webhook → API Keys / Docs 单壳联动与真实 API 菜单真值不变，而不是继续依赖整页同名按钮永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_admin_send_scope_real_api.sh`（产物目录 `webhooks-admin-send-scope/20260506-092238`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、普通用户新建 endpoint 后 `POST /api/v1/webhooks/endpoints/<user_endpoint_id>/test-delivery`（202）、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、真实 Webhook 投递链路、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅把 App 层管理员 Webhook 发送测试动作 proof 从低信号 page-global 同名按钮命中收敛为更贴近运行时结构的 current-endpoint-card / row-action scoped 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本/按钮断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，Login/Webhooks focused route-scope proof）
- 本轮继续按“前端优先”收敛 `LoginPage.test.tsx` 与 `WebhooksPage.test.tsx` 中仍残留的低信号 page-global 落点 / 说明文案断言：在不改运行时代码与真实接口合同的前提下，仅把注册壳相关用例对首页回流与注册 CTA 的 proof 收紧为 `login-register-journey` scoped 查询 + `shared-console-home` 路由桩锚点，把 Webhook 首轮集成卡与空态回退 proof 从 page-global 文案命中收紧为步骤 heading / `webhooks-empty-state-actions` scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating、Webhook → API Keys / Docs 接入闭环与真实 API 菜单真值不变，而不是继续依赖易重复的整页文案或宽泛首页文本节点。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/LoginPage.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_login_webhooks_test_scope_real_api.sh`（产物目录 `login-webhooks-test-scope/20260506-095112`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；五路评审一致确认当前 diff 仅把 Login / Webhooks focused proof 从低信号 page-global 文本与宽泛首页落点命中收敛为更贴近运行时结构的 journey-scope、步骤 heading、empty-actions scope 与 route-stub 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理 `App.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本/指标断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，AdminRisk rule editor scoped proof）
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中管理员风控页 rule editor focused 用例仍残留的低信号 page-global 按钮断言：在不改运行时代码与真实接口合同的前提下，仅为 `AdminRiskPage.tsx` 的“可配置风控规则”卡片补充最小 `admin-risk-rule-editor-card` 语义锚点，并把 App 层管理员风控首屏 proof 从 page-global `getByRole('button', { name: '保存规则' })` 收紧为 `admin-risk-overview-card` / `admin-risk-rule-editor-card` scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、风控→审计/API Keys 单壳联动与真实 API 菜单真值不变，而不是继续依赖整页同名按钮永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_risk_rule_editor_scope_real_api.sh`（产物目录 `admin-risk-rule-editor-scope/20260506-073341`；同轮此前也完成 `20260506-073143` 复放），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/login`、`/admin/risk`、`/admin/audit`、`/webhooks`、`/api-keys`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员风控/审计接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；五路评审一致确认当前 diff 仅为 AdminRisk 风控规则编辑卡补充最小语义锚点，并把 App 层管理员风控首屏 proof 从低信号 page-global 按钮命中收敛为更贴近运行时结构的 overview-card / rule-editor-card scoped 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，Webhook send-test button scoped proof）
- 本轮继续按"前端优先"收敛 `WebhooksPage.test.tsx` 与 `App.test.tsx` 中 webhook 发送测试投递 focused 用例仍残留的低信号同名按钮命中：在不改真实接口与角色权限合同的前提下，仅为 `WebhooksPage.tsx` 的 endpoint 行级“发送测试投递”按钮补充最小 `webhooks-send-test-button-${record.id}` 语义锚点，并把页级 / App 级 proof 从 page-global `getByRole('button', { name: '发送测试投递' })` 收紧为 `webhooks-current-endpoints-card` 区域内绑定真实 endpoint id 的 scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Webhook → API Keys / Docs 接入闭环与真实 API 菜单真值不变，而不是继续依赖整页同名动作按钮永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_send_button_scope_real_api.sh`（产物目录 `webhooks-send-test-button-scope/20260506-084608`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/webhooks`、`/api-keys`、`/docs`、`/admin/audit`、`/login` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间已丢弃两组明显跨仓库漂移、错误审阅其他仓库 diff 的 delegate 结果，并以 controller-side live diff、focused tests、build、`go test ./...`、compose 重建与真实 API 回放为最终判定依据。评审一致确认当前 diff 仅为 webhook 行级发送测试投递按钮补充最小语义锚点，并把相关 proof 收敛为更贴近运行时结构的 endpoint-card / row-action scoped 合同，不改变运行时权限、导航或真实 API 合同；非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警，下一前端优先项继续清理 `WebhooksPage.test.tsx`、`App.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本/按钮断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，Dashboard admin scoped proof）
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中管理员 dashboard deep statistics 用例仍残留的低信号 page-global 指标/标题断言：在不改运行时代码与真实接口合同的前提下，仅为 `DashboardPage.tsx` 的"订单完成率"指标卡补充最小 `dashboard-order-completion-rate-card` 语义锚点，并把 App 层对 `订单完成率`、`争议发生率`、`已完成订单流水`、`当前重点关注供应商` 的 proof 从 page-global `findByText/getByText` 收紧为 completion-rate / dispute-rate / revenue / top-supplier card scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与管理员运营概览真值不变，而不是继续依赖整页重复指标标题永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/DashboardPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_dashboard_admin_scope_real_api.sh`（产物目录 `dashboard-admin-scope/20260506-070723`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员概览/风控/审计接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；controller-side 复核确认当前 diff 仅为 Dashboard 页面补充最小语义锚点，并把 App 层管理员概览 focused proof 从低信号 page-global 文本命中收敛为更贴近运行时结构的 metric-card / top-supplier-card scoped 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理 `App.test.tsx`、`AppSidebar.test.tsx`、`ConsoleLayout.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，Settings session scoped proof）
- 本轮继续按"前端优先"收敛 `App.test.tsx` 与 `SettingsPage.test.tsx` 中仍残留的低信号 page-global 会话文案断言：在不改运行时代码与真实接口合同的前提下，仅为 `SettingsPage.tsx` 的“当前登录会话”卡片补充最小 `settings-session-card` 语义锚点，并把 App / 设置页层对 `控制台模式`、注册后 API Keys 首屏等 proof 从 page-global `findByText/getByText` 收紧为 `settings-session-card` / `api-keys-hero-card` / heading scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与共享接入入口真值不变，而不是继续依赖整页重复说明文案永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/SettingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_settings_session_scope_real_api.sh`（产物目录 `settings-session-scope/20260506-064609`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`POST /api/v1/auth/logout` 与前端 `/settings`、`/login` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、会话信息卡、API Keys 首屏与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间已丢弃一组明显跨仓库漂移的 delegate 评审结果，并以控制器基于 live diff、focused tests、build、`go test ./...` 与真实 API 回放的 repo-scoped 复核为准。当前 diff 仅为 Settings 会话卡补充最小语义锚点，并把 App / Settings focused proof 从低信号 page-global 文本命中收敛为更贴近运行时结构的 session-card / hero-card / heading scoped 合同，不改变运行时权限、导航或真实 API 合同；下一前端优先项继续清理 `App.test.tsx`、`WebhooksPage.test.tsx`、`ApiDocsPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，AdminRisk scoped proof）
- 本轮继续按"前端优先"收敛 `App.test.tsx` 与 `AdminRiskPage.test.tsx` 中管理员风控页仍残留的低信号 page-global 风险文本断言：在不改运行时代码与真实接口合同的前提下，仅为 `AdminRiskPage.tsx` 的 mission-signals 容器补充最小 `admin-risk-mission-signals` 语义锚点、为高风险指标卡补充最小 `admin-risk-high-risk-metric` 锚点，并把 App / 页面层对 `规则命中概览`、`高风险` 等 proof 从 page-global `findByText/getByText` 收紧为 overview-card / metric-card / mission-flow / shared-console-bridge scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、风控→审计/API Keys/文档共享联动与真实 API 菜单真值不变，而不是继续依赖整页重复风险标签永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/AdminRiskPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_risk_scope_real_api.sh`（产物目录 `admin-risk-scope/20260506-060846`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/login`、`/admin/risk`、`/admin/audit`、`/webhooks`、`/api-keys`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员风控/审计接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；controller-side 复核确认当前 diff 仅为 AdminRisk 页面补充最小语义锚点，并把 App / AdminRisk focused proof 从低信号 page-global 风险文本命中收敛为更贴近运行时结构的 metric-card / overview-card / mission-signal / shared-console-bridge scoped 合同，不改变运行时权限、导航或真实 API 合同。期间已丢弃 1 组明显跨仓库漂移的外部评审结果，最终以控制器基于 live diff、build、测试、compose 与真实 API 回放的五维复核为准；下一前端优先项继续清理 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06）
- 本轮继续按"前端优先"收敛 `WebhooksPage.test.tsx` 中管理员 metrics focused 用例仍残留的低信号 page-global guidance 文本断言：在不改运行时代码的前提下，仅为 `WebhooksPage.tsx` 顶部角色说明卡补充最小 `webhooks-role-guidance` 语义锚点，并把 `重点关注 failed / pending 重试链路与 last_error` 的 proof 从 page-global `screen.getByText(...)` 收紧为该 guidance 卡片内部的 scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、管理员 Webhook 运维观测页指标/角色提示与真实 API 菜单真值不变，而不是继续依赖整页同名说明文案永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/App.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_guidance_scope_real_api.sh`（产物目录 `webhooks-guidance-scope/20260506-053655`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；controller-side 复核与 repo-scoped 独立评审一致确认当前 diff 仅为 `WebhooksPage.tsx` 顶部角色说明卡补充最小 `webhooks-role-guidance` 语义锚点，并把 `WebhooksPage.test.tsx` 中管理员 guidance proof 从低信号 page-global 文本命中收敛为更贴近运行时结构的 role-guidance 卡片 scoped 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 Semi UI `findDOMNode`、React `act(...)` 噪声与前端 chunk 体积告警；下一前端优先项继续清理 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-04）
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中 bootstrap failure focused 用例仍残留的低信号文本等待：在不改运行时代码的前提下，仅把 `clears local session on bootstrap failure without revoking refresh session server-side` 用例里对登录页回退的 proof 从 `waitFor + getByText('登录 Nexus-Mail')` 收紧为 `findByRole('heading', { name: '登录 Nexus-Mail' })`，继续保持 new-api 风格单一登录后深色共享控制台、登录壳 heading 语义与 bootstrap failure 时仅清本地会话而不触发服务端 refresh revoke 的真实合同不变，而不是继续把宽泛文本节点与额外轮询包装当作该 focused 用例的主真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_bootstrap_heading_real_api.sh`（产物目录 `app-bootstrap-heading-real-api/20260506-043720`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 bootstrap failure 登录页回退 proof 从宽泛 `getByText` + 额外 `waitFor` 轮询收敛为更贴近运行时可访问语义的 heading 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 复用与非语义文本节点导致的误报。继续保留既有 Semi UI `findDOMNode`、局部 React `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`LoginPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号文本等待 / page-global 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中仍残留的低信号 page-global proof：在不改真实权限、菜单、路由与 API 合同的前提下，仅为 `DashboardPage.tsx` 的角色菜单映射卡与管理员运营摘要卡补充最小 `dashboard-role-surface-map` / `dashboard-admin-ops-summary-card` 语义锚点、为 `ProfilePage.tsx` 的左侧身份主卡补充 `profile-primary-identity-card` 锚点，并把 App 层 supplier / admin focused 用例从 page-global 文本与未分区按钮查询收紧为更贴近运行时结构的 scoped proof，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、供应商资料页→域名管理与管理员总览统计真值不变，而不是继续依赖整页同名 copy 或无作用域按钮永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_focused_scope_real_api.sh`（产物目录 `app-focused-scope/20260506-040529`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/profile`、`/supplier/domains`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、供应商资料页联动、管理员总览/风控/审计入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；代码质量评审曾指出新增 `profile-scene-card` 锚点未被实际测试消费，控制器已在提交前移除该冗余锚点并复跑 `src/App.test.tsx`、前端 build、`go test ./...` 与真实 API 回放确认全部通过。评审一致认为当前 diff 仅把 App 层 supplier profile / supplier dashboard / admin dashboard deep statistics proof 从低信号 page-global 查询收敛为更贴近运行时结构的 identity-card / route-map / ops-summary scoped 合同，不改变运行时权限、导航或真实 API 合同，同时继续保留既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx` 中管理员风控页仍残留的 `规则命中概览` / `处置建议` / `高风险` / `API Key 白名单拦截频繁` 等低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中 supplier dashboard focused 用例仍残留的低信号 page-global heading 断言：在不改运行时代码的前提下，仅把该用例对 `供应商主任务` 的可达性 proof 从宽泛 `findByText(...)` 收紧为 `heading` 语义合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、供应商首页 → 域名管理单壳路径与真实 API 菜单真值不变，而不是继续把任意同名文本节点当作 supplier dashboard focused 的主真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_heading_scope_real_api.sh`（产物目录 `app-heading-scope/20260505-023331`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/docs`、`/webhooks`、`/api-keys`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；其中 controller-side 复核已纠正一份外部 reviewer 对 focused Vitest 结果的误读，当前以控制器实测为准确认 `src/App.test.tsx`、`src/pages/ApiDocsPage.test.tsx`、`src/pages/WebhooksPage.test.tsx` 共 41 项用例全部通过。评审一致认为当前 diff 仅把 supplier dashboard focused proof 从 page-global 文本命中收敛为更贴近运行时结构的 heading 合同，不改变运行时权限、导航或真实 API 合同，同时继续保留既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx` 中 admin metrics 仍残留的 page-global 指标/说明文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `WebhooksPage.test.tsx` 中 admin metrics focused 用例仍残留的低信号 page-global 指标/说明断言：在保持单一登录后 new-api 风格深色共享控制台、角色菜单 gating 与 Webhook 管理员运维工作台真实合同不变的前提下，仅为三个指标卡片补充最小 `webhooks-endpoint-metric` / `webhooks-sent-delivery-metric` / `webhooks-attention-delivery-metric` 语义锚点，并将测试从 page-global `screen.getByText(...)` 收紧为指标卡片内部的 scoped metrics proof，避免未来页面其他区块复用相同指标/说明文案时产生误报，而不是继续依赖整页同名文本永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_role_heading_scope_real_api.sh`（产物目录 `webhooks-role-heading-scope/20260506-031547`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 metrics scoped 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；控制器已按当前 repo live diff 直接完成五维复核，并丢弃了三份错误引用其他仓库/其他变更内容的外部 delegate 结果，避免 cross-repo review drift 污染本仓库判断。评审一致认为当前 diff 仅为 Webhooks 管理员指标卡片补充最小语义锚点，并把 focused proof 从 page-global 文本命中收敛为更贴近运行时结构的 metric-card scoped 合同，不改变运行时权限、导航或真实 API 合同，同时继续保留既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例中仍残留的低信号 page-global 指标/说明文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中管理员 Webhook 首屏用例仍残留的低信号 page-global heading / metric 文本断言：在不改真实权限、菜单、路由与 API 合同的前提下，仅把 `renders webhook settings page for authenticated admin` 从宽泛 `findByText/getByText` 收紧为 `heading` 语义合同、`webhooks-endpoint-metric` / `webhooks-attention-delivery-metric` 指标卡 scoped proof，并为运行时 `当前 endpoint` 卡片补充最小 `webhooks-current-endpoints-card` 语义锚点，使 App 层回归校验不再依赖整页同名指标/标题永远唯一这一过宽假设，同时继续保持 new-api 风格单一登录后深色共享控制台与角色菜单 gating 真值不变。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_role_heading_scope_real_api.sh`（本轮产物目录 `webhooks-role-heading-scope/20260506-033815`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；其中 controller-side 复核已否决一份把旧 diff 状态当成当前 live 状态的测试/可靠性外部评审，最终以控制器实测为准确认 `src/App.test.tsx`、`src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway` 与真实 API 回放全部通过。评审一致认为当前 diff 仅把 App 层管理员 Webhook 首屏 proof 从低信号 page-global 文本命中收敛为更贴近运行时结构的 heading / metric-card / current-endpoint-card scoped 合同，不改变运行时权限、导航或真实 API 合同，同时继续保留既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx` 其余 shared-console focused 用例中仍残留的低信号 page-global heading / metric 文本断言，再回到 Phase 4/5 其余后端能力。

- 本轮继续按"前端优先"收敛 Docs ↔ Webhooks shared-console focused 合同中仍残留的低信号 page-global / 长文案断言：在不改运行时代码的前提下，仅把 `WebhooksPage.test.tsx` 的首轮 integration loop proof 从 page-level 文案快照收紧为 `webhooks-first-integration-loop` 区域内 CTA + 无 fallback 合同，把 `ApiDocsPage.test.tsx` 的 docs mission-control proof 从 page-global `OpenAPI 3 / Redoc` / `API Keys` / `Webhook 设置` 文本等待收紧为 iframe surface + `docs-shared-console-bridge` / `docs-shared-console-loop` 区域内 CTA 合同，并把 `App.test.tsx` 中 supplier 无首选落点时回退到 docs 的证明从宽泛 `API 文档` 文本命中收紧为 `nexus-mail-api-docs` iframe 真正落点；继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与 Docs → API Keys / Webhooks 单壳桥接不变，而不是继续依赖易重复页面文本当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/pages/ApiDocsPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_docs_webhooks_loop_contract_real_api.sh`（产物目录 `docs-webhooks-loop-contract/20260505-015731`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints` 与前端 `/`、`/docs`、`/webhooks`、`/api-keys` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、接入链路或单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Docs ↔ Webhooks shared-console focused proof 从低信号 copy 命中收敛为更贴近运行时结构/行为的 iframe、CTA presence/absence 与 docs 落点合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案润色与重复文本导致的误报。继续保留既有 Semi UI `findDOMNode`、局部 React `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx` 中 admin metrics 仍残留的 page-global 指标/说明文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ApiDocsPage.test.tsx` 中 docs bridge / loop focused 用例仍残留的低信号全局 copy 断言：在不改运行时代码的前提下，仅把 bridge / loop 顶层叙事 copy 的 page-global `screen.getByText(...)` 等待收紧为 `docs-shared-console-bridge` / `docs-shared-console-loop` 语义区域内的 scoped 文本 + CTA 合同，同时保留 `回到项目市场校验真实业务输入`、`收敛最小权限 API Key`、`完成 Webhook 回调联调`、`返回资金工作台核对预算与售后`、`先回到 API Keys 收口最小权限`、`随后校验 Webhook delivery`、`最后回到业务主链路复放` 等关键用户可见桥接/闭环标题，继续保持 new-api 风格单一登录后深色共享控制台、Docs→API Keys→Webhook→业务回放单壳连续路径与真实菜单 gating 不变，而不是继续依赖页面其他区域可能复用的同名文本节点。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_docs_webhooks_signal_scope_real_api.sh`（最新产物目录 `api-docs-webhooks-signal-scope/20260505-005827`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/docs`、`/api-keys`、`/webhooks` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一度指出若完全移除 bridge/loop 卡片标题断言会削弱 Docs 工作台的用户可见桥接语义保护，控制器已改为保留关键 lane 内标题并把断言收紧到 `docs-shared-console-bridge` / `docs-shared-console-loop` 区域内后复验通过。继续保留既有 Semi UI `findDOMNode`、局部 React `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`App.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SettingsPage.test.tsx` 中普通用户首轮引导 focused 用例仍残留的低信号首屏文本等待：在不改运行时代码的前提下，仅把 `首次使用清单` 首屏 proof 从普通 `findByText(...)` 文本等待收紧为 `heading` 语义合同，同时保留 checklist 容器、重新打开首轮引导、共享首页回流与 supplier CTA 不泄漏合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与角色菜单 gating 不变，而不是继续把任意同名文本节点当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/pages/SettingsPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_docs_settings_heading_scope_real_api.sh`（产物目录 `app-docs-settings-heading-scope/20260504-234140`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/docs`、`/api-keys`、`/webhooks`、`/settings`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前实际 diff 仅把 `SettingsPage.test.tsx` 中普通用户首轮 checklist 的首屏等待从宽泛文本查询收紧为更贴近运行时结构的 heading 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案复用与非语义文本节点导致的误报。继续保留既有 Semi UI `findDOMNode`、局部 React `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`ApiDocsPage.test.tsx`、`App.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SupplierOfferingsPage.test.tsx` 中仍残留的装饰性 Mission Control 标签等待：在不改运行时代码的前提下，仅把 supplier 供货页 focused 用例从 `Supplier Mission Control` 装饰标签等待收紧为 `供货规则编排中枢` heading 语义合同，并将首屏任务流存在性从普通文本节点升级为 `供应商主任务流` heading 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、供货规则→资源→结算→API Keys 单壳连续路径与真实 API 菜单真值不变，而不是继续把英文装饰标签当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_supplier_offerings_heading_scope_real_api.sh`（产物目录 `supplier-offerings-heading-scope/20260504-201756`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/supplier/offerings`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 SupplierOfferings focused 用例从装饰性 Mission Control 标签等待收敛为更贴近运行时结构的 heading 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 视觉标签/文案微调导致的误报。继续保留既有 Semi UI `findDOMNode`、`async-validator` 局部噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`App.test.tsx`、`LoginPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ProjectsPage.test.tsx`、`OrdersPage.test.tsx` 与 `AdminUsersPage.shared-console.test.tsx` 中仍残留的装饰性 tag / 低信号长说明等待：在不改运行时代码的前提下，仅把采购页与订单页 focused 用例从 `共享控制台采购切片` / `共享控制台履约切片` 等装饰性标签等待收紧为 `项目市场` / `订单中心` heading 语义合同，并把管理员资金页首屏 proof 从英文装饰标签 `Admin Finance Mission Control` 收紧为 `用户管理` heading，同时保留 `admin-users-shared-console-bridge` 区域内共享联动长说明与 API Keys / Webhook / Docs 桥接标签真值，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、管理员资金 mission-flow 与采购/履约→接入单壳连续路径不变，而不是继续把装饰性标签或 page-global 长说明当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx src/pages/AdminUsersPage.shared-console.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_test_heading_scope_real_api.sh`（最新产物目录 `front-test-heading-scope` 下本轮时间戳目录），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs`、`/admin/users` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Projects / Orders / AdminUsers 三个 focused 用例从装饰性标签与过宽长说明等待收敛为更贴近运行时结构的 heading / bridge 区域合同，同时保留管理员资金页共享联动文案与桥接标签真值，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、局部 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`App.test.tsx`、`LoginPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ApiDocsPage.test.tsx` 中仍残留的低信号首屏/区域文案等待：在不改运行时代码的前提下，仅把管理员 docs 角色校验从整段 admin 说明 copy 收紧为稳定 `Docs Mission Control` heading 合同，并把 bridge/loop 多轮重渲染用例的 readiness proof 分别收紧为 `docs-shared-console-bridge` / `docs-shared-console-loop` 语义区域存在性，而不再反复等待 `共享控制台桥接路径`、`文档与接入工作台继续保持单壳闭环` 这类易随 copy 润色变化的文本；继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Docs → API Keys / Webhooks / Balance / 推荐工作台 单壳桥接与回流合同不变，而不是继续把装饰性区域标题或长说明 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_docs_front_scope_real_api.sh`（最新产物目录 `api-docs-front-scope/20260504-195856`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/docs`、`/api-keys`、`/webhooks`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 ApiDocs focused 用例从易变 admin 说明 copy 与 bridge/loop 区域文本等待收敛为更贴近运行时结构的 heading / 区域级 readiness 合同，同时保留 bridge/loop CTA 与导航真值，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`LoginPage.test.tsx`、`SupplierResourcesPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `AdminAuditPage.test.tsx` 首个 shared-console focused 用例中仍残留的低信号 page-global 长说明 / 标题断言：在不改运行时代码的前提下，仅把审计工作台首屏 proof 从 `Audit Mission Control` 装饰标签、整段 `/api/v1/admin/audit` 说明文案、若干 page-global 指标/区块标题文本，收紧为 `heading` 语义合同、`admin-audit-mission-flow` 区域内三条真实动作按钮、`admin-audit-shared-console-bridge` 区域内 API Keys / 风控中心 / API 文档 三个共享桥接标签，以及 `admin-audit-events-table-card` 内真实审计事件合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、审计→风控/资金/API Keys 单壳联动与真实 API 菜单真值不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminAuditPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_audit_heading_scope_real_api.sh`（产物目录 `admin-audit-heading-scope/20260504-162607`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 AdminAudit focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED（仅建议后续若继续强调 section heading 可在单独切片补更精确标题断言）、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 AdminAudit 首个 focused 用例从 page-global 长说明 copy 收敛为更贴近运行时结构的 heading / mission-flow / bridge / table 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案润色与重复文本导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `SupplierSettlementsPage.test.tsx`、`ProjectsPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ProjectsPage.test.tsx` 与 `OrdersPage.test.tsx` 首个 shared-console hero focused 用例中仍残留的低信号 page-global 长说明断言：在不改运行时代码的前提下，仅把采购 hero / 履约 hero 的首屏 proof 从整页长段落 copy 收紧为 `projects-shared-console-hero` / `orders-shared-console-hero` 语义区域内的 heading + 稳定 hero signal 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、采购→履约→API 接入单壳连续路径与真实 API 返回信号不变，而不是继续把高频润色说明 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_projects_orders_hero_scope_real_api.sh`（产物目录 `projects-orders-hero-scope/20260504-160115`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、`GET /api/v1/projects/inventory`、`GET /api/v1/orders/activations`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 hero focused 收敛未破坏真实注册、共享菜单、采购/履约接口、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Projects/Orders 首个 hero 用例从 page-global 长说明 copy 收敛为更贴近运行时结构的 hero 区域 heading / signal 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 微调与页面其他区块复用同名文案导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `AdminRiskPage.test.tsx`、`AdminAuditPage.test.tsx`、`SettingsPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例中仍残留的长说明 / page-global 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ApiDocsPage.test.tsx` 首个 focused 用例中仍残留的低信号 page-global 文案断言：在不改运行时代码的前提下，仅把普通用户 docs mission-control 校验从 `共享控制台 · API 契约`、`注册后连续路径`、长说明 copy 与多条 page-global surface 文案，收紧为 `heading` 语义合同、OpenAPI / API Keys / Webhook 三个稳定 surface 标签、`docs-shared-console-bridge` / `docs-shared-console-loop` 两个真实区域内 CTA scoped 合同与 API Keys 导航落点，同时将测试标题改为更准确地描述 bridge + loop CTA proof，继续保持 new-api 风格单一登录后深色共享控制台、文档→接入→业务回放单壳闭环与真实菜单 gating 合同不变，而不是继续把易变长文案当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx`、`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/pages/LoginPage.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_docs_front_scope_real_api.sh`（产物目录 `api-docs-front-scope/20260504-153557`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/docs`、`/api-keys`、`/webhooks` SPA 壳 200 均保持成立，本轮 docs focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；安全/集成评审一度指出若完全删掉 docs surface 合同，可能削弱对共享控制台文档入口与菜单 gating 的防回归灵敏度，控制器已补回 `OpenAPI 3 / Redoc`、`API Keys`、`Webhook 设置` 三个稳定 surface 标签断言，并把首个测试标题收敛为更准确的 bridge + loop CTA proof 后复验通过。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`LoginPage.test.tsx` 等 shared-console focused 用例中仍残留的低信号长说明 / page-global 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ApiKeysPage.test.tsx` 与 `SupplierSettlementsPage.test.tsx` 中仍残留的低信号 page-global / 结构耦合断言：为 `ApiKeysPage` 运行时代码最小补充 `api-keys-hero-card` 与 `api-keys-shared-console-bridge` 语义锚点，并将 supplier 角色接入提示、共享接入桥接 CTA 合同从 `.closest('.semi-card')` / page-global 查询收紧为对应卡片 scoped 断言；同时把 `SupplierSettlementsPage` 的 mission-control 壳层校验从英文装饰标签与 page-global step 文案，收敛为中文主 heading + `supplier-settlements-mission-flow` 区域内步骤合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、API Keys/Webhook/Docs 共享桥接与 supplier 资金闭环不变，而不是继续依赖第三方卡片包装类名或整页同名文案永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_keys_supplier_settlements_focus_real_api.sh`（产物目录 `api-keys-supplier-settlements-focus/20260504-145949`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers`、`/supplier/settlements` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商财务入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；首轮代码质量评审指出 `ApiKeysPage.test.tsx` 新增的 `.closest('.semi-card')` 会把 scoped 断言重新耦合到 Semi UI 包装类名，控制器已在运行时代码补充最小 testid 并改为语义区域 scoped 查询后复验通过。其余评审一致认为当前 diff 仅为 API Keys hero/bridge 卡片补充最小语义锚点，并把 supplier finance / API Keys focused proof 收紧到更贴近运行时结构的 heading / bridge / mission 区域，不改变权限、导航或真实 API 合同；既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警继续记为非阻塞债务。下一前端优先项转为继续清理 `LoginPage.test.tsx`、`WebhooksPage.test.tsx`、`ApiDocsPage.test.tsx` 等 shared-console focused 用例中仍残留的长说明 / page-global copy 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ProfilePage.test.tsx` 中 profile shared-console return/fallback focused 用例仍残留的低信号长说明 copy 断言：在不改运行时代码的前提下，仅把 supplier/admin 返回卡片场景从 `供应商角色扩展` / `管理员角色扩展` 与整段说明文案断言，收紧为角色焦点 heading + 返回 CTA + 导航落点 + user-only heading 不泄漏合同；同时把 user/supplier fallback 返回卡片从长说明段落断言收紧为卡片内标题文本 + 返回按钮合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与 profile→settings / profile→shared-home 回流语义不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_profile_heading_scope_real_api.sh`（产物目录 `profile-heading-scope/20260504-143042`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 Profile focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Profile 返回/回流 focused proof 从 page-global 长说明 copy 收敛为更贴近运行时结构的 heading / CTA / 导航落点合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 微调导致的误报。非阻塞建议仅包括：若后续仍想覆盖返回卡片语义，可考虑补更短的区域内标签/标题断言而非整段说明文案；既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警继续记为债务。下一前端优先项转为继续清理 `SupplierSettlementsPage.test.tsx`、`ApiKeysPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明/页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中普通用户首轮引导 heading 断言与 dismiss 合同：在不改运行时代码的前提下，仅把两处 `欢迎进入共享控制台` page-global 文本查询改为复用 `expectDefaultUserFirstRunLane()` 的 lane scoped 合同，并将 dismiss 校验收紧为 `localStorage` 已写入 + heading 语义消失且 shared next-steps lane 仍保留，从而继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与真实注册 / 首轮引导回流合同不变，而不是继续把易变文案节点本身当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_first_run_lane_heading_contract_real_api.sh`（产物目录 `app-first-run-lane-heading-contract/20260504-130754`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮首轮引导 heading focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把普通用户首轮引导 proof 从脆弱的 page-global 文本查询收敛为更贴近运行时结构/状态变化的 heading + lane 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 copy 微调导致的 focused 误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ProfilePage.test.tsx`、`SupplierSettlementsPage.test.tsx` 等 shared-console focused 用例里仍残留的长说明 copy / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中普通用户首轮引导仍残留的低信号长文案断言：在不改运行时代码的前提下，仅把 `shows first-run onboarding guidance for default user dashboard and allows dismissing it` 用例从 `欢迎进入共享控制台`、角色说明句与两条旅程 helper copy 的 page-global 文本断言，收紧为 `expectDefaultUserFirstRunLane()` 区域内余额/项目/订单/API Keys 四个真实 CTA 存在、API Keys 集成卡片 heading 合同与 `打开 Webhook 设置` 不泄漏的 scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与预算→采购→履约→接入 单壳路径不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_first_run_lane_scope_real_api.sh`（产物目录 `app-first-run-lane-scope/20260504-122259`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮首轮引导 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把普通用户首轮引导 proof 从易变长文案收敛为更贴近运行时结构/行为的 CTA 与集成卡片合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 copy 微调导致的 focused 误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ProfilePage.test.tsx`、`SettingsPage.test.tsx` 等 shared-console focused 用例里仍残留的长说明 copy / page-level 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 与 `LoginPage.test.tsx` 中登录/注册共享壳仍残留的低信号长文案断言：在不改运行时代码的前提下，仅把 register journey proof 从供应商说明段落、权限说明长句与注册 helper 长文案，收紧为 `login-register-journey` 区域内的 heading/button 结构合同与切换到注册态后的 submit CTA 合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色路径说明与真实注册链路不变，而不是继续把易变营销/说明 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_login_register_shell_scope_real_api.sh`（产物目录 `login-register-shell-scope/20260504-113622`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu` 与前端 `/`、`/login`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮登录/注册共享壳 focused 收敛未破坏真实注册、共享菜单、管理员菜单与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把登录/注册共享壳 proof 从易变长文案收敛为更贴近运行时结构的 heading/button 合同，不改变权限、导航或真实 API 契约，同时进一步降低 copy 微调导致的 focused 误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`DashboardPage.test.tsx` 等 shared-console focused 用例中仍残留的长文案 / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ProjectsPage.test.tsx`、`OrdersPage.test.tsx` 与 `SupplierDomainsPage.test.tsx` 中仍残留的低信号隐藏态 / 失败态文案断言：在运行时代码仅为 SupplierDomains 区域统计卡片补充最小 `supplier-domains-region-metrics` 语义锚点的前提下，将采购 hero 与履约 hero 的 hidden-state proof 分别从长说明文案缺失收紧为对应 hero 区域内 `查看 API 文档` / `打开 API Keys` 按钮级缺失断言，并把 SupplierDomains overview 失败态从 page-global 区域统计文案检查收紧到 table-card + region-metrics 两个真实区域 scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、采购→履约→接入连续路径与供应商域名运营页错误态合同不变，而不是继续把易变 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx src/pages/SupplierDomainsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_hero_copy_scope_real_api.sh`（产物目录 `front-hero-copy-scope/20260504-111434`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/projects`、`/orders`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商域名运营入口与单壳前端连续路径。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Projects/Orders 两个 hero 的隐藏态 proof 收敛为更贴近真实交互的按钮级合同，并为 SupplierDomains 区域统计卡片补充最小语义锚点后把 overview 失败态断言限制到真实区域，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 细调与 page-global 重复文案导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `AdminSuppliersPage.test.tsx`、`AdminUsersPage.shared-console.test.tsx`、`AdminRiskPage.test.tsx`、`AdminAuditPage.test.tsx` 等 bridge tag/route 文本缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `DashboardPage.test.tsx` 中共享控制台 next-steps lane 仍残留的低信号长说明文案断言：在不改运行时代码的前提下，仅删除对 `采购 → 订单 → 接入` 推荐顺序说明与 `继续进入 API Keys、Webhook 与文档` 集成长句的逐字断言，保留 `dashboard-next-steps-lane` 区域内四步 heading/按钮存在性、隐藏态与真实导航合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与共享旅程 lane 行为不变，而不是继续把容易随文案润色变化的说明句当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_dashboard_lane_scope_real_api.sh`（产物目录 `dashboard-lane-scope/20260504-094629`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 Dashboard lane focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅删除两条容易随 copy 微调波动的长说明断言，保留共享旅程 lane 的 step/CTA/导航主合同，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `OrdersPage.test.tsx`、`ProjectsPage.test.tsx` 等 shared-console focused 用例中仍残留的长说明/通用文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `LoginPage.test.tsx` 中注册壳 shared journey 的低信号长文案断言：在不改运行时代码的前提下，仅保留 `login-register-journey` 区域内的 heading 语义合同（`注册后默认进入共享控制台`、`用户路径`、`供应商路径`、`管理员路径`），删除对营销式接入说明与中英混排 runway copy 的逐句快照式断言，并继续保留 CTA 切换到注册态、`注册 Nexus-Mail` 标题与注册 helper 文案，从而保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与角色路径说明合同不变，同时降低未来登录页 copy 微调带来的 focused 误报。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`（产物目录 `app-login-register-scope/20260504-092530`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮登录壳 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把注册壳 journey proof 从长文案快照收敛为更贴近运行时结构的 heading 语义合同，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `DashboardPage.test.tsx`、`OrdersPage.test.tsx` 等 shared-console focused 用例中仍残留的低信号长说明文案断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中登录/注册共享壳仍残留的 page-global 长文案断言：在不改运行时代码的前提下，仅复用既有 `login-register-journey` 语义锚点，把 `shows register journey guidance and role entry descriptions on the login shell` 用例从整页 `getByText(...)` 查询收紧为区域内 heading/button/text scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、注册入口与角色路径说明合同不变，而不是继续依赖整页同名文案永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`（产物目录 `app-login-register-scope/20260504-090240`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮登录/注册共享壳 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；产品评审仅提出一项非阻塞建议：若未来把"用户路径"辅助说明文案也视为关键 UX，可再补一条更精确的区域内文案断言；其余评审一致认为当前 diff 仅把登录壳 journey proof 收紧到现有语义区域，不改变运行时权限、导航或真实 API 合同，同时进一步降低未来登录页重复 copy 导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`LoginPage.test.tsx`、`DashboardPage.test.tsx` 等 shared-console focused 用例中仍残留的低信号长文案 / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ProfilePage.test.tsx` 与 `SettingsPage.test.tsx` 中仍残留的 DOM 包装依赖：在不改运行时代码的前提下，仅去掉 supplier/admin 资料页对 `供应商运营焦点` / `管理员运营焦点` 标题 `parentElement` 的结构遍历，改为直接用 user-only heading `采购与订单串联` 的缺失来证明角色焦点未串台；同时把设置页"重新打开首轮引导"后的共享首页验证从 `parentElement` 容器遍历收敛为页面级 supplier 快捷按钮缺失 + checklist 消失合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与角色菜单 gating 合同不变，只降低 focused 用例对测试 stub 包装节点的依赖。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/SettingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_profile_settings_scope_real_api.sh`（产物目录 `profile-settings-scope/20260504-075341`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 去结构耦合未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；代码质量评审仅提示 `ProfilePage.test.tsx` 中 `采购与订单串联` 的页面级 heading 缺失断言未来若出现重复 heading 可再收紧到更小语义区域，但当前 diff 已去除 `parentElement` 脆弱遍历且未削弱角色/菜单真值保护；继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务。下一前端优先项转为继续清理其余 shared-console focused 用例中仍残留的 `parentElement` / 低信号 page-global 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `AdminSuppliersPage.test.tsx` 中 shared-console bridge 标题断言的结构耦合：在不改运行时代码的前提下，仅把 `共享接入桥接` 的 focused 校验从依赖 Semi UI `.closest('.semi-card')` 包装层，调整为 heading 语义断言 + `admin-suppliers-shared-console-bridge` 区域内的 shared link scoped 查询，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、管理员供应商运营 mission-flow 与 shared bridge 合同不变，而不是继续把第三方 UI 包装 DOM 形状当作测试真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_suppliers_bridge_scope_real_api.sh`（产物目录 `admin-suppliers-bridge-scope/20260504-063825`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、`GET /api/v1/admin/wallet-users`、`GET /api/v1/admin/disputes` 与前端 `/`、`/admin/suppliers`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 bridge 标题断言去结构耦合未破坏注册后共享菜单、真实 API、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一度指出把 bridge 标题断言降级为 page-global 文本查询会削弱局部合同，控制器已收敛为 heading 语义断言并保留 `admin-suppliers-shared-console-bridge` 区域内的 shared link scoped 查询后复验通过。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `LoginPage.test.tsx`、`App.test.tsx` 等其余 shared-console focused 用例中仍残留的 `.closest('.semi-card')` / 低信号 page-global copy 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `LoginPage.test.tsx` 与 `SettingsPage.test.tsx` 中仍残留的结构/整页级脆弱断言：为登录页注册后共享控制台旅程卡片补充最小 `login-register-journey` 语义锚点，使 focused 用例不再依赖 `.closest('.semi-card')`；同时为设置页快捷入口卡片补充 `settings-shortcut-cards` 语义锚点，并将普通用户重开首轮引导后对供应商快捷动作的缺失判断收紧到跳转落点容器而非整页全局扫描，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与角色菜单 gating 合同不变，只降低第三方 UI 包装结构与整页同名按钮造成的 focused 误报风险。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/pages/SettingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_login_settings_front_scope_real_api.sh`（产物目录 `login-settings-front-scope/20260504-065856`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 去脆弱化未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审仅提出两项非阻塞建议：`settings-shortcut-cards` 语义锚点后续可在对应 focused 用例中直接利用、设置页跳转落点容器未来可改为更稳定的 route stub 锚点；其余评审一致认为当前 diff 仅补充最小语义锚点并收紧 focused 断言作用域，不改变运行时权限、导航或真实 API 合同，同时继续把既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警记为非阻塞债务。下一前端优先项转为继续清理 `App.test.tsx` 等其余 shared-console focused 用例里仍残留的 page-global copy / CTA 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”补上 shared-console 导航契约层的 focused 回归：新增 `web/src/utils/consoleNavigation.test.tsx`，把 `/api-keys`、`/webhooks`、`/docs`、`/profile` 等共享壳关键路由常量、shared/supplier/admin 分组、角色 landing 候选、preferred landing 解析、quick actions 顺序与 route key/path 唯一性统一钉死为单一测试入口，避免 shared shell / App / mission-control 页面仅靠各自页面测试而让导航常量或菜单契约静默漂移；该切片不改运行时 UI/交互，只补上 new-api 风格单壳共享控制台最底层的路由/菜单合同保护。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/utils/consoleNavigation.test.tsx src/App.test.tsx src/pages/LoginPage.test.tsx src/pages/DashboardPage.test.tsx src/pages/AdminAuditPage.test.tsx src/pages/AdminSuppliersPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_console_navigation_contract_real_api.sh`（产物目录 `console-navigation-contract/20260504-052833`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮契约测试补强未破坏注册后共享菜单、真实 API、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 staged diff 仅新增导航契约测试文件，能把 shared-console 底层路由/菜单真值从分散页面用例中抽离为单点保护，不改变运行时权限、导航或真实 API 合同；非阻塞债务仍包括既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警。下一前端优先项转为继续清理 `DashboardPage.test.tsx`、`AdminSuppliersPage.test.tsx`、`LoginPage.test.tsx` 等 focused 用例里仍残留的低信号 route/copy 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `ProfilePage.test.tsx`、`SupplierDomainsPage.test.tsx` 与 `ApiDocsPage.test.tsx` 中仍残留的低信号 page-global fallback / copy 断言：在不改运行时逻辑的前提下，仅删除 `ProfilePage` 与 `SupplierDomainsPage` 中对 fallback 容器已缺失时重复的 page-level `返回共享工作台` / `返回推荐工作台` 按钮负断言，并将 `ApiDocsPage` 管理员文档桥接断言收紧为 `docs-shared-console-bridge` 区域内的按钮级 scoped 查询，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、注册后统一壳与 docs→integration bridge 合同不变，而不是继续依赖整页同名按钮/文案永远不存在这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/ApiDocsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_scope_docs_profile_domains_real_api.sh`（产物目录 `front-scope-docs-profile-domains/20260504-024841`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/profile`、`/supplier/domains`、`/docs` SPA 壳 200 均保持成立，本轮 focused 去脆弱化未破坏注册后共享菜单、真实 API、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅删除在 fallback 容器缺失时冗余的页面级按钮负断言，并把管理员文档桥接断言收紧到真实桥接区域，不改变运行时权限、导航或真实 API 合同，同时进一步降低未来重复文案与多区域同名 CTA 导致的误报。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`AppSidebar.test.tsx`、`ConsoleLayout.test.tsx` 等 shared shell / route guard 用例里仍残留的 page-global copy / menu 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `AppSidebar.test.tsx`、`ConsoleLayout.test.tsx` 与 `App.test.tsx` 中 shared shell / route guard 仍残留的低信号 page-global / menu-global 缺失断言：在不改运行时代码的前提下，仅将 `AppSidebar` 供应商菜单与加载态场景里的 `风控中心` / `API 文档` 缺失判断收紧到真实分组卡片与加载卡片，将 `ConsoleLayout` 当前页快捷操作排除校验保持在壳层按钮合同内，并将 `App.test.tsx` 中等待服务端菜单、注册后普通用户壳层、supplier 设置页 checklist 等缺失断言收紧到 `main[aria-label="控制台主内容"]` 或实际导航菜单区域，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、注册后统一壳与真实共享路由合同不变，而不是继续依赖整页/整壳永远不存在同名菜单或 checklist 文案这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_shell_scope_real_api.sh`（产物目录 `app-shell-scope-hardening/20260504-032716`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 shared shell 作用域收敛未破坏注册后共享菜单、真实 API、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；首轮代码质量评审曾建议不要把 `ConsoleLayout` 快捷操作断言降级为纯文本数组扫描，控制器已恢复为真实按钮级 role 合同；其余评审一致认为当前 diff 仅把 shared shell 缺失断言收敛到更贴近运行时语义的主内容区、菜单区与加载/分组卡片，不改变运行时权限、导航或真实 API 合同，同时进一步降低未来重复文案与共享壳多区域同名菜单导致的误报。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ProtectedRoute.test.tsx`、`DashboardPage.test.tsx`、`SettingsPage.test.tsx` 等 shared shell / role guard 用例里仍残留的 page-global copy / route guard 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `ProjectsPage.test.tsx`、`OrdersPage.test.tsx`、`AdminProjectsPage.test.tsx` 与 `SupplierDomainsPage.test.tsx` 中仍残留的 hero / mission / fallback 结构耦合：为采购 hero 与履约 hero 运行时代码最小补充 `projects-shared-console-hero`、`orders-shared-console-hero` 语义锚点，并将对应 focused 用例从 `.closest('.semi-card')` 收紧为 hero 区域 scoped 断言；同时将管理员价格页“仅保留 fallback CTA”场景改为显式断言 `admin-pricing-mission-flow` 不存在，并继续在 `main[aria-label="控制台主内容"]` 范围内确认 `查看风控中心` / `查看审计日志` / `打开 API Keys` 不泄漏；供应商域名页则保留 mission/shared fallback testid 缺失与 page-level `返回推荐工作台` 按钮缺失断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与单壳导航合同不变，只降低 Semi UI 包装结构漂移造成的 focused 误报风险。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx src/pages/AdminProjectsPage.test.tsx src/pages/SupplierDomainsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_hero_scope_tightening_real_api.sh`（产物目录 `front-hero-scope-tightening/20260504-022144`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/projects/inventory`、`GET /api/v1/orders/activations`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、`GET /api/v1/admin/wallet-users`、`GET /api/v1/admin/disputes` 与前端 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs`、`/admin/users`、`/admin/pricing`、`/supplier/domains` SPA 壳 200 均保持成立，本轮 hero / mission / fallback 语义锚点收敛未破坏注册后共享菜单、真实采购/履约/管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、性能/运维 PASS；首轮评审曾阻断管理员价格页为测试方便而无条件渲染空任务流容器以及去掉供应商域名页 page-level fallback 按钮负断言，控制器已恢复运行时条件渲染、补回 `main` 作用域下的 CTA 缺失校验与域名页 page-level fallback 保护后复验通过。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)`、Semi Form `setValues()` 挂载时序与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`ProfilePage.test.tsx`、`SettingsPage.test.tsx` 等页中仍残留的 page-global copy / CTA 缺失断言与重复文本脆弱选择器，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `AdminUsersPage.shared-console.test.tsx` 中管理员资金工作台 shared bridge 的结构耦合：为运行时代码最小补充 `admin-users-shared-console-bridge` 语义锚点，并将 fallback 场景里对 `API Keys · /api-keys`、`Webhook 设置 · /webhooks`、`API 文档 · /docs` 的隐藏校验，从 `共享接入桥接` heading 的 `.closest('.semi-card')` 包装结构依赖收紧为 bridge 区域内的 scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、管理员资金 mission-flow 与 shared-console fallback 合同不变，只降低 Semi UI 包装结构变动导致的 focused 误报风险。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_users_bridge_scope_real_api.sh`（产物目录 `admin-users-bridge-scope/20260504-013238`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、`GET /api/v1/admin/wallet-users`、`GET /api/v1/admin/disputes` 与前端 `/`、`/admin/users`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 bridge 语义锚点收敛未破坏注册后共享菜单、管理员真实运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅为管理员资金页共享桥接卡片补充最小语义锚点，并把 focused 测试从 `.closest('.semi-card')` 收紧为更贴近运行时合同的 bridge 区域 scoped 断言，不改变权限、导航或真实 API 合同，同时进一步降低 future Semi UI 包装层调整带来的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `AdminProjectsPage.test.tsx`、`ProjectsPage.test.tsx`、`OrdersPage.test.tsx` 等页中仍残留的 `.closest('.semi-card')` hero / fallback 结构选择器，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `WebhooksPage.test.tsx` 中 shared-console empty fallback 的低信号 page-global CTA 缺失断言：保持单一登录后 new-api 风格深色共享控制台、角色菜单 gating 与 Webhook → API Keys / Docs 接入闭环合同不变，仅将 empty-state 场景下对 `先配置 API Keys` / `查看 API 文档` 的缺失判断从整页 `screen.queryByRole(...)` 收紧为 `webhooks-empty-state-actions` 语义区域内的 scoped role 断言，继续验证该 fallback 区域只保留 `返回推荐工作台` 动作，而不是继续依赖整页永远不存在同名 CTA 这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_fallback_scope_real_api.sh`（产物目录 `webhooks-fallback-scope/<timestamp>`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/webhooks`、`/api-keys`、`/docs` SPA 壳 200 均保持成立，本轮 focused 去脆弱化未破坏注册后共享菜单、真实 Webhook/API Keys 接口、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Webhooks empty fallback 的按钮缺失检查收敛到真实语义区域，避免 future bridge/loop 区域复用相同 CTA 文案时出现误报，也未改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ProfilePage.test.tsx`、`SettingsPage.test.tsx` 等 shared-console focused 用例里仍残留的 page-global copy / fallback 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `ApiDocsPage.test.tsx` 中 docs bridge / loop fallback 的低信号 copy 缺失断言：保持单一登录后 new-api 风格深色共享控制台、角色菜单 gating 与文档→接入→业务回放单壳合同不变，仅将“无 bridge 目标时回退推荐工作台”的 focused 用例从 `queryByText(...)` 文案缺失检查改为 `docs-shared-console-bridge` 与 `docs-shared-console-loop` 语义区域内的按钮级 role 断言，分别验证 bridge fallback 按钮存在、项目市场/API Keys/Webhook/余额中心按钮缺失，以及 loop 内只保留 `返回推荐工作台` 而不再泄露 API Keys / Webhook 动作。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_docs_bridge_fallback_real_api.sh`（产物目录 `api-docs-bridge-fallback/20260503-223130`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/docs`、`/api-keys`、`/webhooks` SPA 壳 200 均保持成立，本轮 docs fallback 作用域收敛未破坏注册后共享菜单、管理员真实运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 docs fallback 检查从桥接说明文案缺失收敛为更贴近运行时合同的按钮级/区域级断言，不改变运行时逻辑且进一步降低未来 copy 漂移带来的误报。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `AdminUsersPage.shared-console.test.tsx` 等页面中残留的长文案/低信号 shell copy 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `SupplierSettlementsPage.test.tsx` 中 supplier finance mission-control focused 回归的稳定性与生命周期合同：补充 `afterEach` 复位 auth store，避免多次重渲染/菜单覆写在后续用例间泄漏；同时将成本模型提交流程断言改为对当前真实初始化合同的验证——保留 `currency=CNY`、`status=active` 默认值并仅校验用户实际修改的项目键/成本字段，避免因对 Semi Form 默认字段的脆弱输入假设而把 focused 用例误判为前端回归，继续保持单一登录后 new-api 风格深色共享控制台、supplier/shared fallback 语义与真实供应商财务闭环不变。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API 回放脚本 `.hermes/local-output/run_supplier_settlements_milestone_real_api.sh`（产物目录 `supplier-settlements-milestone-20260503-225549/real-api`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/supplier/resources/overview`（预期 403）、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、`GET /api/v1/supplier/settlements`（200）均保持成立，本轮 focused 稳定性修复未破坏注册后共享菜单、真实资源权限边界、管理员运营接口与 supplier finance 单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审指出的唯一阻塞项是 `SupplierSettlementsPage.test.tsx` 对成本模型默认字段的脆弱输入假设与缺失的 store 清理，控制器已修正并复验通过。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`ApiKeysPage.test.tsx`、`SupplierDomainsPage.test.tsx` 等 focused 用例中残留的 page-global fallback/CTA 缺失断言与 act 噪声，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 shared-console bridge/fallback focused 回归中的低信号页面级负断言：仅调整 `SupplierOfferingsPage.test.tsx`、`SupplierDomainsPage.test.tsx` 与 `ApiDocsPage.test.tsx`，把“隐藏 bridge 入口/文案”的判断从页面级 `screen.queryByText/queryByRole` 收紧到既有 `supplier-offerings-shared-console-bridge`、`supplier-domains-mission-flow` / `supplier-domains-shared-console-bridge`、`docs-shared-console-bridge` 语义区域内，同时在 supplier domain / offerings 场景改为桥接按钮级 role 负断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与 bridge/fallback 分区合同不变，而不是继续依赖整页不存在同名桥接文案这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/ApiDocsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_shared_scope_bridge_real_api.sh`（产物目录 `front-shared-scope-bridge/20260503-214909`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/offerings`、`/supplier/domains`、`/supplier/settlements` SPA 壳 200 均保持成立，本轮 bridge 作用域收紧未破坏注册后共享菜单、管理员真实运营接口与 supplier/shared 单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 bridge 缺失检查收敛到真实语义区域与按钮级合同，减少未来页面其他区块重复文案导致的误报，也未削弱权限/menu 真值约束。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)`、`async-validator` 局部噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `SupplierSettlementsPage.test.tsx` 中剩余的 shared/mission fallback 负断言与按钮归属细节，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `SupplierSettlementsPage.test.tsx` 中 shared / mission fallback 的按钮归属合同：在保持运行时结构不变的前提下，仅删除一条会误判共享 fallback 外部行动按钮的过宽页面级负断言，保留并复验 `supplier-settlements-mission-fallback` 内唯一可点击 `返回推荐工作台` 与 `supplier-settlements-shared-console-fallback` 卡片内仅保留同名说明文案、无同名按钮的 scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与 fallback 区域职责不变，而不是继续依赖页面级按钮缺失假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_supplier_settlements_fallback_scope_real_api.sh`（产物目录 `supplier-settlements-fallback-scope/20260503-220837`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/supplier/resources/overview`（预期 403）、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/supplier/settlements` SPA 壳 200 均保持成立，本轮 fallback 归属收敛未破坏注册后共享菜单、真实资源权限边界、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅删除一条与运行时结构不一致的过宽负断言，并保留 mission/shared fallback 各自的 scoped 合同校验，从而降低 future copy / 外部动作区调整导致的误报，也未削弱权限、导航或真实 API 边界验证。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)`、`async-validator` 局部噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例里仍残留的 page-global fallback 文案/按钮缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛管理员价格页 / 管理员资金页 / 供应商供货页 focused 回归中的 page-global CTA 缺失断言：为 `AdminProjectsPage` 与 `SupplierOfferingsPage` 补充最小 `admin-pricing-mission-flow` / `supplier-offerings-mission-flow` 语义锚点，将 `AdminProjectsPage.test.tsx`、`AdminUsersPage.shared-console.test.tsx`、`SupplierOfferingsPage.test.tsx` 的 mission CTA 缺失检查收紧到 mission-flow 作用域，并把管理员价格页多跳导航测试改为显式 `view.unmount()` + fresh render，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与 bridge/fallback 分区合同不变，而不是继续依赖页面级同名按钮缺失假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx src/pages/AdminUsersPage.shared-console.test.tsx src/pages/SupplierOfferingsPage.test.tsx src/pages/ProfilePage.test.tsx src/pages/DashboardPage.test.tsx src/pages/WebhooksPage.test.tsx src/pages/AdminAuditPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_mission_flow_scope_real_api.sh`（产物目录 `front-mission-flow-scope/20260503-210030`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 以及前端 `/`、`/api-keys`、`/webhooks`、`/settings`、`/admin/risk`、`/admin/audit`、`/admin/pricing`、`/supplier/offerings` SPA 壳 200 均保持成立，本轮 mission-flow 作用域收敛未破坏注册后共享菜单、管理员真实运营接口与供应商供货页单壳入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅新增最小 mission-flow 语义锚点并把缺失 CTA 检查收敛到正确区域，不改变运行时权限或导航语义。测试/可靠性仅记录一项非阻塞后续项：`SupplierOfferingsPage` 正向导航测试后续可继续收紧到 `supplier-offerings-mission-flow` 作用域，以进一步降低未来 bridge 区域同名 CTA 误匹配风险。
- 本轮继续按“前端优先”收敛 `App.test.tsx` 中注册后共享控制台与角色差异断言的低信号全局查询：将普通用户注册后首轮引导里对“供应商主任务 / 管理员主任务”的缺失判断收敛到 `dashboard-next-steps-lane` 作用域；将 supplier dashboard 的“普通用户首轮引导 / 结算页 / Webhook”缺失断言收敛到 `main[aria-label="控制台主内容"]` 作用域；同时为 supplier settings 场景补充 `settings-user-first-run-checklist` 缺失断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与真实 onboarding / settings 合同不变，而不是继续依赖 App 级全局缺失查询误伤无关区域。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API 回放脚本 `.hermes/local-output/run_app_scope_cleanup_real_api.sh`（产物目录 `app-scope-cleanup/20260503-203612`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/api-keys`、`/settings`、`/admin/risk`、`/admin/audit`、`/docs` SPA 壳 200 均保持成立，本轮 App 级断言收敛未破坏注册后共享菜单、管理员真实运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 `App.test.tsx` 中与注册后 onboarding / shared-console 角色差异相关的局部断言收敛到更合适的区域或补充更精确的缺失检查，不改变运行时权限、真实 API、导航覆盖或部署行为。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例中仍残留的 page-global fallback 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 shared-console focused 回归中残留的 page-global `返回推荐工作台` 负断言脆弱点：仅调整 `SupplierSettlementsPage.test.tsx`、`SupplierResourcesPage.test.tsx`、`BalancePage.test.tsx`、`AdminRiskPage.test.tsx`、`AdminAuditPage.test.tsx`，移除在 fallback 区域已通过 `data-testid` / `within(...)` 明确约束时重复的页面级按钮缺失断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与 mission/shared fallback 分区合同不变，而不是继续依赖全页同名 CTA 永远不存在这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/BalancePage.test.tsx src/pages/AdminRiskPage.test.tsx src/pages/AdminAuditPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_fallback_scope_real_api.sh`（产物目录 `front-fallback-scope/20260503-212158`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/api-keys`、`/webhooks`、`/settings`、`/admin/risk`、`/admin/audit`、`/supplier/resources`、`/supplier/settlements` SPA 壳 200 均保持成立，本轮 focused 去脆弱化未破坏注册后共享菜单、管理员真实运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅删除 fallback 区域已被更精确语义锚点覆盖时多余的页面级 `返回推荐工作台` 负断言，既减少未来重复文案带来的误报，也未削弱 shared-console 权限/回退合同。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)`、`async-validator` 局部噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例里仍残留的 page-global 文案/按钮缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛剩余 shared-console focused 用例中的全局 `cleanup()` / 多棵 Router 树 teardown 依赖：已将 `ProfilePage.test.tsx`、`WebhooksPage.test.tsx`、`SupplierOfferingsPage.test.tsx`、`SupplierResourcesPage.test.tsx`、`SupplierSettlementsPage.test.tsx`、`SupplierDomainsPage.test.tsx` 内多跳导航/双 fallback 回放统一改为显式 `view.unmount()` + fresh render，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与真实导航合同不变，而不是继续依赖 Testing Library 全局清理语义。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/WebhooksPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx src/pages/SupplierDomainsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API 回放脚本 `.hermes/local-output/run_front_cleanup_unmount_real_api.sh`（产物目录 `front-cleanup-unmount/20260503-194713`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/profile`、`/webhooks`、`/supplier/resources`、`/supplier/settlements`、`/supplier/offerings`、`/supplier/domains` SPA 壳 200 均保持成立，本轮 focused 生命周期收敛未破坏注册后共享菜单、管理员真实运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把剩余 supplier / profile / webhook focused 测试内的全局 `cleanup()` 收紧为显式 `unmount()`，不改变运行时权限、真实 API、导航覆盖或部署行为。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例中仍残留的重复 fallback 文案 / 全局缺失断言低信号耦合，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `AdminUsersPage.shared-console.test.tsx` 中管理员资金工作台导航用例的渲染生命周期表达：将同一用例内多次 `cleanup()` + 重新 `renderAdminUsersPage()` 的全局清理模式，收紧为显式 `view.unmount()` 后再 fresh render 的局部 teardown，保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与 `admin-users-mission-flow` 真实导航合同不变，只减少对 Testing Library 全局清理语义的依赖。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API 回放脚本 `.hermes/local-output/run_admin_users_shared_console_unmount_real_api.sh`（产物目录 `admin-users-shared-console-unmount/20260503-192011`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`（201）、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/admin/users`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 生命周期收敛未破坏注册后共享菜单、管理员真实运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把管理员资金工作台导航测试的 teardown 从全局 `cleanup()` 收紧为显式 `unmount()`，并同步移除失效 import，不改变运行时权限、真实 API、导航覆盖或部署行为。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例中仍残留的全局 cleanup / 多棵 Router 树 teardown 依赖，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 supplier shared-console fallback focused 回归中的重复 fallback 文案计数脆弱点：将 `SupplierResourcesPage.test.tsx` 与 `SupplierOfferingsPage.test.tsx` 中对“返回推荐工作台”说明文案的全页计数断言，收紧为 mission/shared fallback 各自 `data-testid` 区域内的 `within(...)` scoped 校验，避免把页面其他共享卡片中的相同文案误算进来，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与真实回退合同不变，而不是继续依赖全页重复文案数量。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_supplier_fallback_copy_scope_real_api.sh`（产物目录 `supplier-fallback-copy-scope/20260503-190214`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/supplier/resources/overview`（预期 403）、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/supplier/resources`、`/supplier/offerings`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 fallback 文案 scoped 收敛未破坏注册后共享菜单、真实资源权限边界、管理员运营接口与 supplier 单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 supplier fallback 文案计数从全页统计收敛为真实 fallback 区域 scoped 断言，减少 future copy / 卡片复用带来的误报，也未削弱 shared-console 权限或真实 API 边界验证。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例中仍残留的全局重复文案计数与结构选择器耦合，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `ApiKeysPage.test.tsx` 的 shared-console focused 回归合同：删除误导性的“API 下单 runner / 提交激活订单”伪契约测试与无效 activation mock，保留并补强真实运行时已有的 API Key 生命周期合同——白名单清空后 reload、白名单后端报错透出、审计卡片 scoped 查询、bridge/fallback 导航与精确撤销确认文案；继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与真实 API Keys / Webhooks / Docs 单壳入口不变，而不是在测试中虚构尚未存在的下单 runner 前端能力。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_keys_scope_cleanup_real_api.sh`（产物目录 `api-keys-scope-cleanup-20260503-234658`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 ApiKeys focused 合同修正未破坏注册后共享菜单、管理员真实运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；首轮评审曾阻断伪造 `api-keys-order-runner` 的测试标题/断言错配与缺失的白名单失败路径覆盖，控制器已删去不存在功能的伪契约、恢复白名单错误与 reload 合同，并以 scoped 审计卡片查询与精确撤销确认文案复验通过。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ProjectsPage` / `OrdersPage` / 其余 shared-console focused 用例中的剩余低信号全局文案/结构查询，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `ProjectsPage.test.tsx`、`OrdersPage.test.tsx` 与 `AdminUsersPage.shared-console.test.tsx` 中残留的低信号 page-global 文案缺失断言：在不改运行时逻辑的前提下，仅把采购 hero、履约 hero 与管理员共享接入桥接区对接入文案/API Keys/Webhook/Docs 标签缺失的判断，从整页 `screen.queryByText(...)` 收紧为对应 hero/card 区域内的 scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与 bridge/fallback 分区合同不变，而不是继续依赖整页永远不存在同名文案这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx src/pages/AdminUsersPage.shared-console.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_projects_orders_adminusers_scope_real_api.sh`（产物目录 `projects-orders-adminusers-scope/20260504-001545`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/projects/inventory`、`GET /api/v1/orders/activations`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、`GET /api/v1/admin/wallet-users`、`GET /api/v1/admin/disputes` 与前端 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/admin/users` SPA 壳 200 均保持成立，本轮 focused 去脆弱化未破坏注册后共享菜单、真实采购/履约接口、管理员资金运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Projects/Orders/AdminUsers 三个共享控制台 focused 用例中的负断言收敛到真实 hero/bridge 语义区域，既减少未来重复文案导致的误报，也未削弱 menu-truth、真实 API、权限或导航合同。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`ProfilePage.test.tsx`、`SettingsPage.test.tsx` 等其余 shared-console focused 用例里仍残留的 page-global 文案/按钮缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `SettingsPage.test.tsx` 与 `ProfilePage.test.tsx` 中残留的低信号 page-global 缺失断言：保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与角色差异菜单合同不变，仅将 supplier 设置页对用户首轮清单的缺失判断从 `queryByText('首次使用清单')` 收紧为 `settings-user-first-run-checklist` 容器级断言，并将 supplier/admin 资料页对用户专属聚焦卡片的缺失判断从全局 `queryByText('采购与订单串联')` 收紧为 heading 级负断言，避免未来说明文案复用或正文重复时出现误报。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/pages/ProfilePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_settings_profile_scope_real_api.sh`（产物目录 `settings-profile-scope/20260504-011028`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/profile`、`/settings` SPA 壳 200 均保持成立，本轮 focused 去脆弱化未破坏注册后共享菜单、真实 API、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅收紧 Settings/Profile 两页 focused 用例中的容器级/heading 级负断言与测试标题表述，不改变运行时权限、路由或真实接口合同，并进一步降低未来文案复用导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例里仍残留的 page-global copy / fallback 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `ProtectedRoute.test.tsx`、`SettingsPage.test.tsx` 与 `ProfilePage.test.tsx` 中残留的低信号 page-global 路由/文案缺失断言：将 `ProtectedRoute` / `SupplierRoute` / `AdminRoute` focused 用例从普通文本节点切换为带 heading 语义的路由落点断言，并保留未授权/越权时受保护内容不可见的最小合同；同时把 `ProfilePage` supplier/admin 回退卡片对用户专属“采购与订单串联”文案的否定校验收紧到各自角色焦点区域，把 `SettingsPage` 重新打开首轮引导后的断言恢复为共享控制台首页 + 供应商快捷动作不可见并补充 checklist 容器缺失校验，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、注册后统一壳与真实路由守卫合同不变，而不是继续依赖整页同名文本永远不存在这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/components/ProtectedRoute.test.tsx src/pages/SettingsPage.test.tsx src/pages/ProfilePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_route_guard_scope_real_api.sh`（产物目录 `front-route-guard-scope/20260504-035257`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview` 与前端 `/`、`/profile`、`/settings` SPA 壳 200 均保持成立，本轮 route-guard / profile / settings focused 去脆弱化未破坏注册后共享菜单、真实认证接口、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；首轮代码质量评审曾指出 `SettingsPage.test.tsx` 只保留 checklist 缺失断言会弱化原有“非供应商快捷动作不可见”的证明，控制器已恢复 supplier 快捷按钮缺失校验并复验通过。其余评审一致认为当前 diff 仅把路由守卫与资料/设置页 focused 断言收紧到更贴近运行时语义的 heading/区域级合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 漂移与整页同名文本重复导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`DashboardPage.test.tsx`、`AdminSuppliersPage.test.tsx` 等其余 shared-console focused 用例里仍残留的 page-global copy / CTA 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `AppSidebar.test.tsx` 与 `ConsoleLayout.test.tsx` 中 shared shell 仍残留的结构/整页级脆弱断言：为 `AppSidebar` 基础工作台、供应商扩展、管理员扩展与共享菜单加载态卡片补充最小 `data-testid` 语义锚点，并为 `ConsoleLayout` 顶部快捷操作区补充 `console-layout-quick-actions` 区域锚点；focused 用例随之从 `.closest('[style*=border-radius]')` 和整页 quick-action 按钮缺失查询，收紧为对真实分组卡片与快捷操作容器的 `within(...)` scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、当前页不出现在快捷操作区与共享菜单加载态合同不变，而不是继续依赖样式包装层与整页同名按钮永远不存在这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_shared_shell_semantic_anchors_real_api.sh`（产物目录 `app-shell-scope-hardening/20260504-041732`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 shared shell 语义锚点收敛未破坏注册后共享菜单、真实 API、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅为 shared shell 分组卡片、加载态卡片与顶部快捷操作区补充最小语义锚点，并把 focused 测试收紧到真实区域 scoped 合同，不改变运行时权限、导航或真实 API 合同，同时显著降低 future Semi UI 包装层/整页重复按钮文本导致的误报。非阻塞后续项仅包括：`app-sidebar-admin-group` 锚点后续可补一条管理员分组 scoped 断言、`ConsoleLayout` 第二条快捷操作顺序用例未来可继续收敛到同一区域、既有 Semi UI `findDOMNode` / RTL `act(...)` / 前端 chunk 体积告警继续记为债务；下一前端优先项转为继续清理 `App.test.tsx`、`DashboardPage.test.tsx`、`AdminSuppliersPage.test.tsx` 等其余 shared-console focused 用例里仍残留的 page-global copy / CTA 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛注册后共享控制台与 plain-user 共享壳断言：仅调整 `App.test.tsx` 与 `LoginPage.test.tsx`，将新注册普通用户壳层校验从 page-global `供应商扩展` / `管理员扩展` 缺失文案，收紧为 `app-sidebar-shared-group` 内共享菜单存在且 `app-sidebar-supplier-group` / `app-sidebar-admin-group` 整组缺失的语义断言，同时把注册成功用例标题改回与真实证明一致的“会话落库 + 跳转共享首页”合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与真实 API 注册链路不变，而不是继续依赖整页同名分组文案永远不存在这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_register_shared_shell_real_api.sh`（产物目录 `app-register-shared-shell/20260504-044301`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮注册后 shared-shell 断言收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审唯一非阻塞建议是避免让 `LoginPage.test.tsx` 标题声称其验证了供应商/管理员侧边栏缺失，因此控制器已在提交前把该标题收敛回“注册成功后会话落库并跳转共享首页”的真实证明范围。其余评审一致认为当前 diff 仅把 plain-user 壳层断言收紧到真实 sidebar group 语义锚点，并提升 autonomous run 下失败定位清晰度，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `DashboardPage.test.tsx`、`AdminSuppliersPage.test.tsx`、`LoginPage.test.tsx` 等其余 shared-console / register focused 用例里仍残留的低信号 copy / route-contract 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `DashboardPage.test.tsx`、`AdminSuppliersPage.test.tsx` 与 `LoginPage.test.tsx` 中剩余的低信号标题/区域断言：在不改运行时代码的前提下，仅把 Dashboard 总览断言从普通文本节点收紧为 heading 级合同，把 AdminSuppliers 首屏桥接/任务流断言收紧为 `admin-suppliers-mission-flow` 与 bridge card 区域 scoped 查询，并把 Login 注册前桥接内容收紧到“注册后默认进入共享控制台”卡片内的 scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与管理员/普通用户共享工作台合同不变，而不是继续依赖整页重复文案或非语义标题查询。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/pages/AdminSuppliersPage.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_dashboard_login_adminsuppliers_real_api.sh`（产物目录 `dashboard-login-adminsuppliers-real-api/$STAMP`，以实际时间戳目录保存），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；测试/可靠性评审一度误把本地未提交回放脚本的掩码输出当成当前 diff 问题，控制器已用直接文件读取与实际 rerun 纠正并确认脚本当前可重跑。其余评审一致认为当前 diff 仅收紧 Dashboard/AdminSuppliers/Login 三个 focused 用例到更贴近运行时语义的 heading/bridge/card 作用域，不改变运行时权限、导航或真实 API 合同；既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警继续记为非阻塞债务。下一前端优先项转为继续清理 `App.test.tsx`、`ProfilePage.test.tsx`、`SettingsPage.test.tsx` 等 shared-console focused 用例中仍残留的 `.closest('.semi-card')` / page-global copy 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收紧 `App.test.tsx` 中默认普通用户首轮引导 focused 合同：将 `expectDefaultUserFirstRunLane()` 从依赖长文案/营销 copy 的断言，改为 `dashboard-next-steps-lane` 内 `前往项目市场` / `查看订单中心` / `管理 API Keys` 按钮级 scoped 合同，并显式验证 `前往域名管理` / `查看风控中心` 不会泄露到普通用户首轮路径；同时为 Dashboard 首轮引导的 `稍后再看` 动作补充 `dashboard-first-run-dismiss` 语义锚点，把设置页“重新打开首轮引导”验证收紧到 `settings-user-first-run-checklist` 容器级范围，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与真实首轮引导/设置页回流合同不变，而不是继续把高频 copy 细节当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_first_run_scope_real_api.sh`（产物目录 `app-first-run-scope/20260504-072509`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮首轮引导 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；本轮控制器对五维评审采用直接 controller-side 结果收敛：产品/规格侧确认当前 diff 仅把普通用户首轮引导/设置回流 proof 从长文案收敛为按钮/容器级合同，代码质量侧确认新增 `dashboard-first-run-dismiss` 语义锚点与 helper 重用保持单一职责，安全/集成侧确认无权限边界或真实 API 合同漂移，测试/可靠性侧确认 `App.test.tsx` focused、构建、Go 测试、Compose 重建与真实 API 回放全部通过，性能/运维侧仅继续记录既有前端 chunk 体积告警为非阻塞债务。下一前端优先项转为继续清理 `App.test.tsx`、`ProfilePage.test.tsx`、`SettingsPage.test.tsx` 等 shared-console focused 用例中仍残留的 page-global copy / fallback 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `App.test.tsx` 与 `SettingsPage.test.tsx` 中残留的低信号 shared-shell / 回流断言：在不改运行时代码的前提下，仅将注册成功后的共享控制台可达性从 page-global `控制台总览` 文案收紧为默认普通用户首轮 lane helper `expectDefaultUserFirstRunLane()`，并把 plain-user sidebar 场景从通用总览文本收紧为 `app-sidebar-shared-group` 语义锚点；同时将设置页“重新打开首轮引导”后的 supplier CTA 缺失检查收窄到共享首页落点容器，并为第二次 render 显式 `unmount()`，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与真实注册/回流合同不变，而不是继续依赖整页通用标题或 page-global 负断言。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/SettingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_test_scope_real_api.sh`（产物目录 `front-test-scope-20260504-081516`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；代码质量与测试/可靠性评审仅提出两项非阻塞建议：`App.test.tsx` 中 `onboardingRegion` 变量若只用于触发 helper 可标记为显式已消费，`SettingsPage.test.tsx` 后续若首页 stub 演进可考虑用稳定容器锚点替代 `parentElement` 缩域；控制器已在当前 diff 中加入 `void onboardingRegion` 明确变量用途，其余保持为后续债务。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`LoginPage.test.tsx`、`DashboardPage.test.tsx` 等 shared-console focused 用例中仍残留的长文案/通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `SettingsPage.test.tsx` 中最后一处基于 `parentElement` 的首页落点结构依赖：为测试内共享首页 route stub 补充最小 `shared-console-home-route` 语义锚点，并把"重新打开首轮引导"后的 supplier CTA 缺失校验收紧为该首页锚点内的 scoped role 断言，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与设置页回流合同不变，而不是继续依赖测试 stub DOM 包装层级。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/App.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_test_scope_real_api.sh`（产物目录 `front-test-scope-20260504-084440`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；五位 reviewer 一致确认当前 diff 仅为 `SettingsPage.test.tsx` 的测试 stub 增加最小首页锚点并移除 `parentElement` 结构遍历，没有弱化"重新打开首轮引导后回到共享首页、supplier CTA 不泄漏、checklist 消失"的原合同，也未引入额外运行时或运维复杂度。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例中残留的低信号长文案 / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `DashboardPage.test.tsx` 中下一步旅程 lane 的低信号文案耦合：在运行时代码仅补充最小 `dashboard-next-step-${step.key}` / `dashboard-next-step-action-${step.key}` 语义锚点的前提下，将 shared-console focused 用例里对 `先确认预算与钱包状态`、`再进入项目市场采购`、`最后完成 API 接入` 等长文案/按钮文本的存在与缺失判断，收紧为 lane 区域内基于 step key 的结构合同，同时保留 integration 卡片标题 `最后完成 API 接入` 与 CTA 可访问名称 `管理 API Keys` 的用户层证明，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、预算→采购→履约→接入 单壳导航顺序与真实 API Keys 跳转合同不变，而不是继续把易变 copy 当作唯一 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_dashboard_step_contract_real_api.sh`（产物目录 `dashboard-step-contract/20260504-101442`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 Dashboard lane 语义锚点收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；测试/可靠性评审初版一度指出改动若完全切换到 testid 会弱化用户层按钮/标题合同，控制器已在 integration 卡片内补回 `最后完成 API 接入` 标题与 `管理 API Keys` 可访问按钮断言后复验通过。其余评审一致认为当前 diff 仅以最小语义锚点把 Dashboard 下一步旅程 lane 的 brittle copy 断言收敛为更贴近运行时结构/行为的合同，不改变运行时权限、导航或真实 API 合同；既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警继续记为非阻塞债务。下一前端优先项转为继续清理 `SupplierDomainsPage.test.tsx`、`ProjectsPage.test.tsx`、`OrdersPage.test.tsx` 等页中仍残留的长文案 / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SupplierDomainsPage.test.tsx`、`ProjectsPage.test.tsx` 与 `OrdersPage.test.tsx` 中残留的低信号文案/标题耦合：在不改运行时代码的前提下，仅把 `SupplierDomainsPage` 的 overview 失败场景校验从笼统的 error-vs-empty-state 描述，收紧为更贴近真实 UI 的"显式 overview 失败态 + table card 内 empty-state 文案缺失"合同；同时把 Projects/Orders 的 CTA 抑制用例标题分别明确为 procurement hero / fulfillment hero 区域合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、采购→履约→接入单壳连续路径与真实 API 菜单真值不变，而不是继续依赖含糊的测试标题或过宽的空态文案表述。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_hero_copy_scope_real_api.sh`（产物目录 `front-hero-copy-scope/20260504-105404`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/projects`、`/orders`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商域名运营入口与单壳前端连续路径。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 SupplierDomains 的失败态 proof 收紧到更贴近真实区域合同的断言，并把 Projects/Orders 两个 hero CTA 抑制用例标题改为更准确的区域说明，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 调整与测试语义歧义造成的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`LoginPage.test.tsx`、`DashboardPage.test.tsx`、`SupplierDomainsPage.test.tsx` 等 shared-console focused 用例中仍残留的长文案 / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 与 `DashboardPage.test.tsx` 中剩余的低信号标题/长文案断言：在不改运行时代码的前提下，仅把未认证登录壳校验从普通文本查询收紧为 `heading` 语义合同，并把 Dashboard 下一步旅程 lane 的 smoke proof 从四条易变长文案收敛为四个稳定 step/action 语义锚点，同时继续保留 API Keys 集成卡片的用户可见 heading / button 合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与预算→采购→履约→接入 单壳路径不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/DashboardPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_dashboard_step_contract_real_api.sh`（产物目录 `dashboard-step-contract/20260504-115743`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把未认证登录壳校验收紧为 heading 语义合同，并把 Dashboard 下一步旅程 lane 的 brittle 长文案 proof 收敛为更贴近运行时结构的 step/action 语义锚点，同时保留 API Keys 集成卡片的用户层标题/按钮真值，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`LoginPage.test.tsx`、`DashboardPage.test.tsx`、`SupplierDomainsPage.test.tsx` 等 shared-console focused 用例中仍残留的长文案 / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 shared shell focused 契约：仅调整 `AppSidebar.test.tsx` 与 `ConsoleLayout.test.tsx`，将侧边栏等待条件从手写 `waitFor(getByText('Nexus-Mail'))` 收敛为 heading 语义等待，并把供应商当前路由选中断言缩到 `app-sidebar-supplier-group` 内唯一选中项；同时将顶部快捷操作顺序与导航断言缩到 `console-layout-quick-actions` 区域，继续保持 new-api 风格单一登录后深色共享控制台、共享路由 schema 顺序、角色分组菜单与统一布局合同不变，而不是继续依赖整页按钮扫描或全局选中节点。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_sidebar_layout_contract_real_api.sh`（产物目录 `sidebar-layout-contract/20260504-133352`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 shared shell focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 shared shell focused proof 从整页等待/按钮扫描收敛为更贴近运行时语义的 heading、group 与 quick-actions scoped 合同，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、AppSidebar `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ProfilePage.test.tsx`、`SupplierSettlementsPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明/页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ProfilePage.test.tsx` 首个 shared-console focused 用例中仍残留的低信号 page-global 长说明断言：在不改运行时代码的前提下，仅将 user capability 场景从 `账号身份、会话边界...`、`当前账号默认以用户身份进入共享控制台...`、`控制台桥接能力`、`深色共享账号中枢` 等易变说明 copy 收紧为 `Profile Mission Control` 标签、`个人资料` / `用户接入焦点` / `采购与订单串联` / `集成准备` 等 heading 语义合同，并继续保留 `profile-capability-bridge` 区域内 API Keys / Webhook / Docs 三个真实导航按钮与跳转结果，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与 Profile 共享账号中枢导航合同不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_profile_heading_scope_real_api.sh`（产物目录 `profile-heading-scope/20260504-140457`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 Profile focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Profile 首个 focused 用例从 page-global 长说明 copy 收敛为更贴近运行时结构的标签/heading 骨架与 capability-region 按钮合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 微调导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `SupplierSettlementsPage.test.tsx`、`ApiKeysPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明/页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SettingsPage.test.tsx`、`SupplierResourcesPage.test.tsx` 与 `BalancePage.test.tsx` 首个 mission-control focused 用例中仍残留的低信号 page-global 长说明断言：在不改运行时代码的前提下，仅把设置中心、供应商资源页与余额中心的首屏 proof 从大段 hero 说明、整组页面级叙事 copy 收紧为更贴近 shared-console/new-api 单壳结构的 heading + mission-flow / capability / bridge 稳定合同，同时保留 `Docs / Webhooks / API Keys 已统一到单一壳内导航`、供应商三步任务流以及普通用户资金路径/角色差异等关键用户可见真值，继续保持注册后统一壳、角色菜单 gating 与真实入口导航合同不变，而不是继续把高频润色长文案当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/BalancePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_mission_heading_scope_real_api.sh`（最新产物目录 `front-mission-heading-scope/20260504-170052`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/settings`、`/balance`、`/supplier/resources`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 SAFE、性能/运维 PASS；评审过程中曾指出 Settings / SupplierResources / Balance 三页的首屏 focused proof 若只保留通用 heading 会弱化对共享控制台能力矩阵、供应商三步任务流、普通用户资金路径与统一接入入口的保护，控制器已补回这些稳定用户可见合同后复验通过。继续保留既有 Semi UI `findDOMNode`、Balance focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `SupplierSettlementsPage.test.tsx`、`ApiKeysPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ApiKeysPage.test.tsx`、`ProfilePage.test.tsx` 与 `SupplierSettlementsPage.test.tsx` 中仍残留的低信号装饰性标签 / 长说明断言：在不改运行时代码的前提下，仅把 `ApiKeysPage` supplier hero proof 保持为 `api-keys-hero-card` 区域内两条稳定 supplier tip 合同，把 `ProfilePage` capability 场景从装饰性 `Profile Mission Control` 纯文本查询收紧为 `个人资料` heading 语义合同，并将 `SupplierSettlementsPage` 首个 mission-control proof 从三条易变任务流长文案收敛为 `supplier-settlements-mission-flow` 区域内三枚真实 CTA 按钮合同，同时保留 bridge 区域与关键 heading/指标真值，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、API Keys / Profile / Supplier Finance 单壳联动与真实 API 菜单真值不变，而不是继续把装饰标签或高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/pages/ProfilePage.test.tsx src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_api_keys_profile_settlements_scope_real_api.sh`（产物目录 `front-api-keys-profile-settlements-scope/20260504-171958`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/settlements` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 API Keys / Profile / Supplier Settlements 三个 focused 用例从装饰性标签或易变长文案收敛为更贴近运行时结构的 heading / supplier tip / mission CTA 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 微调与重复说明文本导致的误报。继续保留既有 Semi UI `findDOMNode`、ApiKeys focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`ApiDocsPage.test.tsx`、`AdminAuditPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SupplierSettlementsPage.test.tsx` 中残留的装饰性英文 tag 依赖：在不改运行时代码的前提下，仅把后续导航 / fallback / 提交流程用例里的 `Supplier Finance Mission Control` 普通文本断言统一替换为 `供应商资金与争议指挥台` heading 语义合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、supplier finance mission-flow / shared-console fallback 分区与真实供应商结算 API 合同不变，而不是继续把装饰性英文标签当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_supplier_settlements_heading_scope_real_api.sh`（产物目录 `supplier-settlements-heading-scope/20260504-173913`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/settlements` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 SupplierSettlements 后续 focused 用例从装饰性英文标签收敛为更贴近运行时结构的中文 heading 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 视觉标签/文案微调导致的误报。继续保留既有 Semi UI `findDOMNode`、SupplierSettlements focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`ApiDocsPage.test.tsx`、`AdminAuditPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `AdminAuditPage.test.tsx` 中剩余的装饰性英文 tag 依赖：在不改运行时代码的前提下，仅把后续导航 / fallback / 查询过滤用例里的 `Audit Mission Control` 普通文本断言统一替换为 `审计日志` heading 语义合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、admin audit mission-flow / shared-console fallback / filter 合同与真实管理员审计 API 合同不变，而不是继续把装饰性英文标签当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminAuditPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_audit_heading_scope_real_api.sh`（产物目录 `admin-audit-heading-scope/20260504-180855`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 AdminAudit 后续 focused 用例从装饰性英文标签收敛为更贴近运行时结构的中文 heading 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 视觉标签/文案微调导致的误报。继续保留既有 Semi UI `findDOMNode`、AdminAudit focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`ApiDocsPage.test.tsx`、`App.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `WebhooksPage.test.tsx` 中 user integration loop focused 用例仍残留的低信号长说明断言：在不改运行时代码的前提下，仅把 Webhooks 首轮 shared-console bridge 卡片 proof 从整段 `在同一套控制台里先创建 endpoint...` 长说明 copy 收紧为更贴近 new-api 单壳 shared-console 合同的 heading 语义 + `webhooks-first-integration-loop` 区域内 API Keys / API 文档 两个真实 CTA scoped 断言，并同步把相关页面首屏可达性从普通文本查询收敛为 `开发者 Webhook 接入工作台` heading 合同，继续保持注册后统一壳、角色菜单 gating、Webhook → API Keys / Docs 接入闭环与真实 API 菜单真值不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_integration_scope_real_api.sh`（产物目录 `webhooks-integration-scope/20260504-182838`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；产品/规格评审认为当前 diff 仍保留 Webhooks 首轮 bridge 卡片的标题与两个关键 CTA 真值，代码质量评审仅建议后续可把测试标题进一步改成更偏用户行为表述，安全/集成评审确认未引入 route/menu/API 合同漂移，测试/可靠性评审确认仅去除了 brittle 长文案快照且未新增 flaky 风险，性能/运维评审确认该 diff 只降低前端测试维护噪声、不会影响构建或部署。继续保留既有 Semi UI `findDOMNode`、Webhooks focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`App.test.tsx`、`LoginPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SupplierDomainsPage.test.tsx` 首个 shared-console focused 用例中仍残留的低信号 Mission Control / page-global 长说明断言：在不改运行时代码的前提下，仅把域名运营页首屏 proof 从装饰性 `Supplier Domain Mission Control` 英文标签、若干 mission-step 长说明与 bridge tag 文本，收紧为 `heading` 语义合同、`supplier-domains-mission-flow` 区域内供应商资源 / 供货规则两个真实 CTA，以及 `supplier-domains-shared-console-bridge` 区域内 API Keys / Webhook / API 文档 三个共享桥接按钮合同，同时继续保留 `单一供应商工作台`、`域名 readiness 优先`、`角色扩展但不伪造升级` 三个稳定 capability 真值，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、域名→资源→供货→结算单壳连续路径与真实菜单真值不变，而不是继续把装饰标签或高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放（控制器串行复核 `GET /healthz`、普通用户注册 + `/auth/menu`、供应商登录 + `/auth/menu` + `/supplier/resources/overview` + `/supplier/settlements`、管理员登录 + `/auth/menu` + `/admin/overview` + `/admin/risk` + `/admin/audit?limit=5`，以及前端 `/`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs`、`/admin/audit` SPA 壳 200），确认本轮 SupplierDomains focused 收敛未破坏真实注册、共享菜单、供应商资源/结算入口、管理员运营接口与单壳前端入口；对应本地回放脚本已更新保存在 `.hermes/local-output/run_supplier_domains_heading_scope_real_api.sh` 供后续复用。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 SupplierDomains 首个 focused 用例从装饰性 Mission Control 标签与 page-global 长说明 copy 收敛为更贴近运行时结构的 heading / mission-flow / shared-console-bridge 按钮合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 微调与多区域重复文本导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `SupplierResourcesPage.test.tsx`、`BalancePage.test.tsx`、`ApiDocsPage.test.tsx` 等 shared-console focused 用例里仍残留的装饰性 Mission Control 标签 / 长说明断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SupplierResourcesPage.test.tsx`、`BalancePage.test.tsx` 与 `SettingsPage.test.tsx` 中仍残留的装饰性英文 Mission Control 标签 / 长说明依赖：在不改运行时代码的前提下，仅把 SupplierResources 与 Balance 的后续导航 / fallback / 提交流程用例从 `Supplier Resource Mission Control` / `Finance Mission Control` 英文标签等待收紧为 `供应商资源` / `余额中心` heading 语义合同，并删除 Settings 首个 shared-console 结构用例里对 `Console Mission Control` 与浅色占位叙事长句的低信号断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、供应商资源 / 资金 / 设置页共享壳导航与真实 API 菜单真值不变，而不是继续把装饰性英文标签或高频润色说明 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/BalancePage.test.tsx src/pages/SettingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_mission_heading_scope_real_api.sh`（产物目录 `front-mission-heading-scope/20260504-191748`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/balance`、`/supplier/resources`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 SupplierResources / Balance 的后续 focused 用例从装饰性英文标签收敛为更贴近运行时结构的中文 heading 合同，并删除 Settings 首个 shared-console 结构用例中的低信号装饰性标签/叙事长句断言，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 视觉标签/文案微调导致的误报。继续保留既有 Semi UI `findDOMNode`、Balance focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`AdminUsersPage.shared-console.test.tsx`、`AdminRiskPage.test.tsx`、`SupplierResourcesPage.test.tsx`、`BalancePage.test.tsx` 等 shared-console focused 用例里仍残留的装饰性 Mission Control 标签 / 长说明断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `AdminProjectsPage.test.tsx`、`AdminSuppliersPage.test.tsx` 与 `SupplierDomainsPage.test.tsx` 中仍残留的装饰性 Mission Control / page-global 长说明依赖：在不改运行时代码的前提下，仅把管理员价格页与管理员供应商页后续 focused 用例里的 `Admin Pricing Mission Control` / `Supplier Mission Control` 装饰性英文标签等待统一收紧为 `价格策略` / `供应商管理` heading 语义合同，并把 SupplierDomains 首屏 proof 从仅剩的泛化标签校验补回到更贴近真实 overview 数据的指标说明与 region 聚合合同（保留 `可继续参与供货编排的域名数量。`、`支持泛收件的域名数量。`、`去重后的 region 数量。` 以及 `hk/us/global/eu · 1` 聚合结果），同时继续保留三页各自的 mission-flow CTA、shared-console bridge、fallback 与真实 service payload 提交流程断言，保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与真实 API 菜单真值不变，而不是继续把装饰标签或高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx src/pages/AdminSuppliersPage.test.tsx src/pages/SupplierDomainsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_projects_admin_suppliers_supplier_domains_focus_real_api.sh`（产物目录 `admin-projects-admin-suppliers-supplier-domains-focus/20260504-212701`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/admin/pricing`、`/admin/suppliers`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；代码质量与安全/集成评审一度指出 `SupplierDomainsPage.test.tsx` 如果只保留通用指标标签会削弱对 overview 聚合/region 归一化的集成证明，控制器已补回指标说明与 `hk/us/global/eu · 1` 聚合结果断言后复验通过。其余评审一致认为当前 diff 仅把三页 focused 用例从装饰性 Mission Control 标签与 page-global 长说明收敛为更贴近运行时结构的 heading / mission-flow / bridge 合同，并保留关键 overview / service payload 真值，不改变运行时权限、导航或真实 API 合同；既有 Semi UI `findDOMNode`、局部 `act(...)` / `FormApi.setValues()` 噪声与前端 chunk 体积告警继续记为非阻塞债务。下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx`、`LoginPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中仍残留的装饰性英文标签与宽泛 page-global 落点断言：在不改运行时代码的前提下，仅把 admin risk / audit workspace proof 从 `Risk Mission Control`、`Audit Mission Control` 等装饰性英文标签收紧为 `规则命中概览`、`admin-audit-events-table-card` 等更贴近运行时数据区合同，并把 plain-user 误入 supplier 路由后的回退证明从宽泛首页文本收紧为 `dashboard-next-steps-lane` 共享首页落点合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与真实 admin/supplier route guard 合同不变，而不是继续把装饰标签或整页通用文本当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/WebhooksPage.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_front_heading_real_api.sh`（最新产物目录 `app-front-heading-real-api/20260504-222229`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 `App.test.tsx` 从装饰性英文标签与宽泛 page-global 文本断言收敛为更贴近运行时结构的 risk/audit 数据区与 shared dashboard lane 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案润色、视觉标签调整与首页重复文本导致的误报。唯一非阻塞建议仅为后续把测试标题 `shared shared-shell` 中的重复词顺手改正；既有 Semi UI `findDOMNode`、局部 `act(...)` 噪声与前端 chunk 体积告警继续记为非阻塞债务。下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx`、`LoginPage.test.tsx`、`App.test.tsx` 其余 focused 用例中仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `LoginPage.test.tsx` 首个注册壳 focused 用例中仍残留的低信号角色路径 heading 断言：在不改运行时代码的前提下，仅把注册旅程首屏 proof 从 `注册后默认进入共享控制台`、`用户路径`、`供应商路径`、`管理员路径` 四条易随文案/布局微调变化的说明 heading，收紧为 `login-register-journey` 区域内稳定 `立即注册，进入共享控制台` CTA 合同，以及点击后 `注册 Nexus-Mail` / `创建账户并进入控制台` 的真实模式切换合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与真实注册链路不变，而不是继续把角色路径说明 heading 当作该 focused 用例的主真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_login_register_shell_scope_real_api.sh`（产物目录 `login-register-shell-scope/20260504-225353`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 Login 注册壳 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；代码质量评审仅提出一项非阻塞建议：将该 focused 用例的点击动作也保持在 `login-register-journey` scoped 查询内，控制器已当轮修正并复验通过。其余评审一致认为当前 diff 仅把 Login 注册壳首个 focused 用例从低信号角色路径 heading 收敛为更贴近运行时结构/行为的 CTA + 模式切换合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 微调与布局润色导致的误报。继续保留既有 LoginPage focused 中已有的 React `act(...)` 警告、Semi UI `findDOMNode` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx`、`App.test.tsx` 其余 focused 用例中仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。

- 本轮继续按"前端优先"收敛 `App.test.tsx` 与 `ApiDocsPage.test.tsx` 中仍残留的低信号装饰性 heading / 角色路径说明依赖：在不改运行时代码的前提下，仅把登录注册壳 focused 用例从 `注册后默认进入共享控制台 / 用户路径 / 供应商路径 / 管理员路径` 等易变 journey heading 收紧为 `login-register-journey` 区域内稳定注册 CTA + 注册模式切换合同，并把管理员 docs focused 用例从装饰性 `Docs Mission Control` heading 等待收紧为 `管理员扩展 · API 契约` badge、`/openapi/index.html` iframe surface 与 `docs-shared-console-bridge` 区域内 API Keys / menu gating 合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与 Docs / API Keys / Webhooks 共享桥接不变，而不是继续把装饰性 heading 或角色路径说明当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_docs_login_scope_real_api.sh`（产物目录 `app-docs-login-scope/20260504-231615`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/docs`、`/api-keys`、`/webhooks`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 `App.test.tsx` 与 `ApiDocsPage.test.tsx` 中的登录注册旅程 / 管理员 docs focused 用例从装饰性 heading 与低信号角色路径说明收敛为更贴近运行时结构与用户行为的 CTA、badge、iframe surface 与 shared-console bridge gating 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案润色与视觉标签调整导致的误报。继续保留既有 Semi UI `findDOMNode`、局部 React `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`App.test.tsx` 其余 focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。

- 本轮继续按"前端优先"收敛 `WebhooksPage.test.tsx` 中供应商 / 管理员角色 focused 用例仍残留的低信号长说明断言：在不改运行时代码的前提下，仅把 role-aware guidance proof 从 `优先使用固定公网出口服务承接回调`、`重点关注 failed / pending 重试链路` 这类易随 copy 润色变化的整句说明，收紧为 `供给事件回调工作台` / `Webhook 运维与回调观测` heading 语义合同，并继续保留 `供应商视角` / `管理员视角` 角色标记真值，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Webhook → API Keys / Docs 接入闭环与真实 API 菜单真值不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_role_heading_scope_real_api.sh`（产物目录 `webhooks-role-heading-scope/20260505-003123`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 `WebhooksPage.test.tsx` 中 supplier/admin 角色 guidance 用例从低信号长说明 copy 收敛为更贴近运行时结构的 heading + role badge 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案润色导致的 focused 误报。继续保留既有 Semi UI `findDOMNode`、Webhooks focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 其余 focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx`、`ApiDocsPage.test.tsx` 与 `WebhooksPage.test.tsx` 中仍残留的低信号 page-global / copy 断言：在不改运行时代码的前提下，仅把设置页首轮引导入口 proof 从普通文本等待收紧为 `首次使用清单` heading + `重新打开首轮引导` button 语义合同，把 docs 管理员 badge 与 bridge/loop lane 标题 proof 收紧为 `aria-label` / scoped heading 合同，并把 Webhook 运维页首屏 proof 从 page-global `最近回调` 指标标题与装饰性说明文本，收紧为 `Webhook 运维与回调观测` heading、`webhooks-latest-delivery-metric` 区域内最新回调信号与稳定指标合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、注册后统一壳、Docs→API Keys→Webhook 单壳桥接与真实 API 菜单真值不变，而不是继续把易变 copy 或重复文本节点当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_focus_signal_scope_real_api.sh`（本轮产物目录 `front-focus-signal-scope/20260505-140634`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/docs`、`/webhooks`、`/api-keys`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 中若干 focused 用例从低信号 page-global 文本/说明 copy 收敛为更贴近运行时结构的 heading、button、badge、metric-card 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案润色与重复文本导致的误报。继续保留既有 Semi UI `findDOMNode`、React `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx` 里 `renders webhook settings page for authenticated admin` 等仍残留的 page-global heading / metric 文本断言，再回到 Phase 4/5 其余后端能力。

## 0. 项目目标

构建一个面向商业化运营的 **邮件接码平台**，核心产品逻辑对齐成熟 **SIM 接码平台**，但资源从“手机号 / 号码池”切换为“域名 / 邮箱别名 / 邮件接收能力”。

### 明确目标
- 后端：**Go**
- 前端：**React**
- 数据存储：
  - 高性能持久存储：**PostgreSQL**
  - 缓存数据库：**Redis**
- 编译与部署：**必须使用 Docker**
- UI 风格：参考 **QuantumNous/new-api**
- 权限模型：**同一套系统页面 + 同一套布局骨架**，仅通过菜单和路由权限扩展区分用户 / 供应商 / 管理员，逻辑对齐 new-api 的共享控制台模式
- 单独页面：必须提供 **API 文档页面**
- 商业目标规模：
  - 注册用户：**1 万+**
  - 并发：**1000+**

---

## 1. 已完成调研结论（用于本计划）

## 1.1 new-api UI / 权限模式结论
基于公开资料调研，`new-api` 的前端特征可提炼为：

- 使用 **React + Vite**
- UI 组件偏向 **Semi Design (`@douyinfe/semi-ui`)** 风格
- 采用 **单一共享控制台布局**：侧边栏 + 顶栏 + 内容区
- 非“用户端 / 管理端”双前端拆分，而是在 **同一前端壳** 中按角色决定可见菜单与路由
- 管理员角色通过菜单项追加、路由守卫与权限控制显示更多能力

### 对 Nexus-Mail 的直接落地要求
- 只维护 **一个 React 控制台项目**，不拆成三个独立后台
- 通过角色与权限声明控制菜单：
  - 普通用户菜单
  - 供应商扩展菜单
  - 管理员扩展菜单
- 所有角色共享相同 Layout：
  - `Sidebar`
  - `Topbar`
  - `Content`
  - `Breadcrumb`
  - `GlobalNotice / Toast`
- 严禁做三套皮肤逻辑分叉；差异应体现在：
  - 菜单树
  - 页面权限
  - 页面中局部操作按钮

---

## 1.2 架构与中间件调研结论
结合商业项目目标与 1000+ 并发要求，推荐基础设施：

### 核心推荐
- **Go 后端**：高并发 API 与异步处理主服务
- **PostgreSQL**：主业务数据库，系统唯一事实源（system of record）
- **PgBouncer**：PostgreSQL 连接池层，避免高并发直接打爆数据库连接
- **Redis**：缓存、会话、限流、幂等键、短生命周期状态
- **RabbitMQ（Quorum Queues）**：可靠异步任务队列
- **MinIO**：原始邮件 MIME、附件、审计快照等对象存储
- **OpenAPI + Redoc**：单独 API 文档页面
- **Postfix**：公网 25 端口 SMTP 接收边缘
- **Go Mail Processor**：在 Postfix 后方做业务解析，而不是直接把 Go API 暴露成公网 SMTP 第一跳

### 主流邮箱供应商接入结论
供应商资源不能只局限于“自建域名邮箱”，还必须纳入主流公网邮箱与商业邮箱账号池。规划上应支持：

- 自建域名 / 自建邮局
- Google 体系：Gmail、Google Workspace
- Microsoft 体系：Outlook.com、Hotmail、Live、Microsoft 365
- Apple：iCloud Mail
- Yahoo 体系：Yahoo Mail、AOL Mail
- 中国公网邮箱：QQ 邮箱、163 邮箱、126 邮箱、yeah 邮箱
- 商业邮箱：Zoho Mail、Fastmail、Yandex Mail、GMX、Mail.com
- 特殊邮箱：Proton Mail（通过 Proton Bridge）

### 接入方式结论
系统设计上必须原生支持以下授权 / 协议模式：
- `MX / SMTP inbound`
- `IMAP pull`
- `POP3 pull`
- `OAuth2`
- `App Password`
- `授权码`
- `Bridge / 本地代理`

### 统一资源建模结论
为了避免后续实现混乱，必须统一采用以下分类：
- `source_type`
  - `self_hosted_domain`
  - `hosted_mailbox`
  - `public_mailbox_account`
  - `bridge_mailbox`
- `auth_mode`
  - `password`
  - `oauth2`
  - `app_password`
  - `authorization_code`
  - `bridge_local_credential`
- `protocol_mode`
  - `smtp_inbound`
  - `imap_pull`
  - `pop3_pull`
