# Nexus-Mail 完整开发计划（todo.md）

> **执行模式已切换：** 本文档不再只是规划稿，而是当前仓库的**执行中路线图**。后续提交必须对照阶段目标更新状态，并在阶段完成后进入下一阶段。

## 执行状态看板（持续维护）

### 当前总体进度
- Phase 0：**已完成**（开发底座、Docker、多服务骨架已提交）
- Phase 1：**已完成**（身份权限、共享控制台骨架已提交）
- Phase 2：**已完成**（项目/库存/激活订单主链路、管理员项目配置、供应商资源录入已提交）
- Phase 3：**已完成**（已完成 SMTP 会话落盘、metadata 入库、RabbitMQ 解析任务入队、MinIO 原始对象上传，并打通验证码/link 提取回退增强、订单 READY/FINISHED 自动迁移、真实 OAuth2 刷新接入、授权码/App Password/密码型凭证录入与健康状态落库、`env://` secret_ref 解析、IMAP/POP3 实际登录校验、官方邮箱端点限制、Proton Bridge 接入策略与 Postfix 转发样例）
- Phase 4：进行中（已完成用户钱包、冻结余额、成功扣费、超时退款、供应商待结算余额、管理员调账能力与基础结算页面；已完成供应商报表筛选、供货规则维护、管理员争议单显式筛选、供应商域名运营页、管理员供应商运营页与共享控制台深色单壳导航收敛；已完成 Webhooks / 风控 / 审计真实页面收敛与对应真实 API 回放验证；当前前端后续重点继续围绕 shared-console focused 用例去脆弱化、语义锚点收敛与 new-api 风格单壳一致性补强，再回到 Phase 4/5 余下后端能力）
- Phase 5：进行中（已完成管理员高危操作二次确认、OpenAPI + `/docs`、管理员审计查询、管理员概览/风控真实页、API Key 默认 scopes / 白名单 / Redis 全局限流 / 审计、Webhook 异步投递与 delivery 查询、风控规则 CRUD 等里程碑；后续在“前端先行、真实 API 验证、五维评审后提交”的约束下继续推进 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片）
- Phase 6：未开始

### 本机环境现状（2026-04-13 更新）
- `go version`：可用
- `docker version`：可用
- `docker compose version`：可用
- `pnpm -v`：可用
- 本机已验证可绑定 **25 端口**

### 本地参考资料
- 已在项目目录放置竞品调研参考：`docs/reference-local/luckyous-reference.md`
- 该目录已加入本地 Git 排除规则，**仅作为本机开发参考，不推送远程仓库**

### 最新执行检查点（2026-05-22，ApiKeysPage 命名 region / shared-console bridge 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，选择 `web/src/pages/ApiKeysPage.tsx` 与 `web/src/pages/ApiKeysPage.test.tsx` 作为下一前端 focused slice，而不是继续扩散到新的后端逻辑。目标不是新增 API Key 后端能力、不是改 `/auth/menu` 真值、不是拆独立后台，而是在既有 API Key 生命周期、白名单、审计与 shared-console bridge 已经具备真实页面和真实 API 的前提下，把 hero、当前密钥、创建表单、审计卡片与 bridge 导航 proof 从残留的 `data-testid` 主导查询进一步收口为 named `role="region" + aria-labelledby` 合同，并保持 projects / webhooks / docs 仍留在同一登录后的单壳路由里。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ApiKeysPage.tsx`、`web/src/pages/ApiKeysPage.test.tsx` 并审查现有未提交 diff，确认根因不是 API Key 路由失效、不是 `/auth/menu` 漂移、也不是真实 API 契约变化，而是 ApiKeysPage 仍有多处关键区块沿用 `Card title + data-testid` 作为主要 proof，focused tests 也仍混用 `findByTestId('api-keys-current-keys-card')`、`getByTestId('api-keys-shared-console-bridge')` 与全局 heading 断言。随后先在测试侧制造并完成 RED→GREEN：把 supplier/admin role-aware bridge、创建表单、当前密钥、审计卡片、空态/回退与 bridge 导航断言统一切换到 `findByRole('region', { name: ... })`；运行时代码仅做最小实现，为 hero、创建 API Key、当前密钥、审计日志补 `role="region" + aria-labelledby` 与显式 heading id，保留 whitelist editor 等尚未进入本轮 slice 的局部 test-id，不改任何后端接口、鉴权逻辑或角色菜单真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 通过（14 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/App.test.tsx` 通过（37 tests，ApiKeysPage focused + App shared-shell 集成仍稳定）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面，本轮先发现历史脚本 `.hermes/local-output/run_api_keys_region_heading_real_api.sh` 仍把 repo root 错算到 `.hermes/local-output` 且包含已被 redaction 污染的 token/password 片段，控制器未据此误判应用故障，而是按本地忽略脚本安全规则改用控制器侧一次性 Python 回放完成真实验证，并把最新成功产物写入 `.hermes/local-output/api-keys-region-heading-real-api/20260522-121205`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/auth/api-keys/audit?limit=5`、`/api/v1/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`，并确认 supplier 菜单继续保留 `/supplier/domains`、`/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/audit?limit=5` 返回 `200`，并确认 admin 菜单继续保留 `/admin/users`、`/admin/suppliers`、`/admin/pricing`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；前端 `http://127.0.0.1:5173/`、`/login`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（本轮诚实完成的是 ApiKeysPage 命名 region / bridge 语义收口，而不是夸大为新的 API Key 业务能力或新的后端权限切片；single-shell role-aware bridge 叙事保持不变）；代码质量 APPROVED（运行时改动仅集中在 hero、创建卡、当前密钥与审计卡片四个区块的语义增强，tests 同步从 test-id 收口到 region + heading proof，没有引入新的 wrapper traversal；独立评审仅提示 whitelist editor 仍是局部 test-id，可作为后续 slice）；安全/集成 SAFE（静态 added-line 扫描无 secrets / shell / eval / SQL 风险；控制器补充审查确认 diff 未更改 auth/menu/route contract，真实 API 再次证明 user/supplier/admin 菜单与 API Keys/Webhooks/Docs/shared routes 鉴权链路稳定；对历史 redacted 本地脚本的问题已 fail-closed 处理，没有把坏脚本当成应用故障）；测试/可靠性 PASS（focused Vitest、App 集成、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新的 region 查询更贴近用户可见合同，同时保留了 projects / webhooks / docs 导航与 revoke/whitelist/create 行为级保护）；性能/运维 PASS（仅新增轻量语义锚点与 heading，不增加请求、状态或部署负担；build 仅保留既有 chunk-size warning。额外记录一条运维后续：历史 `run_api_keys_region_heading_real_api.sh` 仍需在后续本地清理切片中修复 root 解析与 redaction 污染，避免再次误导 unattended replay）。

### 最新执行检查点（2026-05-22，ProfilePage 角色焦点 / 共享接入桥接语义收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，选择 `web/src/pages/ProfilePage.tsx` 与 `web/src/pages/ProfilePage.test.tsx` 作为下一前端 focused slice，而不是继续扩散到后端逻辑。目标不是新增 profile 后端能力、不是改 `/auth/menu` 真值、不是拆独立后台，而是在既有个人资料页已经承担账号中枢与角色连续路径入口的前提下，把 role focus / role expansion 从残留的 `title`/`data-testid` proof 收口为 named `role="region" + aria-labelledby` 合同，并保持 user/supplier/admin 仍然只通过既有共享控制台菜单与路由边界进入对应能力区。
- 控制器按 TDD + `systematic-debugging` 已先回读 `web/src/pages/ProfilePage.tsx` 与 `web/src/pages/ProfilePage.test.tsx`，确认根因不是 Profile 路由失效、不是 shared-console 菜单漂移、也不是真实 API 契约变化，而是 profile 的两个关键卡片仍依赖裸 `title`/`data-testid`，未与页面可访问语义一致。随后已做最小实现：为 role focus 与 role expansion card 补 `role="region" + aria-labelledby`，把 card title 迁入真实 heading；测试侧同步把 profile 头图与角色说明 proof 收口为 `getByRole('region', { name: ... })` 与 in-region heading 断言，保留 profile→API Keys/Webhook/Docs 的行为级导航断言，不改任何后端接口、菜单真值或 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/App.test.tsx` 通过（32 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_profile_region_real_api_20260522.sh`，最新产物目录记录于 `.hermes/local-output/profile-region-real-api-20260522/latest.txt`，最新一次产物为 `profile-region-real-api-20260522/20260522-101847`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`，并确认 supplier 菜单继续保留 `/supplier/domains`、`/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/audit?limit=5` 返回 `200`，并确认 admin 菜单继续保留 `/admin/users`、`/admin/suppliers`、`/admin/pricing`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；前端 `http://127.0.0.1:5173/`、`/login`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（本轮诚实完成的是 ProfilePage 角色焦点与角色扩展语义收口，而不是夸大为新的 profile 或权限能力；单一登录后 shared-console/new-api 单壳叙事与 user/supplier/admin 角色边界未被 diff 破坏）；代码质量 APPROVED（diff 仅集中在 1 个运行时文件与 1 个 focused 测试文件，运行时代码只补 heading/region 语义，测试同步升级为 region + heading 合同，没有扩散到路由/服务层）；安全/集成 SAFE（静态 added-line 扫描无 secrets / shell / eval / SQL 风险，真实 API 再次证明注册、菜单、Profile/API Keys/Webhooks/Docs/Settings 与 supplier/admin 真实链路稳定）；测试/可靠性 PASS（focused Vitest、App integration、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新的 region 查询仍保留 Profile→API Keys/Webhook/Docs 的行为级导航保护，比裸 test-id 更贴近用户可见合同）；性能/运维 PASS（仅新增轻量语义锚点与 heading，不引入新请求、状态、部署或容器假设，build 仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-22，SupplierDomainsPage named-region / fallback 语义收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，选择 `web/src/pages/SupplierDomainsPage.tsx` 与 `web/src/pages/SupplierDomainsPage.test.tsx` 作为下一前端 focused slice，而不是继续扩散到后端逻辑。目标不是新增 supplier 后端能力、不是改 `/auth/menu` 真值、不是拆独立供应商后台，而是在既有域名池运营页已经具备真实 supplier overview/domain create/shared bridge 的前提下，把 hero、供应商主任务流、共享接入桥接与两个 fallback 从残留的弱 test-id / 裸按钮容器进一步收口为 named `role="region" + aria-labelledby` 合同，对齐仓库里更成熟的 SupplierOfferingsPage / shared-console 页面语义模式。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/SupplierDomainsPage.tsx`、`web/src/pages/SupplierDomainsPage.test.tsx`，并与 `SupplierOfferingsPage` 对比确认根因：Domains 页虽然已有 capability matrix region 与 shared bridge 文案，但 hero 仍缺少 named region，mission section 仍只靠 `data-testid`，mission fallback / shared-console fallback 仍是裸 `div + Button`，focused tests 也因此继续依赖 `getByTestId(...)`。随后仅做最小实现：为 hero card 补 `role="region" + aria-labelledby="supplier-domains-hero-heading"`；为 mission section 补命名 region；把 mission fallback 与 shared-console fallback 改为带 heading/说明文案/button test id 的 Card region；并把 shared bridge 的 region 语义上提到外层 Card。测试侧同步制造并完成 RED→GREEN：将 hero / mission / bridge / fallback 的 proof 从 `getByTestId(...)` 收口为 `getByRole('region', { name: ... })` 与 in-region heading/文案/按钮断言，只保留 mission-flow/bridge 内部局部 test id 作为必要 scoped 容器。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx` 通过（7 tests，仅既有 React/Semi `act(...)` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_supplier_domains_region_real_api.sh`，最新产物目录记录于 `.hermes/local-output/supplier-domains-region-real-api/latest.txt`，最新一次产物为 `supplier-domains-region-real-api/20260522-015901`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；非法邮箱注册返回 `400`；短密码注册返回 `400`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`，并通过 `POST /api/v1/supplier/resources/domains` 成功创建一条真实域名记录，证明域名运营页对应的 real API 入口仍工作；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/audit?limit=5` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（本轮诚实完成的是 SupplierDomainsPage named-region/fallback 语义收口，而不是夸大为新的 supplier 业务能力；hero/mission/bridge/fallback 现已直接保护 shared-console 命名区域）；代码质量 APPROVED（diff 仅集中在 1 个运行时文件与 1 个 focused 测试文件，语义增强与测试收口范围一致，未扩散到后端）；安全/集成 SAFE（静态 added-line 扫描无 secrets / shell / eval / SQL 风险，真实 API 再次证明 user/supplier/admin 菜单、supplier overview/domain create 与 shared routes 鉴权链路稳定）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新的 region 查询比裸 test-id / 裸 fallback button 更贴近用户可见合同）；性能/运维 PASS（仅新增轻量语义容器与文案，无额外运行时开销，build 仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-22，SettingsPage 角色连续路径运行时入口收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先把 `SettingsPage` 从“仅有静态快捷入口/首轮说明”推进到更真实的角色连续路径运行时入口，而不是继续只做 test-id/命名 region 微调。目标不是改后端角色模型、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有 Dashboard 首轮引导、Settings 首次使用清单、shared-console 单壳与深色 new-api 风格基础上，让设置页本身也能根据当前服务端角色与实际菜单，直接给出 user/supplier/admin 各自的“继续当前角色主路径”卡片组。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/utils/consoleNavigation.tsx`、`web/src/pages/SettingsPage.tsx`、`web/src/pages/SettingsPage.test.tsx` 与 `web/src/App.test.tsx`，确认根因不是 Settings 路由失效、不是 shared-console 菜单漂移、也不是真实 API 契约变化，而是 Settings 运行时仍把“连续路径”能力停留在普通用户首次使用清单与若干静态 shortcut/bridge 文案上，supplier/admin 则缺少同等级的 runtime follow-up lane。随后先在测试侧制造 RED：为 `SettingsPage.test.tsx` 新增 user / supplier / admin 三条 `继续当前角色主路径` 命名 region 断言，并把既有普通用户首轮清单从 test-id 查询收口为命名 region；同时在 `consoleNavigation` 单测中新增 `resolveContinueConsoleSteps(...)` 的 user/supplier/admin 顺序与动作文案断言。运行时代码仅做最小实现：在 `web/src/utils/consoleNavigation.tsx` 中新增 `ContinueConsoleStep` 与 `resolveContinueConsoleSteps(menu, role)`，按服务端真实菜单生成 role-aware 下一步卡片；在 `web/src/pages/SettingsPage.tsx` 中新增 `settings-continue-console-lane` 具名 region，把 user 显示为 项目市场 / 订单中心 / API Keys，supplier 显示为 域名池运营中枢 / 供应商资源 / 供应商资金与争议指挥台，admin 显示为 供应商管理 / 风控中心 / 审计日志；并把普通用户 `首次使用清单` 也补成真实 named region，确保 Settings 页从首轮清单、共享接入桥接到角色连续路径都留在同一 shared-console 壳内。
- 控制器验证结果：`pnpm --dir web exec vitest run src/utils/consoleNavigation.test.ts src/utils/consoleNavigation.test.tsx src/pages/SettingsPage.test.tsx src/App.test.tsx` 通过（50 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_settings_continue_lane_real_api.sh`，最新产物目录 `.hermes/local-output/settings-continue-lane-real-api/20260522-012130`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 且不泄漏 `/admin/risk`、`/supplier/resources`、`/supplier/settlements`；seeded supplier 登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 返回 `200`，并确认 supplier 菜单继续保留 `/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 返回 `200`，并确认管理端菜单继续保留 `/admin/users`、`/admin/suppliers`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；前端 `http://127.0.0.1:5173/`、`/login`、`/settings`、`/api-keys`、`/webhooks`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（本轮不再只是 test-only 命名补强，而是把 Settings 页真正提升为 role-aware 连续路径入口：user/supplier/admin 都获得与当前菜单一致的下一步卡片，且仍留在单一 shared-console/new-api 壳内）；代码质量 APPROVED（diff 主要集中在 `web/src/utils/consoleNavigation.tsx`、`web/src/pages/SettingsPage.tsx` 与相关 focused tests，新增 helper 复用真实路由真值而非复制页面逻辑，保持最小前端 slice）；安全/集成 SAFE（纯前端运行时入口增强，不触碰 auth/session/backend contract；静态 added-line 扫描无 secrets / shell / eval / SQL 风险；真实 API 回放再次证明 user/supplier/admin 菜单与 Settings/API Keys/Webhooks/shared routes 鉴权链路稳定）；测试/可靠性 PASS（consoleNavigation 单测、Settings focused、App integration、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新的 role-aware lane 由 route-truth helper 驱动，比零散 shortcut/fallback 更诚实稳定）；性能/运维 PASS（仅新增轻量前端 helper 与设置页卡片渲染，无后端部署开销；构建仍仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-21，LoginPage 注册文案与 tabpanel 语义收口）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/LoginPage.tsx` 与 `web/src/pages/LoginPage.test.tsx` 这两处 focused 前端切片，不扩散到新的后端逻辑。目标不是改注册接口、不是改 `/auth/menu` 真值、不是引入独立后台，而是在既有登录/注册页与共享控制台单壳都已成型的前提下，把注册模式 helper 文案收口为更诚实的“后续菜单与页面能力按角色开放”，并为认证模式切换补上 `tab` -> `tabpanel` 的可访问语义连接，保持 LoginPage 的注册跑道与 role-based shell 叙事一致。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/LoginPage.tsx`、`web/src/pages/LoginPage.test.tsx`，确认根因不是注册功能缺失、不是菜单/路由漂移、也不是真实 API 契约变化，而只是注册 helper 文案仍旧高估了 post-auth continuation，且 `login-auth-mode-switch` 的两个 tab 还缺少 `aria-controls` / `tabpanel` 语义。随后仅做最小切片：把注册 helper 文案改为“注册后直接进入共享控制台，后续菜单与页面能力按角色开放。”；为登录/注册两个 tab 增补稳定 id 与 `aria-controls="login-auth-panel"`；在 auth shell 外层补 `role="tabpanel"` 与 `aria-labelledby`，并在 focused test 中显式检查 `tablist`、`aria-controls`、`tabpanel` 与 banner 文案，不改任何后端接口、菜单真值或 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（12 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings）；`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（35 tests，仅既有 shared-shell / Semi warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_login_helper_copy_real_api.sh`，最新产物目录 `.hermes/local-output/login-helper-copy-real-api/20260521-134318`：验证 `GET /healthz` 返回 `200 + redis=ok + status=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs` 且不泄漏 `/admin/risk`、`/supplier/resources`；seeded supplier 登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 返回 `200`；seeded admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（注册 helper 文案不再把 post-auth continuation 夸大成固定后续路径，tab/tabpanel 语义与 login/register shell 的可访问结构一致，focused test 也已显式锁住这层语义）；代码质量 APPROVED（diff 极小，仅集中在 1 个运行时文件与 1 个 focused 测试文件，selector 与语义补强均保持最小切片）；安全/集成 SAFE（静态 added-line 扫描无 secrets / shell / eval / SQL 风险，纯前端语义补强不触碰 auth/session/menu/backend contract；真实 API 回放再次证明注册、菜单与 shared routes 鉴权链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新的语义锚点比裸 test-id 更诚实稳定）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本，未新增运维风险）。

### 最新执行检查点（2026-05-21，Orders/Projects hero 与首轮 lane named-region 合同补强）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/OrdersPage.tsx`、`web/src/pages/OrdersPage.test.tsx`、`web/src/pages/ProjectsPage.tsx`、`web/src/pages/ProjectsPage.test.tsx` 这 4 个 Orders/Projects focused 前端文件，而不是扩散到新的后端逻辑。目标不是改注册后角色模型、不是改 `/auth/menu` 真值、不是新增独立采购/履约后台，而是在既有 user shared-console 采购→履约→接入链路已经成型的前提下，把 Orders/Projects hero、共享接入桥接与首轮 lane 从 `data-testid` 主导的 proof 继续收口为命名 `role="region" + aria-labelledby` 合同，并保持 local route-stub 断言与真实页面语义一致。
- 控制器按 TDD + `systematic-debugging` 先回读当前工作树中的 4 个目标文件，确认根因不是 Projects/Orders 运行时 CTA 缺失、不是 shared-console route/menu 漂移、也不是真实 API 契约变化，而只是两个页面仍残留若干 hero / bridge / lane 语义锚点未完全显式命名，focused tests 也仍混用 `getByTestId(...)` 与全局 heading proof。随后仅做最小切片：为 `OrdersPage` 的 hero 与 continuation lane 增补 `role="region" + aria-labelledby`，并把相关 focused tests 升级为 `findByRole('region', { name: '订单中心' | '订单结果 → API 接入 → 再次采购' | '共享接入桥接' })`；为 `ProjectsPage` 的 hero 增补命名 region，并把 bridge / first-run lane tests 升级为 `findByRole('region', { name: '项目市场' | '共享接入桥接' | '采购 → 履约 → 接入' })`。未改任何后端接口、未改菜单真值、未改角色边界。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/OrdersPage.test.tsx src/pages/ProjectsPage.test.tsx` 通过（27 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings）；`pnpm --dir web exec vitest run src/pages/OrdersPage.test.tsx src/pages/ProjectsPage.test.tsx src/App.test.tsx` 通过（50 tests，仅既有 shared-shell / Semi warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_orders_projects_region_real_api.sh`，最新产物目录 `.hermes/local-output/orders-projects-region-real-api/20260521-020015`：验证 `GET /healthz` 返回 `200 + status=ok`；非法邮箱注册返回 `400`；短密码注册返回 `400`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/orders/activations`、`/projects/inventory`、`/auth/api-keys`，并显式确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 且不泄漏 `/admin/risk`、`/admin/pricing`、`/supplier/resources`、`/supplier/offerings`；seeded admin 登录后 `/auth/menu`、`/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/auth/menu`、`/supplier/resources/overview` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/projects`、`/orders`、`/api-keys`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（Orders/Projects 本轮仅补强 shared-console 命名 region 合同，没有把 local focused proof 夸大成新后端能力或独立后台）；代码质量 APPROVED（diff 极小，主要是 `region + aria-labelledby + heading` 对齐与测试 selector 从 test-id 收口到可访问语义；仅修正了 `ProjectsPage.test.tsx` 中一次 `const` 复赋值回归）；安全/集成 SAFE（纯前端语义补强，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明 user/supplier/admin 菜单与 Orders/Projects 链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、真实 API 回放全部通过，新的 named-region 查询比裸 test-id 更诚实稳定）；性能/运维 PASS（零运行时代码/部署开销，仅补 landmark 语义与 focused 护栏，未新增构建或部署风险）。
