# Nexus-Mail 完整开发计划（todo.md）

> **执行模式已切换：** 本文档不再只是规划稿，而是当前仓库的**执行中路线图**。后续提交必须对照阶段目标更新状态，并在阶段完成后进入下一阶段。

## 执行状态看板（持续维护）

### 最新执行检查点（2026-05-23，AdminUsersPage 管理员主任务流 / fallback focused 合同从 wrapper traversal 收口到 named region）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，不提前扩散到新的后端 Webhook / API Key / 风控 / 审计逻辑，而是优先处理 `AdminUsersPage` 中仍残留的 shared-console mission-flow / fallback focused 旧 proof。目标不是新增管理员钱包、结算或争议后端接口，不是改 `/auth/menu` 真值，也不是重做用户管理页运行时 UI，而是在既有深色 shared-console/new-api 页面与管理员角色菜单已经稳定的前提下，把 `AdminUsersPage.test.tsx` 对 `管理员主任务流` 与 `回到共享工作台继续管理员主链路` 的查询从 `findByRole(...heading...).closest('.semi-card')` 收口到用户可见的 `getByRole('region', { name: ... })` / `findByRole('region', { name: ... })`，让 focused 合同贴近真实无障碍树与运行时语义，而不是继续依赖 Semi UI 包装结构。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/AdminUsersPage.tsx` 与 `web/src/pages/AdminUsersPage.test.tsx`，确认根因不是 `/admin/users` 路由失效、不是真实 `/api/v1/auth/menu` 管理员菜单漂移、也不是 mission-flow / fallback 运行时缺失，而是 focused 文件仍有三处通过 heading + `.closest('.semi-card')` 进入 shared-console 区域，和运行时代码已提供的 `role="region" + aria-labelledby` 契约不一致。随后先在测试侧 RED：将 mission-flow render proof、mission-flow 导航 proof 与 fallback proof 统一切到 named region 查询；控制器补跑后 GREEN，无需修改运行时代码，说明本轮属于 AdminUsersPage focused 合同继续收口，而不是新的运行时功能开发。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminUsersPage.test.tsx` 先 RED 后 GREEN 通过（8 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web exec vitest run src/pages/AdminUsersPage.test.tsx src/App.test.tsx` 通过（33 tests，仅既有 warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 继续使用本地忽略脚本 `.hermes/local-output/run_admin_users_region_real_api.sh`，最新产物目录为 `.hermes/local-output/admin-users-region-real-api/20260523-024137`：验证 `GET /healthz` 返回 `200` 且 `redis=ok`；动态注册普通用户后 `/api/v1/auth/me`、`/api/v1/auth/menu` 返回 `200` 且菜单继续包含 `/projects`、`/orders`、`/api-keys`，普通用户访问 `/api/v1/admin/risk` 继续返回 `403`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`；前端 `/`、`/admin/users`、`/admin/risk`、`/settings`、`/api-keys` shell 均返回 `200`。本轮五维评审摘要保存于 `.hermes/local-output/admin-users-region-review-20260523.md`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 诚实完成的是 AdminUsersPage mission-flow / fallback focused 合同从 wrapper traversal 收口到 named region，不夸大为新的用户管理/钱包/争议后端能力）；代码质量 APPROVED（移除 `.closest('.semi-card')` 后，测试更少依赖 Semi UI DOM 包装结构，且 region 查询与运行时 `role=region + aria-labelledby` 契约一致）；安全/集成 SAFE（纯测试侧改动，不触碰 auth/menu/session/backend contract，真实 API 回放继续证明 user/admin/supplier 路由与权限边界稳定）；测试/可靠性 PASS（mission-flow 导航 destination proof、fallback heading/button proof 仍保留，coverage 未弱化，且查询入口更贴近真实无障碍树）；性能/运维 PASS（无运行时代码、请求、状态或部署负担变化，仅提升 focused 测试稳健性）。

### 最新执行检查点（2026-05-23，SupplierResourcesPage / AdminAuditPage 残留 bridge/fallback focused 合同继续从 test-id 收口到 named region）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，不提前扩散到新的后端 Webhook / API Key / 风控 / 审计逻辑，而是优先收掉供应商资源页与管理员审计页中仍残留的 shared-console bridge / capability / fallback focused 旧 proof。目标不是新增供应商资源或管理员审计后端接口，不是改 `/auth/menu` 真值，也不是重做两页运行时 UI，而是在既有深色 shared-console/new-api 页面、角色差异菜单与真实 API 回放已经稳定的前提下，把 `SupplierResourcesPage.test.tsx`、`AdminAuditPage.test.tsx` 中对 `共享接入桥接`、`控制台能力矩阵`、`返回共享工作台` 的查询继续从 `getByTestId(...)` 收口到用户可见的 `getByRole('region', { name: ... })`，让 focused 合同更贴近真实无障碍树与可见语义。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/SupplierResourcesPage.tsx`、`web/src/pages/SupplierResourcesPage.test.tsx`、`web/src/pages/AdminAuditPage.tsx`、`web/src/pages/AdminAuditPage.test.tsx`，确认根因不是 `/supplier/resources` / `/admin/audit` 路由失效、不是真实 `/auth/menu` 菜单漂移、也不是 bridge/fallback 运行时缺失，而是这两个 focused 文件仍有少量测试从 `supplier-resources-shared-console-bridge`、`supplier-resources-capability-matrix`、`admin-audit-shared-console-bridge`、`admin-audit-shared-console-fallback` 等 test id 进入查询。随后先在测试侧 RED：把 SupplierResources 的 bridge/capability region 身份断言切到 `getByRole('region', { name: '共享接入桥接' })` 与 `getByRole('region', { name: '控制台能力矩阵' })`；把 AdminAudit 的 bridge 导航、fallback 抑制与 fallback 隐藏用例切到 `getByRole('region', { name: '共享接入桥接' })`、`queryByRole('region', { name: '控制台能力矩阵' })`、`getByRole('region', { name: '返回共享工作台' })`。focused RED 后无需修改运行时代码即可 GREEN，说明本轮属于前端 focused 合同继续收口，而不是新的运行时功能开发。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/AdminAuditPage.test.tsx` 先 RED 后 GREEN 通过（18 tests）；`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/AdminAuditPage.test.tsx src/App.test.tsx` 通过（43 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 继续执行本地忽略脚本：`.hermes/local-output/run_supplier_resources_region_real_api.sh` 最新产物目录为 `.hermes/local-output/supplier-resources-region-real-api/20260523-021943`，验证 `GET /healthz` 返回 `200`，普通用户动态注册后 `/api/v1/auth/me`、`/api/v1/auth/menu` 返回 `200` 且菜单继续包含 `/projects`、`/orders`、`/api-keys`、`/webhooks`、`/settings`；supplier seed 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/resources/offerings`、`/api/v1/supplier/settlements` 返回 `200`，并继续通过菜单确认 `/supplier/domains` 路径仍由 shared-console 暴露；admin seed 登录后 `/api/v1/auth/menu`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 返回 `200`，普通用户访问 `/api/v1/admin/risk` 继续返回 `403`；前端 `/`、`/supplier/resources`、`/api-keys`、`/webhooks`、`/docs` shell 均返回 `200`。管理员审计页复核继续使用既有脚本 `.hermes/local-output/run_admin_audit_region_real_api.sh`，最新产物目录为 `.hermes/local-output/admin-audit-region-real-api/20260523-021607`。本轮五维评审摘要保存于 `.hermes/local-output/20260523-frontslice-review.md`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 诚实完成的是 SupplierResourcesPage / AdminAuditPage 残留 bridge/fallback focused 合同从 test id 继续收口到 named region，不夸大为新的资源/审计后端能力）；代码质量 APPROVED（diff 集中在两个 focused 文件，沿用既有 runtime region/heading 契约，没有删掉导航 destination proof，也没有退回到 wrapper traversal）；安全/集成 SAFE（纯测试侧改动，不触碰 auth/menu/session/backend contract，真实 API 回放再次证明 user/supplier/admin 菜单与受保护接口边界稳定）；测试/可靠性 PASS（focused、grouped、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，既有 React/Semi warnings 未被本轮 diff 放大）；性能/运维 PASS（无运行时代码增量，不增加请求、状态或部署负担，本地 replay/review 产物继续保存在忽略目录，便于后续无上下文复查）。

### 最新执行检查点（2026-05-23，ApiDocsPage 共享接入桥接 / 文档回流 lane focused 合同从 test-id 收口到 named region）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，不提前扩散到新的后端 Webhook / API Key / 风控 / 审计逻辑，而是优先处理 `/docs` 页面里仍残留的 shared-console bridge / loop lane focused 旧 proof。目标不是新增 API 文档后端接口、不是改 OpenAPI/Redoc 内容、也不是重做 docs 运行时 UI，而是在既有 dark shared-console/new-api 文档工作台、注册后 `API Keys → Webhooks → Docs` 连续路径与真实 API 回放已经稳定的前提下，把 `ApiDocsPage.test.tsx` 中 `docs-shared-console-bridge` / `docs-shared-console-loop` 的查询入口收口到用户可见的命名 region，并让运行时代码对 bridge lane 显式暴露 `aria-label="共享接入桥接"`，使 focused 合同贴近真实无障碍/可见语义，而不是继续依赖实现细节 test id。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ApiDocsPage.tsx`、`web/src/pages/ApiDocsPage.test.tsx` 与 `web/src/utils/consoleNavigation.tsx`，确认根因不是 `/docs` 路由失效、不是真实 `/api/v1/auth/menu` 菜单漂移、不是 API Keys / Webhook / Balance 页面不可达，也不是注册后共享控制台接入链路断裂，而是 docs 页 focused 用例仍多处从 `getByTestId('docs-shared-console-bridge')` / `getByTestId('docs-shared-console-loop')` 进入查询。先在测试侧 RED：将 bridge / loop lane 用例切换为 `getByRole('region', { name: '共享接入桥接' })` 与 `getByRole('region', { name: '文档核对后继续沿共享控制台接入链路推进' })`；初次 RED 暴露 bridge lane 运行时尚未具备对应 accessible name，控制器随后只做最小运行时修正：删除 bridge/loop lane 旧 `data-testid`，为 bridge lane 增加 `role="region" aria-label="共享接入桥接"`，保留 loop lane 的 `section role="region" aria-labelledby="docs-shared-console-loop-heading"` 命名合同，其余共享控制台叙事、按钮路径、页面内容与角色菜单 gating 保持不变。期间控制器还发现并修复 `web/src/utils/consoleNavigation.tsx` 中已存在的 `API_KEYS_ROUTE='***'` 常量异常，恢复为真实 `/api-keys` 常量后重新补跑导航相关 focused / grouped 测试，确认这一仓内常量恢复与本轮 docs lane 语义收口共同构成可提交前端切片。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 先 RED 后 GREEN 通过（6 tests）；`pnpm --dir web exec vitest run src/utils/consoleNavigation.test.tsx src/pages/ApiDocsPage.test.tsx src/App.test.tsx` 通过（39 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 继续使用并执行本地忽略脚本 `.hermes/local-output/run_apidocs_region_real_api.sh`，最新产物目录为 `.hermes/local-output/apidocs-region-real-api/20260523-014647`：验证 `GET http://127.0.0.1:8080/healthz` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/docs`、`/api-keys`、`/webhooks`、`/balance` 均返回 `200`；动态注册普通用户后 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`、`/api/v1/wallet/overview` 返回 `200`，并继续确认普通用户菜单包含 `/`、`/api-keys`、`/webhooks`、`/docs`、`/balance` 且不泄漏 `/admin/risk`、`/supplier/resources`、`/supplier/settlements`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`。本轮五维评审摘要保存于本地忽略文件 `.hermes/local-output/apidocs-region-review-20260523.md`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 诚实完成的是 ApiDocsPage 共享接入桥接 / 文档回流 lane focused 合同从 test-id 收口到 named region，并顺手恢复真实 `/api-keys` 路由常量，不夸大为新的文档/接入后端能力）；代码质量 APPROVED（bridge lane 通过 `aria-label="共享接入桥接"` 暴露稳定可见语义，loop lane 继续复用现有 heading 驱动的命名 region，测试以 `within(region)` 保留可见 heading/button 合同，未弱化导航 proof）；安全/集成 SAFE（diff 不引入新的 auth/session/permission 语义，按钮路径仍受既有 menu gating 约束，`API_KEYS_ROUTE` 恢复为真实 `/api-keys` 后导航/菜单/route 常量与真实页面再次对齐）；测试/可靠性 PASS（focused Vitest、navigation helper focused、ApiDocsPage + App grouped、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且 bridge/loop lane 不再依赖实现细节 test id）；性能/运维 PASS（运行时代码仅增加轻量 aria 语义并删除 test id，不新增请求/状态/部署负担；构建仍仅保留既有大 chunk warning，未被本轮 diff 放大，本地 replay/review 产物继续保存在忽略目录便于后续无上下文复查）。

### 最新执行检查点（2026-05-23，LoginPage / App 注册接入路径 focused 合同从 test-id 收口到 named region）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理登录页与集成壳里仍残留的注册入口 focused 旧 proof，而不是提前扩散到新的后端 Webhook / API Key / 风控逻辑。目标不是新增登录/注册后端接口、不是改变 `/auth/register` 真实行为、不是重做登录页运行时 UI，而是在既有深色 single shared-console/new-api 登录壳、注册后共享控制台主链路已经稳定的前提下，把 `LoginPage.test.tsx` 与 `App.test.tsx` 中“首轮接入路径”查询从 `login-register-journey` test id 收口到用户可见的 `getByRole('region', { name: '首轮接入路径' })`，让 focused proof 更贴近真实无障碍/可见语义合同。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/LoginPage.tsx`、`web/src/pages/LoginPage.test.tsx` 与 `web/src/App.test.tsx`，确认根因不是注册功能缺失，不是真实 `/api/v1/auth/register` 契约漂移，也不是共享控制台注册后首轮接入链路失效，而是两处 focused 用例仍从 `data-testid="login-register-journey"` 进入查询。由于运行时代码已经稳定暴露 `role="region" + aria-labelledby="login-register-journey-heading"` 与可见标题“首轮接入路径”，因此先在测试侧 RED：把 `LoginPage.test.tsx` 的 `getRegisterJourneyScope()` 与 `App.test.tsx` 登录壳注册 CTA 用例改为通过具名 region 获取 scope；补跑后 GREEN，无需修改运行时代码，说明本轮是 shared-console 登录/注册入口 focused 合同收口，而不是新的运行时功能开发。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（14 tests）；`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（39 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 继续使用并执行本地忽略脚本 `.hermes/local-output/run_login_register_journey_real_api.sh`，最新产物目录为 `.hermes/local-output/login-register-journey-real-api/20260523-011920`：验证 `GET /healthz` 返回 `200`；动态注册普通用户后 `/api/v1/auth/me`、`/api/v1/auth/menu` 返回 `200`，菜单包含 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；前端共享控制台 `http://127.0.0.1:5173/` 正常返回 shell。五维评审摘要保存于本地忽略文件 `.hermes/local-output/login-register-journey-review-20260523.md`。
- 本轮五维评审结论：产品/规格 PASS（diff 诚实完成的是 LoginPage / App 注册接入路径 focused 合同从 test id 收口到 named region，而不是夸大为新的登录/注册后端能力）；代码质量 APPROVED（`getByRole('region', { name: '首轮接入路径' })` 与 `LoginPage.tsx` 中真实 `aria-labelledby` 契约一致，变量名 `registerJourneyRegion` 也更准确）；安全/集成 SAFE（纯测试侧改动，不触碰 auth/session/backend/menu 真值，shared-console 注册 CTA、注册表单 region 与后续接入链路证明仍在）；测试/可靠性 PASS（focused Vitest、grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且查询入口更贴近用户可见语义）；性能/运维 PASS（无运行时代码增量，不增加请求、状态或部署负担，本地 replay / review 产物继续保存在忽略目录便于后续无上下文复查）。

### 最新执行检查点（2026-05-23，consoleNavigation 路由优先级/continue-step 语义收口）
- 本轮仍遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：围绕 `web/src/utils/consoleNavigation.tsx` 与 `web/src/utils/consoleNavigation.test.tsx` 收口 shared-console 路由/标题/continue-step 生成逻辑，把 unknown path 的标题回退恢复为 `/` 之外的原始 pathname，以保持 breadcrumb/diagnostic 可读性，同时把 `ADMIN_USERS_ROUTE` 的 continue-step 文案改回与真实 `用户管理` 菜单一致，并把 `ADMIN_PRICING_ROUTE` 的前后端中文标签统一回 `价格策略`。此外补了 focused 测试覆盖 `resolveContinueConsoleSteps()` 的 user/admin 语义与 unknown route 回退合同。
- 控制器验证结果：`pnpm --dir web exec vitest run src/utils/consoleNavigation.test.tsx` 通过（9 tests）；`pnpm --dir web exec vitest run src/utils/consoleNavigation.test.tsx src/App.test.tsx` 通过（34 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 回放脚本 `.hermes/local-output/run_console_navigation_contract_real_api.sh` 通过，确认健康检查、普通用户注册后的 `/api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`，以及 admin 的 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 都保持 200，前端 `/`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit`、`/admin/suppliers` shell 均可达。
- 五维评审结论：产品/规格 PASS、代码质量 APPROVED、security/integration SAFE、test/reliability PASS、performance/ops PASS；该切片已达到当前可提交状态。
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理 DashboardPage 中普通用户首轮推荐下一步 lane 的 focused 合同，而不是提前扩散到新的后端 Webhook / API Key / 风控逻辑。目标不是新增仪表盘后端接口、不是改 `/auth/menu` 真值、也不是重做 dashboard 运行时 UI，而是在既有 shared-console 新样式与首轮采购→履约→接入链路已经跑通的前提下，把该 lane 的 focused proof 从纯实现细节 test-id 为主，收敛到“结构锚点 + 用户可见 heading/button 语义”的混合合同，保持 single shared-console/new-api 单壳叙事稳定。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/DashboardPage.tsx` 与 `web/src/pages/DashboardPage.test.tsx`，确认根因不是 `/balance`、`/projects`、`/orders`、`/api-keys` 路由失效，不是真实 `/auth/menu` 菜单漂移，也不是 DashboardPage 运行时缺少推荐下一步卡片，而是 focused 用例仍主要依赖 `dashboard-next-step-*` / `dashboard-next-step-action-*` test id。先在测试侧 RED：尝试改为以用户可见 heading/button 作为更高层 proof；独立代码质量评审随后指出如果完全删去结构锚点，会削弱对卡片/动作骨架存在性的保护。控制器据此整改：最终保留 `dashboard-next-step-balance`、`dashboard-next-step-projects`、`dashboard-next-step-orders` 与对应 action test id 的结构性断言，同时新增对 `先确认预算与钱包状态`、`再进入项目市场采购`、`随后追踪订单履约`、`查看余额中心`、`前往项目市场`、`查看订单中心` 的可见语义校验，并继续对 `dashboard-next-step-api-keys` 卡片内的 heading/button 做 scoped proof。运行时代码无需修改，说明本轮是 Dashboard focused 测试合同去脆弱化与语义补强，而不是新的 UI/后端功能开发。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx` 先 RED 后 GREEN 通过（10 tests）；`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx` 通过（35 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 继续使用并执行本地忽略脚本 `.hermes/local-output/run_dashboard_next_step_region_real_api.sh`，最新产物目录为 `.hermes/local-output/dashboard-next-step-region-real-api/latest`：验证前端 `http://127.0.0.1:5173/`、`/settings`、`/profile`、`/balance`、`/projects`、`/orders`、`/api-keys` 均返回 `200`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/wallet/overview`、`/api/v1/projects/inventory`、`/api/v1/orders/activations`、`/api/v1/auth/api-keys`，并继续确认普通用户菜单包含 `/balance`、`/projects`、`/orders`、`/api-keys` 且不泄漏 `/admin/risk`、`/supplier/resources`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`。本轮五维评审摘要已保存到本地忽略文件 `.hermes/local-output/dashboard-next-step-region-review-20260523.md`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 诚实完成的是 DashboardPage 推荐下一步 lane focused 合同从纯 test-id 依赖升级为“结构锚点 + 可见语义”的组合 proof，而不是夸大为新的 dashboard 能力或新的后端接口）；代码质量 APPROVED（初版 reviewer 认为完全移除结构锚点会弱化合同，控制器已据此补回前三张卡片与 action 的 test id 存在性断言，同时保留用户可见 heading/button proof，最终平衡了稳定结构与语义覆盖）；安全/集成 SAFE（纯测试侧改动，不触碰 auth/menu/session/backend contract，真实 API 再次证明 user/admin/supplier 菜单与共享控制台主链路稳定）；测试/可靠性 PASS（Dashboard focused、Dashboard+App grouped、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新的 heading/button proof 没有牺牲原有 card/action 骨架保护）；性能/运维 PASS（无运行时代码增量，不增加请求、状态或部署负担，local replay 与 review 产物继续保存在忽略目录，便于后续无上下文复查）。

### 最新执行检查点（2026-05-23，DashboardPage 首轮推荐下一步 lane focused 合同从 wrapper traversal 收口到 lane-scoped test-id + visible heading）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先收掉 DashboardPage 首轮推荐下一步 lane 里残留的 wrapper traversal focused proof，而不是扩散到新的后端 Webhook / API Key / 风控逻辑。目标不是改运行时 dashboard 行为、不是改 `/auth/menu` 真值、也不是新增 dashboard 后端接口，而是在既有 `推荐下一步` lane 已提供 `dashboard-next-step-api-keys` test id 与用户可见 `开发者 API 接入工作台` heading 的前提下，把 focused 用例从 `parentElement` 链式遍历收口到更稳定的 lane-scoped test id + visible heading 组合。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/DashboardPage.test.tsx` 与 `web/src/pages/DashboardPage.tsx`，确认根因不是 `/balance`、`/projects`、`/orders`、`/api-keys` 路由失效，不是真实 `/auth/menu` 菜单漂移，也不是推荐下一步卡片运行时缺失，而是 focused 测试在 API Keys integration 卡上仍通过 `button.parentElement?.parentElement?.parentElement` 进入查询。随后先在测试侧 RED：改为先获取 `推荐下一步` named region，再在 `within(lane)` 内直接定位 `dashboard-next-step-api-keys`，并通过 `within(integrationCard).getByRole('heading', { name: '开发者 API 接入工作台' })` 做语义 proof；补跑后 GREEN，无需修改运行时代码，说明本轮是 dashboard focused 合同收口，而不是新的运行时功能开发。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx` 通过（10 tests）；`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx` 通过（35 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build` 通过；`git diff --check` 通过。真实 API 继续复用本地忽略脚本 `.hermes/local-output/run_dashboard_shared_shell_real_api.sh`，最新产物目录为 `.hermes/local-output/dashboard-shared-shell-real-api/20260523-071058`：验证 `GET /healthz` 返回 `200`，admin 登录后 `/api/v1/auth/menu` 返回 `200` 且菜单包含 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/supplier/domains`、`/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/admin/users`、`/admin/suppliers`、`/admin/pricing`、`/admin/risk`、`/admin/audit`、`/docs`；`/api/v1/dashboard/overview` 与 `/api/v1/admin/overview` 均返回 `200`。
- 本轮五维评审摘要保存于 `.hermes/local-output/dashboard-next-step-region-review-20260523.md`，结论为：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS。


### 最新执行检查点（2026-05-22，OrdersPage 空态 / 履约任务流 named-region 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理普通用户注册后采购 → 履约 → 接入主链路中的 `OrdersPage` focused 切片，而不是提前扩散到新的后端 Webhook / API Key / 风控逻辑。目标不是新增订单后端接口、不是改 `/auth/menu` 真值、不是拆独立履约后台，而是在既有 OrdersPage 已承接深色共享控制台履约叙事的前提下，把空态下一步 CTA 区与 `履约任务流` 卡从残留 `test id` proof 继续收口为用户可见的 named-region 合同。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/OrdersPage.tsx`、`web/src/pages/OrdersPage.test.tsx` 与现有 diff，确认根因不是 `/orders` 路由失效、不是真实 `/orders/activations` / `/projects/inventory` / `/auth/api-keys` 契约漂移、也不是角色菜单变化，而是空态 CTA 区仍只暴露 `orders-empty-state-actions` test id，`履约任务流` 卡也还没有显式 named region，导致 focused 用例无法直接通过用户可见语义证明首轮履约后的下一步动作。随后先在测试侧 RED：把三组空态用例与履约任务流用例改为查询 `getByRole('region', { name: '订单为空时的下一步' })` 与 `getByRole('region', { name: '履约任务流' })`；初次 RED 失败后，运行时代码只做最小实现：为空态 CTA 区补上 `section role=region + aria-labelledby=orders-empty-state-actions-heading` 与显式 heading/文案，为履约任务流卡补上 `role=region + aria-labelledby=orders-fulfillment-guidance-heading` 与语义 title heading；其余 shared-console bridge、continuation lane、auth/menu、订单数据加载与后端契约保持不变。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/OrdersPage.test.tsx` 先 RED 后 GREEN 通过（12 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；随后 `pnpm --dir web exec vitest run src/pages/OrdersPage.test.tsx src/App.test.tsx` 通过（37 tests，Orders focused + App shared-shell 集成稳定）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_orders_region_real_api.sh`，最新产物目录为 `.hermes/local-output/orders-region-real-api/latest`：验证 `GET /healthz` 返回 `200`；前端 `http://127.0.0.1:5173/orders`、`/projects`、`/api-keys`、`/settings` 均返回 `200` 且继续提供 SPA shell；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/orders/activations`、`/api/v1/projects/inventory`、`/api/v1/auth/api-keys`，并显式确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/api-keys`、`/settings`、`/docs` 且不泄漏 `/admin/risk`、`/supplier/resources`、`/supplier/settlements`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 诚实完成的是 OrdersPage 空态 / 履约任务流 named-region 收口，而不是夸大为新的订单后端能力；single shared-console/new-api 单壳叙事保持不变）；代码质量 APPROVED（diff 集中在 1 个运行时文件与 1 个 focused 测试文件，测试从 test-id 迁移到用户可见 region/heading 后更贴近真实合同；仅保留少量遗留 test id 作为后续可选清理）；安全/集成 SAFE（diff 不触碰 auth/menu/route 真值与后端契约，CTA 仍受 `hasMenuPath(...)` / `resolvePreferredConsoleRoute(...)` 约束，真实 API 再次证明 user/admin/supplier 菜单边界稳定）；测试/可靠性 PASS（Orders focused、Orders+App grouped、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新语义锚点均被 focused 用例直接覆盖；既有 React/Semi warnings 未被本轮 diff 放大）；性能/运维 PASS（仅新增轻量 DOM/ARIA 语义包装，不引入额外请求、状态或部署负担，真实 API 回放产物和五维评审摘要均保存在本地忽略目录，便于后续无上下文复查）。

### 最新执行检查点（2026-05-22，SettingsPage 控制台运行快捷入口 region 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先选择 `SettingsPage` 中仍残留 `test id` 依赖、但直接承接注册后 shared-console 接入入口的快捷入口轨道作为下一可提交前端切片，而不是提前扩散到新的 Phase 4/5 后端逻辑。目标不是新增设置后端接口、不是改 `/auth/menu` 真值、也不是拆独立设置后台，而是在既有 `SettingsPage` 已完成 hero / capability matrix / shared bridge / continue-current-role lane 命名 region 收口的前提下，把 focused 用例里 `控制台运行快捷入口` 的 proof 从 `getByTestId('settings-shortcut-cards')` 继续收口为用户可见的 `getByRole('region', { name: '控制台运行快捷入口' })`，保持“单一登录后控制台、角色差异菜单与页面仍留在同一壳内”的 new-api 风格契约。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/SettingsPage.tsx`、`web/src/pages/SettingsPage.test.tsx`，确认根因不是 `/settings` 路由失效、不是真实 `/auth/menu` 菜单漂移、也不是 shortcut cards 运行时缺失，而是 focused 测试在 shared bridge 之后仍通过 `settings-shortcut-cards` test id 定位快捷入口轨道，和运行时代码已经提供的 `role="region" + aria-labelledby="settings-shortcut-cards-heading"` 合同不一致。随后先在测试侧 RED：把该段断言改为 `screen.getByRole('region', { name: '控制台运行快捷入口' })` 并用 `shortcutScope` 继续做 scoped heading / button proof；初次 GREEN 尝试中额外加入了对标题纯文本的重复断言，但五维 reviewer 指出该断言与 region accessible-name proof 轻度冗余，控制器随即删去该冗余文本断言，仅保留 region + scoped CTA/heading 合同。运行时代码无需修改，说明这一轮属于 focused 测试合同收口，而非新的 UI/后端功能开发。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 通过（10 tests）；`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/App.test.tsx` 通过（35 tests，仅既有 React/Semi `act(...)` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_settings_shortcut_region_real_api.sh`，最新产物目录为 `.hermes/local-output/settings-shortcut-region-real-api/latest`：验证前端 `http://127.0.0.1:5173/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs` 均返回 `200`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/settings` 且不泄漏 `/admin/risk`、`/supplier/resources`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 诚实完成的是 SettingsPage “控制台运行快捷入口” focused 合同从 test id 向命名 region 收口，而不是夸大为新的设置/接入运行时能力；single shared-console/new-api 单壳叙事保持不变）；代码质量 APPROVED（初版 reviewer 指出对标题纯文本的重复断言轻微冗余，控制器已删除，仅保留基于 named region 的 scoped heading/button proof；最终 diff 极小且更贴近无障碍树合同）；安全/集成 SAFE（纯测试侧改动，不触碰 auth/menu/session/backend contract；真实 API 再次证明 user/admin/supplier 菜单与 `/settings`、`/api-keys`、`/webhooks` 共享路由链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新的 region 查询减少了对实现细节 test id 的依赖）；性能/运维 PASS（无运行时代码增量，不增加请求、状态或部署负担，本地 replay 与 review 产物保存于忽略目录，便于后续无上下文复查）。

### 当前总体进度
- Phase 0：**已完成**（开发底座、Docker、多服务骨架已提交）
- Phase 1：**已完成**（身份权限、共享控制台骨架已提交）
- Phase 2：**已完成**（项目/库存/激活订单主链路、管理员项目配置、供应商资源录入已提交）
- Phase 3：**已完成**（已完成 SMTP 会话落盘、metadata 入库、RabbitMQ 解析任务入队、MinIO 原始对象上传，并打通验证码/link 提取回退增强、订单 READY/FINISHED 自动迁移、真实 OAuth2 刷新接入、授权码/App Password/密码型凭证录入与健康状态落库、`env://` secret_ref 解析、IMAP/POP3 实际登录校验、官方邮箱端点限制、Proton Bridge 接入策略与 Postfix 转发样例）
- Phase 4：进行中（已完成用户钱包、冻结余额、成功扣费、超时退款、供应商待结算余额、管理员调账能力与基础结算页面；已完成供应商报表筛选、供货规则维护、管理员争议单显式筛选、供应商域名运营页、管理员供应商运营页与共享控制台深色单壳导航收敛；已完成 Webhooks / 风控 / 审计真实页面收敛与对应真实 API 回放验证；当前前端后续重点继续围绕 shared-console focused 用例去脆弱化、语义锚点收敛与 new-api 风格单壳一致性补强，再回到 Phase 4/5 余下后端能力）
- Phase 5：进行中（已完成管理员高危操作二次确认、OpenAPI + `/docs`、管理员审计查询、管理员概览/风控真实页、API Key 默认 scopes / 白名单 / Redis 全局限流 / 审计、Webhook 异步投递与 delivery 查询、风控规则 CRUD 等里程碑；后续在“前端先行、真实 API 验证、五维评审后提交”的约束下继续推进 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片）
- Phase 6：未开始

### 本机环境现状（2026-04-13 更新）
- `go version`：可用
- `docker version`：可用
- `docker compose version`：可用
- `pnpm -v`：可用
- 本机已验证可绑定 **25 端口**

### 本地参考资料
- 已在项目目录放置竞品调研参考：`docs/reference-local/luckyous-reference.md`
- 该目录已加入本地 Git 排除规则，**仅作为本机开发参考，不推送远程仓库**
