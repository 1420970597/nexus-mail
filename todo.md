# Nexus-Mail 完整开发计划（todo.md）

> **执行模式已切换：** 本文档不再只是规划稿，而是当前仓库的**执行中路线图**。后续提交必须对照阶段目标更新状态，并在阶段完成后进入下一阶段。

## 执行状态看板（持续维护）

### 当前总体进度
- Phase 0：**已完成**（开发底座、Docker、多服务骨架已提交）
- Phase 1：**已完成**（身份权限、共享控制台骨架已提交）
- Phase 2：**已完成**（项目/库存/激活订单主链路、管理员项目配置、供应商资源录入已提交）
- Phase 3：**已完成**（已完成 SMTP 会话落盘、metadata 入库、RabbitMQ 解析任务入队、MinIO 原始对象上传，并打通验证码/link 提取回退增强、订单 READY/FINISHED 自动迁移、真实 OAuth2 刷新接入、授权码/App Password/密码型凭证录入与健康状态落库、`env://` secret_ref 解析、IMAP/POP3 实际登录校验、官方邮箱端点限制、Proton Bridge 接入策略与 Postfix 转发样例）
- Phase 4：进行中（已完成用户钱包、冻结余额、成功扣费、超时退款、供应商待结算余额、管理员调账能力与基础结算页面；已完成供应商报表筛选、供货规则维护、管理员争议单显式筛选、供应商域名运营页、管理员供应商运营页与共享控制台深色单壳导航收敛；已完成 Webhooks / 风控 / 审计真实页面收敛与对应真实 API 回放验证；当前前端后续重点继续围绕 shared-console focused 用例去脆弱化、语义锚点收敛与 new-api 风格单壳一致性补强，再回到 Phase 4/5 余下后端能力）
- Phase 5：进行中（已完成管理员高危操作二次确认、OpenAPI + `/docs`、管理员审计查询、管理员概览/风控真实页、API Key 默认 scopes / 白名单 / Redis 全局限流 / 审计、Webhook 异步投递与 delivery 查询、风控规则 CRUD 等里程碑；后续在“前端先行、真实 API 验证、五维评审后提交”的约束下继续推进 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片）
- Phase 6：未开始

### 本机环境现状（2026-04-13 更新）
- `go version`：可用
- `docker version`：可用
- `docker compose version`：可用
- `pnpm -v`：可用
- 本机已验证可绑定 **25 端口**

### 本地参考资料
- 已在项目目录放置竞品调研参考：`docs/reference-local/luckyous-reference.md`
- 该目录已加入本地 Git 排除规则，**仅作为本机开发参考，不推送远程仓库**

### 最新执行检查点（2026-05-10，ApiDocs 控制台能力矩阵 canonical wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/ApiDocsPage.tsx` 与 `web/src/pages/ApiDocsPage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，把 ApiDocs 页 capability matrix 从旧的 `共享控制台能力矩阵` 收口为当前仓库 canonical wording `控制台能力矩阵`，并补齐与其它 shared-console/new-api 页面一致的 3 个深色能力信号标签：`统一文档入口 / 共享接入桥接 / 角色菜单扩展`；继续保持 docs / api-keys / webhooks / balance / projects 都停留在同一登录后的共享控制台里，而不是把文档页误导成独立后台或孤立说明页。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `ApiDocsPage.tsx` / `ApiDocsPage.test.tsx` 回读现状，再先让 focused proof 对新的 capability-matrix 合同 RED。首轮仅修改 `ApiDocsPage.test.tsx` 后，`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 明确失败为运行时仍渲染旧标题 `共享控制台能力矩阵`，且不存在新的 canonical capability tags；根因确认不是 `/docs` 路由缺失、不是 bridge / loop CTA 失效、不是 `/auth/menu` 漂移、也不是真实 API 失败，而只是 ApiDocs 能力矩阵表面仍未与当前仓库 shared-console/new-api canonical wording 对齐。随后仅最小修改 2 个前端文件：运行时代码把 matrix title 收口为 `控制台能力矩阵`，并新增 3 枚深色标签 `统一文档入口 / 共享接入桥接 / 角色菜单扩展`；focused proof 同步改为在 `docs-capability-matrix` 内做 scoped 结构断言，并继续保留对 bridge / loop 路由导航的真实点击 proof。五维评审中代码质量与测试/可靠性 reviewer 共同指出首版补上的 `toContainHTML(...)` 断言过于 DOM 细节化、增益有限；控制器已同轮删除该断言，仅保留新旧 wording 的可见文本正反断言与路由行为 proof，避免把 focused 合同过度绑死在 UI 库内部 DOM 形状上。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 先 RED 后 GREEN，最终通过（5 tests）；`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/App.test.tsx` 通过（27 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning，无新失败）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面执行本地忽略脚本 `.hermes/local-output/run_apidocs_capability_matrix_real_api.sh`，最新产物目录 `.hermes/local-output/apidocs-capability-matrix-real-api/20260510-120859`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且菜单继续暴露 `/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/risk`、`/admin/audit` 全部 `200`；前端 `/docs`、`/api-keys`、`/webhooks` SPA shell 全部 `200`。控制器还对本轮 live diff 新增行执行静态扫描，产物 `.hermes/local-output/apidocs-capability-matrix-static-scan-20260510-controller.json` 显示 hardcoded secret / shell injection / eval-exec / pickle / SQL format 5 类模式均 0 命中。
- 本轮五维评审结论：产品/规格 PASS（能力矩阵 wording 已与当前 single shared-console/new-api 单壳叙事对齐，`统一文档入口 / 共享接入桥接 / 角色菜单扩展` 对 docs 页保持诚实，未引入独立后台心智）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，并已按 reviewer 意见删除低价值 `toContainHTML(...)` 断言，保持最小且清晰的合同）；安全/集成 SAFE（未改 auth/session/menu truth/backend path，route titles 与现有 bridge/loop 行为保持一致，静态扫描 5 类模式 0 命中）；测试/可靠性 PASS（focused、App 关联 grouped rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且未削弱 bridge / loop 的真实导航 proof）；性能/运维 PASS（纯前端静态标签与标题收口，不新增请求、轮询、持久化或部署复杂度，仅延续仓库既有 Vite chunk warning 与 React/Semi warning 作为非阻塞历史噪声）。

### 最新执行检查点（2026-05-10，Login 登录前 shared-console 角色工作区说明补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/LoginPage.tsx` 与 `web/src/pages/LoginPage.test.tsx` 做一个小而完整的登录前前端垂直切片，在不改注册/登录 API、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，为登录页左侧补齐一组更接近 new-api 单壳入口语义的 `角色工作区` 说明卡片，让用户在登录前就能理解共享路由会停留在同一壳内、额外工作区只会在服务端授予对应角色后展开、运营治理仍留在共享控制台，而不是误以为存在第二套独立后台。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `LoginPage.tsx` / `LoginPage.test.tsx` 回读现状，再先让 focused proof 对新增 `login-role-workspaces` 合同 RED。首轮仅修改测试后，`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 明确失败为运行时缺少 `login-role-workspaces` 区块；根因确认不是注册功能缺失、不是 `/auth/menu` 漂移、不是 shared shell 崩坏、也不是真实 API 失败，而只是登录页在“控制台入口信号”与“首轮接入路径”之间还缺少一层更明确的单壳角色展开说明。随后仅最小修改 2 个前端文件：运行时代码新增 3 张静态说明卡片，首版曾直接枚举“普通用户 / 供应商 / 管理员”，经五维评审 fail-closed 指出这会把登录前最小事实升级成角色菜单预览后，已同轮整改为更诚实的三层说明——`共享路由常驻同一壳`、`额外工作区按服务端角色展开`、`运营治理仍留在共享控制台`；focused proof 也同步从逐句硬编码收口为 `expectedRoleWorkspaces` 的 scoped 结构断言，既保护新信息层存在与关键信号，又避免把每句说明都变成高脆弱契约。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 先 RED 后 GREEN，最终通过（8 tests）；`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 多轮 grouped rerun 中仍会命中仓库既有 `App.test.tsx > falls back to the docs workspace when no preferred role landing route exists` 异步噪声，已按 fail-closed 用 isolated rerun 复核通过，不将其误判为当前 diff 回归；其余 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面重跑本地忽略脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录 `.hermes/local-output/app-login-register-scope/20260510-114347`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` shell 全部 `200`，证明本轮登录前信息层补齐没有破坏注册后共享路由与角色工作区的真实后端契约。
- 本轮五维评审结论：产品/规格 PASS（整改后不再在登录前枚举具体角色菜单，而是继续强调“同一入口登录 + 服务端授权后在同一壳内展开工作区”的真实 single-shell 叙事）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，并已按评审意见把测试从逐句硬编码收口为数据驱动的 scoped 结构断言）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/login-role-workspaces-static-scan-20260510.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/backend path）；测试/可靠性 PASS（focused、grouped rerun、isolated rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，仓库既有 App flake 已明确隔离，不误归因当前 diff）；性能/运维 PASS（纯前端静态信息层补齐，不新增请求、轮询、持久化或部署复杂度，仅延续仓库既有 Vite chunk warning 与 App 异步噪声作为非阻塞历史问题）。

### 最新执行检查点（2026-05-10，Projects 采购桥接切片深色单壳收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/ProjectsPage.tsx` 与 `web/src/pages/ProjectsPage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，把 Projects 右侧 `共享接入桥接` 与 `控制台能力矩阵` 收口到更明确的深色单壳采购叙事，继续强调项目采购、履约追踪与开发接入仍停留在同一登录后的共享控制台里，而不是拆成第二套后台。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `ProjectsPage.tsx` / `ProjectsPage.test.tsx` 回读现状，再先让 focused proof 对新 bridge / capability wording RED。首轮仅修改 `ProjectsPage.test.tsx` 后，`pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx` 明确失败为运行时仍保留旧 bridge 文案 `项目采购页不是孤立列表...` 与旧矩阵标签 `共享履约桥接 / 开发接入延伸`；根因确认不是 `/projects` 路由缺失、不是库存/下单真实 API 异常、不是 `/auth/menu` 漂移，也不是注册功能缺口，而只是 Projects 页 bridge / matrix 仍未与当前 shared-console/new-api 深色单壳语气对齐。随后仅最小修改 2 个前端文件：运行时代码把 5 个 menu-truth bridge 文案分支统一收口到深色单壳叙事，把矩阵标签收口为 `统一采购入口 / 共享接入桥接 / 单壳接入延伸`；focused proof 同步覆盖 full state、无下游入口 state，以及新增 `orders+api-keys` / `orders-only` / `api-keys-only` 三个 partial-menu 分支，避免测试只验证全开与全关两种路径。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx` 先 RED 后 GREEN，最终通过（14 tests）；`pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx src/App.test.tsx` 通过（48 tests）；`pnpm --dir web exec vitest run src/App.test.tsx -t "uses the shared API keys route constant for register onboarding entrypoints|keeps shared integration routes reachable after register bootstrap"` isolated rerun 通过；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_projects_shared_console_real_api.sh`，最新产物目录 `.hermes/local-output/projects-shared-console-real-api/20260510-105735`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/me`、`/auth/menu`、`/dashboard/overview`、`/projects/inventory`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded admin 登录后 `GET /api/v1/admin/wallet-users` 与 `POST /api/v1/admin/wallet-adjustments` 全部 `200`；随后用普通用户 Bearer token 成功调用 `POST /api/v1/orders/activations`、`GET /api/v1/orders/activations` 与 `GET /api/v1/orders/activations/:id/result`，产物摘要确认 `menu_paths` 包含 `/projects /orders /api-keys /docs`，并记录创建订单 `created_order_status=ALLOCATED`、`result_terminal=False`，证明 Projects 页采购→履约→接入链路仍与真实后端契约一致。
- 本轮五维评审结论：产品/规格 PASS（Projects bridge / matrix 更贴近单一登录后 shared-console/new-api 深色单壳叙事，且未把隐藏入口写成既成能力）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，并已按评审意见补齐 partial-menu 分支覆盖，使测试合同与运行时 5 个文案分支对齐）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/projects-projects-static-scan-20260510.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，route 常量与 `hasMenuPath(...)` 菜单真值保持不变，没有新增权限漂移）；测试/可靠性 PASS（focused、grouped rerun、isolated rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过；新增 partial-menu proof 后，bridge 文案 5 个状态分支已被如实覆盖，不再只验证全开/全关）；性能/运维 PASS（纯前端文案/标签与测试覆盖收口，不新增请求、轮询、持久化或部署复杂度；真实 API 与现有 compose 服务持续 healthy）。

### 最新执行检查点（2026-05-10，Webhooks 共享接入桥接分层收口与菜单真值补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/WebhooksPage.tsx` 与 `web/src/pages/WebhooksPage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，把原先混在 `注册后首轮回调联调建议` 卡片中的共享接入说明拆成独立 `共享接入桥接` + `控制台能力矩阵` 信息层，并继续保持 Webhooks / API Keys / Docs 全部停留在同一登录后的共享控制台里，而不是拆成独立后台或让桥接文案夸大未开放入口。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `WebhooksPage.tsx` / `WebhooksPage.test.tsx` 回读现状，再先让 focused proof 对新增 `webhooks-shared-console-bridge` 合同 RED。首轮仅修改测试后，`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx` 明确失败为运行时缺少独立 bridge 卡片；根因确认不是 `/webhooks` 路由缺失、不是真实 Webhook API 异常、不是 `/auth/menu` 漂移，也不是注册/登录缺口，而只是 Webhooks 页仍把 bridge/capability 语义混在 first-loop 卡片里，无法像当前 shared-console/new-api 其他页面那样显式表达共享接入层。随后仅最小修改 2 个前端文件：运行时代码新增独立 `webhooks-shared-console-bridge` 卡片、把 capability matrix 留在 bridge 内部、并把 first-loop 卡片收口为纯 onboarding / step lane；focused test 同步改为 bridge 卡片与 CTA 的 scoped proof。首轮五维评审进一步指出 bridge 文案必须对部分菜单开放和零额外入口状态保持诚实，不能在 `API Keys` 或 `Docs` 未暴露时仍写成固定的完整链路，也不能在 fallback 实际不存在时还假装“返回共享工作台”；控制器已同轮整改为：运行时代码新增 `bridgeHeading` / `bridgeDescription` 与 `hasBridgeFallbackRoute`，按 `API Keys + Docs`、仅 `API Keys`、仅 `Docs`、仅当前 `/webhooks` 仍可见、以及可回退到其他共享路由 5 种状态分别输出 honest copy；focused proof 进一步补齐 `API Keys-only`、`Docs-only`、`fallback to /`、`webhooks-only no-fallback` 以及 docs-only CTA 真导航合同，确保 bridge 真正受菜单真值约束而非前端想当然扩权。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx` 最终通过（19 tests）；`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/pages/ApiKeysPage.test.tsx src/pages/ApiDocsPage.test.tsx src/App.test.tsx` 多轮 grouped rerun 中曾先后命中仓库既有 `App.test.tsx` 异步噪声（`falls back to the docs workspace when no preferred role landing route exists`、`uses the shared API keys route constant for register onboarding entrypoints`），均经 isolated rerun 复核为历史 flake 而非当前 diff 回归；当前 diff 的 focused Webhooks、相关 App isolated rerun 与至少一轮 grouped rerun 均已恢复全绿。其余 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面执行并复跑本地忽略脚本产物目录 `.hermes/local-output/webhooks-bridge-real-api/20260510-101418`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/me`、`/auth/menu`、`/dashboard/overview`、`/webhooks/endpoints`、`/auth/api-keys` 全部 `200`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/webhooks/endpoints`、`/admin/audit?limit=5` 全部 `200`；seeded supplier 登录后 `GET /api/v1/auth/menu`、`/webhooks/endpoints` 全部 `200`；前端 `/login`、`/webhooks`、`/api-keys`、`/docs` shell 全部 `200` 且菜单继续暴露 `/webhooks`、`/api-keys`、`/docs` 的真实共享入口。
- 本轮五维评审结论：产品/规格 PASS（bridge 现已对完整/部分/无额外入口状态保持诚实，继续强化“单一登录后控制台 + 菜单真值 + 共享接入桥接”叙事，未把 Webhooks 页误导成独立后台）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，bridge / first-loop 分层清晰，变量命名与分支语义已按评审意见收口）；安全/集成 SAFE（静态扫描新增行 5 类模式 0 命中，未改 auth/session/menu truth/backend path，所有 CTA 继续受 `hasMenuPath` / `resolvePreferredConsoleRoute` / `resolveRouteTitle` 约束，没有权限语义漂移）；测试/可靠性 PASS（focused、grouped rerun、isolated rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，既有 React/Semi `act(...)` / `findDOMNode` warning 未恶化）；性能/运维 PASS（纯前端信息层与测试合同补强，不新增请求、轮询、持久化或部署复杂度，仅保留仓库既有 Vite chunk warning 与 App async flake 作为非阻塞历史噪声）。

### 最新执行检查点（2026-05-10，SupplierResources 共享接入桥接能力矩阵补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/SupplierResourcesPage.tsx` 与 `web/src/pages/SupplierResourcesPage.test.tsx` 做一个小而完整的 supplier 前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，为 SupplierResources 右侧 `共享接入桥接` 区补齐与 SupplierOfferings / SupplierSettlements / ApiKeys / Balance 等页面一致的深色 `控制台能力矩阵`，显式收口 `统一资源入口 / 共享接入桥接 / 服务端菜单扩展` 三个 single-shell 能力信号，继续强调资源准备、接入链路与供应商后续运营仍停留在同一登录后的共享控制台内，而不是拆成独立后台。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `SupplierResourcesPage.tsx` / `SupplierResourcesPage.test.tsx` 回读现状，再先让 focused test 对新增 `supplier-resources-capability-matrix` 合同 RED。首轮仅修改测试后，`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx` 明确失败为运行时缺少能力矩阵；根因确认不是 `/supplier/resources` 路由缺失、不是 shared-console bridge CTA 失效、不是 `/auth/menu` 漂移、不是注册功能缺口，也不是真实 API 异常，而只是 SupplierResources bridge 区仍缺少一层与当前仓库 shared-console/new-api 深色单壳一致的 capability matrix。随后仅最小修改 2 个前端文件：在 bridge 区新增 `supplier-resources-capability-matrix` 深色矩阵卡，并同步让 focused proof 断言 `控制台能力矩阵 / 统一资源入口 / 共享接入桥接 / 服务端菜单扩展`，不扩散到提交逻辑、表单字段、菜单 gating 或任何后端请求。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx` 先 RED 后 GREEN，最终通过（12 tests）；`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx` 通过（26 tests，仅既有 React/Semi `findDOMNode` / `act(...)` / form validation warning，无新失败）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_supplier_resources_capability_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-resources-capability-real-api/20260510-093506`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；seeded supplier `supplier@nexus-mail.local / Supplier123!` 登录后 `GET /api/v1/auth/menu` 返回 `200` 且继续暴露 `/supplier/resources`、`/supplier/domains`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs`；随后真实创建域名 `hermes-093506.mail.nexus.test`、Bridge 账号 `hermes-093506@example.com` 与邮箱池记录 `hermes-093506`，并由二次 `GET /api/v1/supplier/resources/overview` 复核新记录已落入真实 overview 返回。
- 本轮五维评审结论：产品/规格 PASS（新增矩阵继续强化“单一登录后控制台 + 角色差异菜单 + 共享接入桥接”叙事，且未把资源页误导成独立后台）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，新增矩阵与现有 bridge 区结构一致，职责清晰且范围最小）；安全/集成 SAFE（静态扫描 5 类模式 0 命中，未改 auth/session/menu truth/backend path，现有 CTA 与 fallback 继续受 `hasMenuPath` / `resolvePreferredConsoleRoute` 约束，没有权限语义漂移）；测试/可靠性 PASS（focused、supplier vertical slice、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，既有 warning 未恶化；首轮 real-API script 误把 `201` 创建响应当作 `200` 并在未显式传 `address` 时命中邮箱唯一键冲突，已按 fail-closed 修正脚本后复跑通过）；性能/运维 PASS（纯前端静态信息层补齐，不新增请求、轮询、持久化或部署复杂度；新增本地 replay 脚本继续放在忽略目录，不污染仓库）。

### 最新执行检查点（2026-05-10，Settings 共享接入桥接卡补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/SettingsPage.tsx` 与 `web/src/pages/SettingsPage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，为设置中心右侧补齐一张与当前深色 shared-console/new-api 页面一致的 `共享接入桥接` 卡片，让设置页也能显式承接 API Keys / Webhook / Docs 的单壳入口关系，同时继续避免把设置页误导成独立后台或夸大服务端未开放能力。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与上述 2 个文件回读现状，再先让 `SettingsPage.test.tsx` 对新增 `settings-shared-console-bridge` focused contract RED。首轮仅修改测试后，`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 明确失败为运行时缺少 bridge 卡片；根因确认不是 `/settings` 路由缺失、不是设置页现有任务流失效、不是 `/auth/menu` 漂移、也不是真实 API 失败，而只是 Settings 页尚未把共享接入入口显式化到右侧信息层。随后最小修改 `SettingsPage.tsx` 与 `SettingsPage.test.tsx`：运行时代码新增以既有 `missionCards` 为真值的数据复用型 bridge 卡片，focused proof 同步断言 bridge 标题、说明文案、3 个 CTA 与无入口时隐藏合同。五维评审首轮指出 bridge 文案不应硬编码 `shared-console/new-api` 实现词，也不应在部分菜单开放场景下固定宣称 API Keys / Webhook / Docs 全部已开放；控制器已同轮整改为仅在 `missionCards.length > 0` 时展示卡片，并把说明收口为“当前账号已开放的接入入口”，避免能力夸大与集成语义漂移。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 最终通过（6 tests）；`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/pages/LoginPage.test.tsx src/App.test.tsx` 最终通过（36 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning，无新失败；其中一次 grouped run 命中仓库既有 `App.test.tsx > falls back to the docs workspace when no preferred role landing route exists` 异步噪声，isolated rerun 与下一次 grouped rerun 均恢复全绿，按 fail-closed 记为既有噪声而非当前 diff 回归）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。随后执行 `docker compose up -d --build web gateway` 完成前端重建并等待 health 恢复；真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_settings_shared_bridge_real_api.sh`，最新产物目录 `.hermes/local-output/settings-shared-bridge-real-api/20260510-074631`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户返回 `201`，随后 `GET /api/v1/auth/menu`、`/auth/me`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200` 且普通用户菜单继续暴露 `/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；seeded supplier 登录后 `GET /api/v1/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 全部 `200`；前端 `/`、`/login`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs` SPA shell 全部 `200`。
- 本轮五维评审结论：产品/规格 PASS（整改后 Settings bridge 继续保持单一登录后共享控制台叙事，不再使用内部实现命名，也不再夸大未开放入口）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，bridge 直接复用既有 `missionCards` 真值，结构小且职责清晰）；安全/集成 SAFE（静态扫描 5 类模式无新增风险命中，未改 auth/session/menu truth/backend path，bridge CTA 继续受既有菜单过滤约束，无权限绕过与集成语义漂移）；测试/可靠性 PASS（focused、grouped rerun、isolated rerun、build、Go tests、compose config、容器重建与真实 API 回放全部通过，既有 warning 未恶化）；性能/运维 PASS（纯前端信息层补齐，不新增请求、轮询、持久化或部署复杂度，Vite chunk warning 属仓库既有噪声，未被本 diff 恶化）。

### 最新执行检查点（2026-05-10，ApiDocs 共享接入桥接 canonical wording 与注册后 API Keys 落点合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/ApiDocsPage.tsx`、`web/src/pages/ApiDocsPage.test.tsx` 与 `web/src/App.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，把 API 文档页 bridge 标签从旧词 `共享控制台接入桥` 收口为当前仓库 canonical wording `共享接入桥接`，并同步去脆弱化注册后 onboarding → API Keys 的 App 级落点 proof，继续保持 docs / api-keys / webhooks 仍处于同一登录后的 shared-console/new-api 单壳，而不是独立后台或 docs 孤岛。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与上述 3 个文件回读现状，再先让 `ApiDocsPage.test.tsx` 对新 bridge wording 合同 RED。首轮仅修改 focused proof 后，`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 明确失败为运行时仍渲染旧标签 `共享控制台接入桥`；根因确认不是 `/docs` 路由缺失、不是 docs → API Keys / Webhook bridge CTA 失效、不是 `/auth/menu` 漂移，也不是真实 API 失败，而只是 ApiDocs bridge tag 尚未与当前 shared-console/new-api canonical wording 对齐。随后最小修改 `ApiDocsPage.tsx` 与 `ApiDocsPage.test.tsx`：运行时代码仅把 tag 收口为 `共享接入桥接`，focused proof 同步新增新词正向断言与旧词负向断言。期间 grouped 前端回归一度暴露 `App.test.tsx > uses the shared API keys route constant for register onboarding entrypoints` 仍依赖多匹配的 hero heading / hero testid；控制器按 fail-closed 先 isolated 复现并回读 `ApiKeysPage.tsx`，根因确认不是注册功能缺失、不是路由常量错误、也不是 API Keys 页面未渲染，而是该用例把“进入 API Keys 页面”的证明绑在更易重复匹配的 hero heading 上。随后仅最小修改 `App.test.tsx`，把落点 proof 收口为更稳定且更贴近真实页面主体的 `api-keys-current-keys-card` + `当前密钥` heading，可继续真实证明“注册成功后经 onboarding CTA 到达共享 API Keys 页面”。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 先 RED 后 GREEN，最终通过（5 tests）；`pnpm --dir web exec vitest run src/App.test.tsx -t "uses the shared API keys route constant for register onboarding entrypoints"` isolated rerun 通过；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/ApiDocsPage.test.tsx` 最终通过（27 tests，仅既有 React/Semi `act(...)` / `findDOMNode` / jsdom xhr warning，无新失败）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。随后执行 `docker compose up -d --build web gateway` 完成前端重建并等待健康恢复；`GET /healthz` 返回 `200` 且 `{"redis":"ok","status":"ok"}`。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_apidocs_bridge_real_api.sh`，最新产物目录 `.hermes/local-output/apidocs-bridge-real-api/20260510-065554`：再次验证动态注册普通用户返回 `201`；`GET /api/v1/auth/me`、`/auth/menu`、`/auth/api-keys` 全部 `200`，且普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/api-keys` 而不泄漏 `/admin/risk`；前端 `/docs` 与 `/api-keys` HTTP shell 均返回 `200`。静态扫描产物 `.hermes/local-output/apidocs-bridge-static-scan-20260510.json` 显示 hardcoded secret / shell injection / eval-exec / unsafe deserialization / SQL format 5 类模式均 0 命中。
- 本轮五维评审结论：产品/规格 PASS（ApiDocs bridge canonical wording 已与当前 single shared-console/new-api 单壳叙事对齐，没有把 docs 页误导成独立后台；注册后 onboarding → API Keys proof 仍真实证明共享路由落地）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 2 个 focused tests，结构小且职责清晰，`App.test.tsx` 落点合同较原先更稳定也更贴近真实页面主体）；安全/集成 SAFE（静态扫描 5 类模式 0 命中，未改 auth/session/menu truth/backend path，docs bridge / onboarding CTA 继续落在既有共享路由常量上）；测试/可靠性 PASS（focused、isolated rerun、App+ApiDocs grouped rerun、build、Go tests、compose config、diff-check、容器重建后 health 与真实 API 回放全部通过，既有 warning 未恶化）；性能/运维 PASS（纯前端 wording / focused proof 收口，不新增请求、轮询、持久化或部署复杂度，仅保留仓库既有 Vite chunk warning 与测试 warning 作为非阻塞历史噪声）。

### 最新执行检查点（2026-05-10，SupplierDomains 控制台能力矩阵 canonical wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/SupplierDomainsPage.tsx` 与 `web/src/pages/SupplierDomainsPage.test.tsx` 做一个小而完整的 supplier 前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，把域名运营页 `控制台能力矩阵` 从旧的 `单一供应商工作台 / 域名 readiness 优先 / 角色扩展但不伪造升级` 收口为与 `SupplierOfferings / SupplierResources / SupplierSettlements / Balance / Dashboard` 等当前 shared-console/new-api 单壳页面一致的 canonical wording：`统一供给入口 / 共享接入桥接 / 服务端菜单扩展`，继续明确“单一登录后控制台、角色差异菜单、接入链路不拆独立后台”的产品心智。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与上述 2 个文件回读现状，再先让 `SupplierDomainsPage.test.tsx` 对新 capability matrix 标签合同 RED。首轮仅修改 focused proof 后，`pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx` 明确失败为运行时仍渲染旧矩阵标签 `单一供应商工作台 / 域名 readiness 优先 / 角色扩展但不伪造升级`；根因确认不是 `/supplier/domains` 路由缺失、不是 supplier `/auth/menu` 漂移、不是 bridge CTA 导航错误、也不是任何真实 API 失败，而只是 SupplierDomains 页 capability matrix 仍未与当前仓库 shared-console/new-api canonical wording 对齐。随后仅最小修改 2 个前端文件：运行时代码把 `consolePillars` 收口为 `统一供给入口 / 共享接入桥接 / 服务端菜单扩展`，并同步细化 summary，使“服务端菜单扩展”继续明确由服务端菜单授予、不伪造本地升级；focused proof 同步增加新标签正向断言与旧标签负向断言。期间真实 API 首版回放脚本一度误把前端页面路由当成后端 API（错误访问 `/supplier/resources/domains`）并假设结算 summary 路由是 `/supplier/settlements/summary`；控制器按 fail-closed 先检查前端 service 与后端路由真值，确认 SupplierDomains 页真实数据底座仍是 `/supplier/resources/overview`，结算页真实接口是 `/supplier/settlements`，于是立即修正本地忽略脚本后再继续验证，避免把脚本假设错误误判为应用缺陷。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx` 先 RED 后 GREEN，最终通过（7 tests）；`pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx` 通过（25 tests，仅既有 React/Semi `act(...)` / `findDOMNode` / form validation warning，无新失败）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。随后执行 `docker compose up -d --build web gateway` 完成前端重建并等待健康恢复，`GET /healthz` 返回 `200` 且 `{"redis":"ok","status":"ok"}`，前端根路径 `http://127.0.0.1:5173/` 返回 `200` 与 HTML shell。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_supplier_domains_capability_matrix_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-domains-capability-matrix-real-api/20260510-062132`：再次验证动态注册普通用户返回 `201`，随后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded supplier 登录后 `GET /api/v1/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements` 全部 `200` 且 supplier 菜单继续暴露 `/supplier/domains`、`/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs`；管理员登录后 `GET /api/v1/auth/menu`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`。相关静态扫描产物 `.hermes/local-output/supplier-domains-capability-matrix-static-scan-20260510.json` 显示 hardcoded secret / shell injection / eval-exec / unsafe deserialization / SQL format 5 类模式均 0 命中。
- 本轮五维评审结论：产品/规格 PASS（SupplierDomains 能力矩阵已与当前 single shared-console/new-api 单壳 supplier 页面 canonical wording 对齐，没有把域名运营页误导成独立后台，也没有把服务端菜单能力写成本地既成升级）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，改动只涉及 `consolePillars` 与对应合同断言，结构小且职责清晰）；安全/集成 SAFE（静态扫描 5 类模式 0 命中，未改 auth/session/menu truth/backend path；真实 API 首轮脚本误判已被修正并复核通过，当前结论以 live route/service contract 为准）；测试/可靠性 PASS（focused、supplier vertical slice grouped rerun、build、Go tests、compose config、diff-check、容器重建后 health 与真实 API 回放全部通过，既有 React/Semi warning 未恶化）；性能/运维 PASS（纯前端矩阵文案/语义收口，不新增请求、轮询、持久化或部署复杂度，web/gateway 重建后保持 healthy）。

### 最新执行检查点（2026-05-10，Balance 共享接入桥接与能力矩阵补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/BalancePage.tsx` 与 `web/src/pages/BalancePage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，把余额中心右侧信息层收口为与 Dashboard / ApiKeys / Webhooks / Projects / Orders 一致的深色 `控制台能力矩阵` + `共享接入桥接` 组合，明确表达 `统一资金入口 / 共享接入桥接 / 角色菜单扩展` 三个 single-shell 能力信号，并保持充值、争议、采购、履约与程序化接入全部停留在同一登录后的共享控制台里，而不是拆成独立资金后台。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与上述 2 个文件回读现状，并先让 `BalancePage.test.tsx` 对新矩阵标签、独立 bridge 卡片与 bridge CTA 合同 RED。首轮仅修改 focused test 后，`pnpm --dir web exec vitest run src/pages/BalancePage.test.tsx` 明确失败为运行时仍保留旧的 `资金观察与售后同层 / 角色差异仍共用单壳` 文案，且不存在独立 `balance-shared-console-bridge`。根因确认不是 `/balance` 路由缺失、不是钱包真实 API 异常、不是 `/auth/menu` 漂移、也不是共享壳导航错误，而只是 Balance 页尚未完成与当前 shared-console/new-api 单壳一致的信息层收口。随后仅最小修改 2 个前端文件：运行时代码新增 `consoleCapabilitySignals` 与独立 `balance-shared-console-bridge` 卡片，把 capability matrix 顶部收口为 `统一资金入口 / 共享接入桥接 / 角色菜单扩展`，并把 API Keys / Webhook / Docs CTA 集中到 bridge 卡片内；focused proof 同步改为锚定新的 signals 容器、bridge 文案与 bridge 内按钮导航。五维评审过程中代码质量/可靠性 reviewer 进一步指出最初 `getAllByText(...).length > 0` 断言过松、且 fallback 用例未直接覆盖 bridge 内 API Keys CTA 抑制；控制器已同轮整改为给 signal 区新增 `balance-capability-signals` 语义锚点，并补上 `balance-capability-actions` 中 `打开 API Keys` 的负向断言，保持新信息层合同既准确又不过度脆弱。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/BalancePage.test.tsx` 先 RED 后 GREEN，最终通过（7 tests）；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/BalancePage.test.tsx src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx` 通过（54 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning，无新失败）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_balance_shared_console_real_api.sh`（首版脚本因 redaction 与状态码/字段假设失真后已改用 shell 内即时重放验证为准），最新产物目录 `.hermes/local-output/balance-shared-console-real-api/20260510-054411`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok`；前端根路径与 `/balance` HTTP shell 均 `200`；动态注册普通用户返回 `201`；随后 `GET /api/v1/auth/menu`、`/wallet/overview`、`/wallet/transactions` 全部 `200`，且菜单继续暴露 `/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`。相关本地脚本与产物保持在 `.hermes/local-output/` 并受 `.git/info/exclude` 保护，不进入版本库。
- 本轮五维评审结论：产品/规格 PASS（Balance 页已明确收口为 shared-console/new-api 单壳资金入口，没有把余额中心误导成独立后台，也没有夸大当前菜单未暴露能力）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，并已按 reviewer 意见补齐 signal 容器锚点、bridge 抑制断言与 `ConsolePillar` 类型定义，结构小且职责清晰）；安全/集成 SAFE（静态扫描 5 类模式 0 命中，未改 auth/session/menu truth/backend path，bridge CTA 继续受 `hasMenuPath` 约束，没有权限语义漂移）；测试/可靠性 PASS（focused、App+余额+项目/订单 grouped rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过；其间 isolated rerun 复核了仓库既有 docs fallback / onboarding 异步噪声，未归因为当前 diff 回归）；性能/运维 PASS（纯前端信息层补齐，不新增请求、轮询、持久化或部署复杂度，build 仅保留既有 chunk warning，运行与真实 API 链路稳定）。

### 最新执行检查点（2026-05-10，AdminProjects 共享接入桥接与能力矩阵收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/AdminProjectsPage.tsx` 与 `web/src/pages/AdminProjectsPage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，把管理员价格策略页右侧 `共享控制台接入桥` 收口为与其余 admin/shared-console 页面一致的 `共享接入桥接`，并把能力矩阵从旧的 `角色壳模式 / 风控联动 / 审计联动 / 接入入口` 表述，收口为当前仓库 new-api 单壳 canonical wording：`统一运营入口 / 共享接入桥接 / 管理员菜单扩展`。同时保持价格策略页仍是单一登录后 shared shell 内的管理员工作台，而不是独立后台。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与上述 2 个文件回读现状，再先让 focused proof 对新桥接标题与能力矩阵合同 RED。首轮仅修改测试后，`pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx` 明确失败为仍渲染旧标题 `共享控制台接入桥` 与旧能力矩阵行文；根因确认不是 `/admin/pricing` 路由缺失、不是管理员真实 API 失败、不是 `/auth/menu` 漂移，也不是 shared-console bridge CTA 导航错误，而只是价格策略页尚未完成和当前 admin/shared-console 页面一致的桥接/矩阵命名收口。随后仅最小修改 2 个前端文件：运行时代码把 bridge 标题改为 `共享接入桥接`，在能力矩阵顶部新增 3 枚 canonical tags，并把矩阵状态文案改为 `统一运营入口 / 共享接入桥接 / 管理员菜单扩展`；focused test 同步新增新标题/新矩阵断言与旧文案负向断言。五维评审首轮指出 `共享接入桥接` 状态如果仅基于 `bridgeCards.length > 0` 就直接宣称 `API Keys / Webhooks / Docs` 全部同壳联动，会夸大当前菜单真值；控制器已同轮整改为区分 `hasFullConsoleBridge` 与部分可见桥接，运行时仅在三条入口都可见时保留完整联动文案，否则回退到 `API Keys 已与管理员价格策略保持同壳联动，其余接入入口等待菜单开放` 一类按可见 bridge tags 生成的诚实文案，并补上 focused proof 覆盖“无 bridge 菜单”与“仅 API Keys + 风控可见”的降级状态。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx` 先 RED 后 GREEN，最终通过（5 tests）；`pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx src/pages/AdminSuppliersPage.test.tsx src/pages/AdminRiskPage.test.tsx` 通过（19 tests，仅既有 React/Semi warning，无新失败）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_admin_projects_mission_control_real_api.sh`，最新产物目录 `.hermes/local-output/admin-projects-mission-control-real-api/20260510-051352`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/projects`、`/admin/projects/offerings`、`/admin/risk`、`/admin/audit?limit=5`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且菜单继续暴露 `/admin/pricing`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；同时对首个项目执行真实 `PATCH /api/v1/admin/projects/:id` 更新描述并立即 restore，双向都返回 `200`，证明价格策略页仍对齐 live admin project contract；前端 `/admin/pricing`、`/webhooks`、`/docs` SPA shell 全部 `200`。相关本地脚本与产物保持在 `.hermes/local-output/` 并受 `.git/info/exclude` 保护，不进入版本库。
- 本轮五维评审结论：产品/规格 PASS（整改后 bridge/capability wording 与 shared-console/new-api 单壳叙事保持一致，且不再夸大当前菜单未暴露的接入入口）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，并按 reviewer 意见补齐部分可见 bridge 的诚实文案和降级测试合同，结构小且职责清晰）；安全/集成 SAFE（静态扫描 5 类模式 0 命中，未改 auth/session/menu truth/backend path，且 capability matrix 继续受菜单真值约束，没有权限语义漂移）；测试/可靠性 PASS（focused、grouped 前端回归、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，既有 React/Semi warning 未恶化）；性能/运维 PASS（纯前端桥接/矩阵收口，不新增请求、轮询、持久化或部署复杂度；真实 API replay 与当前 compose 服务均保持 healthy）。

### 最新执行检查点（2026-05-10，AdminSuppliers 控制台能力矩阵补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/AdminSuppliersPage.tsx` 与 `web/src/pages/AdminSuppliersPage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，为管理员供应商运营页 `共享接入桥接` 区补齐与 AdminRisk / AdminAudit / AdminUsers 同风格的深色 `控制台能力矩阵`，继续把“单一登录后控制台 / 角色差异菜单 / shared-console bridge”显式化到管理员供应商运营页，而不是退回独立后台心智。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与上述 2 个文件回读现状，并先让 focused proof 对新增 `admin-suppliers-capability-matrix` 合同 RED。首轮仅修改测试后，`pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx` 明确失败为运行时缺少 `admin-suppliers-capability-matrix`，根因确认不是 `/admin/suppliers` 路由缺失、不是管理员真实 API 失败、不是 `/auth/menu` 漂移、也不是 shared-console bridge CTA 失真，而只是 AdminSuppliers bridge 区仍缺少一层与仓库当前 new-api 深色单壳一致的 capability matrix。随后仅最小修改 2 个前端文件：运行时代码在 `sharedConsoleLinks` 可见时补齐一张深色矩阵卡，focused test 同步新增标题与能力标签断言；五维评审中的代码质量 reviewer 进一步指出展示条件语义应显式命名、且应补上“无 shared-console bridge 路由时矩阵不渲染”的负向 proof，控制器已同轮把条件收口为 `canShowConsoleBridge` 并补齐 fallback 场景下 capability matrix 缺失断言，保持契约清晰且继续以服务端菜单真值为准。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx` 先 RED 后 GREEN，最终通过（8 tests）；`pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx src/pages/AdminUsersPage.shared-console.test.tsx src/pages/AdminAuditPage.test.tsx` 通过（18 tests，仅既有 warning，无新失败）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_admin_suppliers_capability_matrix_real_api.sh`，最新产物目录 `.hermes/local-output/admin-suppliers-capability-matrix-real-api/20260510-043339`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/wallet-users`、`/admin/risk`、`/admin/audit?limit=5`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且菜单继续暴露 `/admin/suppliers`、`/admin/users`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；前端 `/admin/suppliers`、`/admin/users`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs` SPA shell 全部 `200`。静态扫描结果显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，相关本地脚本与产物已保持在 `.hermes/local-output/` 并受 `.git/info/exclude` 保护，不进入版本库。
- 本轮五维评审结论：产品/规格 PASS（矩阵补齐后继续强化“单一登录后控制台 + 角色差异菜单 + 共享接入桥接”叙事，且未引入独立后台心智）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，并已按 reviewer 意见补齐显式条件命名与负向合同，结构小且职责清晰）；安全/集成 SAFE（静态扫描 5 类模式 0 命中，未改 auth/session/menu truth/backend path，且 capability matrix 继续受可见 shared links 约束，没有权限语义漂移）；测试/可靠性 PASS（focused、grouped 前端回归、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，既有 React/Semi warning 未恶化）；性能/运维 PASS（纯前端矩阵卡补齐，不新增请求、轮询、持久化或部署复杂度，真实 API 与当前 compose 服务均保持 healthy）。

### 最新执行检查点（2026-05-10，AdminRisk bridge 能力矩阵补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/AdminRiskPage.tsx` 与 `web/src/pages/AdminRiskPage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，为管理员风控页 `共享接入桥接` 区补齐与 AdminAudit / AdminUsers / Dashboard 同风格的深色 `控制台能力矩阵`，继续把“单一登录后控制台 / 角色差异菜单 / shared-console bridge”显式化到风险治理页，而不是退回独立风控后台心智。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与上述 2 个文件回读现状，并先让 focused proof 对新增 `admin-risk-capability-matrix` 合同 RED。根因确认不是 `/admin/risk` 路由缺失、不是管理员真实 API 失败、不是 `/auth/menu` 漂移、也不是 shared-console bridge CTA 失真，而只是风控页 bridge 区仍缺少一层与仓库当前 new-api 深色单壳一致的 capability matrix。随后仅最小修改 2 个文件：`AdminRiskPage` 在 `visibleSharedConsoleLinks.length > 0` 时补齐一张深色矩阵卡，`AdminRiskPage.test.tsx` 同步新增标题与能力标签断言；同时把 fallback 逻辑从重复的 `!canOpenAudit && !canOpenApiKeys && !canOpenDocs` 收口为 `!canShowConsoleBridge`，保持 capability matrix / fallback 都继续以服务端菜单真值为准，不夸大当前账号能力。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminRiskPage.test.tsx` 通过（6 tests）；`pnpm --dir web exec vitest run src/pages/AdminRiskPage.test.tsx src/App.test.tsx` 通过（28 tests，仅既有 warning，无新失败）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_admin_risk_capability_matrix_real_api.sh`，最新产物目录 `.hermes/local-output/admin-risk-capability-matrix-real-api/20260510-040223`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/risk/rules`、`/admin/audit?limit=5` 全部 `200`；seeded supplier 登录后 `GET /api/v1/auth/menu`、`/supplier/resources/overview` 全部 `200`；前端 `/login`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs` SPA shell 全部 `200`。静态扫描产物 `.hermes/local-output/admin-risk-capability-matrix-static-scan-20260510.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中；五维评审纪要已落盘 `.hermes/local-output/admin-risk-capability-matrix-review-20260510.md`。
- 本轮五维评审结论：产品/规格 PASS（矩阵补齐后继续强化“单一登录后控制台 + 角色差异菜单 + 共享接入桥接”叙事，且 matrix / fallback 继续遵循 menu truth，不夸大服务端授权）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，结构最小、合同同步且无额外重构）；安全/集成 SAFE（静态扫描 5 类模式 0 命中，未改 auth/session/menu truth/backend path，且 capability matrix / fallback 继续受可见 shared links 约束）；测试/可靠性 PASS（focused、App grouped rerun、build、Go tests、compose config、diff-check、real API replay 全部通过，既有 warning 未恶化）；性能/运维 PASS（纯前端矩阵卡补齐，不新增请求、轮询、持久化或部署复杂度；构建与 compose 校验均稳定）。

### 最新执行检查点（2026-05-10，AdminAudit / AdminUsers bridge 能力矩阵补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/AdminAuditPage.tsx`、`web/src/pages/AdminAuditPage.test.tsx`、`web/src/pages/AdminUsersPage.tsx`、`web/src/pages/AdminUsersPage.shared-console.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，为管理员审计页与用户运营页的 `共享接入桥接` 区补齐与 Profile / ApiKeys / SupplierOfferings 同风格的深色 `控制台能力矩阵`，继续把“单一登录后控制台 / 角色差异菜单 / shared-console bridge”显式化到管理员运营页，而不是退回独立后台心智。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与上述 4 个文件回读现状，并先让 focused proof 对新增 `admin-audit-capability-matrix` / `admin-users-capability-matrix` 合同 RED。根因确认不是 `/admin/audit` 或 `/admin/users` 路由缺失、不是管理员真实 API 失败、不是 `/auth/menu` 漂移，也不是 shared-console bridge CTA 失真，而只是两页 bridge 区仍缺少一层与仓库当前 new-api 深色单壳一致的 capability matrix。随后仅最小修改 4 个文件：两个运行时页面各自补齐一张深色矩阵卡，两个 focused tests 同步新增标题与能力标签断言。安全/集成评审进一步指出能力矩阵若在无 bridge 菜单能力时仍固定展示，会形成“UI 宣称能力 > 服务端当前授权”的语义漂移；控制器已同轮整改为仅在 `visibleSharedConsoleLinks.length > 0` 时渲染矩阵，并同步把 fallback 场景断言改为 capability matrix 不渲染，保持菜单真值优先级不被前端文案越权覆盖。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminAuditPage.test.tsx src/pages/AdminUsersPage.shared-console.test.tsx` 通过（10 tests）；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/AdminAuditPage.test.tsx src/pages/AdminUsersPage.shared-console.test.tsx` 最终通过（32 tests），其中首轮 grouped run 的 `uses the shared API keys route constant for register onboarding entrypoints` 失败经 isolated rerun 复核后再次 grouped rerun 恢复全绿，按 fail-closed 记为仓库既有异步噪声而非当前 diff 回归；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_admin_audit_users_capability_real_api.sh`，最新产物目录 `.hermes/local-output/admin-audit-users-capability-real-api/20260510-024230`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/auth/me`、`/admin/overview`、`/admin/wallet-users`、`/admin/disputes?limit=5`、`/admin/audit?limit=5`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且菜单继续暴露 `/admin/users`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；前端 `/admin/users` 与 `/admin/audit` SPA shell 全部 `200`。随后执行 `docker compose up -d --build web gateway` 完成重建并再次确认健康恢复；浏览器守护进程本轮启动失败，因此登录后视觉复核降级为 controller-side build / compose / real API 证据，未把 browser provider 问题误判为应用回归。静态扫描产物 `.hermes/local-output/admin-audit-users-capability-static-scan-20260510.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中；五维评审纪要已落盘 `.hermes/local-output/admin-audit-users-capability-review-summary-20260510.md`。
- 本轮五维评审结论：产品/规格 PASS（矩阵补齐后继续强化“单一登录后控制台 + 角色差异菜单 + 共享接入桥接”叙事，且整改后不再在无 bridge 权限时夸大当前账号能力）；代码质量 APPROVED（diff 仅集中在 2 个运行时页面与 2 个 focused tests，结构最小、合同同步且无额外重构）；安全/集成 SAFE（静态扫描 5 类模式 0 命中，未改 auth/session/menu truth/backend path，并已修正 bridge 能力矩阵在未授权场景下的展示语义漂移）；测试/可靠性 PASS（focused、App grouped rerun、isolated rerun、build、Go tests、compose config、diff-check、real API replay 与重建后 health 均通过，首轮 App 失败已确认属于仓库既有异步噪声）；性能/运维 PASS（纯前端矩阵卡补齐，不新增请求、轮询、持久化或部署复杂度；浏览器守护进程失败已按降级策略处理，不阻断当前前端里程碑）。

### 最新执行检查点（2026-05-10，Dashboard shared shell 能力矩阵与桥接卡补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/DashboardPage.tsx` 与 `web/src/pages/DashboardPage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，为总览页补齐 new-api 风格的 `控制台能力矩阵` 与 `共享接入桥接` 卡片，让登录后第一屏直接显式化“统一身份入口 / 共享接入桥接 / 角色菜单扩展”三层 single-shell 能力信号，同时把 API Keys / Webhook / Docs 的接入延续关系留在同一套深色控制台里表达清楚。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `DashboardPage.test.tsx` 回读现状，并先让 focused proof 对新增 `dashboard-capability-matrix` / `dashboard-shared-console-bridge` 合同 RED。根因确认不是 Dashboard 路由缺失、不是登录后 shared shell 崩坏、不是 `/auth/menu` 漂移、也不是真实 API 失败，而是总览页虽然已有 first-run lane、角色任务流与角色菜单映射，却还缺少一个能把 shared-console 的能力矩阵与共享接入桥接显式化的总览层。随后仅最小修改 2 个前端文件：Dashboard 右侧新增深色 capability matrix + bridge 卡片，focused test 同步新增正向合同断言。首轮五维评审指出 bridge CTA 不应绕过 menu truth、Webhook CTA 不应退化成泛化标题；控制器已同轮整改为 `hasMenuPath(...)` 条件渲染、`resolveRouteTitle(WEBHOOKS_ROUTE, user?.role)` 角色化按钮标题，并补上“菜单未暴露时不显示 Webhook / Docs bridge CTA”的负向测试，确保新卡片继续遵循服务端菜单真值而不是前端自作主张扩权。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx` 通过（10 tests）；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/DashboardPage.test.tsx` 经单例隔离复核后复跑整组恢复全绿（32 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。随后执行 `docker compose up -d --build web gateway` 完成前端重建并等待健康恢复，`GET /healthz` 返回 `200` 且 `{"redis":"ok","status":"ok"}`。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_dashboard_shared_shell_real_api.sh`，最新产物目录 `.hermes/local-output/dashboard-shared-shell-real-api/20260510-014608`：再次验证 seeded admin 登录后 `GET /api/v1/auth/menu`、`/dashboard/overview`、`/admin/overview` 全部 `200`，且菜单继续暴露 `/`、`/api-keys`、`/webhooks`、`/docs` 等共享路径；前端 `GET /` 与 `/docs` shell 均 `200`。浏览器在重建后再次登录 admin，已直接看到 Dashboard 中新增的 `控制台能力矩阵` 与 `共享接入桥接` 卡片，以及对应 bridge CTA / role-aware 总览内容。静态扫描产物 `.hermes/local-output/dashboard-shared-shell-static-scan-20260510.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中；五维评审纪要已落盘 `.hermes/local-output/dashboard-shared-shell-review-summary-20260510.md`。
- 本轮五维评审结论：产品/规格 PASS（整改后 bridge CTA 不再绕过菜单真值，继续强化“单一登录后控制台 + 角色差异菜单 + 共享接入桥接”叙事，未引入独立后台或错误能力承诺）；代码质量 APPROVED（diff 仅集中在 Dashboard 运行时与 focused proof，新增卡片、桥接 CTA 与负向测试合同收口一致，未削弱既有总览导航断言）；安全/集成 SAFE（新增 UI 仅复用既有路由常量和 menu truth，没有新增凭据、危险调用或后端契约漂移；真实 API 回放再证 dashboard/admin/menu 契约稳定）；测试/可靠性 PASS（focused Dashboard、App+Dashboard grouped rerun、build、Go tests、compose config、diff-check、浏览器登录 sanity 与真实 API 回放全部通过，首轮 App API Keys 跳转失败已通过 isolated rerun 证实为仓库既有异步噪声而非当前 diff 回归）；性能/运维 PASS（纯前端卡片补齐，不新增请求、轮询、持久化或部署复杂度；重建后的 `web` / `gateway` 健康恢复正常，构建期 chunk-size warning 未新增且本轮不构成当前垂直切片阻塞）。

### 最新执行检查点（2026-05-10，shared shell 拓扑卡补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/components/AppSidebar.tsx`、`web/src/components/AppSidebar.test.tsx`、`web/src/layouts/ConsoleLayout.tsx`、`web/src/components/ConsoleLayout.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，为 shared shell 的侧栏顶部与页头摘要区补齐 `控制台拓扑` 卡片，继续把单一登录后控制台、角色差异菜单与共享接入桥接收口为可见的 new-api 风格信息层。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态、`AppSidebar.test.tsx` 与 `ConsoleLayout.test.tsx` 入手，对新增的 `app-sidebar-workspace-topology` / `console-layout-workspace-topology` 合同先 RED。根因确认不是路由缺失、不是 shared shell 崩坏、不是 `/auth/menu` 漂移、也不是真实 API 失败，而是当前 shared shell 虽已有深色单壳 header/sidebar，却缺少一个能把共享菜单数量、角色工作区数量和接入桥接开放状态直接显式化的拓扑摘要层。随后仅最小修改 4 个文件：`AppSidebar` 基于 `groupedConsolePaths()` 与 `hasMenuPath(...)` 新增拓扑卡；`ConsoleLayout` 基于当前菜单与共享接入路由新增页头拓扑卡；focused tests 同步收口到新的语义锚点与数值合同，不扩散到 store、router、auth、service 或后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx` 通过（7 tests）；`pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx src/App.test.tsx` 通过（29 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_shared_shell_topology_real_api.sh`，最新产物目录 `.hermes/local-output/shared-shell-topology-real-api/20260510-011601`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/me`、`/auth/menu`、`/dashboard/overview`、`/projects/inventory`、`/orders/activations`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且 `/auth/menu` 继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded admin 再次验证 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/webhooks/endpoints` 全部 `200`；seeded supplier 再次验证 `GET /api/v1/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements` 全部 `200`；前端 `/`、`/login`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/settings`、`/docs`、`/admin/pricing`、`/admin/risk`、`/supplier/domains`、`/supplier/resources`、`/supplier/offerings` SPA shell 全部 `200`。浏览器本地 sanity QA 再次确认当前登录入口页整体仍保持统一深色风格且无明显布局破碎；受未登录态限制，browser session 未直接进入登录后控制台，但 controller-side focused tests、build 与真实 API 已作为本轮 shared shell 变更的主验证来源。
- 本轮五维评审结论：产品/规格 PASS（拓扑卡把 shared shell 的共享菜单、角色工作区与接入桥接显式化，继续强化“单一登录后控制台 + 角色差异菜单”叙事，未引入独立后台或错误能力承诺）；代码质量 APPROVED（diff 仅集中在 sidebar/layout 与对应 focused tests，复用 `groupedConsolePaths`、`hasMenuPath`、`resolveRouteTitle` 作为真值，未复制新菜单模型）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/shared-shell-topology-static-scan-20260510.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改鉴权、会话、权限边界与后端契约）；测试/可靠性 PASS（focused sidebar/layout、关联 App、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，既有 React/Semi warnings 未恶化）；性能/运维 PASS（纯前端静态拓扑信息卡，不新增请求、轮询、持久化或部署复杂度；评审纪要已落盘 `.hermes/local-output/shared-shell-topology-review-summary-20260510.md`）。

### 最新执行检查点（2026-05-10，ApiKeys 创建桥接 focused 稳定性补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/ApiKeysPage.test.tsx` 做一个小而完整的前端 focused 测试稳定性切片，不改任何运行时代码、不改注册/登录接口、不改 `/auth/menu` 菜单真值，也不改 Phase 4/5 的 API Key / Webhook / 风控 / 审计后端契约。目标是把 `navigates to shared integration routes from the scoped bridge before and after key creation` 中创建 API Key 前的最后两处逐字符 `user.type(...)` 输入收口为与同文件现有 create-key payload proof 一致的 `fireEvent.change(...)`，降低 grouped run 时的输入/表格时序噪声，同时保持明文密钥出现、bridge CTA 点击与 Webhook 目标路由断言不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `web/src/pages/ApiKeysPage.test.tsx` 回读/复现现状。根因确认不是 API Key 创建功能缺失、不是 `/auth/menu` 漂移、不是 shared-console bridge 路由错误、也不是真实 API 失败，而是该桥接导航用例在创建新密钥前仍保留 2 处 `user.type(...)` 长链输入；它与同文件 `loads and creates api key with trimmed scopes and whitelist` 已采用的 `fireEvent.change(...)` payload 型证明不一致，在带 Semi UI table / skeleton 噪声的 grouped run 中更容易放大等待成本。随后仅最小修改 1 个测试文件：把 `名称` 与 `权限范围` 输入改为 `fireEvent.change(...)`，其余点击、明文 key 展示与 bridge 导航断言全部保持不变。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 通过（14 tests）；`pnpm --dir web exec vitest run src/App.test.tsx -t "shows first-run onboarding guidance for default user dashboard and allows dismissing it"` isolated rerun 通过；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/ApiKeysPage.test.tsx` 首轮命中仓库既有 `dashboard-first-run-dismiss` 异步噪声，按 fail-closed 对该单例 isolated rerun 后再次复跑整组恢复全绿（36 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_api_keys_create_bridge_real_api.sh`，最新产物目录 `.hermes/local-output/api-keys-create-bridge-real-api/20260510-005042`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok`；动态注册普通用户后 `GET /api/v1/auth/me`、`/auth/menu`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；随后真实创建新的 API Key（`POST /api/v1/auth/api-keys` 返回 `201`）并再次 `GET /api/v1/auth/api-keys` 确认新 key 已出现在列表中；前端 `/api-keys`、`/webhooks`、`/docs` SPA shell 全部 `200`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只是将 ApiKeys bridge create-flow 输入方式收口为与既有 focused 合同一致的更稳定路径，不改变任何 shared-console / API Key / Webhook / Docs 工作流或角色语义）；代码质量 APPROVED（diff 仅 1 个测试文件、2 行输入方式修改，与同文件现有创建密钥 payload proof 保持一致，没有削弱明文 key / bridge 导航断言）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/api-keys-create-bridge-static-scan-20260510.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 回放继续通过）；测试/可靠性 PASS（focused ApiKeys、App isolated、group rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，首轮 grouped failure 已确认属于既有 App 异步噪声）；性能/运维 PASS（纯测试层稳定性补强，不新增请求、轮询、持久化或部署复杂度；评审纪要已落盘 `.hermes/local-output/api-keys-create-bridge-review-summary-20260510.md`）。

### 最新执行检查点（2026-05-10，App 注册共享路由 focused 稳定性补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/App.test.tsx` 做一个小而完整的前端 focused 测试稳定性切片，不改任何运行时代码、不改注册/登录接口、不改 `/auth/menu` 菜单真值，也不改 Phase 4/5 的 Webhook / API Key / 风控 / 审计后端契约。目标是把 App 级注册后共享路由可达性用例中的最后一处逐字符 `user.type(...)` 注册表单填充收口为与仓库现有 Login/App 注册测试一致的 `fireEvent.change(...)`，降低 grouped run 中的输入时序抖动，同时保持 `tab` 切换、注册提交、首轮 onboarding 与共享菜单路径断言不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `web/src/App.test.tsx` 回读/复现现状。根因确认不是注册功能缺失、不是 `/auth/menu` 漂移、不是真实 API 失败、不是共享控制台路由错误，而是 `keeps shared integration routes reachable after register bootstrap` 这条 App 集成用例仍保留 3 处 `user.type(...)` 长链输入；它与同文件 `submitRegistration(...)` helper、`LoginPage.test.tsx` 已完成的注册输入稳定化策略不一致，在带 shell/bootstrap 噪声的 grouped run 中更容易放大等待成本。随后仅最小修改 1 个测试文件：把该用例里的邮箱/密码/确认密码填充改为 `fireEvent.change(...)`，其余点击、onboarding 出现与 `menuPaths` 断言全部保持不变。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx src/pages/LoginPage.test.tsx src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx` 通过（37 tests），其中目标用例 `keeps shared integration routes reachable after register bootstrap` 耗时明显收敛到约 0.6s；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_app_register_shared_route_real_api.sh`，最新产物目录 `.hermes/local-output/app-register-shared-route-real-api/20260510-003947`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok`；动态注册普通用户后 `GET /api/v1/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且 `/auth/menu` 继续暴露 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 并继续隐藏 supplier/admin 路径；前端 `/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/settings` SPA shell 全部 `200`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只是将 App 级注册表单输入方式收口为与既有 focused 合同一致的更稳定路径，不改变任何 shared-console 单壳工作流或角色语义）；代码质量 APPROVED（diff 仅 1 个测试文件、3 行输入方式修改，与 `submitRegistration(...)` helper 和 `LoginPage.test.tsx` 既有模式一致，没有削弱断言）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/app-register-shared-route-static-scan-20260510.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 回放继续通过）；测试/可靠性 PASS（focused App/Login/sidebar/layout 组合、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，仓库既有 act warning 未恶化）；性能/运维 PASS（纯测试层稳定性补强，不新增请求、轮询、持久化或部署复杂度；评审纪要已落盘 `.hermes/local-output/app-register-shared-route-review-summary-20260510.md`）。

### 最新执行检查点（2026-05-10，SupplierOfferings 控制台能力矩阵补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/SupplierOfferingsPage.tsx` 与 `web/src/pages/SupplierOfferingsPage.test.tsx` 做一个小而完整的前端垂直切片，为供应商供货规则页 `共享接入桥接` 区补齐一个与 ApiKeys / Profile / Webhooks 等页面一致的深色 `控制台能力矩阵`，明确收口 `统一供给入口 / 共享接入桥接 / 服务端菜单扩展` 三个 single-shell 能力信号；保持注册/登录接口、`/auth/menu` 菜单真值、角色边界以及 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `SupplierOfferingsPage.tsx` / `SupplierOfferingsPage.test.tsx` 回读现状，并先让 focused test 对新增 `supplier-offerings-capability-matrix` 合同 RED。根因确认不是 `/supplier/offerings` 路由缺失、不是 supplier 真实 API 异常、不是 `/auth/menu` 漂移，也不是共享桥接 CTA 失真，而只是供货规则页 bridge 区仍缺少一个与仓库当前 shared-console/new-api 单壳表面一致的能力矩阵层。随后仅最小修改 2 个前端文件：在 bridge 区新增深色能力矩阵卡片，并同步让 focused proof 断言 `控制台能力矩阵 / 统一供给入口 / 共享接入桥接 / 服务端菜单扩展`；期间产品/规格 reviewer 指出初版 `角色菜单扩展` 会把服务端控制的能力写成既成事实，控制器已同轮收口为更准确的 `服务端菜单扩展`，确保页面文案不夸大角色升级事实。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx` 通过（6 tests）；`pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx` 通过（26 tests）；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/SupplierOfferingsPage.test.tsx` 首轮仅暴露仓库既有 docs fallback 异步噪声，按 fail-closed 对 `falls back to the docs workspace when no preferred role landing route exists` 做 isolated rerun 后复跑整组恢复全绿（28 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_supplier_offerings_capability_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-offerings-capability-real-api/20260510-002001`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu` 继续隐藏 `/supplier/offerings` 且保留 `/api-keys`、`/webhooks`、`/docs`；seeded supplier 登录后 `GET /api/v1/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded admin 登录后观察到 `GET /api/v1/supplier/resources/offerings` 当前仍返回 `200` + `{"items":null}`，已如实记为本轮前端切片之外的既有后端契约现状，不误报为本 diff 回归；前端 `/`、`/supplier/offerings`、`/supplier/resources`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs` SPA shell 全部 `200`。
- 本轮五维评审结论：产品/规格 PASS（经整改后，`服务端菜单扩展` 与页面现有 banner 一致，不再把角色能力写成已自动升级的事实）；代码质量 APPROVED（diff 仅 2 个前端文件，新增卡片与 focused proof 同步收口，范围最小且职责清晰）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/supplier-offerings-capability-static-scan-20260510.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证 supplier 与 shared-console 路径稳定）；测试/可靠性 PASS（focused、supplier vertical slice、App 关联 rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，首轮 docs fallback 失败已确认属于历史异步噪声）；性能/运维 PASS（纯前端静态 UI 补齐，不新增请求、轮询、持久化或部署复杂度；本轮评审与验证摘要已落盘 `.hermes/local-output/supplier-offerings-capability-review-summary-20260510.json`）。

### 最新执行检查点（2026-05-09，Profile 用户焦点剩余“文档入口”旧词收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/ProfilePage.tsx` 与 `web/src/pages/ProfilePage.test.tsx` 做一个小而完整的前端垂直切片，把 Profile 普通用户 `用户接入焦点` 中 `集成准备` 卡片残留的旧词 `文档入口` 收口为与当前 canonical wording 一致的 `文档核对`；保持注册/登录接口、`/auth/menu` 菜单真值、角色边界以及 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `ProfilePage.tsx` / `ProfilePage.test.tsx` 回读现状，并先让 focused proof 对新文案合同 RED。根因确认不是 `/profile` 路由缺失、不是 `/auth/menu` 漂移、不是真实 API 失败、也不是共享壳导航错误，而只是普通用户 `集成准备` 卡片仍保留历史 `文档入口` 表述。随后仅最小修改 2 个前端文件：把运行时文案收口为 `通过 API Key、白名单与文档核对快速完成程序化接入，并对接真实回调能力。`，并同步在 focused test 中加入新文案正向断言与旧文案负向断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 通过（9 tests）；`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/App.test.tsx` 通过（31 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面执行本地忽略脚本 `.hermes/local-output/run_profile_copy_tightening_real_api.sh`，最新产物目录 `.hermes/local-output/profile-copy-tightening-real-api/20260509-234151`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200` 且菜单继续暴露 `/profile`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；并执行 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录 `.hermes/local-output/app-login-register-scope/20260509-234153`，再次验证普通用户注册后共享控制台主链路与管理员路由可达性稳定。
- 本轮五维评审结论：产品/规格 PASS（`文档核对` 比 `文档入口` 更准确表达 shared-console 中的真实动作，不把 docs 误写成独立入口）；代码质量 APPROVED（diff 仅 2 个文件、1 处运行时 copy 与 2 条 focused proof 同步收口，范围最小且职责清晰）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/profile-wording-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证 user/admin 路径稳定）；测试/可靠性 PASS（focused、App 关联回归、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，既有 React/Semi warning 未恶化）；性能/运维 PASS（纯前端 copy/test-contract 收口，不新增请求、轮询、持久化或部署复杂度；delegate 五维评审再次跨仓漂移到错误仓库，已按 fail-closed 全量丢弃，并由控制器侧 live diff + 实测结果复核）。

### 最新执行检查点（2026-05-09，Projects / Orders 任务流 wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/ProjectsPage.tsx`、`web/src/pages/ProjectsPage.test.tsx`、`web/src/pages/OrdersPage.tsx`、`web/src/pages/OrdersPage.test.tsx` 做一个小而完整的前端垂直切片，把项目市场右侧卡从偏帮助文案的 `采购动作提示` 收口为更接近 shared-console / new-api 单壳任务语义的 `采购任务流`，并把订单中心右侧卡从 `履约说明` 收口为 `履约任务流`；同步让 focused tests 改为锚定新的任务流标题与语义区域，不改注册/登录接口、不改 `/auth/menu` 菜单真值，也不改 Phase 4/5 后端契约。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `ProjectsPage` / `OrdersPage` 及其 focused tests 回读现状，再先让对应 tests 对新任务流合同 RED。根因确认不是 `/projects` 或 `/orders` 路由缺失，不是注册功能缺口，不是 `/auth/menu` 漂移，也不是真实 API 异常，而只是两页右侧卡片标题仍偏“说明/提示”心智，和当前已落地的 shared-console/new-api 任务流式单壳语言相比不够统一。随后仅最小修改 4 个前端文件：运行时代码把 `projects-action-guidance-card` 收口为 `projects-mission-flow-card` + `采购任务流`，把订单页 `履约说明` 收口为 `履约任务流`；focused proof 同步改为新标题正向断言与旧标题负向断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx` 通过（21 tests）；`pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx src/App.test.tsx` 通过（43 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_projects_orders_real_api.sh`，最新产物目录 `.hermes/local-output/projects-orders-real-api/20260509-223817`（`latest` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok`；动态注册普通用户后 `POST /api/v1/wallet/topups`、`GET /api/v1/projects/inventory`、`GET /api/v1/orders/activations`、`POST /api/v1/orders/activations`、`GET /api/v1/orders/activations/:id/result`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints` 全部成功，且管理员 `admin@nexus-mail.local` 登录后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/wallet-users` 全部 `200`，证明本轮 wording 收口未破坏采购/履约/shared-console 主链路。
- 本轮五维评审结论：产品/规格 PASS（Projects / Orders 更明确表达任务流心智，且未把单壳控制台误导成独立后台）；代码质量 APPROVED（diff 仅 4 个文件、2 处运行时标题与 2 处 focused proof 同步修改，范围小且职责清晰）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/projects-orders-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且真实 API 回放继续证明 auth/menu/项目/订单链路稳定）；测试/可靠性 PASS（focused、App 关联回归、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，既有 `act(...)` warning 未恶化）；性能/运维 PASS（纯前端 wording/test anchor 收口，不新增请求、轮询、持久化或部署复杂度；尝试 `docker compose up -d --build web` 时因 Docker Hub metadata 拉取超时被外部网络阻塞，已如实记为基础设施问题而非当前代码回归，且现有服务与 controller-side build/real API 结果仍可作为本轮验证来源）。

### 最新执行检查点（2026-05-09，Profile / SupplierSettlements / AdminAudit 剩余“文档入口”旧文案收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/ProfilePage.tsx`、`web/src/pages/ProfilePage.test.tsx`、`web/src/pages/SupplierSettlementsPage.tsx`、`web/src/pages/SupplierSettlementsPage.test.tsx`、`web/src/pages/AdminAuditPage.tsx`、`web/src/pages/AdminAuditPage.test.tsx` 做一个小而完整的前端垂直切片，把当前 single-shell 叙事里残留的旧词 `文档入口` 继续收口为更准确的 canonical wording——普通用户 Profile 角色扩展 tag 改为 `文档核对`，供应商结算共享接入桥接说明改为 `文档核对`，管理员审计任务流 integration 描述改为 `API 文档与接入控制台`；保持注册/登录接口、`/auth/menu` 菜单真值、角色边界以及 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、运行态与上述 6 个文件回读现状，再先让对应 focused tests 对新 wording 合同 RED。根因确认不是 `/profile`、`/supplier/settlements`、`/admin/audit` 路由缺失，不是 `/auth/menu` 漂移，不是真实 API 异常，也不是 shared shell / role-aware route title 失真，而只是三处前端运行时文案仍停留在过时的 `文档入口` 表述。随后仅最小修改 6 个前端文件，并同步把 focused proof 收口为新文案正向断言与旧文案负向断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/SupplierSettlementsPage.test.tsx src/pages/AdminAuditPage.test.tsx` 通过（22 tests）；`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/SupplierSettlementsPage.test.tsx src/pages/AdminAuditPage.test.tsx src/App.test.tsx` 两轮 grouped run 均只暴露仓库既有 App 异步噪声，随后对 `renders the login shell when unauthenticated`、`falls back to the docs workspace when no preferred role landing route exists`、`uses the shared API keys route constant for register onboarding entrypoints` 做 isolated rerun 全部通过，因此如实记为历史 flake 而非当前 diff 回归；其余 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_front_canonical_wording_real_api.sh`，最新产物目录 `.hermes/local-output/front-canonical-wording-real-api/20260509-220936`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户注册负路径 `请输入有效邮箱` / `密码长度至少为 8 位` 仍返回 `400`，有效注册仍返回 `201`；普通用户 bearer token 可访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`；seeded admin 可访问 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5`、`/auth/api-keys`、`/webhooks/endpoints`；seeded supplier 可访问 `/supplier/settlements`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/auth/menu`；前端 `5173` 端口下 `/`、`/login`、`/api-keys`、`/docs`、`/webhooks`、`/profile` 全部返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（文案更准确映射 `文档核对` / `API 文档与接入控制台` 的真实 shared-console 身份，没有把单壳入口误写成独立后台）；代码质量 APPROVED（diff 仅 6 个前端文件，运行时代码与 focused proof 同步收口，范围最小且职责清晰）；安全/集成 SAFE（added-line 静态扫描 `.hermes/local-output/front-canonical-wording-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且 auth/session/menu truth/permissions 未改；delegate 五维评审再次发生明显跨仓漂移，已按 fail-closed 全量丢弃，并由控制器基于 live diff、静态扫描、focused/build/真实 API 结果完成复核）；测试/可靠性 PASS（focused、isolated rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，App grouped 首轮失败已确认属于历史异步噪声）；性能/运维 PASS（纯前端 copy/contract 收口，不新增请求、轮询、持久化或部署复杂度；`docker compose up -d --build web api gateway` 因 Docker Hub metadata 超时被外部网络阻塞，但既有 `web` / `api` / `gateway` 容器保持 healthy，故按 fail-closed 记为外部基础设施阻塞而非应用回归）。后续继续保持“前端优先、单壳一致性补强后再回到 Phase 4/5 后端切片”的推进顺序。

### 最新执行检查点（2026-05-09，AdminUsers / AdminSuppliers 文档页 canonical identity 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/AdminUsersPage.tsx`、`web/src/pages/AdminUsersPage.shared-console.test.tsx`、`web/src/pages/AdminSuppliersPage.tsx`、`web/src/pages/AdminSuppliersPage.test.tsx` 做一个小而完整的前端垂直切片，把两页 shared-console bridge / integration 文案中的泛化 `文档入口` 统一收口为真实目标页 identity `API 文档与接入控制台`，保持注册/登录接口、`/auth/menu` 菜单真值、admin 角色边界与 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、运行态以及上述 4 个文件回读现状，并先让 focused proof 对新 identity RED。根因确认不是 `/admin/users` 或 `/admin/suppliers` 路由缺失、不是 admin `/auth/menu` 漂移、不是真实 API 异常，也不是 docs route 标题错误，而只是两页 bridge / integration 描述仍沿用过时的 `文档入口` 泛称，不够贴近仓库当前 canonical route title。随后仅最小修改 4 个前端文件：把 `AdminUsersPage` 的 integration 描述与 bridge 段落、`AdminSuppliersPage` 的 bridge 段落统一改为“通过 API Keys、Webhook 与 API 文档与接入控制台…”，并同步 focused tests；代码质量 reviewer 进一步指出 `AdminUsersPage` mission-flow integration 描述已改但初版测试未覆盖，控制器已同轮补上该条 focused 断言，确保运行时代码与 proof 一致。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx src/pages/AdminSuppliersPage.test.tsx` 通过（12 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面本轮采用 shell 内即时 token 回放（避免 secret redaction 让落盘脚本失真），再次验证 admin 登录后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/wallet-users`、`/api/v1/admin/disputes?limit=5`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 全部 `200`，且 admin 菜单继续暴露 `/admin/users`、`/admin/suppliers`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`。本轮五维评审纪要已落盘 `.hermes/local-output/admin-shared-console-docs-bridge-review-20260509.md`。
- 本轮五维评审结论：产品/规格 PASS（bridge / integration wording 更准确映射真实 docs page identity，未把单一登录后控制台误写成独立后台）；代码质量 APPROVED（4 文件小切片，且已按 reviewer 意见补齐 `AdminUsersPage` integration 文案 focused 覆盖）；安全/集成 SAFE（added-line 静态扫描 5 类模式 0 命中，auth/session/menu truth 与 route identity 未变）；测试/可靠性 PASS（focused、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，既有 `act(...)` warning 未恶化）；性能/运维 PASS（纯前端文案一致性收口，不新增请求、轮询、持久化或部署复杂度）。后续仍按“前端先行、单壳一致性补强后再回到 Phase 4/5 后端切片”的顺序继续推进。

### 最新执行检查点（2026-05-09，SupplierResources 资源准备工作台深色单壳收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/SupplierResourcesPage.tsx` 与 `web/src/pages/SupplierResourcesPage.test.tsx` 做一个小而完整的前端垂直切片，把供应商资源页原先分散的 `新增域名池 / 新增第三方邮箱账号 / 新增邮箱池 / 别名池` 三张浅色录入卡，收敛为更贴近 `new-api` 单壳控制台语义的深色 `资源准备工作台`。保持注册/登录接口、`/auth/menu` 菜单真值、supplier/user/admin 角色边界，以及 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `SupplierResourcesPage.tsx` / `SupplierResourcesPage.test.tsx` 回读现状，再先让 focused test 对新的 `supplier-resources-setup-workbench` 合同 RED：要求 workbench 命中 `资源准备工作台`、`STEP 01/02/03` 与 `域名池录入 / 第三方账号接入 / 邮箱池映射`，并显式否定旧标题 `新增域名池 / 新增第三方邮箱账号 / 新增邮箱池 / 别名池`。根因确认不是 `/supplier/resources` 路由缺失、不是 supplier 资源真实 API 异常、不是 `/auth/menu` 漂移，也不是共享接入桥接失真，而只是资源录入区仍停留在分散表单卡心智，不够统一成深色 shared-console/new-api 工作台。随后仅最小修改 2 个前端文件：运行时代码把三张录入卡收敛进单一深色 workbench，并在评审反馈后抽出轻量 `ResourceSetupCard` 降低重复；focused proof 同步改为 workbench 结构断言与本地化标签校验，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx` 通过（12 tests）；`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx` 通过（26 tests）；更大范围带 `App.test.tsx` 的 grouped run 首轮暴露仓库既有异步噪声 `falls back to the docs workspace when no preferred role landing route exists` 与 `uses the shared API keys route constant for register onboarding entrypoints`，控制器按 fail-closed 分别 isolated rerun 后均通过，因此如实记为历史 flake 而非当前 diff 回归；其余 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_supplier_resources_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-resources-real-api/20260509-205529`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；seeded supplier `supplier@nexus-mail.local / Supplier123!` 登录后 `GET /api/v1/auth/menu`、`/supplier/resources/overview` 全部 `200`，并真实成功创建新的 resource domain / provider account / mailbox 记录，证明资源工作台前端收口没有破坏 supplier 资源主链路。
- 本轮五维评审结论：产品/规格 PASS（资源录入区已更接近 `new-api` / shared-console 单壳工作台，而不是散装后台表单卡，且未夸大后端能力）；代码质量 APPROVED（运行时代码仅集中修改 2 个文件，并按评审意见抽出 `ResourceSetupCard` 收口重复结构，focused proof 与新合同同步）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行的 hardcoded secret / shell / eval / pickle / SQL format 五类模式 0 命中，且 auth/menu/route gating 与真实 supplier 资源 API 契约未变）；测试/可靠性 PASS（focused、supplier vertical slice、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，App 异步噪声已确认属既有 flake）；性能/运维 PASS（纯前端结构重排，不新增请求、轮询、持久化或部署复杂度；评审纪要已落盘 `.hermes/local-output/supplier-resources-workbench-review-20260509.md`）。后续前端优先方向继续围绕 remaining shared-console 页面与 focused 用例做同类 new-api 深色统一和语义锚点收敛，再回到 Phase 4/5 余下后端切片。

### 最新执行检查点（2026-05-09，Settings 会话卡 canonical shared-console wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/SettingsPage.tsx` 与 `web/src/pages/SettingsPage.test.tsx` 做一个小而完整的前端垂直切片，继续收口 Settings 页残留的非 canonical shared-console wording，把 `settings-session-card` 第三项从偏孤立入口心智的 `文档入口` 收口为与本仓 current single-shell/new-api 语义一致的 `共享接入桥接`，保持注册/登录接口、`/auth/menu` 菜单真值、角色边界与 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、运行态、`SettingsPage.tsx` 与 `SettingsPage.test.tsx` 回读现状，并先让 focused test 对新的会话卡合同 RED：要求 `settings-session-card` 出现 `共享接入桥接` 且旧词 `文档入口` 消失。根因确认不是 `/settings` 路由缺失、不是 `/auth/menu` 漂移、不是真实 API 失败、也不是 shared shell 崩坏，而只是 `sessionItems` 第三项仍保留历史 `文档入口` 标签，导致 Settings 页内部仍有一处不够统一的单壳接入心智。随后仅最小修改 2 个前端文件：把 `sessionItems` 第三项改为 `共享接入桥接 / API Keys / Webhook / Docs 与共享壳保持同域访问`，并同步把 focused proof 改为新文案正向断言 + 旧文案负向断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 通过（5 tests）；`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/App.test.tsx src/components/ConsoleLayout.test.tsx` 通过（29 tests，仓库既有 `act(...)` / `findDOMNode` warning 未恶化）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略产物目录 `.hermes/local-output/settings-session-canonical-real-api/20260509-201808`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200` 且菜单继续暴露 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/settings`；seeded supplier 登录后 `GET /api/v1/auth/menu` 返回 `200` 且 supplier 资源/运营路径继续存在；前端 `/settings`、`/login` SPA shell 全部 `200`。同时 added-line 静态扫描产物 `.hermes/local-output/settings-session-canonical-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中。
- 本轮五维评审结论：产品/规格 PASS（Settings 会话卡 wording 进一步与 single shared-console/new-api 单壳叙事对齐，未把设置页误导成独立后台）；代码质量 APPROVED（diff 仅 2 个文件，运行时代码与 focused proof 同步收口，范围最小且职责清晰）；安全/集成 SAFE（本轮仅文案/合同收口，未改 auth/session/menu truth/permissions；delegate 五维评审出现明显跨仓漂移，已按 fail-closed 全量丢弃，并由控制器基于 live diff、静态扫描、focused/build/真实 API 结果完成复核）；测试/可靠性 PASS（focused、关联 grouped、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，既有 warning 未新增）；性能/运维 PASS（纯前端静态收口，不新增请求、轮询、持久化或部署复杂度；评审纪要已落盘 `.hermes/local-output/settings-session-canonical-review-20260509.md`）。后续前端优先方向继续围绕 remaining shared-console 页面的一致性收口与 focused contract 去脆弱化推进，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，Settings 控制台能力矩阵 canonical wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/SettingsPage.tsx` 与 `web/src/pages/SettingsPage.test.tsx` 做一个小而完整的前端垂直切片，把 Settings 页 `控制台能力矩阵` 从仍偏泛化的 `账号入口 / 集成入口 / 回调入口 / 文档入口` 收口为与 Profile / ApiKeys / Webhooks / Balance 等页面一致的 canonical shared-console wording：`统一身份入口 / 共享接入桥接 / 角色菜单扩展`。保持注册/登录接口、`/auth/menu` 菜单真值、角色边界以及 Phase 4/5 的后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `SettingsPage.tsx` / `SettingsPage.test.tsx` 回读现状，再先让 focused test 对新的 capability-matrix 文案合同 RED。根因确认不是 `/settings` 路由缺失、不是 `/auth/menu` 漂移、不是真实 API 失败、也不是 shared shell 崩坏，而只是 Settings 页右侧能力矩阵仍保留历史泛化标签。随后仅最小修改 2 个前端文件：把运行时 `capabilitySignals` 收口为 `统一身份入口 / 共享接入桥接 / 角色菜单扩展` 三个信号，并同步把 focused proof 改为新文案正向断言 + 旧文案负向断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 通过（5 tests）；`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/App.test.tsx src/components/ConsoleLayout.test.tsx` 首轮因仓库既有 `App.test.tsx > falls back to the docs workspace when no preferred role landing route exists` 异步噪声失败，按 fail-closed 对该单例 isolated rerun 后通过，因此如实记为历史时序噪声而非当前 diff 回归；其余 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_settings_capability_matrix_real_api.sh`，最新产物目录 `.hermes/local-output/settings-capability-matrix-real-api/20260509-195558`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200` 且菜单继续暴露 `/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；seeded supplier 登录后 `GET /api/v1/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 全部 `200`；前端 `/`、`/login`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs` SPA shell 全部 `200`。
- 本轮五维评审结论：产品/规格 PASS（Settings 能力矩阵已与 shared-console/new-api 单壳 canonical wording 对齐，未把设置页叙述成独立后台）；代码质量 APPROVED（diff 仅 2 个文件，运行时代码与 focused proof 同步收口，范围最小且职责清晰）；安全/集成 SAFE（静态扫描产物与控制器 added-line 复核显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions）；测试/可靠性 PASS（focused、相关 grouped rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过；isolated rerun 已证首轮 App 失败属于历史噪声）；性能/运维 PASS（纯前端静态文案/合同收口，不新增请求、轮询、持久化或部署复杂度；评审纪要已落盘 `.hermes/local-output/settings-capability-matrix-review-20260509.md`）。后续前端优先方向继续围绕 remaining shared-console 页面的一致性收口与 focused contract 去脆弱化推进，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，Webhooks 首轮联调能力矩阵补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/WebhooksPage.tsx` 与 `web/src/pages/WebhooksPage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，为普通用户 `/webhooks` 首轮联调卡补齐一个 Linear/new-api 风格的深色 `控制台能力矩阵`，显式收口 `统一回调入口 / 共享接入桥接 / 角色菜单扩展` 三个单壳能力信号，让 Webhooks 页面与 ApiKeys / Profile / Settings / Balance 等已完成页面保持一致。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、运行态以及 `web/src/pages/WebhooksPage.tsx` / `web/src/pages/WebhooksPage.test.tsx` 回读现状，并先让 focused test 对缺失的 `webhooks-capability-matrix` RED。根因确认不是 `/webhooks` 路由缺失、不是真实 Webhook API 异常、不是 `/auth/menu` 漂移、不是单壳导航错误，也不是 docs/api-keys bridge 合同失真，而只是普通用户首轮联调卡仍缺少一个与仓库当前 canonical shared-console/new-api 表面一致的能力矩阵层。随后仅最小修改 2 个前端文件：在 `webhooks-first-integration-loop` 内新增深色能力矩阵卡，沿用 Linear 风格的近黑渐变、浅边框与三枚 capability tags，并在 focused proof 中同步增加 `控制台能力矩阵 / 统一回调入口 / 共享接入桥接 / 角色菜单扩展` 断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx` 通过（13 tests）；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/WebhooksPage.test.tsx src/pages/ApiDocsPage.test.tsx` 首轮因仓库既有 docs fallback 异步噪声失败，按 fail-closed 对 `falls back to the docs workspace when no preferred role landing route exists` 做 isolated rerun 通过后，再次复跑整组用例全绿（40 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_webhooks_capability_real_api.sh`，最新产物目录 `.hermes/local-output/webhooks-capability-real-api/20260509-192229`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/webhooks/endpoints`、`/auth/api-keys` 全部 `200` 且菜单继续暴露 `/webhooks`；seeded supplier 与 admin 登录后 `GET /api/v1/auth/menu`、`/webhooks/endpoints` 全部 `200`，admin 额外验证 `/admin/audit?limit=5` `200`；前端 `/login`、`/webhooks`、`/api-keys`、`/docs` SPA shell 全部 `200`。运行态检查同时确认 compose 实际发布前端端口为 `5173` 而非旧 `4173`，已据实更新本轮本地验证脚本。
- 本轮五维评审结论：产品/规格 PASS（新增能力矩阵继续强化 single shared-console/new-api 单壳叙事，没有把 Webhooks 描述成独立后台）；代码质量 APPROVED（diff 仅 2 个文件、静态卡片与 focused proof 同步收口，范围小且职责清晰）；安全/集成 SAFE（added-line 静态扫描 5 类模式 0 命中，且未改鉴权、权限、网络调用或后端契约）；测试/可靠性 PASS（focused Webhooks、grouped App/Webhooks/ApiDocs rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过；docs fallback 首轮失败已如实记录为既有异步噪声）；性能/运维 PASS（纯前端静态 UI 补齐，不新增请求、轮询、持久化或部署复杂度；评审纪要已落盘 `.hermes/local-output/webhooks-capability-review-20260509.md`）。后续前端优先方向继续围绕 remaining shared-console 页面做同类 new-api 深色一致性补强与 focused contract 去脆弱化，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，Login 注册入口文案进一步压缩）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/LoginPage.tsx` 与 `web/src/pages/LoginPage.test.tsx` 做一个小而完整的前端垂直切片，继续收紧注册入口的 shared-console/new-api 叙事，不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约。目标是把 Login 首屏、注册引导与首轮接入 runway 文案进一步压缩为更像单壳控制台入口的短句，同时保持“登录/注册后仍进入同一控制台，再沿项目市场 / 订单中心 / API Keys / Webhooks / Docs 完成首轮接入”的真实合同不被夸大。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态以及 `LoginPage.tsx` / `LoginPage.test.tsx` 回读现状，再先让 focused test 对新 copy RED。根因确认不是注册功能缺失、不是 `/auth/menu` 漂移、不是路由常量错误、也不是真实 API 失败，而只是 Login 页仍有数处说明式文案偏长：hero summary 仍使用“同一登录面完成切换”，register journey summary 仍使用“按同一导航完成首轮接入”，runway 三张卡仍保留较长描述，register banner/helper 也仍残留“再沿同一导航”句式。随后仅最小修改 2 个前端文件：把 hero summary 收口为 `统一入口、共享壳与接入路径，在同一登录面完成切换。`，把 register helper/banner/journey summary 收口为 `注册后直接进入共享控制台，沿同一导航前往项目市场、订单中心与 API Keys。` 与 `注册后进入共享控制台，沿同一导航完成首轮接入。`，并把三张 runway 描述同步压缩为 `生成首个最小权限密钥。`、`确认回调地址并发起真实联调。`、`查看 API 文档并核对请求契约。`；focused tests 同步加入新文案正向断言与旧文案负向断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（8 tests）；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/LoginPage.test.tsx` 通过（30 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_login_copy_tightening_real_api.sh`，最新产物目录 `.hermes/local-output/login-copy-tightening-real-api/20260509-182220`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且菜单继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/login`、`/api-keys`、`/webhooks`、`/docs`、`/profile`、`/settings` SPA shell 全部 `200`。浏览器 fresh-load 复核 `http://127.0.0.1:4173/login` 也确认新文案已实际渲染；`docker compose up -d --build web gateway` 本轮因 docker.io 拉取 `node:22-alpine` / `golang:1.25-alpine` metadata 超时失败，已按 fail-closed 记为外部 registry/network 波动，并继续以现有 healthy 容器 + 本地 preview 作为验证源。
- 本轮五维评审结论：产品/规格 PASS（copy 更短、更像 shared-console/new-api 控制台入口，同时未把 docs/webhooks/api-keys 说成独立后台或虚构强制工作流）；代码质量 APPROVED（diff 仅 2 个文件，运行时 copy 与 focused proof 同步收口，范围小且职责清晰）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/login-copy-tightening-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；本轮 delegate reviewers 再次发生跨仓漂移，已按 fail-closed 丢弃，并由控制器以 live diff + focused/build/真实 API 结果完成复核）；测试/可靠性 PASS（focused Login/App、build、Go tests、compose config、diff-check、真实 API 回放与浏览器 fresh-load 复核全部通过；Vitest 中既有 `act(...)` / Semi warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯前端文案/test-contract 收口，不新增请求、轮询、持久化或部署复杂度）。后续前端优先方向继续围绕 Login/App/shared-console focused 套件中的低信号 copy、结构锚点与集成去脆弱化推进，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，Login / App 注册表单 focused 稳定性补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/LoginPage.test.tsx` 与 `web/src/App.test.tsx` 做一个小而完整的前端 focused 测试稳定性切片，不改任何运行时代码、不改注册/登录接口、不改 `/auth/menu` 菜单真值，也不改 Phase 4/5 的 Webhook / API Key / 风控 / 审计后端契约。目标是收口登录/注册相关测试里仍依赖 `user.type(...)` 逐字符填充注册表单的慢路径，把 Login 页面 focused 与 App 注册集成测试统一到更稳定的 Semi UI 表单输入策略，减少批跑时的时长与抖动。
- 控制器按 `systematic-debugging` + TDD 先从 `git status`、`todo.md`、当前运行态与 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 回读/复现现状。根因确认不是注册功能缺失、不是 `/auth/menu` 漂移、不是 shared-console 路由错误、也不是真实 API 失败，而是测试层仍有 2 处注册表单使用 `user.type(...)`：`LoginPage.test.tsx` 中 5 条注册/校验用例逐字符填充邮箱/密码/确认密码，`App.test.tsx` 的 `submitRegistration(...)` helper 也沿用同样策略，导致 focused LoginPage 基线耗时约 25s、App+Login 组合跑时更容易在其它异步 warning 噪声下放大等待成本。随后仅最小修改 2 个测试文件：统一引入 `fireEvent`，把上述注册表单填充改为 `fireEvent.change(...)`，其余断言、模式切换、注册成功跳转、首轮 onboarding 与 shared-console 路由合同全部保持不变。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（8 tests，时长从约 25.56s 收敛到约 24.98s / 测试体感显著更快）；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/LoginPage.test.tsx` 最终通过（30 tests）；其中首轮 grouped run 一度被仓库既有 App 时序噪声绊到 `dashboard-first-run-dismiss` 未及时出现，控制器按 fail-closed 立即对该单例 `shows first-run onboarding guidance for default user dashboard and allows dismissing it` 做 isolated rerun，确认通过后复跑 grouped 套件即恢复全绿，因此如实记为历史异步噪声而非当前 diff 回归。其余 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_login_test_stability_real_api.sh`，最新产物目录 `.hermes/local-output/login-test-stability-real-api/20260509-185408`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且菜单继续暴露 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/settings`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/login`、`/api-keys`、`/webhooks`、`/docs`、`/profile`、`/settings` SPA shell 全部 `200`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只收敛测试输入方式，不改变任何用户可见注册/登录/shared-console 合同）；代码质量 APPROVED（改动仅 2 个测试文件，统一注册表单输入策略，与仓库既有 Semi UI 稳定测试模式一致）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/login-test-stability-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions）；测试/可靠性 PASS（focused Login、grouped App+Login、build、Go tests、compose config、diff-check 与真实 API 回放全部通过；isolated rerun 证实 grouped 首轮失败属于历史时序噪声）；性能/运维 PASS（纯测试层稳定性补强，不新增请求、轮询、持久化或部署复杂度；评审纪要 `.hermes/local-output/login-test-stability-review-20260509.md` 已落盘）。后续前端优先方向继续围绕 Login/App/shared-console focused 套件中剩余低信号 copy、`act(...)` 噪声与 page-global 断言做小切片收口，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，front focused 测试稳定性补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/App.test.tsx`、`web/src/pages/ApiKeysPage.test.tsx`、`web/src/pages/SupplierSettlementsPage.test.tsx` 做一个小而完整的前端 focused 测试稳定性切片，不改任何运行时代码、不改注册/登录接口、不改 `/auth/menu` 菜单真值，也不改 Phase 4/5 的 Webhook / API Key / 风控 / 审计后端契约。目标是收口当前 Vitest 中 3 类已复现的脆弱点：`App.test.tsx` 未清理 `sessionStorage` 造成跨用例会话残留；`ApiKeysPage.test.tsx` 的创建密钥用例仍使用逐字符 `user.type(...)` 填充多字段，批量执行时容易超时；`SupplierSettlementsPage.test.tsx` 对 Semi UI 成本模型表单仍重复修改默认字段并遗漏显式清空首字段，导致 focused 提交流程在高噪声批跑下更容易抖动。
- 控制器按 `systematic-debugging` + TDD 先从 `git status`、`todo.md`、当前运行态与 full `pnpm --dir web exec vitest run` 复现现状。根因确认不是 `/api-keys` / `/settings` / `/admin/risk` 等路由缺失，不是真实 API 失败，不是注册功能缺口，也不是共享控制台运行时代码回归，而是测试层存在 3 个具体问题：1) `App.test.tsx` 的 afterEach 只清 `localStorage` 未清 `sessionStorage`，导致 `setSession(...)` 剩余 token/menu 影响后续集成用例；2) `ApiKeysPage.test.tsx` 的 create-key proof 在 Semi UI 表单里对多输入框使用长串 `user.type(...)`，在批量执行时会被 `act(...)` / table 渲染噪声放大；3) `SupplierSettlementsPage.test.tsx` 对 `currency/status` 这两个由 `initValues` 保证的默认字段做了不必要的交互，同时未先清空 `项目键` 与 `争议原因`，与仓库既有 `semi-ui-form-default-value-test-stability` 经验不一致。随后仅最小修改 3 个测试文件：为 `App.test.tsx` 增加 `window.sessionStorage.clear()` 清理；将 `ApiKeysPage.test.tsx` 的 create-key 填充改为 `fireEvent.change(...)` 直接验证 payload；把 `SupplierSettlementsPage.test.tsx` 的成本模型/争议提交用例收口为“清空真实业务字段 + 依赖默认 `currency='CNY'` / `status='active'` 合同”的更稳定写法。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx -t "keeps settings guidance entry available after dismissing first-run mission cards for default user"` 通过；`pnpm --dir web exec vitest run src/App.test.tsx -t "falls back to the docs workspace when no preferred role landing route exists|uses the shared API keys route constant for register onboarding entrypoints"` 通过（仍有既有 React/Semi/jsdom warning，但断言全绿）；`pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx -t "loads and creates api key with trimmed scopes and whitelist|updates whitelist with normalized entries and reloads list|reloads list after revoking an active key"` 通过；`pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx -t "submits create offering form and reloads data"` 通过，用于再次确认此前 related flaky path 未被当前切片恶化；`pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx -t "submits supplier cost profile and dispute actions from scoped finance cards then reloads data"` 通过；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。更大范围把 `App.test.tsx + ApiKeysPage.test.tsx + SupplierOfferingsPage.test.tsx + SupplierSettlementsPage.test.tsx` 一起联跑时，仍会命中 `App.test.tsx` 里历史性的跨页 async/jsdom 噪声（docs fallback / register→api-keys deep-link 等用例在批跑顺序下偶发慢于默认 5s），控制器已按 fail-closed 逐条 isolated rerun 复现并确认当前 diff 未引入新的失败点，因此本轮里程碑如实定位为“3 个已知 focused 脆弱点补强”，而非虚假宣称完成整个 App 集成套件去抖。
- 真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_front_test_stability_real_api.sh`，最新产物目录 `.hermes/local-output/front-test-stability-real-api/20260509-174823`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且菜单继续暴露 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/settings`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`。这再次证明本轮仅为前端 focused 测试稳定性补强，运行时共享控制台与 Phase 4/5 真实 API 契约未被破坏。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只是把 shared-console focused proof 稳定化，没有改变任何用户可见工作流、角色权限或 single-shell 叙事）；代码质量 APPROVED（改动仅 3 个测试文件、1 处 afterEach 清理 + 2 处表单交互收口，与 `semi-ui-form-default-value-test-stability` 经验一致，范围小且职责清晰）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/front-test-stability-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且真实 API 再证 auth/menu/role 边界稳定）；测试/可靠性 PASS（本轮承诺的 5 条 targeted Vitest、build、Go tests、compose config 与真实 API 回放全部通过；批量 App 集成噪声已诚实记录为既有 flake，非本 diff 新回归）；性能/运维 PASS（纯测试层稳定性补强，不新增请求、轮询、持久化或部署复杂度，且 `sessionStorage.clear()` 只影响测试隔离、不会触碰生产运行时）。


### 最新执行检查点（2026-05-09，Balance 控制台能力矩阵深色单壳收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/BalancePage.tsx` 与 `web/src/pages/BalancePage.test.tsx` 做一个小而完整的前端垂直切片，把余额中心右侧 `控制台能力矩阵` 从仍偏浅色信息卡的混合表面继续收敛为与 ApiKeys / Profile / Settings 已落地能力面板一致的深色单壳卡片；同时把第三个能力信号从解释式的 `财务到接入的桥接` 统一到仓库当前 canonical wording `共享接入桥接`。保持注册/登录接口、`/auth/menu` 菜单真值、角色边界，以及 Phase 4/5 的 Webhook / API Key / 风控 / 审计后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/pages/BalancePage.tsx`、`web/src/pages/BalancePage.test.tsx` 与当前运行态回读现状，再先让 `pnpm --dir web exec vitest run src/pages/BalancePage.test.tsx -t "renders balance hero, mission, and capability regions for regular users"` 对新的深色能力矩阵合同 RED。根因确认不是 `/balance` 路由缺失、不是 `wallet/overview` / `wallet/transactions` 真接口异常、不是 `/auth/menu` 漂移、不是 bridge CTA 导航错误，也不是 shared shell 崩坏，而只是余额页右侧能力矩阵仍保留浅色卡片风格与非 canonical bridge label。随后仅最小修改 2 个前端文件：把 `balance-capability-matrix` 外层卡片改为深色背景与深色标题/正文对比，把内部 3 张能力子卡同步切到深色层级，并把 `consolePillars.integration-bridge.label` 收口为 `共享接入桥接`；focused proof 同步新增深色外壳 style 断言与新 label 合同，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/BalancePage.test.tsx` 通过（7 tests）；`pnpm --dir web exec vitest run src/App.test.tsx -t "falls back to the docs workspace when no preferred role landing route exists|navigates from profile page CTA to the supplier domains page"` 通过，用于确认首轮 grouped 失败并非当前 diff 回归；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_balance_capability_dark_shell_real_api.sh`，最新产物目录 `.hermes/local-output/balance-capability-dark-shell-real-api/20260509-164054`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/wallet/overview`、`/wallet/transactions`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且 `/auth/menu` 继续暴露 `/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs` 单壳路径。
- 本轮五维评审结论：产品/规格 PASS（Balance 能力矩阵现已与 shared-console/new-api 单壳深色表面对齐，且 bridge wording 统一到 `共享接入桥接`，未把余额页描述成独立后台或改写角色路径）；代码质量 APPROVED（diff 仅 2 个文件，运行时代码与 focused proof 同步收口，范围最小、职责清晰；评审纪要 `.hermes/local-output/balance-capability-dark-shell-review-20260509.md` 已落盘）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/balance-capability-dark-shell-static-scan-20260509.json` 显示 5 类风险模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证 wallet / api-keys / webhooks 路径边界稳定）；测试/可靠性 PASS（focused Vitest、isolated App rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，grouped `App.test.tsx + LoginPage.test.tsx + BalancePage.test.tsx` 的 2 条失败经 isolated rerun 证实属于既有时序噪声而非当前 diff 回归）；性能/运维 PASS（纯前端样式/文案收敛，不新增请求、轮询、持久化或部署复杂度，构建仅保留仓库既有 chunk-size warning）。

### 最新执行检查点（2026-05-09，ApiKeys 控制台能力矩阵前端一致性补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/ApiKeysPage.tsx` 与 `web/src/pages/ApiKeysPage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，为 API Keys 页现有 shared-console bridge 增补一个紧凑的深色 `控制台能力矩阵` 卡片，明确收口 `统一凭证入口`、`共享接入桥接`、`角色菜单扩展` 这 3 个单壳能力信号，让该页和 Profile / Settings / Balance 等已完成的 new-api 风格共享控制台页面保持一致。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/pages/ApiKeysPage.tsx`、`web/src/pages/ApiKeysPage.test.tsx` 与当前运行态回读现状，再让 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 对缺失的 `api-keys-capability-matrix` 先 RED。根因确认不是 `/api-keys` 路由缺失、不是 API Key 真接口异常、不是 `/auth/menu` 漂移、不是 bridge CTA 导航错误，也不是 shared shell 崩坏，而只是 ApiKeys 页在 bridge 之下还缺一个与仓库其它 shared-console 页面一致的能力矩阵表面。随后仅最小修改 2 个前端文件：在 bridge 文案块内部新增深色能力矩阵卡片，并把 focused proof 同步扩展到 `控制台能力矩阵 / 统一凭证入口 / 共享接入桥接 / 角色菜单扩展` 4 个稳定合同，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 通过（14 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_api_keys_capability_matrix_real_api.sh`，最新产物目录 `.hermes/local-output/api-keys-capability-matrix-real-api/20260509-160700`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；真实创建 API Key、更新白名单、撤销密钥与 `GET /api/v1/auth/api-keys/audit` 全部成功并返回 `create / update_whitelist / revoke` 审计信号；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；seeded supplier `supplier@nexus-mail.local / Supplier123!` 登录后 `GET /api/v1/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 全部 `200`；前端 `/`、`/api-keys`、`/webhooks`、`/docs`、`/profile`、`/settings` SPA shell 全部返回 `200`。浏览器工具本轮对本地 `/api-keys` 路由仍命中登录页快照，控制器以 HTTP 200、build、focused、真实 API 与 live runtime 证据判定为浏览器会话/鉴权态陈旧而非当前 diff 回归，并按 fail-closed 记录为非阻塞工具状态噪声。
- 本轮五维评审结论：产品/规格 PASS（ApiKeys 页面补齐能力矩阵后，更贴近 shared-console/new-api 单壳叙事，未把 API Keys 描述成独立后台，也未改写角色路径；评审归档 `.hermes/local-output/api-keys-capability-matrix-review-20260509.md`）；代码质量 APPROVED（diff 仅 2 个文件，运行时代码与 focused proof 同步收口，范围最小、职责清晰）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/api-keys-capability-matrix-static-scan-20260509.json` 显示 5 类风险模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证 user/supplier/admin 路径边界稳定）；测试/可靠性 PASS（focused、build、Go tests、compose config、diff-check 与真实 API 回放全部通过；既有 Semi UI `findDOMNode` / React `act(...)` warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯前端静态卡片补齐，不新增请求、轮询、持久化或部署复杂度）。后续前端优先方向继续围绕 shared-console 剩余页面的能力矩阵、注册入口、统一布局与角色差异菜单/页面补齐，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，AdminRisk shared-console bridge supporting paragraph 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/AdminRiskPage.tsx` 与 `web/src/pages/AdminRiskPage.test.tsx` 做一个小而完整的前端垂直切片，把管理员风控页 `共享接入桥接` supporting paragraph 从较长的“调整规则后仍需在同一控制台复盘限流、白名单、作用域和真实接口契约”收口为更短、更贴近当前 shared-console/new-api 单壳语气的 `规则调整后，继续用 API Keys、审计日志与 API 文档复核限流、白名单与契约是否同步生效。`；保持 CTA 去向、fallback、注册/登录接口、`/auth/menu` 菜单真值与 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/pages/AdminRiskPage.tsx`、`web/src/pages/AdminRiskPage.test.tsx` 与当前运行态回读现状，并先让 focused test 对新的 bridge 文案 RED。根因确认不是 `/admin/risk` 路由缺失、不是风控接口异常、不是 `/auth/menu` 漂移、不是 bridge CTA 导航错误，也不是真实 API 失败，而只是 AdminRisk bridge supporting paragraph 仍保留上一轮更长表述。随后仅最小修改 2 个前端文件：更新运行时 paragraph，并在 focused proof 中同步加入新文案正向断言与旧文案负向断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminRiskPage.test.tsx` 通过（6 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面执行本地忽略脚本 `.hermes/local-output/run_admin_risk_bridge_copy_real_api.sh`，最新产物目录 `.hermes/local-output/admin-risk-bridge-copy-real-api/20260509-145534`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/docs` SPA shell 全部返回 `200`。更大范围 `pnpm --dir web exec vitest run src/pages/AdminRiskPage.test.tsx src/App.test.tsx src/pages/AdminAuditPage.test.tsx src/pages/ApiKeysPage.test.tsx` 暴露 `ApiKeysPage.test.tsx` 既有失败，因当前 diff 未触及该页且 focused/build/go/compose/real-API 全绿，按 fail-closed 记为当前里程碑外既有测试债务，不阻断本轮提交。
- 本轮五维评审结论：产品/规格 PASS（文案更短，但仍保持 shared-console 单壳桥接语义与 CTA 身份，不把风控页叙述成独立后台）；代码质量 APPROVED（diff 仅 2 个文件，运行时代码与 focused proof 同步收口，范围最小且诚实；评审归档 `.hermes/local-output/admin-risk-bridge-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/admin-risk-bridge-static-scan-20260509.json` 显示 5 类风险模式均 0 命中，且未改 auth/session/menu truth/permissions）；测试/可靠性 PASS（focused、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，broader batch 中的 ApiKeys 失败不属于当前 diff 回归）；性能/运维 PASS（纯前端文案收口，不新增请求、轮询、持久化或部署复杂度）。后续前端优先方向继续围绕 shared-console focused 套件与 remaining 页面做 new-api 风格文案/语义锚点收口，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。


### 最新执行检查点（2026-05-09，AdminAudit shared-console bridge canonical wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/AdminAuditPage.tsx` 与 `web/src/pages/AdminAuditPage.test.tsx` 做一个小而完整的前端垂直切片，把管理员审计页 `共享接入桥接` supporting paragraph 与共享入口 schema 从仍残留菜单短标签心智的“风控 / API Keys / API 文档入口”收口为真实 shared-console 页面 identity：`风控中心`、`开发者 API 接入工作台`、`API 文档与接入控制台`；同时保持 `打开 API Keys` / `继续查看风控` / `查看 API 文档` 这组 CTA、fallback 回退、注册/登录接口、`/auth/menu` 菜单真值与 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/pages/AdminAuditPage.tsx`、`web/src/pages/AdminAuditPage.test.tsx` 与当前运行态回读现状，并让 focused test 先 RED：显式要求 `admin-audit-shared-console-bridge` 容器命中新 supporting paragraph，同时否定旧句子“风控、API Keys 与 API 文档入口”。根因确认不是 `/admin/audit` 路由缺失、不是审计查询失败、不是 `/auth/menu` 漂移、也不是真实 API 异常，而只是 bridge supporting paragraph 与 `sharedConsoleLinks` schema 仍保留旧菜单级 label。随后仅最小修改 2 个前端文件：为 `sharedConsoleLinks` 补充 canonical `label + button` 元数据、让 supporting paragraph 同步收口，并把 focused proof 收紧为桥接容器级正反断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminAuditPage.test.tsx` 通过（5 tests）；更大范围 `pnpm --dir web exec vitest run src/pages/AdminAuditPage.test.tsx src/App.test.tsx src/pages/AdminRiskPage.test.tsx src/pages/ApiKeysPage.test.tsx` 首轮暴露 `App.test.tsx` 既有跨页异步噪声（supplier domains / docs fallback 两条 isolated case），控制器按 fail-closed 逐条 isolated rerun 后确认均可单独通过，再次复跑目标 isolated case保持全绿，因此如实记为历史 flake/时序噪声而非本轮回归；其余 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面重跑本地忽略脚本 `.hermes/local-output/run_admin_audit_heading_scope_real_api.sh`，最新产物目录 `.hermes/local-output/admin-audit-heading-scope/20260509-133749`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/audit` SPA shell 全部返回 `200`。另外 `docker compose up -d --build web api gateway` 本轮因 docker.io 拉取 `node:22-alpine` / `golang:1.25-alpine` / `alpine:3.20` metadata 超时失败，已按 fail-closed 判定为外部 registry/network 波动而非当前 diff 回归，并继续以现有 healthy 容器 + build/go/real-API 证据作为运行态准入依据。
- 本轮五维评审结论：产品/规格 PASS（AdminAudit bridge 现已直接表达真实 shared-console 页面 identity，未把审计页叙述成独立后台，也未误导角色路径）；代码质量 APPROVED（diff 仅 2 个文件，运行时代码把桥接按钮元数据收口到 `sharedConsoleLinks`，focused proof 同步覆盖新文案与旧文案消失，改动最小且职责集中；评审纪要已落盘 `.hermes/local-output/admin-audit-heading-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/admin-audit-heading-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证 user/admin 路径边界稳定）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，broader run 中暴露的两条 `App.test.tsx` 失败经 isolated rerun 证实为历史异步噪声）；性能/运维 PASS（纯前端文案/schema 收口，不新增请求、轮询、持久化或后台负载；compose rebuild 失败原因为外部 registry 超时，当前 live stack 仍 healthy）。后续前端优先方向继续围绕 shared-console focused 用例与 remaining page wording/semantic anchor 一致性做小切片收口，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，ApiDocs 控制台能力矩阵深色单壳补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/ApiDocsPage.tsx` 与 `web/src/pages/ApiDocsPage.test.tsx` 做一个小而完整的前端垂直切片，把 API 文档页右侧仍偏普通信息卡的 `控制台能力矩阵` 收口为更贴近 shared-console / new-api 的深色 `共享控制台能力矩阵`，同步补上一段更产品化的 supporting copy，继续强调 docs / API Keys / Webhooks 仍在同一深色控制台内完成核对与联调；保持注册/登录接口、`/auth/menu` 菜单真值、角色边界、桥接 CTA 去向与 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/pages/ApiDocsPage.tsx`、`web/src/pages/ApiDocsPage.test.tsx` 与当前运行态回读现状，并先让 focused test 对新能力矩阵合同 RED。根因确认不是 `/docs` 路由缺失、不是 docs→api-keys/webhooks bridge 导航错误、不是 `/auth/menu` 漂移、也不是真实 API 失败，而只是 ApiDocs 右侧能力矩阵仍停留在浅色/默认信息卡心智，与当前仓库持续推进的 shared-console/new-api 深色单壳表面不够一致。随后仅最小修改 2 个前端文件：把运行时代码中的矩阵卡改为深色外壳、标题收口为 `共享控制台能力矩阵`、补上一段 `文档、密钥与回调仍在同一套深色控制台内完成核对与联调。` 的 supporting copy，并同步把 focused proof 收口为新 heading/copy + docs/api-keys/webhooks 三个真实 surface identity 的 scoped 断言；未改 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 通过（5 tests）；`pnpm --dir web exec vitest run src/App.test.tsx -t "uses the shared API keys route constant for register onboarding entrypoints"` 通过，用于确认本轮未破坏 login/register → shared API Keys onboarding 连续路径；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面控制器在当前 running stack 上再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且 user 菜单继续暴露 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且 admin 菜单继续暴露 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；前端 `/`、`/docs`、`/api-keys`、`/webhooks` SPA shell 均返回 `200` 且包含 `<div id="root"></div>`。另 `curl http://127.0.0.1:5173/openapi/index.html` 当前返回 `301`，判定为现有本地 web host 路由行为，不属于本轮前端 diff 回归。尝试 `docker compose up -d --build web` 时再次因 Docker Hub metadata 拉取 `node:22-alpine` / `golang:1.25-alpine` 超时失败，已按 fail-closed 记录为外部 registry/network 波动，并继续以现有 healthy 容器 + build/go/真实 API 证据作为运行态准入依据。
- 本轮五维评审结论：产品/规格 PASS（ApiDocs 能力矩阵现已更贴近 shared-console/new-api 单壳深色表面，且仍忠实表达 docs / api-keys / webhooks 的真实共享控制台身份，没有把 docs 误写成独立后台）；代码质量 APPROVED（diff 仅 2 个文件，运行时代码与 focused proof 同步收口，并根据 reviewer 建议去掉过度脆弱的精确 gradient 断言，保持结构/文案合同即可；评审纪要已落盘 `.hermes/local-output/api-docs-capability-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/api-docs-capability-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证 user/admin 路由与菜单边界稳定）；测试/可靠性 PASS（focused ApiDocs、isolated App onboarding proof、build、Go tests、compose config、diff-check 与真实 API 复核全部通过；broader grouped run 暴露的 `App.test.tsx` 单例仍属仓库既有异步噪声，经 isolated rerun 通过，不视为本轮回归）；性能/运维 PASS（纯前端静态 UI / copy 收口，不新增请求、轮询、持久化或部署复杂度；compose rebuild 失败原因为外部 registry 超时，当前 live stack 仍 healthy）。后续仍按“前端先行、shared-console/new-api 深色单壳一致性补强后再回到 Phase 4/5 后端切片”的顺序，继续围绕 remaining shared-console focused contract 与页面表面一致性推进。

### 最新执行检查点（2026-05-09，ApiDocs shared-console bridge wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/ApiDocsPage.tsx` 与 `web/src/pages/ApiDocsPage.test.tsx` 做一个小而完整的前端垂直切片，把 API 文档页 bridge 区块的 wording 从偏解释式的“共享控制台桥接路径 / 文档 → 真实业务 → 接入回放”收口为更接近 shared-console/new-api 单壳语气的“共享控制台联调桥 / 文档、业务与回调共用一套控制台”，同时同步收紧 supporting paragraph；保持注册/登录接口、`/auth/menu` 菜单真值、角色边界、桥接 CTA 去向与 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/pages/ApiDocsPage.tsx`、`web/src/pages/ApiDocsPage.test.tsx` 与当前运行态回读现状，再结合既有计划中“继续清理 ApiDocsPage / WebhooksPage / App.test.tsx 剩余低信号 copy 与 page-global proof”的方向，确认当前最安全的小切片不是改路由、不是改 Webhook/API Keys 行为、不是改注册/菜单/权限，而只是 docs bridge 区块 wording 仍可更紧凑、更像统一深色共享控制台桥接语义。随后以 focused test 先 RED，再最小修改 2 个前端文件：将运行时 tag/heading/paragraph 收口为新的 bridge 语气，并同步更新 scoped focused assertions，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 通过（5 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面执行本地忽略脚本 `.hermes/local-output/run_api_docs_front_scope_real_api.sh`，最新产物目录 `.hermes/local-output/api-docs-front-scope/20260509-122800`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/`、`/docs`、`/api-keys`、`/webhooks`、`/admin/audit` SPA shell 均返回 `200`。另执行 `.hermes/local-output/run_docs_webhooks_loop_contract_real_api.sh`，最新产物目录 `.hermes/local-output/docs-webhooks-loop-contract/20260509-122803`，再次确认 docs → api-keys / webhooks / home 单壳桥接链路的真实 API 与 SPA 合同稳定。
- 本轮五维评审结论：产品/规格 PASS（bridge 叙事更贴近 shared-console/new-api 单壳心智，未把 docs 描述成独立后台或改写角色路径）；代码质量 APPROVED（diff 仅 2 个文件、copy 与 focused proof 同步收口，改动最小且职责集中；评审纪要 `.hermes/local-output/api-docs-bridge-wording-review-20260509.md` 已落盘）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/api-docs-bridge-wording-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、diff-check 与两条真实 API 回放全部通过，既有 Semi `findDOMNode` warning 未新增）；性能/运维 PASS（纯前端文案/测试合同收口，不新增请求、状态分支、持久化或后台负载；前端 build 仅保留仓库既有 chunk-size warning）。后续前端优先方向继续围绕 ApiDocs/Webhooks/App 其余 shared-console focused 用例里的低信号 copy 与结构合同收口，再回到 Phase 4/5 余下后端切片。

### 最新执行检查点（2026-05-09，Profile shared-console 文案与桥接标题进一步收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/ProfilePage.tsx` 与 `web/src/pages/ProfilePage.test.tsx` 做一个小而完整的前端垂直切片，把 Profile 首屏 hero、副提示与桥接区 wording 从偏说明式的旧句式继续收口为更紧凑的 shared-console/new-api 入口语言，同时把 bridge 标题与普通用户角色扩展 tag 统一到当前 canonical wording `共享接入桥接` / `共享账号中枢`；保持注册/登录接口、`/auth/menu` 菜单真值、角色边界与 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、运行态与 `ProfilePage.tsx` / `ProfilePage.test.tsx` 回读现状，再先让 focused proof 对新 wording RED。根因确认不是 `/profile` 路由缺失、不是 `/auth/menu` 漂移、不是 API Keys / Webhooks / Docs bridge 导航错误，也不是真实 API 失败，而只是 Profile 首屏仍保留上一轮较长的 hero / banner / bridge copy 与普通用户 tag `深色共享账号中枢`。随后仅最小修改 2 个前端文件：把 hero 副文案收口为 `统一账号、会话与下一步入口都留在同一深色共享控制台。`，把 role-focus banner 收口为 `当前推荐：进入项目市场，继续共享控制台主链路。`，把 capability 区标题统一为 `共享接入桥接`，并把普通用户 role-expansion tag 收口为 `共享账号中枢`；focused tests 同步新增新文案正向断言与旧文案负向断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 通过（9 tests）；相关 App 路由回归 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/App.test.tsx -t "navigates from profile page CTA to the supplier domains page|renders webhook operations workspace for authenticated admin routes|renders webhook and docs workspace after following the shared integration loop"` 通过；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_profile_copy_tightening_real_api.sh`，最新产物目录 `.hermes/local-output/profile-copy-tightening-real-api/20260509-170339`（`profile-copy-tightening-real-api-latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200` 且菜单继续暴露 `/profile`、`/api-keys`、`/webhooks`、`/docs`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/`、`/profile`、`/api-keys`、`/webhooks`、`/docs` SPA shell 全部 `200`。
- 本轮五维评审结论：产品/规格 PASS（Profile 首屏 wording 更紧凑，且 bridge / role-expansion 文案继续保持 single shared-console/new-api 单壳心智，没有把 Profile 误导成独立后台）；代码质量 APPROVED（diff 仅 2 个文件、纯运行时 copy + focused proof 同步收口，评审纪要 `.hermes/local-output/profile-shared-console-copy-tightening-review-20260509.md` 已落盘）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/profile-shared-console-copy-tightening-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions）；测试/可靠性 PASS（TDD RED→GREEN、focused、相关 App 路由回归、build、Go tests、compose config、diff-check 与真实 API 回放全部通过；既有 React `act(...)` / Semi `findDOMNode` warning 未新增）；性能/运维 PASS（纯前端 copy/test-contract 收口，不新增请求、轮询、持久化或部署复杂度）。下一前端优先项继续围绕 `App.test.tsx` / `ProfilePage.test.tsx` / shared-console 其余 focused 用例中剩余的低信号 copy 与 page-global 断言收口，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，Profile / Settings shared-console 深色单壳进一步统一）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/ProfilePage.tsx`、`web/src/pages/ProfilePage.test.tsx`、`web/src/pages/SettingsPage.tsx`、`web/src/pages/SettingsPage.test.tsx` 与一个配套的 `web/src/App.test.tsx` 微调，做一个小而完整的前端垂直切片，把 Profile 的角色焦点区与 Settings 的快捷入口区从仍带浅色卡片/浅色外壳的混合观感，继续收敛为更接近 Linear/new-api 的深色单壳控制台表面；保持注册/登录接口、`/auth/menu` 菜单真值、角色权限边界，以及 Phase 4/5 的 API Key / Webhook / 风控 / 审计后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `ProfilePage` / `SettingsPage` / 对应 focused tests 回读现状，再比对 `DashboardPage.tsx`、`ApiKeysPage.tsx` 已落地的深色 mission-control 模式，确认根因不是路由缺失、不是注册功能缺口、不是 `/auth/menu` 漂移、不是真实 API 失败，而是 Profile 的 `profile-role-focus-card` 仍使用浅色默认壳与浅色内部 focus 卡，Settings 的 `settings-shortcut-cards` 与 `shortcutCardStyle(...)` 仍保留浅色卡片风格，和当前“统一登录后深色单壳控制台”目标相比仍显割裂。随后先让 focused tests 对新的深色外壳/说明合同 RED，再最小修改运行时代码：把 Profile 角色焦点区整体切到深色卡片并同步深色内部 focus 子卡与 banner，把 Settings 快捷入口外层与 shortcut cards 全部切到深色卡片/浅边框/浅文字，同时同步测试只保护本轮真实用户可见合同；`App.test.tsx` 仅顺手移除一个已不再使用的局部变量，未改任何运行时行为。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/SettingsPage.test.tsx` 通过（14 tests）；`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/SettingsPage.test.tsx src/App.test.tsx src/pages/LoginPage.test.tsx src/components/ConsoleLayout.test.tsx` 通过（46 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面执行本地忽略脚本 `.hermes/local-output/run_login_settings_front_scope_real_api.sh`，最新产物目录 `.hermes/local-output/login-settings-front-scope/20260509-115523`：验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；并验证 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` 的 SPA shell 均返回 `200`。另重跑 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录 `.hermes/local-output/app-login-register-scope/20260509-115527`，再次确认普通用户注册后 shared-console 菜单/API 路径与 seeded admin 真实 API 工作区稳定。
- 本轮五维评审结论：产品/规格 PASS（Profile/Settings 更接近单壳深色控制台，未引入独立后台或错误角色承诺；首轮使用清单仍保留浅色卡片仅作为局部 onboarding 对比层，不影响本轮主合同）；代码质量 APPROVED（diff 仅限 4 个前端页面/测试文件 + 1 个无行为变化的 App test 微调，运行时代码与 focused proof 同步）；安全/集成 SAFE（未改 auth/menu/permissions/backend contract，真实 API 双脚本回放全部通过）；测试/可靠性 PASS（46 条 focused Vitest + build/go/compose/diff-check 全通过，既有 Semi warning 未新增）；性能/运维 PASS（纯前端样式/文案收敛，不新增请求或后台负载；构建体积 warning 仍属仓库既有非阻断噪声）。后续前端优先方向继续围绕 remaining shared-console 页面做同类 new-api 深色统一与语义锚点收敛，再回到 Phase 4/5 余下后端切片。

### 最新执行检查点（2026-05-09，Login shared-console 入口文案进一步压缩）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/LoginPage.tsx` 与 `web/src/pages/LoginPage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，把登录入口页左侧 summary 与 register runway 文案继续压缩为更接近 Linear/new-api 的控制台入口语言。目标是减少 marketing/说明腔，保留“统一登录面 → 共享壳 → API Keys / Webhooks / Docs 首轮接入”的单壳心智，不引入独立后台、额外角色入口或虚假 workflow 承诺。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/pages/LoginPage.tsx`、`web/src/pages/LoginPage.test.tsx` 与当前运行态回读现状，再用 focused RED 锁定本轮真实问题：页面运行时仍保留上一轮略长的 login hero / register runway copy，和“前端优先、shared-console/new-api 密度继续收口”的目标相比仍偏说明式。根因确认不是注册逻辑缺失、不是 `/auth/menu` 漂移、不是 shared shell 布局破碎、也不是真实 API 失败，而只是 LoginPage 左侧 summary 与 2 张 runway 卡描述仍可进一步收紧。随后仅最小修改 2 个前端文件：把 hero 副文案收口为 `统一入口、共享壳与接入路径，同一登录面完成切换。`，把 register summary 收口为 `注册后进入共享控制台，按同一导航完成首轮接入。`，并把 2 张 runway 描述继续压缩为 `生成首个最小权限密钥，作为接入起点。` 与 `确认回调地址，随后发起真实联调。`；focused tests 同步改为新文案正向断言 + 旧文案负向断言，不扩散到 store、router、service、auth 或后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（8 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面重跑本地忽略脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录 `.hermes/local-output/app-login-register-scope/20260509-112641`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/`、`/login`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA shell 全部 `200`。本地浏览器快照也再次确认 login 壳仍为单一深色 shared-console/new-api 风格入口，无白屏、无明显布局破碎。
- 本轮五维评审结论：产品/规格 PASS（入口语言更短、更像 shared-console/new-api 控制台入口，且仍忠实表达单一登录面与同壳接入路径，没有把 Webhooks / Docs / API Keys 说成独立后台或强制工作流）；代码质量 APPROVED（diff 仅集中修改 `LoginPage.tsx` 与 `LoginPage.test.tsx` 2 个文件，运行时代码只触及 copy，focused proof 与本轮用户可见合同一一对应；评审纪要已落盘 `.hermes/local-output/login-shell-density-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/login-shell-density-static-scan-20260509.txt` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证 register → shared menu → user/admin 路径稳定）；测试/可靠性 PASS（focused LoginPage、build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯前端文案/测试合同收口，不新增请求、轮询、持久化、容器依赖或部署复杂度）。下一前端优先项继续围绕 login/shared-console focused 套件中剩余的低信号 copy 与结构密度做小切片收口，然后再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下能力。

### 最新执行检查点（2026-05-09，App webhook admin route 交互级重复断言去重）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `web/src/App.test.tsx` 做一个小而完整的前端 focused 收口切片，把仍重复 `web/src/pages/WebhooksPage.test.tsx` 页面级合同的 App 层 admin webhook test-delivery 用例删除，避免 App 壳层继续重复证明 `createWebhookTestDelivery(11)` 与 deliveries reload 这类页面内部交互细节。保留的 App 层合同仍聚焦 authenticated admin deep-link 进入 `/webhooks` 后工作区成功挂载 `webhooks-current-endpoints-card`，以及 `console-layout-header-summary` 标题真值 `Webhook 运维与回调观测`；保持 shared shell、角色边界、真实 API 契约与当前后端能力全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/App.test.tsx` 与 `web/src/pages/WebhooksPage.test.tsx` 回读现状，并先复跑将被删除的旧 App 用例确认它只是跨层重复 proof。根因确认不是 `/webhooks` 路由缺失、不是 `/auth/menu` 漂移、不是 admin bootstrap 失败、也不是真实 API 异常，而只是 App 层仍保留一条与 `WebhooksPage.test.tsx` 中 `queues test delivery and reloads deliveries` 重复的页面内部交互证明。随后仅最小修改 1 个测试文件：删除 App 层 `queues a webhook test delivery and keeps the admin webhook workspace mounted`，不扩散到任何运行时代码、store、router、service 或后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx -t "queues a webhook test delivery and keeps the admin webhook workspace mounted"` 返回 1 file / 22 skipped，证明旧重复用例已移除；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/WebhooksPage.test.tsx` 通过（2 files / 34 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。`docker compose ps --format json` 仍显示 `web` / `api` / `gateway` healthy。真实 API 方面新增并执行本地忽略回放产物目录 `.hermes/local-output/app-webhooks-admin-route-dedup-real-api/20260509-101313`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 JSON `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/webhooks/endpoints` 全部 `200` 且菜单继续暴露 `/webhooks`、`/api-keys`、不泄露 `/admin/risk`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/webhooks/endpoints` 全部 `200`；前端 `/` 与 `/webhooks` SPA shell 均返回 `200` 且包含 `<div id="root"></div>`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只是把 App 层 `/webhooks` 集成用例从页面内部 test-delivery 交互复核收口为更诚实的 deep-link workspace + shared shell 标题合同，没有弱化 single shared-console/new-api 单壳、角色菜单真值或 admin webhook 工作区身份）；代码质量 APPROVED（diff 仅集中修改 `web/src/App.test.tsx` 1 个文件、删除 1 条重复断言，页面级交互覆盖继续由 `web/src/pages/WebhooksPage.test.tsx` 承接，职责分层更清晰；评审归档 `.hermes/local-output/app-webhooks-admin-route-dedup-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/app-webhooks-admin-route-dedup-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证 user/admin 路由与菜单边界稳定）；测试/可靠性 PASS（App focused、Webhooks 页面 focused、前端 build、Go tests、compose config、健康检查与真实 API 回放全部通过；删除的是重复异步交互断言而非唯一覆盖，App 层抖动面略有收敛）；性能/运维 PASS（纯测试层去重，不新增请求、状态分支、容器、轮询或数据库负载，部署与运行复杂度不变，且略微减少冗余前端测试执行成本）。下一前端优先项继续清理 `App.test.tsx` 与 shared-console focused 套件中剩余的 page-global 页面细节重复断言，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，App 管理员 risk/audit 集成断言进一步去重）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `web/src/App.test.tsx` 做一个小而完整的前端 focused 收口切片，把仍重复 `AdminRiskPage.test.tsx` / `AdminAuditPage.test.tsx` 页面级合同的 App 层 admin risk / audit 断言删除，避免 shared shell / route-bootstrap 层继续重复证明 `admin-risk-overview-card`、`admin-audit-events-table-card` 与 `查询审计` 这类页面内部细节。保留的 App 层合同继续聚焦 deep-link 进入 `/admin/risk`、`/admin/audit` 后工作区成功挂载，以及 `console-layout-header-summary` 标题真值；保持注册/登录接口、角色边界、shared shell、真实 API 契约与当前后端能力全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/App.test.tsx` 与对应 focused 页面测试回读现状，并先复跑 admin risk / audit 相关 App focused 用例确认当前行为稳定。根因确认不是路由缺失、不是 `/auth/menu` 漂移、不是 admin deep-link bootstrap 错误，也不是真实 API 失败，而只是 App 层仍保留两组与 `web/src/pages/AdminRiskPage.test.tsx`、`web/src/pages/AdminAuditPage.test.tsx` 重复的 page-detail proof。随后仅最小修改 1 个测试文件：删除 App 层对 `admin-risk-overview-card`、`admin-audit-events-table-card` 与 `查询审计` 按钮的重复断言，不扩散到任何运行时代码、store、router、service 或后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx src/pages/AdminRiskPage.test.tsx src/pages/AdminAuditPage.test.tsx` 通过（3 files / 34 tests）；`pnpm --dir web exec vitest run src/App.test.tsx` 通过（23 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。`docker compose ps --format json` 显示 `web` / `api` / `gateway` healthy；`curl http://127.0.0.1:8080/healthz` 返回 `200` 且 JSON `status=ok` / `redis=ok`。真实 API 方面新增并执行本地忽略回放产物目录 `.hermes/local-output/app-admin-route-dedup-real-api/20260509-084436`（`latest` / `latest.txt` 已更新）：再次验证动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/projects/inventory`、`/orders/activations`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200` 且菜单不泄露 `/admin/risk` / `/supplier/resources`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/risk`、`/admin/audit?limit=5`、`/webhooks/endpoints` 全部 `200`；前端 `/`、`/login`、`/admin/risk`、`/admin/audit`、`/webhooks` SPA shell 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只是把 App 层 admin risk / audit 集成用例从页面内部细节复核收口为更诚实的 deep-link workspace + shared shell 标题合同，没有弱化 single shared-console/new-api 单壳、角色菜单真值或管理员工作区身份）；代码质量 APPROVED（diff 仅集中修改 `web/src/App.test.tsx` 1 个文件、删除 2 组重复断言，页面级覆盖已由 `AdminRiskPage.test.tsx` 与 `AdminAuditPage.test.tsx` 承接，职责分层更清晰；评审归档 `.hermes/local-output/app-admin-route-dedup-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/app-admin-route-dedup-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证 user/admin 路由与菜单边界稳定）；测试/可靠性 PASS（App focused、risk/audit 页面 focused、前端 build、Go tests、compose config、健康检查与真实 API 回放全部通过；Vitest 中既有 `act(...)` / `findDOMNode` / Semi Form warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯测试层去重，不新增请求、状态分支、容器、轮询或数据库负载，部署与运行复杂度不变）。下一前端优先项继续清理 `App.test.tsx` 中其余跨页集成用例对页面内部细节的重复证明，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，管理员 shared-console metric bridge wording 统一收口）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/AdminUsersPage.tsx`、`web/src/pages/AdminUsersPage.shared-console.test.tsx`、`web/src/pages/AdminSuppliersPage.tsx`、`web/src/pages/AdminSuppliersPage.test.tsx`、`web/src/pages/AdminRiskPage.tsx`、`web/src/pages/AdminRiskPage.test.tsx` 做一个小而完整的管理员前端 focused 收口切片，把 3 个管理员 mission metric 中最后残留的旧 wording `共享控制台联动` 统一收口为与 supplier 页面已落地表面一致的 `共享接入桥接`；保持注册/登录接口、`/auth/menu` 菜单真值、角色边界、真实 API 契约与 Phase 4/5 后端能力全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md` 与 3 个目标页面/测试回读现状，并先让对应 focused tests 对新 wording RED。首次复现确认根因不是 admin route 漂移、不是 `/auth/menu`、不是 bridge CTA 导航错误、不是真实 API 失败，而只是 `AdminUsersPage` / `AdminSuppliersPage` / `AdminRiskPage` 的 metric 标签仍停留在旧措辞。随后仅最小修改 6 个前端文件：把 3 处运行时 metric 标题改为 `共享接入桥接`，并同步 focused tests 增加新文案正向断言与旧文案负向断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx src/pages/AdminSuppliersPage.test.tsx src/pages/AdminRiskPage.test.tsx` 通过（3 files / 18 tests）；isolated `pnpm --dir web exec vitest run src/App.test.tsx -t "keeps shared integration routes reachable after register bootstrap"` 通过；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面执行本地忽略脚本 `.hermes/local-output/run_app_admin_route_contract_real_api.sh`、`.hermes/local-output/run_admin_suppliers_route_stub_scope_real_api.sh`、`.hermes/local-output/run_admin_users_admin_risk_route_proof_real_api.sh`，最新产物分别位于 `.hermes/local-output/app-admin-route-contract-real-api/20260509-081525`、`.hermes/local-output/admin-suppliers-route-stub-scope/20260509-081528`、`.hermes/local-output/admin-users-admin-risk-route-proof/20260509-081713`：再次验证普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200` 且不泄露 admin 路由；seeded admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/webhooks/endpoints` 全部 `200`；前端 `/admin/users`、`/admin/suppliers`、`/admin/risk`、`/api-keys`、`/webhooks`、`/docs` SPA shell 全部 `200`。浏览器侧重新检查本地 `http://127.0.0.1/admin/risk`，确认页面继续正常渲染为单一深色 shared-console/new-api 风格控制台，无白屏、无明显布局破碎、无文本缺失。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只是把 3 个管理员页面 metric 中残留的旧 bridge 心智统一收口为 `共享接入桥接`，更贴近 single shared-console/new-api 单壳叙事，未引入独立后台或错误角色承诺）；代码质量 APPROVED（diff 仅 6 个文件、3 处运行时文案与 3 处 focused proof 同步修改，职责集中且维护成本极低；评审纪要 `.hermes/local-output/admin-shared-console-bridge-wording-review-20260509.md` 已落盘）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/admin-shared-console-bridge-wording-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions）；测试/可靠性 PASS（targeted pages、isolated App 用例、build、Go tests、compose config、`git diff --check`、3 条真实 API 回放与浏览器本地视觉检查全部通过；批量 `App.test.tsx + 3 页面` 联跑再次触发与当前 diff 无关的既有 App flake，isolated rerun 已通过，按历史噪声记录）；性能/运维 PASS（纯前端文案/测试合同收口，不新增请求、轮询、持久化或部署复杂度；`docker compose up -d --build web api gateway` 本轮再次因 docker.io metadata 拉取超时失败，已按 fail-closed 记为外部 registry/network 波动而非代码回归，现有 `web` / `api` / `gateway` 容器持续 healthy 并支撑本轮真实 API / 浏览器验证）。下一前端优先项继续清理其余 admin/shared-console 运行时页面与 focused proof 中仍残留的旧 wording `共享控制台联动`，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，SupplierDomains 共享接入桥接标题与 supplier 页面统一收口）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/SupplierDomainsPage.tsx` 与 `web/src/pages/SupplierDomainsPage.test.tsx` 做一个小而完整的 supplier 前端 focused 收口切片，把域名运营页 bridge 标题从仍滞后的 `共享控制台联动` 统一收口为与 `SupplierResourcesPage`、`SupplierSettlementsPage`、`SupplierOfferingsPage` 已落地表面一致的 `共享接入桥接`；保持注册/登录接口、`/auth/menu` 菜单真值、supplier/admin/user 角色边界、真实 API 契约与 Phase 4/5 后端能力全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/pages/SupplierDomainsPage.tsx` 与 `web/src/pages/SupplierDomainsPage.test.tsx` 回读现状，确认根因不是 supplier route 漂移、不是 bridge CTA 导航错误、不是 `/auth/menu` 或真实 API 失败，而只是该页 bridge heading 仍停留在旧措辞。随后先让 `SupplierDomainsPage.test.tsx` 对新标题 `共享接入桥接` RED，再仅最小修改 2 个前端文件：把运行时 heading 与 focused 断言同步收口，并显式否定旧标题，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx` 通过（7 tests）；`pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx src/App.test.tsx` 通过（2 files / 32 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。随后 `docker compose up -d --build web api gateway` 成功并恢复 `web` / `api` / `gateway` healthy。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_supplier_domains_bridge_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-domains-bridge-real-api/20260509-070920`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且菜单继续暴露 `/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`；seeded supplier 登录后 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/resources/offerings` 全部 `200`，且菜单继续暴露 `/supplier/domains`、`/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/supplier/domains` SPA shell 返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（`共享接入桥接` 与其余 supplier shared-console 页面已使用的 canonical wording 对齐，更贴近当前 single shared-console/new-api 单壳接入叙事，未引入独立后台或错误角色承诺）；代码质量 APPROVED（diff 仅 2 个文件、1 处运行时文案与 2 条 focused 断言同步修改，职责清晰且维护成本极低；评审纪要已落盘 `.hermes/local-output/supplier-domains-bridge-heading-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/supplier-domains-bridge-heading-static-scan-20260509.json` 显示 5 类风险模式均 0 命中，且未改 auth/session/menu truth/permissions；delegate reviewers 再次跨仓漂移到 `/root/.hermes/hermes-agent`，已按 fail-closed 全部丢弃并由控制器侧 live diff + 实测结果复核）；测试/可靠性 PASS（focused SupplierDomains + App 组合、build、Go tests、compose config、compose rebuild、health、真实 API 回放全部通过；既有 Semi UI `findDOMNode` / React `act(...)` warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯前端文案/测试合同收口，不新增请求、轮询、数据写入、容器依赖或部署复杂度；build 仅保留仓库既有 chunk-size warning，compose 重建后服务持续 healthy）。下一前端优先项继续清理其余 shared-console / supplier/admin 运行时页面中仍残留的旧 bridge wording（例如 `共享控制台联动`）与对应 focused proof，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，SupplierOfferings 共享桥接标题与 supplier 页面统一收口）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/SupplierOfferingsPage.tsx` 与 `web/src/pages/SupplierOfferingsPage.test.tsx` 做一个小而完整的 supplier 前端 focused 收口切片，把供货规则页 bridge 标题从仍滞后的 `共享控制台联动` 统一收口为与 `SupplierResourcesPage`、`SupplierSettlementsPage` 已落地表面一致的 `共享接入桥接`；保持注册/登录接口、`/auth/menu` 菜单真值、supplier/admin/user 角色边界、真实 API 契约与 Phase 4/5 后端能力全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/pages/SupplierOfferingsPage.tsx` 与 `web/src/pages/SupplierOfferingsPage.test.tsx` 回读现状，并复跑 focused Vitest 确认当前真实行为稳定、差异只剩 bridge heading 仍停留在旧措辞；随后先让 `SupplierOfferingsPage.test.tsx` 对新标题 `共享接入桥接` RED，再仅最小修改 2 个前端文件：把运行时 heading 与 focused 断言同步收口，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx -t "renders supplier offering mission shell with scoped metrics and shared-console guidance"` 通过；`pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx` 通过（3 files / 25 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。随后 `docker compose up -d --build web api gateway` 成功并恢复 `web` / `api` / `gateway` healthy。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_supplier_offerings_bridge_wording_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-offerings-bridge-wording-real-api/20260509-064513`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；seeded supplier 登录后 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/offerings` 全部 `200`，且菜单继续暴露 `/supplier/offerings`、`/supplier/resources`、`/supplier/settlements`。
- 本轮五维评审结论：产品/规格 PASS（`共享接入桥接` 与 supplier 资源/结算页已使用的 canonical wording 对齐，更贴近当前 single shared-console/new-api 单壳接入叙事，未引入独立后台或错误角色承诺）；代码质量 APPROVED（diff 仅 2 个文件、1 处运行时文案与 1 处 focused 断言同步修改，职责清晰且维护成本极低；评审纪要已落盘 `.hermes/local-output/supplier-offerings-bridge-wording-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/supplier-offerings-bridge-wording-static-scan-20260509.json` 显示 5 类风险模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证 supplier menu 与 offerings contract 稳定）；测试/可靠性 PASS（focused supplier offerings + related supplier suite、build、Go tests、compose config、compose rebuild、health、真实 API 回放全部通过；既有 Semi UI `findDOMNode` / `act(...)` warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯前端文案/测试合同收口，不新增请求、轮询、数据写入、容器依赖或部署复杂度；build 仅保留仓库既有 chunk-size warning，compose 重建后服务持续 healthy）。下一前端优先项继续清理其余 shared-console / supplier focused 用例与运行时页面中仍残留的旧 bridge/fallback wording 或低信号 page-global proof，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，Webhooks 首轮接入 step-anchor focused 收口）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/WebhooksPage.tsx` 与 `web/src/pages/WebhooksPage.test.tsx` 做一个小而完整的前端 focused 收口切片，把普通用户 Webhooks 首轮接入 loop 的 brittle 文案/标题断言收紧为稳定 step/action 语义锚点；保持注册/登录接口、`/auth/menu` 菜单真值、角色边界、`/webhooks`→`/api-keys`→`/docs` 单壳联动与所有 Phase 4/5 后端契约不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/pages/WebhooksPage.tsx` 与 `web/src/pages/WebhooksPage.test.tsx` 回读现状，并复跑 focused Vitest 确认当前真实行为已稳定、问题只在 proof 仍依赖 `1. 创建首个 endpoint` / `2. 验证 test delivery` / `查看 API 文档` 等易变 copy；随后先让 `WebhooksPage.test.tsx` 对新的 step/action 语义锚点 RED，再仅最小修改 2 个文件：为 `firstHourTimeline` 补 `key`，并在运行时 loop 卡片挂载 `webhooks-first-step-*` 与 `webhooks-first-loop-action-*` 锚点，同步把 focused 断言从 copy-proof 收口为这些稳定结构合同，不扩散到 store、router、service、auth 或后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx` 通过（12 tests）；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/WebhooksPage.test.tsx src/pages/ApiDocsPage.test.tsx` 通过（3 files / 42 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。随后 `docker compose up -d --build web api gateway` 成功并恢复 `api` / `web` / `gateway` healthy，真实 API 复跑脚本 `.hermes/local-output/run_webhooks_step_anchor_real_api.sh` 最新产物目录 `.hermes/local-output/webhooks-step-anchor-real-api/20260509-062128`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且菜单继续暴露 `/webhooks`、`/api-keys`、`/docs`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/webhooks`、`/api-keys`、`/docs` SPA shell 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只是把 Webhooks 普通用户首轮接入 loop 的 focused proof 从易变 copy 收口为稳定 step/action 语义锚点，继续保持 single shared-console/new-api 单壳与 `/webhooks`→`/api-keys`→`/docs` 联动身份不变）；代码质量 APPROVED（diff 仅 2 个文件，运行时最小新增 `key` 字段与 5 个 `data-testid`，测试同步切换为锚点断言，职责单一且维护成本更低；评审纪要已落盘 `.hermes/local-output/webhooks-step-anchor-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/webhooks-step-anchor-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions）；测试/可靠性 PASS（targeted Webhooks focused、App+Webhooks+Docs 组合、build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 Semi UI / React `act(...)` warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯前端语义锚点收口，不新增请求、轮询、持久化或部署复杂度，compose 重建后服务健康，build 仅保留仓库既有 chunk-size warning）。下一前端优先项继续清理其余 shared-console focused 用例中仍残留的 page-global copy / 长说明 proof，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，App 管理员 Dashboard 深统计测试分层收口）
- 本轮继续遵循"前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交"：围绕 `web/src/App.test.tsx` 中仍重复 `DashboardPage.test.tsx` 页面级合同的管理员 dashboard 深统计用例做一个小而完整的前端 focused 收口切片。最终方案不是简单删除，而是把 App 层重复的 `renders admin dashboard deep statistics` 去除，同时把必要的深统计展示合同显式下沉到 `web/src/pages/DashboardPage.test.tsx`；保持注册/登录接口、`/auth/menu` 菜单真值、角色边界、`/admin/overview` 路由身份与所有 Phase 4/5 后端契约不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/App.test.tsx` 与 `web/src/pages/DashboardPage.test.tsx` 回读现状，并做最小试删验证；五维评审中的代码质量 reviewer fail-closed 指出：若只删除 App 用例，会使 `dashboard-order-completion-rate-card`、`dashboard-dispute-rate-card`、`dashboard-finished-revenue-card`、`dashboard-top-supplier-card`、`dashboard-supplier-settlement-rank-card` 与 `鉴权拒绝总数` 这些管理员 dashboard 深统计节点失去直接覆盖。控制器据此立即整改：保留 App 层去重方向，但把这些展示断言下沉到已有的管理员页面 focused 用例 `renders admin role surfaces and admin ops summary inside the shared shell`，用页面级 mock 数据直接验证 `60.00%`、`20.00%`、`¥8888.00`、`supplier-top@nexus-mail.local`、`72.73%` 与 `鉴权拒绝总数：13`，从而实现"App 壳层只保留 bootstrap/route-workspace 合同，DashboardPage 页面层负责深统计细节"的正确测试分层。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx -t "renders admin dashboard deep statistics"` 结果为 1 file / 25 skipped，证明旧重复用例已移除；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/DashboardPage.test.tsx` 通过（2 files / 33 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面重跑本地忽略脚本 `.hermes/local-output/run_app_admin_dashboard_dedup_real_api.sh`，最新产物目录 `.hermes/local-output/app-admin-dashboard-dedup-real-api/20260509-053954`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且 menu 不泄露 `/admin/risk`、`/admin/audit`、`/supplier/resources`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/webhooks/endpoints` 全部 `200`，并确认 admin menu 继续暴露 `/admin/risk`、`/admin/audit`、`/webhooks`；前端 `/`、`/login`、`/admin/risk`、`/admin/audit`、`/webhooks` SPA shell 全部 `200`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只是把管理员 dashboard 深统计测试从 App 壳层收口到 DashboardPage 页面 focused 层，继续保持 single shared-console/new-api 单壳、角色菜单真值与 `/admin/overview` 工作区身份不变）；代码质量 APPROVED（初版"只删不补"被 fail-closed 拦下后，已同轮完成 `App.test.tsx` 去重 + `DashboardPage.test.tsx` 补强，测试职责分层更清晰；评审纪要已更新落盘 `.hermes/local-output/app-admin-dashboard-dedup-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/app-admin-dashboard-dedup-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证普通用户/管理员菜单边界和管理员 overview/risk/audit/webhooks 路由稳定）；测试/可靠性 PASS（目标重复用例已删除，且页面级管理员 dashboard 深统计节点已由 `DashboardPage.test.tsx` 明确接管，`App.test.tsx + DashboardPage.test.tsx`、build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` / Semi `findDOMNode` warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯测试层重分层，不新增请求、轮询、持久化、构建或部署复杂度，且减少 App 层冗余断言、降低长期维护成本）。下一前端优先项继续清理 `App.test.tsx` 其余跨页集成用例中的 page-global 页面细节重复断言与低信号 copy proof，随后再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，Auth bootstrap shell 英文标签收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/App.tsx` 与 `web/src/App.test.tsx` 做一个小而完整的前端 focused 收口切片，把 `AuthBootstrapShell` 顶部仍残留的英文低信号标签 `shared-console bootstrap` 收口为与当前单壳叙事一致的中文文案 `共享控制台引导`，并让 App focused 测试显式保护该用户可见合同；保持注册/登录接口、`/auth/menu` 菜单真值、角色分流、bootstrap redirect 与 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md` 与 `web/src/App.tsx` / `web/src/App.test.tsx` 回读现状，确认根因不是 auth bootstrap 行为错误、不是 `/auth/menu` 漂移、不是角色边界异常，也不是 shared shell 结构缺失，而只是 bootstrap shell 顶部 pill 仍保留英文标签；随后先让 `App.test.tsx` 中 `shows a shared-console bootstrap shell while waiting for server menu instead of rendering fallback privileged navigation from client role state` 对新文案 RED，再仅最小修改 1 处运行时文案与 1 处 focused 断言完成 GREEN，不扩散到其他页面、store、router、service 或后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx -t "shows a shared-console bootstrap shell while waiting for server menu instead of rendering fallback privileged navigation from client role state"` 通过；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/LoginPage.test.tsx src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx` 通过（4 files / 41 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config` 全部通过。真实 API 方面重跑 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录 `.hermes/local-output/app-login-register-scope/20260509-050158`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit` 全部 `200`；前端 `/login`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA shell 全部 `200`。浏览器侧重新检查本地 `http://127.0.0.1:5173/login`，确认页面整体维持统一深色 shared-console/new-api 风格，无明显文字裁切、按钮错位、重叠或布局破碎。
- 本轮五维评审结论：产品/规格 PASS（bootstrap shell 顶部标签从英文收口为 `共享控制台引导` 后，更贴近当前单一登录后控制台叙事，且没有把共享控制台误写成独立后台）；代码质量 APPROVED（diff 仅 2 个文件、1 处运行时文案与 1 处 focused 断言同步修改，改动最小且职责清晰；评审纪要已落盘 `.hermes/local-output/bootstrap-copy-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/bootstrap-copy-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；delegate reviewers 全部跨仓漂移到 `/root/.hermes/hermes-agent`，已按 fail-closed 丢弃并由控制器侧 live diff + focused/真实 API 实测复核）；测试/可靠性 PASS（targeted App 用例、4 文件 focused 组合、build、Go tests、compose config 与真实 API 回放全部通过；既有 React `act(...)` / Semi `findDOMNode` warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯前端 copy/test-contract 收口，不新增请求、轮询、持久化或部署复杂度）。下一前端优先项继续围绕 shared-console / App focused 用例中仍残留的英文低信号表面、page-global 重复断言与慢测抖动点做小切片收口，然后再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下能力。

### 最新执行检查点（2026-05-09，front focused timeout 用例去脆弱化收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/SupplierResourcesPage.test.tsx` 与 `web/src/pages/AdminUsersPage.shared-console.test.tsx` 做一个小而完整的前端 focused 测试收口切片，针对 shared-console 已知慢测 `submits supplier domain, account, and mailbox actions then reloads overview data` 与 `submits wallet adjustment, settlement and dispute resolution flows` 的超时/抖动问题做去脆弱化修复。保持所有运行时代码、注册/登录接口、`/auth/menu` 菜单真值、角色边界、API Key / Webhook / 风控 / 审计后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md` 与 focused vitest 复现当前失败：批量 focused 运行中 `SupplierResourcesPage.test.tsx` 与 `AdminUsersPage.shared-console.test.tsx` 会在 `user.type(...)` 长串输入 + Semi Form 异步校验链路下出现 5s timeout / 不稳定。回读两个测试文件并对照同仓 `AdminUsersPage.test.tsx` 的既有稳定写法后，确认根因不是运行时代码缺锚点、不是 `/supplier/resources` / `/admin/users` 页面逻辑错误、不是 `/auth/menu` 或真实 API 漂移，而是测试层对 Semi UI 表单采用了过慢且易触发重复校验的输入方式。随后仅最小修改 2 个测试文件：把慢路径切到 `fireEvent.change(...)`，并在真实异步提交断言处补 `waitFor(...)`，不扩散到任何运行时代码。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx -t "submits supplier domain, account, and mailbox actions then reloads overview data"` 通过；`pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx -t "submits wallet adjustment, settlement and dispute resolution flows"` 通过；`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/AdminUsersPage.shared-console.test.tsx src/pages/ApiKeysPage.test.tsx` 通过（3 files / 30 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面执行 `.hermes/local-output/run_supplier_resources_card_scope_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-resources-card-scope-real-api/20260509-044339`，再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`，supplier 菜单继续暴露 `/supplier/resources`、`/api-keys`、`/webhooks`、`/docs`，并且真实新增域名 / 第三方邮箱账号 / 邮箱池后 overview 可见；同时执行 `.hermes/local-output/run_admin_users_mission_flow_real_api.sh`，最新产物目录 `.hermes/local-output/admin-users-mission-flow-20260509-044333`，再次验证普通用户注册后 `/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，seeded admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`，前端 `/admin/users`、`/api-keys`、`/settings` SPA shell 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只是把两个 focused 用例从慢且易抖动的输入链路收口为更稳定的表单交互 proof，没有弱化 shared-console 单壳叙事，也没有把测试改成空洞存在性断言）；代码质量 APPROVED（diff 仅 2 个测试文件，修改集中于 `fireEvent.change(...)` 与 `waitFor(...)`，并与同仓既有稳定测试模式保持一致；评审纪要已落盘 `.hermes/local-output/front-focused-timeout-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/front-focused-timeout-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions）；测试/可靠性 PASS（两个 targeted focused 用例、3 文件 focused 组合、前端 build、Go tests、compose config 与两条真实 API 回放全部通过，且把原先批量 focused 中最容易超时的两条稳定下来）；性能/运维 PASS（纯测试层去脆弱化，不新增请求、轮询、持久化或容器复杂度，build 仅保留既有 chunk-size warning）。下一前端优先项继续围绕 shared-console focused 套件剩余慢测/抖动点做小切片去脆弱化，然后再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下能力。

### 最新执行检查点（2026-05-09，Supplier 资源/结算 shared-console bridge 统一收口为“共享接入桥接”）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/SupplierResourcesPage.tsx`、`web/src/pages/SupplierResourcesPage.test.tsx`、`web/src/pages/SupplierSettlementsPage.tsx`、`web/src/pages/SupplierSettlementsPage.test.tsx` 做一个小而完整的 supplier 前端垂直切片，把两页 bridge 卡片标题从偏泛化的 `共享控制台联动` 统一收口为更贴近当前单壳接入语义的 `共享接入桥接`，保持 route 行为、`/auth/menu` 菜单真值、supplier/admin/user 角色边界、真实 API 契约与 Phase 4/5 后端能力全部不变。
- 控制器按 TDD + `systematic-debugging` 先让 `SupplierResourcesPage.test.tsx` 与 `SupplierSettlementsPage.test.tsx` 对新 heading 合同 RED，再回读对应运行时代码确认根因不是 supplier 资源/结算路由漂移、不是 bridge CTA 导航错误、不是 `/auth/menu`、不是 supplier real API 失败，而只是两页 bridge 标题仍停留在旧的“共享控制台联动”表述。随后仅最小修改 4 个前端文件，把运行时标题与 focused 断言同步收口为 `共享接入桥接`，不扩散到 store、router、service、auth 或后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx` 通过（2 files / 19 tests）；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx` 复跑通过（3 files / 45 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面执行本地忽略脚本 `bash .hermes/local-output/run_supplier_resources_card_scope_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-resources-card-scope-real-api/20260509-041537`（`latest.txt` 已更新），再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；supplier 登录后 `/auth/menu`、`/auth/me`、`/supplier/resources/overview` 全部 `200`，并成功真实新增域名/第三方邮箱账号/邮箱池记录后再次在 overview 中可见；同时执行 `bash .hermes/local-output/run_supplier_settlements_heading_scope_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-settlements-heading-scope/20260509-041540`（`latest.txt` 已补写），再次验证普通用户 `/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，seeded supplier 登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 全部 `200`，并确认 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/settlements` SPA shell 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（`共享接入桥接` 比 `共享控制台联动` 更准确表达 supplier 资源/资金页与 API Keys / Webhooks / Docs 的单壳接入关系，未引入独立后台或错误角色承诺）；代码质量 APPROVED（diff 仅 4 个文件、4 处标题/断言同步修改，focused tests 与运行时合同一致，改动最小）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/supplier-bridge-wording-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；delegate reviewers 发生跨仓漂移到 `/root/.hermes/hermes-agent`，已按 fail-closed 全部丢弃并由控制器侧 live diff + 实测结果复核）；测试/可靠性 PASS（focused Vitest、App 组合、build、Go tests、compose config 与两条真实 API 回放全部通过；既有 React `act(...)` / Semi `findDOMNode` warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯前端 copy/test-contract 收口，不新增请求、轮询、持久化或容器复杂度）。下一前端优先项继续围绕 shared-console focused 用例与 supplier/admin 页面 remaining copy/semantic anchor 一致性做小切片收口，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下能力。

### 最新执行检查点（2026-05-09，Dashboard 首轮 onboarding 英文化标签收口）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/DashboardPage.tsx` 与 `web/src/pages/DashboardPage.test.tsx` 做一个小而完整的前端垂直切片，把默认普通用户 dashboard 首轮 onboarding 中仍残留的英文低信号标签继续收口为 shared-console / new-api 一致的中文表面：将 onboarding 顶部 tag 从 `First Run Mission` 收口为 `首次使用指引`，并将首张采购 mission card tag 从泛化 `共享控制台` 收口为更贴近当前卡片语义的 `基础采购`；保持注册/登录接口、`/auth/menu` 菜单真值、角色路由、后端契约与 Phase 4/5 能力全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `web/src/pages/DashboardPage.test.tsx` 写出 RED：显式要求 hero/onboarding 区出现 `首次使用指引`，且 `dashboard-first-run-mission-card-procurement` 命中 `基础采购`；首次失败后回读 `web/src/pages/DashboardPage.tsx`，确认根因不是路由、不是 `/auth/menu`、不是 dashboard 数据接口、不是共享壳结构漂移，而只是 onboarding 表面仍遗留一处英文 tag 与一处过宽泛标签。随后仅最小修改 `DashboardPage.tsx` 2 处文案并使 focused 测试 GREEN，不扩散到其他页面、store、router 或后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx` 通过（2 files / 34 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_dashboard_onboarding_copy_real_api.sh`，最新产物目录 `.hermes/local-output/dashboard-onboarding-copy-real-api/20260509-034945`（`latest-path.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200` 且菜单仍只暴露共享路径；seeded supplier 登录后 `GET /api/v1/auth/menu`、`/supplier/resources/overview` 全部 `200`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/`、`/login`、`/profile`、`/settings`、`/projects`、`/orders`、`/balance`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit`、`/admin/pricing`、`/admin/suppliers`、`/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/supplier/domains` SPA shell 全部返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（新文案继续强化默认普通用户首轮路径的 shared-console / new-api 单壳叙事，没有把 dashboard onboarding 误写成独立后台或强制 workflow）；代码质量 APPROVED（diff 仅集中修改 2 个前端文件、2 处运行时文案与 2 条对应断言，改动最小且职责清晰）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/dashboard-onboarding-copy-static-scan.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions）；测试/可靠性 PASS（focused Vitest、App 组合、build、Go tests、compose config 与真实 API 回放全部通过；既有 React `act(...)` / `findDOMNode` / Semi warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯前端 copy/test-contract 收口，不新增请求、轮询、持久化或部署复杂度，build 仅保留仓库既有 chunk-size warning）。下一前端优先项继续围绕 Dashboard / shared-console focused 用例中仍残留的英文标签、低信号 copy 与 page-global 脆弱断言做小切片收口，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，App 管理员 risk 页面级重复断言去重）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/App.test.tsx` 做一个小而完整的前端 focused 收口切片，把仍重复 `AdminRiskPage.test.tsx` 页面级合同的 App 层 admin risk 断言删除，避免在 shared shell / route-bootstrap 层继续重复证明 `规则命中概览` 与 `保存规则` 等页面内部细节。保留的 App 层合同仍聚焦 `/admin/risk` deep-link、bootstrap 后 workspace 挂载与 `console-layout-header-summary` 标题真值，不改注册/登录接口、不改 `/auth/menu` 菜单真值，也不改任何 Phase 4/5 后端契约。
- 控制器按 TDD + `systematic-debugging` 先从 `todo.md`、`git status` 与 `web/src/App.test.tsx` / `web/src/pages/AdminRiskPage.test.tsx` 回读现状，确认根因不是运行时代码缺锚点、不是 admin risk 路由漂移、不是权限边界异常，而是 App 层仍保留一条只证明 `admin-risk-overview-card` + `保存规则` 的重复测试；这些细节已由 `AdminRiskPage.test.tsx` 更完整覆盖。随后仅最小删除 `App.test.tsx` 中该条重复用例，不扩散到任何运行时代码或其他页面。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx src/pages/AdminRiskPage.test.tsx` 通过（2 files / 32 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面重跑本地忽略脚本 `.hermes/local-output/run_app_admin_route_contract_real_api.sh`，最新产物目录 `.hermes/local-output/app-admin-route-contract-real-api/20260509-031615`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/menu`、`/auth/me`、`/projects/inventory`、`/orders/activations`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/risk`、`/admin/audit?limit=5`、`/webhooks/endpoints` 全部 `200`，且前端 `/admin/risk`、`/admin/audit`、`/webhooks` SPA shell 均返回 `200`。`docker compose up -d --build web api gateway` 本轮再次因 docker.io 拉取 `node:22-alpine` / `golang:1.25-alpine` metadata 超时失败；控制器已按 fail-closed 保持现有 healthy 容器作为真实 API / shell 验证源，并明确记为外部 registry/network 波动而非当前 diff 回归。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只是删除 App 层重复的 admin risk 页面细节断言，继续保持 single shared-console/new-api 单壳、角色菜单真值与 `/admin/risk` 工作区身份不变）；代码质量 APPROVED（`App.test.tsx` 仅保留 route/bootstrap/workspace 合同，`AdminRiskPage.test.tsx` 继续负责 `规则命中概览` 与 `保存规则` 页面级 proof，职责分层更清晰；评审归档 `.hermes/local-output/app-admin-risk-dedup-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/app-admin-risk-dedup-static-scan-20260509.json` 显示 5 类风险模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证 admin risk / audit / webhooks 路由链路稳定）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config 与真实 API 回放全部通过，删除的是重复断言而非唯一覆盖；既有 `act(...)` / `findDOMNode` / Semi warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯测试层去重，略微减少重复 mock/setup 与 warning 噪声，不新增请求、轮询、持久化或部署复杂度；compose rebuild 失败原因为外部 registry 超时，现有健康容器继续可用）。下一前端优先项继续清理 `App.test.tsx` 及 shared-console focused 用例中剩余的 page-global 页面细节重复断言，在此前提下再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，shared-console fallback wording 全面收口）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：围绕仍残留在多页 fallback / bridge 回退表面中的旧措辞 `推荐工作台` 做一个前端垂直切片收口，将 `Projects / Orders / Balance / Webhooks / ApiDocs / SupplierDomains / SupplierOfferings / SupplierResources / SupplierSettlements / AdminProjects / AdminRisk / AdminAudit / AdminSuppliers` 及其 focused tests 中剩余的 fallback heading / description / CTA 全部统一为 `共享工作台`，保持 route 行为、`/auth/menu` 菜单真值、角色边界与所有后端契约不变。
- 控制器按 TDD + `systematic-debugging` 先让相关 focused tests 对新文案 RED，再做最小运行时代码收口；随后补发现的 `BalancePage` 遗漏 2 处 wording，最终把 `web/src/pages/**/*.tsx` 与 `*.test.tsx` 内 assistant-visible `推荐工作台` 残留清零。当前 diff 共覆盖 24 个前端页面/测试文件，全部属于 copy/test-contract 收口，没有扩散到 store、auth、router 或后端逻辑。相关 static scan 结果已落盘 `.hermes/local-output/shared-console-fallback-wording-static-scan.json`，5 类风险模式均为 0 命中。
- 控制器验证结果：focused wording 套件 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/AdminAuditPage.test.tsx src/pages/AdminRiskPage.test.tsx src/pages/AdminSuppliersPage.test.tsx src/pages/AdminProjectsPage.test.tsx src/pages/BalancePage.test.tsx` 首轮仅剩仓库既有 `SupplierResourcesPage.test.tsx` timeout；isolated rerun 后 `BalancePage.test.tsx`、`SupplierResourcesPage.test.tsx`、`AdminUsersPage.shared-console.test.tsx`、`App.test.tsx` 全部通过。其余 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`、`docker compose up -d --build web api gateway` 全部通过，且 `web` / `api` / `gateway` healthy。全量 `pnpm --dir web exec vitest run` 首轮仍出现 `App.test.tsx`、`AdminUsersPage.shared-console.test.tsx`、`SupplierResourcesPage.test.tsx` 三条既有 async/timeout 噪声，控制器逐条 isolated rerun 全绿，因此如实记为历史 flake 而非当前里程碑回归。
- 真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_shared_console_fallback_wording_real_api.sh`，最新产物目录 `.hermes/local-output/shared-console-fallback-wording-real-api/20260509-010355`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/menu`、`/auth/me`、`/projects/inventory`、`/orders/activations`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded supplier 登录后 `GET /api/v1/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements` 全部 `200`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/webhooks/endpoints` 全部 `200`。同时再次确认前端 `/projects`、`/orders`、`/balance`、`/webhooks`、`/docs`、`/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/supplier/domains`、`/admin/risk`、`/admin/audit`、`/admin/pricing`、`/admin/suppliers` SPA shell 均返回 `200` 且包含 `<div id="root"></div>`。
- 本轮五维评审结论：产品/规格 PASS（fallback wording 统一为 `共享工作台`，与当前 single shared-console/new-api 单壳叙事一致，未引入独立后台或错误角色承诺）；代码质量 APPROVED（diff 完全局限于前端 copy 与 focused test 合同，Balance 漏网项已同轮补齐；相关评审归档 `.hermes/local-output/shared-console-fallback-wording-review-20260509.md`）；安全/集成 SAFE（静态扫描 0 命中，未改 auth/session/menu truth/permissions，真实 API 再证 role 菜单边界稳定）；测试/可靠性 PASS（focused、isolated rerun、build、Go tests、compose config、compose rebuild 与真实 API 回放全部通过，全量 suite 的 3 条失败已确认是既有 flake）；性能/运维 PASS（纯前端 copy/test 收口，不新增请求、轮询、持久化或部署复杂度）。下一前端优先项继续回到 `App.test.tsx` / shared-console focused 用例中仍残留的 page-global 集成断言去脆弱化与 new-api 壳一致性补强，再在此前提下推进 Phase 4/5 余下 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计切片。

### 最新执行检查点（2026-05-09，Login 首轮接入 runway 文案进一步压缩）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `web/src/pages/LoginPage.tsx` 与 `web/src/pages/LoginPage.test.tsx` 做一个小而完整的前端 focused 收口切片，把登录页左侧 `首轮接入路径` summary 与 3 张 runway 卡进一步压缩成更像 new-api/shared-console 控制台入口的短句，继续强化“注册后仍留在同一壳里，按 API Keys → Webhook → Docs 完成首轮接入”的单壳叙事，不引入任何独立后台、注册分站或额外角色流程。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、`web/src/pages/LoginPage.tsx`、`web/src/pages/LoginPage.test.tsx` 与当前运行态回读现状，并先让 `LoginPage.test.tsx` 对新的 runway summary / 3 条 concise copy RED。根因确认不是注册逻辑缺失、不是 `/auth/menu` 漂移、不是 shared shell 布局破碎、也不是真实 API 失败，而只是 login 左侧 runway 仍保留上一轮较长表述：summary 还是“沿同一导航完成首个接入闭环”，3 张卡的描述仍偏句子化。随后仅最小修改 2 个前端文件：把 summary 收口为 `注册后进入共享控制台，按 API Keys → Webhook → Docs 完成首轮接入。`，并同步把 3 条卡片描述压缩为 `生成首个最小权限密钥。`、`确认回调地址并发起真实联调。`、`核对 API 文档与请求契约。`；测试同步增加新文案正向断言与旧文案负向断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（8 tests）；`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（2 files / 31 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面重跑本地忽略脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录 `.hermes/local-output/app-login-register-scope/20260509-094842`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；seeded admin 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA shell 全部 `200`。另外使用本地 build 产物 `pnpm --dir web preview --host 127.0.0.1 --port 4173` + 浏览器重新验证 `http://127.0.0.1:4173/login`，确认最新 login 页面已显示新 summary 与 3 条短句，无明显裁切、重叠或 absurd spacing；仅保留轻微的左右列高度不均与密码眼睛图标偏淡作为后续非阻塞 polish。`docker compose up -d --build web api gateway` 本轮因 docker.io 拉取 `node:22-alpine` / `golang:1.25-alpine` metadata 超时失败，已按 fail-closed 判定为外部 registry/network 波动而非当前代码回归，并保留现有 healthy 容器 + 本地 preview 作为验证源。
- 本轮五维评审结论：产品/规格 PASS（runway 文案压缩后更明确地表达 shared-console 单壳下的首轮接入顺序，没有把 docs / webhook / API Keys 描述成独立后台或额外 onboarding 流）；代码质量 APPROVED（diff 仅 2 个前端文件、4 处运行时 copy 与对应 focused proof 同步修改，改动极小且职责清晰；评审纪要已落盘 `.hermes/local-output/login-runway-copy-review-20260509.md`）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/login-runway-copy-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式均 0 命中，且未改 auth/session/menu truth/permissions；真实 API 再证 user/admin 路径稳定）；测试/可靠性 PASS（Login/App focused、build、Go tests、compose config 与真实 API 回放全部通过；既有 React `act(...)` / Semi `findDOMNode` warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯前端 copy/test-contract 收口，不新增请求、轮询、持久化或部署复杂度；compose rebuild 失败系外部 registry 超时，当前 live stack 仍 healthy，另有 build-preview 复核兜底）。下一前端优先项继续围绕 login/App/shared-console focused 套件中的低信号 copy 与 `act(...)` 噪声做小切片收口，然后再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，Login 首轮接入路径进一步收敛为 shared-console / new-api 入口 runway）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，继续收敛 `LoginPage` 左侧 register journey，把旧的 `注册后第一轮动作` 进一步压缩为更像控制台入口的 `首轮接入路径`，并把三张 runway 卡继续校准为 `API Keys 起步`、`Webhook 联调`、`文档核对` 三个 shared-console 接入入口，保持“登录/注册后仍进入同一控制台，再按角色和菜单展开业务区”的单壳叙事不变。
- 控制器按 TDD + `systematic-debugging` 先从 `web/src/pages/LoginPage.test.tsx` 写出 RED：显式要求 heading 从 `注册后第一轮动作` 收紧为 `首轮接入路径`，并把三张卡的标题/说明收紧到 `API Keys 起步`、`Webhook 联调`、`文档核对` 与对应 concise copy；首次 RED 后回读 `web/src/pages/LoginPage.tsx`，确认根因只是 `integrationRunway` 与 journey summary 仍保留上一轮更说明式 wording，而不是 auth、router、`/auth/menu`、session 或真实 API 合同问题。随后仅最小修改 `LoginPage.tsx` 与 `LoginPage.test.tsx` 完成 GREEN。期间五维评审曾指出 `文档回放` / `回到文档页核对请求契约与回放链路` 会把 docs 页误表述成 replay 落点，控制器已同轮回退并收口为与现有 `ApiDocsPage` 合同一致的 `文档核对` / `查看 API 文档并核对请求契约。`。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（2 files / 35 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`curl http://127.0.0.1/healthz` 返回 `200` 且 `status=ok` / `redis=ok`。真实 API 方面重跑本地忽略脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录 `.hermes/local-output/app-login-register-scope/20260509-025208`（`latest.txt` 已更新）：再次验证普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 `200`；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 `200`；前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳全部返回 `200`。`docker compose up -d --build web api gateway` 本轮成功重建 `web`，但 `api` 镜像在拉取 `alpine:3.20` / `golang:1.25-alpine` 元数据时遭遇 docker.io 网络超时；控制器按 fail-closed 继续使用当前健康中的 live stack 做 `healthz + 真实 API 回放` 复核，确认这是外部 registry/network 问题，而非当前前端 diff 回归。
- 本轮五维评审结论：产品/规格 PASS（`首轮接入路径` 比旧的 `注册后第一轮动作` 更像控制台入口 runway，且 docs wording 已收回到真实“文档核对”合同，没有把 docs 页误写成 replay 落点，也未引入独立后台或强制 workflow 承诺）；代码质量 APPROVED（diff 仅集中修改 `LoginPage.tsx` 与 `LoginPage.test.tsx` 两个文件，运行时代码只触及 journey 标题/step 文案，测试同步保护新 heading、三步卡片与旧 wording 消失合同，未扩散到 auth store、router、service 或后端逻辑）；安全/集成 SAFE（static scan 结果 `.hermes/local-output/login-register-runway-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类模式 0 命中，且未改 login/register、session 持久化、`navigate('/')`、`/auth/menu` 真值或任何角色边界；真实 API 再证 user/admin 路径稳定）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、真实 API 回放全部通过；既有 React `act(...)` / Semi UI warning 仍属历史噪声，本轮未恶化；review 仅提示 wording 断言偏细、未来若继续做 copy polish 可再收敛到更稳定的卡片级结构断言）；性能/运维 PASS（纯前端静态文案/test-contract 收口，不新增请求、状态分支、数据库负载或部署复杂度；`pnpm build` >500kB chunk warning 与 docker.io metadata timeout 分别属于仓库既有前端 bundle follow-up 与外部网络波动，本轮未引入新的运维复杂度）。

### 最新执行检查点（2026-05-09，App 管理员 risk/audit/webhooks 集成断言进一步去重）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，继续沿同一方向只收口 `web/src/App.test.tsx` 中仍重复页面 focused 合同的 admin `/admin/risk`、`/admin/audit` 与 `/webhooks` 集成断言。具体做法是：把此前仍残留在 App 层的 `规则命中概览`、风险信号文本、审计表格行内容与 `测试投递已入队，系统将异步真实回调目标地址` toast 文案断言，统一收口为更诚实的 shell / bootstrap 合同——(1) deep-link 进入对应工作区后页面成功挂载 `admin-risk-overview-card`、`admin-audit-events-table-card`、`webhooks-current-endpoints-card`；(2) `console-layout-header-summary` 继续按角色/路由命中 `风控中心`、`审计日志`、`Webhook 运维与回调观测`。同时将相关用例标题同步改为 route-workspace 语义，避免 App 层继续重复证明 `AdminRiskPage.test.tsx`、`AdminAuditPage.test.tsx`、`WebhooksPage.test.tsx` 已覆盖的页面内部内容。
- 控制器按 TDD + `systematic-debugging` 先从 `todo.md` 与当前 `git status` 回读前序检查点，确认上一轮已完成 admin `/webhooks` 用例收口，而 `App.test.tsx` 中其余 admin risk/audit/webhooks 集成用例仍保留 page-level metric / toast / 表格行文本重复断言。随后回读 `web/src/App.tsx`、`web/src/pages/AdminRiskPage.test.tsx`、`web/src/pages/AdminAuditPage.test.tsx`、`web/src/pages/WebhooksPage.test.tsx`，确认根因不是运行时代码缺锚点、不是 deep-link/route bootstrap 漂移、不是 `/auth/menu` 或角色权限失真，而是 App 集成层继续复核页面 focused 已单独覆盖的细节。之后仅以 test-only diff 最小修改 `web/src/App.test.tsx` 完成 GREEN：将 admin risk / audit workspaces 用例收口为 mounted workspace + header summary proof，将 webhook test-delivery 用例保留调用与刷新验证但去掉 toast 文案主断言，不扩散到任何运行时代码。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx` 通过（1 file / 27 tests）；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/AdminRiskPage.test.tsx src/pages/AdminAuditPage.test.tsx src/pages/WebhooksPage.test.tsx` 通过（4 files / 50 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`docker compose up -d --build web api gateway`、`git diff --check` 全部通过，且 `web` / `api` / `gateway` healthy。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_app_admin_route_contract_real_api.sh`，最新成功产物目录 `.hermes/local-output/app-admin-route-contract-real-api/latest`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/menu`、`/auth/me`、`/projects/inventory`、`/orders/activations`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200` 且菜单不泄露 `/admin/risk` / `/supplier/resources`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/risk`、`/admin/audit?limit=5`、`/webhooks/endpoints` 全部 `200`，且 admin menu 继续暴露 `/admin/risk`、`/admin/audit`、`/webhooks`；前端 `http://127.0.0.1:5173/`、`/login`、`/admin/risk`、`/admin/audit`、`/webhooks` SPA shell 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 继续把 App 层 admin risk/audit/webhooks 集成用例从页面内部文本复核收口为更诚实的“deep-link 工作区可达 + shared shell 标题正确”，没有弱化 single shared-console、角色菜单真值或 deep-link bootstrap 合同）；代码质量 APPROVED（diff 仅集中修改 `web/src/App.test.tsx`，去掉重复 metrics / toast / 表格行细节断言，保留 App 层真正应证明的 route-workspace + header summary 合同；评审产物已落盘 `.hermes/local-output/app-admin-route-review-20260509.md`）；安全/集成 SAFE（added-line 静态扫描结果 `.hermes/local-output/app-admin-route-static-scan.json` 中 hardcoded secret / shell / eval / pickle / SQL 注入模式全部 0 命中，且未改 auth、session、`/auth/menu`、role-route、真实 API 契约；真实 API 再证 admin risk/audit/webhooks 与普通用户共享菜单边界稳定）；测试/可靠性 PASS（focused `App.test.tsx`、admin risk/audit/webhooks 相关页面组合、前端 build、Go tests、compose config、compose rebuild 与真实 API 回放全部通过；现存 `act(...)` / `findDOMNode` / chunk-size warning 继续判定为历史噪声，本轮未恶化）；性能/运维 PASS（纯测试层收口，不新增请求、轮询、数据库负载或部署复杂度，容器重建后服务健康）。下一前端优先项继续清理 `App.test.tsx` 其余跨页集成用例中的 page-global 文本 / metric 重复断言，并在完成这一轮前端收口后再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-09，App webhook admin route 集成断言去脆弱化）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `web/src/App.test.tsx` 做一个小而完整的前端 focused 收口切片，把管理员 `/webhooks` 集成路由用例里仍残留的 page-level 页面内部断言去脆弱化。具体做法是：将 `renders webhook settings page for authenticated admin` 收口为更诚实的 `renders webhook operations workspace for authenticated admin routes`，仅保留两层合同——(1) App 级 bootstrap 后管理员可以落到 `/webhooks` 并挂载 `webhooks-current-endpoints-card`；(2) shared shell header summary 继续通过 role-aware canonical title 命中 `Webhook 运维与回调观测`。同时移除在 App 集成层重复验证 `webhooks-role-guidance`、`webhooks-metrics-strip`、`webhooks-latest-delivery-metric`、固定 endpoint URL 与 `webhooks-send-test-button-11` 等已由 `WebhooksPage.test.tsx` focused 用例单独覆盖的页面内部合同，避免 App 级测试继续绑定到页面细节与重复 metric/card 文本。
- 控制器按 TDD + `systematic-debugging` 先锁定目标：先从 `todo.md` 当前前端优先说明回读“下一前端优先项继续清理 App.test.tsx 中仍残留的 page-global heading / metric 断言”，再读取 `web/src/App.test.tsx`、`web/src/pages/WebhooksPage.test.tsx` 与 `web/src/pages/WebhooksPage.tsx` 确认根因并非运行时代码缺少锚点、也不是 `/auth/menu`、`resolveRouteTitle(...)`、管理员角色权限或真实 Webhook API 漂移，而是 App 集成测试仍在重复证明页面 focused 已覆盖的 guidance/metrics/current-endpoint 细节。随后仅最小修改 `App.test.tsx`：先让旧用例 RED 到新标题/更窄合同，再以 test-only diff 完成 GREEN，不扩散到运行时代码。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx` 通过（1 file / 27 tests）；`pnpm --dir web exec vitest run` 全量前端 suite 首轮仍出现仓库既有 3 条非当前 diff timeout（`AdminUsersPage.shared-console.test.tsx`、`ApiKeysPage.test.tsx`、`SupplierResourcesPage.test.tsx`），控制器按 fail-closed 分别 isolated rerun 证实全部通过，因此如实记为历史 flake/时序噪声而非当前里程碑回归；其余 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`、`docker compose up -d --build web api gateway` 全部通过，且 `web` / `api` / `gateway` healthy。真实 API 方面新增本地忽略回放脚本 `.hermes/local-output/run_app_webhooks_admin_route_real_api.sh`（首次版本因 shell heredoc 传参错误中止，已按 fail-closed 改为控制器串行直跑并保留脚本供后续修正），最新成功产物目录为 `.hermes/local-output/app-webhooks-admin-route-real-api/20260509-001914`：再次验证 `GET /healthz` 返回 `200` 且 JSON `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/menu`、`/auth/me`、`/webhooks/endpoints` 全部 `200`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/webhooks/endpoints` 全部 `200`，并额外断言 admin menu 保持暴露 `/webhooks`；前端 `http://127.0.0.1:5173/` 与 `/webhooks` SPA shell 均返回 `200` 且继续包含 `<div id="root"></div>`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只是把 App 级 `/webhooks` admin 集成用例从“页面内部细节复核”收口为更诚实的“路由工作区可达 + shared shell 标题正确”，没有弱化单一登录后 shared-console、角色菜单真值或真实 Webhook 工作区身份）；代码质量 APPROVED（diff 仅集中修改 `web/src/App.test.tsx` 1 个文件，删去重复 metrics/card/button 细节断言，保留 App 层真正应证明的 mounted workspace + header summary 合同，改动集中且最小；本轮静态扫描摘要已落盘 `.hermes/local-output/app-webhooks-admin-route-review-20260509.md`）；安全/集成 SAFE（controller-side added-line 静态扫描对 hardcoded secret / shell / eval / pickle / SQL 注入模式 0 命中，且未改 auth、session、`/auth/menu`、role-route 或任何后端契约；真实 API 继续证明 admin `/webhooks`、risk、audit、overview 与普通用户 `/webhooks/endpoints` 合同稳定）；测试/可靠性 PASS（focused `App.test.tsx`、build、Go tests、compose config、compose rebuild 与真实 API 回放全部通过；全量前端 suite 的 3 条 timeout 经 isolated rerun 全部通过，判定为历史 flake/时序噪声而非当前回归）；性能/运维 PASS（纯测试层收口，不新增请求、轮询、数据库负载或部署复杂度；构建仍仅保留既有前端 chunk-size warning）。下一前端优先项继续沿同一方向清理 `App.test.tsx` 中其余跨页集成用例的 page-global 文本/metric 重复断言，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-08，ProfilePage fallback shared-console wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `ProfilePage` 做一个小而完整的前端 shared-console 收口切片，把个人资料页 fallback 返回区中仍残留的“推荐工作台”措辞统一收口为与当前单壳叙事一致的“共享工作台”。具体包括：把普通用户在项目市场未暴露时的 fallback 标题 / 描述 / CTA 从 `回到推荐工作台继续主链路` / `返回推荐工作台` 收口为 `回到共享工作台继续主链路` / `返回共享工作台`；同时把 supplier / admin 在设置入口未暴露时的 fallback 标题与描述从“回到推荐工作台继续角色扩展链路”收口为“回到共享工作台继续角色扩展链路”，保持 `fallbackRoute` / `SETTINGS_ROUTE` 行为、角色分流与统一深色共享控制台不变。
- 控制器按 TDD + `systematic-debugging` 先从 `web/src/pages/ProfilePage.test.tsx` 写出 RED：把普通用户、supplier、admin 三条 fallback proof 从旧文案 `推荐工作台` 收紧为 `共享工作台`，并保留点击 fallback CTA 后仍回到 shared home / settings route stub 的导航合同。首次 RED 后回读 `web/src/pages/ProfilePage.tsx`，确认根因不是 `resolvePreferredConsoleRoute(...)`、不是 `/auth/menu`、不是 settings/profile route 漂移，也不是角色权限异常，而是运行时代码中的 user fallback 分支与 supplier/admin fallback 分支仍保留旧的“推荐工作台”标题/描述/按钮表述。随后仅最小修改 `ProfilePage.tsx` 与 `ProfilePage.test.tsx` 两个文件完成 GREEN，未扩散到 profile capability bridge、Webhooks / API Keys / Docs 入口、鉴权或任意后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/App.test.tsx` 通过（2 files / 36 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_profile_shared_console_fallback_real_api.sh`，最新产物目录位于 `.hermes/local-output/profile-shared-console-fallback/20260508-235346`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/menu`、`/auth/me`、`/auth/api-keys` 全部 `200`；seeded supplier `supplier@nexus-mail.local / Supplier123!` 登录后 `GET /api/v1/auth/menu`、`/supplier/resources/overview` 全部 `200`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/risk` 全部 `200`；最新 `summary.json` 继续证明 user/supplier/admin 的菜单路径边界稳定，当前切片未改变任何真实 API 或角色可见性合同。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 仅把 ProfilePage fallback 语言从“推荐工作台”收口为与 single shared-console 更一致的“共享工作台”，没有引入独立后台、额外 workflow 或错误角色页承诺）；代码质量 APPROVED（diff 仅集中修改 `ProfilePage.tsx` 与 `ProfilePage.test.tsx` 两个文件，运行时代码只触及 fallback 文案，测试同步覆盖 user/supplier/admin 三条 fallback 合同，改动集中且最小）；安全/集成 SAFE（controller-side added-line 静态扫描对 hardcoded secret / shell / eval / pickle / SQL 注入模式 0 命中，且未改 auth、session、`/auth/menu`、fallbackRoute 解析或任何权限边界；真实 API 继续证明菜单与角色路由可见性稳定）；测试/可靠性 PASS（focused `ProfilePage + App.test.tsx`、build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；Vitest/Semi 的既有 `act(...)` / `findDOMNode` warning 未因本轮 diff 恶化）；性能/运维 PASS（纯前端 copy/test-contract 收口，不新增请求、轮询、数据库负载或部署复杂度，Vite build 仅保留既有 chunk-size warning）。下一前端优先项继续扫描 shared-console 其它仍残留“推荐工作台”旧说法的 fallback/bridge 页面，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-08，ApiKeys fallback shared-console wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `ApiKeysPage` 做一个小而完整的前端 shared-console 收口切片，把 API Keys 页中仍残留的 fallback / bridge 旧心智“推荐工作台”，统一收口为与仓库当前 shared-console 叙事更一致的“共享工作台”。具体包括：把 bridge 顶部补充说明中的兜底句从“回到推荐工作台继续真实业务主链路”收口为“回到共享工作台继续真实业务主链路”，把 `api-keys-shared-console-fallback` 卡片标题、说明文案与主 CTA 从 `返回推荐工作台` / `先回到推荐工作台...` 全部收口为 `返回共享工作台` / `先回到共享工作台...`，并把当前密钥空态下 fallback 按钮同步改为 `返回共享工作台`。目标是在继续保持单一登录后控制台、角色差异菜单、fallback 按 `/auth/menu` 真值收敛的前提下，让 `ApiKeysPage` 与此前已收口的 AdminUsers 等页面保持一致，不再在同一 shared-console 中混用“推荐工作台 / 共享工作台”两套说法。
- 控制器按 TDD + `systematic-debugging` 先从 `web/src/pages/ApiKeysPage.test.tsx` 写出 RED：把“projects/webhooks/docs 全部未暴露但仍存在首选路由”场景下的 fallback proof 从旧文案 `返回推荐工作台` 收紧为新的 heading / description / CTA 合同，并保留点击后仍回到 shared home 的导航验证。首次 RED 后回读 `web/src/pages/ApiKeysPage.tsx`，确认根因不是 `fallbackRoute` 解析错误、不是 `/auth/menu`、不是 `/api-keys`/`/webhooks`/`/docs` 菜单漂移，也不是共享壳导航异常，而是运行时代码中的 bridge 顶部说明、fallback 卡片和空态 fallback 按钮仍停留在旧的“推荐工作台”表述。随后仅最小修改 `ApiKeysPage.tsx` 与对应 focused 测试，未扩散到 API Key 创建、白名单、审计、权限 gating 或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 通过（1 file / 14 tests）；随后 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/App.test.tsx` 通过（2 files / 41 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。更大范围 `pnpm --dir web exec vitest run` 首轮暴露的是仓库既有 `App.test.tsx > keeps shared integration routes reachable after register bootstrap`、`AdminUsersPage.shared-console.test.tsx > submits wallet adjustment, settlement and dispute resolution flows` 与 `SupplierResourcesPage.test.tsx > submits supplier domain, account, and mailbox actions then reloads overview data` 三条非当前 diff 用例的单次失败/timeout；控制器按 fail-closed 分别做 isolated rerun 全部通过，因此如实记为历史 suite flake/异步噪声而非本里程碑回归。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_api_keys_shared_console_fallback_real_api.sh`，最新产物目录 `.hermes/local-output/api-keys-shared-console-fallback-real-api/20260508-232713`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/me`、`/auth/menu`、`/auth/api-keys` 全部 `200` 且 `GET /api/v1/admin/overview` 继续 `403`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu` 与 `GET /api/v1/admin/overview` 全部 `200`，且 real-API 产物中的 menu path 断言继续证明普通用户未暴露 admin path，而管理员仍保有 `/admin/users`、`/admin/risk`、`/admin/audit` 等路径；前端 `http://127.0.0.1:5173/api-keys` shell 返回 `200` 且保留 `<div id="root"></div>`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 仅把 ApiKeys fallback / bridge 语言从“推荐工作台”收口为与 single shared-console 更一致的“共享工作台”，没有引入独立后台或额外 workflow 承诺）；代码质量 APPROVED（diff 仅集中修改 `ApiKeysPage.tsx` 与 `ApiKeysPage.test.tsx` 两个文件，运行时代码只触及 bridge/fallback/empty-state copy，测试同步覆盖新文案与 click-through 合同，改动集中且最小；delegated 代码质量 reviewer 漂移到错误仓库 `/root/.hermes/hermes-agent`，已按 fail-closed 丢弃并改由控制器基于 live diff 完成最终复核）；安全/集成 SAFE（controller-side added-line 静态扫描对 hardcoded secret / shell / eval / pickle / SQL 注入模式 0 命中，且未改 auth、session、`/auth/menu`、role-route 或任意后端契约；真实 API 继续证明普通用户 admin 概览仍为 `403`，`/auth/menu` 对 user/admin 的路径边界保持稳定；delegated 安全集成 reviewer 同样漂移到错误仓库，已按 fail-closed 丢弃）；测试/可靠性 PASS（focused、`ApiKeysPage + App.test.tsx` 组合、build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；全量 Vitest 首轮失败经 isolated rerun 证实为仓库既有 flake/异步噪声，非本里程碑回归）；性能/运维 PASS（纯前端 copy/test-contract 收口，不新增请求、轮询、数据库负载或部署复杂度；Vite build 仍仅提示既有 chunk-size warning，本轮未恶化）。下一前端优先项继续扫描 shared-console 各页 fallback / bridge / role-aware wording 中仍残留的低信号旧说法，尤其是其它仍保留“推荐工作台”表述的 fallback 页面；在此前提下再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-08，AdminUsers fallback shared-console wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `AdminUsersPage` 做一个小而完整的前端 shared-console 收口切片，把管理员资金页 fallback 从仍偏旧心智的“推荐工作台”措辞，收口为更贴近当前单壳共享控制台叙事的“共享工作台”。具体包括：把 fallback 标题从 `回到推荐工作台继续管理员主链路` 收口为 `回到共享工作台继续管理员主链路`，把说明文案从泛化“当前菜单未暴露...”收口为更贴近 menu-truth 合同的 `当风控、审计与共享接入入口暂未由服务端暴露时，先回到共享工作台完成当前管理员主链路，再等待后续菜单授权。`，并把 CTA 从 `返回推荐工作台` 同步收口为 `返回共享工作台`。目标是在继续保持单一登录后控制台、角色差异菜单、fallback 按 `/auth/menu` 真值收敛的前提下，让管理员资金页的 fallback 语言与仓库其余 new-api/shared-console 页面更一致，不再混用“推荐工作台 / 共享工作台”两套说法。
- 控制器按 TDD + `systematic-debugging` 先从 `web/src/pages/AdminUsersPage.shared-console.test.tsx` 写出 RED：把“only finance page remains”场景下的 fallback proof 从旧文案 `返回推荐工作台` 收紧为新的 heading / description / CTA 合同，并保留点击后仍回到 shared home 的导航验证。首次 RED 后回读 `web/src/pages/AdminUsersPage.tsx`，确认根因不是 `fallbackRoute` 解析错误、不是 `/auth/menu`、不是 admin route 缺失、也不是共享壳导航异常，而是运行时代码中的 fallback 标题、说明文案和按钮文案仍停留在旧的“推荐工作台”表述。随后仅最小修改 `AdminUsersPage.tsx` 与对应 focused 测试，未扩散到调账、结算、争议处理、风控/审计入口或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx` 通过（1 file / 5 tests）；随后 `pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx src/App.test.tsx` 通过（2 files / 32 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_admin_users_fallback_real_api.sh`，最新产物目录 `.hermes/local-output/admin-users-fallback-real-api/20260508-230458`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `GET /api/v1/auth/me`、`/auth/menu`、`/dashboard/overview` 全部 `200` 且 `GET /api/v1/admin/overview` 继续 `403`；seeded admin `admin@nexus-mail.local / Admin123!` 登录后 `GET /api/v1/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；控制器进一步对 real-API 产物中的 `user_menu.json` / `admin_menu.json` 做了 menu path 断言，确认普通用户未暴露 admin path，而管理员仍保有 `/admin/risk`、`/admin/audit` 等路径。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 仅把 AdminUsers fallback 语言从“推荐工作台”收口为与 single shared-console 更一致的“共享工作台”，没有引入独立后台或额外 workflow 承诺）；代码质量 APPROVED（diff 仅集中修改 `AdminUsersPage.tsx` 与 `AdminUsersPage.shared-console.test.tsx` 两个文件，运行时代码只触及 fallback heading/description/button，测试同步覆盖新文案与 click-through 合同，改动集中且最小）；安全/集成 SAFE（controller-side added-line 静态扫描对 hardcoded secret / shell / eval / pickle / SQL 注入模式 0 命中，且未改 auth、session、`/auth/menu`、role-route 或任意后端契约；真实 API 继续证明普通用户 admin 概览仍为 `403`）；测试/可靠性 PASS（focused、`AdminUsersPage + App.test.tsx` 组合、build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；全量 `pnpm exec vitest run` 首轮暴露的是仓库既有的 `App.test.tsx` / `ApiKeysPage.test.tsx` 隔离即过噪声与 `SupplierResourcesPage.test.tsx` 历史 timeout flake，控制器已按 fail-closed 用 isolated rerun 证明非当前切片回归，因此如实记录为历史 suite 噪声而非本里程碑 blocker）；性能/运维 PASS（纯前端 copy/test-contract 收口，不新增请求、轮询、数据库负载或部署复杂度；本轮五维评审明细已落盘 `.hermes/local-output/admin-users-fallback-review-20260508.md`）。下一前端优先项继续扫描 shared-console 各页 fallback / bridge / role-aware wording 中仍残留的低信号旧说法，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-08，App 首轮引导 scoped onboarding 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `App.test.tsx` 做一个小而完整的前端 focused 收口切片，把普通用户注册后首轮引导与 dismiss 后状态中仍残留的低信号 copy 断言收紧为更贴近运行时结构的 scoped onboarding 合同。具体包括：把 `dashboard-next-steps-lane` 中对 `供应商主任务` / `管理员主任务` 的文案缺失检查，收紧为普通用户 onboarding lane 内三张稳定 step card `dashboard-next-step-projects`、`dashboard-next-step-orders`、`dashboard-next-step-api-keys` 的存在合同；并把 dismiss 后状态从 `欢迎进入共享控制台` heading 消失，收紧为 `dashboard-first-run-dismiss` 控件消失且 `dashboard-next-steps-lane` 仍存在的行为合同，继续保持注册后统一进入单一共享控制台、按角色扩展菜单、不拆独立后台的 new-api 方向。
- 控制器先按 TDD + `systematic-debugging` 推进：先通过 repo 内 focused 读取与已有 `DashboardPage.tsx` runtime anchors 确认根因并非运行时代码缺失，而是 `App.test.tsx` 仍有三处 brittle proof——两处以 page copy `欢迎进入共享控制台` 作为 dismiss 后状态代理，一处以 role 文案缺失代理普通用户 onboarding 合同。随后只做最小 test-only diff：把注册后用例收紧为 onboarding lane 内 3 张 step card 的存在断言，并保留 `打开 API Keys` 的用户可见 CTA 合同；再把两处 dismiss 后状态统一改为等待 `dashboard-first-run-dismiss` 消失后断言 `dashboard-next-steps-lane` 仍在。五维代码质量复审进一步建议把正向存在断言从 `queryByTestId(...)` 收紧为 `getByTestId(...)`，控制器已同轮完成收口。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx` 通过；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/DashboardPage.test.tsx` 通过（2 files / 35 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_app_onboarding_scope_real_api.sh`，最新产物目录 `.hermes/local-output/app-onboarding-scope-real-api/20260508-222904`：再次验证 `GET /healthz` 返回 `200` 且 JSON `status=ok` / `redis=ok`；普通用户动态注册 `POST /api/v1/auth/register` 返回 `201`，随后 `GET /api/v1/auth/menu`、`/auth/me`、`/dashboard/overview` 全部 `200` 且菜单继续包含 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/settings`；supplier seeded 登录 `supplier@nexus-mail.local / Supplier123!` 后 `GET /api/v1/auth/menu` 与 `GET /api/v1/supplier/resources/overview` 继续 `200` 且菜单仍含 `/docs`；前端 `http://127.0.0.1:5173/` 与 `/docs` shell 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 仅把普通用户 onboarding / dismiss proof 从低信号文案收紧为 step-card 与 dismiss-control 合同，未改变单一登录后 shared-console、角色菜单真值或注册后统一壳叙事）；代码质量 APPROVED（diff 仅集中修改 `web/src/App.test.tsx`，并已按评审建议把正向存在断言收紧为 `getByTestId(...)`，无新增 runtime hook）；安全/集成 SAFE（controller-side added-line 静态扫描对 hardcoded secret / shell / eval / pickle / SQL 注入模式 0 命中，且未改 auth/bootstrap、`/auth/menu`、role-route 或任何运行时代码）；测试/可靠性 PASS（focused `App.test.tsx`、`App + DashboardPage` 组合、build、Go tests、compose config 与真实 API 回放全部通过；更大范围 `pnpm --dir web exec vitest run` 仍出现与当前 diff 无关的既有异步/超时噪声，控制器已用 targeted rerun 证明并非本里程碑回归，因此按 fail-closed 记为历史噪声而非当前 blocker）；性能/运维 PASS（仅为测试层收口，不新增请求、轮询、持久化、容器或部署复杂度）。下一前端优先项继续扫描 `App.test.tsx` / shared-console focused 用例中剩余的低信号 role-boundary 文本断言，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-08，ApiKeysPage role-aware Webhooks bridge 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `ApiKeysPage` 做一个小而完整的前端 shared-console 收口切片，把页内共享接入桥标题从泛化的 `API Keys → Webhook → 文档` 收口为 role-aware canonical route identity：user=`API Keys → 开发者 Webhook 接入工作台 → API 文档与接入控制台`、supplier=`API Keys → 供给事件回调工作台 → API 文档与接入控制台`、admin=`API Keys → Webhook 运维与回调观测 → API 文档与接入控制台`。目标是在继续保持单一登录后控制台、角色差异菜单和统一深色壳不变的前提下，让 API Keys 页 bridge 与仓库其余 shared-console 页面一样，直接表达角色真实目标页身份，而不是继续停留在低信号泛化 `Webhook` 文案。
- 控制器按 TDD + `systematic-debugging` 先在 `web/src/pages/ApiKeysPage.test.tsx` 写出 RED：新增 supplier/admin focused 合同，显式要求 `api-keys-shared-console-bridge` 容器分别命中 `供给事件回调工作台` / `Webhook 运维与回调观测`，并要求点击 `继续配置 Webhook` 后 route-stub heading 继续走 `resolveRouteTitle(WEBHOOKS_ROUTE, role)` 真值；同时把既有普通用户 bridge / navigation proof 收口到同一 source of truth。首次 RED 后回读 `web/src/pages/ApiKeysPage.tsx`，确认根因不是 `/auth/menu`、`/auth/api-keys`、`navigate(...)`、路由常量或真实 API 漂移，而是 bridge headline 仍硬编码为泛化 `API Keys → Webhook → 文档`。随后仅最小修改运行时代码：引入 `resolveRouteTitle(...)` 并以 `webhookRouteTitle` 替换这一处 bridge 标题中间段，同时把相关 tests 的 route-stub heading 与普通用户 proof 一并收口到同一真值，没有扩散到 key 创建/白名单/审计/后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 通过（1 file / 14 tests）；随后 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/App.test.tsx` 通过（2 files / 41 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_api_keys_role_aware_webhooks_real_api.sh`，最新产物目录 `.hermes/local-output/api-keys-role-aware-webhooks-real-api/20260508-215634`：再次验证 `GET /healthz` 返回 `200` 且 JSON `status=ok` / `redis=ok`；seeded admin `admin@nexus-mail.local / Admin123!` 与 supplier `supplier@nexus-mail.local / Supplier123!` 登录均 `200`；`GET /api/v1/auth/menu` 对 admin/supplier 均继续暴露 `/api-keys`、`/webhooks`、`/docs`；`GET /api/v1/auth/api-keys` 对 admin/supplier 分别返回 `200`；前端 `http://127.0.0.1:5173/` 与 `/api-keys` shell 也全部返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（ApiKeysPage bridge 现已按角色显式表达真实 Webhooks 目标页身份，继续保持同一登录后控制台与角色差异菜单叙事，未引入独立后台）；代码质量 APPROVED（diff 仅集中修改 `ApiKeysPage.tsx` 与 `ApiKeysPage.test.tsx` 两个文件，运行时代码复用 `resolveRouteTitle(...)` 作为 source of truth，测试补齐 supplier/admin focused bridge + navigation proof，改动集中且最小）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，且未改鉴权、session、`/auth/menu` 真值或任何权限边界；真实 API 继续证明 `/auth/menu` 与 `/auth/api-keys` 合同稳定）；测试/可靠性 PASS（focused、`ApiKeysPage + App` 组合、前端 build、Go tests、compose config 与真实 API 回放全部通过；后续额外 broader/full Vitest rerun 仍暴露若干与当前 diff 无关的历史异步/超时噪声，已按 fail-closed 作为非本里程碑 blocker 记录而未误归因到当前切片）；性能/运维 PASS（仅新增一次轻量标题解析，不引入新请求、轮询、持久化、容器或部署复杂度）。另外，本轮 delegated 五维评审再次漂移到错误仓库 `/root/.hermes/hermes-agent`，已按 fail-closed 规则整批丢弃，并改由控制器基于目标仓库 live diff、验证结果与真实 API 回放完成最终评审归档到 `.hermes/local-output/api-keys-role-aware-review-20260508.md`。下一前端优先项继续扫描 shared-console 其余页面中仍残留的 role-aware bridge / capability wording 漂移与 focused 用例脆弱点，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-08，ProfilePage role-aware Webhooks bridge 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `ProfilePage` 做一个小而完整的 shared-console 收口切片，把个人资料页 bridge 中仍硬编码为普通用户身份的 Webhooks 卡片标题，收口为按角色感知的 canonical page identity：user=`开发者 Webhook 接入工作台`、supplier=`供给事件回调工作台`、admin=`Webhook 运维与回调观测`。目标是在保持同一登录后控制台、角色差异菜单和统一深色壳不变的前提下，补齐 profile -> webhooks 这条共享接入桥在 supplier/admin 视角下的真实页面身份，不再把所有角色都误导为普通用户 Webhooks 工作台。
- 控制器先按 TDD + `systematic-debugging` 推进：先在 `web/src/pages/ProfilePage.test.tsx` 写出 RED，新增 supplier/admin 两条 focused 合同，显式要求 `profile-capability-bridge` 中的 Webhooks heading 跟随 `resolveRouteTitle(WEBHOOKS_ROUTE, role)` 命中 `供给事件回调工作台` / `Webhook 运维与回调观测`，并在点击 `继续配置 Webhook` 后继续用 role-aware route-stub heading 验证导航结果；首次 RED 后回读 `web/src/pages/ProfilePage.tsx`，确认根因不是 `/auth/menu`、`navigate(...)`、路由常量、真实 API 或角色权限漂移，而是 bridge schema 仍把 `webhooks` 卡片标题硬编码为 `开发者 Webhook 接入工作台`。随后仅最小修改运行时代码：引入 `resolveRouteTitle` 并以 `webhooksTitle` 替换 bridge 中这一处硬编码标题，同时把测试 route-stub heading 也收口到同一 source of truth，未扩散到 fallback、settings 回跳、主身份卡或任何后端逻辑。
- 控制器验证结果：`cd web && pnpm exec vitest run src/pages/ProfilePage.test.tsx` 通过（1 file / 9 tests）；随后 `cd web && pnpm exec vitest run src/pages/ProfilePage.test.tsx src/pages/WebhooksPage.test.tsx` 通过（2 files / 21 tests）；`cd web && pnpm exec vitest run src/pages/ProfilePage.test.tsx src/App.test.tsx` 通过（2 files / 36 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_profile_role_aware_webhooks_real_api.sh`，最新产物目录 `.hermes/local-output/profile-role-aware-webhooks-real-api/20260508-212543`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册 `POST /api/v1/auth/register` 返回 `201`，随后 user/supplier/admin 的 `GET /api/v1/auth/menu` 分别继续暴露 `/profile`、`/api-keys`、`/webhooks`、`/docs` 等共享壳路径；supplier/admin seeded 登录均 `200`；前端 `/profile`、`/webhooks`、`/api-keys`、`/docs` SPA shell 全部返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（ProfilePage bridge 现已按角色暴露正确 Webhooks 页面身份，继续保持单一登录后控制台与角色差异菜单叙事，未引入独立后台）；代码质量 APPROVED（diff 仅集中修改 `ProfilePage.tsx` 与 `ProfilePage.test.tsx` 两个文件，运行时代码复用既有 `resolveRouteTitle(...)` 作为 source of truth，测试补齐 supplier/admin focused 导航 proof，改动集中且最小）；安全/集成 SAFE（controller-side added-line 静态扫描对 hardcoded secret / shell / eval / pickle / SQL 注入模式 0 命中，且未改鉴权、session、`/auth/menu` 真值或任何权限边界）；测试/可靠性 PASS（focused、相关页组合、`App.test.tsx` 组合、前端 build、Go tests、compose config 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（仅新增一次轻量标题解析，不引入新请求、轮询、持久化、容器或部署复杂度）。另外，本轮 delegated 五维评审再次漂移到错误仓库 `/root/.hermes/hermes-agent`，已按 fail-closed 规则整批丢弃，并改由控制器基于目标仓库 live diff、验证结果与真实 API 回放完成最终评审归档到 `.hermes/local-output/profile-role-aware-webhooks-review-20260508.md`。下一前端优先项继续扫描其余 shared-console 页面 bridge / capability matrix / partial-menu 场景中仍残留的 role-aware identity 漂移与低信号文案，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-08，AdminProjects shared-console bridge canonical identity 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `AdminProjectsPage` 做一个小而完整的前端 shared-console 切片，把管理员价格策略页从“任务流只暴露 API Keys，Webhook / Docs 仍停留在旧 route-stub / capability row 表述”收口为更贴近单壳真实页面身份的共享接入桥。具体做法是：新增 `admin-pricing-shared-console-bridge`，在同一登录后控制台内直接暴露 `开发者 API 接入工作台`、`Webhook 运维与回调观测`、`API 文档与接入控制台` 三张 bridge CTA 卡片，并把 capability matrix 的接入入口行同步切到 canonical route title，继续强化“管理员价格策略 / 风控 / 审计 / 接入联动仍在同一深色 shared-console 内完成，而不是拆出冗余独立后台”的 new-api 方向。
- 控制器先按 TDD + `systematic-debugging` 收敛测试与实现：先在 `web/src/pages/AdminProjectsPage.test.tsx` 写出 RED，把 `/webhooks` 与 `/docs` route-stub heading 从旧的 `Webhook 设置` / `API 文档` 收紧为 `resolveRouteTitle(...)` 驱动的 admin canonical heading，并新增 bridge scoped 合同，显式要求 `admin-pricing-shared-console-bridge` 命中三组 canonical title 与 `打开 API Keys` / `打开 Webhooks` / `查看 API 文档` CTA，再补上从 bridge 点击进入 `/webhooks`、`/docs` 的导航 proof；首次 RED 后回读 `web/src/pages/AdminProjectsPage.tsx`，确认根因不是 `/auth/menu`、管理员权限、真实 API 漂移或路由缺失，而是页面运行时仍只有 mission-flow 中的单个 API Keys 集成入口，且 capability matrix 仍保留泛化 `API Keys / Webhooks / Docs` 文案。随后仅最小修改 `AdminProjectsPage.tsx`：复用 `resolveRouteTitle(...)` 收口 capability row，并新增受 `hasMenuPath(...)` gating 的 `bridgeCards` + `admin-pricing-shared-console-bridge` 卡片区；五维代码质量复审指出本轮初版存在重复 `BridgeCard` 类型与两处过度精确的长句测试断言，控制器已同轮收敛为复用 `MissionCard` 形状、并把相关 focused proof 放宽到容器级 `toHaveTextContent(...)`，避免不必要的 copy 脆弱性，同时补上 capability row 文案中的可读性空格。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx` 通过（1 file / 4 tests）；随后 `pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx src/App.test.tsx` 通过（2 files / 31 tests）；`pnpm --dir web exec vitest run` 全量前端测试通过（26 files / 207 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面复用并执行本地忽略脚本 `.hermes/local-output/admin_pricing_real_api.sh`：再次验证 `POST /api/v1/auth/login` 200、`GET /api/v1/auth/menu` 200、`GET /api/v1/admin/projects` 200、`GET /api/v1/admin/projects/offerings` 200、`PATCH /api/v1/admin/projects/:id` 200 且 restore 200、`GET /api/v1/admin/risk` 200、`GET /api/v1/admin/audit?limit=5` 200、`GET /api/v1/auth/api-keys` 200、`GET /api/v1/webhooks/endpoints` 200、`GET /docs` 200；前端 `http://127.0.0.1:5173/admin/pricing`、`/webhooks`、`/docs` shell 也全部返回 200。
- 本轮五维评审结论：产品/规格 PASS（AdminProjects 现已补齐共享接入桥与 canonical page identity，对管理员 `/webhooks` 明确使用 `Webhook 运维与回调观测`，未引入独立后台或多壳叙事）；代码质量 APPROVED（初版重复类型与过度精确 copy 断言已同轮收敛，当前 diff 仅集中修改 `AdminProjectsPage.tsx` 与 `AdminProjectsPage.test.tsx` 两个文件，运行时代码复用 `resolveRouteTitle(...)` 作为标题真值，测试保留 bridge/导航/矩阵核心合同且不过度依赖整句文案）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，且新增 bridge CTA 仍完全受 `hasMenuPath(menu, path)` gating 控制，未改鉴权、会话、权限边界或任何 Phase 4/5 后端契约）；测试/可靠性 PASS（focused Vitest、App 组合、全量 web Vitest、build、Go tests、compose config 与真实 API 回放全部通过；review 仅留下非阻塞建议：后续可补一个部分 menu 可见性的 bridge focused 用例）；性能/运维 PASS（本轮只新增一组轻量 memoized bridge schema 与固定数量 CTA 卡片，不引入新请求、轮询、持久化或部署复杂度变化，现有容器健康状态维持稳定）。下一前端优先项继续扫描其余 shared-console 页面中仍残留的低信号 bridge / capability matrix / partial-menu 合同缺口，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-08，SupplierSettlements shared-console bridge canonical identity 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `SupplierSettlementsPage` 做一个小而完整的前端 shared-console 切片，把供应商结算页右侧 bridge 从仍残留 path-label 心智的 `API Keys · /api-keys`、`供给事件回调工作台 · /webhooks`、`API 文档与接入控制台 · /docs`，继续收口为更贴近单壳真实页面身份的 canonical 卡片标题：`开发者 API 接入工作台`、`供给事件回调工作台`、`API 文档与接入控制台`。目标是让供应商资金/争议中枢里的共享接入桥接不再一边使用 canonical CTA、一边仍把卡片标题写成路径标签，而是与仓库其余 shared-console 页面统一表达“同一登录后控制台中的真实目标页身份”。
- 控制器按 TDD + `systematic-debugging` 先从 `web/src/pages/SupplierSettlementsPage.test.tsx` 写出 RED：在首个 mission-control shell 用例中新增 bridge scoped 合同，显式要求 `supplier-settlements-shared-console-bridge` 命中三组 canonical title，并否定旧 path-label 文本；首次失败后回读 `web/src/pages/SupplierSettlementsPage.tsx`，确认根因不是 `/auth/menu` gating、路由常量、供应商权限或真实 API 漂移，而是 `consolePillars` 仍沿用旧的 `label + path` 文案。随后仅最小修改 `SupplierSettlementsPage.tsx` 中 3 个 `consolePillars` 标题，并同步补强 focused 测试为 bridge 容器 `toHaveTextContent(...)` + 旧 path-label 消失 proof，没有扩散到资金/争议表单、mission-flow、fallback、路由动作或任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx` 通过（1 file / 8 tests）；随后 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx src/App.test.tsx` 通过（2 files / 35 tests）；`pnpm --dir web exec vitest run` 全量前端测试通过（26 files / 207 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 后 `docker compose ps web api gateway` 显示 `web` / `api` / `gateway` healthy，`GET /healthz` 返回 `200 {"redis":"ok","status":"ok"}`。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_supplier_settlements_canonical_cta_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-settlements-canonical-cta/20260508-202105`：再次验证普通用户动态注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200` 且访问 `/api/v1/supplier/settlements` 正确 `403`；supplier seeded 登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements` 全部 `200`，且菜单继续包含 `/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs`；admin seeded 登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 也全部 `200`；前端 `/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs` shell 全部返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（SupplierSettlements bridge 标题已与 shared-console canonical page identity 对齐，不再把路径标签冒充真实页面身份，也未引入独立供应商后台）；代码质量 APPROVED（diff 仅集中修改 `SupplierSettlementsPage.tsx` 与 `SupplierSettlementsPage.test.tsx` 两个文件，运行时代码只触及 3 个静态标题，测试补齐 scoped canonical-title 与旧 path-label 消失合同；代码质量评审仅提出非阻塞建议：未来若此模式继续扩展，可考虑复用统一 route-title source of truth 进一步降低硬编码漂移风险）；安全/集成 SAFE（controller-side added-line 静态扫描对 hardcoded secret / shell / eval / pickle / SQL 注入模式均 0 命中，且未改 `/auth/menu`、权限边界、导航动作或任何 Phase 4/5 后端契约；security/integration 评审确认 current diff 只是前端标题收口，不改变鉴权/菜单/API 合同）；测试/可靠性 PASS（focused Vitest、App 组合、全量 web Vitest、build、Go tests、compose config、容器重建与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` 噪声仍属历史非阻塞 warning，本轮未恶化）；性能/运维 PASS（仅为现有 bridge 卡片静态标题收口与最小 focused proof 增补，不新增请求、轮询、持久化或部署负担）。下一前端优先项继续扫描其余 shared-console 页面中仍残留的低信号 path-label / bridge / capability-card 标题漂移，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-08，ApiDocs capability matrix canonical identity 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `ApiDocsPage` 做一个小而完整的前端 shared-console 切片，把 `控制台能力矩阵` 从仍沿用旧菜单短标签/低信号摘要（如 `API Keys`、`Webhook 设置`、`最小权限 API Key`、`真实回调验证`）继续收口为更贴近单壳真实页面身份的 canonical 能力面板：`API 文档与接入控制台`、`开发者 API 接入工作台`，以及按角色区分的 Webhook identity（user=`开发者 Webhook 接入工作台`、admin=`Webhook 运维与回调观测`）。目标是让 docs 页不仅 bridge/loop 卡片保持 canonical wording，连右侧 capability matrix 也直接表达同一登录后控制台中的真实目标页身份，而不是退回旧菜单短标签心智。
- 控制器先按 TDD + `systematic-debugging` 收敛测试与实现：先在 `web/src/pages/ApiDocsPage.test.tsx` 写出 RED，新增 regular-user 与 admin 两组 scoped 合同，显式要求 `控制台能力矩阵` 区域命中 canonical heading / role-aware webhook title，并否定旧 wording `API Keys`、`Webhook 设置`、`最小权限 API Key`、`真实回调验证`；随后回读 `web/src/pages/ApiDocsPage.tsx`，确认根因不是 `/docs` iframe、`/auth/menu` gating、bridge/loop schema、后端接口或角色权限漂移，而是 `surfaceItems(...)` 仍停留在旧菜单短标签层。实现上仅最小修改 `surfaceItems(...)` 的静态文案映射，并补一个最小语义锚点 `docs-capability-matrix` 取代测试里的 `.closest('.semi-card')` 结构依赖，使 focused proof 更贴近运行时区域合同。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 通过（1 file / 5 tests）；随后 `pnpm --dir web exec vitest run` 全量前端测试通过（26 files / 207 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_apidocs_capability_matrix_real_api.sh`，最新产物目录 `.hermes/local-output/apidocs-capability-matrix-real-api/20260508-194909`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；普通用户动态注册后 `/auth/menu`、`/auth/me`、`/auth/api-keys`、`/webhooks/endpoints` 均 `200` 且菜单继续包含 `/api-keys`、`/webhooks`、`/docs`；管理员 seeded 登录后 `/auth/menu`、`/admin/risk`、`/admin/audit` 均 `200` 且菜单继续包含 `/api-keys`、`/webhooks`、`/docs`、`/admin/risk`；前端 `/docs`、`/api-keys`、`/webhooks` SPA shell 全部返回 `200` 且保留 `<div id="root"></div>`。
- 本轮五维评审结论：产品/规格 PASS（capability matrix 已与 docs 页 canonical page identity 对齐，未引入独立后台或多壳叙事）；代码质量 APPROVED（运行时代码只收口 `surfaceItems(...)` 文案并新增 1 个最小语义锚点，测试改为区域级 scoped proof，避免继续依赖 `.closest('.semi-card')`）；安全/集成 SAFE（未改 `/auth/menu`、路由常量、角色边界或任何后端契约；controller-side added-line 静态扫描对 hardcoded secret / shell / eval / pickle / SQL 注入模式均 0 命中）；测试/可靠性 PASS（focused Vitest、全量前端 Vitest、build、Go tests、compose config、真实 API 回放全部通过；既有 React/Semi warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（仅为现有页面静态能力文案映射与最小语义锚点调整，不新增请求、轮询、持久化或部署负担）。另外，本轮一组 delegated 五维评审误漂移到错误仓库 `/root/.hermes/hermes-agent`，已按 fail-closed 规则整批丢弃，并改由控制器基于 live diff 与实测结果完成最终评审归档到 `.hermes/local-output/apidocs-capability-matrix-review-20260508.md`。下一前端优先项继续扫描 shared-console 其余 focused 用例和页面能力矩阵/bridge/fallback 中残留的低信号 copy 与结构耦合点，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-08，AdminUsers / AdminSuppliers shared-console bridge canonical identity 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `AdminUsersPage` 与 `AdminSuppliersPage` 做一个小而完整的前端共享控制台切片，把两页右侧 shared-console bridge 从“仅按钮跳转 + 旧菜单短标签心智”继续收口为更贴近 canonical route identity 的桥接卡片。具体做法是：保留现有 menu gating、fallback 与 `navigate(...)` 行为不变，但让 bridge surface 直接展示 `resolveRouteTitle(...)` 驱动的真实页面身份，并同步把 CTA 从泛化按钮收敛为 `打开 开发者 API 接入工作台`、`打开 Webhook 运维与回调观测`、`打开 API 文档与接入控制台` 这类 role-aware canonical 文案，进一步强化“管理员运营工作区仍在同一登录后单壳内完成风险/审计/供应商运营与接入联动，而不是拆出独立后台”的 new-api 方向。
- 控制器先按 TDD + `systematic-debugging` 收敛测试与实现：先把 `web/src/pages/AdminUsersPage.shared-console.test.tsx` 与 `web/src/pages/AdminSuppliersPage.test.tsx` 写成 RED，显式要求 bridge 区同时命中 canonical title 与 role-aware CTA，并要求 admin `/webhooks` route-stub heading 走 `resolveRouteTitle(WEBHOOKS_ROUTE, 'admin')` 真值；随后回读 `AdminUsersPage.tsx` / `AdminSuppliersPage.tsx` 确认根因不是 `/auth/menu`、后端权限、路由常量或真实 API 失败，而是 bridge 仍停留在旧的 menu-label/按钮层，未直接暴露 canonical 页面 identity。实现上仅最小修改 4 个前端文件：两页都把 shared bridge 数据结构收口为 `title + button + path` 的 role-aware schema，并在 bridge 卡片内展示 canonical heading + CTA；测试侧同步用 `resolveRouteTitle(...)` 作为 source of truth。其间五维代码质量 / 安全集成复审指出 `AdminSuppliersPage.test.tsx` 的 admin webhook route-stub 仍硬编码标题，控制器已同轮改为同样复用 `resolveRouteTitle(...)`，避免测试与 canonical route catalog 再次漂移。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx src/pages/AdminSuppliersPage.test.tsx src/App.test.tsx` 通过（3 files / 39 tests）；随后 `pnpm --dir web exec vitest run` 全量前端测试通过（26 files / 207 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面复用并执行本地忽略脚本 `.hermes/local-output/run_admin_users_shared_console_real_api.sh` 与 `.hermes/local-output/run_admin_suppliers_shared_console_links_real_api.sh`：前者最新产物目录 `.hermes/local-output/admin-users-shared-console-real-api-20260508-191816` 再次验证 `GET /healthz` 200 且 `status=ok`/`redis=ok`、普通用户注册后 `/auth/menu` `/auth/me` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 全部 200 且访问 `/api/v1/admin/overview` 正确 403、管理员登录后 `/auth/menu` `/admin/overview` `/admin/risk` `/admin/audit?limit=5` 全部 200；后者最新产物目录记录在 `.hermes/local-output/admin-suppliers-shared-console-links/latest.txt`，再次验证普通用户菜单不泄露 admin/supplier 路径、管理员 `/auth/menu` 继续暴露 `/admin/suppliers` `/admin/users` `/admin/risk` `/admin/audit` `/api-keys` `/webhooks` `/docs`，且 `/` `/admin/suppliers` `/api-keys` `/webhooks` `/docs` SPA shell 全部返回 200。`docker compose ps --format json` 同时确认 `web` / `api` / `gateway` 以及相关依赖容器均 healthy。
- 本轮五维评审结论：产品/规格 PASS（bridge 表面现已直接表达 canonical page identity，进一步强化 single shared-console / role-aware workspace 叙事，未引入独立后台或页面身份误导）；代码质量 APPROVED（实现复用 `resolveRouteTitle(...)` 作为 source of truth，测试也同步复用；剩余仅是少量非阻塞重复求值/既有 warning 噪声，不影响当前切片）；安全/集成 SAFE（新增 bridge 卡片仍完全受 `hasMenuPath(menu, path)` gating 控制，未放宽 `/auth/menu` 真值、角色边界或任何 Phase 4/5 后端契约；控制器静态扫描当前 4 个变更文件对 hardcoded secret / shell / eval / pickle / SQL 注入模式 0 命中）；测试/可靠性 PASS（focused Vitest、`App.test.tsx` 组合、全量 web Vitest、build、Go tests、compose config 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` 警告仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅新增固定数量 bridge 卡片与轻量标题解析，不引入新请求、轮询、持久化、容器或部署复杂度变化，容器健康状态保持稳定）。下一前端优先项继续扫描 shared-console remaining bridge / fallback / route-stub 中仍残留的低信号 copy 与结构脆弱点，再在此前提下回到 Phase 4/5 余下 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计切片。

### 最新执行检查点（2026-05-08，Login 注册前端最小校验与 8 位密码边界收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `LoginPage` 做一个小而完整的前端垂直切片，把注册表单中仍完全依赖后端返回的两类最小输入错误前移到提交前校验：邮箱缺少 `@` 时直接提示 `请输入有效邮箱`，密码少于 8 位时直接提示 `密码长度至少为 8 位`；同时保持既有“确认密码不一致”前端拦截、`register(email, password)` 调用形状、注册成功后进入同一 shared-console 壳，以及 new-api 风格深色统一登录入口不变。
- 控制器按 TDD 先在 `web/src/pages/LoginPage.test.tsx` 写出 RED：新增两条 focused 负向用例，显式要求非法邮箱与短密码场景都不得调用 `register`；首次失败后按 `systematic-debugging` 回读 `web/src/pages/LoginPage.tsx` 与后端 `internal/modules/auth/service.go`/`http.go`，确认根因并非 `/auth/register` 契约缺失、也不是后端错误文案漂移，而是前端注册态在提交前仅校验了确认密码一致性，尚未镜像后端已存在的最小邮箱/密码规则。随后仅最小修改 `LoginPage.tsx` 的注册分支早返回校验完成 GREEN。首轮五维测试/可靠性复审进一步指出“至少 8 位”合同还缺少边界保护，因此控制器同轮再补一条“恰好 8 位密码可成功注册并进入 shared home”的 focused 用例，避免实现误写成 `<= 8` 时仍被放过；运行时代码无需再改。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（1 file / 8 tests）；随后 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（2 files / 35 tests）；`pnpm --dir web exec vitest run` 全量前端测试最终通过（26 files / 205 tests）；期间首轮全量回归一度在既有 `App.test.tsx` 的 dashboard onboarding dismiss 用例上出现单次失败，控制器按 fail-closed 先隔离重跑该用例通过，再完整重跑 `src/App.test.tsx` 与全量 suite 全绿，因此将其如实记为历史 flake 而非本轮回归。其余验证全部通过：`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_login_register_validation_real_api.sh`，最新产物目录 `.hermes/local-output/login-register-validation-real-api/20260508-180205`：验证 `GET /healthz` 返回 `200` 且 `status=ok`、`redis=ok`；`POST /api/v1/auth/register` 对非法邮箱返回 `400` + `请输入有效邮箱`、对短密码返回 `400` + `密码长度至少为 8 位`、对有效注册返回 `201`；随后普通用户 `GET /auth/me`、`GET /auth/menu`、`GET /dashboard/overview` 均 `200`，管理员 seeded 登录后的 `GET /auth/menu`、`GET /admin/risk`、`GET /admin/audit?limit=5` 也均 `200`；前端 `/login`、`/api-keys`、`/webhooks` shell 返回 `200`，浏览器 fresh load 到 `/login` 并切换到注册 tab 后，`注册并进入统一控制台` 表单仍正常存在。
- 本轮五维评审结论：产品/规格 PASS（diff 仅把注册前端行为向后端已存在真实合同对齐，没有引入独立注册后台、多壳跳转或新 workflow 承诺）；代码质量 APPROVED（运行时代码只在注册分支新增 2 个常量级早返回校验，测试同步补齐 2 条负向合同与 1 条 8 位边界正向合同，改动集中且最小）；安全/集成 SAFE（前端校验与后端 `validateCredentials` 规则及错误文案一致，且服务端校验仍是最终防线，未改变 `/auth/register` 请求体、会话处理、`/auth/menu` gating 或任何权限边界）；测试/可靠性 PASS（focused、`LoginPage + App` 组合、全量前端 205 tests、build、Go tests、compose config、真实 API 回放均通过；已把单次 `App.test.tsx` 失败按历史 flake 而非本轮回归如实记录）；性能/运维 PASS（仅提交路径上的两次轻量字符串检查，减少无效注册请求，不引入新依赖、配置、容器或部署复杂度）。下一前端优先项继续扫描 shared-console focused 用例与登录后首轮引导表面中仍残留的低信号 copy / 结构脆弱点，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片。

### 最新执行检查点（2026-05-08，Webhooks 用户视角 API Keys canonical wording 全面收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `WebhooksPage` 做一个小而完整的前端收口切片，把普通用户视角下仍残留的 API Keys 旧说法一次性统一到 canonical wording `打开 API Keys`。与上一轮只收口首轮 integration loop CTA 不同，本轮进一步补齐两个仍会泄露旧文案 `先配置 API Keys` 的表面：其一是用户视角 `webhooks-role-guidance` 提示区中的联调 tips，其二是 endpoint 空态 `webhooks-empty-state-actions` 中的主 CTA。目标是让同一登录后共享控制台里，`/api-keys` 在 Webhooks 页不再同时出现“先配置 / 打开”两套说法，同时保持 single-shell、role-aware、menu-truth、不拆独立后台的整体合同不变。
- 控制器按 TDD 先从 `web/src/pages/WebhooksPage.test.tsx` 写出 RED：新增一条 user guidance scoped 用例，要求 `webhooks-role-tips` 必须命中新文案“打开 API Keys 与文档，再补上回调消费端校验逻辑”，并显式否定旧文案“先配置 API Keys 与文档，再补上回调消费端校验逻辑”。首次实现后，产品/规格复审 fail-closed 指出空态主 CTA 虽已改成 `打开 API Keys`，但尚缺少正向回归保护；控制器随即继续按 TDD 补写第二条 empty-state focused 用例，在 `items=[]` 且共享接入路由可用的场景下，显式要求 `webhooks-empty-state-actions` 中出现 `打开 API Keys`、`查看 API 文档`，并否定旧按钮 `先配置 API Keys`。根因经 `systematic-debugging` 回读 `WebhooksPage.tsx` 确认，并非 menu gating、route 常量、后端 API 或角色边界问题，而是用户视角文案在 `roleCopy(default).tips` 与空态 CTA 两处仍保留历史 wording。随后仅最小修改 `WebhooksPage.tsx` 两处静态文案，并同步补强 focused 测试，不扩散到 supplier/admin 文案、真实 API 服务调用或其他页面逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx` 通过（1 file / 12 tests）；随后 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/App.test.tsx` 通过（2 files / 39 tests）；`cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 207 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面重跑本地忽略脚本 `.hermes/local-output/run_webhooks_integration_scope_real_api.sh`，最新产物目录 `.hermes/local-output/webhooks-integration-scope/20260508-184806`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok`、`redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu` 继续返回 `/api-keys`、`/webhooks`、`/docs` 等共享控制台路径，管理员 seeded 登录后 `/api/v1/auth/menu`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 依旧 `200`；前端 `/login` browser fresh load 保持统一登录壳，真实 API 回放未见任何路由/权限漂移。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 已把 Webhooks 用户视角下与 `/api-keys` 相关的 canonical wording 全部收口到 `打开 API Keys`，不再遗留 `先配置 API Keys`，且未引入独立后台、多壳叙事或超出当前里程碑的 workflow 承诺）；代码质量 APPROVED（运行时代码只触及 `WebhooksPage.tsx` 两处静态文案，测试在同文件新增 2 条 scoped focused 合同，分别锁住 user guidance 与 empty-state 两个真实用户表面，无 `.closest(...)`、page-global 宽泛命中或多余 runtime hook）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，且 `navigate(API_KEYS_ROUTE)`、`hasMenuPath(menu, ...)`、`/auth/menu` 真值、supplier/admin role 文案与所有后端权限契约均未改变）；测试/可靠性 PASS（focused Webhooks、`WebhooksPage + App.test.tsx`、全量 web Vitest、前端 build、Go tests、compose config 与真实 API 回放全部通过；针对上一轮 reviewer 指出的 empty-state 正向覆盖缺口已在本轮补齐，现有 warning 仍为仓库既有非阻塞噪声而非本轮回归）；性能/运维 PASS（纯前端 copy/test-contract 收口，不新增请求、轮询、状态同步、数据库负载或部署复杂度；新增 2 条 jsdom 单测的成本很小，真实容器与 API 运行状态继续 healthy）。下一前端优先项继续围绕 shared-console focused 用例中仍残留的低信号 copy / 结构脆弱点做小切片收口，再在此前提下回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下能力。

### 最新执行检查点（2026-05-08，共享控制台 canonical route title 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，先完成一个小而完整的前端共享壳切片，把 `AppSidebar` 与 `ConsoleLayout` 中仍直接显示服务端菜单旧标签（如 `API Keys`、`Webhook 设置`、`域名管理`、`供应商结算`）的表面，统一收敛为 route catalog 中的 canonical title / role-aware title（如 `开发者 API 接入工作台`、`开发者 Webhook 接入工作台`、`供给事件回调工作台`、`Webhook 运维与回调观测`、`域名池运营中枢`、`供应商资金与争议指挥台`），继续强化“同一登录后控制台、按角色区分功能区、不拆独立后台”的 new-api 单壳体验，同时保持真正的菜单可见性与路径真值仍由服务端 `/auth/menu` 决定。
- 控制器按 TDD 先从 `web/src/components/AppSidebar.test.tsx` 与 `web/src/components/ConsoleLayout.test.tsx` 写出 RED：要求 shared/sidebar/menuitem 与 header quick actions 不再展示旧式服务端 label，而是命中 `resolveRouteTitle(...)` 导出的 canonical identity，并保留点击后仍进入正确 route stub 的导航 proof。首次 RED 后按 `systematic-debugging` 回读 `AppSidebar.tsx`、`ConsoleLayout.tsx`、`consoleNavigation.tsx` 与当前可访问名称输出，确认根因不是 `/auth/menu` 路径缺失、不是角色权限漂移、不是登录/注册或后端 API 失败，而是 shared shell 继续直接渲染 server label，导致页面标题/面包屑/快捷动作与侧栏文案不一致。随后仅最小修改 4 个前端文件：让 sidebar 与 quick actions 都复用 `resolveRouteTitle(...)`，并同步收紧 focused 测试；期间独立代码质量评审指出初版只 canonicalize shared group 容易让 supplier/admin 组再次漂移，控制器已同轮改为所有侧栏分组统一走 route title source of truth，再次复验通过。
- 控制器验证结果：`pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx src/App.test.tsx` 通过（3 files / 34 tests）；随后 `cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 202 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_canonical_shell_labels_real_api.sh`，最新产物目录 `.hermes/local-output/canonical-shell-labels-real-api/20260508-172327`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok`、`redis=ok`；普通用户 / 供应商 / 管理员 seeded 登录均成功，`GET /api/v1/auth/me`、`GET /api/v1/auth/menu` 全部 `200`，且服务端返回的路径集合仍保持共享单壳契约（`/api-keys`、`/webhooks`、`/docs` 及 supplier/admin 扩展路径齐全）。该回放同时再次证明后端菜单真值仍是旧 label，但前端共享壳现已在不修改后端契约的前提下，把显示层统一收敛到 canonical route identity。
- 本轮五维评审结论：产品/规格 PASS（共享壳标题、侧栏与快捷动作已统一回到 route catalog 真值，强化 single-shell / role-aware navigation 叙事，未引入独立后台、未改注册路径、未更动任何菜单权限边界）；代码质量 APPROVED（当前 diff 仅 4 个前端文件，`AppSidebar` 与 `ConsoleLayout` 都复用同一 `resolveRouteTitle(...)` source of truth；复审后已消除“仅 shared group canonicalize”的维护性问题，剩余仅是测试中 `new RegExp(resolveRouteTitle(...))` 与既有 `act(...)`/Semi 警告这类非阻塞噪声）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式；未改 `/auth/login`、`/auth/register`、`/auth/me`、`/auth/menu`、ProtectedRoute、role-route 或任何后端权限契约，仍由服务端 path truth 决定显示集合）；测试/可靠性 PASS（focused Vitest、`App.test.tsx` 组合回归、全量 202 条前端测试、build、Go tests、compose config 全部通过；真实 API 回放也证明登录/菜单路径真值未漂移）；性能/运维 PASS（本轮只把侧栏/快捷动作标题解析集中到既有 route catalog，新增开销只是极小的前端内存映射，不引入新请求、轮询、数据库负载或部署复杂度；build 仍仅保留仓库既有 chunk-size warning，Compose 服务继续 healthy）。下一前端优先项继续回到 `App.test.tsx` / shared-console focused 收口与注册后首轮旅程一致性，再在此前提下推进 Phase 4/5 余下 API Key / Webhook / 白名单 / 风控 / 审计联动切片。

### 最新执行检查点（2026-05-08，Projects hero docs bridge scoped text-content 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `ProjectsPage` 做一个小而完整的前端 focused 收口切片。目标是把项目市场 hero 中“继续前往开发者 API 接入工作台：文档与密钥配置仍留在同一控制台”这一 docs-bridge 信号的 proof，从容易受 Tag 内部文本节点分裂影响的 `within(heroCard).getByText(/.../)` 收紧为 `projects-shared-console-hero` 区域上的 `toHaveTextContent(...)` 合同，同时保留 `采购路径信号` 标签断言，继续证明项目市场仍是 new-api 风格单壳共享控制台中的采购→履约→接入衔接入口，而不是拆出独立 onboarding 后台。
- 控制器按 TDD 先在 `web/src/pages/ProjectsPage.test.tsx` 写出 RED：把该 hero 用例从整句 regex `getByText` 改成 hero 容器级 `toHaveTextContent(...)`，并把用例标题收紧为“scoped shared-console docs bridge signal”。首次 RED 后按 `systematic-debugging` 回看当前 DOM 与测试失败输出，确认根因不是 `ProjectsPage.tsx` 运行时缺少 docs bridge，也不是 `/auth/menu`、`DOCS_ROUTE`、库存接口或共享布局异常，而是 Semi Tag 文本节点结构让 focused proof 仍偏脆弱。期间曾短暂尝试新增 runtime `data-testid` 与 `aria-label` 查询，但五维代码质量复审认为这会把 proof 推向实现细节；控制器已立即回退运行时代码，仅保留 test diff，最终以 hero 容器级文本合同完成 GREEN。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx` 通过（1 file / 11 tests）；随后 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/App.test.tsx` 通过（2 files / 38 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_projects_hero_scope_real_api.sh`，最新产物目录 `.hermes/local-output/projects-hero-scope/20260508-165740`：再次验证 `GET /healthz`（200，`status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/projects/inventory`、管理员 `POST /api/v1/auth/login` + `GET /api/v1/admin/risk`、供应商 `POST /api/v1/auth/login` + `GET /api/v1/supplier/resources/overview` 均 200，且前端 `/`、`/projects`、`/api-keys`、`/webhooks`、`/docs` SPA shell 全部 200 并保留 `<div id="root"></div>`。
- 本轮五维评审结论：产品/规格 PASS（diff 仅把 Projects hero 的 docs-bridge focused proof 收紧为 hero 区域文本合同，未改变 single-shell / new-api 叙事、采购页 identity、API Keys / Docs 路径意义或任一路由菜单真值）；代码质量 APPROVED（最终 live diff 仅改 1 个测试文件，运行时代码保持不变；评审曾阻断新增 production `data-testid` 与依赖 Semi Tag `Tag:` accessible label 的方案，控制器已回退为更稳妥的 hero 容器 `toHaveTextContent(...)` 断言）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，且未改 `/auth/menu` gating、`DOCS_ROUTE`、库存 API、权限边界或任何后端契约）；测试/可靠性 PASS（focused Vitest、`ProjectsPage + App.test.tsx` 组合、前端 build、Go tests、compose config 与真实 API 回放全部通过；新的 proof 仍保留 exact sentence 合同，但避免直接依赖 Tag 内部文本节点匹配）；性能/运维 PASS（本轮为纯 test-only diff，不引入运行时代码、构建产物、请求或部署变化；Compose 服务继续 healthy，仅保留仓库既有 chunk size warning / React act 噪声为非阻塞债务）。下一前端优先项继续回到 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里剩余的低信号全局文案/结构耦合，再在此前提下推进 Phase 4/5 余下切片。

### 最新执行检查点（2026-05-08，Webhooks 管理员角色提示 scoped proof 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `WebhooksPage` 管理员角色提示做一个小而完整的前端 focused 收口切片。目标是把 `WebhooksPage.test.tsx` 里仍依赖散落文本命中的 admin role tips proof，收紧为更贴近运行时结构的 `webhooks-role-guidance -> webhooks-role-tips` 区域合同：继续要求管理员视角下保留 `Webhook 运维与回调观测` heading 与 `管理员视角` badge，并在同一 tips 区域内验证 `failed / pending`、`last_error`、`测试投递返回 202` 三个真实运维提示仍然存在，而不是继续让 focused 测试依赖对单个 Tag 文本节点的脆弱匹配。该切片不新增后端能力、不改变菜单/路由/角色边界，也不引入独立后台，只增强单壳共享控制台下 Webhooks 管理员运维页的语义化测试合同。
- 控制器按 TDD 先从 `web/src/pages/WebhooksPage.test.tsx` 写出 RED：把管理员 metrics / delivery operations 用例中的 role tips 断言从 `within(roleTips).getByText(...)` 收紧为 `roleTips` 容器级 `toHaveTextContent(...)` 合同，显式要求继续命中 `failed / pending`、`last_error` 与 `测试投递返回 202`；首次 focused 失败后，再按 `systematic-debugging` 回读 `WebhooksPage.tsx` 当前运行时结构，确认根因并非 role tips 缺失，也不是 `/auth/menu`、Webhooks 实际 API、路由常量或管理员权限边界错误，而是测试 proof 过度依赖 Tag 内部文本节点。随后只最小调整 `WebhooksPage.test.tsx`，保持运行时代码不变，将 proof 收敛到已有 `webhooks-role-tips` 语义区域，不扩散到任何后端逻辑或页面 copy。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx` 通过（1 file / 10 tests）；随后 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/App.test.tsx` 通过（2 files / 37 tests）；`pnpm --dir web exec vitest run` 全量前端测试通过（26 files / 202 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose ps web api gateway` 显示 `web` / `api` / `gateway` healthy。真实 API 方面新增并重跑本地忽略脚本 `.hermes/local-output/run_webhooks_admin_tip_anchor_real_api.sh`，最新产物目录为 `.hermes/local-output/webhooks-admin-tip-anchor/20260508-162555`：已再次验证 `GET /healthz`（200，`status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements`，以及前端 `/`、`/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立。
- 本轮五维评审结论：产品/规格 PASS（diff 仅把 Webhooks 管理员 role tips 的 focused proof 从分散文本命中收紧到已有语义区域，未改变单壳 shared-console 叙事、管理员页面 identity、Webhooks/API Keys/Docs 桥接关系或任何路由/菜单真值）；代码质量 APPROVED（初版曾尝试在运行时代码新增按 index 分配的 admin tip test id，被独立代码质量评审判定为不必要且易漂移；控制器已立即回退该实现，改为仅用现有 `webhooks-role-tips` 容器级合同完成 proof，最终 diff 只剩 1 个测试文件的最小修改，无新增 runtime hook、无顺序耦合 test id）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，且运行时代码未改，`/auth/menu` gating、管理员角色边界、Webhook 实时投递语义与真实 API 契约均未漂移）；测试/可靠性 PASS（focused Vitest、`WebhooksPage + App.test.tsx` 组合、全量前端 Vitest、构建、Go tests、compose config 与真实 API 回放全部通过；保留仓库既有 Semi UI `findDOMNode` 与 React `act(...)` 警告为历史噪声，本轮未新增 blocker）；性能/运维 PASS（本轮无运行时代码路径变更，仅降低测试对内部文本节点的脆弱依赖；Compose 服务继续 healthy，web build 仅保留既有 chunk size warning，无新增部署/运行复杂度）。下一前端优先项继续回到 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里剩余的低信号全局文案/结构耦合，再在此前提下推进 Phase 4/5 余下后端切片。

### 最新执行检查点（2026-05-08，Webhooks 首轮 API Keys CTA canonical wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `WebhooksPage` 做一个小而完整的前端 focused 收口切片；具体把普通用户首轮接入卡片中仍残留的低信号 CTA `先配置 API Keys`，统一收敛为与仓库 shared-console canonical page identity 一致的 `打开 API Keys`，避免同一控制台内 `/api-keys` 在 Dashboard / Profile / Docs / Supplier / Admin 场景都已收口为 canonical wording 后，Webhooks 首轮卡片仍保留另一套“先配置”说法。
- 控制器按 TDD 先从 `web/src/pages/WebhooksPage.test.tsx` 写出 RED：把首轮 integration loop 导航用例与 loop CTA proof 从旧的 `先配置 API Keys` 收紧为 `打开 API Keys`，并补上 scoped 负断言，显式要求 `webhooks-first-integration-loop` 与 `webhooks-empty-state-actions` 在当前 diff 影响范围内不再暴露旧文案。首次 focused 失败后，再按 `systematic-debugging` 回读 `web/src/pages/WebhooksPage.tsx`，确认根因是首轮 integration loop 的运行时代码仍保留旧 CTA `先配置 API Keys`，而不是路由常量、menu gating、空态 fallback 或后端 API 合同问题。随后仅最小修改这一处运行时按钮文案，并同步更新 focused 测试，不扩散到空态行为、真实 API 服务调用或任意后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx` 通过（1 file / 10 tests）；随后 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/App.test.tsx` 通过（2 files / 37 tests）；`pnpm --dir web exec vitest run` 全量前端测试通过（26 files / 202 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 后 `docker compose ps web api gateway` 显示 `web` / `api` / `gateway` healthy。真实 API 方面新增并重跑本地忽略产物目录 `.hermes/local-output/webhooks-api-keys-canonical-cta/20260508-160040`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok`、`redis=ok`；普通用户动态注册后 `/auth/menu`、`/webhooks/endpoints` 均 `200` 且菜单继续包含 `/api-keys`、`/webhooks`、`/docs`；管理员 seeded 登录后 `/auth/menu`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200` 且菜单继续包含 `/webhooks`；前端 `/`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA shell 全部 `200` 且保留 `<div id="root"></div>`。
- 本轮五维评审结论：产品/规格 PASS（Webhooks 首轮卡片中的 API Keys 入口已与仓库 shared-console canonical identity 对齐，不再在同一控制台里一边叫 `打开 API Keys` 一边叫 `先配置 API Keys`，也未引入独立后台或虚假的强制接入流程）；代码质量 APPROVED（diff 仅集中修改 `WebhooksPage.tsx` 与 `WebhooksPage.test.tsx` 两个文件，运行时代码只触及 1 处文案，测试使用 `webhooks-first-integration-loop` / `webhooks-empty-state-actions` scoped role query 与旧文案消失 proof，无 `.closest(...)`、page-global 宽泛命中或新的 runtime hook）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，且未改认证、会话、`API_KEYS_ROUTE`、menu gating 或任何 Phase 4/5 后端权限契约）；测试/可靠性 PASS（focused Vitest、Webhooks+App 组合 Vitest、全量 web Vitest、前端 build、Go tests、compose config、容器重建与真实 API 回放全部通过，既有 React `act(...)` / Semi UI `findDOMNode` / chunk-size warning 仍为仓库历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯前端 copy/test-contract 收口，不新增请求、轮询、数据库负载或部署复杂度，`web` / `api` / `gateway` 持续 healthy；本轮五维评审明细已落盘 `.hermes/local-output/webhooks-cta-review-20260508.md`）。

### 最新执行检查点（2026-05-08，Login / Projects 低信号长文案断言收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `LoginPage` 与 `ProjectsPage` 做一个小而完整的前端 focused 收口切片。目标是把仍残留的 page-global 长文案断言继续收敛到更贴近运行时结构的 shared-console 合同：`LoginPage` 侧为左上 hero 摘要补最小 `login-entry-summary` 语义锚点，并让测试从依赖整句“统一入口、共享控制台与接入链路收敛到同一登录面。”切到 heading + summary 容器文本合同，同时把注册态 guidance proof 从整页 helper 文案收敛到 `login-auth-guidance-banner`；`ProjectsPage` 侧则把 hero 中“继续前往开发者 API 接入工作台：文档与密钥配置仍留在同一控制台”这一长句的断言收敛到 `projects-shared-console-hero` 作用域内，辅以 `采购路径信号` 稳定标签，避免继续依赖 page-global 长文案命中。
- 控制器按 TDD 先改测试写出 RED：`web/src/pages/LoginPage.test.tsx` 新要求 `login-entry-summary` 容器、register banner scoped proof 与旧 page-global copy 弱依赖移除；`web/src/pages/ProjectsPage.test.tsx` 新要求 hero 区域 scoped 长句 + `采购路径信号` 标签合同。首次 focused 失败后，再按 `systematic-debugging` 回读运行时代码，确认根因分别是 `LoginPage.tsx` 左上 hero 缺少稳定语义锚点、而 `ProjectsPage` 现有测试仍在 page-global 范围直接命中 hero 长句；并非路由常量、菜单 gating、注册流程或后端 API 契约问题。随后仅最小修改 `LoginPage.tsx` 增加 `login-entry-summary`，并同步收紧两份 focused 测试，不扩散到任何后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/pages/ProjectsPage.test.tsx` 通过（2 files / 16 tests）；随后 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/pages/SettingsPage.test.tsx src/App.test.tsx` 之外，进一步执行 `cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 202 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；新增并重跑本地忽略真实 API 脚本 `.hermes/local-output/login-projects-longcopy-scope-real-api/run.sh`，最新产物目录为 `.hermes/local-output/login-projects-longcopy-scope-real-api/20260508-152403`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok`、`redis=ok`，普通用户动态注册后 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均 `200` 且菜单继续包含 `/api-keys`、`/webhooks`、`/docs` 而不泄露 `/admin/risk`、`/supplier/domains`；管理员 seeded 登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`；前端 `/`、`/login`、`/projects`、`/api-keys`、`/webhooks`、`/docs` SPA shell 全部 `200`。
- 本轮五维评审结论：产品/规格 PASS（本轮只把 Login/Projects 的 proof 从 page-global 长文案收敛到更贴近单壳 shared-console 的 summary/hero 容器合同，没有引入独立后台、额外 onboarding 页面或多壳分叉）；代码质量 APPROVED（diff 仅 3 个前端文件，运行时代码只新增 1 个最小 `data-testid`，测试同步转向 scoped/semantic proof，无 `.closest(...)`、宽泛 page-global 重复命中或新的 runtime hook）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，且未改注册/登录 API、session 持久化、`/auth/menu` gating 或任何 Phase 4/5 后端权限契约）；测试/可靠性 PASS（focused Vitest、全量 web Vitest、前端 build、Go tests、compose config、容器重建与真实 API 回放全部通过，既有 React `act(...)` / Semi UI `findDOMNode` / chunk-size warning 仍为仓库历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯前端测试合同/语义锚点收口，不新增请求、轮询、数据库负载或部署复杂度，`web` / `api` / `gateway` 继续 healthy）。

### 最新执行检查点（2026-05-08，Dashboard role actions canonical CTA / admin Webhook identity 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `DashboardPage` 做一个小而完整的 shared-console focused 收口切片；具体把普通用户角色动作中仍残留的旧式 `集成与回调` / `管理 API Keys` 收口为与 `/api-keys` canonical page identity 一致的 `开发者 API 接入工作台` / `打开 API Keys`，并把管理员角色动作中仍泛化的 `/webhooks` 入口 `共享接入入口` / `打开 Webhook 工作台` 收口为 admin role-aware canonical identity `Webhook 运维与回调观测` / `继续配置 Webhook`，继续保持“单一登录后控制台、按角色分区功能区、不拆独立后台”的 new-api 单壳方向。
- 控制器按 TDD 先从 `web/src/pages/DashboardPage.test.tsx` 写出 RED：在既有 Dashboard proof 基础上，补一条 admin `dashboard-role-action-webhooks` scoped 合同，显式要求命中 `Webhook 运维与回调观测` 与 `继续配置 Webhook`，并否定旧文案 `共享接入入口` / `打开 Webhook 工作台`；首次 focused 失败后，再按 `systematic-debugging` 回读 `web/src/pages/DashboardPage.tsx`，确认根因是 `roleActions(menu, role)` 中 admin `/webhooks` 动作与普通用户 `/api-keys` 动作仍保留旧标题/按钮文案，而不是菜单 gating、路由常量、注册登录或后端 API 合同问题。随后仅最小修改运行时代码这两组 role action 的 title / description / button，并保持 `navigate(item.path)`、`/auth/menu` 真值 gating 与其余 shared-console shell 结构不变。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx` 通过（1 file / 8 tests）；随后 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx` 通过（2 files / 35 tests）；`cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 202 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 后 `docker compose ps web api gateway` 显示 `web` / `api` / `gateway` healthy。真实 API 方面新增并重跑本地忽略脚本 `.hermes/local-output/run_dashboard_canonical_role_actions_real_api.sh`，最新产物目录为 `.hermes/local-output/dashboard-canonical-role-actions-real-api/20260508-142634`：再次验证 `GET /healthz` 返回 `200 {"redis":"ok","status":"ok"}`；普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且 menu 继续包含 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；管理员 seeded 登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/webhooks/endpoints` 全部 `200`，且 admin menu 继续包含 `/admin/risk`、`/admin/audit`、`/admin/suppliers`、`/api-keys`、`/webhooks`、`/docs`；供应商 seeded 登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements`、`/webhooks/endpoints` 全部 `200`，且 supplier menu 保持 `/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs`；前端 `/`、`/login`、`/webhooks`、`/api-keys`、`/docs` SPA shell 全部 `200`。
- 本轮五维评审结论：产品/规格 PASS（Dashboard 中普通用户 API Keys 入口与管理员 Webhook 入口已分别收口到 canonical page identity / role-aware heading，不再残留 `集成与回调`、`管理 API Keys`、`共享接入入口`、`打开 Webhook 工作台` 等低信号旧说法，也未引入独立后台）；代码质量 APPROVED（diff 仅集中修改 `DashboardPage.tsx` 与 `DashboardPage.test.tsx` 两个文件，运行时代码只触及 2 组 role action copy，测试同步补强 admin webhook action 的 scoped heading/button/旧文案消失 proof，无 `.closest(...)`、page-global 宽泛命中或额外 runtime hook）；安全/集成 SAFE（所有 CTA 仍完全受 `roleActions(menu, user?.role)` 与服务端 `/auth/menu` 真值 gating 控制，controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式；delegate reviewers 本轮发生跨仓库污染并被 fail-closed 废弃，当前结论以 controller-side live repo diff + focused/full verification + real API 回放为准）；测试/可靠性 PASS（focused Vitest、Dashboard+App 组合 Vitest、全量 web Vitest、前端 build、Go tests、compose config、容器重建与真实 API 回放全部通过，既有 React `act(...)` / Semi UI `findDOMNode` / chunk-size warning 未因本轮 diff 新增或恶化）；性能/运维 PASS（纯前端文案/测试合同收口，不新增请求、轮询、数据库负载或部署复杂度；`web` / `api` / `gateway` healthy，评审明细已落盘 `.hermes/local-output/dashboard-role-actions-review-20260508.md`）。

### 最新执行检查点（2026-05-08，Login 入口信号条 / 首轮动作文案收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录 API 契约、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端能力的前提下，仅对 `LoginPage` 做一个小而完整的前端垂直切片，把登录页左侧仍偏“说明摘要”的两块内容继续收敛为更接近 Linear / new-api 的控制台入口语言。具体包括：把 hero 副文案从“只保留登录入口、共享控制台与接入路径这三个稳定承诺...”收紧为更短的入口信号句“统一入口、共享控制台与接入链路收敛到同一登录面。”；把 `login-control-plane-readiness` 从 `控制台入口摘要` / `登录前只保留...三条最小事实` 收口为更产品化的 `控制台入口信号` / `先确认统一入口、共享壳与接入落点，再进入登录或注册。`；同时把两条信号值从 `登录与注册同入口`、`登录后直达同一壳` 收紧为 `登录 / 注册同入口`、`登录后进入同一壳`；并把注册 runway 标题从旧的 `首轮接入路径` / `注册后沿同一导航完成...` 收口为更像控制台动作区的 `注册后第一轮动作` / `在同一控制台完成 Key、Webhook 与文档核对。`。
- 控制器按 TDD 先从 `web/src/pages/LoginPage.test.tsx` 写出 RED：把 focused 合同从旧的 summary/runway 文案切到新 signal-strip / first-actions 文案，并显式要求旧文案消失；首次 focused 失败后，再按 `systematic-debugging` 回读 `web/src/pages/LoginPage.tsx`，确认根因是运行时 `readinessRows`、hero 段落、`login-control-plane-readiness` heading/description 与 `login-register-journey` heading/description 仍停留在上一轮更偏说明式的 copy，而认证模式切换、注册表单、真实 `/auth/register` 接口与 shared-console 跳转行为本身并无问题。随后仅最小修改这 6 处运行时文案与对应 focused 断言，没有扩散到路由、权限、会话持久化或后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（1 file / 5 tests）；随后 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（2 files / 32 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`docker compose up -d --build web api gateway`、`docker compose ps web api gateway`、`git diff --check` 全部通过。真实 API 方面新增并重跑本地忽略脚本 `.hermes/local-output/run_login_shell_signal_tightening_real_api.sh`，最新产物目录为 `.hermes/local-output/login-shell-signal-tightening/20260508-135430`：验证了 `GET /healthz` 返回 `200 {"redis":"ok","status":"ok"}`，动态注册用户 `hermes.login.20260508-135430@example.com` 成功拿到 Bearer token，`GET /auth/me`、`GET /auth/menu`、`GET /dashboard/overview`、`GET /auth/api-keys`、`GET /webhooks/endpoints` 全部 200，且 `/auth/menu` 返回普通用户共享控制台路径 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`。SPA 侧通过 `http://127.0.0.1:80/login` controller/browser fresh reload 与 shell HTTP 200 再验证，新文案 `控制台入口信号`、`注册后第一轮动作` 已在本地重建后真实呈现；本轮 `browser_vision` / `vision_analyze` 遇到上游 504，因此按降级策略以 fresh browser snapshot + build/redeploy + HTTP/real API 证据作为视觉/运行时准入依据，并把更细的主观视觉微调留给后续前端切片继续推进。
- 本轮五维评审结论：产品/规格 PASS（登录页左侧已从“摘要/说明块”进一步收敛为更产品化的信号条与动作区，仍保持单一登录后 shared-console 叙事，没有引入独立注册后台、多壳跳转或虚假的硬编码 onboarding workflow）；代码质量 APPROVED（diff 仅集中修改 `LoginPage.tsx` 与 `LoginPage.test.tsx` 两个前端文件，运行时代码只触及 6 处静态 copy，测试同时保护新文案出现与旧文案消失，没有引入新 hook、`.closest(...)` 或 page-global 宽泛选择器）；安全/集成 SAFE（controller-side 静态扫描对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，且未改登录/注册 API、token 持久化、`/auth/menu` gating、ProtectedRoute/role-route、跳转目标或任何后端契约）；测试/可靠性 PASS（focused 与 `LoginPage + App` 组合 Vitest 全绿，前端 build、Go tests、compose config、容器重建、真实 API 回放与 fresh browser snapshot 一致通过；既有 React `act(...)`、Semi UI `findDOMNode` 与大 bundle chunk 警告未因本轮 diff 新增或恶化，继续记录为非阻塞债务）；性能/运维 PASS（本轮仅文案/测试合同收口，不新增查询、接口、轮询或状态同步路径；重建后 `web`/`api`/`gateway` healthy，真实 API 与 shell 端口仍分别稳定于 `8080` / `80`）。

### 最新执行检查点（2026-05-08，ApiKeys CTA canonical wording / empty-state CTA 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：针对 `ApiKeysPage` 仍残留的旧式 Webhook CTA 文案做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，把 API Keys 页内两处仍会把 `/webhooks` 落点叫作 `前往 Webhook 设置` 的入口统一收敛为 shared-console canonical CTA `继续配置 Webhook`，并把顶部提示 banner 从旧的“继续前往 Webhook 设置与 API 文档”改为和当前单壳桥接合同一致的“继续配置 Webhook并查看 API 文档”；同时补一条 focused regression test，显式证明空态 action area 也已完成相同收口，不再让 API Keys 当前密钥空态与 bridge/created-key 成功态暴露两套说法。
- 控制器按 TDD 先从 `web/src/pages/ApiKeysPage.test.tsx` 写出 RED：一方面在既有 `api-keys-shared-console-bridge` proof 中新增旧文案消失 / 新 banner copy 出现断言；另一方面新增空态用例，让 `mockedGetAPIKeys` 返回空列表后显式要求 empty-state action area 命中 `继续配置 Webhook` 与 `查看 API 文档`，并否定旧按钮 `前往 Webhook 设置`。首次 focused 失败后，再按 `systematic-debugging` 回读 `web/src/pages/ApiKeysPage.tsx`，确认根因是运行时代码确有两处旧文案：一处 `Banner` 仍写“继续前往 Webhook 设置与 API 文档”，另一处空态 `Empty` CTA 仍写 `前往 Webhook 设置`，而 bridge/created-key success action 已经使用 canonical wording。随后仅最小修改这两处运行时 copy，保持 `navigate(WEBHOOKS_ROUTE)`、`hasMenuPath(menu, ...)`、API Key 创建/白名单/撤销逻辑与其余 single-console bridge 结构不变。
- 控制器验证结果：focused Vitest `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 通过（1 file / 13 tests）；随后与 `src/App.test.tsx` 的组合回归通过（2 files / 40 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 后 `docker compose ps web api gateway` 显示 `web` / `api` / `gateway` healthy，`GET /healthz` 返回 `200 {"redis":"ok","status":"ok"}`，前端 `/api-keys` shell 返回 `200` 且包含 `<div id="root"></div>`。真实 API 方面新增并重跑本地忽略脚本 `.hermes/local-output/run_api_keys_canonical_cta_real_api.sh`，最新产物目录为 `.hermes/local-output/api-keys-canonical-cta-real-api/20260508-124616`：再次验证普通用户注册 `201` 后 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，且 user menu 继续包含 `/api-keys`、`/webhooks`、`/docs`；管理员 seeded 登录 `200` 后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`，且 admin menu 继续包含 `/api-keys`、`/webhooks`、`/admin/risk`、`/admin/audit`、`/docs`。浏览器 fresh load 直接打开 `/api-keys` 因未登录正确回落到 `/login`，与 shared shell 鉴权契约一致，没有出现未授权页面泄露。
- 本轮五维评审结论：产品/规格 PASS（API Keys 页 bridge、banner、empty-state CTA 已统一收敛到 canonical `/webhooks` shared-console identity，不再一边叫“继续配置 Webhook”一边叫“前往 Webhook 设置”，也未引入独立后台或双壳叙事）；代码质量 APPROVED（diff 仅集中修改 `ApiKeysPage.tsx` 与 `ApiKeysPage.test.tsx` 两个前端文件，运行时代码只触及两处 copy，测试补强了 bridge banner 与 empty-state action area 两个真实用户表面，没有引入新的 runtime hook 或广泛 selector 漂移）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式；所有 CTA 仍完全受既有 `hasMenuPath(menu, ...)` / `resolvePreferredConsoleRoute(...)` / 服务端 `/auth/menu` 真值 gating 控制，未放宽任何鉴权、角色路由或 Phase 4/5 后端 API 权限边界）；测试/可靠性 PASS（focused Vitest、ApiKeys+App 组合 Vitest、前端 build、Go tests、compose config、容器重建、健康检查与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk-size warning 仍为仓库历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯前端 copy/test-contract 收口，不新增请求、轮询、数据库负载或部署复杂度；真实容器重建与 health/API replay 证明运行态稳定）。

### 最新执行检查点（2026-05-08，Projects / Orders / Balance / ApiDocs canonical CTA 二次收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：围绕仍残留在 shared-console 前端中的低信号 CTA / 标题漂移做一个小而完整的前端垂直切片，集中收口 4 个页面的接入文案与入口一致性，而不触碰注册/登录接口、`/auth/menu` 菜单真值或 Phase 4/5 后端契约。具体包括：把 `OrdersPage` 履约说明卡里仍残留的 `管理 API Keys` 改为 canonical CTA `打开 API Keys`；把 `ProjectsPage` hero 与首轮采购 lane 中的“API 接入准备”统一收敛为更贴近目标页身份的“开发者 API 接入工作台”；把 `BalancePage` 能力桥接按钮从旧的 `打开 Webhook 设置` / `打开 API 文档` 收敛为与全仓 shared-console 合同一致的 `继续配置 Webhook` / `查看 API 文档`；以及把 `ApiDocsPage` 中仍残留的 action cards 旧标题 `最小权限 API Key` / `真实回调验证` 与旧按钮 `打开 API Keys 工作台` / `打开 Webhook 设置` 收敛到 canonical page identity 与 role-aware Webhook 标题，确保 docs bridge、loop、dashboard next step、orders guidance、projects hero、balance capability actions 不再对同一路由输出多套说法。
- 控制器按 TDD 先改 focused 测试写出 RED：`OrdersPage.test.tsx` 补 `orders-fulfillment-guidance-card` 内 `打开 API Keys` 存在与 `管理 API Keys` 消失 proof；`ProjectsPage.test.tsx` 收紧 hero 与首轮 lane copy 到“开发者 API 接入工作台”新合同；`BalancePage.test.tsx` 收紧 capability actions 的 Webhook / Docs CTA 命名并保持基于 `data-testid` 的导航 click-through；`ApiDocsPage.test.tsx` 为 bridge / loop 补上“旧 heading 不再出现”的负断言；`DashboardPage.test.tsx` 补上 integration card 不再显示旧标题 `集成与回调` 的防回归检查。首次失败后按 `systematic-debugging` 回读运行时代码，定位根因分别是 4 个页面仍保留旧 CTA / 旧标题字符串，而不是路由常量、menu gating、权限边界或 API 合同问题；随后仅最小修改 `OrdersPage.tsx`、`ProjectsPage.tsx`、`BalancePage.tsx`、`ApiDocsPage.tsx` 的静态文案/schema，保持既有 `navigate(...)`、`hasMenuPath(...)`、role-aware `webhookConsoleTitle(...)` 与 shared-console 单壳布局不变。
- 控制器验证结果：focused Vitest `pnpm --dir web exec vitest run src/pages/OrdersPage.test.tsx src/pages/ProjectsPage.test.tsx src/pages/BalancePage.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/DashboardPage.test.tsx` 通过（5 files / 41 tests）；随后与 `src/App.test.tsx` 组合回归通过（6 files / 68 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 后 `docker compose ps web api gateway` 显示 `web` / `api` / `gateway` healthy；控制器额外验证 `GET /healthz` 返回 `200 {"redis":"ok","status":"ok"}`，前端根路由与 `/login`、`/docs` shell 均 `200` 且包含 `<div id="root"></div>`。真实 API 方面新增并重跑本地忽略脚本 `.hermes/local-output/run_frontend_canonical_cta_real_api.sh`，最新产物目录为 `.hermes/local-output/frontend-canonical-cta-real-api/20260508-121309`：再次验证普通用户注册 `201`，`/auth/me`、`/auth/menu`、`/dashboard/overview`、`/projects/inventory`、`/orders/activations`、`/wallet/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，管理员 seeded 登录 `200` 后 `/auth/menu`、`/admin/risk`、`/admin/audit` 全部 `200`，并把 user/admin 菜单路径摘要写入 `summary.json` 以证明 canonical CTA 指向的 shared-console 路径依旧由服务端真值暴露。
- 本轮五维评审结论：产品/规格 PASS（4 个页面的 shared-console CTA / 标题已向 canonical identity 收敛，不再把 `/api-keys` 同时描述成“管理 API Keys / 最小权限 API Key / API 接入准备”，也不再把 `/webhooks` 在余额页与文档页继续叫作“打开 Webhook 设置 / 真实回调验证”；整体保持单一登录后控制台与按角色分区，而未引入独立后台）；代码质量 APPROVED（diff 仅限 9 个前端文件，运行时代码改动只触及静态 copy/schema，测试用 scoped region/query 与已有 test id 补强旧文案消失 proof，无 `.closest(...)`、宽泛 page-global 选择器或多余 runtime hook）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式；所有 CTA 仍受现有 `hasMenuPath` / `resolvePreferredConsoleRoute` / `/auth/menu` 真值 gating 控制，未改变登录、刷新、登出、角色路由或任何后端 API 权限边界）；测试/可靠性 PASS（focused Vitest、加 App 的集成 Vitest、前端 build、Go tests、compose config、容器重建、健康检查与真实 API 回放全部通过；现存 React `act(...)`、Semi UI `findDOMNode` 与 chunk-size warning 仍为仓库既有非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯前端 copy/schema 收口，不增加请求、轮询、数据库负载或部署复杂度；控制器用真实容器重建与角色菜单回放复验了运行态路径，不是仅做冒烟测试）。

### 最新执行检查点（2026-05-08，Orders 空态 API Keys canonical CTA 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `OrdersPage` 做一个小而完整的 shared-console focused 收口切片；具体把订单空态 `orders-empty-state-actions` 中仍残留的低信号 CTA `查看 API 接入准备`，统一收敛为与 `/api-keys` canonical page identity 一致的动作文案 `打开 API Keys`，避免订单页在“无订单 → 继续接入”场景下继续把目标页误表述成准备说明页，也继续保持“项目市场 → 订单中心 → API Keys”全部位于同一登录后控制台单壳内，而不是拆出独立后台或额外接入准备页。
- 控制器按 TDD 先从 `web/src/pages/OrdersPage.test.tsx` 写出 RED：直接把两个空态导航 proof（普通空态与空态 API continuation route-stub 场景）从旧的 `查看 API 接入准备` 收紧为 `打开 API Keys`，同时新增负断言显式否定旧文案仍留在 `orders-empty-state-actions`；首次 focused 失败后，再按 `systematic-debugging` 回读 `web/src/pages/OrdersPage.tsx`，确认根因是 Empty actions 中 API Keys CTA 仍使用旧说明式按钮文本，而不是路由常量、menu gating 或落点 heading 问题。随后仅最小修改运行时代码：保持既有 `canOpenApiKeys` gating 与 `navigate(API_KEYS_ROUTE)` 行为不变，只把空态按钮文案对齐到 `打开 API Keys`，并按性能/运维评审建议顺手恢复该 JSX 片段缩进一致性。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/OrdersPage.test.tsx` 通过（1 file / 10 tests）；随后 `pnpm --dir web exec vitest run src/pages/OrdersPage.test.tsx src/App.test.tsx` 通过（2 files / 37 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 后 `docker compose ps web api gateway` 显示 `web` / `api` / `gateway` healthy。真实 API 方面新增并重跑本地忽略脚本 `.hermes/local-output/run_orders_empty_state_cta_real_api.sh`，最新产物目录为 `.hermes/local-output/orders-empty-state-cta-real-api/20260508-111049`：再次验证 `GET /healthz` 返回 `200 {status=ok}`、普通用户注册 `201`、`/auth/menu` 与 `/auth/me` 均 `200` 且菜单继续包含 `/projects`、`/orders`、`/api-keys`，`/orders/activations` 返回 `200` 且观察到真实合同 `items` 可为 `null`，`/auth/api-keys` 返回 `200` 且 `items` 为数组，同时 `/orders` 与 `/api-keys` SPA 壳均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（Orders 空态 CTA 已与 `/api-keys` 的 canonical page identity 对齐，不再把真实落点表述成“接入准备”说明页，也未引入独立后台或单壳叙事漂移）；代码质量 APPROVED（diff 仅集中修改 `OrdersPage.tsx` 与 `OrdersPage.test.tsx` 两个文件，测试同时保护新文案存在、旧文案消失与 click-through 落点，选择器仍为 scoped role query，无 `.closest(...)`、page-global 宽泛命中或无意义 test hook）；安全/集成 SAFE（CTA 仍完全受 `canOpenApiKeys` / `API_KEYS_ROUTE` / 服务端 `/auth/menu` 真值 gating 控制，未放宽权限边界、未改变任何 API 契约，也未把按钮语义变成创建/提升权限）；测试/可靠性 PASS（focused Vitest、Orders+App 组合 Vitest、前端 build、Go tests、compose config、容器重建与真实 API 回放全部通过，且本轮显式接受 `/orders/activations` 的真实 `items nullable` 合同，未把空列表/空态验证写成错误强断言；非阻塞建议仅为后续补一条“menu 不含 `/api-keys` 时空态 CTA 不出现”的直接权限保护测试）；性能/运维 PASS（纯前端 CTA 文案收口，不新增请求、状态分支、数据库负载或部署复杂度；web build 既有 chunk warning 未恶化，运维健康检查与 SPA 可达性继续稳定）。

### 最新执行检查点（2026-05-08，Dashboard / Settings 首轮 API Keys canonical CTA 对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 Dashboard 与 Settings 两个“普通用户首轮引导”表面做一个小而完整的 shared-console focused 收口切片；具体把 Dashboard 首轮 mission cards / next-step lane 中仍残留的 `继续准备 API 接入`、`完成 API 接入准备`、`最后完成 API 接入` 与 CTA `管理 API Keys`，统一收敛为与 `/api-keys` canonical page identity 一致的 `开发者 API 接入工作台` + `打开 API Keys`，并同步把 Settings `首次使用清单` 第 3 步从旧的 `完成 API 接入准备` / `管理 API Keys` 对齐到同一合同，避免登录后首页与设置页对同一路径继续出现两套 API Keys 入口语义。
- 控制器按 TDD 先从 `web/src/pages/DashboardPage.test.tsx` 与 `web/src/App.test.tsx` 写出 RED：把 default-user onboarding / next-step proof 从旧的 `管理 API Keys` 与 `最后完成 API 接入` 收紧为 `打开 API Keys` 与 `开发者 API 接入工作台`，并要求继续命中既有 `dashboard-next-step-api-keys` scoped 容器；首次 GREEN 后，五维代码质量评审 fail-closed 指出 Settings `首次使用清单` 仍保留旧合同，会让两个同属首轮引导的表面继续漂移。控制器据此同轮回读 `web/src/pages/SettingsPage.tsx` / `web/src/pages/SettingsPage.test.tsx`，仅最小补齐第 3 步标题与按钮文案对齐，并新增 checklist 区域内 `3. 开发者 API 接入工作台` scoped 断言，避免只改按钮不保护标题合同。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/pages/SettingsPage.test.tsx src/App.test.tsx` 通过（3 files / 40 tests）；随后 `cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 201 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 后 `docker compose ps web api gateway` 显示 `web` / `api` / `gateway` healthy。真实 API 方面重跑 `.hermes/local-output/run_dashboard_journey_tightening_real_api.sh`，最新产物目录为 `.hermes/local-output/dashboard-journey-tightening/20260508-103935`，再次验证 `GET /healthz` 返回 `200 {status=ok, redis=ok}`、普通用户注册 `201`、`/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`，管理员 `/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 `200`，且 `/`、`/api-keys`、`/webhooks`、`/docs` SPA 壳可达均为 `200`。
- 本轮五维评审结论：产品/规格 PASS（Dashboard 与 Settings 两个首轮引导表面已统一收敛到 `/api-keys` 的 canonical page identity，不再让同一路径同时暴露“完成 API 接入准备 / 管理 API Keys”与“开发者 API 接入工作台 / 打开 API Keys”两套说法，也未引入独立后台）；代码质量 APPROVED（diff 仅集中修改 5 个前端文件中的静态文案与对应 focused 断言，且按 fail-closed 评审补齐了 Settings checklist heading 的 scoped proof，没有引入 `.closest(...)`、page-global 宽泛命中或额外 runtime hook）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，且 API Keys 可见性仍完全受 `API_KEYS_ROUTE` 与服务端 `/auth/menu` 真值 gating 控制，未放宽 auth/session/role 边界）；测试/可靠性 PASS（focused Vitest、全量 web Vitest、前端 build、Go tests、compose config、容器重建与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` / chunk size warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯前端 copy/schema 收口，不新增请求、状态分支或运行时容器，仅把两个首轮入口表面对齐到同一 canonical route title，运维侧健康检查与 SPA 路由可达性继续稳定）。

### 最新执行检查点（2026-05-08，Login auth shell copy tightening / single-shell wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，优先继续把 `LoginPage` 右侧 auth shell 的文案从“说明式承诺”收敛为更接近 new-api 控制台入口的短句合同；本次只做一个小而完整的登录页垂直切片：把 login helper 从“使用统一控制台访问用户、供应商与管理员能力。”收紧为“登录后按角色展开工作区，无需切换后台。”，把 register helper / banner 从偏强承诺的“完成首个接入动作 / 不会跳转到独立新手页”收敛为更贴近真实产品合同的“注册后直接进入共享控制台，并在同一套导航中继续接入。”与“首轮路径会继续带你完成项目市场 → 订单中心 → API Keys。”，继续保持“登录前不预设独立后台、登录后由共享壳 + 角色菜单展开能力”的 single-shell 叙事。
- 控制器按 TDD 先从 `web/src/pages/LoginPage.test.tsx` 写出 RED：新增对 login helper、register helper 与 auth guidance banner 文案的 focused 合同，同时保留 `tablist` 模式切换、注册 CTA、注册成功进入 shared shell 等既有行为真值不变；首次 focused 失败后，仅最小修改 `web/src/pages/LoginPage.tsx` 的 `modeCopy` 与 login/register banner 文案完成 GREEN。随后产品/规格评审 fail-closed 指出首版 register helper“并沿同一路径完成首个接入动作”与 banner“首轮路径会继续带你完成...”组合起来偏像强制引导流程、强于当前实际合同；控制器据此同轮把 helper 软化为“并在同一套导航中继续接入”，避免过度承诺不存在的硬编码 workflow，再复验通过。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（1 file / 5 tests）；随后 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（2 files / 32 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web gateway` 后 `docker compose ps web api gateway` 显示 `web` / `api` / `gateway` healthy。真实 API 方面重跑 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/app-login-register-scope/20260508-093207`；再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200，管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 继续健康；静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，产物已落盘 `.hermes/local-output/login-shell-copy-tightening-static-scan.json`。
- 本轮五维评审结论：产品/规格 PASS（在根据 fail-closed 评审意见软化 register helper 后，登录壳文案已更贴近 single-shell 真实合同，不再把 shared-console 首轮引导表述成强制 workflow，也未引入独立新手页或多后台叙事）；代码质量 APPROVED（运行时代码仅集中修改 `LoginPage.tsx` 文案与 `LoginPage.test.tsx` 对应断言，diff 仅 2 个文件，测试仍真实保护本轮改动合同）；安全/集成 SAFE（未改登录/注册 API、session 持久化、`/auth/menu` 真值、ProtectedRoute / role-route 边界或任何后端契约，真实 API 回放继续健康）；测试/可靠性 PASS（focused Vitest、`LoginPage.test.tsx + App.test.tsx` 组合 Vitest、前端 build、Go tests、compose config、容器重建与真实 API 回放全部通过，既有 React `act(...)` / Semi UI `findDOMNode` 警告仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯前端 copy 收敛，不新增请求、状态分支、数据库负载或部署复杂度；评审明细已落盘 `.hermes/local-output/login-shell-copy-tightening-five-review-20260508.md`）。下一步继续优先扫描 shared-console 前端中仍残留的低信号说明 copy / route-stub / role-aware 标题漂移，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-08，Login 首轮接入路径 product-style runway 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，优先继续把 `LoginPage` 收敛为更接近 Linear / new-api 的统一控制台入口；本次只针对左侧 `login-register-journey` 做一个小而完整的垂直切片：把原先仍偏说明式的三张注册 runway 卡改为更 product-style 的三步入口 `STEP 01/02/03`，并补齐更清晰的 eyebrow + description，使 `创建 Key → 配置 Webhook → 对照文档` 既保留真实共享接入路径，又比上一轮更像登录后单壳控制台的首轮操作条而不是营销说明区。
- 控制器按 TDD 先从 `web/src/pages/LoginPage.test.tsx` 写出 RED：先把 focused proof 从旧的 shortened registration journey 收紧为 product-style runway 合同，显式要求 `STEP 01/02/03`、`最小权限起步`、`生成首个最小权限 API Key。`、`先确认回调地址，再补投递验证。`、`文档核对` 与 `回到同一控制台核对请求契约。` 命中，并否定旧说明句 `生成首个 API Key。`、`补齐回调地址并发起一次联调。`、`回到文档核对请求契约。`；首次 focused 失败后，再按 `systematic-debugging` 回读 `LoginPage.tsx`，确认根因是 `integrationRunway` 仍只有 title + description 的低信号说明结构。随后仅最小修改运行时代码：为 `integrationRunway` 增加 `step` / `eyebrow` 字段，并把三张卡片收口为更接近 Linear/new-api 的 step label + title + eyebrow + concise description 结构。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（1 file / 5 tests）；随后 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（2 files / 32 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web gateway` 后 `docker compose ps web api gateway` 显示 `web` / `api` / `gateway` healthy。真实 API 方面重跑 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/app-login-register-scope/20260508-085503`；再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200，管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 继续健康。
- 本轮五维评审结论：产品/规格 PASS（runway 已更像 shared-console/new-api 的 product-style 首轮入口，且在 reviewer 指出“同一壳 / endpoint / delivery”措辞偏内部后，控制器已同轮收敛为更直白的用户向文案，并未引入独立新手页或多后台分叉）；代码质量 APPROVED（运行时代码仅集中修改 `LoginPage.tsx` 的 `integrationRunway` 数据与三张卡片内部结构，测试同步更新 `LoginPage.test.tsx`，无 `.closest(...)`、page-global 宽泛查询或额外 runtime hook；非阻塞建议仅为后续继续降低 copy 级断言耦合度）；安全/集成 SAFE（未改登录/注册 API、session 持久化、`/auth/menu` 真值、ProtectedRoute / role-route 边界，controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，真实 API 回放继续健康）；测试/可靠性 PASS（focused Vitest、`LoginPage.test.tsx + App.test.tsx` 组合 Vitest、前端 build、Go tests、compose config、容器重建与真实 API 回放全部通过，既有 React `act(...)` / Semi UI `findDOMNode` 警告仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅增加少量静态文案字段、Typography 节点与轻量 inset shadow，无新请求、effect、状态分支或部署配置变化；评审明细已落盘 `.hermes/local-output/login-runway-review-20260508.md`）。

### 最新执行检查点（2026-05-08，SettingsPage shortcut lane canonical CTA / role-aware Webhook 标题收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `SettingsPage` 做一个小而完整的 shared-console focused 收口切片；除保持既有 `settings-mission-cards` canonical new-api mission cards 外，继续把 `settings-shortcut-cards` 运行态快捷入口从低信号的 `API 接入入口 / 管理 API Keys / Webhook 回调工作台 / 打开 Webhook 设置` 收敛为与仓库 shared-console canonical identity 一致的快捷入口：`开发者 API 接入工作台` / `打开 API Keys`，以及按角色区分的 Webhook identity（user=`开发者 Webhook 接入工作台`、supplier=`供给事件回调工作台`、admin=`Webhook 运维与回调观测`）统一配合 CTA `继续配置 Webhook`；同时移除管理员视角中与 common shortcut 重复的 `Webhook 观测` 卡，避免在同一设置页出现双 webhook 入口或双标题漂移，继续保持“设置中心里的接入链路仍位于同一登录后深色控制台内，而不是拆出独立后台”。
- 控制器按 TDD 先从 `web/src/pages/SettingsPage.test.tsx` 写出 RED：新增 regular-user / supplier / admin 三组 shortcut-lane focused proof，显式要求 `settings-shortcut-cards` 内命中新 canonical heading / CTA，并否定旧按钮 `管理 API Keys` / `打开 Webhook 设置` 与管理员重复 webhook 标题；首次 focused 失败后，再按 `systematic-debugging` 回读 `SettingsPage.tsx`，确认根因是 `shortcuts` 仍保留旧的 `API 接入入口` / `Webhook 回调工作台` / `管理 API Keys` / `打开 Webhook 设置` 合同，且 admin 分支还额外拼接了一张重复 `Webhook 观测` 卡。随后仅最小修改运行时代码：新增 `webhookShortcutTitle(role)` helper，统一 common shortcut 中 API Keys / Webhooks 的 title/button/tag/description，并移除 admin 分支重复 webhook shortcut；期间五维评审进一步指出 mission cards 与 shortcut lane 的 webhook identity 应保持 role-aware 一致、以及测试不应自复制 role-title switch，控制器同轮把 `sharedMissionCards` 改为按 role 生成，并让 test route stub 改用 `resolveRouteTitle(WEBHOOKS_ROUTE, role)` 完成 GREEN。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 通过（1 file / 5 tests）；随后第二次 `pnpm --dir web exec vitest run` 全量前端测试通过（26 files / 201 tests）。首轮全量回归仅在仓库既有 `SupplierResourcesPage.test.tsx > submits supplier domain, account, and mailbox actions then reloads overview data` 出现 5s timeout；控制器按 fail-closed 立即隔离重跑该单测并通过，再完整复跑全量 suite 恢复全绿，因此将其判定为历史 flake 而非本轮回归。其余验证全部通过：`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`；`docker compose up -d --build web api gateway` 后 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。真实 API 方面新增并重跑 `.hermes/local-output/run_settings_shortcuts_real_api.sh`，最新产物目录为 `.hermes/local-output/settings-shortcuts-real-api/20260508-080109`：再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均返回 200 且菜单继续包含 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`；supplier/admin 登录后 `/auth/menu` 分别继续包含 supplier/admin 角色专属路径与 shared routes；前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs` SPA 壳全部返回 200。
- 本轮五维评审结论：产品/规格 PASS（Settings shortcut lane 已与 canonical CTA / role-aware Webhook identity 合同对齐，不再让 `API 接入入口`、`管理 API Keys`、`Webhook 回调工作台`、`打开 Webhook 设置` 这类低信号标签或 admin 重复卡片混入设置中心，也未引入独立后台）；代码质量 APPROVED（运行时代码仅集中修改 `SettingsPage.tsx` 的 shortcut schema、role-aware helper 与 mission-card role 化生成，测试同步改为 scoped shortcut-lane / route-stub 合同；经复审已消除 mission cards 与 shortcut lane 对同一路径的标题漂移，并让测试直接复用 `resolveRouteTitle(...)` 而非再复制一份 switch）；安全/集成 SAFE（shortcut / mission card 仍完全受 `menuHasPath(menu, path)` gating 控制，未放宽 `/auth/menu` 真值、角色边界或任何 Phase 4/5 后端契约；controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，真实 user/supplier/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、全量 web Vitest、前端 build、Go tests、compose config、容器重建与真实 API 回放全部通过；新增 admin duplicate-CTA 计数断言与 supplier/admin role-aware route stub 落点 proof，且对首轮全量 suite 暴露的既有 SupplierResources flake 已做隔离复验，不将历史异步噪声误判为本轮 blocker）；性能/运维 PASS（纯前端 shortcut 文案/入口收敛与小规模 role-aware branching，不新增请求、轮询、数据库负载或部署复杂度；`pnpm build` chunk >500kB warning 仍属仓库既有非阻塞项，本轮未恶化；评审明细已落盘 `.hermes/local-output/settings-shortcuts-review-20260508.md`）。下一步继续优先扫描 shared-console 前端中仍残留的低信号 CTA / route-stub / role-aware 标题漂移，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-08，ApiDocs shared-console canonical CTA / role-aware heading 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `ApiDocsPage` 做一个小而完整的 shared-console focused 收口切片；具体把 docs 页 bridge / loop 中仍残留的低信号 CTA 与标题 `打开 API Keys 工作台` / `打开 Webhook 设置` / `收敛最小权限 API Key` / `先回到 API Keys 收口最小权限` 等旧术语，统一收敛为与仓库 shared-console canonical identity 一致的按钮与标题：`开发者 API 接入工作台` / `打开 API Keys`、以及按角色区分的 Webhook canonical title（user=`开发者 Webhook 接入工作台`、supplier=`供给事件回调工作台`、admin=`Webhook 运维与回调观测`）配合 CTA `继续配置 Webhook`；同时补齐 fallback 场景对新文案泄露的负合同，继续保持“文档 → API Keys / Webhooks / 业务工作台”全部位于同一登录后深色控制台单壳内，而不是退回旧按钮命名或页面身份漂移。
- 控制器按 TDD 先从 `web/src/pages/ApiDocsPage.test.tsx` 写出 RED：先把 docs bridge / loop proof 收紧为命中新 canonical heading 与 CTA，并显式否定旧按钮 `打开 API Keys 工作台` / `打开 Webhook 设置`；首次 focused 失败后，再按 `systematic-debugging` 回读 `ApiDocsPage.tsx`，确认根因是 bridge / loop schema 仍保留旧标题与 CTA 文案。随后仅最小修改运行时代码：将两组 schema 收敛为 canonical 标题/按钮，并在 controller-side 五维评审指出“Webhook 标题被硬编码成普通用户标题、fallback 仍未否定新 CTA 泄露”后，同轮继续补齐 `webhookConsoleTitle(role)` 角色分支与 fallback 新文案负断言，最终完成 GREEN。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 通过（1 file / 5 tests）；随后 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/App.test.tsx` 通过（2 files / 32 tests）；`cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 199 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 后 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。真实 API 方面重跑 `.hermes/local-output/run_route_heading_canonical_real_api.sh`，最新产物目录为 `.hermes/local-output/route-heading-canonical-real-api/20260508-064616`；再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均返回 200，管理员登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 均返回 200，前端 `/`、`/api-keys`、`/webhooks`、`/docs`、`/settings` SPA 壳继续返回 200。
- 本轮五维评审结论：产品/规格 PASS（docs bridge / loop 已与 canonical CTA / role-aware heading 合同对齐，不再把旧低信号按钮或普通用户 Webhook 标题错误投射到 supplier/admin 视角，也未引入独立后台）；代码质量 APPROVED（运行时代码仅集中修改 `ApiDocsPage.tsx` 两组静态 schema 与 `useMemo` 取值点，测试同步收敛为 scoped heading/button 合同，无 `.closest(...)`、page-global 宽泛查询或额外 runtime hook；评审明细已落盘 `.hermes/local-output/apidocs-canonical-cta-review-20260508.md`）；安全/集成 SAFE（controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，且未改路由常量、鉴权、iframe 源、菜单 gating 或任何 Phase 4/5 后端契约；真实 API 回放继续健康）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、全量 web Vitest、前端 build、Go tests、compose config、容器重建与真实 API 回放全部通过；已补齐 fallback 对新 CTA 文案泄露的负合同，避免测试虚假通过）；性能/运维 PASS（仅静态 schema/copy 与测试合同收口，不引入新的请求、状态轮询、数据库负担或部署复杂度；当前 `pnpm build` chunk warning 仍属仓库既有非阻塞项，本轮未恶化）。下一步继续优先清理 shared-console 前端中仍残留的低信号 CTA / route-stub / role-aware 标题漂移，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-08，SupplierOfferings shared-console bridge 去除残留路径标签按钮行）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `SupplierOfferingsPage` 做一个小而完整的 shared-console focused 收口补丁；具体针对上一轮 bridge 已升级为 new-api CTA 卡片后仍残留的第二排路径标签按钮 `API Keys · /api-keys`、`供给事件回调工作台 · /webhooks`、`API 文档与接入控制台 · /docs`，继续收敛为只保留三张 canonical CTA 卡片 `开发者 API 接入工作台` / `打开 API Keys`、`供给事件回调工作台` / `继续配置 Webhook`、`API 文档与接入控制台` / `查看 API 文档`，避免 supplier 供货规则页再次回退到“卡片 + 路径按钮双轨并存”的低信号 bridge 合同，继续保持“供货编排 → 接入联调 → 文档对照”全部位于同一登录后深色控制台内，而不是拆出独立后台。
- 控制器按 TDD 先从 `web/src/pages/SupplierOfferingsPage.test.tsx` 写出 RED：在既有 bridge scoped proof 基础上，新增 3 条负断言，显式要求 `supplier-offerings-shared-console-bridge` 内不再出现 `API Keys · /api-keys`、`供给事件回调工作台 · /webhooks`、`API 文档与接入控制台 · /docs` 这组遗留路径标签文本；首次 focused 失败后，再按 `systematic-debugging` 回读 `SupplierOfferingsPage.tsx`，确认根因是 bridge card 区下方仍保留一整排旧按钮 `<Space wrap spacing={12}>...</Space>`，导致页面同时暴露“canonical CTA 卡片 + 被动路径标签按钮”两套入口，而不是菜单 gating、路由常量或鉴权状态问题。随后仅最小修改运行时代码：删除这排冗余路径按钮，保留上方 `consolePillars` CTA 卡片与既有 fallback 逻辑不变；测试侧继续保持 scoped heading + CTA 合同，并新增路径标签消失 proof，防止同类冗余入口回归。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx` 通过（1 file / 6 tests）；随后 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/App.test.tsx` 通过（2 files / 33 tests）；`cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 199 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 成功且 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。真实 API 方面重跑 `.hermes/local-output/run_supplier_offerings_route_stub_real_api.sh`，最新产物目录为 `.hermes/local-output/supplier-offerings-route-stub-real-api/20260508-061419`：再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 返回 200 且不暴露 supplier/admin 路径；供应商登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements` 返回 200，且菜单继续包含 `/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs`；管理员登录后 `/auth/menu`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `/`、`/supplier/offerings`、`/api-keys`、`/webhooks`、`/docs` SPA 壳全部返回 200。
- 本轮五维评审结论：产品/规格 PASS（SupplierOfferings bridge 已彻底收敛为 single-console canonical CTA 卡片，不再保留 path-label 第二入口，也未引入独立供应商后台）；代码质量 APPROVED（运行时代码仅删除 bridge 区 1 组冗余按钮行，测试同步补齐路径标签消失合同；无 `.closest(...)`、page-global 宽泛查询或额外 runtime hook；评审明细已落盘 `.hermes/local-output/supplier-offerings-bridge-review-20260508.md`）；安全/集成 SAFE（bridge CTA 仍完全受 `hasMenuPath(menu, path)` gating 控制，未放宽 `/auth/menu` 真值、supplier/admin 权限边界或任何 Phase 4/5 后端契约；controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式）；测试/可靠性 PASS（focused Vitest、`SupplierOfferingsPage.test.tsx + App.test.tsx`、全量 web Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；全量 suite 仍仅保留仓库既有 React `act(...)` / Semi UI `findDOMNode` / form validation 警告，本轮未恶化）；性能/运维 PASS（本轮只删除冗余前端 bridge DOM，不增加新请求、轮询、后端负载或部署复杂度；重建后 `web` / `api` / `gateway` 持续 healthy）。下一步继续优先扫描 shared-console 前端中仍残留的类似“canonical CTA 已存在但路径标签/冗余第二入口未删净”的切片，再回到 Phase 4/5 余下后端能力。


### 最新执行检查点（2026-05-08，SettingsPage canonical new-api mission cards 对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `SettingsPage` 做一个小而完整的 shared-console focused 收口切片；具体把设置页 `集成任务流` 中仍残留的旧文案卡片 `先完成 API 密钥发放` / `继续联调回调投递` / `最后回到 API 文档`，统一收敛为与仓库 single-console canonical identity 一致的 new-api mission cards：`开发者 API 接入工作台` / `打开 API Keys`、`开发者 Webhook 接入工作台` / `继续配置 Webhook`、`API 文档与接入控制台` / `查看 API 文档`，继续保持设置中心里的接入链路位于同一登录后深色控制台内，而不是退回旧的低信号任务文案或拆出独立后台。
- 控制器按 TDD 先从 `web/src/pages/SettingsPage.test.tsx` 写出 RED：先把 regular-user 设置页 proof 从命中旧 heading / CTA 文案，收紧为 `settings-mission-cards` 区域内同时要求命中新 canonical heading `开发者 API 接入工作台` / `开发者 Webhook 接入工作台` / `API 文档与接入控制台` 与新 CTA `打开 API Keys` / `继续配置 Webhook` / `查看 API 文档`，并显式否定旧按钮 `打开 Webhook 设置` / `打开 API 文档`；首次 focused 失败后，再按 `systematic-debugging` 回读 `SettingsPage.tsx`，确认根因是 `sharedMissionCards` 仍保留旧的标题、tag 与 CTA 文案，而不是菜单 gating、路由常量或认证状态问题。随后仅最小修改运行时代码：保持 `menuHasPath(menu, item.path)` 与既有 `navigate(item.path)` 结构不变，只同步更新 3 张 mission card 的 `title` / `description` / `button` / `tag` 文案，并让 focused 测试验证 scoped heading + CTA + Docs click-through 合同。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 通过（1 file / 3 tests）；随后 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/App.test.tsx` 通过（2 files / 30 tests）；`cd web && pnpm exec vitest run` 全量前端测试通过（日志中仅保留仓库既有 React `act(...)` / Semi UI `findDOMNode` 警告，本轮未新增 blocker）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面重跑 `.hermes/local-output/run_settings_front_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/settings-front-scope/20260508-054909`：再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；新注册普通用户 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 返回 200，且菜单继续包含 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；管理员登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs` SPA 壳全部返回 200。
- 本轮五维评审结论：产品/规格 PASS（SettingsPage mission cards 已与 new-api 单壳 canonical identity 对齐，不再让旧任务式文案冒充真实落点页面身份，也未引入独立后台）；代码质量 APPROVED（运行时代码仅集中修改 `sharedMissionCards` 文案数据与对应 focused test 合同，scoped role 查询真实保护新标题/CTA 合同，无 `.closest(...)` 或 page-global 宽泛命中）；安全/集成 SAFE（mission cards 仍完全受 `menuHasPath(menu, item.path)` gating 控制，未放宽 `/auth/menu` 真值、角色边界或任何 Phase 4/5 后端契约；controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、全量 web Vitest、前端 build、Go tests、compose config 与真实 API 回放全部通过；全量 Vitest 中仅存在仓库既有警告，本轮未恶化）；性能/运维 PASS（纯前端文案/测试合同收口，无新增请求、状态源、构建/部署风险或运行时负担）。

### 最新执行检查点（2026-05-08，Profile shared-console bridge canonical heading / CTA 对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `ProfilePage` 做一个小而完整的 shared-console focused 收口切片；具体把个人资料页 `控制台桥接能力` 区域中的 capability card 从旧的低信号 `API Keys` / `回调联调入口` / `API 文档入口` 标题与 `打开 Webhook 设置` / `打开 API 文档` 按钮，统一收敛为与仓库共享控制台 canonical identity 一致的 new-api CTA：`开发者 API 接入工作台` / `打开 API Keys`、`开发者 Webhook 接入工作台` / `继续配置 Webhook`、`API 文档与接入控制台` / `查看 API 文档`，继续保持“账号中枢 → 接入桥接 → 角色扩展”全部位于同一登录后深色单壳控制台内，而不是回退到菜单短标题或拆出独立接入后台。
- 控制器按 TDD 先从 `web/src/pages/ProfilePage.test.tsx` 写出 RED：先把 `profile-capability-bridge` 区域 proof 从只命中旧按钮文案，收紧为同时要求命中新 canonical heading `开发者 API 接入工作台` / `开发者 Webhook 接入工作台` / `API 文档与接入控制台`，并把 Webhook / Docs 点击动作从旧的 `打开 Webhook 设置` / `打开 API 文档` 收敛为 `继续配置 Webhook` / `查看 API 文档`；首次 focused 失败后，再按 `systematic-debugging` 回读 `ProfilePage.tsx`，确认根因是 `capabilityCards` 仍保留旧标题与旧 CTA 文案，而不是菜单 gating、路由常量或鉴权状态问题。随后仅最小修改运行时代码：保持 `hasMenuPath(menu, ...)` 与既有 `navigate(item.path)` 结构不变，只同步更新 3 张 capability card 的 `title` / `description` / `buttonText`，再让 focused 测试验证 scoped heading + CTA 存在性与 Webhook/Docs click-through 合同。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 通过（1 file / 7 tests）；随后 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/App.test.tsx` 通过（2 files / 34 tests）；`cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 199 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 成功且 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。真实 API 方面重跑 `.hermes/local-output/run_profile_capability_canonical_heading_real_api.sh`，最新产物目录为 `.hermes/local-output/profile-capability-canonical-heading/20260508-051712`：再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；新注册普通用户 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 返回 200，且菜单继续包含 `/profile`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 而不暴露 `/admin/risk`、`/supplier/domains`；管理员登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `/`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/settings`、`/admin/risk`、`/supplier/domains` SPA 壳均返回 200。
- 本轮五维评审结论：产品/规格 PASS（Profile bridge 已与共享控制台 canonical page identity 对齐，不再让 `API Keys` / `回调联调入口` / `API 文档入口` 这类低信号标题冒充落点页面身份，也未引入独立后台）；代码质量 APPROVED（运行时代码仅集中修改 `capabilityCards` 文案与对应 focused test 合同；一条 reviewer 指出 route-stub 不能单独证明目标页真实 copy，控制器据本轮 `App.test.tsx` 组合回归、全量 Vitest、真实 API 与 SPA 可达性复核将其降为非阻塞 follow-up，并保持当前 focused proof 仍真实保护 Profile 页 bridge 自身合同）；安全/集成 SAFE（`hasMenuPath(menu, ...)` 仍是入口显隐真值，未放宽 `/auth/menu`、角色边界或任何 Phase 4/5 后端契约；controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式）；测试/可靠性 PASS（focused Vitest、`ProfilePage.test.tsx + App.test.tsx`、全量 web Vitest、前端 build、Go tests、compose config、容器重建与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` 告警仍属仓库历史噪声，本轮未恶化）；性能/运维 PASS（纯前端文案/测试合同收口，无新增请求、状态复杂度、构建/部署风险或运行时负担；评审明细已落盘 `.hermes/local-output/profile-bridge-canonical-review-20260508.md`）。

### 最新执行检查点（2026-05-08，SupplierDomains shared-console bridge canonical CTA 与 route-heading 对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `SupplierDomainsPage` 做一个小而完整的 shared-console focused 收口切片；具体把右侧 bridge 从旧的低信号 `API Keys · /api-keys`、`Webhook 设置 · /webhooks`、`API 文档 · /docs`、`供应商结算 · /supplier/settlements` 标签式入口，统一收敛为与仓库其他 supplier/admin 页一致的 new-api canonical CTA：`开发者 API 接入工作台` / `打开 API Keys`、`供给事件回调工作台` / `继续配置 Webhook`、`API 文档与接入控制台` / `查看 API 文档`、`供应商资金与争议指挥台` / `打开供应商结算`，继续保持“同一登录后控制台内完成域名运营 → 接入联动 → 资金复盘”，而不是回退到路径标签或拆分第二套供应商后台。
- 控制器按 TDD 先从 `web/src/pages/SupplierDomainsPage.test.tsx` 写出 RED：先把 `/webhooks` 与 `/docs` route-stub heading 从旧的 `Webhook 设置` / `API 文档` 收紧为 supplier canonical heading `供给事件回调工作台` / `API 文档与接入控制台`，再把 bridge proof 从旧的 `打开 <label+path>` 收紧为 4 个真实 CTA 文案与逐个 click-through 导航合同；首次 focused 失败后，再按 `systematic-debugging` 回读运行时代码，确认根因是 `SupplierDomainsPage.tsx` 的 `visibleBridgeLinks` 仍把长标题和 path 拼成单个 label，并复用为按钮 accessible-name，导致 bridge 仍停留在“路径标签式入口”而非 canonical CTA。随后仅最小修改运行时代码：把 `visibleBridgeLinks` 重构为 `label + button + summary + path`，并把 bridge 按钮统一改为 `item.button`；再同步最小更新 focused route-stub、存在性断言、隐藏态断言与 Webhook/Docs/Settlements 导航 proof。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx` 通过（1 file / 7 tests）；随后 `pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx src/App.test.tsx` 通过（2 files / 34 tests）；`cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 199 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 后 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。真实 API 方面重跑 `.hermes/local-output/run_supplier_domains_scoped_navigation_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-domains-scoped-navigation-20260508-044808` 再次验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通注册用户 `POST /api/v1/auth/register` 返回 201，后续 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/projects/inventory`、`/api/v1/orders/activations`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200，而 `GET /api/v1/supplier/resources/overview` 继续返回 403；前端 `/supplier/domains` SPA shell 返回 200。
- 本轮五维评审结论：产品/规格 PASS（SupplierDomains bridge 已与 single-console canonical CTA / route-heading 合同对齐，不再让 path label 冒充真实页面 identity，也未引入独立供应商后台）；代码质量 APPROVED（运行时代码仅集中修改 `visibleBridgeLinks` schema 与按钮文案，测试同步覆盖 4 个 CTA 存在性、Webhooks/Docs/Settlements click-through 与隐藏态，没有引入 `.closest(...)`、page-global 宽泛查询或无意义 test hook）；安全/集成 SAFE（CTA 仍完全受 `hasMenuPath(menu, ...)` gating 控制，未放宽 `/auth/menu` 真值、supplier 权限边界或任何 Phase 4/5 后端契约；controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、全量 web Vitest、前端 build、Go tests、compose config、容器重建与真实 API 回放全部通过；既有 React `act(...)` / Semi UI warning 仍属历史噪声，本轮未恶化）；性能/运维 PASS（纯前端 CTA/heading 收口，无新增请求、effect、状态源或 compose/runtime 风险；评审明细已落盘 `.hermes/local-output/supplier-domains-canonical-cta-review-20260508.md`）。

### 最新执行检查点（2026-05-08，SupplierResources shared-console bridge 从静态路径标签收口为 new-api CTA 卡片）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `SupplierResourcesPage` 做一个小而完整的 shared-console focused 收口切片；具体把供应商资源页右侧 `共享控制台联动` 区域从“静态路径标签卡片 + 独立按钮行”的双轨合同，收敛为更贴近 `new-api` 单壳接入台的三张 CTA 卡片：`开发者 API 接入工作台`、`供给事件回调工作台`、`API 文档与接入控制台`；每张卡片都直接暴露真实动作按钮 `打开 API Keys`、`继续配置 Webhook`、`查看 API 文档`，让供应商在同一登录后控制台内完成“资源准备 → 接入联调 → 文档对照”，而不是停留在静态路径说明或拆出额外后台。
- 控制器按 TDD 先从 `web/src/pages/SupplierResourcesPage.test.tsx` 写出 RED：先把 bridge proof 从 `supplier-resources-bridge-api-keys/webhooks/docs` 的静态路径标签命中，收紧为 `supplier-resources-shared-console-bridge` 区域内的真实按钮合同与 click-through 导航证明；首次 focused 失败后，再按 `systematic-debugging` 回读运行时代码，确认根因是 `SupplierResourcesPage.tsx` 仍将 bridge 渲染为说明卡 + 按钮行分离结构，测试容器本身并不拥有 CTA。随后仅最小修改运行时代码：把 shared-console bridge 卡片整体升级为容器级 CTA 区，在 `canOpenApiKeys/canOpenWebhooks/canOpenDocs` gating 下直接渲染三张 route-aware CTA 子卡，并保留现有 supplier fallback 语义与资源表单/真实接口不变；测试侧同步收敛为 scoped button 查询与 route-stub canonical heading 合同，避免继续依赖静态路径标签文本。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx` 通过（1 file / 11 tests）；随后 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/App.test.tsx` 通过（2 files / 38 tests）；`cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 199 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 后 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。真实 API 方面重跑 `.hermes/local-output/run_app_login_register_scope_real_api.sh` 与 `.hermes/local-output/run_supplier_resources_card_scope_real_api.sh`，最新产物目录分别为 `.hermes/local-output/app-login-register-scope/20260508-040123` 与 `.hermes/local-output/supplier-resources-card-scope-real-api/20260508-040124`：再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 返回 200；供应商登录后 `/auth/menu`、`/supplier/resources/overview` 返回 200，且菜单继续包含 `/supplier/resources`、`/api-keys`、`/webhooks`、`/docs`；脚本还实际创建并校验了新的域名、第三方账号与邮箱池记录，确认资源运营真实 API 与 shared-console 接入入口未被本轮前端收口破坏。
- 本轮五维评审结论：产品/规格 PASS（SupplierResources bridge 已从静态路径标签升级为真实 single-console CTA 卡片，且落点 identity 收敛到 API Keys / Webhooks / Docs 的 canonical 页面，不引入独立供应商后台）；代码质量 APPROVED（运行时代码仅集中修改 `SupplierResourcesPage.tsx` bridge 容器，测试同步切换到 scoped button + route-heading 合同，没有引入 `.closest(...)`、page-global 宽泛查询或无意义 test hook；相关五维评审明细已落盘到本地忽略文件 `.hermes/local-output/supplier-resources-bridge-review-20260508.md`）；安全/集成 SAFE（CTA 仍完全受 `canOpenApiKeys/canOpenWebhooks/canOpenDocs` gating 控制，未放宽 `/auth/menu` 真值、supplier/admin 权限边界或任何 Phase 4/5 后端契约；controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，真实 user/supplier/admin 回放继续健康）；测试/可靠性 PASS（focused Vitest、`SupplierResourcesPage.test.tsx + App.test.tsx`、全量 web Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 Semi UI / React `findDOMNode` 与 `act(...)` 告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（本轮只把前端 bridge 收口为本地导航 CTA 卡片，不增加新请求、轮询、后端负载或部署复杂度，且重建后 `web` / `api` / `gateway` 持续 healthy）。下一步继续优先清理 shared-console 前端中仍残留的低信号 bridge / capability card / route-stub 合同，再回到 Phase 4/5 余下后端切片。

### 最新执行检查点（2026-05-08，SupplierOfferings shared-console bridge 从被动路径标签升级为 new-api CTA 卡片）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `SupplierOfferingsPage` 做一个小而完整的 shared-console focused 收口切片；具体把供应商供货规则页 `共享控制台联动` 区域中原本仍残留的被动路径按钮/标签合同，收敛为更贴近 `new-api` 单壳接入台的三张 CTA 卡片：`开发者 API 接入工作台`、`供给事件回调工作台`、`API 文档与接入控制台`；每张卡片都在说明文案下直接暴露真实动作按钮 `打开 API Keys`、`继续配置 Webhook`、`查看 API 文档`，让供应商在同一登录后控制台内完成“供货编排 → 接入联调 → 文档对照”，而不是停留在静态说明或拆出额外后台。
- 控制器按 TDD 先从 `web/src/pages/SupplierOfferingsPage.test.tsx` 写出 RED：先把 bridge proof 从旧的 `API Keys · /api-keys` / `... · /webhooks` / `... · /docs` 文本合同收紧为三张真实 bridge pillar card 内的 CTA 合同，并显式要求 `SupplierOfferingsPage` 的 mission-flow 与 shared-console bridge 在 duplicate CTA label 场景下仍保持作用域明确；首次 focused 失败后，再按 `systematic-debugging` 回读运行时代码，确认根因是 `SupplierOfferingsPage.tsx` 的 `consolePillars` 仍是三张说明卡，没有真实 CTA，也没有和 menu gating 对齐的 route-level pillar schema。随后仅最小修改运行时代码：把 `consolePillars` 重构为携带 `icon/button/path` 的 route-aware pillar schema，在 bridge 区域按 `canOpenApiKeys/canOpenWebhooks/canOpenDocs` 过滤后直接渲染 CTA 卡片，同时保留既有 fallback 卡与 supplier 主任务流不变；测试侧同步把 bridge 断言切换到 `supplier-offerings-console-pillar-api/webhooks/docs`，并把 mission-flow 的 `打开 API Keys` 点击改为 `within(mission-flow)` 作用域查询，避免与新 bridge CTA 产生歧义多命中。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx` 通过（1 file / 6 tests）；随后 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/App.test.tsx` 通过（2 files / 33 tests）；`cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 199 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 后 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。真实 API 方面两次重跑 `.hermes/local-output/run_supplier_offerings_route_stub_real_api.sh`，最新产物目录分别为 `.hermes/local-output/supplier-offerings-route-stub-real-api/20260508-032610` 与 `.hermes/local-output/supplier-offerings-route-stub-real-api/20260508-033007`：再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 返回 200 且不暴露 supplier/admin 路径；供应商登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements` 返回 200 且菜单包含 `/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs`；管理员登录后 `/auth/menu`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `/`、`/supplier/offerings`、`/api-keys`、`/webhooks`、`/docs` SPA 壳全部返回 200。
- 本轮五维评审结论：产品/规格 PASS（SupplierOfferings bridge 已从被动路径标签升级为真实 single-console CTA 卡片，且落点 identity 收敛到 API Keys / Webhooks / Docs 的 canonical 页面，不引入独立供应商后台）；代码质量 APPROVED（运行时代码仅集中修改 `SupplierOfferingsPage.tsx` bridge schema 与渲染，测试同步切换到 card-scoped CTA proof，并修复 duplicate-label 场景为 mission-flow 作用域点击，没有引入 `.closest(...)`、page-global 宽泛查询或无意义 test hook）；安全/集成 SAFE（bridge CTA 仍完全受 `hasMenuPath(menu, path)` gating 控制，未放宽 `/auth/menu` 真值、supplier/admin 权限边界或任何 Phase 4/5 后端契约；controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，真实 user/supplier/admin 回放继续健康）；测试/可靠性 PASS（focused Vitest、`SupplierOfferingsPage.test.tsx + App.test.tsx`、全量 web Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；全量 suite 仍有历史 Semi UI / React `act(...)` 噪声但无新增失败）；性能/运维 PASS（本轮只把前端 bridge 说明卡升级为本地导航 CTA，不增加新请求、轮询、后端负载或部署复杂度；Vite chunk warning 仍是历史 follow-up debt，未因本轮恶化）。本轮五维评审明细已落盘到本地忽略文件 `.hermes/local-output/supplier-offerings-bridge-review-20260508.md`。下一步继续优先收敛 shared-console 前端中仍残留的低信号 bridge / capability card / route-stub 合同，再回到 Phase 4/5 余下后端切片。

### 最新执行检查点（2026-05-08，AdminRisk shared-console bridge 从静态标签升级为可导航 CTA）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `AdminRiskPage` 做一个小而完整的 shared-console focused 收口切片；具体把管理员风控页“共享接入桥接”中原本只展示 `API Keys · /api-keys`、`审计日志 · /admin/audit`、`API 文档 · /docs` 的静态标签，升级为同一控制台壳内可直接点击的 CTA：`打开 API Keys`、`继续查看审计`、`查看 API 文档`，让“风控规则调整 → 审计复盘 / 接入验证 / 文档对照”不再停留在说明文案，而是形成真实可走通的单壳导航路径；同时把 `/api-keys` 与 `/docs` 的 focused route-stub heading 收敛到当前真实页面 canonical identity `开发者 API 接入工作台` 与 `API 文档与接入控制台`，继续保持“不拆独立后台，只在同一登录后控制台里按角色组织工作区”的 new-api 方向。
- 控制器按 TDD 先从 `web/src/pages/AdminRiskPage.test.tsx` 写出 RED：先把 bridge proof 从静态 tag 文本命中收紧为 `admin-risk-shared-console-links` 内的真实按钮合同，并新增 bridge click-through 导航证明，显式要求点击 `打开 API Keys`、`继续查看审计`、`查看 API 文档` 后分别命中 `/api-keys`、`/admin/audit`、`/docs` route-stub canonical heading；首次 focused 失败后，再按 `systematic-debugging` 回读运行时代码，确认根因是 `AdminRiskPage.tsx` 仍将 shared-console links 渲染为只读 `Tag` 列表，而不是 bridge CTA。随后仅最小修改 `web/src/pages/AdminRiskPage.tsx`：保留原有 `visibleSharedConsoleLinks` / `canOpen*` gating 与风控后端调用不变，只把 bridge 区域替换为带 `navigate(item.path)` 的 borderless `Button` CTA，并补齐最小语义锚点 `admin-risk-shared-console-links`；没有扩散到鉴权、菜单、规则保存、审计接口或高危动作逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminRiskPage.test.tsx` 通过（1 file / 6 tests）；随后 `pnpm --dir web exec vitest run src/pages/AdminRiskPage.test.tsx src/App.test.tsx` 通过（2 files / 33 tests）；`cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 199 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 成功且 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。真实 API 方面新增并重跑本地回放脚本 `.hermes/local-output/run_admin_risk_bridge_real_api.py`，最新产物目录为 `.hermes/local-output/admin-risk-bridge-real-api/20260508-030119`（`latest.txt` 已同步更新）；再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 返回 200 且菜单不泄露 `/admin/risk`，管理员登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/auth/api-keys` 返回 200 且菜单包含 `/admin/risk` / `/admin/audit` / `/api-keys` / `/docs`，供应商登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 返回 200 且不暴露管理员风控路径；前端 `/login`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/docs` SPA 壳全部返回 200。浏览器侧按 `browser-verification-degradation-fallback` 做本地 QA：fresh `browser_navigate` + `browser_snapshot` 已再次确认 `/login` 仍呈现深色 single-console 统一登录入口结构；但 `browser_vision` 本轮再次命中上游 504，因此未将该浏览器工具噪声误判为应用回归，而改以 fresh snapshot + focused/full Vitest + build/redeploy + real API 回放 + 容器健康作为视觉/运行时验收依据。
- 本轮五维评审结论：产品/规格 PASS（AdminRisk bridge 已从静态说明升级为真实单壳 CTA，且落点 heading 收敛到页面 canonical identity，没有引入独立风控后台或角色叙事漂移）；代码质量 APPROVED（运行时代码仅在 `AdminRiskPage.tsx` 用最小按钮替换静态 tag，测试同步收敛为 scoped button + route-heading 合同，没有引入 `.closest(...)`、page-global 宽泛命中或额外状态分支；diff 仅 2 个前端文件，职责单一）；安全/集成 SAFE（新增 CTA 仍完全受 `visibleSharedConsoleLinks` 与 `canOpenAudit/canOpenApiKeys/canOpenDocs` menu-path gating 控制，未放宽 `/auth/menu` 真值、鉴权边界或风控/审计/API Key 后端契约；controller-side static scan 对 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，真实 user/admin/supplier 回放继续健康）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、全量 web Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` / form validation 告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（运行时仅把 3 个 bridge tag 升级为 3 个本地导航按钮，不引入新请求、轮询、后端负载或部署复杂度；重建后 `web` / `api` / `gateway` 持续 healthy，且本地证据目录已精确记录到 `.hermes/local-output/admin-risk-bridge-real-api/20260508-030119`）。下一步继续优先清理 shared-console 前端中仍残留的低信号 bridge / capability-matrix / route-stub 合同，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-08，Profile 账号中枢补齐 dark single-console capability matrix）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `ProfilePage` 做一个小而完整的 shared-console / new-api 垂直切片；具体把个人资料页右侧“角色扩展说明”从纯说明卡继续收敛成更像统一控制台账号中枢的 dark mission-control 卡：新增 `控制台能力矩阵` 子卡，明确展示 `统一身份入口`、`共享接入桥接`、`角色菜单扩展` 三个稳定能力信号；同时为整张角色扩展卡补齐最小语义锚点 `profile-role-expansion-card`，继续保持“同一登录后单壳控制台，通过角色和菜单扩展能力，而不是拆分冗余后台”的 new-api 方向。
- 控制器按 TDD 先从 `web/src/pages/ProfilePage.test.tsx` 写出 RED：先把用户场景 proof 从只验证 `profile-capability-bridge` 的三个 CTA，补强为同时要求命中新的 `profile-capability-matrix` 与 `profile-role-expansion-card`，并显式验证 `控制台能力矩阵 / 统一身份入口 / 共享接入桥接 / 角色菜单扩展 / 最小权限 / Webhook / API / 单一文档入口` 这些 dark single-console 合同；首次 focused 失败后，再按 `systematic-debugging` 回读运行时代码，确认根因是 `ProfilePage.tsx` 右侧卡片仍缺少独立 capability matrix 容器与整卡语义锚点，而不是路由、菜单或登录态问题。随后仅最小修改运行时代码：为角色扩展卡新增 `data-testid="profile-role-expansion-card"`，并在卡片内部插入一个小型 dark `profile-capability-matrix` 子卡，不扩散到后端、菜单、鉴权或其他页面。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 通过（1 file / 7 tests）；随后 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/App.test.tsx` 通过（2 files / 34 tests）；`cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 198 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 成功且 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。真实 API 方面重跑 `.hermes/local-output/run_profile_shared_console_real_api.sh`，最新产物目录为 `.hermes/local-output/profile-shared-console-fallback-20260508-021049`；再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 返回 200，且菜单路径继续包含 `/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`。浏览器侧按 `browser-verification-degradation-fallback` 做本地 QA：fresh `browser_snapshot` 已再次确认 `/login` 注册态仍呈现单壳入口结构与注册表单合同；但 `browser_vision` 本轮命中上游 504，且无登录浏览器会话下 `/profile` fresh load 会正确回退到 `/login`，因此本轮未将该浏览器工具噪声误判为应用回归，而改以 fresh snapshot + HTTP 200 + focused/full Vitest + real API 回放 + 容器健康作为视觉/运行时验收依据。
- 本轮五维评审结论：产品/规格 PASS（Profile 现已更明确表达“账号中枢 → 接入桥接 → 角色扩展”这一 single-console 叙事，未引入独立后台或角色分叉）；代码质量 APPROVED（运行时代码仅在 `ProfilePage.tsx` 内新增 1 个整卡锚点与 1 个最小 capability matrix 子卡，测试同步验证真实用户可见标签与 CTA 合同，没有引入 `.closest(...)` 或 page-global 宽泛命中）；安全/集成 SAFE（未改 `useAuthStore` 会话模型、`/auth/menu` 真值、路由常量或角色边界；controller-side static scan 对当前 diff 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式，真实注册用户菜单与受保护接口继续健康）；测试/可靠性 PASS（focused Vitest、组合 Vitest、全量 web Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` / chunk 体积告警保持历史水平，未因本轮恶化）；性能/运维 PASS（仅新增轻量前端卡片与标签，不引入新请求、轮询、后端负载或部署复杂度；重建后 `web` / `api` / `gateway` 继续 healthy）。下一步继续优先补齐其余 shared-console 页面中仍缺少 new-api 风格 capability matrix / mission-control 深色账号中枢收口的前端切片，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-08，SupplierSettlements shared-console bridge 从被动标签收口为可导航 CTA）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `SupplierSettlementsPage` 做一个小而完整的 shared-console focused 收口切片；具体把供应商结算页“共享控制台联动”中原本展示 `API Keys · /api-keys`、`供给事件回调工作台 · /webhooks`、`API 文档与接入控制台 · /docs` 的被动标签卡片，升级为同一卡片内部可直接点击的 CTA：`打开 API Keys`、`继续配置 Webhook`、`查看 API 文档`，让“结算 / 争议处理 → 接入验证”不再停留在说明文案，而是继续保持同一登录后单壳控制台内的真实可走通路径；同时把 shared-console fallback 的返回按钮也收拢进 fallback 卡片内部，避免卡片信息与动作分离。
- 控制器按 TDD 先从 `web/src/pages/SupplierSettlementsPage.test.tsx` 写出 RED：先把 bridge proof 从被动文本命中收紧为 `supplier-settlements-shared-console-bridge` 内的真实按钮合同，并新增 bridge click-through 导航证明，显式要求点击 `打开 API Keys`、`继续配置 Webhook`、`查看 API 文档` 后分别命中 `/api-keys`、`/webhooks`、`/docs` route-stub canonical heading；首次 focused 失败后，再按 `systematic-debugging` 回读运行时代码，定位根因是 `SupplierSettlementsPage.tsx` 仍将 shared-console bridge 渲染为纯说明卡片，而真实按钮被放在卡片外部独立按钮行中，与当前页面合同不一致。随后仅最小修改运行时代码：把 CTA 移入每个 bridge card 内部，并把 shared-console fallback 按钮也移入 fallback card；再根据 focused 失败把 fallback 测试收敛为现有按钮级语义锚点 `supplier-settlements-shared-console-fallback-button`，避免重复文本导致的歧义断言。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx` 通过（1 file / 8 tests）；随后 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx src/App.test.tsx` 通过（2 files / 35 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 成功且 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。真实 API 方面重跑 `.hermes/local-output/run_supplier_settlements_milestone_real_api.sh`，再次验证 `GET /healthz` 返回 200，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均返回 200，且普通用户访问 `/api/v1/supplier/resources/overview` 继续返回 403；管理员登录后 `/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/supplier/settlements` 返回 200。前端回归方面，`pnpm --dir web exec vitest run` 全量测试首轮再次命中既有非本轮引入的历史 flaky（`SupplierResourcesPage.test.tsx` 单测超时）；控制器按 fail-closed 立即隔离复现：单独运行该失败用例通过，再执行 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx src/App.test.tsx src/pages/SupplierResourcesPage.test.tsx` 组合回归通过（3 files / 46 tests），据此将首轮全量失败判定为历史异步噪声而非本轮回归。
- 本轮五维评审结论：产品/规格 PASS（bridge 已从被动说明升级为真实单壳 CTA，且 fallback 动作收拢回所属卡片，没有引入独立后台或角色叙事漂移）；代码质量 APPROVED（运行时代码仅在 `SupplierSettlementsPage.tsx` 内做最小 DOM 结构收口：把既有按钮移入 bridge / fallback card 并保留现有 menu-path gating；测试同步收敛为 scoped button + route-heading 合同，没有引入 `.closest(...)` 或 page-global 宽泛命中）；安全/集成 SAFE（新增 CTA 仍完全受 `visibleConsolePillars` 与 `hasMenuPath(...)` 控制，shared-console fallback 也未泄露未授权入口；controller-side static scan 0 命中，真实 user/admin API 回放继续健康，普通用户 supplier 资源接口仍为 403）；测试/可靠性 PASS（focused Vitest、组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；全量前端首轮失败已 controller-side 隔离复验并确认属于既有 flake，而非当前 diff 引入）；性能/运维 PASS（本轮仅调整前端卡片内部按钮布局，不增加请求、轮询、后端负载或部署复杂度；构建后 chunk 体积警告保持历史水平、未因本轮切片恶化）。尝试使用 delegate reviewer 做五维评审时再次出现跨仓漂移到错误仓库，相关结果已按 fail-closed 全部丢弃，本轮评审最终以 controller-side live diff、验证命令与真实 API 实测为准。下一步继续优先清理 shared-console 前端中仍残留的被动 bridge / 低信号 fallback 合同，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-08，AdminUsers shared-console bridge 从静态标签升级为可导航 CTA）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `AdminUsersPage` 做一个小而完整的 shared-console focused 收口切片；具体把管理员资金工作台“共享接入桥接”中原本只展示 `API Keys · /api-keys`、`Webhook 设置 · /webhooks`、`API 文档 · /docs` 的被动标签桥接，升级为同一控制台壳内可直接点击的 CTA：`打开 API Keys`、`继续配置 Webhook`、`查看 API 文档`，让“账务/争议处理 → 接入验证”不再停留在说明文案，而是形成真实可走通的单壳导航路径；同时把该页 focused harness 中 `/api-keys`、`/webhooks`、`/docs` 的 route-stub heading 收敛到当前真实页面 canonical identity `开发者 API 接入工作台`、`开发者 Webhook 接入工作台`、`API 文档与接入控制台`，继续保持“不拆独立后台，只在同一登录后控制台里按角色组织工作区”的 new-api 方向。
- 控制器按 TDD 先从测试合同入手：先修改 `web/src/pages/AdminUsersPage.shared-console.test.tsx` 写出 RED，把 bridge proof 从静态 tag 文本命中收紧为 `admin-users-shared-console-links` 内的真实按钮合同，并新增 bridge click-through 导航证明：依次点击 `打开 API Keys`、`继续配置 Webhook`、`查看 API 文档` 后命中对应 route-stub canonical heading；首次 focused 失败后，再按 `systematic-debugging` 回读运行时代码，确认根因是 `web/src/pages/AdminUsersPage.tsx` 仍然输出只读 `<Tag>` 而非可点击 CTA，于是仅最小修改该 bridge 容器为 borderless `Button` 列表，并保持原有 menu-path gating / fallback 逻辑不变；随后再把原 mission-flow 中 `/api-keys` 落点断言同步收敛为 `开发者 API 接入工作台`，避免继续把菜单短标签误当作真实页面身份。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx` 通过（1 file / 5 tests）；随后 `pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx src/App.test.tsx` 通过（2 files / 32 tests）；`cd web && pnpm exec vitest run` 全量前端测试首轮命中既有非本轮引入的跨页 flaky（`App.test.tsx` onboarding 设置入口、`SupplierResourcesPage.test.tsx` 超时），控制器按 fail-closed 立即改为隔离复现：分别单独重跑失败用例后均通过，再重新执行 `cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 197 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 成功且 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式。真实 API 方面复用并重跑 `.hermes/local-output/run_app_login_register_scope_real_api.sh` 与 `.hermes/local-output/run_admin_users_bridge_scope_real_api.sh`（最新产物目录分别见 `.hermes/local-output/app-login-register-scope/latest.txt` 与 `.hermes/local-output/admin-users-bridge-scope/latest.txt`），再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 返回 200；管理员登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit`、`/admin/wallet-users`、`/admin/disputes` 返回 200；前端 `/`、`/admin/users`、`/api-keys`、`/webhooks`、`/docs` SPA 壳全部返回 200。
- 本轮五维评审结论：产品/规格 PASS（bridge 已从被动说明升级为真实单壳 CTA，且 `/api-keys`、`/webhooks`、`/docs` 落点 heading 全部收敛到页面 canonical identity，没有引入独立后台或角色叙事漂移）；代码质量 APPROVED（运行时代码仅在 `AdminUsersPage.tsx` 用最小按钮替换静态 tag，测试同步收敛为 scoped button + route-heading 合同，没有引入 `.closest(...)`、page-global 宽泛命中或无意义 test hook；当前 diff 仅 2 个文件变更且职责单一）；安全/集成 SAFE（新增 CTA 仍完全受 `visibleSharedConsoleLinks` 的 menu-path gating 控制，未放宽 `/auth/menu` 真值或路由边界；controller-side static scan 0 命中，真实 user/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、组合 Vitest、全量 web Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；全量前端首轮暴露的 2 个失败已经 controller-side 隔离复现并确认是历史 flake/异步噪声而非本轮 bridge 改动引入，二次全量复跑已恢复全绿）；性能/运维 PASS（运行时仅把 3 个静态 tag 升级为 3 个本地导航按钮，不引入新请求、轮询、后端负载或部署复杂度；容器重建后 `web` / `api` / `gateway` 保持 healthy）。下一步继续优先清理 shared-console 前端中仍残留的静态 bridge / fallback / route-stub 低信号 proof，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-08，AdminAudit shared-console bridge 从静态标签升级为可导航 CTA）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `AdminAuditPage` 做一个小而完整的 shared-console focused 收口切片；具体把管理员审计页“共享接入桥接”中原本只展示 `API Keys · /api-keys`、`风控中心 · /admin/risk`、`API 文档 · /docs` 的被动标签桥接，升级为同一控制台壳内可直接点击的 CTA：`打开 API Keys`、`继续查看风控`、`查看 API 文档`，让“审计回放 → 风控复核 / API 接入 / 文档对照”不再停留在说明文案，而是形成真实可走通的单壳导航路径；同时补齐 `/docs` route-stub canonical heading `API 文档与接入控制台`，继续保持“不拆独立后台，只在同一登录后控制台里按角色组织工作区”的 new-api 方向。
- 控制器按 TDD 先从测试合同入手：先修改 `web/src/pages/AdminAuditPage.test.tsx` 写出 RED，把 bridge proof 从静态 tag 文本命中收紧为 `admin-audit-shared-console-bridge` / `admin-audit-shared-console-links` 内的真实按钮合同，并新增 bridge click-through 导航证明：依次点击 `打开 API Keys`、`继续查看风控`、`查看 API 文档` 后命中对应 route-stub canonical heading；首次 focused 失败后，再最小修改 `web/src/pages/AdminAuditPage.tsx`，仅将 shared-console links 从只读 `Tag` 列表升级为带 `navigate(item.path)` 的 borderless `Button` CTA。随后根据 focused 失败继续做同轮收口：把按钮 accessible-name 查询改为 `/打开 API Keys/`、`/继续查看风控/`、`/查看 API 文档/` 正则匹配，兼容 Semi UI 图标前缀进入可访问名称而不削弱真实按钮合同。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminAuditPage.test.tsx` 通过（1 file / 5 tests）；随后 `pnpm --dir web exec vitest run src/pages/AdminAuditPage.test.tsx src/App.test.tsx` 通过（2 files / 32 tests）；`cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 196 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web gateway` 成功且 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式。真实 API 方面新增并重跑 `.hermes/local-output/run_admin_audit_bridge_real_api.sh`（最新产物目录见 `.hermes/local-output/admin-audit-bridge-real-api/latest.txt`），再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 返回 200；管理员登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `/`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs` SPA 壳全部返回 200。
- 本轮五维评审结论：产品/规格 PASS（bridge 已从被动说明升级为真实单壳 CTA，且 `/docs` 落点 heading 收敛到页面 canonical identity，没有引入独立后台或角色叙事漂移）；代码质量 APPROVED（运行时代码仅在 `AdminAuditPage.tsx` 用最小按钮替换静态 tag，测试同步收敛为 scoped button + route-heading 合同，没有引入 `.closest(...)` 或 page-global 宽泛命中；尝试委托产品/代码/安全评审时再次出现跨仓漂移到错误仓库，结果已按 fail-closed 全部丢弃，相关维度改由 controller-side live diff 与实测作为最终依据）；安全/集成 SAFE（新增 CTA 仍完全受 `visibleSharedConsoleLinks` / menu-path gating 控制，未放宽 `/auth/menu` 真值或路由边界；controller-side static scan 0 命中，真实 user/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、全量 web Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` / chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（运行时仅把 3 个静态 tag 升级为 3 个本地导航按钮，不引入新请求、轮询、后端负载或部署复杂度；容器重建后 `web` / `api` / `gateway` 保持 healthy）。下一步继续优先清理 shared-console 前端中仍残留的 page-global / 低信号 bridge/fallback proof，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-08，AdminSuppliers shared-console bridge 从标签升级为可导航 CTA）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 `AdminSuppliersPage` 做一个小而完整的 shared-console focused 收口切片；具体把管理员供应商运营页中原本只展示 `API Keys · /api-keys`、`Webhook 设置 · /webhooks`、`API 文档 · /docs` 的被动标签桥接，升级为同一控制台壳内可直接点击的 CTA：`打开 API Keys`、`继续配置 Webhook`、`查看 API 文档`，让“供应商运营 → 接入联调”不再停留在说明文案，而是形成真实可走通的单壳导航路径；同时把对应 route-stub heading 从旧菜单短标签收敛到当前真实页面 canonical heading `Webhook 运维与回调观测` 与 `API 文档与接入控制台`，继续保持“不拆独立后台，只在同一登录后控制台里按角色组织工作区”的 new-api 方向。
- 控制器按 TDD 先从测试合同入手：先修改 `web/src/pages/AdminSuppliersPage.test.tsx` 写出 RED，把 bridge proof 从静态 tag 文本命中收紧为 `admin-suppliers-shared-console-links` 内的真实按钮合同，并显式点击 `继续配置 Webhook` / `查看 API 文档` 后验证 route-stub canonical heading；首次失败后再最小修改 `web/src/pages/AdminSuppliersPage.tsx`，仅将 shared-console links 从只读 `Tag` 列表升级为带 `navigate(item.path)` 的 borderless `Button` CTA。随后根据 focused 失败继续做同轮收口：为测试引入 `cleanup` 以隔离两次 bridge 导航断言，并把按钮 accessible-name 查询改为 `/打开 API Keys/`、`/继续配置 Webhook/`、`/查看 API 文档/` 正则匹配，兼容 Semi UI 图标前缀进入可访问名称而不削弱真实按钮合同。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx src/App.test.tsx` 通过（2 files / 34 tests）；随后 `cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 196 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过；`docker compose up -d --build web api gateway` 成功且 `docker compose ps` 显示 `web` / `api` / `gateway` healthy。静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式。真实 API 方面新增并重跑 `.hermes/local-output/run_admin_suppliers_shared_console_links_real_api.sh`（最新产物目录见 `.hermes/local-output/admin-suppliers-shared-console-links/latest.txt`），再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 返回 200 且未泄露 admin/supplier 路径；管理员登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `/`、`/admin/suppliers`、`/api-keys`、`/webhooks`、`/docs` SPA 壳全部返回 200。
- 本轮五维评审结论：产品/规格 PASS（bridge 已从被动说明升级为真实单壳 CTA，且落点 heading 收敛到页面 canonical identity，没有引入独立后台或角色叙事漂移）；代码质量 APPROVED（运行时代码仅在 `AdminSuppliersPage.tsx` 用最小按钮替换静态 tag，测试同步收敛为 scoped button + route-heading 合同，没有引入 `.closest(...)` 或 page-global 宽泛命中；尝试委托产品/代码/安全三位 reviewer 时出现跨仓漂移到错误仓库，结果已按 fail-closed 全部丢弃，相关维度改由 controller-side live diff 与实测作为最终依据）；安全/集成 SAFE（新增 CTA 仍完全受 `sharedConsoleLinks` 的 menu-path gating 控制，未放宽 `/auth/menu` 真值或路由边界；controller-side static scan 0 命中，真实 user/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、全量 web Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；委托的测试/可靠性 reviewer 在正确仓库下给出 PASS，指出 mid-test `cleanup()` 只是非阻塞噪声）；性能/运维 PASS（运行时仅把 3 个静态 tag 升级为 3 个本地导航按钮，不引入新请求、轮询、后端负载或部署复杂度；委托的性能/运维 reviewer 在正确仓库下给出 PASS）。下一步继续优先清理 shared-console 前端中仍残留的 page-global / 低信号 bridge/fallback proof，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，Webhooks 管理员提示区作用域锚点与指标合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对管理员 Webhooks 页做一个小而完整的 shared-console focused 收口切片；具体把 `web/src/pages/WebhooksPage.test.tsx` 中仍依赖整段管理员提示文案命中的 proof，收敛为对 `webhooks-role-guidance` 内真实提示区 `webhooks-role-tips` 的作用域断言，并把指标条 proof 继续从长说明 copy 收紧为“提示区关键短语 + attention 数值 + 最近回调描述”这组稳定合同，继续保持“同一登录后控制台壳内，通过角色引导卡、指标条与 endpoint 卡片组织能力，而不是拆分独立后台”的 new-api 方向。
- 控制器按 TDD 先从测试合同入手：先修改 `web/src/pages/WebhooksPage.test.tsx` 写出 RED，显式要求管理员 guidance 区内必须命中独立 `webhooks-role-tips` 容器，并在该容器内验证 `failed / pending`、`last_error`、`测试投递返回 202` 三条关键提示；同时把 attention metric 补强为数值 `1` 与原说明文案并存、把 latest delivery metric 补强为 `最近一次成功送达的回调时间` 描述合同。首次 focused 失败后，再最小修改 `web/src/pages/WebhooksPage.tsx`，仅为既有 tips `<Space>` 补齐 `data-testid="webhooks-role-tips"` 完成 GREEN。过程中根据五维评审中的代码质量/安全建议，保留 attention 说明文案断言，避免只验证数值而弱化真实运维提示合同。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/App.test.tsx` 通过（2 files / 37 tests）；随后 `cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 196 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check` 全部通过。静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式。真实 API 方面重跑 `.hermes/local-output/run_webhooks_role_heading_scope_real_api.sh`（最新产物目录 `.hermes/local-output/webhooks-role-heading-scope/20260507-233811`）与 `.hermes/local-output/run_webhooks_integration_scope_real_api.sh`（最新产物目录 `.hermes/local-output/webhooks-integration-scope/20260507-233813`），再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均返回 200；管理员登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `/`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳继续返回 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把管理员 Webhooks 页 focused proof 从 guidance 区整段提示文案命中收敛为真实 tips 容器与稳定指标合同，没有引入独立后台或角色叙事漂移）；代码质量 APPROVED（运行时代码仅新增 1 个最小语义锚点 `webhooks-role-tips`，测试同步消费该锚点并保留 attention 文案/数值与 latest delivery 描述合同，没有退化为纯 testid 存在性检查；controller-side 代码质量评审提出“保留 attention 说明文案断言”的建议已同轮落实）；安全/集成 SAFE（auth/menu/route/backend contract 未改，controller-side static scan 0 命中，新增锚点不会掩盖真实权限/路由状态；真实 user/admin 链路健康）；测试/可靠性 PASS（focused Vitest、全量 web Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；测试/可靠性评审确认新断言更聚焦且未引入新的 async flake，既有 React `act(...)` / Semi UI `findDOMNode` 告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅补 1 个前端运行时锚点与少量 focused 断言收敛，不引入新请求、状态分支、镜像负担或部署复杂度变化）。下一步继续优先清理 shared-console 前端中仍残留的 page-global / 低信号合同，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，Dashboard role-surface map 作用域锚点与 supplier 导航合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 Dashboard 页 `dashboard-role-surface-map` 做一个小而完整的 shared-console focused 收口切片；具体把供应商场景仍残留的 page-global role-surface 文本命中，收敛为对真实 surface card 的作用域证明，并同步为 `RoleSurfaceItem` 补齐稳定 `key`、让 surface card 使用 `dashboard-role-surface-*` 最小语义锚点，同时把 supplier 导航 proof 从弱计数/文案存在性升级为点击 `基础工作台`、`供应商扩展`、`共享接入` 三张 surface card 后命中真实 `/`、`/supplier/domains`、`/settings` route-stub/heading 合同，继续保持“同一登录后控制台壳内，通过角色卡片、surface map 与工作台跳转组织能力，而不是拆分独立后台”的 new-api 方向。
- 控制器按 TDD 先从测试合同入手：先修改 `web/src/pages/DashboardPage.test.tsx` 写出 RED，显式要求 supplier role-surface map 必须命中 `dashboard-role-surface-basic`、`dashboard-role-surface-supplier-domains`、`dashboard-role-surface-settings` 三张作用域卡片，并在卡片内验证 `基础工作台` / `/`、`供应商扩展` / `/supplier/domains`、`共享接入` / `/settings`；首次 focused 失败后，再最小修改 `web/src/pages/DashboardPage.tsx`：给 `RoleSurfaceItem` 增加稳定 `key`，据此生成 `data-testid` 并同步作为 React list key。随后根据代码质量/可靠性评审再做一次收口，把原先仅统计 `打开该工作台` 按钮数量的弱 proof 升级为显式点击三张 supplier role-surface card 并验证真实落点 heading，避免只证明 DOM 存在而不证明导航行为。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx` 通过；随后 `cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 196 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check` 全部通过。静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式。真实 API 方面重跑 `.hermes/local-output/run_dashboard_role_anchor_real_api.sh`，最新产物目录为 `.hermes/local-output/dashboard-role-anchor-real-api/20260507-231456`；再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均返回 200；supplier 登录后 `/auth/menu`、`/auth/me`、`/supplier/resources/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均返回 200；admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `/`、`/supplier/domains`、`/settings`、`/api-keys`、`/webhooks`、`/docs` SPA 壳继续返回 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 supplier Dashboard role-surface map 的 focused proof 从 page-global 文本命中收敛为真实 surface card 合同，并补上 supplier 在同一 shared-console 壳内跳往总览/域名页/设置页的导航证明，没有引入独立后台或角色叙事漂移）；代码质量 APPROVED（运行时代码仅新增 `RoleSurfaceItem.key` 与最小 `dashboard-role-surface-*` 语义锚点，并将该稳定 key 同时用作 React list key；测试同步消费锚点并把弱按钮计数 proof 升级为真实 click-through route proof，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（auth/menu/route/backend contract 未改，controller-side static scan 0 命中，新增锚点不会掩盖真实权限/路由状态；真实 user/supplier/admin 链路健康）；测试/可靠性 PASS（focused Vitest、全量 web Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` / chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅补充前端最小语义锚点与局部导航合同，不引入新请求、状态分支、镜像负担或部署复杂度变化）。下一步继续优先清理 shared-console 前端中仍残留的 page-global / 低信号合同，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，Webhooks metrics strip 作用域锚点收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅对 Webhooks 管理页做一个小而完整的 shared-console focused 收口切片；具体把 `web/src/App.test.tsx` 中管理员 `/webhooks` 页面仍残留的分散指标断言，收敛为对真实指标带 `webhooks-metrics-strip` 容器的作用域证明，并同步在 `web/src/pages/WebhooksPage.tsx` 为现有指标条补 1 个最小运行时语义锚点，继续保持“同一登录后控制台壳内，通过角色卡片、指标条和工作台卡片组织能力，而不是拆分独立后台”的 new-api 方向。
- 控制器按 TDD 先从测试合同入手：先修改 `web/src/App.test.tsx` 写出 RED，显式要求管理员 Webhooks 页面在 `webhooks-role-guidance` 之下还必须命中 `webhooks-metrics-strip`，并在该 strip 内存在 `webhooks-endpoint-metric`、`webhooks-attention-delivery-metric` 与 `webhooks-latest-delivery-metric`；随后最小修改 `web/src/pages/WebhooksPage.tsx` 仅为既有指标条 `<Space>` 补齐 `data-testid="webhooks-metrics-strip"` 完成 GREEN。过程中没有扩散到鉴权、菜单、路由常量、真实 API、表单逻辑或投递逻辑，只把原本分散的 page/card 级断言收紧为真实运行时指标条合同。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx src/pages/WebhooksPage.test.tsx` 通过（2 files / 37 tests）；随后 `cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 196 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check` 全部通过。静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式。真实 API 方面重跑 `.hermes/local-output/run_webhooks_integration_scope_real_api.sh`（最新产物目录 `.hermes/local-output/webhooks-integration-scope/20260507-224505`）与 `.hermes/local-output/run_app_login_register_scope_real_api.sh`（最新产物目录 `.hermes/local-output/app-login-register-scope/20260507-224507`），再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均返回 200；管理员登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳继续返回 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把管理员 Webhooks 页 focused proof 从分散指标卡命中收敛为真实 metrics strip 合同，更准确表达“角色引导卡 + 指标条 + 当前 endpoint 卡片”这一 shared-console 页面结构，没有引入独立后台或角色叙事漂移）；代码质量 APPROVED（运行时代码仅新增 1 个最小语义锚点，测试同步消费该锚点并保留 role-guidance 与当前 endpoint 行为断言，没有退化为纯 testid 存在性检查；期间尝试 delegate reviewer 做五维评审时再次出现跨仓漂移到 `/root/.hermes/hermes-agent` 的污染结果，已按 fail-closed 全部丢弃，最终以 controller-side live diff 与实测结果为准）；安全/集成 SAFE（auth/menu/route/backend contract 未改，controller-side static scan 0 命中，真实 user/admin 链路健康）；测试/可靠性 PASS（focused Vitest、全量 web Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` 告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅补 1 个前端运行时锚点与少量测试断言收敛，不引入新请求、状态分支、镜像负担或部署复杂度变化）。下一步继续优先清理 shared-console 前端中仍残留的 page-global / 低信号合同，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，Supplier shared-console canonical heading 与 bridge 文案对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅收敛 supplier 资源/供货两页 shared-console bridge 中仍残留的菜单级短标题，把 `/webhooks` 从 `Webhook 设置` 统一提升为真实供应商页面 canonical heading `供给事件回调工作台`，把 `/docs` 从 `API 文档` 统一提升为 `API 文档与接入控制台`；同时同步对齐 `SupplierOfferingsPage.test.tsx` / `SupplierResourcesPage.test.tsx` 的 route-stub 与 bridge scoped proof，继续保持“供应商资源 → 供货规则 → API Keys / Webhooks / Docs 全部位于同一登录后深色共享控制台单壳内”的 new-api 方向，不拆分冗余供应商后台。
- 控制器按 TDD 先修改 `web/src/pages/SupplierOfferingsPage.test.tsx` 与 `web/src/pages/SupplierResourcesPage.test.tsx` 写出 RED：显式要求 bridge 按钮与 route-stub 命中新 canonical heading 文案；随后最小修改 `web/src/pages/SupplierOfferingsPage.tsx` 与 `web/src/pages/SupplierResourcesPage.tsx` 仅替换 4 处运行时 bridge 标签完成 GREEN。其间 `SupplierOfferingsPage.test.tsx` 首轮 focused 失败暴露根因是运行时按钮仍输出旧短标题 `Webhook 设置 · /webhooks`、`API 文档 · /docs`；控制器据此只修正文案与对应断言，没有扩散到权限、路由或布局逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx` 通过（2 files / 17 tests）；随后 `cd web && pnpm exec vitest run` 全量前端测试通过（26 files / 196 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check` 全部通过。静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式。真实 API 方面重跑 `.hermes/local-output/run_supplier_offerings_route_stub_real_api.sh`（最新产物目录 `.hermes/local-output/supplier-offerings-route-stub-real-api/20260507-222007`）与 `.hermes/local-output/run_supplier_resources_label_contract_real_api.sh`（最新产物目录见对应 latest.txt），再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，supplier `/auth/menu`、`/auth/me`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements`、`/auth/api-keys`、`/webhooks/endpoints` 均返回 200，admin `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 返回 200，前端 `/`、`/supplier/offerings`、`/supplier/resources`、`/api-keys`、`/webhooks`、`/docs` SPA 壳继续返回 200。
- 本轮五维评审结论：产品/规格 PASS（supplier 侧 bridge 文案已与真实页面 canonical heading 收敛，不再把菜单短标题误当作目标页身份，且未引入独立后台或角色叙事漂移）；代码质量 APPROVED（diff 仅落在 4 个前端文件、11 行增删对等，属于最小文案/断言收口；测试继续锚定真实按钮与 heading 合同，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（未改 auth/menu/route/backend contract，controller-side static scan 0 命中，真实 supplier/admin 链路均健康）；测试/可靠性 PASS（focused Vitest、全量 web Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过，既有 React `act(...)` / Semi UI `findDOMNode` 告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅收敛前端 bridge 文案与测试断言，零新增请求、状态分支、容器配置或部署复杂度变化）。下一步继续优先清理 shared-console 中其余 supplier/admin bridge 的菜单短标题与 page-global 低信号合同，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，Dashboard 首轮任务卡中文标签与作用域锚点收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，仅把 `web/src/pages/DashboardPage.tsx` 首轮任务卡里仍残留的英文 shared-console 标签 `Shared Console` / `Integration` / `Role-aware` 收敛为中文运行时合同 `共享控制台` / `统一接入` / `角色扩展`，并为首轮任务卡补齐最小语义锚点 `dashboard-first-run-mission-card-*`，继续保持“同一登录后控制台内完成采购、订单、接入与角色扩展，不拆分冗余后台”的 new-api 方向。
- 控制器按 TDD 先修改 `web/src/pages/DashboardPage.test.tsx` 写出 RED：先显式要求 procurement 首轮任务卡作用域内不再出现旧英文标签，并要求页面出现新的 `统一接入` / `角色扩展` 中文标签；首次 focused 复跑失败后，再最小修改 `web/src/pages/DashboardPage.tsx` 中的 3 处标签文案，并仅补 1 处 mission-card 级 `data-testid` 完成 GREEN。过程中根据代码质量经验再收敛一次测试实现：把最初依赖 `.closest('.semi-card')` 的卡片定位改为运行时显式语义锚点，避免把 Semi UI wrapper 结构耦合进 focused proof。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx` 通过（1 file / 8 tests）；随后 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx` 通过（2 files / 35 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check` 全部通过。静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式。真实 API 方面复用并重跑 `.hermes/local-output/run_dashboard_journey_tightening_real_api.sh`，最新产物目录为 `.hermes/local-output/dashboard-journey-tightening/20260507-215048`；再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均返回 200，管理员登录后 `/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 均返回 200，前端 `/`、`/api-keys`、`/webhooks`、`/docs` SPA 壳继续返回 200。
- 本轮五维评审结论：产品/规格 PASS（diff 仅把 Dashboard 首轮任务卡的残留英文标签收敛为中文 shared-console 合同，没有引入独立后台、角色分叉或页面身份漂移）；代码质量 APPROVED（运行时代码只改 3 处静态标签并新增 1 个最小 mission-card 语义锚点；测试同时验证旧英文标签消失与新中文标签出现，且已从 `.closest(...)` 收敛为显式 card 作用域合同）；安全/集成 SAFE（auth/session、菜单过滤、路由常量与真实 API 链路均未改动，controller-side real API replay 再次确认 user/admin 权限边界健康）；测试/可靠性 PASS（focused Vitest、`DashboardPage.test.tsx + App.test.tsx` 组合 Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` 噪声仍属历史非阻塞告警，本轮未恶化）；性能/运维 PASS（仅是前端静态标签替换与最小测试锚点收敛，不引入新请求、状态分支、镜像负担或部署复杂度变化）。下一步继续优先清理 shared-console 前端中仍残留的英文标签、page-global 低信号合同与 role-aware surface 漂移，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，ApiDocs Webhooks route-stub canonical heading 对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约与运行时代码的前提下，仅把 `web/src/pages/ApiDocsPage.test.tsx` 中仍残留的一处 `/webhooks` 菜单级 route-stub 标题 `Webhook 设置` 收紧到当前真实用户页 canonical heading `开发者 Webhook 接入工作台`，并把 loop CTA 点击后的落点断言同步从弱文本命中升级为 `role=heading` 语义合同，继续保持“菜单可短、目标页身份必须与真实 shared-console 页面一致”的单壳 new-api 方向。
- 控制器按 TDD 先修改 `web/src/pages/ApiDocsPage.test.tsx` 写出 RED：先让 docs→integration loop 在点击 `打开 Webhook 设置` 后显式要求命中新 canonical heading；首次 focused 复跑失败后，按 `systematic-debugging` 先回读测试 harness，定位根因是同文件内 `/webhooks` route stub 仍停留在旧的 `<h1>Webhook 设置</h1>`，而不是应用逻辑或页面运行时回退；随后仅最小修改同一测试文件中的 route-stub heading 与断言完成 GREEN，没有扩散到运行时代码或其他页面。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 通过（1 file / 5 tests）；随后 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/App.test.tsx` 通过（2 files / 32 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式。真实 API 方面复用并重跑 `.hermes/local-output/run_route_heading_canonical_real_api.sh`，最新产物目录为 `.hermes/local-output/route-heading-canonical-real-api/20260507-212956`；再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均返回 200，管理员登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 均返回 200，前端 `/`、`/api-keys`、`/webhooks`、`/docs`、`/settings` SPA 壳继续返回 200。
- 本轮五维评审结论：产品/规格 PASS（本次 diff 只继续把 ApiDocs focused harness 的 `/webhooks` 落点页身份从菜单短标签收敛为真实页面级 canonical heading，没有引入独立后台、角色分叉或文档/控制台双壳漂移）；代码质量 APPROVED（diff 仅落在单个 focused 测试文件，且是严格的最小修正：一处 route-stub `<h1>` 与一处 heading 断言同步对齐，没有引入 `.closest(...)`、page-global 宽泛命中、额外 testid 或运行时代码膨胀）；安全/集成 SAFE（静态扫描无命中，真实 `/auth/menu`、`/webhooks/endpoints`、`/docs` 与 shared-console 权限边界均未放宽；controller-side real API replay 再次确认用户/admin 链路健康）；测试/可靠性 PASS（focused Vitest、`ApiDocsPage.test.tsx + App.test.tsx` 组合 Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` 噪声仍属历史非阻塞告警，本轮未恶化）；性能/运维 PASS（仅收敛测试 harness 页面身份合同，不引入任何运行时请求、状态分支、部署拓扑或容器复杂度变化）。下一步继续优先清理 shared-console 前端中仍残留的菜单级/低信号 route-stub 与 page-global 合同，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，Login 入口减法与表单对比度收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，优先继续把 `web/src/pages/LoginPage.tsx` 收敛为更像统一控制台入口而不是营销页的单壳登录面；具体围绕浏览器视觉复核指出的三类问题做最小垂直切片：一是把左侧 `控制台入口摘要` 从 4 张说明卡压缩为 3 条最小事实，删除重复的“角色菜单”独立卡并收紧文案；二是同步缩短主说明与首轮接入区副文案，减弱登录前解释成本；三是加强右侧 auth shell 输入框/placeholder/提示 banner 的深色对比度，让真正的登录表单比说明区更像主视觉中心，继续保持“注册成功后进入同一套共享控制台，而不是独立新手页或多后台分叉”的 new-api 方向。
- 控制器按 TDD 先修改 `web/src/pages/LoginPage.test.tsx` 写出 RED：把 readiness 区合同改为新的 `统一入口 / 共享控制台 / 开发接入` 三条最小事实，并保持注册 CTA、tablist 语义与注册成功进入 shared shell 的既有合同不变；随后最小修改 `web/src/pages/LoginPage.tsx` 完成 GREEN。过程中配合浏览器视觉检查做一次控制器侧收敛：首轮 `browser_vision` 明确指出页面虽然不是纯营销页，但左侧仍偏重、右侧表单存在感不够强、输入区与提示区对比度不足，因此本轮没有扩展新页面或后端逻辑，只继续在 Login 单页上做减法与对比度修正。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（1 file / 5 tests）；随后 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（2 files / 32 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`docker compose up -d --build web gateway` 与 `git diff --check` 全部通过。静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式。真实 API 方面复用并重跑 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/app-login-register-scope/20260507-205431`；再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均返回 200，管理员登录后 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 均返回 200，前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳继续返回 200。浏览器侧 fresh-load `http://127.0.0.1:5173/login` 后 snapshot 已更新为新的三条摘要文案，`browser_vision` 结论确认“更像统一控制台入口而不是营销页”，剩余问题仅是左侧仍可继续减法、提示区略长、输入框还可再加强，但不再构成当前垂直切片阻塞。
- 本轮五维评审结论：产品/规格 PASS（本次 diff 只继续把 Login 入口从“说明型页面”收敛为“统一控制台入口”，没有引入独立后台或登录前角色分叉，且注册成功仍直达同一 shared shell）；代码质量 APPROVED（diff 仅落在 `LoginPage.tsx` 与 `LoginPage.test.tsx`，持续使用既有语义锚点 `login-control-plane-readiness` / `login-register-journey` / `login-auth-shell`，未引入额外运行时 test hook 或 wrapper class 耦合；期间测试/可靠性评审先后指出 readiness 合同缺少“移除旧卡片”负断言、缺少对“三条事实”数量上界的保护、以及计数断言不应 page-global，控制器已同轮补齐负断言、精确 3 项计数与 readiness 区局部计数后复验通过）；安全/集成 SAFE（静态扫描无命中，登录/注册 API、`setSession(...)`、`navigate('/')`、`/auth/menu` 真值与真实角色边界均未放宽；controller-side real API replay 再次确认 user/admin 链路健康）；测试/可靠性 PASS（focused LoginPage Vitest、`LoginPage.test.tsx + App.test.tsx` 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` / chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅做登录页文案减法、样式对比度与现有测试合同收敛，不增加运行时请求、状态分支或部署复杂度）。下一步继续优先寻找 shared-console 前端中仍残留的 page-global / 低信号合同与登录入口剩余减法空间，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，Settings supplier shortcut lane scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，优先继续收紧 `SettingsPage` 在 shared-console 内的角色差异快捷入口合同；具体仅聚焦 `web/src/pages/SettingsPage.test.tsx`，把供应商场景从 page-global 的 `查看供应商资源` / `前往供应商结算` / `管理 API Keys` 命中，收敛为 `settings-shortcut-cards` 运行时快捷入口区内的 scoped proof，并新增同区负断言，显式证明普通用户采购入口 `打开项目市场` 不会泄露到 supplier 设置页，继续保持“统一登录后单壳控制台，通过角色与菜单区分快捷工作区，而不是拆分冗余后台”的 new-api 方向。
- 控制器按 TDD 先修改 `web/src/pages/SettingsPage.test.tsx` 写出 RED：先要求 supplier shared shortcuts 必须在 `settings-shortcut-cards` 中命中，同时要求同一区域内不存在 plain-user `打开项目市场` CTA；初次 GREEN 尝试曾给每张快捷卡片加 route-derived `data-testid`，但五维代码质量评审明确指出这是不必要的运行时代码扩散与路径耦合。控制器据此立即回退运行时代码，仅保留测试层的 lane-scoped 查询，让本次切片维持“零运行时代码变化、只收紧 focused proof”的最小前端修正。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 通过（1 file / 3 tests）；随后 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/App.test.tsx` 通过（2 files / 30 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check` 全部通过。静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式。真实 API 方面复用并重跑 `.hermes/local-output/run_settings_front_scope_real_api.sh` 与 `.hermes/local-output/run_settings_session_scope_real_api.sh`，最新产物目录分别为 `.hermes/local-output/settings-front-scope/20260507-202952` 与 `.hermes/local-output/settings-session-scope/20260507-202953`；再次验证 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户注册后 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均返回 200，供应商登录后 `/auth/menu` 继续包含 `/settings` 与 supplier 路径，前端 `/login`、`/settings` SPA 壳返回 200。浏览器侧 fresh-load 检查 `http://127.0.0.1:5173/login` snapshot 正常，`browser_vision` 结论为左右卡片无明显裁切/重叠，仅有非阻塞的中部留白偏大与次级文字对比略低问题。
- 本轮五维评审结论：产品/规格 PASS（supplier 设置页 proof 现在明确绑定到真实 `settings-shortcut-cards` 快捷入口区，而不是整页宽泛命中；同时新增“用户采购 CTA 不泄露到 supplier 设置页”的负合同，更准确表达 shared-console 内的角色差异捷径边界）；代码质量 APPROVED（首轮给快捷卡片添加 route-derived `data-testid` 的方案已按评审意见回退，最终 diff 仅落在单个 focused 测试文件，持续使用现有语义区锚点 `settings-shortcut-cards` 而未引入 wrapper class 依赖、`.closest(...)` 或额外运行时测试钩子）；安全/集成 SAFE（auth/session、`useAuthStore`、菜单过滤与导航逻辑均未改动，controller-side real API replay 再次确认 `/auth/menu` 角色边界、supplier 路径与共享 `/settings` 契约保持健康）；测试/可靠性 PASS（focused SettingsPage Vitest、`SettingsPage.test.tsx + App.test.tsx` 组合 Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` 警告仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（最终提交不引入任何运行时逻辑、额外请求、状态分支或新 DOM 钩子，只增强测试对现有快捷入口区的验证密度，构建产物与容器配置保持稳定）。下一步继续优先寻找 shared-console 前端中仍残留的 page-global / 低信号合同并做最小 vertical slice 收敛，然后再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，shared-console canonical shell route-title 对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改 Phase 4/5 后端契约的前提下，优先收敛统一控制台壳层标题与真实页面身份的漂移；具体把 `web/src/utils/consoleNavigation.tsx`、`web/src/layouts/ConsoleLayout.tsx`、`web/src/utils/consoleNavigation.test.tsx` 与 `web/src/App.test.tsx` 作为一组前端优先垂直切片，新增 route-level `titleByRole` 能力，并把 shared/supplier 关键路由的 shell canonical title 对齐到当前真实页面身份：`/api-keys` → `开发者 API 接入工作台`、`/docs` → `API 文档与接入控制台`、`/supplier/domains` → `域名池运营中枢`、`/supplier/offerings` → `供货规则编排中枢`、`/supplier/settlements` → `供应商资金与争议指挥台`；同时为角色差异显式保留 `/webhooks` 的 user/supplier/admin 三态标题与 `/supplier/resources` 的 supplier/admin 壳层标题覆盖，避免把管理员/供应商真实页面重新压回菜单短标签或错误 user 标题。
- 控制器按 TDD 先从测试合同入手：先修改 `web/src/utils/consoleNavigation.test.tsx` 写出 RED，显式要求 `resolveRouteTitle(...)` 返回上述 canonical shell title，并覆盖 `/webhooks` 的 role-aware 标题差异；随后最小修改 `consoleNavigation.tsx` 与 `ConsoleLayout.tsx` 完成 GREEN。过程中根据产品/规格 fail-closed 评审与 focused rerun 再做一次系统化修正：首轮把 `/webhooks` 与 `/supplier/resources` 一律改为新 canonical title 后，评审发现 admin/supplier 壳层标题会与真实页面 heading 失配；控制器据 `WebhooksPage.tsx` / `SupplierResourcesPage.tsx` 运行时 heading 反向收敛为 `titleByRole` 覆盖，而不是继续硬推单一字符串。随后 `App.test.tsx` 中供应商 Dashboard → 域名页 proof 因 page-global CTA / heading 路径在 grouped run 下发生不稳定，控制器按同轮调试把点击与落点断言收紧到 `dashboard-role-actions` 和 `supplier-domains-hero-card`，避免把无关页面中的相同按钮/heading 误当成当前路由结果。
- 控制器验证结果：`pnpm --dir web exec vitest run src/utils/consoleNavigation.test.tsx src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx src/components/ProtectedRoute.test.tsx src/App.test.tsx` 通过（5 files / 50 tests）；随后 `pnpm --dir web exec vitest run` 全量通过（26 files / 196 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check` 全部通过。静态安全扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / pickle / SQL 注入模式。真实 API 方面新增并重跑本地回放脚本 `.hermes/local-output/run_route_title_canonical_real_api.sh`，最新产物目录为 `.hermes/local-output/route-title-canonical-real-api/20260507-195214`（并已写入 `.hermes/local-output/route-title-canonical-real-api/latest.txt`）；本轮再次验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200 且菜单不泄露 supplier/admin 路径；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 均返回 200；前端 `http://127.0.0.1:5173/`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/domains`、`/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/admin/risk`、`/admin/audit` 均返回 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS（shared shell 标题现已与真实页面身份收敛，不再把 `API Keys` / `API 文档` / `供应商结算` 这类菜单短标题误当成落点页面 identity；同时保留 `/webhooks` 与 `/supplier/resources` 的角色差异标题，符合“同一控制台壳、按角色区分页面表面”的 new-api 方向）；代码质量 APPROVED（`titleByRole` 改动集中在导航 schema 与 layout 读取点，未扩散到运行时页面；`App.test.tsx` 供应商 CTA proof 已从 page-global 查询收紧为 role-actions / hero-card 合同；repo-scoped 代码质量评审结论 APPROVED）；安全/集成 SAFE（静态扫描无命中，`allowedRoles`、`/auth/menu`、`ProtectedRoute` / `SupplierRoute` / `AdminRoute`、bootstrap 与 real API 合同均未被该 diff 放宽；repo-scoped 安全/集成评审结论 SAFE）；测试/可靠性 PASS（focused Vitest、全量 Vitest、build、Go tests、compose config、真实 API 回放全部通过；首轮 grouped run 暴露的 supplier Dashboard CTA flake 已在同轮通过区域作用域修正收敛，repo-scoped 测试/可靠性评审结论 PASS；既有 React `act(...)` / Semi UI `findDOMNode` 噪声仍属历史非阻塞项，本轮未恶化）；性能/运维 PASS（仅增加极小型前端 route metadata 与 role-aware title 解析，不引入新的请求、状态轮询、部署配置或运维复杂度；repo-scoped 性能/运维评审结论 PASS）。下一前端优先项继续围绕 shared-console 页头/hero/mission-flow 的 canonical title 与 focused proof 去脆弱化收口，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，auth bootstrap 会话恢复 / 深链稳定性收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：优先修补统一登录后控制台的会话恢复与深链稳定性，而不是继续扩散新的后端切片；在不改注册/登录 API 契约、不改角色菜单真值与 Phase 4/5 业务接口的前提下，完成 `web/src/store/authStore.ts`、`web/src/App.tsx`、`web/src/components/ProtectedRoute.tsx` 的一组前端优先垂直切片：补齐 refresh token 的 `sessionStorage` 持久化、引入 `bootstrapStatus` 明确区分 `idle/loading/ready`、为 shared-console 增加 Linear/new-api 风格的 `auth-bootstrap-shell` 恢复面板，并把 admin/supplier 路由保护改为“等待 bootstrap 完成后再执行角色校验”，避免刷新或深链进入 `/admin/audit`、`/supplier/resources` 时因 `user` 尚未回填而错误回退到 `/`。同时将登录后优选落点逻辑收敛为仅在初始路径是 `/` 时生效，避免每次站内导航都重新触发整轮 bootstrap 与错误重定向。
- 控制器按 TDD 先从 `web/src/App.test.tsx` 写出 RED：显式要求 `setSession(...)` 必须持久化 refresh token、等待服务端菜单时必须出现 `auth-bootstrap-shell` 与“正在恢复共享控制台”提示、以及 admin 深链 `/admin/audit` 在 bootstrap 后不得被错误改写为偏好首页；随后最小修改运行时代码完成 GREEN。过程中根据五维评审反馈执行一次收敛修正：首轮产品/代码质量评审指出 bootstrap effect 依赖 `location.pathname` 会导致每次站内导航都重新闪回恢复壳并重复请求 `/auth/me` + `/auth/menu`，控制器已同轮将其修正为只在初始进入路径记录 `initialPathRef` 并移除路径依赖，保持“仅首轮恢复、正常导航不再重复 bootstrap”的单壳体验。
- 配套测试与契约同步收口：`web/src/components/AppSidebar.test.tsx`、`web/src/components/ConsoleLayout.test.tsx`、`web/src/components/ProtectedRoute.test.tsx` 全部补齐 `bootstrapStatus` 初始化，确保 shared shell focused proof 与新的会话恢复状态机一致；`web/src/App.test.tsx` 中新增/收紧了 refresh token 持久化、bootstrap shell 显示、admin deep-link 保持 `/admin/audit` 的集成合同，并顺手把两个既有脆弱断言去偶发化（Webhook test delivery 改为比较调用增量、AdminAudit 表格断言改为稳定的现有行内容），避免本轮会话状态改动引出无关 flaky。
- 控制器验证结果：`pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx src/components/ProtectedRoute.test.tsx src/App.test.tsx` 通过（43 tests），随后 `pnpm --dir web exec vitest run` 全量通过（26 files / 196 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config` 全部通过。真实 API 方面新增并重跑本地回放脚本 `.hermes/local-output/run_auth_bootstrap_real_api.sh`，最新产物目录为 `.hermes/local-output/auth-bootstrap-real-api/20260507-191612`（上一轮产物 `.hermes/local-output/auth-bootstrap-real-api/20260507-190337` 也保留）；本轮再次验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu` 继续只暴露 shared 路径 `/` `/projects` `/orders` `/balance` `/profile` `/api-keys` `/webhooks` `/settings` `/docs`，管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/audit?limit=5` 返回 200 且有审计事件，供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/supplier/resources/overview` 返回 200 且包含 `domains/accounts/mailboxes`。
- 本轮五维评审结论：产品/规格 PASS（会话恢复面板与 deep-link 保持行为符合“单一登录后 shared-console/new-api 单壳”要求，不再把正常导航误当作重新登录 bootstrap）；代码质量 APPROVED（refresh token 持久化、`bootstrapStatus` 状态机与 `initialPathRef` 约束清晰，控制器已修复评审指出的“路径变化触发重复 bootstrap”阻塞问题；当前 diff 仅扩展必要状态与最小恢复壳，不引入额外路由分叉）；安全/集成 SAFE（静态扫描当前 live diff 新增行 0 命中 hardcoded secret / shell / eval / SQL 注入；refresh token 持久化与现有 401 refresh interceptor、logout API 契约一致，admin/supplier 路由仅在 `bootstrapStatus=ready` 后执行角色判定，没有放宽真实权限边界）；测试/可靠性 PASS（focused Vitest、全量 Vitest、前端 build、Go tests、compose config 与真实 API 回放全部通过；全量 Vitest 中既有 React `act(...)` / Semi UI `findDOMNode` / chunk 体积警告仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（bootstrap 现在限制为初始恢复时执行，避免路径切换重复拉取 `/auth/me` 与 `/auth/menu`；新增恢复壳为纯前端展示层，不引入容器、数据库或 worker 侧额外负担）。

### 最新执行检查点（2026-05-07，Dashboard supplier role-guidance scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限真值、不改 Dashboard 运行时角色动作与 Phase 4/5 后端契约的前提下，仅把 `web/src/App.test.tsx` 中供应商 Dashboard focused proof 继续从 page-global 负断言收紧到真实运行时角色引导区；具体为 `DashboardPage.tsx` 既有角色工作台导引卡片补齐 `dashboard-role-guidance` 语义锚点，并将 App 内 `does not show first-run onboarding guidance for supplier dashboard` 用例从整页 `main` 范围内的 `普通用户首轮引导` / `查看结算页` / `打开 Webhook 设置` 缺失判断，收敛为 `within(dashboard-role-guidance)` 作用域合同，同时正向证明供应商 `供应商主任务` 与 `维护域名池` 仍位于同一共享控制台角色导引卡片中，继续保持“单一登录后控制台、角色差异只体现在同一壳内引导区与菜单”的 new-api 风格产品方向。
- 控制器按 TDD 先修改 `web/src/App.test.tsx`：RED 阶段先显式要求供应商场景必须命中新角色导引容器 `dashboard-role-guidance`，并把 supplier guidance proof 收紧到容器内 heading / timeline 合同；随后复跑 `pnpm --dir web exec vitest run src/App.test.tsx -t "does not show first-run onboarding guidance for supplier dashboard"` 观察到因缺少运行时锚点而失败，再最小修改 `web/src/pages/DashboardPage.tsx` 仅为既有 role-guidance `Card` 补齐 `data-testid="dashboard-role-guidance"` 完成 GREEN。随后重新通过 `pnpm --dir web exec vitest run src/App.test.tsx -t "does not show first-run onboarding guidance for supplier dashboard"`（1 test 通过）与 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/DashboardPage.test.tsx`（2 files / 34 tests 全绿），并继续通过 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web api gateway`，确认最新 shared-console 运行时已生效。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh` 与 `.hermes/local-output/run_dashboard_role_anchor_real_api.sh`；本轮最新产物目录分别为 `.hermes/local-output/app-login-register-scope/20260507-181712` 与 `.hermes/local-output/dashboard-role-anchor-real-api/20260507-181715`。本轮再次验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/supplier/resources/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；前端 `/`、`/login`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers`、`/supplier/domains` 的 SPA shell 均返回 200，证明本轮仅收紧 Dashboard/App 前端语义区域合同，未破坏 shared-console 菜单或 Phase 4/5 真实 API 链路。
- 本轮五维评审结论：产品/规格 PASS（supplier dashboard proof 现在明确绑定到真实 `dashboard-role-guidance` 卡片，而不是宽泛整页缺失假设，更准确表达“供应商主任务”属于角色导引区）；代码质量 APPROVED（运行时代码仅新增 1 个最小语义锚点，测试把 supplier guidance proof 收紧为 `within(dashboard-role-guidance)`，未引入 `.closest(...)`、page-global 宽泛命中或额外实现耦合；仅有一条非阻塞建议指出 `维护域名池` 仍是文案级合同，但当前可接受）；安全/集成 SAFE（静态扫描 live diff 新增行 0 命中，且 auth/menu/route/backend 契约未改；controller-side real API replay 与安全评审都确认该锚点不会掩盖真实权限/路由状态）；测试/可靠性 PASS（focused supplier Vitest、`App.test.tsx` + `DashboardPage.test.tsx` 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` / chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅新增前端 data-testid 语义锚点，不引入新请求、状态分支、数据库负担或部署复杂度，且 `web` / `api` / `gateway` 重建后恢复 healthy）。

### 最新执行检查点（2026-05-07，Profile role-focus semantic region tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/Phase 4/5 后端契约的前提下，仅把 `web/src/pages/ProfilePage.tsx` 与 `web/src/pages/ProfilePage.test.tsx` 中仍残留的 page-global 用户焦点 heading 命中收紧到真实运行时角色焦点卡片；具体为 Profile 页 role-focus 容器补齐 `profile-role-focus-card`，并把 `用户接入焦点` / `采购与订单串联` / `集成准备` 三条合同从 page-global heading 查询收敛为 `within(roleFocusCard)` 作用域断言，继续保持“个人资料页只是统一登录后共享控制台中的身份/桥接页，而不是独立后台”的 new-api 风格单壳叙事。
- 控制器按 TDD 先修改 `web/src/pages/ProfilePage.test.tsx`：RED 阶段先让 focused 用例显式要求上述三条 heading 必须出现在新的角色焦点容器内，并立即复跑 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 观察到因缺少运行时锚点而失败；随后最小修改 `web/src/pages/ProfilePage.tsx` 仅为角色焦点 `Card` 补齐 `data-testid="profile-role-focus-card"` 完成 GREEN，并重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/App.test.tsx`（2 files / 33 tests 全绿）。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_profile_capability_canonical_heading_real_api.sh`；最新产物目录为 `.hermes/local-output/profile-capability-canonical-heading/20260507-174855`。本轮再次验证：`docker compose up -d --build web api gateway` 后 `GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit` 均返回 200；前端 `/`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/settings`、`/admin/risk`、`/supplier/domains` 的 SPA shell 全部返回 200，证明本轮仅收紧 Profile 前端语义区域合同，未破坏 shared-console 菜单或真实 API 链路。
- 本轮五维评审结论：产品/规格 PASS（角色焦点 heading 现在明确绑定到 Profile 的真实 role-focus 卡片，而不是任意页面角落的重复 heading，更准确表达个人资料页中的身份焦点区）；代码质量 APPROVED（运行时代码仅新增 1 个最小语义锚点，测试仅把 3 条 heading 断言收紧为 `within(profile-role-focus-card)`，未引入 `.closest(...)`、page-global 宽泛命中或额外实现耦合）；安全/集成 SAFE（静态扫描 live diff 新增行 0 命中，且 auth/menu/route/backend 契约未改；delegate 安全评审与 controller-side real API replay 都确认该锚点不会掩盖集成真相）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)` / Semi UI `findDOMNode` / chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅新增前端 data-testid 语义锚点，不引入新请求、状态分支、数据库负担或部署复杂度，且容器重建后 `api` / `web` / `gateway` 恢复 healthy）。

### 最新执行检查点（2026-05-07，Webhooks/Balance role-guidance scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录运行时代码、不改菜单权限、不改 Phase 4/5 后端契约的前提下，仅把 `web/src/App.test.tsx` 与 `web/src/pages/BalancePage.test.tsx` 中仍残留的 page-global 角色引导命中收紧到真实运行时语义区域；具体将 App 内管理员 Webhooks 页面 proof 从全页 `Webhook 运维与回调观测` 命中收敛为 `webhooks-role-guidance` 区域内 heading 合同，并将 Balance 页 supplier/admin 两条角色说明从 page-global 文本命中收敛为 `balance-hero-card` 内的 heading + guidance 合同，继续保持“统一登录后控制台、角色差异只体现在同一壳内可见区域与菜单”这一 new-api 风格产品方向。
- 控制器按 TDD 先修改上述 2 个 focused 测试文件，让 RED 阶段显式要求角色文案必须出现在真实 hero/guidance 容器内，再以最小测试改动完成 GREEN；随后重新通过 `pnpm --dir web exec vitest run src/pages/BalancePage.test.tsx src/App.test.tsx`（2 files / 33 tests 全绿），并额外复跑 `pnpm --dir web exec vitest run src/pages/BalancePage.test.tsx src/App.test.tsx src/pages/WebhooksPage.test.tsx`（3 files / 43 tests 全绿），确认新的 scoped 合同未引入 flaky 或削弱 Webhooks 现有页面级 proof。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_webhooks_guidance_scope_real_api.sh`、`.hermes/local-output/run_balance_real_api.sh` 与 `.hermes/local-output/run_app_login_register_scope_real_api.sh`；本轮最新产物目录分别为 `.hermes/local-output/webhooks-guidance-scope/20260507-172052`、`.hermes/local-output/real-api-balance-20260507-172102`、`.hermes/local-output/app-login-register-scope/20260507-172114`。本轮再次验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；余额真实回放还再次确认 `GET /api/v1/wallet/overview`、`GET /api/v1/wallet/transactions`、`POST /api/v1/wallet/topups` 返回成功，且负向争议请求继续稳定返回 400；前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` 的 SPA 壳全部返回 200。
- 本轮五维评审结论：产品/规格 PASS（新断言更准确地证明角色引导文案属于对应 hero/guidance 容器，而不是任意页面角落的重复文本）；代码质量 APPROVED（diff 仅收紧 2 个测试文件到既有语义锚点，未引入新的 runtime wrapper、`.closest(...)` 或脆弱索引选择器）；安全/集成 SAFE（控制器静态扫描当前 live diff 新增行 0 命中，且 real API replay 再次确认 auth/menu/dashboard/webhooks/wallet 链路与权限边界健康）；测试/可靠性 PASS（focused Vitest、扩展 3 文件 Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过，既有 React `act(...)` / Semi UI `findDOMNode` / chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯测试层 scoped 合同收敛，不引入新的运行时请求、状态分支、容器变更或部署复杂度，且 controller-side 验证范围已覆盖本次前端-only 里程碑所需基线）。

### 最新执行检查点（2026-05-07，SupplierSettlements canonical heading / shared-console bridge 对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/供应商结算后端契约的前提下，仅把 `web/src/pages/SupplierSettlementsPage.tsx` 与 `web/src/pages/SupplierSettlementsPage.test.tsx` 中仍残留的 supplier shared-console bridge 页面身份从菜单级短标签 `Webhook 设置` / `API 文档` 收敛为当前真实运行时 canonical heading `供给事件回调工作台` / `API 文档与接入控制台`；并同步把 route-stub heading 与 bridge scoped proof 对齐，继续保持“菜单可以短、落点页面身份必须以角色页面实际 heading 为准”的单壳 shared-console/new-api 方向。
- 控制器按 TDD 先修改 `web/src/pages/SupplierSettlementsPage.test.tsx`，让 route-stub 与 shared-console bridge proof 在 RED 阶段显式要求新的 canonical heading，再最小修改 `web/src/pages/SupplierSettlementsPage.tsx` 仅收敛 supplier shared-console bridge 两个运行时 label 完成 GREEN；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`（1 file / 7 tests 全绿）、`pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx src/App.test.tsx`（2 files / 33 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行 `docker compose up -d --build web` 确认最新前端容器运行时已生效（本轮 Compose 同步重建了 api 镜像，但未引入新的后端逻辑变更）。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_supplier_settlements_heading_scope_real_api.sh`；最新产物目录为 `.hermes/local-output/supplier-settlements-heading-scope/20260507-161924`。本轮再次验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 均返回 200；前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/settlements` 的 SPA shell 全部返回 200。供应商菜单 JSON 仍然故意暴露短标签 `Webhook 设置` / `API 文档`，说明本轮只是收敛页面身份合同，并未篡改服务端菜单真值或权限边界。
- 浏览器验证补充：控制器尝试 fresh `browser_navigate` 直接打开本地 `/supplier/settlements` 时，浏览器会话仍回落并停留在 `/login` 旧快照；已按 `browser-verification-degradation-fallback` 将其判定为工具态陈旧/降级，而非应用回归，并以 controller-side 构建、容器重建、HTTP 200 与真实 API 回放产物作为本轮本地验收依据。
- 本轮五维评审结论：产品/规格 PASS（供应商结算页 shared-console bridge 与 route-stub 已统一对齐到 `WebhooksPage.tsx` supplier 视角 `供给事件回调工作台` 和 `ApiDocsPage.tsx` 的 `API 文档与接入控制台`，更准确表达“菜单标签可短、真实落点页身份以运行时 heading 为准”的单壳产品叙事）；代码质量 APPROVED（diff 仅落在单个运行时数组与单个 focused 测试文件，保持 bridge 容器内 scoped 合同，没有引入 `.closest(...)`、page-global 宽泛命中或实现快照；期间 3 个 delegate reviewer 再次跨仓漂移到 `/root/.hermes/hermes-agent`，已按 fail-closed 全部丢弃，最终以 controller-side live diff 与实测结果完成评审）；安全/集成 SAFE（静态扫描当前 live diff 新增行 0 命中，且未改 auth/menu/route/backend 逻辑；real API replay 再次确认 user/supplier 菜单与 supplier settlements 真实接口均健康）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅收敛两个 UI label 与对应 focused proof，不引入额外请求、状态分支、数据库查询或部署复杂度变化，重建后 `web` / `api` 容器恢复 healthy）。

### 最新执行检查点（2026-05-07，Dashboard hero / role-actions scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录、不改菜单权限真值、不改 Dashboard 角色动作运行时路径与 Phase 4/5 后端契约的前提下，仅把 `web/src/pages/DashboardPage.tsx` 与 `web/src/pages/DashboardPage.test.tsx` 中仍残留的 page-global hero / supplier-action 断言收紧到真实运行时语义锚点；具体为 Dashboard 顶部 hero 补齐 `dashboard-hero-card`，为角色动作栅格补齐 `dashboard-role-actions` 与 `dashboard-role-action-*`，并把测试里的 `共享控制台入口`、供应商 `前往域名管理` / `调整供货规则` 等命中从 page-global 查询收敛为 hero / role-actions 容器内 scoped 合同，继续保持“同一控制台总览页内按角色展示下一步动作，而不是依赖脆弱的全页同名按钮命中”的产品方向。
- 控制器按 TDD 先修改 `web/src/pages/DashboardPage.test.tsx`：RED 阶段先要求 hero 标签与 admin/supplier role action cards 都必须通过新语义锚点命中，再最小修改 `web/src/pages/DashboardPage.tsx` 仅新增 3 处运行时锚点完成 GREEN；随后重新通过 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx`（1 file / 8 tests 全绿）、`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx`（2 files / 34 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行 `docker compose up -d --build web api` 确认容器内运行时已更新到最新前端/后端镜像。
- 真实 API 方面新增并重跑本地回放脚本 `.hermes/local-output/run_dashboard_role_anchor_real_api.sh`；最新产物目录为 `.hermes/local-output/dashboard-role-anchor-real-api/20260507-155515`。本轮再次验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200，且菜单继续只暴露 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs`、`/settings`；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/supplier/resources/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200，且菜单继续暴露 `/supplier/domains`、`/supplier/offerings`、`/api-keys`、`/webhooks`、`/docs`；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；同时前端 `/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers`、`/supplier/domains` 的 SPA shell 全部返回 200，证明本轮仅收紧 Dashboard 前端语义锚点与 focused 合同，没有破坏 shared-console 角色菜单或 Phase 4/5 真实 API 链路。
- 本轮五维评审结论：产品/规格 PASS（本次 diff 仅把 Dashboard hero 与角色动作 proof 从 page-global 命中收紧为真实容器级合同，没有改变单壳 shared-console/new-api 叙事，反而更准确地表达“总览 hero”和“角色动作栅格”是两个不同表面）；代码质量 APPROVED（运行时代码仅新增 3 个最小语义锚点，测试收紧为 `within(hero)` / `within(roleActions)` 与动作卡级 testid，没有引入 `.closest(...)`、wrapper class 依赖或过度实现快照；期间 5 个 delegate reviewer 全部发生跨仓漂移并审到无关仓库，已按 fail-closed 全量丢弃，最终以 controller-side live diff 与实测结果完成评审）；安全/集成 SAFE（静态扫描当前 live diff 新增行未发现 hardcoded secret / shell / eval / SQL 注入问题，且未改任何 auth/menu/route 常量或后端处理逻辑；real API replay 再次确认 user/supplier/admin 菜单与 Dashboard / supplier / admin API 均健康）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；本轮新增锚点让 hero 与 supplier/admin role actions 的断言更稳定，既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（新增仅为前端 data-testid 语义锚点，不引入额外请求、状态分支、持久化或部署拓扑变化；重建后的 `web` / `api` 容器已恢复 healthy，本地 ignored 回放脚本可继续复用）。

### 最新执行检查点（2026-05-07，shared-console route-stub canonical heading 二次收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、不改菜单权限、不改 Phase 4/5 后端契约与运行时代码的前提下，仅把 6 个 shared-console focused 测试文件中仍残留的菜单级 route-stub 标题继续收敛到当前真实页面 canonical heading；具体将 `web/src/components/AppSidebar.test.tsx`、`web/src/components/ConsoleLayout.test.tsx`、`web/src/pages/ApiKeysPage.test.tsx`、`web/src/pages/BalancePage.test.tsx`、`web/src/pages/DashboardPage.test.tsx`、`web/src/pages/SettingsPage.test.tsx` 里的 `/webhooks` 落点从旧短标题 `Webhook 设置` 统一收紧为 `开发者 Webhook 接入工作台`，将 `/docs` 落点从旧短标题 `API 文档` 统一收紧为 `API 文档与接入控制台`，并把一处 Dashboard API Keys 落点从宽泛 `findByText(...)` 收紧为 `role=heading` 语义合同，继续保持“菜单标签可以短、落点页面身份必须以真实 canonical heading 为准”的单壳 shared-console 方向。
- 控制器按 TDD 先从测试合同入手做最小垂直切片：先让 6 个 route-stub focused 用例在 RED 阶段显式要求新的页面级 heading，再仅修改对应测试夹具与断言完成 GREEN；随后重新通过 `pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx src/pages/ApiKeysPage.test.tsx src/pages/DashboardPage.test.tsx src/pages/SettingsPage.test.tsx src/pages/BalancePage.test.tsx`（6 files / 37 tests 全绿）、`pnpm --dir web exec vitest run src/App.test.tsx`（1 file / 26 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并对 live diff 执行控制器静态扫描确认新增行未引入 secret / shell / eval / SQL 注入问题。
- 真实 API 方面新增并重跑本地回放脚本 `.hermes/local-output/run_route_heading_canonical_real_api.sh`；最新产物目录为 `.hermes/local-output/route-heading-canonical-real-api/20260507-152802`，并已同步写入 `.hermes/local-output/route-heading-canonical-real-api/latest.txt`。本轮再次验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；前端 `/`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 的 SPA shell 均返回 200，证明本轮仅收紧前端 route-stub 页面身份合同，没有破坏真实共享壳与 Phase 4/5 API 链路。
- 本轮五维评审结论：产品/规格 PASS（本轮目标是把测试夹具继续对齐到真实页面级身份，而不是回退到菜单短标题；`/webhooks` 与 `/docs` 现在在 6 个 focused harness 中统一命中当前运行时 `开发者 Webhook 接入工作台` / `API 文档与接入控制台`，单壳 shared-console 叙事更一致）；代码质量 APPROVED（diff 仅落在测试层，route-stub `<section><h1>` 语义更统一，且一处 Dashboard `findByText` 已同步收紧为 `findByRole('heading', ...)`；期间 3 个 delegate reviewer 发生跨仓漂移并引用了无关仓库，已按 fail-closed 全部丢弃，最终以 controller-side live diff、实测命令与真实仓库文件为准完成评审）；安全/集成 SAFE（控制器静态扫描新增行 0 命中，且本轮未改任何鉴权、菜单、路由常量或后端处理逻辑；route-stub canonical heading 进一步减少了菜单标题与页面标题混用导致的前端集成合同漂移风险）；测试/可靠性 PASS（focused Vitest、App 集成 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯测试层 route-stub 语义收敛，不新增运行时查询、轮询、镜像层复杂度或部署拓扑变化；真实 API 脚本已保存到本地 ignored 输出目录，后续可直接复跑）。

### 最新执行检查点（2026-05-07，Login 单壳入口信息减法 + 认证表单可读性修复）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、会话持久化、角色菜单真值与 Phase 4/5 后端契约的前提下，继续把 `web/src/pages/LoginPage.tsx` / `web/src/pages/LoginPage.test.tsx` 收敛为更像单壳控制台入口而非多角色营销页；具体移除左侧 `login-role-workspaces` 三张角色工作区卡与开发环境快捷账号 banner，把 readiness 区块重写为 `统一认证 / 共享 shell / 角色菜单 / 开发接入` 四张更短摘要卡，并把注册 runway 从 `API Keys 起步 / Webhook 联调 / 文档回放` 调整为更中性的 `创建 Key / 配置 Webhook / 对照文档`。在此基础上，本轮新增修复右侧认证卡暗色表单可读性：为登录/注册表单输入、placeholder、label、提示 banner 与底部 helper copy 明确补齐深色壳专用高对比样式，避免 `fieldStyle` 误落在 wrapper 而导致输入文本仍沿用 Semi 默认深色字、浏览器视觉上近似“表单消失”。
- 控制器先按 TDD 收紧 `web/src/pages/LoginPage.test.tsx`：RED 阶段先让 focused 用例显式断言新的 readiness / runway copy 与邮箱、密码输入高对比可读性；随后最小修改 `web/src/pages/LoginPage.tsx` 完成 GREEN。中途根据浏览器视觉与 DOM 证据执行一次系统化调试：先发现运行时 `fieldStyle.color` 未真正作用到 input/label，浏览器 `getComputedStyle` 仍返回深色文本；再对照 Semi UI `input/index.tsx` / form label 实现，确认真正需要的是 `inputStyle` + 局部 class 覆盖，而不是继续叠加无效 wrapper 样式。最终重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx`（1 file / 5 tests 全绿）、`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx`（2 files / 31 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并两次执行 `docker compose up -d --build web api` 确认真正热更新到容器运行时。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`；本轮最新产物目录为 `.hermes/local-output/app-login-register-scope/20260507-150244`，并已同步写入 `.hermes/local-output/app-login-register-scope/latest.txt`。本轮再次验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` 的 SPA 壳路由全部返回 200。浏览器侧进一步复核 `http://127.0.0.1/login?ts=20260507-1505`：表单 label/placeholder/banner/footer 文本颜色均已提升到可读状态，视觉评审确认右侧 auth card 已从“表单几乎不可见”恢复为可正常使用的单壳登录入口。
- 本轮五维评审结论：产品/规格 PASS（Login 页面继续朝单壳入口做信息减法，不再在登录前过度展开角色工作区叙事；注册成功后仍直达同一共享控制台，并保留 API Keys/Webhooks/Docs 这条统一接入路径）；代码质量 APPROVED（运行时代码改动集中在 Login 单页，先通过 DOM/库实现定位根因，再以 `inputStyle` + 局部 class 覆盖做最小修复，没有引入全局样式污染或额外状态分支；测试继续保留 heading/CTA/route-stub 与可读性合同双重保护）；安全/集成 SAFE（静态扫描当前 live diff 新增行未发现 hardcoded secret / shell / eval / SQL 注入问题，且未改鉴权、菜单、路由常量或后端接口；真实 user/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯前端入口页文案/样式与测试合同收敛，不新增查询、轮询或部署拓扑；浏览器视觉与 controller-side DOM 都确认修复已真实生效，后续可继续把相同暗色表单可读性审计推广到其他 shared-console 页面）。

### 最新执行检查点（2026-05-07，Login/Webhooks route-stub canonical heading semantic alignment）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录运行时代码、不改菜单权限、不改 Phase 4/5 后端契约的前提下，仅把 `web/src/pages/LoginPage.test.tsx` 与 `web/src/pages/WebhooksPage.test.tsx` 中仍残留的低信号 route-stub heading 合同继续对齐到当前真实页面身份；具体将 Login 页内 `/webhooks` 路由桩从菜单级短标题 `Webhook 设置` 收紧为页面级 canonical heading `开发者 Webhook 接入工作台`，将 Login/Webhooks 两处 `/docs` 路由桩统一从旧标题 `API 文档` 收紧为真实页面标题 `API 文档与接入控制台`，并把 Login 页 `/` 落点桩从纯文本 `div` 升级为 `<section data-testid="shared-console-home"><h1>控制台总览</h1></section>`，继续保持“登录/注册入口、API Keys/Webhooks/Docs 与 Dashboard 都属于同一登录后共享控制台单壳，而不是由测试夹具偷偷退化成占位页文案”的产品方向。
- 控制器已按 TDD 先修改 `web/src/pages/LoginPage.test.tsx` 与 `web/src/pages/WebhooksPage.test.tsx`：RED 阶段先要求 Login 内部 route-stub 与 Webhooks integration loop 的 docs 落点命中新的 canonical heading 合同，再最小调整测试夹具与断言完成 GREEN；随后重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/pages/WebhooksPage.test.tsx`（2 files / 15 tests 全绿）、`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/pages/WebhooksPage.test.tsx src/App.test.tsx`（3 files / 41 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`。当前 live diff 仅落在 `web/src/pages/LoginPage.test.tsx`、`web/src/pages/WebhooksPage.test.tsx`，未混入运行时代码。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh` 与 `.hermes/local-output/run_webhooks_integration_scope_real_api.sh`；最新产物目录分别为 `.hermes/local-output/app-login-register-scope/20260507-134611` 与 `.hermes/local-output/webhooks-integration-scope/20260507-134613`，并已同步写入各自 `latest.txt`。本轮再次验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；SPA `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` 继续返回 200，证明本轮只收紧测试层 route-stub 页面身份合同，没有破坏真实共享壳、角色菜单或 Phase 4/5 后端链路。
- 本轮五维评审结论：产品/规格 PASS（Login/Webhooks 的测试桩标题现已与 `WebhooksPage.tsx` / `ApiDocsPage.tsx` 当前真实页面 canonical heading 对齐，且 `/` stub 升级为语义化首页 heading，不再把菜单短标题误当作目标页身份）；代码质量 APPROVED（改动局限于 2 个 focused 测试文件，继续保留 `data-testid` + `role=heading` 双合同，没有引入 `.closest(...)`、page-global 宽泛命中或仅凭文本存在性的脆弱断言）；安全/集成 SAFE（静态扫描当前 live diff 新增行未发现 hardcoded secret / shell / eval / SQL 注入问题，且未改任何 path、权限菜单或鉴权逻辑；route-stub 标题对齐到页面级身份后，反而减少了菜单标题/页面标题混用导致的合同漂移风险）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check` 与两组真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯测试层语义收敛，不引入运行时代码、部署步骤或额外回放复杂度，仅提高后续 route-stub 评审对页面身份合同的可维护性）。

### 最新执行检查点（2026-05-07，Dashboard admin/supplier role-surface scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/Phase 4/5 后端契约与 Dashboard 运行时代码的前提下，仅把 `web/src/pages/DashboardPage.test.tsx` 从此前只覆盖普通用户 `预算 → 采购 → 履约 → 接入` 路径，扩展为覆盖管理员 / 供应商角色在同一登录后控制台内的 role-aware 工作台合同；具体新增 admin `dashboard-role-surface-map` / `dashboard-admin-ops-summary-card` 的 scoped proof，以及 admin CTA 到 `供应商管理` / `风控中心` / `审计日志` 的 route-stub + canonical heading 导航合同，同时为 supplier 增加 `域名池运营中枢` / `供货规则编排中枢` 两条 Dashboard 动作链路 proof，继续保持“同一套深色共享控制台，通过角色权限区分工作区，不拆独立后台”的产品方向。
- 控制器先按 TDD 在 `web/src/pages/DashboardPage.test.tsx` 写出 admin/supplier 新用例并确认 RED，再最小补齐同文件测试夹具：新增 admin/supplier route 常量导入、`seedSession(...)` 帮助器，以及 `/admin/suppliers`、`/admin/risk`、`/admin/audit`、`/supplier/domains`、`/supplier/offerings` 五条 route-stub `<section><h1>` 合同；过程中根据代码质量评审把一处 `getByText(ADMIN_RISK_ROUTE)` 的路径字符串断言回退为更真实的 role-surface 按钮数量断言，并把 supplier 导航 proof 收紧为点击真实 CTA 后命中当前运行时 canonical heading。随后重新通过 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx`（8 tests 全绿）、`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx`（2 files / 34 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_dashboard_admin_scope_real_api.sh` 与 `.hermes/local-output/run_dashboard_lane_scope_real_api.sh`；最新产物目录分别为 `.hermes/local-output/dashboard-admin-scope/20260507-132331` 与 `.hermes/local-output/dashboard-lane-scope/20260507-132334`。本轮再次验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户 `POST /api/v1/auth/register` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；SPA `/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` 继续返回 200，证明本轮仅收紧 Dashboard 前端 focused 合同，没有破坏真实共享壳、角色菜单或 Phase 4/5 后端链路。
- 本轮五维评审结论：产品/规格 PASS（新增用例准确覆盖 Dashboard 中 admin/supplier 在同一 shared-console 单壳内的角色工作面与 CTA 去向，没有把独立后台叙事带回；一条 reviewer 建议已将 supplier 目标标题对齐为当前真实页面 `域名池运营中枢` / `供货规则编排中枢`）；代码质量 APPROVED（改动局限于单个 focused 测试文件，持续使用 `within(...)`、route 常量与 heading 合同，未引入 `.closest(...)` 或 page-global 宽泛命中；一条对原始路径字符串断言的非阻塞意见已同轮修正）；安全/集成 SAFE（当前 diff 为测试层变更，控制器静态扫描新增行未发现 hardcoded secret / shell / eval / SQL 注入问题，且 admin/supplier route 常量、真实页面标题与 live `/auth/menu` 契约复核一致）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check` 与两组真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅前端测试合同扩展，不新增运行时查询、轮询或部署复杂度，容器健康状态维持正常）。

### 最新执行检查点（2026-05-07，SupplierResources canonical heading route-stub 对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改 `SupplierResourcesPage.tsx` 运行时代码、不改注册/菜单权限/Phase 4/5 后端契约的前提下，仅把 `web/src/pages/SupplierResourcesPage.test.tsx` 中供应商资源页 3 条跨页 route-stub heading 合同对齐到当前真实角色页面标题；具体把供应商域名落点从 `域名管理` 收紧为 `域名池运营中枢`，把供应商 Webhook 落点从低信号菜单标签 `Webhook 设置` 收紧为 supplier 运行时标题 `供给事件回调工作台`，把文档落点从通用 `API 文档` 收紧为 `API 文档与接入控制台`，继续保持“供应商资源 → 域名/供货/结算 → API Keys/Webhooks/Docs”全部仍位于同一登录后共享控制台单壳，而非拆分第二套后台。
- 控制器已按 TDD 先修改 `web/src/pages/SupplierResourcesPage.test.tsx`：RED 阶段先把 3 个 route-stub `<section><h1>` 与对应导航断言切换到新的 canonical heading 合同，再复跑 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx` 完成 GREEN；随后继续通过 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/App.test.tsx`（2 files / 37 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`。当前 live diff 仅落在 `web/src/pages/SupplierResourcesPage.test.tsx`，未混入运行时代码或其他页面改动。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_supplier_resources_label_contract_real_api.sh`；最新产物目录已写入 `.hermes/local-output/supplier-resources-label-contract-real-api/latest.txt`，本轮再次验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/supplier/resources/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200，管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；摘要继续确认 supplier 菜单暴露 `/supplier/resources`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs`，shared-console 单壳与真实 API 契约未漂移。
- 本轮五维评审结论：产品/规格 PASS（controller-side 复核以当前角色运行时页面标题为准：`SupplierDomainsPage.tsx` 为 `域名池运营中枢`、`WebhooksPage.tsx` supplier 视角为 `供给事件回调工作台`、`ApiDocsPage.tsx` 为 `API 文档与接入控制台`；本次 diff 仅把 SupplierResources focused route-stub 合同对齐到这些真实落点，不改变单壳叙事）；代码质量 APPROVED（改动局限于单个 focused 测试文件，继续保留 click → route-stub → heading 双合同，没有退化为纯 testid 存在性检查；一份子评审把 `consoleNavigation.tsx` 的菜单 title 误当成页面 hero heading，已按 repo-scoped controller 事实复核降为非阻塞误判）；安全/集成 SAFE（静态扫描当前 live diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且未改任何路由常量、权限菜单或后端接口；真实 supplier/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯测试合同对齐，不引入新请求、部署步骤或运行时分支）。

### 最新执行检查点（2026-05-07，ApiDocs route-stub semantic heading harness 对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改 `ApiDocsPage.tsx` 运行时代码、不改注册/菜单权限/Phase 4/5 后端契约的前提下，仅把 `web/src/pages/ApiDocsPage.test.tsx` 中仍残留的低信号纯文本 route stub harness 收敛为更贴近 shared-console canonical heading 合同的 `<section data-testid="..."> + <h1>` 结构；具体将项目市场、余额中心、API Keys、Webhook 设置与 shared-home 5 条测试路由桩，从纯 `<div>` 文本升级为语义化 heading 容器，继续保持“文档 → API Keys / Webhooks / 余额 / 项目市场 → 控制台总览”全部仍属于同一登录后共享控制台单壳，而不是退回到仅靠弱文本命中的占位页证明。
- 控制器已按 TDD 先修改 `web/src/pages/ApiDocsPage.test.tsx` 测试 harness：RED 阶段先把 route stub 容器升级为 `<section> + <h1>` 语义合同，再复跑 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 完成 GREEN；随后继续通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/App.test.tsx`（2 files / 31 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`。当前 live diff 仅落在 `web/src/pages/ApiDocsPage.test.tsx`，`git diff --name-only` 也确认无其他文件混入本里程碑。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_front_route_heading_stub_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/front-route-heading-stub-scope/20260507-123136`；本轮已验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201，随后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 均返回 200；同时前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs` 的 SPA shell 全部返回 200，且产物摘要继续确认 user/admin/supplier 菜单路径均符合 shared-console 单壳模型。
- 本轮五维评审结论：产品/规格 PASS（子评审确认本次 diff 仅把 ApiDocs route stub 从纯文本占位升级为语义化页面 heading 容器，既未扭曲 shared-console canonical title，也增强了落点页面身份表达）；代码质量 APPROVED（diff 仅收敛测试 harness 结构，无运行时代码改动；route stub 从 `<div>` 提升为 `<section> + <h1>` 后可读性与语义性更强，未退化为过拟合实现细节）；安全/集成 SAFE（静态扫描当前 live diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且 5 维集成复核确认 route constants、权限菜单与真实 API 回放均未受影响）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯测试语义结构增强，不引入新请求、状态分支或部署复杂度，同时使后续 docs/shared-console route-stub 评审更容易基于 heading 合同 fail-closed）。

### 最新执行检查点（2026-05-07，Login register runway densification rollback → tighter copy / stable md triplet）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、会话持久化、角色菜单真值与 Phase 4/5 后端契约的前提下，继续收敛 `web/src/pages/LoginPage.tsx` 左侧注册路径区域，使其更接近 new-api / Linear 的控制台入口而非营销式说明；具体把注册 runway 标题从“注册后进入同一套控制台”压缩为“首轮接入路径”，将 3 张子卡片统一缩短为 `API Keys 起步` / `Webhook 联调` / `文档回放`，同步把说明文案压缩为“注册后直接进入同一深色控制台，用更短的首轮路径完成 API 接入、回调联调与文档回放”，并保留 `md=8` 三列布局以避免为文案微调引入过大的响应式回退，继续保持“注册成功后进入同一套共享控制台，而非独立新手页或多后台分叉”的产品叙事。
- 控制器已按 TDD 先修改 `web/src/pages/LoginPage.test.tsx`：RED 阶段先显式要求 register journey 出现新的 `首轮接入路径` / `API Keys 起步` / `Webhook 联调` / `文档回放` 合同并确认旧标题消失，随后最小修改 `web/src/pages/LoginPage.tsx` 完成 GREEN；期间一轮代码质量评审指出测试不应继续过度绑定整段静态说明文案、且不应把 `md` 断点从三列退化到单列，于是控制器同轮回退该两处问题，仅保留新的标题级合同与更紧凑的运行时 copy。最终重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx`（5 tests 全绿）、`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx`（2 files / 31 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/app-login-register-scope/20260507-121524`；本轮已验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201，随后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；同时 `GET /`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` 这些 SPA 壳路由仍全部返回 200，证明本轮仅压缩登录页注册 runway 文案与层级，没有破坏 shared-console 真正落地路径或角色路由可达性。
- 本轮五维评审结论：产品/规格 PASS（Login 左侧 register journey 已进一步从 onboarding 说明收敛为更像控制台入口的“首轮接入路径”，且继续强调注册后进入同一 shared shell；一轮产品评审建议避免 `Docs` 英文残留后，控制器已同轮收敛为 `文档回放`）；代码质量 APPROVED（运行时代码仅压缩 1 组标题/说明与 3 张子卡片文案，测试只保留标题级合同与旧标题消失 proof，没有继续把整段 copy 当作阻塞性合同；一轮评审指出 `md` 单列回退过大后，控制器已恢复为 `md=8` 的稳定三列布局）；安全/集成 SAFE（静态扫描当前 live diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且登录/注册、session 持久化、`navigate('/')`、`/auth/menu` 真值与 API Keys/Webhooks/Docs 共享接入路径均未改变；真实 user/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check`、容器重建与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅登录页静态 copy/层级微调，无新查询、分支或部署负担；浏览器视觉复核确认左侧已较上一轮更少 marketing 感，当前主要剩余问题只是左列整体仍略重于右侧登录卡，可在后续继续做更小的视觉减法，而不必回滚本轮收口）。

### 最新执行检查点（2026-05-07，AdminSuppliers metrics / shared-console links scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册、菜单权限、管理员供应商运营后端契约与 Phase 4/5 业务逻辑的前提下，仅把 `web/src/pages/AdminSuppliersPage.test.tsx` 首个供应商运营台 focused 用例中仍残留的 page-global 指标/bridge 文本命中，收紧为真实运行时卡片语义锚点 scoped 合同；具体把 4 张顶部 mission signal 指标卡补齐 `admin-suppliers-metric-pending` / `-risk` / `-dispute` / `-console` 最小锚点，并把共享接入桥接里的 API Keys / Webhook / Docs 标签收敛到新的 `admin-suppliers-shared-console-links` 容器内断言，继续保持“管理员供应商运营、风控、审计与接入仍位于同一登录后深色共享控制台单壳内”的产品叙事，不引入独立后台或新的导航分叉。
- 控制器已按 TDD 先修改 `web/src/pages/AdminSuppliersPage.test.tsx`：RED 阶段先显式要求 metric card 与 shared-console-links 的 scoped 合同，再最小修改 `web/src/pages/AdminSuppliersPage.tsx` 仅为 `missionSignals` 卡片与共享接入标签容器补齐最小 `data-testid` 完成 GREEN；随后重新通过 `pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx`（7 tests 全绿）、`pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx src/App.test.tsx`（2 files / 33 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web api gateway`。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/verify_admin_suppliers_front_slice.sh`，最新产物目录为 `.hermes/local-output/admin-suppliers-front-slice-run`；本轮已验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户 `POST /api/v1/auth/register` 返回 201，随后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview` 均返回 200，且菜单继续只暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 等 shared 路径；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200，且菜单继续暴露 `/admin/suppliers`、`/admin/users`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs` 等单壳管理员/共享接入路径；前端 `/admin/suppliers`、`/api-keys`、`/webhooks`、`/docs` SPA shell 全部返回 200。
- 本轮五维评审结论：产品/规格 PASS（本次 diff 仅把 AdminSuppliers 首个 focused 用例从 page-global 文本收紧为 metric/bridge scoped 合同，不改变 shared-console 单壳、角色差异菜单或管理员供应商运营页定位）；代码质量 APPROVED（运行时代码仅新增 5 个最小语义锚点，测试继续锚定真实标题/指标 helper/bridge 标签，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（控制器静态扫描当前 live diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且真实 user/admin 菜单与 overview/risk/audit 回放继续健康）；测试/可靠性 PASS（focused Vitest、AdminSuppliers+App 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（本轮仅前端语义锚点与测试合同收敛，不新增请求、状态分支或部署复杂度；`docker compose up -d --build web api gateway` 后健康检查继续通过）。下一前端优先项继续优先清理 `AdminSuppliersPage.test.tsx` 之外仍残留的 page-global 指标/bridge/fallback proof，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，AdminUsers metrics / shared-console bridge scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册、菜单权限、管理员资金/争议后端契约与 Phase 4/5 业务逻辑的前提下，仅把 `web/src/pages/AdminUsersPage.shared-console.test.tsx` 首个管理员资金工作台 focused 用例中仍残留的 page-global 指标/bridge 文本命中，收紧为真实运行时卡片语义锚点 scoped 合同；具体把 4 张顶部 mission signal 指标卡补齐 `admin-users-metric-wallet` / `-settlement` / `-disputes` / `-console` 最小锚点，并把共享接入桥接里的 API Keys / Webhook / Docs 标签收敛到新的 `admin-users-shared-console-links` 容器内断言，继续保持“管理员账务、风控、审计与接入仍位于同一登录后深色共享控制台单壳内”的产品叙事，不引入独立后台或新的导航分叉。
- 控制器已按 TDD 先修改 `web/src/pages/AdminUsersPage.shared-console.test.tsx`：RED 阶段先显式要求 metric card / mission-flow heading / shared-console-links 的 scoped 合同，再最小修改 `web/src/pages/AdminUsersPage.tsx` 仅为 `missionSignals` 卡片与共享接入标签容器补齐最小 `data-testid` 完成 GREEN；随后重新通过 `pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx`（4 tests 全绿）、`pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx src/pages/AdminUsersPage.test.tsx src/App.test.tsx`（3 files / 35 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。
- 真实 API 方面新增本地回放脚本 `.hermes/local-output/run_admin_users_metric_scope_real_api.sh`，最新产物目录记录在 `.hermes/local-output/admin-users-metric-scope/latest.txt`，本轮已验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`；普通用户 `POST /api/v1/auth/register` 返回 201，随后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200，且访问 `GET /api/v1/admin/overview` 正确返回 403；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5`、`/api/v1/admin/wallet-users`、`/api/v1/admin/disputes?limit=5` 均返回 200，且菜单继续暴露 `/admin/users`、`/admin/risk`、`/admin/audit`、`/api-keys` 等单壳管理员/共享接入路径。
- 本轮五维评审结论：产品/规格 PASS（本次 diff 仅把 AdminUsers 首个 focused 用例从 page-global 文本收紧为 metric/bridge scoped 合同，不改变 shared-console 单壳、角色差异菜单或管理员资金页定位）；代码质量 APPROVED（运行时代码仅新增 5 个最小语义锚点，测试继续锚定真实标题/指标 helper/bridge 标签，没有退化为纯 testid 存在性检查；一次 delegate 代码质量评审漂移到无关仓库，已按 fail-closed 丢弃并由控制器基于 live diff 与实测复核）；安全/集成 SAFE（控制器静态扫描当前 live diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且真实普通用户 403 与管理员 overview/risk/audit/wallet-users/disputes 回放继续健康；一份 delegate 安全/集成评审同样发生跨仓污染，已丢弃）；测试/可靠性 PASS（focused Vitest、AdminUsers+App 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（本轮仅前端语义锚点与测试合同收敛，不新增请求、状态分支或部署复杂度；`docker compose ps` 持续显示 web/api/gateway healthy）。下一前端优先项继续优先清理 `AdminUsersPage.shared-console.test.tsx` 之外仍残留的 page-global 指标/bridge/fallback proof，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，Login control-plane readiness / role workspaces 收敛）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、会话持久化、角色菜单真值与 Phase 4/5 后端契约的前提下，将 `web/src/pages/LoginPage.tsx` 左侧从泛 marketing 说明收敛为更接近 new-api / Linear 的共享控制台入口；具体移除旧的泛化 feature/hero signal 卡片，改为 `真实控制台合同` readiness 面板与 `同一壳内角色工作区` 三张角色卡，明确同一认证入口、服务端角色菜单真值、Dashboard 落点与 API Keys / Webhooks / Docs 接入路径，同时保留 tablist 登录/注册切换、register journey 与注册成功直达 shared shell 的行为合同。
- 控制器已按 TDD 先修改 `web/src/pages/LoginPage.test.tsx`：RED 阶段先要求出现 `真实控制台合同`、`注册 / 登录 -> 同一入口`、`角色菜单 -> 服务端角色真值`、`控制台首页 -> Dashboard 已就绪`、`API 接入 -> Keys / Webhooks / Docs`，以及 `同一壳内角色工作区` 下的用户/供应商/管理员三张角色卡；随后最小修改 `web/src/pages/LoginPage.tsx` 完成 GREEN，并重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx`（5 tests 全绿）、`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx`（2 files / 31 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`docker compose up -d --build web gateway` 与 `git diff --check`。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/app-login-register-scope/20260507-101853`；本轮已验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201，随后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA shell 全部返回 200，且 fresh browser snapshot 已确认 `/login` 渲染新的 readiness/workspace 合同。由于 `browser_vision` 本轮出现上游 504 / 白屏式工具态异常，控制器按 runbook 以 fresh snapshot + DOM/HTTP + controller-side 构建事实作为本地视觉验收依据，未将该工具噪声误判为应用回归。
- 本轮五维评审结论：产品/规格 PASS（左侧已从泛 marketing copy 收敛为更接近控制台入口的 readiness / role workspace 合同，单壳与角色菜单真值叙事更直接；非阻塞剩余点是 register runway 仍稍长，可后续继续压缩）；代码质量 APPROVED（运行时代码仅替换左侧结构并保留现有 auth/register 行为，focused test 仍锚定 heading/tablist/button 与真实运行时合同，没有退化为纯 testid 存在性断言；外部 reviewer 一度发生跨仓漂移，已按 fail-closed 丢弃并由控制器基于 live diff 复核）；安全/集成 SAFE（静态扫描当前 live diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，登录/注册、session 持久化、shared-shell 导航与角色菜单真值合同均未改动；真实 user/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、容器重建与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅前端结构/copy 收敛，不新增请求、状态分支或部署复杂度；`docker compose ps` 继续显示 web/api/gateway healthy）。下一前端优先项继续围绕 shared-console 登录入口与壳内关键页面收敛视觉密度/语义合同，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，Login shared-console dark entry visual tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、会话持久化、角色菜单真值与 Phase 4/5 后端契约的前提下，优先收敛 `web/src/pages/LoginPage.tsx` 的登录/注册入口视觉层级，使其更接近 new-api / Linear 风格的深色单一控制台入口；具体把左侧 hero 标题从偏营销的“邮件接码业务的统一运营控制台”收敛为更直接的“统一登录后控制台”，同步压暗并统一 hero signal / feature / registration runway 三组卡片的背景、边框、阴影与高亮色，收敛 icon 与 CTA 主色到 `#5e6ad2` 系列，并把认证卡 eyebrow 从“统一控制台认证”收敛为“统一控制台登录”，继续保持“注册成功后进入同一套共享控制台，而非独立新手页或多后台分叉”的产品叙事。
- 控制器已按 TDD 先修改 `web/src/pages/LoginPage.test.tsx` 与 `web/src/App.test.tsx`：RED 阶段先把首屏 heading / auth-shell eyebrow 合同切换到 `统一登录后控制台` / `统一控制台登录`，并要求登录页仍保留 register journey 与 `tablist` 切换；随后最小修改 `web/src/pages/LoginPage.tsx` 完成 GREEN。首轮代码质量评审指出首屏测试对大量静态 copy 过拟合后，控制器已在同轮把 `LoginPage.test.tsx` 的首屏 proof 收敛回结构/可交互合同（hero heading、auth shell、tablist、journey CTA），避免把文案微调误当作行为回归；最终重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx`（2 files / 31 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`docker compose up -d --build web` 与 `git diff --check`。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/app-login-register-scope/20260507-093100`；本轮已验证：`GET /healthz` 返回 200 且 `status=ok` / `redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201，随后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA shell 全部返回 200，证明本轮仅收敛登录入口视觉与测试合同，未破坏真实认证、注册回流、角色菜单或 shared-console 单壳导航。
- 本轮五维评审结论：产品/规格 PASS（浏览器视觉复核确认本页已明显比典型营销页更接近深色控制台入口，右侧登录卡成为主视觉中心；非阻塞剩余问题仅是左侧内容仍略偏营销化，可在后续继续减法）；代码质量 APPROVED（首轮评审指出首屏测试过度绑定文案、样式 token 内联较多；控制器已先收敛测试到结构/行为合同，本轮 diff 维持 3 文件 focused scope，未退化为纯 testid 存在性断言；样式 token 抽离记为后续优化债务，不阻塞当前垂直切片）；安全/集成 SAFE（静态扫描当前 live diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且 `login/register` 调用、session 持久化、`navigate('/')` 与 shared-shell `/auth/menu` 引导合同均未改动；真实 API 回放继续健康）；测试/可靠性 PASS（focused Vitest、前端 build、Go tests、compose config、`docker compose up -d --build web`、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（本轮仅前端样式/文案与测试合同收敛，不增加请求、状态分支或部署复杂度；浏览器 fresh-load `/login` 已确认新文案/新层级生效）。

### 最新执行检查点（2026-05-07，SupplierResources account/mailbox labels 中文合同收敛）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册、菜单权限、供应商资源接口契约与 Phase 4/5 后端逻辑的前提下，仅把 `web/src/pages/SupplierResourcesPage.tsx` 中第三方邮箱账号 / 邮箱池表单与账号表格里仍残留的英文运行时标签收敛为中文共享控制台合同；具体将 `Provider`、`Refresh Token`、`Secret Ref`、`Bridge Endpoint`、`Bridge Label`、`local part` 与账号表格列标题 `Provider` 分别收敛为 `服务商`、`刷新令牌`、`密钥引用`、`桥接端点`、`桥接标识`、`本地前缀`、`服务商`，并顺手把校验提示 `请输入 provider` 对齐为 `请输入服务商`，继续保持“供应商资源、API Keys、Webhook、Docs 全部位于同一登录后 shared-console 单壳内”的产品叙事，不派生独立供应商后台。
- 控制器已按 TDD 先修改 `web/src/pages/SupplierResourcesPage.test.tsx`，新增 focused 用例显式要求资源页运行时出现上述中文标签、旧英文标签消失、账号表格列标题同步中文化；RED 阶段 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx` 明确报出 `Unable to find a label with the text of: 服务商`，证明运行时仍残留英文标签。随后最小修改 `web/src/pages/SupplierResourcesPage.tsx` 完成 GREEN，并在产品/规格评审指出校验提示仍残留英文术语后同轮把 `请输入 provider` 收敛为 `请输入服务商`；最终重新通过 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx`（11 tests 全绿）、`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/App.test.tsx`（2 files / 37 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。
- 真实 API 方面新增本地回放脚本 `.hermes/local-output/run_supplier_resources_label_contract_real_api.sh`，最新产物目录记录在 `.hermes/local-output/supplier-resources-label-contract-real-api/latest.txt`；最新回放已验证：`GET /healthz` 返回 `status=ok` 且 `redis=ok`，供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/supplier/resources/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200，且菜单继续暴露 `/supplier/resources`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs`；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；前端 `/supplier/resources` SPA shell 返回 200，证明本轮仅收敛 SupplierResources 中文运行时合同，并未破坏真实注册/登录、角色菜单、供应商资源链路与管理员运营入口。
- 本轮五维评审结论：产品/规格 PASS（首轮评审指出校验提示仍残留 `provider` 英文术语后，控制器已在同轮收敛为 `请输入服务商`；其余 UI 命名与 shared-console 单壳叙事一致）；代码质量 APPROVED（diff 仅落在 SupplierResources 页面与对应 focused 测试，测试继续锚定真实用户可见标签/表格合同，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（静态扫描 live diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且字段 key / 提交 payload / 权限边界未改，真实 supplier/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check`、容器重建与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯前端静态标签/校验文案与 focused 合同收敛，不引入新请求、状态分支或部署复杂度，本地 real-API 脚本也已按 live `/api/v1/admin/audit` 路径校正）。

### 最新执行检查点（2026-05-07，Profile capability bridge canonical heading 收敛）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 Phase 4/5 契约的前提下，仅把 `web/src/pages/ProfilePage.tsx` 中个人资料页 capability bridge 的 API 接入卡片文案从泛化的 `API 接入控制面 / 前往 API Keys` 收敛到与共享路由目录一致的 `API Keys / 打开 API Keys`，并同步把 `web/src/pages/ProfilePage.test.tsx` 中该桥接链路的 focused proof 收紧为“按钮仍作用域限定在 `profile-capability-bridge` 内，且 Webhooks / Docs 两条 shared-console 路由继续命中 canonical heading”；其中 API Keys 继续保持 route-stub 导航命中断言，以避免把跨页目标文案耦合到 Profile 页自身测试合同里，同时保持“个人资料页只作为 shared-console 单壳桥接中枢，不派生独立接入后台”的产品叙事。
- 控制器已按 TDD 先修改 `web/src/pages/ProfilePage.test.tsx` 让 focused 用例在 RED 阶段显式要求 capability bridge 内出现 `打开 API Keys`，并在点击 Webhook / Docs CTA 后命中 `开发者 Webhook 接入工作台`、`API 文档与接入控制台` 两个 canonical heading；随后最小修改 `web/src/pages/ProfilePage.tsx` 的 API 卡片标题与 CTA 文案完成 GREEN，并重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx`（7 tests 全绿）、`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/App.test.tsx`（2 files / 33 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`。
- 真实 API 方面新增本地回放脚本 `.hermes/local-output/run_profile_capability_canonical_heading_real_api.sh`，最新产物目录 `.hermes/local-output/profile-capability-canonical-heading/20260507-080547` 已验证：`GET /healthz` 返回 `status=ok` 且 `redis=ok`；普通用户 `POST /api/v1/auth/register` 成功，随后 `GET /api/v1/auth/menu`、`/api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200，且菜单继续暴露 `/api-keys`、`/webhooks`、`/docs`、`/profile` 并未泄露 supplier/admin 路径；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；前端 `/`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/settings`、`/admin/risk`、`/supplier/domains` 的 SPA shell 均返回 200，证明本轮仅收敛 Profile shared-console 桥接命名与合同，未破坏真实注册、角色菜单与关键前端/后端入口。
- 本轮五维评审结论：产品/规格 PASS（首轮评审指出 API Keys / Webhooks / Docs canonical heading 需避免假阳性后，控制器已把 API Keys 回退为 route-stub 导航命中、保留 Webhooks / Docs 的真实 canonical heading 合同，并保持 shared-console 单壳叙事一致）；代码质量 APPROVED（首轮评审提出不应把 Profile 页测试过度耦合到跨页 API Keys 文案，控制器已修正为仅在当前页收敛 API Keys 按钮文案，并把跨页 heading 强校验限制在 Webhooks / Docs 两条本轮能真实对齐的目标页上；最终 diff 保持 focused scope 稳定）；安全/集成 SAFE（静态扫描 live diff 新增行未发现 hardcoded secret / shell / eval / SQL 注入问题，且 route path、权限门控与真实 API 回放均保持健康；API Keys 路由仍复用共享目录真值 `/api-keys`）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅前端静态文案与测试合同调整，不引入新请求、状态分支、部署步骤或运行时开销）。

### 最新执行检查点（2026-05-07，Webhooks shared integration route-stub canonical heading 收敛）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 Phase 4/5 契约与 Webhook 真实投递链路的前提下，仅把 `web/src/pages/WebhooksPage.test.tsx` 中 shared integration loop 两条落点 proof 从低信号的纯 route-stub 存在性检查，收紧为 `webhooks-route-stub-api-keys` / `webhooks-route-stub-docs` + canonical heading 双合同；同时把测试夹具里旧的 `<div data-testid="api-keys-route-stub">开发者 API 接入工作台</div>`、`<div data-testid="api-docs-route-stub">API 文档</div>` 统一升级为 `<section data-testid="..."><h1>...</h1></section>` 结构，继续保持“API Keys → Webhooks → Docs 全部属于同一登录后 shared-console 单壳接入链路”的产品叙事，而不是退回到弱化的占位页文本命中。
- 控制器已按 TDD 先修改 `web/src/pages/WebhooksPage.test.tsx`，让 focused 用例在 RED 阶段显式要求点击 `先配置 API Keys` 后同时出现 `webhooks-route-stub-api-keys` 与 heading `开发者 API 接入工作台`，点击 `查看 API 文档` 后同时出现 `webhooks-route-stub-docs` 与 heading `API 文档`；随后保持运行时代码零改动，仅收敛测试夹具与断言完成 GREEN，并重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx`（10 tests 全绿）、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/WebhooksPage.test.tsx`（2 files / 36 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_webhooks_integration_scope_real_api.sh`，最新产物目录 `.hermes/local-output/webhooks-integration-scope/20260507-074214` 已验证：`GET /healthz` 返回 `status=ok` 且 `redis=ok`；普通用户 `POST /api/v1/auth/register` 成功，`GET /api/v1/auth/menu` 继续暴露 `/api-keys`、`/webhooks`、`/docs` 且不泄露 supplier/admin 路径，`GET /api/v1/auth/me`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints` 均返回 200；管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 均返回 200；前端 `/`、`/api-keys`、`/webhooks`、`/docs`、`/admin/audit` SPA 壳均返回 200，证明本轮仅收紧 Webhooks shared integration route-stub 合同，未破坏真实注册、角色菜单、Webhook/API Keys/Docs 单壳入口与管理员运营链路。
- 本轮五维评审结论：产品/规格 PASS（diff 仅把 Webhooks integration loop 的两条目标页 proof 从低信号 route-stub 存在性收紧为 route-stub + canonical heading 双合同，更贴近 shared-console 单壳真实用户可见结果，不改变产品叙事）；代码质量 APPROVED（测试夹具统一改用 `<section> + <h1>` 语义结构，断言从纯 testid 存在性升级为语义 heading 合同，没有退化为实现细节快照；一份跨仓库漂移的 delegate 评审已按 fail-closed 丢弃）；安全/集成 SAFE（控制器静态扫描当前 live diff 新增行未发现 hardcoded secret / shell / eval / SQL 注入问题，且本轮仅改测试层；真实 user/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（纯测试合同与夹具语义收敛，不引入运行时查询、部署步骤或构建复杂度增量）。

### 最新执行检查点（2026-05-07，SupplierOfferings mission-step 中文标签对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 Phase 4/5 契约与供应商供货逻辑的前提下，仅把 `web/src/pages/SupplierOfferingsPage.tsx` 中供应商主任务流仍残留的 3 个英文步骤标签 `Resources` / `Settlement` / `Shared Console` 收敛为中文运行时合同 `资源准备` / `结算反馈` / `统一控制台`，并同步把 `web/src/pages/SupplierOfferingsPage.test.tsx` 的 focused proof 收紧到 mission-flow 区域内对这 3 个新标签的显式断言，继续保持“供应商资源 → 供货规则 → 供应商结算 → API Keys/Webhooks/Docs 全部位于同一登录后深色 shared-console 单壳内”的产品叙事，不引入第二套供应商后台。
- 控制器已按 TDD 先修改 `web/src/pages/SupplierOfferingsPage.test.tsx`，让 focused 用例在 RED 阶段显式要求 mission-flow 内出现 `资源准备` / `结算反馈` / `统一控制台`；首轮 RED 失败证明运行时仍输出英文标签。随后仅最小修改 `web/src/pages/SupplierOfferingsPage.tsx` 的 `missionSteps` 三处 `tag` 文案完成 GREEN，并重新通过 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx`（6 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。
- 真实 API 方面复用并重跑本地回放脚本 `.hermes/local-output/run_supplier_offerings_route_stub_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-offerings-route-stub-real-api/20260507-071915` 已验证：`GET /healthz` 返回 `status=ok` 且 `redis=ok`；普通用户 `POST /api/v1/auth/register` 与 `GET /api/v1/auth/menu` 正常，菜单继续暴露 `/api-keys`、`/webhooks`、`/docs` 且未泄露 supplier/admin 路径；供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/resources/offerings`、`GET /api/v1/supplier/settlements` 正常；管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 正常；前端 `/`、`/supplier/offerings`、`/api-keys`、`/webhooks`、`/docs` SPA 壳均返回 200，证明本轮仅前端中文标签收口，未破坏真实注册、角色菜单、供应商供货页与共享控制台单壳入口。
- 本轮五维评审结论：产品/规格 PASS（diff 仅把 SupplierOfferings mission-step 三个运行时标签从英文收敛为中文合同，并同步补强 focused proof，不改变单一登录后控制台、角色差异菜单与后端契约）；代码质量 APPROVED（运行时代码仅 3 处字面标签替换，测试继续锚定真实 mission-flow 区域与行为入口，没有退化为纯 testid 存在性检查；一次污染性子代理评审漂移到错误仓库后已丢弃，最终以控制器 live diff/测试结果为准）；安全/集成 SAFE（静态扫描当前 live diff 新增行未发现 hardcoded secret / shell / eval / SQL 注入问题，且未改 auth/menu/route/API 语义，真实 user/supplier/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；Semi UI `findDOMNode`、chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅静态文案替换，不增加查询、状态分支或部署复杂度，web/api 容器重建后健康）。

### 最新执行检查点（2026-05-07，SupplierDomains metrics / mission / capability scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 Phase 4/5 接口契约的前提下，仅把 `web/src/pages/SupplierDomainsPage.test.tsx` 中仍残留的 page-global 指标/能力矩阵文本命中，收紧为真实运行时卡片/区块语义锚点 scoped 合同；具体把 4 张顶部指标卡补齐最小 `supplier-domains-metric-*` 锚点，把“供应商主任务流”标题收紧到新的 `supplier-domains-mission-section` 容器，把“控制台能力矩阵”收紧到 `supplier-domains-capability-matrix`，并继续保持“同一登录后 shared-console 深色单壳内完成域名运营 → 资源映射 → 供货编排 → API Keys/Webhooks/Docs 联动”的产品叙事，不引入第二套供应商后台。
- 控制器已按 TDD 先修改 `web/src/pages/SupplierDomainsPage.test.tsx` 让 focused 用例在 RED 阶段显式要求 metric card / mission section / capability matrix 的 scoped 合同；首轮测试/可靠性评审指出同步断言异步加载后的指标值会引入竞态，控制器随后把 `expectMetricCard(...)` 修正为 `await within(card).findByRole(...)` 后再做 GREEN，并最小修改 `web/src/pages/SupplierDomainsPage.tsx`：仅为 `MetricCard` 增加可选 `testId`、为任务区块与能力矩阵容器补齐 2 个最小 `data-testid`。随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx`（3 files / 23 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。
- 真实 API 方面新增本地回放脚本 `.hermes/local-output/run_supplier_domains_metric_scope_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-domains-metric-scope/20260507-064954` 已验证：`GET /healthz` 返回 `status=ok` 且 `redis=ok`；普通用户 `POST /api/v1/auth/register` 与 `GET /api/v1/auth/menu` 正常；供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`POST /api/v1/supplier/resources/domains`、`GET /api/v1/supplier/settlements` 正常，且新建域名已在后续 overview 中可见；管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 正常；前端 `/`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs`、`/admin/audit` SPA 壳返回 200，证明本轮 scoped contract 收紧未破坏真实注册、角色菜单、供应商域名运营链路与共享控制台单壳入口。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 SupplierDomains focused proof 从 page-global 文本收紧为真实 metric/section/card 合同，不改变 shared-console 单壳、角色差异菜单或后端契约）；代码质量 APPROVED（运行时代码仅新增 1 个可选 `testId` 透传与 6 个最小语义锚点，测试仍校验真实标题/值/说明与导航行为，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（静态扫描 live diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且真实 supplier menu/overview/domain-create/settlements 与 admin overview/risk/audit 回放继续健康）；测试/可靠性 PASS（已根据评审修复异步指标竞态，focused Vitest、build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode`、chunk 体积告警仍为历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（新增锚点仅为静态属性，不引入新请求、状态分支或部署负担）。下一前端优先项继续清理 shared-console focused 用例中仍残留的 page-global 指标/说明命中与异步断言脆弱点，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，shared-console 英文残留标签中文化对齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录行为、不改菜单权限、不改 Phase 4/5 后端接口契约的前提下，仅收敛 3 处仍残留在运行时壳层里的英文 shared-console 标签，使登录入口、Dashboard 总览与余额页 fallback 文案进一步统一到中文共享控制台术语；具体把 `DashboardPage.tsx` hero tag `Shared Console Entry` 收敛为 `共享控制台入口`，把 `BalancePage.tsx` fallback tag `Shared Console fallback` 收敛为 `共享控制台回退`，并把 `AppSidebar.tsx` 顶部 brand pill 从 `Nexus-Mail · Shared Console` 收敛为 `Nexus-Mail · 统一控制台`，继续保持“注册后进入同一控制台、角色菜单驱动差异能力、无冗余独立后台”的产品叙事不变。
- 控制器已按 TDD 先修改 `DashboardPage.test.tsx`、`BalancePage.test.tsx`、`AppSidebar.test.tsx`，让 focused 用例先显式要求 `共享控制台入口` / `共享控制台回退` / `Nexus-Mail · 统一控制台` 这 3 处新合同，再最小修改对应运行时代码 `DashboardPage.tsx`、`BalancePage.tsx`、`AppSidebar.tsx` 完成 GREEN；随后重新通过 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/pages/BalancePage.test.tsx src/components/AppSidebar.test.tsx`（3 files / 16 tests 全绿）、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/DashboardPage.test.tsx src/pages/BalancePage.test.tsx src/components/AppSidebar.test.tsx`（4 files / 42 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`。真实 API 方面新增本地回放脚本 `.hermes/local-output/run_frontend_shared_console_copy_alignment_real_api.sh`，最新产物目录为 `.hermes/local-output/frontend-shared-console-copy-alignment-real-api/20260507-054951`：其中 `/healthz` 返回 `status=ok` / `redis=ok`，新注册普通用户 `/auth/menu` 暴露 `/`、`/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs`、`/settings`、`/profile`，管理员 `/auth/menu` 暴露 `/admin/users`、`/admin/suppliers`、`/admin/pricing`、`/admin/risk`、`/admin/audit` 等扩展路径，供应商 `/auth/menu` 暴露 `/supplier/domains`、`/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`，并且 `/login`、`/`、`/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs` 的 SPA shell 全部 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 仅收敛 shared-console 壳层英文残留标签到统一中文术语，不改变单一登录后控制台、角色差异菜单或前后端合同）；代码质量 APPROVED（运行时代码仅 3 处字面文案替换，测试同步补上 focused/integration 语义 proof，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（控制器静态扫描当前 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且真实 auth/register/menu/admin overview/supplier menu 回放继续健康，未引入权限或路由漂移）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check`、浏览器 fresh-load `/login` 复核与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅静态文案与测试合同调整，不增加查询、轮询、构建步骤或部署复杂度，同时顺手清理 `.git/info/exclude` 中重复/绝对路径规则，保留 `.hermes/local-output/` 与 `docs/reference-local/` 两条本地忽略真值）。

### 最新执行检查点（2026-05-07，Supplier shared-console menu label / fallback route-stub contract alignment）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改运行时代码、不改注册/菜单权限/后端 Phase 4/5 契约的前提下，仅把 `SupplierOfferingsPage.test.tsx`、`SupplierResourcesPage.test.tsx`、`SupplierSettlementsPage.test.tsx` 中仍残留的旧 shared-console 菜单夹具与低信号 fallback 断言收敛到当前 canonical 合同；具体把 supplier menu fixture 中的 `共享控制台首页` 统一对齐为 `仪表盘`，把 `Webhook` / `Docs` 对齐为运行时真实标题 `Webhook 设置` / `API 文档`，并将 `SupplierOfferingsPage.test.tsx` 的两处 fallback proof 从宽泛 `findByText('控制台总览')` 收紧为 `supplier-offerings-route-stub-shared-home` + heading 双合同，继续保持“供应商资源 / 供货规则 / 结算 / API Keys / Webhooks / Docs 全部仍处于同一登录后共享控制台内”的产品叙事，而不是派生独立供应商后台。
- 控制器已按 TDD 先从 focused 测试合同收紧开始，仅修改 3 个 supplier page test 文件；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx`（3 files / 23 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`。真实 API 方面：`bash .hermes/local-output/run_supplier_offerings_route_stub_real_api.sh` 通过，最新产物目录 `.hermes/local-output/supplier-offerings-route-stub-real-api/20260507-051419`；`bash .hermes/local-output/run_supplier_settlements_metric_scope_real_api.sh` 通过，最新产物目录 `.hermes/local-output/supplier-settlements-metric-scope-real-api/20260507-051424`；`run_supplier_resources_card_scope_real_api.sh` 首次因历史脚本未显式传 `address` 导致 `mailbox_pool_address_key` 唯一键冲突而 fail，控制器按“先定位根因再修复脚本”原则核对 live overview 与仓库约束后，仅修正本地忽略脚本以显式传入唯一 `address`，随后复跑通过，最新产物目录 `.hermes/local-output/supplier-resources-card-scope-real-api/20260507-051911`，确认 supplier `/auth/menu` 暴露 `/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs`，且新建 domain/account/mailbox 均在 live overview 中可见，管理员 `/admin/overview`、`/admin/risk` 继续健康。
- 本轮五维评审结论：产品/规格 PASS（diff 仅把 supplier focused menu fixture 与 fallback route proof 对齐到 shared-console canonical 标题，不改变单壳、角色差异菜单与后端契约）；代码质量 APPROVED（测试改动局部且更贴近 `consoleNavigation.tsx` 真值，`SupplierOfferingsPage` 的 fallback proof 由 page-global 文本收紧为 route-stub + heading 双合同，没有弱化行为覆盖）；安全/集成 SAFE（静态扫描当前 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且未改 auth/menu/route/API 逻辑；真实 supplier/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、前端 build、Go tests、compose config、`git diff --check` 与三组 supplier 真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅测试夹具标题与 fallback 断言收敛，不增加运行时查询或部署复杂度，同时修复了本地忽略的 supplier-resources replay 脚本地址构造问题，降低后续 autonomous real-API 验证误报率）。

### 最新执行检查点（2026-05-07，ConsoleLayout quick-action canonical route contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册、菜单权限、后端 Phase 4/5 契约与 quick-action 计算逻辑的前提下，仅把 `web/src/components/ConsoleLayout.test.tsx` 中仍残留的低信号 quick-action 顺序 / 导航 proof，从 `getAllByRole(...).filter(textContent)` 与点击后 `findAllByText('项目市场')` 这类宽泛命中，收紧为真实 quick-action 按钮级语义锚点 + canonical route-stub 合同；运行时代码仅为 `web/src/layouts/ConsoleLayout.tsx` 的前三个 quick-action 按钮补齐最小 `data-testid`，继续保持 shared-console 单壳、server-menu truth 过滤与角色差异菜单不变，而不是引入独立快捷入口后台或新的导航分叉。
- 控制器已按 TDD 先修改 `web/src/components/ConsoleLayout.test.tsx`，让 focused 用例在 RED 阶段显式要求 `console-layout-quick-action-projects` / `-balance` / `-docs` 与 `console-layout-route-stub-*` 双合同，再最小修改 `web/src/layouts/ConsoleLayout.tsx` 仅为真实可点击 quick-action 按钮补齐 button-level `data-testid`。随后重新通过 `pnpm --dir web exec vitest run src/components/ConsoleLayout.test.tsx`、`pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx src/utils/consoleNavigation.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`；真实 API 方面本轮新增产物目录 `.hermes/local-output/console-layout-quick-actions-real-api/20260507-044201`，其中 `summary.json` 已确认新注册普通用户 `/auth/menu` 暴露 `/`、`/projects`、`/orders`、`/balance`、`/api-keys`、`/webhooks`、`/settings`、`/docs`，管理员菜单继续包含 `/admin/risk`、`/admin/audit` 等角色扩展路径，且 `/`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk` 的 SPA shell 全部 200，`GET /healthz` 返回 `status=ok` / `redis=ok`。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 ConsoleLayout quick-action proof 从 page-global 文本/集合过滤收紧为 button-level quick-action + canonical route-stub 合同，不改变 shared-console 单壳、server-menu truth 或角色边界叙事）；代码质量 APPROVED（运行时代码仅新增 1 处 button-level `data-testid` 生成，测试继续保留真实点击导航与顺序断言，没有退化为纯 testid 存在性检查，也避免继续依赖 `textContent` 过滤）；安全/集成 SAFE（控制器静态扫描当前 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且 quick-action 仍复用现有 `visibleQuickActionPaths(menu, currentPath, role)` 逻辑，真实 user/admin `/auth/menu` 与关键页面 shell 回放继续健康）；测试/可靠性 PASS（focused Vitest、AppSidebar+ConsoleLayout 组合 Vitest、App 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode`、chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅补最小按钮锚点与测试合同收敛，不增加运行时查询、轮询或部署复杂度，`web` 重建后健康）。

### 最新执行检查点（2026-05-07，ApiKeys create/list/whitelist scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 API Key 契约与 Phase 4/5 运行逻辑的前提下，仅把 `web/src/pages/ApiKeysPage.test.tsx` 中仍残留的低信号 page-global 列表加载、创建、白名单编辑与 fallback 入口 proof，收紧为真实运行时卡片作用域合同；具体把 `默认密钥` / `已撤销密钥` 的初始就绪 proof 统一收敛到 `api-keys-current-keys-card`，把 `创建新密钥` 的点击目标收敛到 `api-keys-create-card`，把白名单编辑输入与保存动作收敛到 `api-keys-whitelist-editor-card`，并保持 bridge / fallback 仍然位于同一个共享控制台壳内，不引入独立接入后台语义。
- 控制器已按 TDD 先在 `web/src/pages/ApiKeysPage.test.tsx` 写出更严格的 scoped 合同，再最小修改 `web/src/pages/ApiKeysPage.tsx`：仅为创建卡、当前密钥卡、白名单编辑卡补齐 3 个最小 `data-testid`，随后通过 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 观察 RED→GREEN 收敛，并继续通过 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。真实 API 方面新增本地回放脚本 `.hermes/local-output/run_api_keys_card_scope_real_api.sh`（最新产物目录 `.hermes/local-output/api-keys-card-scope-real-api/20260507-041808`），确认 `GET /healthz` 返回 `status=ok` / `redis=ok`，新注册普通用户 `/auth/menu` 暴露 `/api-keys`、`/webhooks`、`/docs`，真实 `POST /api/v1/auth/api-keys` 成功返回 201 与 `plaintext_key`，`PATCH /api/v1/auth/api-keys/:id/whitelist` 返回 200 且白名单更新为 `172.18.0.1`，`POST /api/v1/auth/api-keys/:id/revoke` 返回 200 且状态为 `revoked`，`GET /api/v1/auth/api-keys/audit` 可见 `create` / `update_whitelist` / `revoke`，同时管理员 `/admin/overview`、`/admin/risk`、`/admin/audit` 与供应商 `/supplier/resources/overview`、`/supplier/settlements` 均继续返回 200，前端 `/api-keys`、`/webhooks`、`/docs`、`/profile`、`/settings` SPA shell 全部 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 API Keys 页的 create/list/whitelist/fallback focused proof 从 page-global 命中收紧为真实卡片语义区域合同，shared-console 单壳、角色扩展菜单与“同一登录后控制台内完成接入链路”的产品叙事保持不变）；代码质量 APPROVED（运行时代码仅新增 3 个最小卡片锚点，测试继续保留真实行为断言，包括 whitelist 更新后的行内数据变化、bridge/fallback 跳转与创建后联动，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（控制器静态扫描当前 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且真实 API Key 创建、白名单更新、撤销与审计链路均按 live contract 成功回放）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅补卡片级语义锚点与 scoped proof，不新增运行时查询、轮询或部署复杂度，`web`/`api` 重建后健康）。

### 最新执行检查点（2026-05-07，SupplierResources metric/table scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册、菜单权限、供应商资源接口契约与 Phase 4/5 后端逻辑的前提下，仅把 `web/src/pages/SupplierResourcesPage.test.tsx` 中仍残留的 page-global 指标/表格文本命中，收紧为运行时真实卡片语义锚点 scoped 合同；具体把 `健康账号`、`可用邮箱池` 两张顶部指标卡从整页 `screen.getByText(...)` 收敛为 `supplier-resources-metric-healthy-accounts` / `supplier-resources-metric-available-mailboxes` 作用域断言，并把供应商资源概览数据 proof 从 page-global 命中收敛为 `supplier-resources-domains-table-card`、`supplier-resources-accounts-table-card`、`supplier-resources-mailboxes-table-card` 三个真实表格卡片内断言，继续保持“供应商资源、API Keys、Webhook、Docs 仍属于同一登录后共享控制台，而不是拆成独立供应商后台”的产品叙事。
- 控制器已按 TDD 先在 `web/src/pages/SupplierResourcesPage.test.tsx` 写出更严格的 scoped 合同，再通过 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx` 观察 RED 阶段因缺少 `supplier-resources-metric-healthy-accounts`、`supplier-resources-domains-table-card` 等运行时锚点而失败；随后仅最小修改 `web/src/pages/SupplierResourcesPage.tsx`：为 `MetricCard` 增加可选 `testId` 透传、给 4 张顶部指标卡补齐最小语义锚点，并为域名池 / 第三方邮箱账号池 / 邮箱池表格卡片补齐最小 `data-testid`。GREEN 后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。真实 API 方面新增本地回放脚本 `.hermes/local-output/run_supplier_resources_card_scope_real_api.sh`（最新产物目录 `.hermes/local-output/supplier-resources-card-scope-real-api/20260507-034544`），确认 `GET /healthz` 返回 `status=ok` / `redis=ok`，供应商 `/auth/menu` 暴露 `/supplier/resources`、`/api-keys`、`/webhooks`、`/docs`，`GET /api/v1/supplier/resources/overview` 返回 200，且真实新增 domain/account/mailbox 后再次读取 overview 可见 `created_domain_visible=true`、`created_account_visible=true`、`created_mailbox_visible=true`；管理员 `/api/v1/admin/overview`、`/api/v1/admin/risk` 也继续返回 200，前端 `/supplier/resources` SPA shell 返回 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 SupplierResources focused proof 从 page-global 文本收紧为真实 metric/table card 作用域合同，不改变 shared-console 单壳、角色扩展菜单或供应商资源页定位）；代码质量 APPROVED（运行时代码仅增加最小 `testId` 透传与 7 个卡片级锚点，测试继续保留真实文案与数据断言，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（控制器静态扫描新增 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且未改 auth/menu/route/API 行为；真实 supplier/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、前端 build、Go tests、compose config、容器重建、真实 API 回放全部通过；既有 React `findDOMNode` / act 噪声仍属历史非阻塞问题，本轮未恶化）；性能/运维 PASS（仅补语义锚点与 scoped proof，不引入新的运行时查询、轮询或部署复杂度；`web` 重建后健康，前端 chunk 体积告警维持既有状态）。本轮曾尝试 delegate 专家评审，但子代理错误漂移到无关仓库，已按 fail-closed 规则全部丢弃，最终五维结论仅以控制器在 `/root/nexus-mail` 的 live diff、测试与真实 API 结果为准。

### 最新执行检查点（2026-05-07，shared-console shell wording + canonical route-stub sweep）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 Phase 4/5 接口契约的前提下，仅推进一组小而完整的 shared-console 质量收口——把 `AppSidebar.tsx` 顶部副标题从英文 `Single login, multi-role workspace` 收敛为中文 `单一登录 · 按角色切换工作区`，同时把 `web/src/components/AppSidebar.test.tsx`、`web/src/pages/LoginPage.test.tsx`、`web/src/pages/DashboardPage.test.tsx` 中仍残留的低信号 route stub 占位文案（如 `项目市场页面`、`API Keys 页面`、`Webhook 设置页面`、`API 文档页面`、`设置中心页面`）统一升级为显式 `<section data-testid="..."> + <h1>` 的 canonical heading 合同，继续保持“注册后进入同一套共享控制台、角色只影响菜单与页面，不拆独立后台”的产品叙事。
- 控制器已按 RED→GREEN 先修改 3 个 focused 测试文件与 `AppSidebar` 文案，再重新通过 `pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/pages/LoginPage.test.tsx src/pages/DashboardPage.test.tsx`（3 files / 14 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`；同时执行真实 API 回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`（最新产物目录 `.hermes/local-output/app-login-register-scope/20260507-031153`）与 `.hermes/local-output/run_sidebar_layout_contract_real_api.sh`，确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，新注册普通用户 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均为 200，管理员 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 均为 200，且 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` 的 SPA shell 全部 200；浏览器 fresh-load + vision 复核也确认登录页继续维持深色 shared-console/new-api 风格，无新增重叠、裁切或白底突兀块。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只收敛 shared-console 顶部单一登录文案与 route-stub canonical heading 合同，不引入独立后台语义，登录→注册→API Keys/Webhooks/Docs 的统一壳叙事保持一致）；代码质量 APPROVED（运行时代码仅 1 处中文副标题更新，测试改为更稳定的 route-stub `<section> + <h1>` 合同，没有退化为纯 testid 存在性断言，也减少了 page-global 占位文本耦合）；安全/集成 SAFE（未改 auth/menu/route/API 逻辑与密钥处理，静态扫描新增 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，真实 user/admin API 与 SPA 壳回放保持健康）；测试/可靠性 PASS（focused Vitest、前端 build、Go tests、compose config、容器重建、真实 API 回放、浏览器 fresh-load/vision 复核全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅文案与测试合同收敛，不新增运行时请求、轮询、状态分支或部署复杂度，`web`/`api` 容器重建后均健康）。

### 最新执行检查点（2026-05-07，AdminPricing scoped route + card contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单权限/后端 Phase 4/5 接口契约的前提下，仅把 `web/src/pages/AdminProjectsPage.test.tsx` 中管理员价格策略页残留的低信号全页文本与旧占位落点 proof 收紧为 canonical route-stub + scoped card 合同；其中把风控/审计/API Keys 三条 mission-flow 落点从 `风控中心页面`、`审计日志页面`、`API Keys 页面` 升级为 `admin-pricing-route-stub-risk` / `admin-pricing-route-stub-audit` / `admin-pricing-route-stub-api-keys` + `<h1>` 标题双合同，并把“控制台能力矩阵”“项目列表”“编辑项目”三块 proof 从 page-global 命中改为 `admin-pricing-capability-matrix`、`admin-pricing-project-list-card`、`admin-pricing-project-editor-card` 语义区域内断言，继续保持管理员价格策略页只是统一共享控制台中的运营中枢，而不是独立后台或额外壳层。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/AdminProjectsPage.test.tsx` 令 focused 用例显式要求新的 route-stub / scoped-card 合同，再最小修改 `web/src/pages/AdminProjectsPage.tsx` 仅为真实运行时卡片补齐 3 个最小 `data-testid`；随后重新通过 `pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx`、`pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`docker compose up -d --build web`、`git diff --check`，并执行真实 API 回放脚本 `.hermes/local-output/admin_pricing_real_api.sh`，确认管理员真实菜单包含 `/admin/pricing`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`，`GET /api/v1/admin/projects` / `GET /api/v1/admin/projects/offerings` / `GET /api/v1/admin/risk` / `GET /api/v1/admin/audit?limit=5` / `GET /api/v1/auth/api-keys` / `GET /api/v1/webhooks/endpoints` / `GET /docs` 均成功，且一次 `PATCH /api/v1/admin/projects/:id` 真改真回滚通过；浏览器 fresh-load 复核本轮重建后的登录页结构正常，本 diff 未引入新的可视布局破坏。
- 本轮五维评审结论：产品/规格 PASS（仅把 AdminPricing 页的导航落点与卡片 proof 从低信号整页文本收紧为 canonical heading + scoped card 合同，shared-console 单壳、角色分区与 new-api 风格叙事保持一致，不引入独立后台语义）；代码质量 APPROVED（运行时代码只新增 3 个最小 `data-testid`，测试改为 `within(...)` 作用域断言与 route-stub heading 双合同，避免重复文本误报且未退化为纯 testid 存在性检查）；安全/集成 SAFE（未改鉴权、菜单、路由常量、真实接口或密钥处理，管理员风控/审计/API Keys 目标路径与 `App.tsx`、真实 `/auth/menu`、真实 API 回放保持一致）；测试/可靠性 PASS（focused Vitest、App 组合 Vitest、前端 build、Go tests、compose config、容器重建、`git diff --check` 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode`、`FormApi.setValues()` 与 chunk 体积告警仍属历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅补充语义锚点与测试合同收敛，不引入新请求、轮询、状态分支或部署复杂度；浏览器视觉复核也未发现本轮 diff 造成的新布局回归）。

### 最新执行检查点（2026-05-06，shared-console hero tag 中文化收口）
- 本轮继续按“前端优先”清理 shared-console / new-api 单壳里剩余的低信号英文 `Mission Control` hero 标签：在不改注册/菜单/权限/真实 API 契约、继续保持单一登录后控制台的前提下，仅把管理员风控/审计/用户运营/价格策略/供应商运营，以及供应商供货/资金争议/域名运营/资源运营、个人资料、设置页等 hero tag 收敛为对应中文中枢标签（如 `风控中枢`、`审计中枢`、`用户运营中枢`、`价格策略中枢`、`供应商运营中枢`、`供货规则中枢`、`资金争议中枢`、`域名运营中枢`、`资源运营中枢`、`个人资料中枢`、`设置中枢`），同步为缺少稳定锚点的页面补齐最小 `*-hero-card` testid，并把 focused proof 从脆弱的 `.closest('.semi-card')` 收敛为 hero-card 作用域内的中文标签合同。
- 控制器已按 RED→GREEN 先更新对应 page tests，再最小修改运行时代码；随后重新通过 `pnpm --dir web exec vitest run src/pages/AdminRiskPage.test.tsx src/pages/AdminAuditPage.test.tsx src/pages/AdminUsersPage.test.tsx src/pages/AdminProjectsPage.test.tsx src/pages/AdminSuppliersPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/ProfilePage.test.tsx src/pages/SettingsPage.test.tsx`（11 files / 66 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；同时执行真实 API / SPA 回放脚本 `.hermes/local-output/mission-tag-real-api/run.sh`（最新产物目录 `.hermes/local-output/mission-tag-real-api/20260506-234737`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户注册后 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/wallet/overview`、`/wallet/transactions`、`/projects/inventory`、`/orders/activations`、`/auth/api-keys`、`/webhooks/endpoints` 全部 200；管理员 `/auth/me`、`/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`、`/admin/wallet-users` 全部 200；供应商 `/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements` 全部 200；且 `/`、`/login`、`/profile`、`/settings`、`/projects`、`/orders`、`/balance`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit`、`/admin/pricing`、`/admin/suppliers`、`/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/supplier/domains` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（中文中枢标签与各页中文 heading / shared-console 单壳叙事一致，不引入独立后台语义）、代码质量 APPROVED（首轮评审指出 `.closest('.semi-card')` 过于脆弱后，控制器已补最小 `*-hero-card` 锚点并改为显式区域断言；最终 diff 可维护性通过）、安全/集成 SAFE（未改动 auth/menu/route/API 契约，仅收敛展示标签与 focused proof，真实 API 回放保持健康）、测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` / Form warning、chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（仅前端静态标签与最小语义锚点调整，不引入新运行时分支或部署负担）。下一前端优先项继续清理其余 shared-console focused 用例里残留的低信号 page-global 文案/断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，SupplierSettlements metrics scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改权限、菜单、后端财务/争议接口合同的前提下，仅把 `SupplierSettlementsPage.test.tsx` 首个 mission-control focused 用例中 4 个顶部资金指标（`待结算余额`、`活跃成本模型`、`开放争议`、`当前列表流水`）从低信号 page-global `screen.getByText(...)` 命中，收紧为 `supplier-settlements-metric-*` 语义锚点 scoped 合同；运行时代码仅为 `SupplierSettlementsPage.tsx` 的 `MetricCard` 增加可选 `testId` 透传，并给 4 张指标卡补齐最小 testid，避免后续供应商资金页或别的 shared-console 卡片复用同名指标时让 focused proof 误报，同时继续保持供应商财务闭环与共享接入桥接属于同一控制台壳，而不是派生新的独立后台。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/SupplierSettlementsPage.test.tsx` 让用例显式要求 `supplier-settlements-metric-pending` / `supplier-settlements-metric-active-profiles` / `supplier-settlements-metric-open-disputes` / `supplier-settlements-metric-entry-total` 四个锚点，再最小修改 `web/src/pages/SupplierSettlementsPage.tsx` 为 `MetricCard` 增加 `testId` 并落到 4 张指标卡；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`、`docker compose up -d --build web gateway`，并执行真实 API 回放脚本 `.hermes/local-output/run_supplier_settlements_metric_scope_real_api.sh`（最新产物目录 `.hermes/local-output/supplier-settlements-metric-scope-real-api/20260507-001312`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，新注册普通用户 `/auth/me`、`/auth/menu`、`/wallet/overview`、`/wallet/transactions` 全部 200，供应商 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements`、`/supplier/reports?limit=5` 全部 200，管理员 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 200，且 `/`、`/balance`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/settlements` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（本次 diff 仅把供应商资金页 4 张指标卡的测试合同从 page-global 文本收紧为卡片级语义锚点，不改变单壳叙事、角色路径或财务页面定位）、代码质量 APPROVED（`MetricCard` 仅增加可选 `testId` 透传，改动局部且与 test contract 一一对应，没有引入新的 DOM 包装耦合）、安全/集成 SAFE（未改鉴权、菜单、接口、密钥、结算或争议逻辑；真实 user/supplier/admin API 与 SPA 壳均保持健康）、测试/可靠性 PASS（focused Vitest、前端 build、Go tests、compose config、git diff --check、容器重建与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（仅补最小 testid 与 focused proof 收敛，不增加运行时查询、分支或部署复杂度）。下一前端优先项继续优先清理 `SupplierSettlementsPage.test.tsx` 其余 report/争议表格中的 page-global 金额/文本命中，再推进其它 shared-console focused 脆弱点，之后再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，SupplierSettlements records scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改权限、菜单、后端财务/争议接口合同的前提下，仅把 `SupplierSettlementsPage.test.tsx` 中“真实结算/报表/争议数据已渲染”这一 focused 用例，从 page-global `screen.getByText(...)` 命中收紧为 `supplier-settlements-entries-card`、`supplier-settlements-reports-card`、`supplier-settlements-cost-profiles-card`、`supplier-settlements-disputes-card` 四个语义区域内的 scoped 合同；运行时代码只为 `SupplierSettlementsPage.tsx` 的成本模型、争议单、结算流水三张卡片补齐最小 `data-testid`，避免后续供应商资金页继续因重复金额/备注/争议文本产生误报，同时保持供应商财务闭环、共享接入桥接与单壳控制台叙事不变，不派生独立后台。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/SupplierSettlementsPage.test.tsx` 让用例显式要求 `supplier-settlements-entries-card` / `supplier-settlements-cost-profiles-card` / `supplier-settlements-disputes-card` 等锚点与作用域内断言，再最小修改 `web/src/pages/SupplierSettlementsPage.tsx` 为三张运行时卡片补齐 testid；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行真实 API 回放脚本 `.hermes/local-output/run_supplier_settlements_metric_scope_real_api.sh`（本轮最新产物目录 `.hermes/local-output/supplier-settlements-metric-scope-real-api/20260507-015114`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，新注册普通用户 `/auth/me`、`/auth/menu`、`/wallet/overview`、`/wallet/transactions` 全部 200，供应商 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements`、`/supplier/reports?limit=5` 全部 200，管理员 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 200，且 `/`、`/balance`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/settlements` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 SupplierSettlements 数据展示 proof 从 page-global 文本收紧为报表/流水/争议/成本四个真实卡片作用域合同，shared-console 单壳、角色路径与财务页面定位不变）、代码质量 APPROVED（新增锚点均落在真实运行时卡片层，避免 `.closest('.semi-card')` 一类结构耦合，也没有把 proof 弱化为纯 testid 存在性）、安全/集成 SAFE（未改 auth/menu/route/API 语义；静态扫描无 hardcoded secret / shell / eval / SQL 注入问题，真实 user/supplier/admin API 回放保持健康）、测试/可靠性 PASS（focused Vitest、supplier pair Vitest、前端 build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（仅补最小语义锚点并收紧测试合同，不增加运行时查询、分支或部署复杂度）。下一前端优先项继续优先清理 `SupplierResourcesPage.test.tsx` / `SupplierSettlementsPage.test.tsx` 其余残留的 page-global copy/金额命中，再推进其它 shared-console focused 脆弱点，之后再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，SupplierSettlements form scoped contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改权限、菜单、后端财务/争议接口合同的前提下，仅把 `SupplierSettlementsPage.test.tsx` 中“提交成本模型 / 提交争议并刷新数据”这一 focused 用例，从 page-global 表单控件与动作按钮命中收紧为 `supplier-settlements-cost-profile-form`、`supplier-settlements-dispute-form` 两个真实表单语义区域内的 scoped 合同；运行时代码仅为 `SupplierSettlementsPage.tsx` 的成本模型表单与争议表单补齐最小 `data-testid`，继续保持供应商财务闭环、共享接入桥接与单壳控制台叙事不变，不派生独立后台。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/SupplierSettlementsPage.test.tsx`，让用例显式要求 scoped form contract，再最小修改 `web/src/pages/SupplierSettlementsPage.tsx` 为两个真实表单补齐语义锚点；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 与 `docker compose up -d --build web`。真实 API 方面重跑本地回放脚本 `.hermes/local-output/run_supplier_settlements_metric_scope_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-settlements-metric-scope-real-api/20260507-062041`：`GET /healthz` 返回 `status=ok` / `redis=ok`，新注册普通用户 `/auth/menu`、`/auth/me`、`/wallet/overview` 返回 200，供应商 `/supplier/settlements`、`/supplier/reports?limit=5` 返回 200，管理员 `/admin/audit?limit=5` 返回 200，且 `/balance`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/settlements` 的 SPA shell 均保持 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 仅把 SupplierSettlements 表单提交 proof 从 page-global 控件命中收紧为真实成本模型/争议表单作用域合同，不改变 shared-console 单壳、角色路径与财务页面定位）；代码质量 APPROVED（运行时代码仅新增两个最小表单级 `data-testid`，测试继续保留真实输入、提交、reload 与 toast 行为断言，没有退化为纯 testid 存在性检查）；安全/集成 SAFE（控制器静态扫描当前 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且未改鉴权、菜单、接口或资金逻辑；真实 user/supplier/admin API 回放继续健康）；测试/可靠性 PASS（focused Vitest、supplier pair Vitest、App 组合 Vitest、前端 build、Go tests、compose config、`git diff --check`、容器重建与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍为历史非阻塞噪声，本轮未恶化）；性能/运维 PASS（仅补最小表单语义锚点并收紧测试合同，不增加运行时查询、分支或部署复杂度）。下一前端优先项继续优先清理其余 shared-console focused 用例里残留的 page-global 表单/按钮命中，再推进下一个前端统一控制台切片，之后再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-07，route stub canonical heading 对齐收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改运行时代码、不改注册/菜单/权限/后端 API 契约的前提下，仅把 `SettingsPage.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 中残留的旧 route stub 占位文案（如 `项目市场页面`、`API Keys 页面`、`Webhook 设置页面`、`API 文档页面`）统一收敛为与共享控制台 canonical heading 一致的 route stub / heading 合同；其中 `SettingsPage.test.tsx` 进一步把首页、项目市场、订单中心、API Keys、API 文档、个人资料、Webhook、供应商资源、供应商结算 9 个测试路由桩全部升级为显式 `settings-route-stub-*` 语义锚点 + `<h1>` 标题，`ApiDocsPage.test.tsx` 与 `WebhooksPage.test.tsx` 则把 API Keys / Webhook / Docs 落点文案对齐为 `开发者 API 接入工作台`、`Webhook 设置`、`API 文档`，避免继续依赖旧占位页文案或 page-global 弱文本匹配，同时继续保持 shared-console/new-api 单壳叙事，不派生独立后台。
- 控制器已按 RED→GREEN 先修改 3 个 focused 测试文件，再重新通过 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx`（3 files / 18 tests 全绿）与 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/SettingsPage.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx`（4 files / 44 tests 全绿）；随后完成 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`。真实 API 方面新增本地复放脚本 `.hermes/local-output/run_front_route_heading_stub_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/front-route-heading-stub-scope/20260507-003541`：`GET /healthz` 返回 200 且 `status=ok`、`redis=ok`；新注册普通用户 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 200；管理员 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 200；供应商 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 全部 200；且前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs` 的 SPA shell 均返回 200。该产物摘要同时确认 3 个角色菜单路径仍与 shared-console 单壳模型一致：普通用户不泄露 supplier/admin 路由，supplier/admin 继续在同一壳内扩展。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 只把 3 个 focused 测试里的旧 route stub 占位文案收敛为 canonical heading 合同，shared-console 单壳、角色扩展菜单与 new-api 风格叙事保持不变）、代码质量 APPROVED（改动局限于测试层，`SettingsPage.test.tsx` 通过最小语义锚点显式化 9 个 route stub，避免再依赖 page-global 占位文案；`ApiDocsPage.test.tsx` / `WebhooksPage.test.tsx` 仅把落点文案对齐真实标题，没有引入新结构耦合）、安全/集成 SAFE（控制器静态扫描新增 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题；真实 user/admin/supplier API 回放与 `/auth/menu` 路径摘要均证明权限、菜单、接口合同未漂移）、测试/可靠性 PASS（focused Vitest、App 组合 Vitest、build、Go tests、compose config、git diff --check 全部通过，并新增本地 front-route-heading-stub-scope 真实 API 回放工件确认 `/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/supplier/domains` 壳路由均保持 200 与既有菜单真值）；性能/运维 PASS（仅前端测试合同收敛，不增加运行时查询或部署复杂度；历史 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警维持非阻塞状态）。
### 最新执行检查点（2026-05-07，Login shared-console wording alignment）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/登录接口、菜单权限、角色路由与后端 Phase 4/5 能力的前提下，仅把登录页登录/注册主标题、左侧 brand tag、hero signals 与注册后接入 runway 的残留英文/中英混杂文案收敛为统一中文 shared-console 术语（如 `登录并进入统一控制台`、`创建账号并进入统一控制台`、`统一登录入口`、`权限菜单真值`、`真实 API 联调`、`注册后配置 API Keys`、`Webhook 回调联调`、`文档回放校验`），继续保持“注册成功后进入同一套共享控制台，而不是独立新手页或多后台分叉”的产品叙事，并让 Login focused proof / App 联动断言同步对齐新合同。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/LoginPage.test.tsx` 与 `web/src/App.test.tsx`，让 focused / integrated 用例先显式要求新的中文 heading、hero tag 与 runway copy，再最小更新 `web/src/pages/LoginPage.tsx` 的运行时文案；随后重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行真实 API 回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`（最新产物目录 `.hermes/local-output/app-login-register-scope/20260507-012911`），确认 `GET /healthz` 返回 `status=ok` / `redis=ok`，新注册普通用户可继续成功访问 `/auth/menu`、`/auth/me`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，管理员 `/admin/overview`、`/admin/risk`、`/admin/audit` 也保持 200，且 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（登录/注册入口与注册后接入跑道统一收敛到 shared-console 中文术语，单壳叙事更一致，没有引入独立后台语义）、代码质量 APPROVED（diff 仅涉及 3 个前端文件的文案与合同对齐，测试仍保留行为级 register-mode 切换与注册提交流程，没有弱化为纯静态文本存在性断言）、安全/集成 SAFE（静态扫描无 hardcoded secret / shell / eval / SQL 注入问题，且 auth/menu/route/API 契约未漂移，真实 user/admin API 回放继续健康）、测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、git diff --check、容器重建与真实 API 回放全部通过；浏览器 fresh-load 快照已确认中文文案实际生效）、性能/运维 PASS（仅前端静态文案与测试合同调整，不增加新查询、分支或部署复杂度；浏览器视觉检查提示 auth card 低对比度仍是既有非阻塞债务，本轮未恶化）。


### 最新执行检查点（2026-05-07，Projects / Orders shared-console contract tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单/权限/后端 Phase 4/5 业务逻辑的前提下，仅把 `web/src/pages/ProjectsPage.test.tsx`、`web/src/pages/OrdersPage.test.tsx` 中残留的低信号 route stub 占位文案（如 `订单中心页面`、`项目市场页面`、`API Keys 页面`、`控制台总览页面`）统一收敛为 route-stub `data-testid` + canonical heading 双合同，并把 `OrdersPage` 的“履约说明”区域补齐最小父级语义锚点 `orders-fulfillment-guidance-card`，让 focused proof 改为在该作用域内断言 `READY 后完成订单`、`异常时看结果面板`、`订单为空时的下一步` 三个真实用户可见 heading，同时继续保持“采购 → 履约 → API 接入”全部属于同一登录后共享控制台，而不是拆成独立订单后台或接入后台。
- 控制器已按 RED→GREEN 先收紧 `ProjectsPage.test.tsx` / `OrdersPage.test.tsx` 的目标合同，再最小修改 `web/src/pages/OrdersPage.tsx` 仅补父级 guidance anchor；期间首轮代码质量评审指出不应新增 3 个子卡片 testid 并把测试弱化为纯 testid 存在性断言，控制器已同轮回退这些子级锚点，改为保留单个父级 `orders-fulfillment-guidance-card` 并在其内部断言 3 个真实 heading。随后重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`（2 files / 21 tests 全绿）、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`。真实 API 方面新增本地复放脚本 `.hermes/local-output/run_projects_orders_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/projects-orders-scope/20260507-021702`：`GET /healthz` 返回 200 且 `status=ok`、`redis=ok`；新注册普通用户 `/auth/menu` 返回 `/projects`、`/orders`、`/api-keys` 等 shared 路径；`/projects/inventory` 返回 200 且非空；`/orders/activations` 返回 200 并观测到当前真实合同为 `{"items": null}`；`/auth/api-keys` 返回 200；前端 `/`、`/projects`、`/orders` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 仅把 Projects / Orders focused 导航落点 proof 从旧占位文案收敛为 canonical heading 合同，并把订单说明 proof 收紧到父级 guidance card 作用域，没有引入独立后台语义，shared-console 单壳叙事保持一致）、代码质量 APPROVED（最终 runtime 仅保留一个最小父级语义锚点；测试继续验证真实 heading 与导航结果，没有退化为纯 testid 存在性断言）、安全/集成 SAFE（未改鉴权、菜单、角色边界或接口路径；真实 API 回放也如实记录 `/orders/activations` 当前 `items` 可能为 `null` 的观测合同，未夸大为前端尚未验证的数组语义）、测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（仅测试合同收敛与单个父级语义锚点补充，不增加运行时查询、状态或部署复杂度；本轮同时把真实 API 观测到的 `orders.items=null` 明确沉淀为后续后端/前端契约收口线索）。

### 最新执行检查点（2026-05-07，Profile route-stub canonical heading tightening）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：在不改注册/菜单/权限/后端 API 契约、也不引入新的运行时代码分叉的前提下，仅把 `web/src/pages/ProfilePage.test.tsx` 中残留的低信号 route stub 占位文案从纯 `<div>` 文本收敛为带 `data-testid` + `<h1>` 的 canonical route-stub 合同；其中项目市场、订单中心、开发者 API 接入工作台、Webhook 设置、API 文档、设置中心、域名池运营中枢、风控中心与控制台总览 9 个落点全部升级为语义化 heading，继续保持“个人资料页只是同一共享控制台里的身份中枢与角色桥接，而非独立后台入口”的产品叙事。
- 控制器已按测试先行执行最小垂直切片：先修改 `web/src/pages/ProfilePage.test.tsx` 的路由桩结构，再复跑 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx`（7 tests 全绿）；随后完成 `pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；真实 API 方面复用并重跑 `.hermes/local-output/run_profile_route_stub_scope_real_api.sh`，最新产物目录为 `.hermes/local-output/profile-route-stub-scope/20260507-005831`，其中 `summary.json` 已确认 `GET /healthz` 返回 `status=ok` / `redis=ok`，新注册普通用户菜单包含 `/`、`/projects`、`/orders`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`，同时 `/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/supplier/domains` 前端壳路由全部返回 200。
- 本轮五维评审结论：产品/规格 PASS（controller-side 复核发现本仓现有多处 focused route stub 本就使用 `开发者 API 接入工作台`、`Webhook 设置`、`API 文档`、`域名池运营中枢`、`控制台总览` 这组 heading 合同，当前 diff 与既有单壳测试真值一致，并未新引入 heading 体系漂移）、代码质量 APPROVED（仅单文件测试 harness 从纯文本 `<div>` 收敛为 `<section>` + `<h1>`，可访问结构和 route-stub 语义更清晰；后续若需要可再统一抽 helper，但当前不构成阻塞）、安全/集成 SAFE（静态扫描新增 diff 未发现 hardcoded secret / shell / eval / SQL 注入问题，且本轮为测试层变更；真实 API 回放继续证明 auth/menu/route 合同未漂移）、测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 告警仍为历史非阻塞噪声）、性能/运维 PASS（仅测试桩结构升级，不增加任何运行时分支、查询或部署成本）。

### 最新执行检查点（2026-05-06，Balance heading alignment / docs bridge wording sync）
- 本轮继续按“前端优先”收敛 shared-console/new-api 单壳中的低信号英文 hero 文案：在不改注册/菜单/权限/真实 API 契约、继续保持单一登录后控制台的前提下，仅把 `BalancePage.tsx` hero 标签从英文 `Finance Mission Control` 收敛为中文 `余额任务总览`，同步把 `BalancePage.test.tsx` 的 hero focused 合同与用例命名改为与当前页面术语一致，并把 `ApiDocsPage.tsx` 中返回余额页的 bridge 描述从旧的 `Finance Mission Control` 指代同步为 `余额任务总览`，再由 `ApiDocsPage.test.tsx` 补齐菜单前置条件与更新后的 bridge 文案断言，避免余额页与文档桥接对同一工作台继续使用中英混杂旧称谓。
- 控制器已按最小垂直切片完成验证：先修正 Balance hero / docs bridge 文案，再重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/pages/BalancePage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/BalancePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；同时执行真实 API 回放脚本 `.hermes/local-output/balance-heading-alignment-real-api/run.sh`（最新产物目录 `.hermes/local-output/balance-heading-alignment-real-api/20260506-231331`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 后 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/wallet/overview`、`/wallet/transactions` 全部 200，管理员 `POST /api/v1/auth/login` 后 `/auth/me`、`/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 200，供应商 `POST /api/v1/auth/login` 后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 全部 200，且 `/`、`/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（仅把余额页 hero 与 docs→balance bridge 对同一工作台的命名收敛到中文 `余额任务总览`，不改变 shared-console/new-api 单壳叙事）、代码质量 APPROVED（补齐了 `ApiDocsPage` 对应 bridge 文案断言并同步测试命名，无新的命名漂移或跨页称谓分叉）、安全/集成 SAFE（未改动任何权限判断、菜单 gating、路由常量或前后端契约，真实 user/admin/supplier API 与 SPA 路由均保持健康）、测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（纯静态文案与测试合同对齐，不引入新的运行时分支、构建配置或部署负担）。下一前端优先项继续清理其余 shared-console focused 用例里残留的低信号英文 mission-control 标签 / page-global 文案断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Balance scoped hero / capability contract tightening）
- 本轮继续按“前端优先”收敛 `BalancePage.test.tsx` 中仍残留的低信号 page-global 页面文案断言：在不改注册/菜单/权限/真实 API 契约、继续保持 new-api 风格单一登录后控制台的前提下，仅把余额页首个 shared-console focused 用例从 page-global `资金任务流` / `控制台能力矩阵` / `角色差异仍共用单壳` / `资金观察与售后同层` / `本次会话新提交的争议` 等散落文本命中，收紧为 `balance-hero-card`、`balance-mission-cards`、`balance-capability-matrix` 与新增 `balance-session-disputes-card` 语义区域内的 scoped 合同，同时为运行时代码最小补充 `balance-hero-card` 与 `balance-session-disputes-card` testid，避免后续 shared-console 文案润色或同页重复文案导致 focused 误报。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/BalancePage.test.tsx`，让首个 focused 用例转向 hero / mission / capability / disputes 四个语义区域，再以最小运行时代码为 `web/src/pages/BalancePage.tsx` 补齐 `balance-hero-card` 与 `balance-session-disputes-card`；随后重新通过 `pnpm --dir web exec vitest run src/pages/BalancePage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/BalancePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`，并执行真实 API 回放脚本 `.hermes/local-output/balance-scoped-contract-real-api/run.sh`（最新产物目录 `.hermes/local-output/balance-scoped-contract-real-api/20260506-224148`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，新注册普通用户 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/wallet/overview`、`/wallet/transactions` 全部 200，管理员 `POST /api/v1/auth/login` 后 `/auth/me`、`/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 200，供应商 `POST /api/v1/auth/login` 后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/settlements` 全部 200，且 `/`、`/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（仅把 Balance 首屏 proof 从 page-global 文本收敛为更贴近 shared-console 运行时结构的 hero / mission / capability / disputes 区域合同，不改变单壳资金页叙事）、代码质量 APPROVED（改动局限于单个 focused 文件与两个最小 testid，保留用户层 heading 与 Banner 文本合同，没有退化为纯 testid 存在性断言）、安全/集成 SAFE（无新权限、菜单、鉴权、路由或接口改动，真实 user/admin/supplier 菜单与资金/运营接口保持健康）、测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 与 chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（仅前端语义锚点与测试合同收敛，不引入新的运行时路径或部署负担）。下一前端优先项继续清理其余 shared-console focused 用例中仍残留的低信号 page-global 文案 / 指标断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，AdminUsers/AdminRisk route-stub + canonical heading contract tightening）
- 本轮继续按“前端优先”收敛管理员共享控制台 focused 用例中仍残留的低信号 page-global 路由落点断言：在不改运行时代码、不改注册/菜单/权限/真实 API 契约、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `AdminUsersPage.shared-console.test.tsx` 与 `AdminRiskPage.test.tsx` 中管理员主任务流 / fallback 导航 proof 从 `风控中心页面`、`审计日志页面`、`资金工作台页面`、`API Keys 页面`、`控制台总览` 这类历史占位或 page-global 文本，统一收紧为 `admin-users-route-stub-*` / `admin-risk-route-stub-*` 语义锚点 + canonical heading 双合同，并把 `ADMIN_USERS_ROUTE` 落点明确对齐真实共享控制台标题 `用户管理`，避免继续把旧占位文案固化进 focused proof。
- 控制器已按最小垂直切片完成验证：先复核 live diff 与路由标题真值，再通过 `pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx src/pages/AdminRiskPage.test.tsx`、`pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx src/pages/AdminRiskPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；同时执行真实 API 回放脚本 `.hermes/local-output/run_admin_users_admin_risk_route_proof_real_api.sh`（最新产物目录 `.hermes/local-output/admin-users-admin-risk-route-proof/20260506-222107`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，新注册普通用户 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 全部 200，管理员 `POST /api/v1/auth/login` 后 `/auth/me`、`/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 全部 200，且 `/admin/users`、`/admin/risk`、`/api-keys`、`/` 的 SPA shell 均返回 200。
- 本轮五维评审结论：产品/规格 PASS（仅把管理员资金/风控两页的导航落点 proof 从旧 page-global 文本收敛为更贴近 shared-console 运行时结构的 route-stub + canonical heading 合同，并把“资金工作台”落点准确对齐实际页面标题 `用户管理`）、代码质量 APPROVED（改动局限于两份 focused 测试文件，保留点击行为断言并新增 route-stub + heading 双合同，没有退化为文本弱断言）、安全/集成 SAFE（无新权限、菜单、鉴权或接口改动，canonical heading 与 `consoleNavigation` 真值及真实 API 菜单保持一致）、测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、git diff --check 与真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` / Form warning、chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（纯测试合同收敛，不引入运行时代码路径或部署开销变化）。下一前端优先项继续清理其余 shared-console focused 用例里残留的低信号 page-global 落点 / 标题断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，SupplierOfferings route-stub + canonical heading alignment）
- 本轮继续按“前端优先”收敛 shared-console focused 用例里残留的低信号 page-global 路由落点断言：在不改运行时代码、不改注册/菜单/权限/真实 API 契约、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `SupplierOfferingsPage.test.tsx` 中供应商主任务流三条导航 proof 从 `供应商资源页` / `供应商结算页` / `API Keys 页面` 这类历史 route stub 文本，统一收紧为 `supplier-offerings-route-stub-*` testid + canonical heading 双合同，并把测试内各个路由桩标题对齐真实共享控制台页面标题（如 `供应商资源`、`供应商结算`、`开发者 API 接入工作台`），避免继续把低信号占位文案固化进 focused proof。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/SupplierOfferingsPage.test.tsx`：新增 route-stub 语义锚点，并把 mission-flow 导航用例改为 `findByTestId(...) + heading` 双合同；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx`、`pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`，并执行真实 API 回放脚本 `.hermes/local-output/run_supplier_offerings_route_stub_real_api.sh`（最新产物目录 `.hermes/local-output/supplier-offerings-route-stub-real-api/20260506-220206`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`；普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/menu` 保持只暴露 shared shell 路径；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements` 返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `http://127.0.0.1:5173/`、`/supplier/offerings`、`/api-keys`、`/webhooks`、`/docs` 均返回 SPA 壳 200。
- 本轮五维评审以 controller-side live diff 与实测为准：产品/规格 PASS（仅把 SupplierOfferings 导航落点 proof 从旧 route 文本收敛为更贴近 shared-console 运行时结构的 route-stub + canonical heading 合同）、代码质量 APPROVED（改动局限于单个 focused 文件，路由桩命名与实际目标页标题对齐）、安全/集成 SAFE（未引入新权限、菜单、接口或凭据处理变化，供应商/管理员真实菜单与接口健康）、测试/可靠性 PASS（focused Vitest、grouped App+SupplierOfferings Vitest、build、Go tests、compose config、真实 API 回放全部通过；既有 Semi UI `findDOMNode`、React `act(...)` 与 chunk 体积告警仍为历史非阻塞噪声）、性能/运维 PASS（仅测试合同与路由桩语义收敛，不引入运行时开销）。下一前端优先项继续清理其余 focused 用例中仍残留的旧 route stub / page-global 落点断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，ApiDocs semantic heading contract tightening）
- 本轮继续按“前端优先”收敛 shared-console focused 用例中仍残留的装饰性英文 docs 标题依赖：在不改注册/菜单/权限/真实 API 契约、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `ApiDocsPage.tsx` 三种角色视图的装饰性 `Docs Mission Control` 标题统一收敛为语义化中文页面标题 `API 文档与接入控制台`，并同步把 `ApiDocsPage.test.tsx` 与 `App.test.tsx` 中 docs fallback / bridge / loop proof 从低信号装饰性标题依赖收紧为语义 heading + bridge/loop 容器 + iframe surface 合同，确保单壳 docs 页面在用户/供应商/管理员视角下都以共享控制台语义呈现，而不是继续依赖易漂移的英文视觉标签。
- 控制器已按 RED→GREEN 先修改 `web/src/pages/ApiDocsPage.test.tsx` 与 `web/src/App.test.tsx`，通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/App.test.tsx` 观察到 RED 阶段明确报错 `Unable to find an accessible element with the role "heading" and name "API 文档与接入控制台"`，随后仅最小修改 `web/src/pages/ApiDocsPage.tsx` 的角色标题文案，再重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；并执行真实 API 回放脚本 `.hermes/local-output/api-docs-semantic-heading-real-api/run.sh`（最新产物目录 `.hermes/local-output/api-docs-semantic-heading-real-api/20260506-214129`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me`、`/auth/menu`、`/auth/api-keys`、`/webhooks/endpoints` 返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；前端 `http://127.0.0.1:5173/`、`/docs`、`/api-keys`、`/webhooks` 均返回 SPA 壳 200。
- 本轮五维评审由控制器基于 live diff 与实测直接完成并作为最终准入：产品/规格 PASS（确认本轮仅把 docs 页面标题从装饰性英文标签收敛为共享控制台语义中文标题，并补强 fallback/bridge/loop 合同，没有改变单壳权限叙事）、代码质量 APPROVED（改动仅 3 个文件、13 行新增/6 行删除，测试命名与断言范围与运行时语义一致）、安全/集成 SAFE（无新凭据/权限/路由漂移，真实 API 菜单与 docs/bridge 容器合同保持成立）、测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、真实 API 回放全部通过；既有 React `act(...)`、Semi UI `findDOMNode` 仍为历史非阻塞噪声）、性能/运维 PASS（仅前端静态文案与测试合同收敛，不引入新运行时开销；前端 chunk 体积告警维持既有状态）。期间 3 个 delegate reviewer 明显漂移到其他仓库/无关 diff，已按 fail-closed 规则全部丢弃，未用于本仓库决策。下一前端优先项继续清理 `WebhooksPage.test.tsx`、`App.test.tsx` 中残留的低信号 page-global/长说明断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，AdminSuppliers / SupplierDomains / ApiKeys route heading alignment sweep）
- 本轮继续按“前端优先”收敛 shared-console focused 用例里残留的低信号 page-global route stub 文案：在不改运行时代码、不动注册/菜单/权限/真实 API 合同、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `AdminSuppliersPage.test.tsx`、`SupplierDomainsPage.test.tsx`、`ApiKeysPage.test.tsx` 中导航落点 proof 从 `结算与争议页面`、`风控中心页面`、`审计日志页面`、`供应商资源页`、`供应商供货页`、`供应商结算页`、`API Keys 页面`、`Webhook 设置页面`、`API 文档页面`、`项目市场页面` 等历史占位/低信号 stub 文本，统一收紧为 route-stub `data-testid` + canonical heading 双合同，并把 shared-home fallback 标题继续对齐运行时 dashboard 真值 `控制台总览`。
- 控制器已按 RED→GREEN 先修改三份 focused 用例中的路由桩与 heading proof，再重新通过 `pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/ApiKeysPage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/AdminSuppliersPage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/ApiKeysPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；并执行真实 API 回放脚本 `.hermes/local-output/front-route-heading-alignment-real-api/run.sh`（最新产物目录 `.hermes/local-output/front-route-heading-alignment-real-api/20260506-211614`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me`、`/auth/menu`、`/auth/api-keys` 返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview`、`/admin/wallet-users`、`/admin/disputes?limit=5`、`/admin/risk`、`/admin/audit?limit=5` 返回 200；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/supplier/resources/overview`、`/supplier/settlements` 返回 200；前端 `http://127.0.0.1:5173/`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/domains`、`/admin/suppliers` 均返回 SPA 壳 200。
- 本轮五维评审以 controller-side live diff 与实测为准：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；其中一次 delegate 安全/集成评审明显漂移到无关仓库，已按 fail-closed 规则丢弃，改由控制器基于 live diff、静态安全扫描、focused/grouped Vitest、build、Go tests、compose config 与真实 API 回放完成 repo-scoped 五维复核。评审一致确认当前 diff 仅把三组 focused 导航/回退 proof 从历史占位 route 文本收敛为更贴近 shared-console 运行时结构的 route-stub + heading 合同，不改变权限、导航、菜单或真实 API 契约；非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警。下一前端优先项继续清理其余 focused 用例里仍残留的低信号 page-global route/documentation 文本断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Balance route-stub + shared-shell heading contract tightening）
- 本轮继续按“前端优先”收敛 `BalancePage.test.tsx` 中仍残留的低信号 page-global 路由落点断言：在不改运行时代码、不改注册/菜单/权限/真实 API 合同、继续保持 new-api 风格单一登录后控制台的前提下，仅把余额页 5 条共享控制台导航 proof（项目市场、订单中心、API Keys、Webhook、Docs）从 `项目市场页面` / `订单中心页面` / `API Keys 页面` / `Webhook 设置页面` / `API 文档页面` 这类 page-global stub 文本，收紧为显式 `balance-route-stub-*` testid + shared-shell canonical heading 双合同；其中 heading 对齐 `resolveRouteTitle()` / `consoleNavigation.tsx` 中的壳内路由标题真值（如 `Webhook 设置`、`API 文档`），避免继续把历史占位文案固化进 focused proof，同时不把页面内部更长 hero 标题与共享壳 breadcrumb/header 标题混为一谈。
- 控制器已按 RED→GREEN 先修改 `BalancePage.test.tsx` 的 focused 用例与路由桩，再重新通过 `pnpm --dir web exec vitest run src/pages/BalancePage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/BalancePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；并执行真实 API 回放脚本 `.hermes/local-output/balance-route-stub-real-api/run.sh`（最新产物目录 `.hermes/local-output/balance-route-stub-real-api/20260506-205004`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me`、`/auth/menu`、`/wallet/overview`、`/wallet/transactions` 返回 200，且前端 `http://127.0.0.1:5173/`、`/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs` 均返回 SPA 壳 200。
- 本轮五维评审以 controller-side 事实为准：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；其中首轮产品评审一度把 Webhooks/Docs 的页面 hero 标题误当成 shared-shell route title，控制器已据 `consoleNavigation.tsx`、`resolveRouteTitle()`、`ConsoleLayout.tsx` 与 grouped App/Balance Vitest 复核，确认本轮合同应对齐共享壳路由标题而非页面 hero copy。评审一致确认当前 diff 仅把 Balance focused 导航落点 proof 从低信号 page-global 文本收敛为更贴近 shared-console 运行时结构的 route-stub + heading 合同，不改变权限、导航、菜单、真实 API 或部署流程；非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警。下一前端优先项继续清理其余 focused 用例中仍残留的 page-global 落点/说明断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Webhooks/AppSidebar shared-home route-stub heading alignment）
- 本轮继续按“前端优先”收敛 shared-console focused 用例里残留的旧 shared-home / page-global 落点文案：在不改运行时代码、不动注册/菜单/权限/真实 API 契约、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `WebhooksPage.test.tsx` 中空 endpoint fallback 的共享首页 stub 从旧文案 `共享控制台首页` 收紧为 `shared-console-home-route-stub` + `控制台总览` heading 双合同，并把 `AppSidebar.test.tsx` 中点击 `Webhook 设置` 后的落点 proof 从 page-global `Webhook 页面` 文本收紧为 `app-sidebar-route-stub-webhooks` + `Webhook 设置` heading 双合同，避免继续把历史占位文案和低信号整页文本固化进 focused proof。
- 控制器已按最小垂直切片完成验证：`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/components/AppSidebar.test.tsx` 通过（2 files / 15 tests）；随后 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 均通过，并执行真实 API 回放脚本 `.hermes/local-output/webhooks-home-stub-real-api/run.sh`（最新产物目录 `.hermes/local-output/webhooks-home-stub-real-api/20260506-203041`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me`、`/auth/menu`、`/webhooks/endpoints` 返回 200，前端 `http://127.0.0.1:5173/webhooks` 返回 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅把 Webhooks/AppSidebar 两个 focused 导航/回退 proof 从旧 shared-home 文案或低信号 page-global 文本收敛为更贴近运行时结构的 route-stub + heading 合同，不改变权限、导航、菜单、真实 API 或部署流程。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理其余 shared-console focused 用例中残留的旧 shared-home / page-global 落点断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，route-stub shared-home canonical heading follow-up）
- 本轮继续按“前端优先”收敛 shared-console focused 用例里仍残留的低信号 shared-home stub 文案：在不改运行时代码、不动注册/菜单/权限/真实 API 合同、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `AdminProjectsPage.test.tsx`、`SupplierDomainsPage.test.tsx`、`BalancePage.test.tsx` 中 fallback / shared-home route stub 的旧文案 `共享控制台首页` 统一对齐为 canonical dashboard heading `控制台总览`，并在管理员价格页、供应商域名页把回退落点 proof 收紧为 route-stub testid + heading 双合同，避免继续把历史首页占位文案固化进 focused proof。
- 控制器已按最小垂直切片完成验证：`pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/BalancePage.test.tsx` 通过（3 files / 18 tests）；随后 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 均通过，并执行真实 API 回放脚本 `.hermes/local-output/front-route-stub-heading-real-api/run.sh`（最新产物目录 `.hermes/local-output/front-route-stub-heading-real-api/20260506-200051`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201 且 `GET /api/v1/auth/me`、`/auth/menu`、`/wallet/overview`、`/wallet/transactions` 返回 200；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/risk`、`/admin/audit?limit=5`、`/admin/projects` 返回 200；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/supplier/resources/overview` 返回 200；前端 `http://127.0.0.1:5173/`、`/balance`、`/supplier/domains`、`/admin/pricing` 均返回 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅把 3 个 focused 测试中的 shared-home route stub / fallback 证明从旧文案 `共享控制台首页` 收敛到与运行时 dashboard 真值一致的 `控制台总览`，未改变权限、导航、真实 API 或部署流程。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、局部 `FormApi.setValues()` 与前端 chunk 体积告警；下一前端优先项继续清理其余残留旧 shared-home stub / page-global route 落点文本，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，shared-home canonical title alignment sweep）
- 本轮继续按“前端优先”收敛 shared-console focused 用例里残留的共享首页旧文案：在不改注册/登录真实 API 契约、不改权限/菜单/后端逻辑、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `ProtectedRoute.test.tsx`、`ProfilePage.test.tsx`、`ApiKeysPage.test.tsx`、`ApiDocsPage.test.tsx`、`LoginPage.test.tsx`、`SettingsPage.test.tsx`、`AdminUsersPage.shared-console.test.tsx`、`AdminRiskPage.test.tsx`、`SupplierOfferingsPage.test.tsx` 中 shared-home route stub / fallback 断言的标题，从旧的 `共享控制台首页` 统一对齐为运行时 canonical dashboard heading `控制台总览`，避免继续把历史首页占位文案固化进 focused proof。
- 控制器已按最小垂直切片完成验证：`pnpm --dir web exec vitest run src/components/ProtectedRoute.test.tsx src/pages/ProfilePage.test.tsx src/pages/ApiKeysPage.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/LoginPage.test.tsx src/pages/SettingsPage.test.tsx src/pages/AdminUsersPage.shared-console.test.tsx src/pages/AdminRiskPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx` 通过（9 files / 56 tests）；随后 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 均通过，并执行真实 API 回放脚本 `.hermes/local-output/console-home-title-alignment-real-api/run.sh`（最新产物目录 `.hermes/local-output/console-home-title-alignment-real-api/20260506-193531`），确认 `GET /healthz` 返回 200，普通用户 `POST /api/v1/auth/register` 返回 201 且 `GET /api/v1/auth/me` `/auth/menu` 返回 200、菜单不泄露 supplier/admin 路径；管理员与供应商 `POST /api/v1/auth/login` 后各自 `GET /api/v1/auth/menu` 返回 200；前端 `http://127.0.0.1:5173/` 返回 200 且 SPA 壳存在 `Nexus-Mail` 标识。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅统一 focused 测试中的 shared-home canonical title，使其与 `DashboardPage` / `resolveRouteTitle(DASHBOARD_ROUTE)` 的真实 `控制台总览` 合同对齐，不改变运行时权限、导航、菜单或真实 API 契约。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理其余未触达 focused 用例中残留的 `共享控制台首页` / 低信号 page-global 落点断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Supplier/Admin route-stub heading alignment tightening）
- 本轮继续按“前端优先”清理 shared-console focused 用例中仍残留的低信号 page-global 落点断言：在不改运行时代码、不改注册/菜单/权限/真实 API 合同、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `SupplierResourcesPage.test.tsx`、`SupplierSettlementsPage.test.tsx`、`AdminAuditPage.test.tsx` 的 mission-flow / shared-console fallback 导航 proof 从 `供应商资源页`、`供应商供货页`、`API Keys 页面`、`共享控制台首页` 这类 page-global stub 文本，收紧为 route-scoped `data-testid` + 与真实页面一致的 heading 双合同；同时把 shared-home stub 标题按当前真实 Dashboard 页对齐为 `控制台总览`，避免把旧的首页占位文案继续固化进测试。
- 控制器已按 RED→GREEN 先让三份 focused 测试暴露 route 落点和 shared-home heading 的低信号问题，再最小收敛测试 harness；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx src/pages/AdminAuditPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/front-route-stub-hardening/run.sh`（最新产物目录 `.hermes/local-output/front-route-stub-hardening/20260506-190617`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，新注册普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me` `/auth/menu` `/dashboard/overview` 返回 200 且菜单不暴露 supplier/admin 路径；管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu` `/admin/audit?limit=5` `/admin/risk` 返回 200；供应商 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu` `/supplier/resources/overview` `/supplier/settlements` 返回 200；前端 `/supplier/resources` `/supplier/settlements` `/admin/audit` `/api-keys` `/webhooks` `/docs` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS（同轮已把 shared-home stub heading 从 `共享控制台首页` 修正为与真实 Dashboard 一致的 `控制台总览`）、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅把 Supplier/Admin 三个 focused 导航/回退 proof 从低信号 page-global 落点文本收敛为更贴近运行时结构的 route-stub + heading 合同，不改变权限、导航或真实 API 契约。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理其余 shared-console focused 用例中仍残留的 page-global 落点/说明断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Projects/Orders guidance scoped contract tightening）
- 本轮继续按“前端优先”推进 shared-console focused 去脆弱化：在不改真实注册/菜单/API 契约、不拆分独立后台、继续保持 new-api 风格单一登录后控制台的前提下，仅把 `ProjectsPage` / `OrdersPage` 中采购与履约任务流的 proof 从 page-global copy 收紧为更贴近运行时结构的 scoped guidance / continuation 合同；其中为 `ProjectsPage.tsx` 的采购动作区补充最小 `projects-action-guidance-card`、`projects-guidance-next-step-card`、`projects-guidance-fallback-card` 语义锚点，并把 `ProjectsPage.test.tsx` / `OrdersPage.test.tsx` 分别改为在对应 scoped 卡片内验证 `下单后下一步`、`共享控制台回退路径`、`订单结果 → API 接入 → 再次采购` 及相关 CTA，而不再依赖易重复的整页说明文案。
- 控制器已按 RED→GREEN 先让新的 scoped focused 断言失败，再以最小运行时代码补齐缺失锚点并复验通过；随后重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_projects_orders_hero_scope_real_api.sh`（本轮最新产物目录 `.hermes/local-output/projects-orders-hero-scope/20260506-183800`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` `/auth/me` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` `/projects/inventory` `/orders/activations` 返回 200，管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu` `/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200，且前端 `/` `/projects` `/orders` `/api-keys` `/webhooks` `/docs` `/admin/suppliers` 均保持 SPA 壳 200。
- 本轮五维评审以 controller-side 事实为准：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；其中 code quality / reliability 仅继续记录既有 Semi UI `findDOMNode`、React `act(...)` 与前端 chunk 体积告警为非阻塞债务。评审一致确认当前 diff 仅把采购/履约任务流 proof 收紧到 scoped 语义区域，不改变权限、导航或真实 API 合同；下一前端优先项继续清理其他 shared-console focused 用例里仍残留的 page-global copy / route-stub 落点断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Login auth shell embedded-tab refinement）
- 本轮继续按“前端优先”推进登录/注册入口的 shared-console / new-api 风格统一：在不改注册/登录真实 API 契约、不拆分独立后台、继续保持“注册成功后进入同一套共享控制台”的前提下，仅收敛 `LoginPage.tsx` 右侧 auth shell 的内部层级与可访问语义 —— 为登录 / 注册切换补齐 `tablist` / `tab` + `aria-selected` 合同，把浅色 segmented control 改成嵌入式深色切换条，引导 banner 压暗到与壳同层的嵌入态，表单输入框同步改为深色高对比输入面，并把注册 guidance 明确收敛到真实共享菜单术语 `项目市场 → 订单中心 → API Keys`，继续保持单一登录后共享控制台叙事而不是独立新手页或多后台分叉。
- 控制器已按 RED→GREEN 先扩展 `LoginPage.test.tsx` 与联动 `App.test.tsx`，让 focused 用例先要求 `login-auth-mode-switch` 的 `tablist` / `tab` 可访问语义、登录/注册 guidance 切换、`统一控制台认证` 壳内 eyebrow 文案，以及 App 层注册入口从 mode switch 触发的真实 register bootstrap 路径；随后以最小运行时代码补齐 auth shell 语义与样式，再重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行真实 API 回放脚本 `.hermes/local-output/login-auth-shell-refine-real-api/run.sh`（最新产物目录 `.hermes/local-output/login-auth-shell-refine-real-api/latest`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，本地 SPA `http://127.0.0.1:5173/login` 在一次启动瞬态 `connection reset` 重试后返回 200 且包含 `Nexus-Mail` 壳标识，普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me` `/auth/menu` `/dashboard/overview` `/auth/api-keys` 均返回 200。
- 本轮五维评审以 controller-side 事实为准：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间两轮 reviewer 曾分别 fail-closed 提醒（1）认证模式切换不应只停留在 `aria-pressed`，应升级为 mutually-exclusive `tablist` / `tab` 语义；（2）注册 guidance 不应使用泛化的 `API 接入` 术语而应与真实共享菜单 `API Keys` 对齐；控制器已在同轮完成整改并复跑 focused Vitest / build / Go tests / compose config / 真实 API 全部通过。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、前端 chunk 体积告警，以及浏览器视觉评审指出 auth shell 文案/输入框仍可继续做一轮对比度微调；下一前端优先项继续围绕登录壳剩余可读性微调，或转向其他 shared-console focused 用例/页面的低信号断言收敛，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Login shared-console auth shell darkening）
- 本轮继续按“前端优先”推进登录/注册入口的 shared-console / new-api 风格统一：在不改注册/登录真实 API 契约、不拆分独立后台、继续保持“注册成功后进入同一套共享控制台”的前提下，仅把 `LoginPage.tsx` 右侧认证卡从高亮白卡收敛为深色 auth shell，并把登录/注册标题与主 CTA 文案统一改为 `登录并进入 Shared Console` / `创建账号并进入 Shared Console`、`登录并进入统一控制台` / `注册并进入统一控制台`，同时补充最小 `login-auth-shell` 语义锚点，确保登录入口视觉与 shared-console 单壳叙事一致，不再像独立营销页弹出的浅色表单。
- 控制器已按 RED→GREEN 先修改 `LoginPage.test.tsx`，让 focused 用例先要求新的 shared-console auth shell 文案与 `login-auth-shell` 锚点，再最小更新运行时代码与 `App.test.tsx` 的联动断言；随后重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`docker compose up -d --build web` 与 `git diff --check`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_login_console_refresh_real_api.sh`（最新产物目录见 `.hermes/local-output/login-console-refresh-real-api/latest.txt`），确认 `GET /healthz` 返回 200 且 `status=ok`、`redis=ok`，前端 `/login` `/` `/api-keys` `/docs` 返回 200，普通用户 `POST /api/v1/auth/register` 返回 201，`GET /api/v1/auth/me` `/auth/menu` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 均返回 200。
- 本轮五维评审以 controller-side 事实为准：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间已丢弃 5 份明显跨仓库漂移、错误审阅到其他仓库的 delegate 结果，并按 fail-closed 规则改用 live diff、focused Vitest、build、Go tests、compose config、静态安全扫描、真实 API 回放与浏览器视觉检查作为最终判定依据。评审一致确认当前 diff 仅收敛登录入口文案与 auth shell 视觉层级，不改变权限、注册/登录契约或 shared-console 单壳导航真值；非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、前端 chunk 体积告警，以及浏览器视觉评审指出的 auth shell 内部分层/提示条仍可继续压暗与 segmented control 仍可进一步精修，下一前端优先项继续围绕登录卡片内部层级、提示条暗色嵌入式样式与 segmented control 精致度收敛，再回到 Phase 4/5 余下能力。

### 最新执行检查点（2026-05-06，AdminSuppliers route-stub scoped proof）
- 本轮继续按“前端优先”收敛 `AdminSuppliersPage.test.tsx` 中仍残留的低信号 page-global 路由落点断言：在不改 `AdminSuppliersPage.tsx` 运行时代码、单一登录后 shared-console / new-api 单壳导航合同、管理员角色菜单 gating 与真实接口合同的前提下，仅把管理员供应商运营台 mission-flow 三条导航 proof（结算 / 争议、风控中心、审计日志）以及共享回退 proof，从 `结算与争议页面`、`风控中心页面`、`审计日志页面`、`共享控制台首页` 这类 page-global stub 文本，收紧为统一命名的 `admin-suppliers-route-stub-*` 语义锚点合同，避免继续依赖整页重复落点文案，同时保持“管理员供应商运营页仍属于同一共享控制台壳，而非独立后台”的产品语义不变。
- 控制器已按 RED→GREEN 先让 `pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx` 验证 focused 用例，再重新通过 `pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`；同时执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_suppliers_route_stub_scope_real_api.sh`（最新产物目录见 `.hermes/local-output/admin-suppliers-route-stub-scope/latest.txt`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` `/auth/me` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 返回 200、管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu` `/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200，且前端 `/` `/admin/suppliers` `/api-keys` `/webhooks` `/docs` `/admin/risk` `/admin/audit` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间根据代码质量/运维评审的非阻塞建议，把 fallback 路由桩命名从不规则的 shared-home 特例统一收敛为 `admin-suppliers-route-stub-shared-home`，随后重新复跑 focused / grouped Vitest、build、Go tests、compose config 与真实 API 回放全部通过。评审一致确认当前 diff 仅把 AdminSuppliers focused 导航落点 proof 从低信号 page-global stub 文本收敛为更贴近运行时结构的 route-stub 合同，不改变权限、导航或真实 API 合同；非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警，下一前端优先项继续清理其余 admin/shared-console focused 用例里的低信号 page-global 落点 / 文本断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Settings capability-matrix scoped proof）
- 本轮继续按“前端优先”收敛 `SettingsPage.test.tsx` 与联动 helper `App.test.tsx` 中仍残留的低信号 page-global 文案断言：在不改 shared-console/new-api 单壳运行时合同的前提下，仅为 `SettingsPage.tsx` 的“控制台能力矩阵”卡片补充最小 `settings-capability-matrix` 语义锚点，把设置页 focused proof 从 `深色共享工作台`、`Docs / Webhooks / API Keys 已统一到单一壳内导航`、普通用户首轮提示整句 copy 等 page-global 文本，收紧为 checklist / mission-cards / capability-matrix scoped 合同，同时删除 `App.test.tsx` 中 `expectDefaultUserFirstRunLane()` 对 `欢迎进入共享控制台` 的 page-global heading 依赖，继续保持注册后统一壳、角色菜单 gating、设置页 → Docs/Webhooks 导航与普通用户首轮回流语义真值不变，而不是继续依赖易变整页文案。
- 控制器已按 RED→GREEN 先让 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 暴露 scoped 锚点缺失，再以最小运行时改动补上 `settings-capability-matrix` 后复验通过；随后重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/SettingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_settings_front_scope_real_api.sh`（最新产物目录 `.hermes/local-output/settings-front-scope/20260506-164205`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` `/auth/me` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 返回 200、管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/auth/menu` `/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200，且前端 `/` `/settings` `/profile` `/api-keys` `/webhooks` `/docs` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅为 Settings focused proof 补最小语义锚点并把断言收敛到更贴近运行时结构的 scoped 合同，不改变权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、局部 `async-validator`/Semi Form warning 与前端 chunk 体积告警；下一前端优先项继续清理其余 shared-console focused 用例里仍残留的 page-global copy / route-stub 落点断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Projects/Orders route-stub scoped proof）
- 本轮继续按“前端优先”收敛 `ProjectsPage.test.tsx` 与 `OrdersPage.test.tsx` 中仍残留的低信号 page-global 路由落点断言：在不改运行时代码与真实接口合同的前提下，仅把采购页与订单页内 4 条共享控制台导航 proof（项目市场 → 订单中心、项目市场 → API Keys、订单中心 → 项目市场、两个 empty-state → 推荐工作台回流）从 `订单中心页面` / `开发者 API 接入工作台` / `控制台总览页面` 这类 page-global 文本，收紧为显式 `*-route-stub` / `*-shared-console-home-route` `data-testid` 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Projects ↔ Orders ↔ API Keys 单壳衔接与共享回退语义真值不变，而不是继续依赖易重复的整页落点文案。
- 控制器已按 RED→GREEN 先完成 focused 用例修正，再重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config` 与 `git diff --check`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_projects_orders_hero_scope_real_api.sh`（最新产物目录 `.hermes/local-output/projects-orders-hero-scope/20260506-161820`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` `/auth/me` `/dashboard/overview` `/projects/inventory` `/orders/activations` 返回 200、管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200，且前端 `/` `/projects` `/orders` `/api-keys` `/webhooks` `/docs` `/admin/suppliers` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间已丢弃 5 份明显跨仓库漂移、错误审阅到 `/root/.hermes/hermes-agent` 的 delegate 结果，并按 fail-closed 规则改用 controller-side live diff、静态安全扫描、focused Vitest、build、`go test ./...`、`docker compose config`、真实 API 回放作为最终判定依据。评审一致确认当前 diff 仅把 Projects / Orders focused 导航 proof 从低信号 page-global 落点文本收敛为更贴近运行时结构的 scoped route-stub 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理其余 shared-console focused 用例里仍残留的 page-global 落点 / 说明断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，ApiKeys route-stub scoped proof）
- 本轮继续按“前端优先”收敛 `ApiKeysPage.test.tsx` 中仍残留的低信号共享接入落点断言：在不改运行时代码与真实接口合同的前提下，仅把 bridge / fallback 导航 proof 从 `Webhook 设置页面` / `API 文档页面` / `项目市场页面` / `共享控制台首页` 这类 page-global 文本，收紧为 `api-keys-shared-console-bridge`、`api-keys-shared-console-fallback` 作用域内 CTA 交互 + `route-stub-*` `data-testid` 合同，并把创建后桥接验证表述收敛为“创建前后均可通过 scoped bridge 导航”，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、API Keys → Webhooks / Docs / Projects 单壳桥接与共享回退语义真值不变，而不是继续依赖易重复的整页落点文案。
- 控制器已按 RED→GREEN 先让 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 验证 focused 用例，再重新通过 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_keys_focused_hardening_real_api.sh`（最新产物目录 `api-keys-focused-hardening-20260506-151730`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/me` `/auth/menu` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 返回 200，且前端 `/api-keys` `/webhooks` `/docs` `/settings` 均保持 SPA 壳 200。期间 controller-side grouped Vitest 曾一次性命中与本 diff 无关的 `App.test.tsx` 历史异步波动（管理员 Webhook endpoint skeleton 先渲染导致文本断言瞬时失败），随后按 fail-closed 规则单独隔离 `renders webhook settings page for authenticated admin` 并重跑完整分组后恢复 38/38 通过，因此该波动记录为既有非阻塞 flake 债务而非本轮回归。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅把 `ApiKeysPage.test.tsx` 中共享接入 bridge / fallback 导航 proof 从低信号 page-global 落点文本收敛为更贴近运行时结构的 scoped CTA + route-stub 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、前端 chunk 体积告警，以及 grouped App/Webhooks 异步骨架偶发波动；下一前端优先项继续清理其余 shared-console focused 用例里仍残留的低信号 page-global 落点/说明断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Dashboard route-stub + async webhook readiness proof）
- 本轮继续按“前端优先”收敛 shared-console focused 用例中的低信号 page-global 落点断言：在不改运行时代码与真实接口合同的前提下，仅把 `DashboardPage.test.tsx` 中 shared journey lane 的四条导航 proof，从 `余额中心页面` / `项目市场页面` / `订单中心页面` / `开发者 API 接入工作台` 这类 page-global stub 文本，收紧为 route-local `dashboard-*-route-stub` 语义锚点 + 与真实页面对齐的 heading 合同；同时顺手把 `App.test.tsx` 中管理员 Webhook 首屏对当前 endpoint 的同步文本命中改为 `findBy*` 异步就绪 proof，避免 grouped App/Dashboard 回归里受首屏 skeleton 时序影响而出现假阴性，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Dashboard 下一步旅程与 Webhook 管理页真实 API 菜单真值不变。
- 控制器已按 RED→GREEN 先让 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx` 暴露 navigation proof 的 page-global 文本脆弱点，再补齐 route-stub + heading 合同，并在系统化调试后修复 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx` 暴露的管理员 Webhook current-endpoint skeleton 时序问题；随后重新通过 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/dashboard-route-stub-scope/run_dashboard_route_stub_scope_real_api.sh`（最新产物目录见 `.hermes/local-output/dashboard-route-stub-scope/latest.txt`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` `/auth/me` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 返回 200、管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200，且前端 `/` `/api-keys` `/webhooks` `/docs` `/settings` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；其中产品评审一度 fail-closed 指出 route-stub heading 应与真实目标页标题一致，代码质量评审提醒 route-stub testid 不应单独替代行为级合同，控制器已在同轮把 Dashboard 三个目标页 heading 改为 `余额中心` / `项目市场` / `订单中心` 并保留 route-stub + heading 双合同后复验通过。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理其余 shared-console focused 用例中仍残留的低信号 page-global 落点 / 文本断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Profile route-stub scoped proof）
- 本轮继续按“前端优先”收敛 `ProfilePage.test.tsx` 中仍残留的低信号 route 落点文本断言：在不改运行时代码与真实接口合同的前提下，仅把 `profile-capability-bridge` 与 `profile-shared-console-return` 两组导航 proof 的目标页判断，从 `API Keys 页面` / `Webhook 设置页面` / `API 文档页面` / `设置中心页面` / `共享控制台首页` 这类 page-global 文本，收紧为显式 `*-route-stub` `data-testid` 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Profile→API Keys/Webhooks/Docs/Settings/shared-home 单壳回流语义真值不变，而不是继续依赖易重复的整页文案节点。
- 控制器已按 RED→GREEN 先让 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 验证 Profile focused 用例，再重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_profile_route_stub_scope_real_api.sh`（最新产物目录 `profile-route-stub-scope/20260506-142821`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` `/auth/me` `/dashboard/overview` `/auth/api-keys` `/webhooks/endpoints` 返回 200、管理员 `POST /api/v1/auth/login` 后 `GET /api/v1/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200，且前端 `/` `/profile` `/settings` `/api-keys` `/webhooks` `/docs` `/admin/risk` `/supplier/domains` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；同轮已丢弃 3 份明显跨仓库漂移、错误审阅到 `/root/.hermes/hermes-agent` 的 delegate 结果，并以 controller-side live diff、focused tests、build、`go test ./...`、`docker compose config`、静态安全扫描与真实 API 回放作为最终判定依据。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、前端 chunk 体积告警，以及当前仓库早已存在且与本次 diff 无关的 `pnpm --dir web lint` 类型错误；下一前端优先项继续清理 `ApiKeysPage.test.tsx` 等仍依赖 route-stub 页面文本落点的 focused 用例，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，route-stub scope hardening）
- 本轮继续按“前端优先”收敛 `ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 与 `App.test.tsx` 中仍残留的低信号 page-global 导航落点断言：在不改运行时代码与真实接口合同的前提下，仅把 Docs / Webhooks 多跳 CTA 的目标页证明从宽泛页面文本收紧为显式 route-stub `data-testid`，并把 App 层 supplier profile / dashboard 跳转后的落点从普通文本收紧为 `域名池运营中枢` heading；同时保留 `WebhooksPage.test.tsx` 首屏 `loads endpoints and auto-loads first endpoint deliveries` 对自动拉取首个 endpoint delivery 的真实断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Docs ↔ API Keys ↔ Webhooks 单壳桥接与 supplier 域名运营路径真值不变，而不是继续依赖易重复的整页文案节点。
- 控制器已按 RED→GREEN 先将 route 落点 proof 改为 stub/heading 合同，再根据产品评审补回 `mockedGetWebhookEndpoints` 与 `mockedGetWebhookDeliveries` 断言以维持测试名与证明内容一致；随后重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_route_stub_scope_real_api.sh`（产物目录 `route-stub-scope-real-api/20260506-124759`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints` 全部返回 200、管理员 `POST /api/v1/auth/login` 后 `/admin/overview` `/admin/risk` `/admin/audit?limit=5` 返回 200、供应商 `/supplier/resources/overview` `/supplier/settlements` 返回 200，且前端 `/` `/login` `/profile` `/settings` `/api-keys` `/webhooks` `/docs` `/supplier/domains` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；产品评审曾 fail-closed 指出 `loads endpoints and auto-loads first endpoint deliveries` 被过度弱化，控制器已在同轮补回自动拉取 delivery 的关键断言后复验通过。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理 `ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx`、`App.test.tsx` 之外其余 shared-console focused 用例里仍残留的低信号 page-global 落点/说明断言，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Webhooks role-guidance scoped proof）
- 本轮继续按"前端优先"收敛 `WebhooksPage.test.tsx` 中仍残留的低信号 page-global 角色引导 / 空态文案断言：在不改运行时代码与真实接口合同的前提下，仅把 supplier/admin 角色视图对 `供给事件回调工作台`、`Webhook 运维与回调观测`、`供应商视角`、`管理员视角` 的证明收紧到 `webhooks-role-guidance` 卡片内，并把空 endpoint 场景对 `当前还没有 Webhook endpoint，先创建第一个回调地址。` 的断言收紧到 `webhooks-current-endpoints-card` 作用域内；同时保留管理员视角对 `最近回调`、`失败 / 排队中`、`重点关注 failed / pending 重试链路与 last_error` 的真实运维信号合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Webhook → API Keys / Docs 接入闭环与真实 API 菜单真值不变，而不是继续依赖易重复的整页文案节点。
- 控制器已按 RED→GREEN 先把 `WebhooksPage.test.tsx` 中 supplier/admin guidance 与 empty-state proof 改为 card-scoped 合同，再依据产品评审把 badge 断言从 `getByLabelText('Tag: ...')` 回调为 `webhooks-role-guidance` 区域内真实可见文案，随后重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/verify_webhooks_role_guidance_scope.sh`（最新产物目录 `webhooks-role-guidance-card-scope/20260506-132538`），确认 `GET /healthz` 返回 200 且 `redis=ok`、普通用户 `POST /api/v1/auth/register` 返回 201、`GET /api/v1/auth/menu` / `GET /api/v1/webhooks/endpoints` 返回 200、管理员 `GET /api/v1/admin/risk` / `GET /api/v1/admin/audit?limit=5` 返回 200、供应商 `GET /api/v1/supplier/resources/overview` / `GET /api/v1/supplier/settlements` 返回 200，且前端 `/` `/login` `/webhooks` `/api-keys` `/docs` `/admin/audit` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；产品评审曾 fail-closed 指出把 badge 断言降级为 `Tag:` 可访问名前缀会弱化用户可见合同，控制器已在同轮回调为 `webhooks-role-guidance` 区域内真实文案断言后复验通过。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理 `ProfilePage.test.tsx`、`ApiKeysPage.test.tsx` 等仍依赖 route-stub 页面文本落点的 focused 用例，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，Login shared-console hero densification）
- 前端优先推进登录/注册页垂直切片：将左侧信息架构从多组冗余旅程/说明/接入卡压缩为更接近 new-api / Linear 风格的深色 hero signals + 单一 registration runway，保留单一登录后共享控制台叙事，并继续维持注册仅 `email/password`、成功后进入同一控制台的真实行为合同。
- 本轮新增 `login-hero-signal-grid` 与更紧凑的 `login-register-journey` 结构，删除重复的 `journeyCards` / `capabilityHighlights` / `firstRunChecklist` / `integrationStages` 大块文案；`LoginPage.test.tsx` 先以 RED→GREEN 方式新增 compact hero / runway focused proof，确认新结构存在且旧冗余区块不再渲染，然后再完成运行时代码收敛。
- 验证结果：`pnpm exec vitest run src/pages/LoginPage.test.tsx` 通过；`pnpm exec vitest run src/App.test.tsx src/pages/LoginPage.test.tsx` 通过；`pnpm build` 通过；`go test ./...` 通过；`docker compose up -d --build web` 成功；真实 API 回放脚本 `/root/nexus-mail/.hermes/local-output/login-milestone/verify_login_milestone.sh` 完成 `POST /auth/register`、`GET /auth/menu`、`GET /auth/me`、`GET /dashboard/overview` 以及 `/login`、`/`、`/projects`、`/api-keys`、`/webhooks`、`/docs` 的 SPA shell 200 检查并全部通过。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 真实推进的是登录入口的信息密度与单壳注册跑道，而非后端认证合同变更。非阻塞债务仍仅包括既有 React `act(...)` 告警与前端 chunk 体积告警；下一前端优先项继续转向 shared-console 首屏 / 仪表盘 remaining focused proof 去脆弱化与视觉收敛，随后再回到 Phase 4/5 的 API Key / Webhook / 风控 / 审计后续切片。

### 最新执行检查点（2026-05-06，SupplierOfferings scoped contract hardening）
- 本轮继续按"前端优先"收敛 `SupplierOfferingsPage.test.tsx` 中供货规则页首个 mission-shell focused 用例仍残留的低信号 page-global 文本/指标命中：在不改运行时代码与真实接口合同的前提下，仅为 `SupplierOfferingsPage.tsx` 的 hero 区补充 `supplier-offerings-hero-card`、为四张汇总指标卡补充 `supplier-offerings-metric-*`、为 shared-console pillar 卡补充 `supplier-offerings-console-pillar-*`、并为当前可售规则表补充 `supplier-offerings-table` / `aria-label` 最小语义锚点；同时把对应 focused proof 从整页标题/文案/按钮命中收紧为 hero / mission-flow / shared-console-bridge / offerings-table scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、供应商任务流与共享接入桥接真值不变，而不是继续依赖整页重复 copy、数字或按钮永远唯一这一过宽假设。
- 控制器已按 RED→GREEN 先让 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx` 暴露多重命中与按钮可访问名问题，再补齐最小锚点后复跑通过；随后重新通过 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/App.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_supplier_offerings_real_api.sh`（最新产物目录 `supplier-offerings-real-api/20260506-121800`），确认 `GET /healthz` 返回 200 且 `redis=ok`、新注册普通用户 `/auth/menu` 暴露共享壳入口、供应商 `/auth/menu` 包含 `/supplier/resources` `/supplier/offerings` `/supplier/settlements` `/api-keys` `/webhooks` `/docs`、供应商真实接口 `/supplier/resources/overview` `/supplier/resources/offerings` `/supplier/settlements` 均返回 200、管理员 `/admin/risk` 与 `/admin/audit?limit=5` 返回 200，且前端 `/` `/supplier/offerings` `/api-keys` `/webhooks` `/docs` 均保持 SPA 壳 200。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；测试/可靠性评审一度指出若只保留两张指标卡会削弱对 `覆盖项目` 与 `可挂接域名池` 的合同保护，控制器已在同轮补回这两项 scoped metric proof 后复验通过。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode`、局部表单校验 warning 与前端 chunk 体积告警；下一前端优先项继续清理 `SupplierOfferingsPage.test.tsx` 之外其余 shared-console focused 用例中的低信号 page-global 命中，再回到 Phase 4/5 余下后端能力。

### 最新执行检查点（2026-05-06，shared shell semantic region hardening）
- 本轮继续按“前端优先”收敛 shared-shell focused 用例中仍残留的 page-global 角色/头部摘要断言：在不改运行时代码与真实接口合同的前提下，仅为 `AppSidebar.tsx` 的角色标签 + 摘要说明补充最小 `app-sidebar-role-summary` 语义区域，并为 `ConsoleLayout.tsx` 的 breadcrumb / 当前页标题 / 角色标签 / 单一登录后控制台提示补充最小 `console-layout-header-summary` 语义区域；同时把 `AppSidebar.test.tsx` 与 `ConsoleLayout.test.tsx` 中对供应商/管理员角色摘要、共享头部标题与单壳提示的 proof 从 page-global 文本命中收紧为对应语义区域 scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、统一布局骨架、角色菜单 gating 与单壳导航真值不变，而不是继续依赖整页同名文本永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API 回放脚本 `.hermes/local-output/run_shared_shell_semantic_region_real_api.sh`（产物目录 `shared-shell-semantic-region-real-api/20260506-103637`），确认 `GET /healthz` 返回 200 且 `redis=ok`、新注册普通用户 `/auth/menu` 仅暴露共享工作台路径并排除 `/admin/risk` 与 `/supplier/domains`、普通用户 `/auth/me` `/dashboard/overview` `/projects/inventory` `/orders/activations` `/auth/api-keys` `/webhooks/endpoints` 全部返回 200、管理员 `/auth/menu` 含 `/admin/risk` 与 `/admin/audit`，且 `/admin/overview` `/admin/risk` `/admin/audit?limit=5` 均返回 200，证明单一登录后共享壳的角色菜单与管理面真实 API 合同未被此次语义锚点收敛破坏。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间已丢弃 3 组明显跨仓库漂移、错误审阅到 `/root/.hermes/hermes-agent` 的 delegate 结果，并以 controller-side live diff、focused tests、build、`go test ./...`、真实 API 回放与静态安全扫描为最终判定依据。评审一致确认当前 diff 仅为 shared shell 角色摘要区与头部摘要区补充最小语义锚点，并把 focused proof 收敛为更贴近运行时结构的 scoped 合同，不改变运行时权限、导航或真实 API 合同；非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警，下一前端优先项继续清理 `App.test.tsx` 及部分 shared-shell/page focused 用例里仍残留的低信号 page-global 文本/落点断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，App webhook send-action scoped proof）
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中管理员 Webhook 页 `creates a webhook test delivery and refreshes the delivery feed` 用例仍残留的低信号 page-global 同名按钮命中：在不改运行时代码与真实接口合同的前提下，仅把点击目标从 page-global `findByRole('button', { name: '发送测试投递' })` 收紧为 `webhooks-current-endpoints-card` 区域内绑定真实 endpoint 行动作的 `webhooks-send-test-button-11` 语义锚点，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Webhook → API Keys / Docs 单壳联动与真实 API 菜单真值不变，而不是继续依赖整页同名按钮永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_admin_send_scope_real_api.sh`（产物目录 `webhooks-admin-send-scope/20260506-092238`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、普通用户新建 endpoint 后 `POST /api/v1/webhooks/endpoints/<user_endpoint_id>/test-delivery`（202）、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、真实 Webhook 投递链路、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致确认当前 diff 仅把 App 层管理员 Webhook 发送测试动作 proof 从低信号 page-global 同名按钮命中收敛为更贴近运行时结构的 current-endpoint-card / row-action scoped 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本/按钮断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，Login/Webhooks focused route-scope proof）
- 本轮继续按“前端优先”收敛 `LoginPage.test.tsx` 与 `WebhooksPage.test.tsx` 中仍残留的低信号 page-global 落点 / 说明文案断言：在不改运行时代码与真实接口合同的前提下，仅把注册壳相关用例对首页回流与注册 CTA 的 proof 收紧为 `login-register-journey` scoped 查询 + `shared-console-home` 路由桩锚点，把 Webhook 首轮集成卡与空态回退 proof 从 page-global 文案命中收紧为步骤 heading / `webhooks-empty-state-actions` scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating、Webhook → API Keys / Docs 接入闭环与真实 API 菜单真值不变，而不是继续依赖易重复的整页文案或宽泛首页文本节点。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/LoginPage.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_login_webhooks_test_scope_real_api.sh`（产物目录 `login-webhooks-test-scope/20260506-095112`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；五路评审一致确认当前 diff 仅把 Login / Webhooks focused proof 从低信号 page-global 文本与宽泛首页落点命中收敛为更贴近运行时结构的 journey-scope、步骤 heading、empty-actions scope 与 route-stub 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理 `App.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本/指标断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，AdminRisk rule editor scoped proof）
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中管理员风控页 rule editor focused 用例仍残留的低信号 page-global 按钮断言：在不改运行时代码与真实接口合同的前提下，仅为 `AdminRiskPage.tsx` 的“可配置风控规则”卡片补充最小 `admin-risk-rule-editor-card` 语义锚点，并把 App 层管理员风控首屏 proof 从 page-global `getByRole('button', { name: '保存规则' })` 收紧为 `admin-risk-overview-card` / `admin-risk-rule-editor-card` scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、风控→审计/API Keys 单壳联动与真实 API 菜单真值不变，而不是继续依赖整页同名按钮永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_risk_rule_editor_scope_real_api.sh`（产物目录 `admin-risk-rule-editor-scope/20260506-073341`；同轮此前也完成 `20260506-073143` 复放），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/login`、`/admin/risk`、`/admin/audit`、`/webhooks`、`/api-keys`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员风控/审计接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；五路评审一致确认当前 diff 仅为 AdminRisk 风控规则编辑卡补充最小语义锚点，并把 App 层管理员风控首屏 proof 从低信号 page-global 按钮命中收敛为更贴近运行时结构的 overview-card / rule-editor-card scoped 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，Webhook send-test button scoped proof）
- 本轮继续按"前端优先"收敛 `WebhooksPage.test.tsx` 与 `App.test.tsx` 中 webhook 发送测试投递 focused 用例仍残留的低信号同名按钮命中：在不改真实接口与角色权限合同的前提下，仅为 `WebhooksPage.tsx` 的 endpoint 行级“发送测试投递”按钮补充最小 `webhooks-send-test-button-${record.id}` 语义锚点，并把页级 / App 级 proof 从 page-global `getByRole('button', { name: '发送测试投递' })` 收紧为 `webhooks-current-endpoints-card` 区域内绑定真实 endpoint id 的 scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Webhook → API Keys / Docs 接入闭环与真实 API 菜单真值不变，而不是继续依赖整页同名动作按钮永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_send_button_scope_real_api.sh`（产物目录 `webhooks-send-test-button-scope/20260506-084608`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/webhooks`、`/api-keys`、`/docs`、`/admin/audit`、`/login` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间已丢弃两组明显跨仓库漂移、错误审阅其他仓库 diff 的 delegate 结果，并以 controller-side live diff、focused tests、build、`go test ./...`、compose 重建与真实 API 回放为最终判定依据。评审一致确认当前 diff 仅为 webhook 行级发送测试投递按钮补充最小语义锚点，并把相关 proof 收敛为更贴近运行时结构的 endpoint-card / row-action scoped 合同，不改变运行时权限、导航或真实 API 合同；非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警，下一前端优先项继续清理 `WebhooksPage.test.tsx`、`App.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本/按钮断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，Dashboard admin scoped proof）
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中管理员 dashboard deep statistics 用例仍残留的低信号 page-global 指标/标题断言：在不改运行时代码与真实接口合同的前提下，仅为 `DashboardPage.tsx` 的"订单完成率"指标卡补充最小 `dashboard-order-completion-rate-card` 语义锚点，并把 App 层对 `订单完成率`、`争议发生率`、`已完成订单流水`、`当前重点关注供应商` 的 proof 从 page-global `findByText/getByText` 收紧为 completion-rate / dispute-rate / revenue / top-supplier card scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与管理员运营概览真值不变，而不是继续依赖整页重复指标标题永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/DashboardPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_dashboard_admin_scope_real_api.sh`（产物目录 `dashboard-admin-scope/20260506-070723`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员概览/风控/审计接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；controller-side 复核确认当前 diff 仅为 Dashboard 页面补充最小语义锚点，并把 App 层管理员概览 focused proof 从低信号 page-global 文本命中收敛为更贴近运行时结构的 metric-card / top-supplier-card scoped 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警；下一前端优先项继续清理 `App.test.tsx`、`AppSidebar.test.tsx`、`ConsoleLayout.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，Settings session scoped proof）
- 本轮继续按"前端优先"收敛 `App.test.tsx` 与 `SettingsPage.test.tsx` 中仍残留的低信号 page-global 会话文案断言：在不改运行时代码与真实接口合同的前提下，仅为 `SettingsPage.tsx` 的“当前登录会话”卡片补充最小 `settings-session-card` 语义锚点，并把 App / 设置页层对 `控制台模式`、注册后 API Keys 首屏等 proof 从 page-global `findByText/getByText` 收紧为 `settings-session-card` / `api-keys-hero-card` / heading scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与共享接入入口真值不变，而不是继续依赖整页重复说明文案永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/SettingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_settings_session_scope_real_api.sh`（产物目录 `settings-session-scope/20260506-064609`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`POST /api/v1/auth/logout` 与前端 `/settings`、`/login` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、会话信息卡、API Keys 首屏与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；期间已丢弃一组明显跨仓库漂移的 delegate 评审结果，并以控制器基于 live diff、focused tests、build、`go test ./...` 与真实 API 回放的 repo-scoped 复核为准。当前 diff 仅为 Settings 会话卡补充最小语义锚点，并把 App / Settings focused proof 从低信号 page-global 文本命中收敛为更贴近运行时结构的 session-card / hero-card / heading scoped 合同，不改变运行时权限、导航或真实 API 合同；下一前端优先项继续清理 `App.test.tsx`、`WebhooksPage.test.tsx`、`ApiDocsPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06，AdminRisk scoped proof）
- 本轮继续按"前端优先"收敛 `App.test.tsx` 与 `AdminRiskPage.test.tsx` 中管理员风控页仍残留的低信号 page-global 风险文本断言：在不改运行时代码与真实接口合同的前提下，仅为 `AdminRiskPage.tsx` 的 mission-signals 容器补充最小 `admin-risk-mission-signals` 语义锚点、为高风险指标卡补充最小 `admin-risk-high-risk-metric` 锚点，并把 App / 页面层对 `规则命中概览`、`高风险` 等 proof 从 page-global `findByText/getByText` 收紧为 overview-card / metric-card / mission-flow / shared-console-bridge scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、风控→审计/API Keys/文档共享联动与真实 API 菜单真值不变，而不是继续依赖整页重复风险标签永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/AdminRiskPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_risk_scope_real_api.sh`（产物目录 `admin-risk-scope/20260506-060846`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/login`、`/admin/risk`、`/admin/audit`、`/webhooks`、`/api-keys`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员风控/审计接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；controller-side 复核确认当前 diff 仅为 AdminRisk 页面补充最小语义锚点，并把 App / AdminRisk focused proof 从低信号 page-global 风险文本命中收敛为更贴近运行时结构的 metric-card / overview-card / mission-signal / shared-console-bridge scoped 合同，不改变运行时权限、导航或真实 API 合同。期间已丢弃 1 组明显跨仓库漂移的外部评审结果，最终以控制器基于 live diff、build、测试、compose 与真实 API 回放的五维复核为准；下一前端优先项继续清理 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-06）
- 本轮继续按"前端优先"收敛 `WebhooksPage.test.tsx` 中管理员 metrics focused 用例仍残留的低信号 page-global guidance 文本断言：在不改运行时代码的前提下，仅为 `WebhooksPage.tsx` 顶部角色说明卡补充最小 `webhooks-role-guidance` 语义锚点，并把 `重点关注 failed / pending 重试链路与 last_error` 的 proof 从 page-global `screen.getByText(...)` 收紧为该 guidance 卡片内部的 scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、管理员 Webhook 运维观测页指标/角色提示与真实 API 菜单真值不变，而不是继续依赖整页同名说明文案永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/App.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_guidance_scope_real_api.sh`（产物目录 `webhooks-guidance-scope/20260506-053655`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；controller-side 复核与 repo-scoped 独立评审一致确认当前 diff 仅为 `WebhooksPage.tsx` 顶部角色说明卡补充最小 `webhooks-role-guidance` 语义锚点，并把 `WebhooksPage.test.tsx` 中管理员 guidance proof 从低信号 page-global 文本命中收敛为更贴近运行时结构的 role-guidance 卡片 scoped 合同，不改变运行时权限、导航或真实 API 合同。非阻塞债务继续仅包括既有 Semi UI `findDOMNode`、React `act(...)` 噪声与前端 chunk 体积告警；下一前端优先项继续清理 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。

### 最新执行检查点（2026-05-04）
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中 bootstrap failure focused 用例仍残留的低信号文本等待：在不改运行时代码的前提下，仅把 `clears local session on bootstrap failure without revoking refresh session server-side` 用例里对登录页回退的 proof 从 `waitFor + getByText('登录 Nexus-Mail')` 收紧为 `findByRole('heading', { name: '登录 Nexus-Mail' })`，继续保持 new-api 风格单一登录后深色共享控制台、登录壳 heading 语义与 bootstrap failure 时仅清本地会话而不触发服务端 refresh revoke 的真实合同不变，而不是继续把宽泛文本节点与额外轮询包装当作该 focused 用例的主真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_bootstrap_heading_real_api.sh`（产物目录 `app-bootstrap-heading-real-api/20260506-043720`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 bootstrap failure 登录页回退 proof 从宽泛 `getByText` + 额外 `waitFor` 轮询收敛为更贴近运行时可访问语义的 heading 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 复用与非语义文本节点导致的误报。继续保留既有 Semi UI `findDOMNode`、局部 React `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`LoginPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号文本等待 / page-global 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中仍残留的低信号 page-global proof：在不改真实权限、菜单、路由与 API 合同的前提下，仅为 `DashboardPage.tsx` 的角色菜单映射卡与管理员运营摘要卡补充最小 `dashboard-role-surface-map` / `dashboard-admin-ops-summary-card` 语义锚点、为 `ProfilePage.tsx` 的左侧身份主卡补充 `profile-primary-identity-card` 锚点，并把 App 层 supplier / admin focused 用例从 page-global 文本与未分区按钮查询收紧为更贴近运行时结构的 scoped proof，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、供应商资料页→域名管理与管理员总览统计真值不变，而不是继续依赖整页同名 copy 或无作用域按钮永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_focused_scope_real_api.sh`（产物目录 `app-focused-scope/20260506-040529`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/profile`、`/supplier/domains`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、供应商资料页联动、管理员总览/风控/审计入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；代码质量评审曾指出新增 `profile-scene-card` 锚点未被实际测试消费，控制器已在提交前移除该冗余锚点并复跑 `src/App.test.tsx`、前端 build、`go test ./...` 与真实 API 回放确认全部通过。评审一致认为当前 diff 仅把 App 层 supplier profile / supplier dashboard / admin dashboard deep statistics proof 从低信号 page-global 查询收敛为更贴近运行时结构的 identity-card / route-map / ops-summary scoped 合同，不改变运行时权限、导航或真实 API 合同，同时继续保留既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx` 中管理员风控页仍残留的 `规则命中概览` / `处置建议` / `高风险` / `API Key 白名单拦截频繁` 等低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中 supplier dashboard focused 用例仍残留的低信号 page-global heading 断言：在不改运行时代码的前提下，仅把该用例对 `供应商主任务` 的可达性 proof 从宽泛 `findByText(...)` 收紧为 `heading` 语义合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、供应商首页 → 域名管理单壳路径与真实 API 菜单真值不变，而不是继续把任意同名文本节点当作 supplier dashboard focused 的主真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_heading_scope_real_api.sh`（产物目录 `app-heading-scope/20260505-023331`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/docs`、`/webhooks`、`/api-keys`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；其中 controller-side 复核已纠正一份外部 reviewer 对 focused Vitest 结果的误读，当前以控制器实测为准确认 `src/App.test.tsx`、`src/pages/ApiDocsPage.test.tsx`、`src/pages/WebhooksPage.test.tsx` 共 41 项用例全部通过。评审一致认为当前 diff 仅把 supplier dashboard focused proof 从 page-global 文本命中收敛为更贴近运行时结构的 heading 合同，不改变运行时权限、导航或真实 API 合同，同时继续保留既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx` 中 admin metrics 仍残留的 page-global 指标/说明文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `WebhooksPage.test.tsx` 中 admin metrics focused 用例仍残留的低信号 page-global 指标/说明断言：在保持单一登录后 new-api 风格深色共享控制台、角色菜单 gating 与 Webhook 管理员运维工作台真实合同不变的前提下，仅为三个指标卡片补充最小 `webhooks-endpoint-metric` / `webhooks-sent-delivery-metric` / `webhooks-attention-delivery-metric` 语义锚点，并将测试从 page-global `screen.getByText(...)` 收紧为指标卡片内部的 scoped metrics proof，避免未来页面其他区块复用相同指标/说明文案时产生误报，而不是继续依赖整页同名文本永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_role_heading_scope_real_api.sh`（产物目录 `webhooks-role-heading-scope/20260506-031547`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 metrics scoped 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；控制器已按当前 repo live diff 直接完成五维复核，并丢弃了三份错误引用其他仓库/其他变更内容的外部 delegate 结果，避免 cross-repo review drift 污染本仓库判断。评审一致认为当前 diff 仅为 Webhooks 管理员指标卡片补充最小语义锚点，并把 focused proof 从 page-global 文本命中收敛为更贴近运行时结构的 metric-card scoped 合同，不改变运行时权限、导航或真实 API 合同，同时继续保留既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例中仍残留的低信号 page-global 指标/说明文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中管理员 Webhook 首屏用例仍残留的低信号 page-global heading / metric 文本断言：在不改真实权限、菜单、路由与 API 合同的前提下，仅把 `renders webhook settings page for authenticated admin` 从宽泛 `findByText/getByText` 收紧为 `heading` 语义合同、`webhooks-endpoint-metric` / `webhooks-attention-delivery-metric` 指标卡 scoped proof，并为运行时 `当前 endpoint` 卡片补充最小 `webhooks-current-endpoints-card` 语义锚点，使 App 层回归校验不再依赖整页同名指标/标题永远唯一这一过宽假设，同时继续保持 new-api 风格单一登录后深色共享控制台与角色菜单 gating 真值不变。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web exec vitest run src/App.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_role_heading_scope_real_api.sh`（本轮产物目录 `webhooks-role-heading-scope/20260506-033815`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；其中 controller-side 复核已否决一份把旧 diff 状态当成当前 live 状态的测试/可靠性外部评审，最终以控制器实测为准确认 `src/App.test.tsx`、`src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway` 与真实 API 回放全部通过。评审一致认为当前 diff 仅把 App 层管理员 Webhook 首屏 proof 从低信号 page-global 文本命中收敛为更贴近运行时结构的 heading / metric-card / current-endpoint-card scoped 合同，不改变运行时权限、导航或真实 API 合同，同时继续保留既有 React `act(...)`、Semi UI `findDOMNode` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx` 其余 shared-console focused 用例中仍残留的低信号 page-global heading / metric 文本断言，再回到 Phase 4/5 其余后端能力。

- 本轮继续按"前端优先"收敛 Docs ↔ Webhooks shared-console focused 合同中仍残留的低信号 page-global / 长文案断言：在不改运行时代码的前提下，仅把 `WebhooksPage.test.tsx` 的首轮 integration loop proof 从 page-level 文案快照收紧为 `webhooks-first-integration-loop` 区域内 CTA + 无 fallback 合同，把 `ApiDocsPage.test.tsx` 的 docs mission-control proof 从 page-global `OpenAPI 3 / Redoc` / `API Keys` / `Webhook 设置` 文本等待收紧为 iframe surface + `docs-shared-console-bridge` / `docs-shared-console-loop` 区域内 CTA 合同，并把 `App.test.tsx` 中 supplier 无首选落点时回退到 docs 的证明从宽泛 `API 文档` 文本命中收紧为 `nexus-mail-api-docs` iframe 真正落点；继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与 Docs → API Keys / Webhooks 单壳桥接不变，而不是继续依赖易重复页面文本当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/pages/ApiDocsPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_docs_webhooks_loop_contract_real_api.sh`（产物目录 `docs-webhooks-loop-contract/20260505-015731`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints` 与前端 `/`、`/docs`、`/webhooks`、`/api-keys` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、接入链路或单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Docs ↔ Webhooks shared-console focused proof 从低信号 copy 命中收敛为更贴近运行时结构/行为的 iframe、CTA presence/absence 与 docs 落点合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案润色与重复文本导致的误报。继续保留既有 Semi UI `findDOMNode`、局部 React `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx` 中 admin metrics 仍残留的 page-global 指标/说明文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ApiDocsPage.test.tsx` 中 docs bridge / loop focused 用例仍残留的低信号全局 copy 断言：在不改运行时代码的前提下，仅把 bridge / loop 顶层叙事 copy 的 page-global `screen.getByText(...)` 等待收紧为 `docs-shared-console-bridge` / `docs-shared-console-loop` 语义区域内的 scoped 文本 + CTA 合同，同时保留 `回到项目市场校验真实业务输入`、`收敛最小权限 API Key`、`完成 Webhook 回调联调`、`返回资金工作台核对预算与售后`、`先回到 API Keys 收口最小权限`、`随后校验 Webhook delivery`、`最后回到业务主链路复放` 等关键用户可见桥接/闭环标题，继续保持 new-api 风格单一登录后深色共享控制台、Docs→API Keys→Webhook→业务回放单壳连续路径与真实菜单 gating 不变，而不是继续依赖页面其他区域可能复用的同名文本节点。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_docs_webhooks_signal_scope_real_api.sh`（最新产物目录 `api-docs-webhooks-signal-scope/20260505-005827`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/docs`、`/api-keys`、`/webhooks` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一度指出若完全移除 bridge/loop 卡片标题断言会削弱 Docs 工作台的用户可见桥接语义保护，控制器已改为保留关键 lane 内标题并把断言收紧到 `docs-shared-console-bridge` / `docs-shared-console-loop` 区域内后复验通过。继续保留既有 Semi UI `findDOMNode`、局部 React `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`App.test.tsx` 等 shared-console focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SettingsPage.test.tsx` 中普通用户首轮引导 focused 用例仍残留的低信号首屏文本等待：在不改运行时代码的前提下，仅把 `首次使用清单` 首屏 proof 从普通 `findByText(...)` 文本等待收紧为 `heading` 语义合同，同时保留 checklist 容器、重新打开首轮引导、共享首页回流与 supplier CTA 不泄漏合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与角色菜单 gating 不变，而不是继续把任意同名文本节点当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/pages/SettingsPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_docs_settings_heading_scope_real_api.sh`（产物目录 `app-docs-settings-heading-scope/20260504-234140`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/docs`、`/api-keys`、`/webhooks`、`/settings`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前实际 diff 仅把 `SettingsPage.test.tsx` 中普通用户首轮 checklist 的首屏等待从宽泛文本查询收紧为更贴近运行时结构的 heading 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案复用与非语义文本节点导致的误报。继续保留既有 Semi UI `findDOMNode`、局部 React `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`ApiDocsPage.test.tsx`、`App.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SupplierOfferingsPage.test.tsx` 中仍残留的装饰性 Mission Control 标签等待：在不改运行时代码的前提下，仅把 supplier 供货页 focused 用例从 `Supplier Mission Control` 装饰标签等待收紧为 `供货规则编排中枢` heading 语义合同，并将首屏任务流存在性从普通文本节点升级为 `供应商主任务流` heading 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、供货规则→资源→结算→API Keys 单壳连续路径与真实 API 菜单真值不变，而不是继续把英文装饰标签当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_supplier_offerings_heading_scope_real_api.sh`（产物目录 `supplier-offerings-heading-scope/20260504-201756`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/supplier/offerings`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 SupplierOfferings focused 用例从装饰性 Mission Control 标签等待收敛为更贴近运行时结构的 heading 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 视觉标签/文案微调导致的误报。继续保留既有 Semi UI `findDOMNode`、`async-validator` 局部噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`App.test.tsx`、`LoginPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ProjectsPage.test.tsx`、`OrdersPage.test.tsx` 与 `AdminUsersPage.shared-console.test.tsx` 中仍残留的装饰性 tag / 低信号长说明等待：在不改运行时代码的前提下，仅把采购页与订单页 focused 用例从 `共享控制台采购切片` / `共享控制台履约切片` 等装饰性标签等待收紧为 `项目市场` / `订单中心` heading 语义合同，并把管理员资金页首屏 proof 从英文装饰标签 `Admin Finance Mission Control` 收紧为 `用户管理` heading，同时保留 `admin-users-shared-console-bridge` 区域内共享联动长说明与 API Keys / Webhook / Docs 桥接标签真值，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、管理员资金 mission-flow 与采购/履约→接入单壳连续路径不变，而不是继续把装饰性标签或 page-global 长说明当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx src/pages/AdminUsersPage.shared-console.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_test_heading_scope_real_api.sh`（最新产物目录 `front-test-heading-scope` 下本轮时间戳目录），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs`、`/admin/users` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Projects / Orders / AdminUsers 三个 focused 用例从装饰性标签与过宽长说明等待收敛为更贴近运行时结构的 heading / bridge 区域合同，同时保留管理员资金页共享联动文案与桥接标签真值，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、局部 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`App.test.tsx`、`LoginPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ApiDocsPage.test.tsx` 中仍残留的低信号首屏/区域文案等待：在不改运行时代码的前提下，仅把管理员 docs 角色校验从整段 admin 说明 copy 收紧为稳定 `Docs Mission Control` heading 合同，并把 bridge/loop 多轮重渲染用例的 readiness proof 分别收紧为 `docs-shared-console-bridge` / `docs-shared-console-loop` 语义区域存在性，而不再反复等待 `共享控制台桥接路径`、`文档与接入工作台继续保持单壳闭环` 这类易随 copy 润色变化的文本；继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Docs → API Keys / Webhooks / Balance / 推荐工作台 单壳桥接与回流合同不变，而不是继续把装饰性区域标题或长说明 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_docs_front_scope_real_api.sh`（最新产物目录 `api-docs-front-scope/20260504-195856`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/docs`、`/api-keys`、`/webhooks`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 ApiDocs focused 用例从易变 admin 说明 copy 与 bridge/loop 区域文本等待收敛为更贴近运行时结构的 heading / 区域级 readiness 合同，同时保留 bridge/loop CTA 与导航真值，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`LoginPage.test.tsx`、`SupplierResourcesPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `AdminAuditPage.test.tsx` 首个 shared-console focused 用例中仍残留的低信号 page-global 长说明 / 标题断言：在不改运行时代码的前提下，仅把审计工作台首屏 proof 从 `Audit Mission Control` 装饰标签、整段 `/api/v1/admin/audit` 说明文案、若干 page-global 指标/区块标题文本，收紧为 `heading` 语义合同、`admin-audit-mission-flow` 区域内三条真实动作按钮、`admin-audit-shared-console-bridge` 区域内 API Keys / 风控中心 / API 文档 三个共享桥接标签，以及 `admin-audit-events-table-card` 内真实审计事件合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、审计→风控/资金/API Keys 单壳联动与真实 API 菜单真值不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminAuditPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_audit_heading_scope_real_api.sh`（产物目录 `admin-audit-heading-scope/20260504-162607`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 AdminAudit focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED（仅建议后续若继续强调 section heading 可在单独切片补更精确标题断言）、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 AdminAudit 首个 focused 用例从 page-global 长说明 copy 收敛为更贴近运行时结构的 heading / mission-flow / bridge / table 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案润色与重复文本导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `SupplierSettlementsPage.test.tsx`、`ProjectsPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ProjectsPage.test.tsx` 与 `OrdersPage.test.tsx` 首个 shared-console hero focused 用例中仍残留的低信号 page-global 长说明断言：在不改运行时代码的前提下，仅把采购 hero / 履约 hero 的首屏 proof 从整页长段落 copy 收紧为 `projects-shared-console-hero` / `orders-shared-console-hero` 语义区域内的 heading + 稳定 hero signal 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、采购→履约→API 接入单壳连续路径与真实 API 返回信号不变，而不是继续把高频润色说明 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_projects_orders_hero_scope_real_api.sh`（产物目录 `projects-orders-hero-scope/20260504-160115`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、`GET /api/v1/projects/inventory`、`GET /api/v1/orders/activations`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 hero focused 收敛未破坏真实注册、共享菜单、采购/履约接口、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Projects/Orders 首个 hero 用例从 page-global 长说明 copy 收敛为更贴近运行时结构的 hero 区域 heading / signal 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 微调与页面其他区块复用同名文案导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `AdminRiskPage.test.tsx`、`AdminAuditPage.test.tsx`、`SettingsPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例中仍残留的长说明 / page-global 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ApiDocsPage.test.tsx` 首个 focused 用例中仍残留的低信号 page-global 文案断言：在不改运行时代码的前提下，仅把普通用户 docs mission-control 校验从 `共享控制台 · API 契约`、`注册后连续路径`、长说明 copy 与多条 page-global surface 文案，收紧为 `heading` 语义合同、OpenAPI / API Keys / Webhook 三个稳定 surface 标签、`docs-shared-console-bridge` / `docs-shared-console-loop` 两个真实区域内 CTA scoped 合同与 API Keys 导航落点，同时将测试标题改为更准确地描述 bridge + loop CTA proof，继续保持 new-api 风格单一登录后深色共享控制台、文档→接入→业务回放单壳闭环与真实菜单 gating 合同不变，而不是继续把易变长文案当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx`、`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/pages/LoginPage.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_docs_front_scope_real_api.sh`（产物目录 `api-docs-front-scope/20260504-153557`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/docs`、`/api-keys`、`/webhooks` SPA 壳 200 均保持成立，本轮 docs focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；安全/集成评审一度指出若完全删掉 docs surface 合同，可能削弱对共享控制台文档入口与菜单 gating 的防回归灵敏度，控制器已补回 `OpenAPI 3 / Redoc`、`API Keys`、`Webhook 设置` 三个稳定 surface 标签断言，并把首个测试标题收敛为更准确的 bridge + loop CTA proof 后复验通过。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`LoginPage.test.tsx` 等 shared-console focused 用例中仍残留的低信号长说明 / page-global 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ApiKeysPage.test.tsx` 与 `SupplierSettlementsPage.test.tsx` 中仍残留的低信号 page-global / 结构耦合断言：为 `ApiKeysPage` 运行时代码最小补充 `api-keys-hero-card` 与 `api-keys-shared-console-bridge` 语义锚点，并将 supplier 角色接入提示、共享接入桥接 CTA 合同从 `.closest('.semi-card')` / page-global 查询收紧为对应卡片 scoped 断言；同时把 `SupplierSettlementsPage` 的 mission-control 壳层校验从英文装饰标签与 page-global step 文案，收敛为中文主 heading + `supplier-settlements-mission-flow` 区域内步骤合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、API Keys/Webhook/Docs 共享桥接与 supplier 资金闭环不变，而不是继续依赖第三方卡片包装类名或整页同名文案永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_keys_supplier_settlements_focus_real_api.sh`（产物目录 `api-keys-supplier-settlements-focus/20260504-145949`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers`、`/supplier/settlements` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商财务入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；首轮代码质量评审指出 `ApiKeysPage.test.tsx` 新增的 `.closest('.semi-card')` 会把 scoped 断言重新耦合到 Semi UI 包装类名，控制器已在运行时代码补充最小 testid 并改为语义区域 scoped 查询后复验通过。其余评审一致认为当前 diff 仅为 API Keys hero/bridge 卡片补充最小语义锚点，并把 supplier finance / API Keys focused proof 收紧到更贴近运行时结构的 heading / bridge / mission 区域，不改变权限、导航或真实 API 合同；既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警继续记为非阻塞债务。下一前端优先项转为继续清理 `LoginPage.test.tsx`、`WebhooksPage.test.tsx`、`ApiDocsPage.test.tsx` 等 shared-console focused 用例中仍残留的长说明 / page-global copy 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ProfilePage.test.tsx` 中 profile shared-console return/fallback focused 用例仍残留的低信号长说明 copy 断言：在不改运行时代码的前提下，仅把 supplier/admin 返回卡片场景从 `供应商角色扩展` / `管理员角色扩展` 与整段说明文案断言，收紧为角色焦点 heading + 返回 CTA + 导航落点 + user-only heading 不泄漏合同；同时把 user/supplier fallback 返回卡片从长说明段落断言收紧为卡片内标题文本 + 返回按钮合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与 profile→settings / profile→shared-home 回流语义不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_profile_heading_scope_real_api.sh`（产物目录 `profile-heading-scope/20260504-143042`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 Profile focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Profile 返回/回流 focused proof 从 page-global 长说明 copy 收敛为更贴近运行时结构的 heading / CTA / 导航落点合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 微调导致的误报。非阻塞建议仅包括：若后续仍想覆盖返回卡片语义，可考虑补更短的区域内标签/标题断言而非整段说明文案；既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警继续记为债务。下一前端优先项转为继续清理 `SupplierSettlementsPage.test.tsx`、`ApiKeysPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明/页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中普通用户首轮引导 heading 断言与 dismiss 合同：在不改运行时代码的前提下，仅把两处 `欢迎进入共享控制台` page-global 文本查询改为复用 `expectDefaultUserFirstRunLane()` 的 lane scoped 合同，并将 dismiss 校验收紧为 `localStorage` 已写入 + heading 语义消失且 shared next-steps lane 仍保留，从而继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与真实注册 / 首轮引导回流合同不变，而不是继续把易变文案节点本身当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_first_run_lane_heading_contract_real_api.sh`（产物目录 `app-first-run-lane-heading-contract/20260504-130754`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮首轮引导 heading focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把普通用户首轮引导 proof 从脆弱的 page-global 文本查询收敛为更贴近运行时结构/状态变化的 heading + lane 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 copy 微调导致的 focused 误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ProfilePage.test.tsx`、`SupplierSettlementsPage.test.tsx` 等 shared-console focused 用例里仍残留的长说明 copy / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中普通用户首轮引导仍残留的低信号长文案断言：在不改运行时代码的前提下，仅把 `shows first-run onboarding guidance for default user dashboard and allows dismissing it` 用例从 `欢迎进入共享控制台`、角色说明句与两条旅程 helper copy 的 page-global 文本断言，收紧为 `expectDefaultUserFirstRunLane()` 区域内余额/项目/订单/API Keys 四个真实 CTA 存在、API Keys 集成卡片 heading 合同与 `打开 Webhook 设置` 不泄漏的 scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与预算→采购→履约→接入 单壳路径不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_first_run_lane_scope_real_api.sh`（产物目录 `app-first-run-lane-scope/20260504-122259`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮首轮引导 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把普通用户首轮引导 proof 从易变长文案收敛为更贴近运行时结构/行为的 CTA 与集成卡片合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 copy 微调导致的 focused 误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ProfilePage.test.tsx`、`SettingsPage.test.tsx` 等 shared-console focused 用例里仍残留的长说明 copy / page-level 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 与 `LoginPage.test.tsx` 中登录/注册共享壳仍残留的低信号长文案断言：在不改运行时代码的前提下，仅把 register journey proof 从供应商说明段落、权限说明长句与注册 helper 长文案，收紧为 `login-register-journey` 区域内的 heading/button 结构合同与切换到注册态后的 submit CTA 合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色路径说明与真实注册链路不变，而不是继续把易变营销/说明 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_login_register_shell_scope_real_api.sh`（产物目录 `login-register-shell-scope/20260504-113622`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu` 与前端 `/`、`/login`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮登录/注册共享壳 focused 收敛未破坏真实注册、共享菜单、管理员菜单与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把登录/注册共享壳 proof 从易变长文案收敛为更贴近运行时结构的 heading/button 合同，不改变权限、导航或真实 API 契约，同时进一步降低 copy 微调导致的 focused 误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`DashboardPage.test.tsx` 等 shared-console focused 用例中仍残留的长文案 / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ProjectsPage.test.tsx`、`OrdersPage.test.tsx` 与 `SupplierDomainsPage.test.tsx` 中仍残留的低信号隐藏态 / 失败态文案断言：在运行时代码仅为 SupplierDomains 区域统计卡片补充最小 `supplier-domains-region-metrics` 语义锚点的前提下，将采购 hero 与履约 hero 的 hidden-state proof 分别从长说明文案缺失收紧为对应 hero 区域内 `查看 API 文档` / `打开 API Keys` 按钮级缺失断言，并把 SupplierDomains overview 失败态从 page-global 区域统计文案检查收紧到 table-card + region-metrics 两个真实区域 scoped 合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、采购→履约→接入连续路径与供应商域名运营页错误态合同不变，而不是继续把易变 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx src/pages/SupplierDomainsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_hero_copy_scope_real_api.sh`（产物目录 `front-hero-copy-scope/20260504-111434`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/projects`、`/orders`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商域名运营入口与单壳前端连续路径。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Projects/Orders 两个 hero 的隐藏态 proof 收敛为更贴近真实交互的按钮级合同，并为 SupplierDomains 区域统计卡片补充最小语义锚点后把 overview 失败态断言限制到真实区域，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 细调与 page-global 重复文案导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `AdminSuppliersPage.test.tsx`、`AdminUsersPage.shared-console.test.tsx`、`AdminRiskPage.test.tsx`、`AdminAuditPage.test.tsx` 等 bridge tag/route 文本缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `DashboardPage.test.tsx` 中共享控制台 next-steps lane 仍残留的低信号长说明文案断言：在不改运行时代码的前提下，仅删除对 `采购 → 订单 → 接入` 推荐顺序说明与 `继续进入 API Keys、Webhook 与文档` 集成长句的逐字断言，保留 `dashboard-next-steps-lane` 区域内四步 heading/按钮存在性、隐藏态与真实导航合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与共享旅程 lane 行为不变，而不是继续把容易随文案润色变化的说明句当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_dashboard_lane_scope_real_api.sh`（产物目录 `dashboard-lane-scope/20260504-094629`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 Dashboard lane focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅删除两条容易随 copy 微调波动的长说明断言，保留共享旅程 lane 的 step/CTA/导航主合同，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `OrdersPage.test.tsx`、`ProjectsPage.test.tsx` 等 shared-console focused 用例中仍残留的长说明/通用文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `LoginPage.test.tsx` 中注册壳 shared journey 的低信号长文案断言：在不改运行时代码的前提下，仅保留 `login-register-journey` 区域内的 heading 语义合同（`注册后默认进入共享控制台`、`用户路径`、`供应商路径`、`管理员路径`），删除对营销式接入说明与中英混排 runway copy 的逐句快照式断言，并继续保留 CTA 切换到注册态、`注册 Nexus-Mail` 标题与注册 helper 文案，从而保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与角色路径说明合同不变，同时降低未来登录页 copy 微调带来的 focused 误报。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`（产物目录 `app-login-register-scope/20260504-092530`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮登录壳 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把注册壳 journey proof 从长文案快照收敛为更贴近运行时结构的 heading 语义合同，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `DashboardPage.test.tsx`、`OrdersPage.test.tsx` 等 shared-console focused 用例中仍残留的低信号长说明文案断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中登录/注册共享壳仍残留的 page-global 长文案断言：在不改运行时代码的前提下，仅复用既有 `login-register-journey` 语义锚点，把 `shows register journey guidance and role entry descriptions on the login shell` 用例从整页 `getByText(...)` 查询收紧为区域内 heading/button/text scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、注册入口与角色路径说明合同不变，而不是继续依赖整页同名文案永远唯一这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`（产物目录 `app-login-register-scope/20260504-090240`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮登录/注册共享壳 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；产品评审仅提出一项非阻塞建议：若未来把"用户路径"辅助说明文案也视为关键 UX，可再补一条更精确的区域内文案断言；其余评审一致认为当前 diff 仅把登录壳 journey proof 收紧到现有语义区域，不改变运行时权限、导航或真实 API 合同，同时进一步降低未来登录页重复 copy 导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`LoginPage.test.tsx`、`DashboardPage.test.tsx` 等 shared-console focused 用例中仍残留的低信号长文案 / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ProfilePage.test.tsx` 与 `SettingsPage.test.tsx` 中仍残留的 DOM 包装依赖：在不改运行时代码的前提下，仅去掉 supplier/admin 资料页对 `供应商运营焦点` / `管理员运营焦点` 标题 `parentElement` 的结构遍历，改为直接用 user-only heading `采购与订单串联` 的缺失来证明角色焦点未串台；同时把设置页"重新打开首轮引导"后的共享首页验证从 `parentElement` 容器遍历收敛为页面级 supplier 快捷按钮缺失 + checklist 消失合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与角色菜单 gating 合同不变，只降低 focused 用例对测试 stub 包装节点的依赖。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/SettingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_profile_settings_scope_real_api.sh`（产物目录 `profile-settings-scope/20260504-075341`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 去结构耦合未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；代码质量评审仅提示 `ProfilePage.test.tsx` 中 `采购与订单串联` 的页面级 heading 缺失断言未来若出现重复 heading 可再收紧到更小语义区域，但当前 diff 已去除 `parentElement` 脆弱遍历且未削弱角色/菜单真值保护；继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务。下一前端优先项转为继续清理其余 shared-console focused 用例中仍残留的 `parentElement` / 低信号 page-global 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `AdminSuppliersPage.test.tsx` 中 shared-console bridge 标题断言的结构耦合：在不改运行时代码的前提下，仅把 `共享接入桥接` 的 focused 校验从依赖 Semi UI `.closest('.semi-card')` 包装层，调整为 heading 语义断言 + `admin-suppliers-shared-console-bridge` 区域内的 shared link scoped 查询，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、管理员供应商运营 mission-flow 与 shared bridge 合同不变，而不是继续把第三方 UI 包装 DOM 形状当作测试真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminSuppliersPage.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_suppliers_bridge_scope_real_api.sh`（产物目录 `admin-suppliers-bridge-scope/20260504-063825`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、`GET /api/v1/admin/wallet-users`、`GET /api/v1/admin/disputes` 与前端 `/`、`/admin/suppliers`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 bridge 标题断言去结构耦合未破坏注册后共享菜单、真实 API、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一度指出把 bridge 标题断言降级为 page-global 文本查询会削弱局部合同，控制器已收敛为 heading 语义断言并保留 `admin-suppliers-shared-console-bridge` 区域内的 shared link scoped 查询后复验通过。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `LoginPage.test.tsx`、`App.test.tsx` 等其余 shared-console focused 用例中仍残留的 `.closest('.semi-card')` / 低信号 page-global copy 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `LoginPage.test.tsx` 与 `SettingsPage.test.tsx` 中仍残留的结构/整页级脆弱断言：为登录页注册后共享控制台旅程卡片补充最小 `login-register-journey` 语义锚点，使 focused 用例不再依赖 `.closest('.semi-card')`；同时为设置页快捷入口卡片补充 `settings-shortcut-cards` 语义锚点，并将普通用户重开首轮引导后对供应商快捷动作的缺失判断收紧到跳转落点容器而非整页全局扫描，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与角色菜单 gating 合同不变，只降低第三方 UI 包装结构与整页同名按钮造成的 focused 误报风险。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/pages/SettingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_login_settings_front_scope_real_api.sh`（产物目录 `login-settings-front-scope/20260504-065856`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 去脆弱化未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审仅提出两项非阻塞建议：`settings-shortcut-cards` 语义锚点后续可在对应 focused 用例中直接利用、设置页跳转落点容器未来可改为更稳定的 route stub 锚点；其余评审一致认为当前 diff 仅补充最小语义锚点并收紧 focused 断言作用域，不改变运行时权限、导航或真实 API 合同，同时继续把既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警记为非阻塞债务。下一前端优先项转为继续清理 `App.test.tsx` 等其余 shared-console focused 用例里仍残留的 page-global copy / CTA 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”补上 shared-console 导航契约层的 focused 回归：新增 `web/src/utils/consoleNavigation.test.tsx`，把 `/api-keys`、`/webhooks`、`/docs`、`/profile` 等共享壳关键路由常量、shared/supplier/admin 分组、角色 landing 候选、preferred landing 解析、quick actions 顺序与 route key/path 唯一性统一钉死为单一测试入口，避免 shared shell / App / mission-control 页面仅靠各自页面测试而让导航常量或菜单契约静默漂移；该切片不改运行时 UI/交互，只补上 new-api 风格单壳共享控制台最底层的路由/菜单合同保护。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/utils/consoleNavigation.test.tsx src/App.test.tsx src/pages/LoginPage.test.tsx src/pages/DashboardPage.test.tsx src/pages/AdminAuditPage.test.tsx src/pages/AdminSuppliersPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_console_navigation_contract_real_api.sh`（产物目录 `console-navigation-contract/20260504-052833`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮契约测试补强未破坏注册后共享菜单、真实 API、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 staged diff 仅新增导航契约测试文件，能把 shared-console 底层路由/菜单真值从分散页面用例中抽离为单点保护，不改变运行时权限、导航或真实 API 合同；非阻塞债务仍包括既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警。下一前端优先项转为继续清理 `DashboardPage.test.tsx`、`AdminSuppliersPage.test.tsx`、`LoginPage.test.tsx` 等 focused 用例里仍残留的低信号 route/copy 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `ProfilePage.test.tsx`、`SupplierDomainsPage.test.tsx` 与 `ApiDocsPage.test.tsx` 中仍残留的低信号 page-global fallback / copy 断言：在不改运行时逻辑的前提下，仅删除 `ProfilePage` 与 `SupplierDomainsPage` 中对 fallback 容器已缺失时重复的 page-level `返回共享工作台` / `返回推荐工作台` 按钮负断言，并将 `ApiDocsPage` 管理员文档桥接断言收紧为 `docs-shared-console-bridge` 区域内的按钮级 scoped 查询，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、注册后统一壳与 docs→integration bridge 合同不变，而不是继续依赖整页同名按钮/文案永远不存在这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/ApiDocsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_scope_docs_profile_domains_real_api.sh`（产物目录 `front-scope-docs-profile-domains/20260504-024841`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/profile`、`/supplier/domains`、`/docs` SPA 壳 200 均保持成立，本轮 focused 去脆弱化未破坏注册后共享菜单、真实 API、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅删除在 fallback 容器缺失时冗余的页面级按钮负断言，并把管理员文档桥接断言收紧到真实桥接区域，不改变运行时权限、导航或真实 API 合同，同时进一步降低未来重复文案与多区域同名 CTA 导致的误报。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`AppSidebar.test.tsx`、`ConsoleLayout.test.tsx` 等 shared shell / route guard 用例里仍残留的 page-global copy / menu 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `AppSidebar.test.tsx`、`ConsoleLayout.test.tsx` 与 `App.test.tsx` 中 shared shell / route guard 仍残留的低信号 page-global / menu-global 缺失断言：在不改运行时代码的前提下，仅将 `AppSidebar` 供应商菜单与加载态场景里的 `风控中心` / `API 文档` 缺失判断收紧到真实分组卡片与加载卡片，将 `ConsoleLayout` 当前页快捷操作排除校验保持在壳层按钮合同内，并将 `App.test.tsx` 中等待服务端菜单、注册后普通用户壳层、supplier 设置页 checklist 等缺失断言收紧到 `main[aria-label="控制台主内容"]` 或实际导航菜单区域，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、注册后统一壳与真实共享路由合同不变，而不是继续依赖整页/整壳永远不存在同名菜单或 checklist 文案这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_shell_scope_real_api.sh`（产物目录 `app-shell-scope-hardening/20260504-032716`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 shared shell 作用域收敛未破坏注册后共享菜单、真实 API、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；首轮代码质量评审曾建议不要把 `ConsoleLayout` 快捷操作断言降级为纯文本数组扫描，控制器已恢复为真实按钮级 role 合同；其余评审一致认为当前 diff 仅把 shared shell 缺失断言收敛到更贴近运行时语义的主内容区、菜单区与加载/分组卡片，不改变运行时权限、导航或真实 API 合同，同时进一步降低未来重复文案与共享壳多区域同名菜单导致的误报。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ProtectedRoute.test.tsx`、`DashboardPage.test.tsx`、`SettingsPage.test.tsx` 等 shared shell / role guard 用例里仍残留的 page-global copy / route guard 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `ProjectsPage.test.tsx`、`OrdersPage.test.tsx`、`AdminProjectsPage.test.tsx` 与 `SupplierDomainsPage.test.tsx` 中仍残留的 hero / mission / fallback 结构耦合：为采购 hero 与履约 hero 运行时代码最小补充 `projects-shared-console-hero`、`orders-shared-console-hero` 语义锚点，并将对应 focused 用例从 `.closest('.semi-card')` 收紧为 hero 区域 scoped 断言；同时将管理员价格页“仅保留 fallback CTA”场景改为显式断言 `admin-pricing-mission-flow` 不存在，并继续在 `main[aria-label="控制台主内容"]` 范围内确认 `查看风控中心` / `查看审计日志` / `打开 API Keys` 不泄漏；供应商域名页则保留 mission/shared fallback testid 缺失与 page-level `返回推荐工作台` 按钮缺失断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与单壳导航合同不变，只降低 Semi UI 包装结构漂移造成的 focused 误报风险。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx src/pages/AdminProjectsPage.test.tsx src/pages/SupplierDomainsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_hero_scope_tightening_real_api.sh`（产物目录 `front-hero-scope-tightening/20260504-022144`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/projects/inventory`、`GET /api/v1/orders/activations`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、`GET /api/v1/admin/wallet-users`、`GET /api/v1/admin/disputes` 与前端 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/docs`、`/admin/users`、`/admin/pricing`、`/supplier/domains` SPA 壳 200 均保持成立，本轮 hero / mission / fallback 语义锚点收敛未破坏注册后共享菜单、真实采购/履约/管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、性能/运维 PASS；首轮评审曾阻断管理员价格页为测试方便而无条件渲染空任务流容器以及去掉供应商域名页 page-level fallback 按钮负断言，控制器已恢复运行时条件渲染、补回 `main` 作用域下的 CTA 缺失校验与域名页 page-level fallback 保护后复验通过。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)`、Semi Form `setValues()` 挂载时序与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`ProfilePage.test.tsx`、`SettingsPage.test.tsx` 等页中仍残留的 page-global copy / CTA 缺失断言与重复文本脆弱选择器，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `AdminUsersPage.shared-console.test.tsx` 中管理员资金工作台 shared bridge 的结构耦合：为运行时代码最小补充 `admin-users-shared-console-bridge` 语义锚点，并将 fallback 场景里对 `API Keys · /api-keys`、`Webhook 设置 · /webhooks`、`API 文档 · /docs` 的隐藏校验，从 `共享接入桥接` heading 的 `.closest('.semi-card')` 包装结构依赖收紧为 bridge 区域内的 scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、管理员资金 mission-flow 与 shared-console fallback 合同不变，只降低 Semi UI 包装结构变动导致的 focused 误报风险。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_users_bridge_scope_real_api.sh`（产物目录 `admin-users-bridge-scope/20260504-013238`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、`GET /api/v1/admin/wallet-users`、`GET /api/v1/admin/disputes` 与前端 `/`、`/admin/users`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 bridge 语义锚点收敛未破坏注册后共享菜单、管理员真实运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅为管理员资金页共享桥接卡片补充最小语义锚点，并把 focused 测试从 `.closest('.semi-card')` 收紧为更贴近运行时合同的 bridge 区域 scoped 断言，不改变权限、导航或真实 API 合同，同时进一步降低 future Semi UI 包装层调整带来的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `AdminProjectsPage.test.tsx`、`ProjectsPage.test.tsx`、`OrdersPage.test.tsx` 等页中仍残留的 `.closest('.semi-card')` hero / fallback 结构选择器，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `WebhooksPage.test.tsx` 中 shared-console empty fallback 的低信号 page-global CTA 缺失断言：保持单一登录后 new-api 风格深色共享控制台、角色菜单 gating 与 Webhook → API Keys / Docs 接入闭环合同不变，仅将 empty-state 场景下对 `先配置 API Keys` / `查看 API 文档` 的缺失判断从整页 `screen.queryByRole(...)` 收紧为 `webhooks-empty-state-actions` 语义区域内的 scoped role 断言，继续验证该 fallback 区域只保留 `返回推荐工作台` 动作，而不是继续依赖整页永远不存在同名 CTA 这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_fallback_scope_real_api.sh`（产物目录 `webhooks-fallback-scope/<timestamp>`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/webhooks`、`/api-keys`、`/docs` SPA 壳 200 均保持成立，本轮 focused 去脆弱化未破坏注册后共享菜单、真实 Webhook/API Keys 接口、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Webhooks empty fallback 的按钮缺失检查收敛到真实语义区域，避免 future bridge/loop 区域复用相同 CTA 文案时出现误报，也未改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ProfilePage.test.tsx`、`SettingsPage.test.tsx` 等 shared-console focused 用例里仍残留的 page-global copy / fallback 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `ApiDocsPage.test.tsx` 中 docs bridge / loop fallback 的低信号 copy 缺失断言：保持单一登录后 new-api 风格深色共享控制台、角色菜单 gating 与文档→接入→业务回放单壳合同不变，仅将“无 bridge 目标时回退推荐工作台”的 focused 用例从 `queryByText(...)` 文案缺失检查改为 `docs-shared-console-bridge` 与 `docs-shared-console-loop` 语义区域内的按钮级 role 断言，分别验证 bridge fallback 按钮存在、项目市场/API Keys/Webhook/余额中心按钮缺失，以及 loop 内只保留 `返回推荐工作台` 而不再泄露 API Keys / Webhook 动作。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_docs_bridge_fallback_real_api.sh`（产物目录 `api-docs-bridge-fallback/20260503-223130`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/docs`、`/api-keys`、`/webhooks` SPA 壳 200 均保持成立，本轮 docs fallback 作用域收敛未破坏注册后共享菜单、管理员真实运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 docs fallback 检查从桥接说明文案缺失收敛为更贴近运行时合同的按钮级/区域级断言，不改变运行时逻辑且进一步降低未来 copy 漂移带来的误报。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `AdminUsersPage.shared-console.test.tsx` 等页面中残留的长文案/低信号 shell copy 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `SupplierSettlementsPage.test.tsx` 中 supplier finance mission-control focused 回归的稳定性与生命周期合同：补充 `afterEach` 复位 auth store，避免多次重渲染/菜单覆写在后续用例间泄漏；同时将成本模型提交流程断言改为对当前真实初始化合同的验证——保留 `currency=CNY`、`status=active` 默认值并仅校验用户实际修改的项目键/成本字段，避免因对 Semi Form 默认字段的脆弱输入假设而把 focused 用例误判为前端回归，继续保持单一登录后 new-api 风格深色共享控制台、supplier/shared fallback 语义与真实供应商财务闭环不变。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API 回放脚本 `.hermes/local-output/run_supplier_settlements_milestone_real_api.sh`（产物目录 `supplier-settlements-milestone-20260503-225549/real-api`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/supplier/resources/overview`（预期 403）、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、`GET /api/v1/supplier/settlements`（200）均保持成立，本轮 focused 稳定性修复未破坏注册后共享菜单、真实资源权限边界、管理员运营接口与 supplier finance 单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审指出的唯一阻塞项是 `SupplierSettlementsPage.test.tsx` 对成本模型默认字段的脆弱输入假设与缺失的 store 清理，控制器已修正并复验通过。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`ApiKeysPage.test.tsx`、`SupplierDomainsPage.test.tsx` 等 focused 用例中残留的 page-global fallback/CTA 缺失断言与 act 噪声，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 shared-console bridge/fallback focused 回归中的低信号页面级负断言：仅调整 `SupplierOfferingsPage.test.tsx`、`SupplierDomainsPage.test.tsx` 与 `ApiDocsPage.test.tsx`，把“隐藏 bridge 入口/文案”的判断从页面级 `screen.queryByText/queryByRole` 收紧到既有 `supplier-offerings-shared-console-bridge`、`supplier-domains-mission-flow` / `supplier-domains-shared-console-bridge`、`docs-shared-console-bridge` 语义区域内，同时在 supplier domain / offerings 场景改为桥接按钮级 role 负断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与 bridge/fallback 分区合同不变，而不是继续依赖整页不存在同名桥接文案这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierDomainsPage.test.tsx src/pages/ApiDocsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_shared_scope_bridge_real_api.sh`（产物目录 `front-shared-scope-bridge/20260503-214909`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/offerings`、`/supplier/domains`、`/supplier/settlements` SPA 壳 200 均保持成立，本轮 bridge 作用域收紧未破坏注册后共享菜单、管理员真实运营接口与 supplier/shared 单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 bridge 缺失检查收敛到真实语义区域与按钮级合同，减少未来页面其他区块重复文案导致的误报，也未削弱权限/menu 真值约束。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)`、`async-validator` 局部噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `SupplierSettlementsPage.test.tsx` 中剩余的 shared/mission fallback 负断言与按钮归属细节，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `SupplierSettlementsPage.test.tsx` 中 shared / mission fallback 的按钮归属合同：在保持运行时结构不变的前提下，仅删除一条会误判共享 fallback 外部行动按钮的过宽页面级负断言，保留并复验 `supplier-settlements-mission-fallback` 内唯一可点击 `返回推荐工作台` 与 `supplier-settlements-shared-console-fallback` 卡片内仅保留同名说明文案、无同名按钮的 scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与 fallback 区域职责不变，而不是继续依赖页面级按钮缺失假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_supplier_settlements_fallback_scope_real_api.sh`（产物目录 `supplier-settlements-fallback-scope/20260503-220837`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/supplier/resources/overview`（预期 403）、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/supplier/settlements` SPA 壳 200 均保持成立，本轮 fallback 归属收敛未破坏注册后共享菜单、真实资源权限边界、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅删除一条与运行时结构不一致的过宽负断言，并保留 mission/shared fallback 各自的 scoped 合同校验，从而降低 future copy / 外部动作区调整导致的误报，也未削弱权限、导航或真实 API 边界验证。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)`、`async-validator` 局部噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例里仍残留的 page-global fallback 文案/按钮缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛管理员价格页 / 管理员资金页 / 供应商供货页 focused 回归中的 page-global CTA 缺失断言：为 `AdminProjectsPage` 与 `SupplierOfferingsPage` 补充最小 `admin-pricing-mission-flow` / `supplier-offerings-mission-flow` 语义锚点，将 `AdminProjectsPage.test.tsx`、`AdminUsersPage.shared-console.test.tsx`、`SupplierOfferingsPage.test.tsx` 的 mission CTA 缺失检查收紧到 mission-flow 作用域，并把管理员价格页多跳导航测试改为显式 `view.unmount()` + fresh render，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与 bridge/fallback 分区合同不变，而不是继续依赖页面级同名按钮缺失假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx src/pages/AdminUsersPage.shared-console.test.tsx src/pages/SupplierOfferingsPage.test.tsx src/pages/ProfilePage.test.tsx src/pages/DashboardPage.test.tsx src/pages/WebhooksPage.test.tsx src/pages/AdminAuditPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_mission_flow_scope_real_api.sh`（产物目录 `front-mission-flow-scope/20260503-210030`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 以及前端 `/`、`/api-keys`、`/webhooks`、`/settings`、`/admin/risk`、`/admin/audit`、`/admin/pricing`、`/supplier/offerings` SPA 壳 200 均保持成立，本轮 mission-flow 作用域收敛未破坏注册后共享菜单、管理员真实运营接口与供应商供货页单壳入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅新增最小 mission-flow 语义锚点并把缺失 CTA 检查收敛到正确区域，不改变运行时权限或导航语义。测试/可靠性仅记录一项非阻塞后续项：`SupplierOfferingsPage` 正向导航测试后续可继续收紧到 `supplier-offerings-mission-flow` 作用域，以进一步降低未来 bridge 区域同名 CTA 误匹配风险。
- 本轮继续按“前端优先”收敛 `App.test.tsx` 中注册后共享控制台与角色差异断言的低信号全局查询：将普通用户注册后首轮引导里对“供应商主任务 / 管理员主任务”的缺失判断收敛到 `dashboard-next-steps-lane` 作用域；将 supplier dashboard 的“普通用户首轮引导 / 结算页 / Webhook”缺失断言收敛到 `main[aria-label="控制台主内容"]` 作用域；同时为 supplier settings 场景补充 `settings-user-first-run-checklist` 缺失断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与真实 onboarding / settings 合同不变，而不是继续依赖 App 级全局缺失查询误伤无关区域。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API 回放脚本 `.hermes/local-output/run_app_scope_cleanup_real_api.sh`（产物目录 `app-scope-cleanup/20260503-203612`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/api-keys`、`/settings`、`/admin/risk`、`/admin/audit`、`/docs` SPA 壳 200 均保持成立，本轮 App 级断言收敛未破坏注册后共享菜单、管理员真实运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 `App.test.tsx` 中与注册后 onboarding / shared-console 角色差异相关的局部断言收敛到更合适的区域或补充更精确的缺失检查，不改变运行时权限、真实 API、导航覆盖或部署行为。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例中仍残留的 page-global fallback 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 shared-console focused 回归中残留的 page-global `返回推荐工作台` 负断言脆弱点：仅调整 `SupplierSettlementsPage.test.tsx`、`SupplierResourcesPage.test.tsx`、`BalancePage.test.tsx`、`AdminRiskPage.test.tsx`、`AdminAuditPage.test.tsx`，移除在 fallback 区域已通过 `data-testid` / `within(...)` 明确约束时重复的页面级按钮缺失断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与 mission/shared fallback 分区合同不变，而不是继续依赖全页同名 CTA 永远不存在这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/BalancePage.test.tsx src/pages/AdminRiskPage.test.tsx src/pages/AdminAuditPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_fallback_scope_real_api.sh`（产物目录 `front-fallback-scope/20260503-212158`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/api-keys`、`/webhooks`、`/settings`、`/admin/risk`、`/admin/audit`、`/supplier/resources`、`/supplier/settlements` SPA 壳 200 均保持成立，本轮 focused 去脆弱化未破坏注册后共享菜单、管理员真实运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅删除 fallback 区域已被更精确语义锚点覆盖时多余的页面级 `返回推荐工作台` 负断言，既减少未来重复文案带来的误报，也未削弱 shared-console 权限/回退合同。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)`、`async-validator` 局部噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例里仍残留的 page-global 文案/按钮缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛剩余 shared-console focused 用例中的全局 `cleanup()` / 多棵 Router 树 teardown 依赖：已将 `ProfilePage.test.tsx`、`WebhooksPage.test.tsx`、`SupplierOfferingsPage.test.tsx`、`SupplierResourcesPage.test.tsx`、`SupplierSettlementsPage.test.tsx`、`SupplierDomainsPage.test.tsx` 内多跳导航/双 fallback 回放统一改为显式 `view.unmount()` + fresh render，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与真实导航合同不变，而不是继续依赖 Testing Library 全局清理语义。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/WebhooksPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx src/pages/SupplierDomainsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API 回放脚本 `.hermes/local-output/run_front_cleanup_unmount_real_api.sh`（产物目录 `front-cleanup-unmount/20260503-194713`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/profile`、`/webhooks`、`/supplier/resources`、`/supplier/settlements`、`/supplier/offerings`、`/supplier/domains` SPA 壳 200 均保持成立，本轮 focused 生命周期收敛未破坏注册后共享菜单、管理员真实运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把剩余 supplier / profile / webhook focused 测试内的全局 `cleanup()` 收紧为显式 `unmount()`，不改变运行时权限、真实 API、导航覆盖或部署行为。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例中仍残留的重复 fallback 文案 / 全局缺失断言低信号耦合，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `AdminUsersPage.shared-console.test.tsx` 中管理员资金工作台导航用例的渲染生命周期表达：将同一用例内多次 `cleanup()` + 重新 `renderAdminUsersPage()` 的全局清理模式，收紧为显式 `view.unmount()` 后再 fresh render 的局部 teardown，保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与 `admin-users-mission-flow` 真实导航合同不变，只减少对 Testing Library 全局清理语义的依赖。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API 回放脚本 `.hermes/local-output/run_admin_users_shared_console_unmount_real_api.sh`（产物目录 `admin-users-shared-console-unmount/20260503-192011`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`（201）、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/admin/users`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 生命周期收敛未破坏注册后共享菜单、管理员真实运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把管理员资金工作台导航测试的 teardown 从全局 `cleanup()` 收紧为显式 `unmount()`，并同步移除失效 import，不改变运行时权限、真实 API、导航覆盖或部署行为。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例中仍残留的全局 cleanup / 多棵 Router 树 teardown 依赖，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 supplier shared-console fallback focused 回归中的重复 fallback 文案计数脆弱点：将 `SupplierResourcesPage.test.tsx` 与 `SupplierOfferingsPage.test.tsx` 中对“返回推荐工作台”说明文案的全页计数断言，收紧为 mission/shared fallback 各自 `data-testid` 区域内的 `within(...)` scoped 校验，避免把页面其他共享卡片中的相同文案误算进来，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与真实回退合同不变，而不是继续依赖全页重复文案数量。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_supplier_fallback_copy_scope_real_api.sh`（产物目录 `supplier-fallback-copy-scope/20260503-190214`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/supplier/resources/overview`（预期 403）、管理员 `POST /api/v1/auth/login`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/supplier/resources`、`/supplier/offerings`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 fallback 文案 scoped 收敛未破坏注册后共享菜单、真实资源权限边界、管理员运营接口与 supplier 单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 supplier fallback 文案计数从全页统计收敛为真实 fallback 区域 scoped 断言，减少 future copy / 卡片复用带来的误报，也未削弱 shared-console 权限或真实 API 边界验证。继续保留既有 Semi UI `findDOMNode`、Testing Library `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例中仍残留的全局重复文案计数与结构选择器耦合，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `ApiKeysPage.test.tsx` 的 shared-console focused 回归合同：删除误导性的“API 下单 runner / 提交激活订单”伪契约测试与无效 activation mock，保留并补强真实运行时已有的 API Key 生命周期合同——白名单清空后 reload、白名单后端报错透出、审计卡片 scoped 查询、bridge/fallback 导航与精确撤销确认文案；继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与真实 API Keys / Webhooks / Docs 单壳入口不变，而不是在测试中虚构尚未存在的下单 runner 前端能力。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_api_keys_scope_cleanup_real_api.sh`（产物目录 `api-keys-scope-cleanup-20260503-234658`），确认 `GET /healthz`（200，`redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 ApiKeys focused 合同修正未破坏注册后共享菜单、管理员真实运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；首轮评审曾阻断伪造 `api-keys-order-runner` 的测试标题/断言错配与缺失的白名单失败路径覆盖，控制器已删去不存在功能的伪契约、恢复白名单错误与 reload 合同，并以 scoped 审计卡片查询与精确撤销确认文案复验通过。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ProjectsPage` / `OrdersPage` / 其余 shared-console focused 用例中的剩余低信号全局文案/结构查询，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `ProjectsPage.test.tsx`、`OrdersPage.test.tsx` 与 `AdminUsersPage.shared-console.test.tsx` 中残留的低信号 page-global 文案缺失断言：在不改运行时逻辑的前提下，仅把采购 hero、履约 hero 与管理员共享接入桥接区对接入文案/API Keys/Webhook/Docs 标签缺失的判断，从整页 `screen.queryByText(...)` 收紧为对应 hero/card 区域内的 scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与 bridge/fallback 分区合同不变，而不是继续依赖整页永远不存在同名文案这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx src/pages/AdminUsersPage.shared-console.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_projects_orders_adminusers_scope_real_api.sh`（产物目录 `projects-orders-adminusers-scope/20260504-001545`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/projects/inventory`、`GET /api/v1/orders/activations`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、`GET /api/v1/admin/wallet-users`、`GET /api/v1/admin/disputes` 与前端 `/`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/admin/users` SPA 壳 200 均保持成立，本轮 focused 去脆弱化未破坏注册后共享菜单、真实采购/履约接口、管理员资金运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Projects/Orders/AdminUsers 三个共享控制台 focused 用例中的负断言收敛到真实 hero/bridge 语义区域，既减少未来重复文案导致的误报，也未削弱 menu-truth、真实 API、权限或导航合同。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`ProfilePage.test.tsx`、`SettingsPage.test.tsx` 等其余 shared-console focused 用例里仍残留的 page-global 文案/按钮缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `SettingsPage.test.tsx` 与 `ProfilePage.test.tsx` 中残留的低信号 page-global 缺失断言：保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与角色差异菜单合同不变，仅将 supplier 设置页对用户首轮清单的缺失判断从 `queryByText('首次使用清单')` 收紧为 `settings-user-first-run-checklist` 容器级断言，并将 supplier/admin 资料页对用户专属聚焦卡片的缺失判断从全局 `queryByText('采购与订单串联')` 收紧为 heading 级负断言，避免未来说明文案复用或正文重复时出现误报。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/pages/ProfilePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_settings_profile_scope_real_api.sh`（产物目录 `settings-profile-scope/20260504-011028`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/profile`、`/settings` SPA 壳 200 均保持成立，本轮 focused 去脆弱化未破坏注册后共享菜单、真实 API、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅收紧 Settings/Profile 两页 focused 用例中的容器级/heading 级负断言与测试标题表述，不改变运行时权限、路由或真实接口合同，并进一步降低未来文案复用导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例里仍残留的 page-global copy / fallback 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `ProtectedRoute.test.tsx`、`SettingsPage.test.tsx` 与 `ProfilePage.test.tsx` 中残留的低信号 page-global 路由/文案缺失断言：将 `ProtectedRoute` / `SupplierRoute` / `AdminRoute` focused 用例从普通文本节点切换为带 heading 语义的路由落点断言，并保留未授权/越权时受保护内容不可见的最小合同；同时把 `ProfilePage` supplier/admin 回退卡片对用户专属“采购与订单串联”文案的否定校验收紧到各自角色焦点区域，把 `SettingsPage` 重新打开首轮引导后的断言恢复为共享控制台首页 + 供应商快捷动作不可见并补充 checklist 容器缺失校验，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、注册后统一壳与真实路由守卫合同不变，而不是继续依赖整页同名文本永远不存在这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/components/ProtectedRoute.test.tsx src/pages/SettingsPage.test.tsx src/pages/ProfilePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_route_guard_scope_real_api.sh`（产物目录 `front-route-guard-scope/20260504-035257`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview` 与前端 `/`、`/profile`、`/settings` SPA 壳 200 均保持成立，本轮 route-guard / profile / settings focused 去脆弱化未破坏注册后共享菜单、真实认证接口、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；首轮代码质量评审曾指出 `SettingsPage.test.tsx` 只保留 checklist 缺失断言会弱化原有“非供应商快捷动作不可见”的证明，控制器已恢复 supplier 快捷按钮缺失校验并复验通过。其余评审一致认为当前 diff 仅把路由守卫与资料/设置页 focused 断言收紧到更贴近运行时语义的 heading/区域级合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 漂移与整页同名文本重复导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`DashboardPage.test.tsx`、`AdminSuppliersPage.test.tsx` 等其余 shared-console focused 用例里仍残留的 page-global copy / CTA 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `AppSidebar.test.tsx` 与 `ConsoleLayout.test.tsx` 中 shared shell 仍残留的结构/整页级脆弱断言：为 `AppSidebar` 基础工作台、供应商扩展、管理员扩展与共享菜单加载态卡片补充最小 `data-testid` 语义锚点，并为 `ConsoleLayout` 顶部快捷操作区补充 `console-layout-quick-actions` 区域锚点；focused 用例随之从 `.closest('[style*=border-radius]')` 和整页 quick-action 按钮缺失查询，收紧为对真实分组卡片与快捷操作容器的 `within(...)` scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、当前页不出现在快捷操作区与共享菜单加载态合同不变，而不是继续依赖样式包装层与整页同名按钮永远不存在这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_shared_shell_semantic_anchors_real_api.sh`（产物目录 `app-shell-scope-hardening/20260504-041732`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 shared shell 语义锚点收敛未破坏注册后共享菜单、真实 API、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅为 shared shell 分组卡片、加载态卡片与顶部快捷操作区补充最小语义锚点，并把 focused 测试收紧到真实区域 scoped 合同，不改变运行时权限、导航或真实 API 合同，同时显著降低 future Semi UI 包装层/整页重复按钮文本导致的误报。非阻塞后续项仅包括：`app-sidebar-admin-group` 锚点后续可补一条管理员分组 scoped 断言、`ConsoleLayout` 第二条快捷操作顺序用例未来可继续收敛到同一区域、既有 Semi UI `findDOMNode` / RTL `act(...)` / 前端 chunk 体积告警继续记为债务；下一前端优先项转为继续清理 `App.test.tsx`、`DashboardPage.test.tsx`、`AdminSuppliersPage.test.tsx` 等其余 shared-console focused 用例里仍残留的 page-global copy / CTA 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛注册后共享控制台与 plain-user 共享壳断言：仅调整 `App.test.tsx` 与 `LoginPage.test.tsx`，将新注册普通用户壳层校验从 page-global `供应商扩展` / `管理员扩展` 缺失文案，收紧为 `app-sidebar-shared-group` 内共享菜单存在且 `app-sidebar-supplier-group` / `app-sidebar-admin-group` 整组缺失的语义断言，同时把注册成功用例标题改回与真实证明一致的“会话落库 + 跳转共享首页”合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与真实 API 注册链路不变，而不是继续依赖整页同名分组文案永远不存在这一过宽假设。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_register_shared_shell_real_api.sh`（产物目录 `app-register-shared-shell/20260504-044301`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮注册后 shared-shell 断言收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审唯一非阻塞建议是避免让 `LoginPage.test.tsx` 标题声称其验证了供应商/管理员侧边栏缺失，因此控制器已在提交前把该标题收敛回“注册成功后会话落库并跳转共享首页”的真实证明范围。其余评审一致认为当前 diff 仅把 plain-user 壳层断言收紧到真实 sidebar group 语义锚点，并提升 autonomous run 下失败定位清晰度，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `DashboardPage.test.tsx`、`AdminSuppliersPage.test.tsx`、`LoginPage.test.tsx` 等其余 shared-console / register focused 用例里仍残留的低信号 copy / route-contract 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `DashboardPage.test.tsx`、`AdminSuppliersPage.test.tsx` 与 `LoginPage.test.tsx` 中剩余的低信号标题/区域断言：在不改运行时代码的前提下，仅把 Dashboard 总览断言从普通文本节点收紧为 heading 级合同，把 AdminSuppliers 首屏桥接/任务流断言收紧为 `admin-suppliers-mission-flow` 与 bridge card 区域 scoped 查询，并把 Login 注册前桥接内容收紧到“注册后默认进入共享控制台”卡片内的 scoped 断言，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与管理员/普通用户共享工作台合同不变，而不是继续依赖整页重复文案或非语义标题查询。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/pages/AdminSuppliersPage.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_dashboard_login_adminsuppliers_real_api.sh`（产物目录 `dashboard-login-adminsuppliers-real-api/$STAMP`，以实际时间戳目录保存），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；测试/可靠性评审一度误把本地未提交回放脚本的掩码输出当成当前 diff 问题，控制器已用直接文件读取与实际 rerun 纠正并确认脚本当前可重跑。其余评审一致认为当前 diff 仅收紧 Dashboard/AdminSuppliers/Login 三个 focused 用例到更贴近运行时语义的 heading/bridge/card 作用域，不改变运行时权限、导航或真实 API 合同；既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警继续记为非阻塞债务。下一前端优先项转为继续清理 `App.test.tsx`、`ProfilePage.test.tsx`、`SettingsPage.test.tsx` 等 shared-console focused 用例中仍残留的 `.closest('.semi-card')` / page-global copy 断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收紧 `App.test.tsx` 中默认普通用户首轮引导 focused 合同：将 `expectDefaultUserFirstRunLane()` 从依赖长文案/营销 copy 的断言，改为 `dashboard-next-steps-lane` 内 `前往项目市场` / `查看订单中心` / `管理 API Keys` 按钮级 scoped 合同，并显式验证 `前往域名管理` / `查看风控中心` 不会泄露到普通用户首轮路径；同时为 Dashboard 首轮引导的 `稍后再看` 动作补充 `dashboard-first-run-dismiss` 语义锚点，把设置页“重新打开首轮引导”验证收紧到 `settings-user-first-run-checklist` 容器级范围，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与真实首轮引导/设置页回流合同不变，而不是继续把高频 copy 细节当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_first_run_scope_real_api.sh`（产物目录 `app-first-run-scope/20260504-072509`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮首轮引导 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；本轮控制器对五维评审采用直接 controller-side 结果收敛：产品/规格侧确认当前 diff 仅把普通用户首轮引导/设置回流 proof 从长文案收敛为按钮/容器级合同，代码质量侧确认新增 `dashboard-first-run-dismiss` 语义锚点与 helper 重用保持单一职责，安全/集成侧确认无权限边界或真实 API 合同漂移，测试/可靠性侧确认 `App.test.tsx` focused、构建、Go 测试、Compose 重建与真实 API 回放全部通过，性能/运维侧仅继续记录既有前端 chunk 体积告警为非阻塞债务。下一前端优先项转为继续清理 `App.test.tsx`、`ProfilePage.test.tsx`、`SettingsPage.test.tsx` 等 shared-console focused 用例中仍残留的 page-global copy / fallback 缺失断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `App.test.tsx` 与 `SettingsPage.test.tsx` 中残留的低信号 shared-shell / 回流断言：在不改运行时代码的前提下，仅将注册成功后的共享控制台可达性从 page-global `控制台总览` 文案收紧为默认普通用户首轮 lane helper `expectDefaultUserFirstRunLane()`，并把 plain-user sidebar 场景从通用总览文本收紧为 `app-sidebar-shared-group` 语义锚点；同时将设置页“重新打开首轮引导”后的 supplier CTA 缺失检查收窄到共享首页落点容器，并为第二次 render 显式 `unmount()`，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与真实注册/回流合同不变，而不是继续依赖整页通用标题或 page-global 负断言。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/SettingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_test_scope_real_api.sh`（产物目录 `front-test-scope-20260504-081516`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；代码质量与测试/可靠性评审仅提出两项非阻塞建议：`App.test.tsx` 中 `onboardingRegion` 变量若只用于触发 helper 可标记为显式已消费，`SettingsPage.test.tsx` 后续若首页 stub 演进可考虑用稳定容器锚点替代 `parentElement` 缩域；控制器已在当前 diff 中加入 `void onboardingRegion` 明确变量用途，其余保持为后续债务。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`LoginPage.test.tsx`、`DashboardPage.test.tsx` 等 shared-console focused 用例中仍残留的长文案/通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按“前端优先”收敛 `SettingsPage.test.tsx` 中最后一处基于 `parentElement` 的首页落点结构依赖：为测试内共享首页 route stub 补充最小 `shared-console-home-route` 语义锚点，并把"重新打开首轮引导"后的 supplier CTA 缺失校验收紧为该首页锚点内的 scoped role 断言，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与设置页回流合同不变，而不是继续依赖测试 stub DOM 包装层级。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/App.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_test_scope_real_api.sh`（产物目录 `front-test-scope-20260504-084440`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/profile`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；五位 reviewer 一致确认当前 diff 仅为 `SettingsPage.test.tsx` 的测试 stub 增加最小首页锚点并移除 `parentElement` 结构遍历，没有弱化"重新打开首轮引导后回到共享首页、supplier CTA 不泄漏、checklist 消失"的原合同，也未引入额外运行时或运维复杂度。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理其余 shared-console focused 用例中残留的低信号长文案 / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `DashboardPage.test.tsx` 中下一步旅程 lane 的低信号文案耦合：在运行时代码仅补充最小 `dashboard-next-step-${step.key}` / `dashboard-next-step-action-${step.key}` 语义锚点的前提下，将 shared-console focused 用例里对 `先确认预算与钱包状态`、`再进入项目市场采购`、`最后完成 API 接入` 等长文案/按钮文本的存在与缺失判断，收紧为 lane 区域内基于 step key 的结构合同，同时保留 integration 卡片标题 `最后完成 API 接入` 与 CTA 可访问名称 `管理 API Keys` 的用户层证明，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、预算→采购→履约→接入 单壳导航顺序与真实 API Keys 跳转合同不变，而不是继续把易变 copy 当作唯一 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_dashboard_step_contract_real_api.sh`（产物目录 `dashboard-step-contract/20260504-101442`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 Dashboard lane 语义锚点收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；测试/可靠性评审初版一度指出改动若完全切换到 testid 会弱化用户层按钮/标题合同，控制器已在 integration 卡片内补回 `最后完成 API 接入` 标题与 `管理 API Keys` 可访问按钮断言后复验通过。其余评审一致认为当前 diff 仅以最小语义锚点把 Dashboard 下一步旅程 lane 的 brittle copy 断言收敛为更贴近运行时结构/行为的合同，不改变运行时权限、导航或真实 API 合同；既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警继续记为非阻塞债务。下一前端优先项转为继续清理 `SupplierDomainsPage.test.tsx`、`ProjectsPage.test.tsx`、`OrdersPage.test.tsx` 等页中仍残留的长文案 / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SupplierDomainsPage.test.tsx`、`ProjectsPage.test.tsx` 与 `OrdersPage.test.tsx` 中残留的低信号文案/标题耦合：在不改运行时代码的前提下，仅把 `SupplierDomainsPage` 的 overview 失败场景校验从笼统的 error-vs-empty-state 描述，收紧为更贴近真实 UI 的"显式 overview 失败态 + table card 内 empty-state 文案缺失"合同；同时把 Projects/Orders 的 CTA 抑制用例标题分别明确为 procurement hero / fulfillment hero 区域合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、采购→履约→接入单壳连续路径与真实 API 菜单真值不变，而不是继续依赖含糊的测试标题或过宽的空态文案表述。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_hero_copy_scope_real_api.sh`（产物目录 `front-hero-copy-scope/20260504-105404`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/projects`、`/orders`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商域名运营入口与单壳前端连续路径。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 SupplierDomains 的失败态 proof 收紧到更贴近真实区域合同的断言，并把 Projects/Orders 两个 hero CTA 抑制用例标题改为更准确的区域说明，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 调整与测试语义歧义造成的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`LoginPage.test.tsx`、`DashboardPage.test.tsx`、`SupplierDomainsPage.test.tsx` 等 shared-console focused 用例中仍残留的长文案 / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 与 `DashboardPage.test.tsx` 中剩余的低信号标题/长文案断言：在不改运行时代码的前提下，仅把未认证登录壳校验从普通文本查询收紧为 `heading` 语义合同，并把 Dashboard 下一步旅程 lane 的 smoke proof 从四条易变长文案收敛为四个稳定 step/action 语义锚点，同时继续保留 API Keys 集成卡片的用户可见 heading / button 合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与预算→采购→履约→接入 单壳路径不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/DashboardPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_dashboard_step_contract_real_api.sh`（产物目录 `dashboard-step-contract/20260504-115743`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把未认证登录壳校验收紧为 heading 语义合同，并把 Dashboard 下一步旅程 lane 的 brittle 长文案 proof 收敛为更贴近运行时结构的 step/action 语义锚点，同时保留 API Keys 集成卡片的用户层标题/按钮真值，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`LoginPage.test.tsx`、`DashboardPage.test.tsx`、`SupplierDomainsPage.test.tsx` 等 shared-console focused 用例中仍残留的长文案 / 通用文本可达性断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 shared shell focused 契约：仅调整 `AppSidebar.test.tsx` 与 `ConsoleLayout.test.tsx`，将侧边栏等待条件从手写 `waitFor(getByText('Nexus-Mail'))` 收敛为 heading 语义等待，并把供应商当前路由选中断言缩到 `app-sidebar-supplier-group` 内唯一选中项；同时将顶部快捷操作顺序与导航断言缩到 `console-layout-quick-actions` 区域，继续保持 new-api 风格单一登录后深色共享控制台、共享路由 schema 顺序、角色分组菜单与统一布局合同不变，而不是继续依赖整页按钮扫描或全局选中节点。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_sidebar_layout_contract_real_api.sh`（产物目录 `sidebar-layout-contract/20260504-133352`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 shared shell focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 shared shell focused proof 从整页等待/按钮扫描收敛为更贴近运行时语义的 heading、group 与 quick-actions scoped 合同，不改变运行时权限、导航或真实 API 合同。继续保留既有 Semi UI `findDOMNode`、AppSidebar `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ProfilePage.test.tsx`、`SupplierSettlementsPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明/页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ProfilePage.test.tsx` 首个 shared-console focused 用例中仍残留的低信号 page-global 长说明断言：在不改运行时代码的前提下，仅将 user capability 场景从 `账号身份、会话边界...`、`当前账号默认以用户身份进入共享控制台...`、`控制台桥接能力`、`深色共享账号中枢` 等易变说明 copy 收紧为 `Profile Mission Control` 标签、`个人资料` / `用户接入焦点` / `采购与订单串联` / `集成准备` 等 heading 语义合同，并继续保留 `profile-capability-bridge` 区域内 API Keys / Webhook / Docs 三个真实导航按钮与跳转结果，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与 Profile 共享账号中枢导航合同不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_profile_heading_scope_real_api.sh`（产物目录 `profile-heading-scope/20260504-140457`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` SPA 壳 200 均保持成立，本轮 Profile focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 Profile 首个 focused 用例从 page-global 长说明 copy 收敛为更贴近运行时结构的标签/heading 骨架与 capability-region 按钮合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 微调导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `SupplierSettlementsPage.test.tsx`、`ApiKeysPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明/页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SettingsPage.test.tsx`、`SupplierResourcesPage.test.tsx` 与 `BalancePage.test.tsx` 首个 mission-control focused 用例中仍残留的低信号 page-global 长说明断言：在不改运行时代码的前提下，仅把设置中心、供应商资源页与余额中心的首屏 proof 从大段 hero 说明、整组页面级叙事 copy 收紧为更贴近 shared-console/new-api 单壳结构的 heading + mission-flow / capability / bridge 稳定合同，同时保留 `Docs / Webhooks / API Keys 已统一到单一壳内导航`、供应商三步任务流以及普通用户资金路径/角色差异等关键用户可见真值，继续保持注册后统一壳、角色菜单 gating 与真实入口导航合同不变，而不是继续把高频润色长文案当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/BalancePage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_mission_heading_scope_real_api.sh`（最新产物目录 `front-mission-heading-scope/20260504-170052`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/settings`、`/balance`、`/supplier/resources`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 SAFE、性能/运维 PASS；评审过程中曾指出 Settings / SupplierResources / Balance 三页的首屏 focused proof 若只保留通用 heading 会弱化对共享控制台能力矩阵、供应商三步任务流、普通用户资金路径与统一接入入口的保护，控制器已补回这些稳定用户可见合同后复验通过。继续保留既有 Semi UI `findDOMNode`、Balance focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `SupplierSettlementsPage.test.tsx`、`ApiKeysPage.test.tsx`、`WebhooksPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `ApiKeysPage.test.tsx`、`ProfilePage.test.tsx` 与 `SupplierSettlementsPage.test.tsx` 中仍残留的低信号装饰性标签 / 长说明断言：在不改运行时代码的前提下，仅把 `ApiKeysPage` supplier hero proof 保持为 `api-keys-hero-card` 区域内两条稳定 supplier tip 合同，把 `ProfilePage` capability 场景从装饰性 `Profile Mission Control` 纯文本查询收紧为 `个人资料` heading 语义合同，并将 `SupplierSettlementsPage` 首个 mission-control proof 从三条易变任务流长文案收敛为 `supplier-settlements-mission-flow` 区域内三枚真实 CTA 按钮合同，同时保留 bridge 区域与关键 heading/指标真值，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、API Keys / Profile / Supplier Finance 单壳联动与真实 API 菜单真值不变，而不是继续把装饰标签或高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/pages/ProfilePage.test.tsx src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_api_keys_profile_settlements_scope_real_api.sh`（产物目录 `front-api-keys-profile-settlements-scope/20260504-171958`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/settlements` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 API Keys / Profile / Supplier Settlements 三个 focused 用例从装饰性标签或易变长文案收敛为更贴近运行时结构的 heading / supplier tip / mission CTA 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 微调与重复说明文本导致的误报。继续保留既有 Semi UI `findDOMNode`、ApiKeys focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`ApiDocsPage.test.tsx`、`AdminAuditPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SupplierSettlementsPage.test.tsx` 中残留的装饰性英文 tag 依赖：在不改运行时代码的前提下，仅把后续导航 / fallback / 提交流程用例里的 `Supplier Finance Mission Control` 普通文本断言统一替换为 `供应商资金与争议指挥台` heading 语义合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、supplier finance mission-flow / shared-console fallback 分区与真实供应商结算 API 合同不变，而不是继续把装饰性英文标签当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_supplier_settlements_heading_scope_real_api.sh`（产物目录 `supplier-settlements-heading-scope/20260504-173913`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/supplier/settlements` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 SupplierSettlements 后续 focused 用例从装饰性英文标签收敛为更贴近运行时结构的中文 heading 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 视觉标签/文案微调导致的误报。继续保留既有 Semi UI `findDOMNode`、SupplierSettlements focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`ApiDocsPage.test.tsx`、`AdminAuditPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `AdminAuditPage.test.tsx` 中剩余的装饰性英文 tag 依赖：在不改运行时代码的前提下，仅把后续导航 / fallback / 查询过滤用例里的 `Audit Mission Control` 普通文本断言统一替换为 `审计日志` heading 语义合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、admin audit mission-flow / shared-console fallback / filter 合同与真实管理员审计 API 合同不变，而不是继续把装饰性英文标签当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminAuditPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_audit_heading_scope_real_api.sh`（产物目录 `admin-audit-heading-scope/20260504-180855`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 AdminAudit 后续 focused 用例从装饰性英文标签收敛为更贴近运行时结构的中文 heading 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 视觉标签/文案微调导致的误报。继续保留既有 Semi UI `findDOMNode`、AdminAudit focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`ApiDocsPage.test.tsx`、`App.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `WebhooksPage.test.tsx` 中 user integration loop focused 用例仍残留的低信号长说明断言：在不改运行时代码的前提下，仅把 Webhooks 首轮 shared-console bridge 卡片 proof 从整段 `在同一套控制台里先创建 endpoint...` 长说明 copy 收紧为更贴近 new-api 单壳 shared-console 合同的 heading 语义 + `webhooks-first-integration-loop` 区域内 API Keys / API 文档 两个真实 CTA scoped 断言，并同步把相关页面首屏可达性从普通文本查询收敛为 `开发者 Webhook 接入工作台` heading 合同，继续保持注册后统一壳、角色菜单 gating、Webhook → API Keys / Docs 接入闭环与真实 API 菜单真值不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_integration_scope_real_api.sh`（产物目录 `webhooks-integration-scope/20260504-182838`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；产品/规格评审认为当前 diff 仍保留 Webhooks 首轮 bridge 卡片的标题与两个关键 CTA 真值，代码质量评审仅建议后续可把测试标题进一步改成更偏用户行为表述，安全/集成评审确认未引入 route/menu/API 合同漂移，测试/可靠性评审确认仅去除了 brittle 长文案快照且未新增 flaky 风险，性能/运维评审确认该 diff 只降低前端测试维护噪声、不会影响构建或部署。继续保留既有 Semi UI `findDOMNode`、Webhooks focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`App.test.tsx`、`LoginPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SupplierDomainsPage.test.tsx` 首个 shared-console focused 用例中仍残留的低信号 Mission Control / page-global 长说明断言：在不改运行时代码的前提下，仅把域名运营页首屏 proof 从装饰性 `Supplier Domain Mission Control` 英文标签、若干 mission-step 长说明与 bridge tag 文本，收紧为 `heading` 语义合同、`supplier-domains-mission-flow` 区域内供应商资源 / 供货规则两个真实 CTA，以及 `supplier-domains-shared-console-bridge` 区域内 API Keys / Webhook / API 文档 三个共享桥接按钮合同，同时继续保留 `单一供应商工作台`、`域名 readiness 优先`、`角色扩展但不伪造升级` 三个稳定 capability 真值，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、域名→资源→供货→结算单壳连续路径与真实菜单真值不变，而不是继续把装饰标签或高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierDomainsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放（控制器串行复核 `GET /healthz`、普通用户注册 + `/auth/menu`、供应商登录 + `/auth/menu` + `/supplier/resources/overview` + `/supplier/settlements`、管理员登录 + `/auth/menu` + `/admin/overview` + `/admin/risk` + `/admin/audit?limit=5`，以及前端 `/`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs`、`/admin/audit` SPA 壳 200），确认本轮 SupplierDomains focused 收敛未破坏真实注册、共享菜单、供应商资源/结算入口、管理员运营接口与单壳前端入口；对应本地回放脚本已更新保存在 `.hermes/local-output/run_supplier_domains_heading_scope_real_api.sh` 供后续复用。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 SupplierDomains 首个 focused 用例从装饰性 Mission Control 标签与 page-global 长说明 copy 收敛为更贴近运行时结构的 heading / mission-flow / shared-console-bridge 按钮合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 微调与多区域重复文本导致的误报。继续保留既有 Semi UI `findDOMNode`、RTL `act(...)` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `SupplierResourcesPage.test.tsx`、`BalancePage.test.tsx`、`ApiDocsPage.test.tsx` 等 shared-console focused 用例里仍残留的装饰性 Mission Control 标签 / 长说明断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `SupplierResourcesPage.test.tsx`、`BalancePage.test.tsx` 与 `SettingsPage.test.tsx` 中仍残留的装饰性英文 Mission Control 标签 / 长说明依赖：在不改运行时代码的前提下，仅把 SupplierResources 与 Balance 的后续导航 / fallback / 提交流程用例从 `Supplier Resource Mission Control` / `Finance Mission Control` 英文标签等待收紧为 `供应商资源` / `余额中心` heading 语义合同，并删除 Settings 首个 shared-console 结构用例里对 `Console Mission Control` 与浅色占位叙事长句的低信号断言，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、供应商资源 / 资金 / 设置页共享壳导航与真实 API 菜单真值不变，而不是继续把装饰性英文标签或高频润色说明 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/SupplierResourcesPage.test.tsx src/pages/BalancePage.test.tsx src/pages/SettingsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_mission_heading_scope_real_api.sh`（产物目录 `front-mission-heading-scope/20260504-191748`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5` 与前端 `/`、`/settings`、`/balance`、`/supplier/resources`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 SupplierResources / Balance 的后续 focused 用例从装饰性英文标签收敛为更贴近运行时结构的中文 heading 合同，并删除 Settings 首个 shared-console 结构用例中的低信号装饰性标签/叙事长句断言，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 视觉标签/文案微调导致的误报。继续保留既有 Semi UI `findDOMNode`、Balance focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`AdminUsersPage.shared-console.test.tsx`、`AdminRiskPage.test.tsx`、`SupplierResourcesPage.test.tsx`、`BalancePage.test.tsx` 等 shared-console focused 用例里仍残留的装饰性 Mission Control 标签 / 长说明断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `AdminProjectsPage.test.tsx`、`AdminSuppliersPage.test.tsx` 与 `SupplierDomainsPage.test.tsx` 中仍残留的装饰性 Mission Control / page-global 长说明依赖：在不改运行时代码的前提下，仅把管理员价格页与管理员供应商页后续 focused 用例里的 `Admin Pricing Mission Control` / `Supplier Mission Control` 装饰性英文标签等待统一收紧为 `价格策略` / `供应商管理` heading 语义合同，并把 SupplierDomains 首屏 proof 从仅剩的泛化标签校验补回到更贴近真实 overview 数据的指标说明与 region 聚合合同（保留 `可继续参与供货编排的域名数量。`、`支持泛收件的域名数量。`、`去重后的 region 数量。` 以及 `hk/us/global/eu · 1` 聚合结果），同时继续保留三页各自的 mission-flow CTA、shared-console bridge、fallback 与真实 service payload 提交流程断言，保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating 与真实 API 菜单真值不变，而不是继续把装饰标签或高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/AdminProjectsPage.test.tsx src/pages/AdminSuppliersPage.test.tsx src/pages/SupplierDomainsPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_admin_projects_admin_suppliers_supplier_domains_focus_real_api.sh`（产物目录 `admin-projects-admin-suppliers-supplier-domains-focus/20260504-212701`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/admin/pricing`、`/admin/suppliers`、`/supplier/domains`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；代码质量与安全/集成评审一度指出 `SupplierDomainsPage.test.tsx` 如果只保留通用指标标签会削弱对 overview 聚合/region 归一化的集成证明，控制器已补回指标说明与 `hk/us/global/eu · 1` 聚合结果断言后复验通过。其余评审一致认为当前 diff 仅把三页 focused 用例从装饰性 Mission Control 标签与 page-global 长说明收敛为更贴近运行时结构的 heading / mission-flow / bridge 合同，并保留关键 overview / service payload 真值，不改变运行时权限、导航或真实 API 合同；既有 Semi UI `findDOMNode`、局部 `act(...)` / `FormApi.setValues()` 噪声与前端 chunk 体积告警继续记为非阻塞债务。下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx`、`LoginPage.test.tsx` 等 shared-console focused 用例里仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx` 中仍残留的装饰性英文标签与宽泛 page-global 落点断言：在不改运行时代码的前提下，仅把 admin risk / audit workspace proof 从 `Risk Mission Control`、`Audit Mission Control` 等装饰性英文标签收紧为 `规则命中概览`、`admin-audit-events-table-card` 等更贴近运行时数据区合同，并把 plain-user 误入 supplier 路由后的回退证明从宽泛首页文本收紧为 `dashboard-next-steps-lane` 共享首页落点合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与真实 admin/supplier route guard 合同不变，而不是继续把装饰标签或整页通用文本当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/WebhooksPage.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_front_heading_real_api.sh`（最新产物目录 `app-front-heading-real-api/20260504-222229`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/api-keys`、`/webhooks`、`/docs`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 `App.test.tsx` 从装饰性英文标签与宽泛 page-global 文本断言收敛为更贴近运行时结构的 risk/audit 数据区与 shared dashboard lane 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案润色、视觉标签调整与首页重复文本导致的误报。唯一非阻塞建议仅为后续把测试标题 `shared shared-shell` 中的重复词顺手改正；既有 Semi UI `findDOMNode`、局部 `act(...)` 噪声与前端 chunk 体积告警继续记为非阻塞债务。下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx`、`LoginPage.test.tsx`、`App.test.tsx` 其余 focused 用例中仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `LoginPage.test.tsx` 首个注册壳 focused 用例中仍残留的低信号角色路径 heading 断言：在不改运行时代码的前提下，仅把注册旅程首屏 proof 从 `注册后默认进入共享控制台`、`用户路径`、`供应商路径`、`管理员路径` 四条易随文案/布局微调变化的说明 heading，收紧为 `login-register-journey` 区域内稳定 `立即注册，进入共享控制台` CTA 合同，以及点击后 `注册 Nexus-Mail` / `创建账户并进入控制台` 的真实模式切换合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳与真实注册链路不变，而不是继续把角色路径说明 heading 当作该 focused 用例的主真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_login_register_shell_scope_real_api.sh`（产物目录 `login-register-shell-scope/20260504-225353`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/api-keys`、`/webhooks`、`/docs` SPA 壳 200 均保持成立，本轮 Login 注册壳 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；代码质量评审仅提出一项非阻塞建议：将该 focused 用例的点击动作也保持在 `login-register-journey` scoped 查询内，控制器已当轮修正并复验通过。其余评审一致认为当前 diff 仅把 Login 注册壳首个 focused 用例从低信号角色路径 heading 收敛为更贴近运行时结构/行为的 CTA + 模式切换合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future copy 微调与布局润色导致的误报。继续保留既有 LoginPage focused 中已有的 React `act(...)` 警告、Semi UI `findDOMNode` 与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx`、`App.test.tsx` 其余 focused 用例中仍残留的低信号长说明 / 页面级文本断言，再回到 Phase 4/5 其余后端能力。

- 本轮继续按"前端优先"收敛 `App.test.tsx` 与 `ApiDocsPage.test.tsx` 中仍残留的低信号装饰性 heading / 角色路径说明依赖：在不改运行时代码的前提下，仅把登录注册壳 focused 用例从 `注册后默认进入共享控制台 / 用户路径 / 供应商路径 / 管理员路径` 等易变 journey heading 收紧为 `login-register-journey` 区域内稳定注册 CTA + 注册模式切换合同，并把管理员 docs focused 用例从装饰性 `Docs Mission Control` heading 等待收紧为 `管理员扩展 · API 契约` badge、`/openapi/index.html` iframe surface 与 `docs-shared-console-bridge` 区域内 API Keys / menu gating 合同，继续保持 new-api 风格单一登录后深色共享控制台、注册后统一壳、角色菜单 gating 与 Docs / API Keys / Webhooks 共享桥接不变，而不是继续把装饰性 heading 或角色路径说明当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/LoginPage.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose up -d --build web api gateway`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_app_docs_login_scope_real_api.sh`（产物目录 `app-docs-login-scope/20260504-231615`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/docs`、`/api-keys`、`/webhooks`、`/admin/risk`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 `App.test.tsx` 与 `ApiDocsPage.test.tsx` 中的登录注册旅程 / 管理员 docs focused 用例从装饰性 heading 与低信号角色路径说明收敛为更贴近运行时结构与用户行为的 CTA、badge、iframe surface 与 shared-console bridge gating 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案润色与视觉标签调整导致的误报。继续保留既有 Semi UI `findDOMNode`、局部 React `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `WebhooksPage.test.tsx`、`App.test.tsx` 其余 focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。

- 本轮继续按"前端优先"收敛 `WebhooksPage.test.tsx` 中供应商 / 管理员角色 focused 用例仍残留的低信号长说明断言：在不改运行时代码的前提下，仅把 role-aware guidance proof 从 `优先使用固定公网出口服务承接回调`、`重点关注 failed / pending 重试链路` 这类易随 copy 润色变化的整句说明，收紧为 `供给事件回调工作台` / `Webhook 运维与回调观测` heading 语义合同，并继续保留 `供应商视角` / `管理员视角` 角色标记真值，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、Webhook → API Keys / Docs 接入闭环与真实 API 菜单真值不变，而不是继续把高频润色 copy 当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_webhooks_role_heading_scope_real_api.sh`（产物目录 `webhooks-role-heading-scope/20260505-003123`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/webhooks`、`/api-keys`、`/docs`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏真实注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 `WebhooksPage.test.tsx` 中 supplier/admin 角色 guidance 用例从低信号长说明 copy 收敛为更贴近运行时结构的 heading + role badge 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案润色导致的 focused 误报。继续保留既有 Semi UI `findDOMNode`、Webhooks focused 中已有的 `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 其余 focused 用例里仍残留的低信号 page-global 文本断言，再回到 Phase 4/5 其余后端能力。
- 本轮继续按"前端优先"收敛 `App.test.tsx`、`ApiDocsPage.test.tsx` 与 `WebhooksPage.test.tsx` 中仍残留的低信号 page-global / copy 断言：在不改运行时代码的前提下，仅把设置页首轮引导入口 proof 从普通文本等待收紧为 `首次使用清单` heading + `重新打开首轮引导` button 语义合同，把 docs 管理员 badge 与 bridge/loop lane 标题 proof 收紧为 `aria-label` / scoped heading 合同，并把 Webhook 运维页首屏 proof 从 page-global `最近回调` 指标标题与装饰性说明文本，收紧为 `Webhook 运维与回调观测` heading、`webhooks-latest-delivery-metric` 区域内最新回调信号与稳定指标合同，继续保持 new-api 风格单一登录后深色共享控制台、角色菜单 gating、注册后统一壳、Docs→API Keys→Webhook 单壳桥接与真实 API 菜单真值不变，而不是继续把易变 copy 或重复文本节点当作 focused 真值。
- 控制器已重新通过 `pnpm --dir web exec vitest run src/App.test.tsx src/pages/ApiDocsPage.test.tsx src/pages/WebhooksPage.test.tsx`、`pnpm --dir web build`、`go test ./...`，并执行真实 API / SPA 回放脚本 `.hermes/local-output/run_front_focus_signal_scope_real_api.sh`（本轮产物目录 `front-focus-signal-scope/20260505-140634`），确认 `GET /healthz`（200，JSON `status=ok` 且 `redis=ok`）、普通用户 `POST /api/v1/auth/register`、`GET /api/v1/auth/menu`、`GET /api/v1/auth/me`、`GET /api/v1/dashboard/overview`、`GET /api/v1/auth/api-keys`、`GET /api/v1/webhooks/endpoints`、管理员 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/admin/overview`、`GET /api/v1/admin/risk`、`GET /api/v1/admin/audit?limit=5`、供应商 `POST /api/v1/auth/login`、`GET /api/v1/auth/menu`、`GET /api/v1/supplier/resources/overview`、`GET /api/v1/supplier/settlements` 与前端 `/`、`/login`、`/docs`、`/webhooks`、`/api-keys`、`/admin/audit` SPA 壳 200 均保持成立，本轮 focused 收敛未破坏注册、共享菜单、管理员运营接口、供应商资源/结算入口与单壳前端入口。
- 本轮五维评审结论：产品/规格 PASS、代码质量 APPROVED、安全/集成 SAFE、测试/可靠性 PASS、性能/运维 PASS；评审一致认为当前 diff 仅把 `App.test.tsx`、`ApiDocsPage.test.tsx`、`WebhooksPage.test.tsx` 中若干 focused 用例从低信号 page-global 文本/说明 copy 收敛为更贴近运行时结构的 heading、button、badge、metric-card 合同，不改变运行时权限、导航或真实 API 合同，同时进一步降低 future 文案润色与重复文本导致的误报。继续保留既有 Semi UI `findDOMNode`、React `act(...)` 噪声与前端 chunk 体积告警为非阻塞债务；下一前端优先项转为继续清理 `App.test.tsx` 里 `renders webhook settings page for authenticated admin` 等仍残留的 page-global heading / metric 文本断言，再回到 Phase 4/5 其余后端能力。


### 最新执行检查点（2026-05-09，Login 注册 runway 文案与语义锚点收敛）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/LoginPage.tsx` 与 `web/src/pages/LoginPage.test.tsx` 做一个小而完整的前端 focused 收口切片，把登录页左侧 `首轮接入路径` 摘要文案从偏解释性的长句收紧为更贴近控制台入口 runway 的短句，并为三张接入卡片补充稳定语义锚点；保持注册/登录接口、菜单真值、角色边界与 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先让 `LoginPage.test.tsx` 对新的 runway 合同 RED：显式要求旧摘要长句退出首个 focused proof，并改为按 `login-runway-card-api-keys` / `login-runway-card-webhooks` / `login-runway-card-docs` 三张卡片分别验证 step、标题与 eyebrow。复现后确认根因不是注册功能失效、不是菜单/路由漂移、不是登录壳渲染错误，而是测试仍依赖整段长说明与整区文本匹配。随后仅最小修改 2 个前端文件：为 `integrationRunway` 增加稳定 `key`，为三张卡片挂载 test id，并把摘要收口为 `注册后进入共享控制台，并沿同一导航完成首个接入闭环。`。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（2 files / 31 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面重跑本地忽略脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录 `.hermes/local-output/app-login-register-scope/20260509-091945`：再次验证健康检查、普通用户动态注册、普通用户菜单/资料/概览/API Keys/Webhooks、管理员菜单/概览/风控/审计以及前端共享壳关键页面可达性全部通过。`docker compose up -d --build web api gateway` 本轮因 Docker Hub 元数据拉取超时失败，已按 fail-closed 记录为外部 registry 网络问题；当前在运行中的 `web/api/gateway` 容器仍保持 healthy，未阻断 controller 侧 build 与真实 API 验证。
- 本轮五维评审结论：产品/规格 PASS（runway 更贴近 shared-console 入口心智，未引入独立后台或角色漂移）；代码质量 APPROVED（diff 仅 2 个文件，1 处摘要文案 + 3 个卡片锚点 + focused proof 收口，职责集中）；安全/集成 SAFE（静态扫描 5 类风险模式 0 命中，且未改 auth/session/menu truth/permissions）；测试/可靠性 PASS（RED→GREEN、focused、build、Go tests、真实 API 回放全部通过，既有 React `act(...)` / Semi 警告未新增）；性能/运维 PASS（纯前端轻量收口，不新增请求，compose rebuild 失败源于外部镜像仓库超时而非应用回归）。下一前端优先项继续围绕登录/共享壳 focused 用例里剩余的低信号长文案与 page-global 断言收口，再回到 Phase 4/5 余下后端能力。


### 最新执行检查点（2026-05-09，Login shared-console 文案进一步收口）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/LoginPage.tsx` 与 `web/src/pages/LoginPage.test.tsx` 做一个小而完整的前端 focused 收口切片，把登录页顶部 summary、登录 helper 与注册 runway 摘要文案进一步压缩为更贴近 Linear / new-api 单壳控制台入口心智的短句；保持注册/登录接口、`/auth/menu` 菜单真值、角色边界与 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先在 `LoginPage.test.tsx` 把目标 focused 合同改成 RED：要求 `login-entry-summary` 显式渲染新 summary `统一入口、共享控制台、接入链路，在同一登录面完成切换。`，要求登录 helper 改为 `登录后按角色展开工作区，继续同一套导航。`，并要求注册 runway 摘要改为 `注册后进入共享控制台，沿同一导航完成首轮接入闭环。`。首轮 RED 证明这些文案尚未落地；根因确认不是注册功能失效、不是 shared-console 路由/菜单漂移、也不是样式壳异常，而只是登录页运行时 copy 仍停留在上一轮较长表达。随后仅最小修改 2 个前端文件：同步更新 `LoginPage.tsx` 运行时 copy 与 `LoginPage.test.tsx` focused 断言，不扩散到 store、router、service、auth 或任何后端逻辑。
- 控制器验证结果：RED 用例 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx -t "renders a denser shared-console entry with a compact signal strip and product-style registration runway"` 先失败、随后 GREEN 通过；进一步通过 `pnpm --dir web exec vitest run src/App.test.tsx -t "uses the shared API keys route constant for register onboarding entrypoints"`、`pnpm --dir web exec vitest run src/App.test.tsx -t "falls back to the docs workspace when no preferred role landing route exists"`、`pnpm --dir web exec vitest run src/App.test.tsx -t "navigates from profile page CTA to the supplier domains page"` 与组合回归 `pnpm --dir web exec vitest run src/App.test.tsx -t "renders the login shell when unauthenticated|keeps shared integration routes reachable after register bootstrap|shows register journey CTA and opens register mode from the login shell|uses the shared API keys route constant for register onboarding entrypoints|falls back to the docs workspace when no preferred role landing route exists"`。随后 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面重跑本地忽略脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录 `.hermes/local-output/app-login-register-scope/20260509-104905`：再次验证健康检查、普通用户动态注册、普通用户菜单/资料/概览/API Keys/Webhooks、管理员菜单/概览/风控/审计以及前端共享壳 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` 可达性全部通过。`docker compose up -d --build web api gateway` 本轮仍因 Docker Hub 元数据拉取 `node:22-alpine` / `golang:1.25-alpine` 超时失败，已按 fail-closed 记录为外部 registry 网络问题；当前运行中的 `web/api/gateway` 容器继续保持 healthy，未阻断 controller 侧 build 与真实 API 验证。
- 本轮五维评审结论：产品/规格 PASS（新 summary / helper / runway 更贴近单一登录后 shared-console 入口心智，未引入独立后台或角色漂移）；代码质量 APPROVED（diff 仅 2 个文件、纯 copy + focused proof 同步收口，职责集中，评审纪要 `.hermes/local-output/login-shared-console-copy-tightening-review-20260509.md` 已落盘）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/login-shared-console-copy-tightening-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类风险模式均 0 命中，且未改 auth/session/menu truth/permissions）；测试/可靠性 PASS（TDD RED→GREEN、focused route 回归、build、Go tests、真实 API 回放全部通过，既有 React `act(...)` / Semi `findDOMNode` 噪声未新增）；性能/运维 PASS（纯前端轻量收口，不新增请求，compose rebuild 失败仅源于外部 registry 超时，`docker compose ps --format json` 显示 `web` / `api` / `gateway` healthy）。下一前端优先项继续围绕登录/共享壳与 App focused 用例里剩余的低信号 copy / page-global 断言收口，再回到 Phase 4/5 其余后端能力.

### 最新执行检查点（2026-05-09，Login register guidance banner shared-console wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/LoginPage.tsx` 与 `web/src/pages/LoginPage.test.tsx` 做一个小而完整的前端 focused 收口切片，把注册模式 auth shell 内 helper 与 success banner 文案统一收口为更贴近 shared-console/new-api 单壳入口心智、且与真实导航命名一致的一句式：`注册后直接进入共享控制台，再沿同一导航前往项目市场、订单中心与 API Keys。`；同时补一个最小语义锚点 `login-auth-copy-block`，让 focused proof 显式覆盖 helper 与 banner 同步一致。保持注册/登录接口、`/auth/menu` 菜单真值、角色边界与 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、运行态与 `LoginPage.tsx` / `LoginPage.test.tsx` 回读现状，并先让 register-mode guidance focused proof RED。首轮仅把 banner 改成“项目、订单、API Keys”后，代码质量 reviewer fail-closed 指出新文案把真实导航名 `项目市场` / `订单中心` 泛化为过宽术语，且 helper 文案变更尚未被 focused proof 直接覆盖。控制器据此只做最小修正，不扩散到 auth/router/service/backend：把 helper 与 banner 一并改为 `注册后直接进入共享控制台，再沿同一导航前往项目市场、订单中心与 API Keys。`，并为 auth shell copy 区补充 `login-auth-copy-block` 语义锚点，在 `LoginPage.test.tsx` 中同时验证 helper/bannner 命中新文案且旧双句式文案消失。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（8 tests）；`pnpm --dir web exec vitest run src/App.test.tsx -t "uses the shared API keys route constant for register onboarding entrypoints|falls back to the docs workspace when no preferred role landing route exists|shows role-aware first-run mission cards after registration and navigates into the shared console|renders the login shell when unauthenticated|shows register journey CTA and opens register mode from the login shell"` 通过（5 tests）；随后 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面重跑本地忽略脚本 `.hermes/local-output/run_app_login_register_scope_real_api.sh`，最新产物目录 `.hermes/local-output/app-login-register-scope/20260509-154104`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`，普通用户动态注册、普通用户 `/auth/menu` / `/auth/me` / `/dashboard/overview` / `/auth/api-keys` / `/webhooks/endpoints`、管理员 `/auth/menu` / `/admin/overview` / `/admin/risk` / `/admin/audit?limit=5` 以及前端共享壳 `/`、`/profile`、`/settings`、`/api-keys`、`/webhooks`、`/docs`、`/admin/suppliers` 全部通过；`docker compose ps --format json` 继续显示 `web` / `api` / `gateway` healthy。
- 本轮五维评审结论：产品/规格 PASS（最终 wording 明确保留“共享控制台”“同一导航”心智，且与真实共享导航名 `项目市场` / `订单中心` / `API Keys` 对齐，没有把登录后体验误导为独立后台）；代码质量 APPROVED（首轮 reviewer 指出的术语漂移已同轮修正，diff 仍只限 2 个文件的 copy + focused proof，并新增最小 `login-auth-copy-block` 锚点保证 helper/banner 同步）；安全/集成 SAFE（静态扫描产物 `.hermes/local-output/login-register-banner-copy-static-scan-20260509.json` 显示 hardcoded secret / shell / eval / pickle / SQL 注入 5 类风险模式均 0 命中，且未改 auth/session/menu truth/permissions）；测试/可靠性 PASS（Login focused、相关 App focused register/bootstrap/docs/API Keys 路由回归、build、Go tests、compose config、diff-check 与真实 API 回放全部通过；既有 React `act(...)` / Semi `findDOMNode` / jsdom AggregateError 噪声未新增）；性能/运维 PASS（纯前端 copy/testid 收口，不新增请求、轮询、构建配置或运行态复杂度；前端 build 仅保留仓库既有 chunk-size warning）。下一前端优先项继续围绕 shared-console / App focused 套件中剩余低信号 copy、重复 page-global proof 与 new-api 单壳一致性补强做小切片收口，再回到 Phase 4/5 的 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下能力。

### 最新执行检查点（2026-05-09，ApiDocs shared-console bridge 文案收口）
- 本轮继续遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：仅对 `web/src/pages/ApiDocsPage.tsx` 与 `web/src/pages/ApiDocsPage.test.tsx` 做一个小而完整的前端 focused 收口切片，把 docs bridge / loop 两个区块的 wording 从较旧的“共享控制台联调桥 / 文档、业务与回调共用一套控制台 / 文档与接入工作台继续保持单壳闭环”收口为更紧凑、但仍明确保留 shared-console 单壳心智的“共享控制台接入桥 / 文档、接入与回调共用一套共享控制台 / 文档核对后继续沿共享控制台接入链路推进”；保持注册/登录接口、`/auth/menu` 菜单真值、角色边界与 Phase 4/5 后端契约全部不变。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、运行态与 `ApiDocsPage.tsx` / `ApiDocsPage.test.tsx` 回读现状，再先让 docs focused proof 对新 wording RED。首轮改动把 bridge wording 过度收口成“共享接入桥接 / 文档、接入与回调共用一套控制台 / 文档回放后继续沿共享接入链路推进”，随后五维评审中的产品/规格与代码质量 reviewer 正确 fail-closed：指出 shared-console canonical 叙事被削弱、且 loop 标题出现 title-vs-proof 漂移。控制器据此只做最小修正，不扩散到 route/menu/auth/service/backend：将运行时与 focused proof 一并改为“共享控制台接入桥 / 文档、接入与回调共用一套共享控制台 / 文档核对后继续沿共享控制台接入链路推进”，既保留单壳心智，也让 wording 与区块实际承载的 API Keys / Webhook / 项目市场 / 余额中心桥接内容保持一致。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 通过（5 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面重跑本地忽略脚本 `.hermes/local-output/run_app_docs_login_scope_real_api.sh`，最新产物目录 `.hermes/local-output/app-docs-login-scope/20260509-130536`：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`，普通用户动态注册、普通用户 `/auth/menu` / `/dashboard/overview` / `/auth/api-keys` / `/webhooks/endpoints`、管理员 `/admin/risk` / `/admin/audit`、供应商 `/supplier/resources/overview` / `/supplier/settlements` 以及前端共享壳 `/`、`/login`、`/docs`、`/api-keys`、`/webhooks`、`/admin/risk`、`/admin/audit` 全部通过；静态扫描产物 `.hermes/local-output/api-docs-bridge-copy-static-scan-20260509.json` 显示 5 类风险模式均 0 命中，五维评审纪要落盘 `.hermes/local-output/api-docs-shared-console-wording-review-20260509.md`。
- 本轮五维评审结论：产品/规格 PASS（最终 wording 明确保留 `共享控制台` canonical 心智，且 bridge / loop 仍忠实表达 docs→API Keys→Webhook→业务回放的一体化单壳路径）；代码质量 APPROVED（review 首轮指出 wording 漂移后，控制器已在同轮收口为更准确的 shared-console wording，diff 仍只限 2 个文件且运行时/测试合同同步）；安全/集成 SAFE（纯前端文案切片，静态扫描 0 命中，未改 auth/menu/route/backend contract）；测试/可靠性 PASS（TDD RED→GREEN、focused、build、Go tests、compose config 与真实 API 回放全部通过，既有 Semi `findDOMNode` 噪声未新增）；性能/运维 PASS（不新增请求或运行时复杂度，仅收口 UI wording，当前运行容器与真实 API 链路保持 healthy）。下一前端优先项继续围绕 `App.test.tsx` / 登录入口 / shared-console focused 用例里剩余的低信号 copy 与 page-global 断言收口，再回到 Phase 4/5 余下后端能力。


### 最新执行检查点（2026-05-10，Projects / Orders shared-console bridge + capability matrix 补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/ProjectsPage.tsx`、`web/src/pages/ProjectsPage.test.tsx`、`web/src/pages/OrdersPage.tsx`、`web/src/pages/OrdersPage.test.tsx` 做一个小而完整的前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，为项目市场与订单中心补齐与 ApiKeys / Webhooks / Profile / Admin 页面一致的深色 `共享接入桥接` 与 `控制台能力矩阵`，继续把“注册后采购 → 履约 → API 接入”留在同一套 new-api 风格共享控制台里表达，而不是退回散装任务卡或独立后台心智。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、运行中的 compose 服务、`ProjectsPage` / `OrdersPage` 及其 focused tests 回读现状，并先让新增 bridge/matrix 合同 RED。首轮 `pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx` 明确失败：`projects-shared-console-bridge` / `projects-capability-matrix` / `orders-shared-console-bridge` / `orders-capability-matrix` 运行时节点缺失，根因确认不是路由、真实 API、shared shell 或 `/auth/menu` 漂移，而只是这两个用户主链路页仍缺少与仓库现有单壳页面一致的 bridge + capability layer。随后仅最小修改 4 个前端文件：两页运行时代码分别新增右侧 bridge 卡与 capability matrix 卡，两页 focused tests 先 RED 后同步补齐 bridge 文案、矩阵标签与按钮导航 proof；期间还按失败输出把既有 lane / mission-flow 导航用例收紧为作用域内点击，避免新同名 CTA 引入多匹配噪声。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/pages/OrdersPage.test.tsx` 先 RED 后 GREEN，最终 2 files / 23 tests 全通过；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_projects_orders_bridge_real_api.sh`，最新产物目录 `.hermes/local-output/projects-orders-bridge-real-api/20260510-032719`（`latest.txt` 已更新）：再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；动态注册普通用户后 `GET /api/v1/auth/menu` 包含 `/projects`、`/orders`、`/api-keys`；`GET /api/v1/projects/inventory` 与 `GET /api/v1/orders/activations` 返回真实 JSON 契约；`GET /api/v1/auth/api-keys` 返回 200；`http://127.0.0.1:5173/projects` 与 `/orders` 均返回 200，证明本轮 bridge/matrix 所依赖的真实前后端链路仍可用。
- 本轮五维评审结论：产品/规格 PASS（Projects / Orders 现已显式呈现采购/履约与 API 接入桥接，不再只是列表+任务卡，继续强化“单一登录后控制台 + 角色差异菜单 + shared-console 接入链路”叙事）；代码质量 APPROVED（diff 仅集中在 2 个运行时页面与 2 个 focused tests，结构小、合同同步，且把新增同名 CTA 影响下的旧导航用例收紧为 scoped proof）；安全/集成 SAFE（静态 diff 未引入凭据、危险调用或权限漂移，bridge 按钮全部复用既有路由常量/菜单 gating，真实 API 再证 `/projects/inventory`、`/orders/activations`、`/auth/menu`、`/auth/api-keys` 契约稳定）；测试/可靠性 PASS（RED→GREEN focused、build、Go tests、compose config、diff-check 与真实 API 回放全部通过；现存 Semi UI `act(...)` 警告未新增为回归）；性能/运维 PASS（纯前端卡片补齐，不新增请求、轮询或部署复杂度，仅新增本地忽略 real-api 验证脚本用于后续复跑）。

### 最新执行检查点（2026-05-10，SupplierSettlements 控制台能力矩阵补齐）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/SupplierSettlementsPage.tsx` 与 `web/src/pages/SupplierSettlementsPage.test.tsx` 做一个小而完整的 supplier 前端垂直切片，在不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约的前提下，为供应商结算页 `共享接入桥接` 区补齐与 SupplierOfferings / SupplierDomains / ApiKeys 等页面一致的深色 `控制台能力矩阵`，继续把“单一登录后控制台 / 角色差异菜单 / shared-console bridge”显式化到资金与争议页，而不是退回独立供应商财务后台心智。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `SupplierSettlementsPage.tsx` / `SupplierSettlementsPage.test.tsx` 回读现状，并先让 focused proof 对新增 `supplier-settlements-capability-matrix` 合同 RED。首轮仅修改测试后，`pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx` 明确失败为运行时缺少 capability matrix；根因确认不是 `/supplier/settlements` 路由缺失、不是供应商真实 API 失败、不是 `/auth/menu` 漂移、也不是 shared-console bridge CTA 失真，而只是结算页 bridge 区仍缺少与仓库当前 new-api 深色单壳一致的 capability matrix。随后仅最小修改 2 个前端文件：运行时代码在 `共享接入桥接` 段落下新增深色矩阵卡，标签收口为 `统一结算入口 / 共享接入桥接 / 服务端菜单扩展`；focused test 同步新增该矩阵与 3 个标签合同断言。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx` 先 RED 后 GREEN，最终通过（8 tests）；`pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierOfferingsPage.test.tsx` 通过（26 tests，仅既有 React/Semi `act(...)` / form validation warning，无新失败）；`pnpm --dir web exec vitest run src/App.test.tsx src/pages/SupplierSettlementsPage.test.tsx` 首轮命中仓库既有 `uses the shared API keys route constant for register onboarding entrypoints` 异步噪声，按 fail-closed 对该单例 isolated rerun 后再次复跑整组恢复全绿（30 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。随后执行 `docker compose up -d --build web gateway` 完成前端重建并等待健康恢复，`GET /healthz` 返回 `200` 且 `{"status":"ok","redis":"ok"}`。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_supplier_settlements_capability_matrix_real_api.sh`，最新产物目录 `.hermes/local-output/supplier-settlements-capability-matrix-real-api/20260510-082211`：再次验证动态注册普通用户后 `/auth/menu` 暴露 `/projects`、`/orders`、`/api-keys`、`/webhooks`、`/settings`；seeded supplier `supplier@nexus-mail.local / Supplier123!` 登录后 `/auth/menu` 暴露 `/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs`，且 `GET /supplier/settlements`、`/supplier/reports?limit=5`、`/supplier/disputes?limit=5`、`/supplier/cost-profiles`、`/auth/api-keys`、`/webhooks/endpoints` 全部 `200`；前端 `/supplier/settlements`、`/supplier/resources`、`/api-keys`、`/webhooks`、`/docs` SPA 壳均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（新增矩阵继续强化“单一登录后控制台 + 角色差异菜单 + 共享接入桥接”叙事，没有把供应商结算页误导成独立财务后台，也未夸大未授权入口）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，结构小且合同同步，未引入额外状态或复杂抽象）；安全/集成 SAFE（静态扫描 5 类模式 0 命中，未改 auth/session/menu truth/backend path，且 matrix 本身不新增可点击入口，真实 CTA 继续受现有 menu gating 与 fallback 约束）；测试/可靠性 PASS（focused、supplier vertical slice grouped rerun、App 关联 rerun、build、Go tests、compose config、diff-check、容器重建后 health 与真实 API 回放全部通过，首轮 App 失败已确认为仓库既有异步噪声）；性能/运维 PASS（纯前端矩阵卡补齐，不新增请求、轮询、持久化或部署复杂度，构建仅保留仓库既有 chunk-size warning）。


### 最新执行检查点（2026-05-10，Login 入口 shared-console wording 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：聚焦 `web/src/pages/LoginPage.tsx` 与 `web/src/pages/LoginPage.test.tsx` 做一个小而完整的前端垂直切片，不改注册/登录接口、不改 `/auth/menu` 菜单真值、不改任何 Phase 4/5 后端契约，只把登录页 hero summary 从较抽象的 `共享壳` 收口为与当前仓库其余页面一致、也更贴近用户心智的 `共享控制台`，继续强化“统一入口 + 单一登录后控制台 + 角色差异菜单”的 new-api 风格叙事。
- 控制器按 TDD + `systematic-debugging` 先从 `git status`、`todo.md`、当前运行态与 `LoginPage` 运行时/focused test 现状回读，再先让 `LoginPage.test.tsx` 对新 summary wording RED。首轮仅修改 focused proof 后，`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx -t "renders a denser shared-console entry with a compact signal strip and product-style registration runway"` 明确失败为运行时仍渲染旧文案 `统一入口、共享壳与接入路径，在同一登录面完成切换。`；根因确认不是注册功能缺失、不是共享壳路由漂移、不是 `/auth/menu` 漂移，也不是真实 API 失败，而只是登录页顶部 summary wording 尚未与当前 shared-console canonical 叙事收口。随后仅最小修改 2 个前端文件：`LoginPage.tsx` 把 hero summary 更新为 `统一入口、共享控制台与接入路径，在同一登录面完成切换。`，`LoginPage.test.tsx` 同步把正向断言切到新文案、并把负向断言收紧为旧文案必须消失，不扩散到 auth/store/router/service/backend。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 先 RED 后 GREEN，最终通过（8 tests）；`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（30 tests，仅既有 React `act(...)` / Semi `findDOMNode` warning，无新增失败）；`pnpm --dir web build`、`go test ./...`、`docker compose config` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_login_polish_real_api.sh`，最新产物目录已记录到 `.hermes/local-output/login-polish-real-api-latest.txt`，再次验证 `GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`，动态注册普通用户后 `/auth/menu` 暴露 `/projects`、`/orders`、`/api-keys`、`/webhooks`、`/settings`，且 `/auth/me`、`/auth/api-keys`、`/webhooks/endpoints` 与前端 `/login` 壳均返回成功，证明本轮 wording 收口未破坏真实注册、共享菜单与单壳入口链路。
- 本轮五维评审结论：产品/规格 PASS（`共享控制台` 比 `共享壳` 更贴近当前单一登录后控制台叙事，且未夸大任何未开放能力）；代码质量 APPROVED（diff 仅集中在 1 个运行时页面与 1 个 focused test，修改粒度最小，正负向合同同步）；安全/集成 SAFE（纯前端 wording/test 收口，未改 auth/session/menu truth、注册语义或路由常量，真实 API 再证登录后链路稳定）；测试/可靠性 PASS（RED→GREEN focused、App 关联回归、build、Go tests、compose config 与真实 API 回放全部通过，既有 warning 未恶化）；性能/运维 PASS（纯文案切片，不新增请求、轮询、持久化或部署复杂度，bundle/build 行为仅保持仓库既有 chunk warning）。


## 0. 项目目标

构建一个面向商业化运营的 **邮件接码平台**，核心产品逻辑对齐成熟 **SIM 接码平台**，但资源从“手机号 / 号码池”切换为“域名 / 邮箱别名 / 邮件接收能力”。

### 明确目标
- 后端：**Go**
- 前端：**React**
- 数据存储：
  - 高性能持久存储：**PostgreSQL**
  - 缓存数据库：**Redis**
- 编译与部署：**必须使用 Docker**
- UI 风格：参考 **QuantumNous/new-api**
- 权限模型：**同一套系统页面 + 同一套布局骨架**，仅通过菜单和路由权限扩展区分用户 / 供应商 / 管理员，逻辑对齐 new-api 的共享控制台模式
- 单独页面：必须提供 **API 文档页面**
- 商业目标规模：
  - 注册用户：**1 万+**
  - 并发：**1000+**

---

## 1. 已完成调研结论（用于本计划）

## 1.1 new-api UI / 权限模式结论
基于公开资料调研，`new-api` 的前端特征可提炼为：

- 使用 **React + Vite**
- UI 组件偏向 **Semi Design (`@douyinfe/semi-ui`)** 风格
- 采用 **单一共享控制台布局**：侧边栏 + 顶栏 + 内容区
- 非“用户端 / 管理端”双前端拆分，而是在 **同一前端壳** 中按角色决定可见菜单与路由
- 管理员角色通过菜单项追加、路由守卫与权限控制显示更多能力

### 对 Nexus-Mail 的直接落地要求
- 只维护 **一个 React 控制台项目**，不拆成三个独立后台
- 通过角色与权限声明控制菜单：
  - 普通用户菜单
  - 供应商扩展菜单
  - 管理员扩展菜单
- 所有角色共享相同 Layout：
  - `Sidebar`
  - `Topbar`
  - `Content`
  - `Breadcrumb`
  - `GlobalNotice / Toast`
- 严禁做三套皮肤逻辑分叉；差异应体现在：
  - 菜单树
  - 页面权限
  - 页面中局部操作按钮

---

## 1.2 架构与中间件调研结论
结合商业项目目标与 1000+ 并发要求，推荐基础设施：

### 核心推荐
- **Go 后端**：高并发 API 与异步处理主服务
- **PostgreSQL**：主业务数据库，系统唯一事实源（system of record）
- **PgBouncer**：PostgreSQL 连接池层，避免高并发直接打爆数据库连接
- **Redis**：缓存、会话、限流、幂等键、短生命周期状态
- **RabbitMQ（Quorum Queues）**：可靠异步任务队列
- **MinIO**：原始邮件 MIME、附件、审计快照等对象存储
- **OpenAPI + Redoc**：单独 API 文档页面
- **Postfix**：公网 25 端口 SMTP 接收边缘
- **Go Mail Processor**：在 Postfix 后方做业务解析，而不是直接把 Go API 暴露成公网 SMTP 第一跳

### 主流邮箱供应商接入结论
供应商资源不能只局限于“自建域名邮箱”，还必须纳入主流公网邮箱与商业邮箱账号池。规划上应支持：

- 自建域名 / 自建邮局
- Google 体系：Gmail、Google Workspace
- Microsoft 体系：Outlook.com、Hotmail、Live、Microsoft 365
- Apple：iCloud Mail
- Yahoo 体系：Yahoo Mail、AOL Mail
- 中国公网邮箱：QQ 邮箱、163 邮箱、126 邮箱、yeah 邮箱
- 商业邮箱：Zoho Mail、Fastmail、Yandex Mail、GMX、Mail.com
- 特殊邮箱：Proton Mail（通过 Proton Bridge）

### 接入方式结论
系统设计上必须原生支持以下授权 / 协议模式：
- `MX / SMTP inbound`
- `IMAP pull`
- `POP3 pull`
- `OAuth2`
- `App Password`
- `授权码`
- `Bridge / 本地代理`

### 统一资源建模结论
为了避免后续实现混乱，必须统一采用以下分类：
- `source_type`
  - `self_hosted_domain`
  - `hosted_mailbox`
  - `public_mailbox_account`
  - `bridge_mailbox`
- `auth_mode`
  - `password`
  - `oauth2`
  - `app_password`
  - `authorization_code`
  - `bridge_local_credential`
- `protocol_mode`
  - `smtp_inbound`
  - `imap_pull`
  - `pop3_pull`
