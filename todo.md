# Nexus-Mail 完整开发计划（todo.md）

> **执行模式已切换：** 本文档不再只是规划稿，而是当前仓库的**执行中路线图**。后续提交必须对照阶段目标更新状态，并在阶段完成后进入下一阶段。

## 执行状态看板（持续维护）

### 当前总体进度
- Phase 0：**已完成**（开发底座、Docker、多服务骨架已提交）
- Phase 1：**已完成**（身份权限、共享控制台骨架已提交）
- Phase 2：**已完成**（项目/库存/激活订单主链路、管理员项目配置、供应商资源录入已提交）
- Phase 3：**已完成**（已完成 SMTP 会话落盘、metadata 入库、RabbitMQ 解析任务入队、MinIO 原始对象上传，并打通验证码/link 提取回退增强、订单 READY/FINISHED 自动迁移、真实 OAuth2 刷新接入、授权码/App Password/密码型凭证录入与健康状态落库、`env://` secret_ref 解析、IMAP/POP3 实际登录校验、官方邮箱端点限制、Proton Bridge 接入策略与 Postfix 转发样例）
- Phase 4：进行中（已完成用户钱包、冻结余额、成功扣费、超时退款、供应商待结算余额、管理员调账能力与基础结算页面；已完成供应商报表筛选、供货规则维护、管理员争议单显式筛选、供应商域名运营页、管理员供应商运营页与共享控制台深色单壳导航收敛；已完成 Webhooks / 风控 / 审计真实页面收敛与对应真实 API 回放验证；当前前端后续重点继续围绕 shared-console focused 用例去脆弱化、语义锚点收敛与 new-api 风格单壳一致性补强，再回到 Phase 4/5 余下后端能力）
- Phase 5：进行中（已完成管理员高危操作二次确认、OpenAPI + `/docs`、管理员审计查询、管理员概览/风控真实页、API Key 默认 scopes / 白名单 / Redis 全局限流 / 审计、Webhook 异步投递与 delivery 查询、风控规则 CRUD 等里程碑；后续在“前端先行、真实 API 验证、五维评审后提交”的约束下继续推进 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片）
- Phase 6：未开始

### 本机环境现状（2026-04-13 更新）
- `go version`：可用
- `docker version`：可用
- `docker compose version`：可用
- `pnpm -v`：可用
- 本机已验证可绑定 **25 端口**

### 本地参考资料
- 已在项目目录放置竞品调研参考：`docs/reference-local/luckyous-reference.md`
- 该目录已加入本地 Git 排除规则，**仅作为本机开发参考，不推送远程仓库**

### 最新执行检查点（2026-05-20，DashboardPage 共享目标页 named-region 前端合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/DashboardPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 dashboard 运行时代码、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有 Dashboard 的共享接入桥接与预算→采购→履约→接入首轮路径已成型的前提下，把余额中心 / 项目市场 / 订单中心 / API Keys 这些目标页 proof 从仅命中 route-stub test-id 收口为命名 `共享控制台 - *` destination region 合同，并把 bridge 抑制用例同步收口到已存在的 `共享接入桥接` 命名 region。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/DashboardPage.tsx` / `web/src/pages/DashboardPage.test.tsx`，确认根因不是 dashboard 首轮路径 CTA 缺失、不是 API Keys/项目/订单/余额落地异常、不是 shared-console 叙事漂移、也不是真实 API 不一致，而只是 focused suite 里 4 个 destination stubs 仍仅暴露 `data-testid`，对应导航断言也仍停留在 `findByTestId(...) + 全局 heading` 层；同时 bridge 抑制用例还在通过 `dashboard-shared-console-bridge` test-id 而不是已存在的命名 `共享接入桥接` region 做约束。随后先在 `DashboardPage.test.tsx` 制造 RED：为 balance / projects / orders / api-keys route stubs 补上 `role="region" + aria-label="共享控制台 - *"`，把导航用例标题改为更准确的 named destination region 合同，并将四段导航断言升级为 `findByRole('region', { name: ... })` 后在对应 region 内验证 heading；bridge 抑制用例则改为直接查询 `getByRole('region', { name: '共享接入桥接' })`。未改任何运行时代码、未改后端接口、未改 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx` 通过（10 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings）；`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx src/App.test.tsx` 通过（33 tests，仅既有 shared-shell / Semi warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/dashboard-route-region-real-api-run.sh`，最新产物目录 `.hermes/local-output/dashboard-route-region-real-api/20260520-111147`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 且不泄漏 `/admin/users`、`/supplier/resources`、`/supplier/offerings`；seeded admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（Dashboard 首轮路径目标页 proof 现直接保护 `共享控制台 - 余额中心 / 项目市场 / 订单中心 / API Keys` 命名 region，bridge 抑制断言也回到已有 `共享接入桥接` region，不再只停留在 test-id 命中）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，selector 从 test-id/全局 heading 收口为 region + in-region heading 合同，命名与断言范围一致）；安全/集成 SAFE（纯前端测试与 route-stub 语义补强，不触碰 auth/session/menu/backend contract；真实 API 回放再次证明 user/supplier/admin 菜单与 dashboard/shared routes 鉴权链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，导航断言更贴近用户可见合同且未引入新的 async/navigation 脆弱点）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，LoginPage 注册跑道命名目标 region 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/LoginPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改注册接口、不是改 `/auth/menu` 真值、不是改共享控制台目的页运行时代码，而是在既有注册跑道已经具备 `共享控制台 - API Keys / Webhooks / API 文档` 命名 region 测试桩的前提下，把残余 1 条仍依赖 `findByTestId(...)` 的 CTA 导航 proof 收口为命名 destination region 合同，并同步把用例标题从“into ... stubs”校正为更准确的“inside the named shared-console destination regions”。
- 控制器按 `systematic-debugging` + TDD 先回读 `web/src/pages/LoginPage.tsx` / `web/src/pages/LoginPage.test.tsx`，确认根因不是 LoginPage 运行时代码缺失、不是注册功能错误、不是 API Keys/Webhooks/Docs 落地异常、不是 shared-console 单壳叙事漂移、也不是真实 API 不一致，而只是 `keeps register CTA routing from the product runway cards into API Keys, Webhooks, and Docs stubs` 这条 focused 用例仍使用 `findByTestId('login-route-stub-*')`，与同文件其余已升级到 `findByRole('region', { name: '共享控制台 - *' })` 的目标页合同不一致。随后仅最小修改 `web/src/pages/LoginPage.test.tsx`：把该用例标题改为 `keeps register CTA routing from the product runway cards inside the named shared-console destination regions`，并将三处断言同步收口为 `findByRole('region', { name: '共享控制台 - API Keys' | '共享控制台 - Webhooks' | '共享控制台 - API 文档' })`；未改任何运行时代码、未改后端接口、未改 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（12 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings）；`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（35 tests，仅既有 shared-shell / Semi warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/login-route-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/login-route-region-real-api/20260520-102628`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 且不泄漏 `/admin/users`、`/supplier/offerings`、`/supplier/resources`；seeded admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（delegate reviewer 确认新标题与断言范围一致，且 destination-region 命名合同比“route stub 存在”更贴近共享控制台真实用户语义）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，断言从 `findByTestId` 收口为 `findByRole('region', ...)`，无作用域漂移）；安全/集成 SAFE（静态 added-line 扫描无硬编码秘密、shell/eval/SQL 风险；纯前端测试补强，不触碰 auth/session/menu/backend contract；真实 API 回放再次证明注册、菜单与 shared routes 鉴权链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新断言与同文件其他 destination-region proof 保持一致，未引入新的 async/navigation 脆弱点）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，WebhooksPage 命名 destination region 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/WebhooksPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 Webhooks 运行时代码、不是改 `/auth/menu` 真值、不是新增独立 webhook 后台，而是在既有 `共享接入桥接` / 首轮联调 lane 已经成型的前提下，把 API Keys、API 文档与共享首页这几类落点 proof 继续从 route-stub test-id 命中收口为命名 shared-console destination region 合同，并同步把用例标题校正为更诚实的 region 目标叙述。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/WebhooksPage.tsx` / `web/src/pages/WebhooksPage.test.tsx`，确认根因不是 bridge CTA 缺失、不是 docs/home/api-keys 落地异常、不是 shared-console 叙事漂移、也不是真实 API 不一致，而只是多条 focused 用例仍使用 `findByTestId('webhooks-route-stub-*')` 与 `getByTestId('webhooks-shared-console-bridge')` 证明导航结果，尚未直接保护“点击后进入的是命名 shared-console destination region”这一更真实的前端合同。随后先在 `WebhooksPage.test.tsx` 制造 RED：为 API Keys / Docs route stubs 补上 `role="region" + aria-label`，并把 fallback、docs-only、first integration loop 相关用例升级为 `findByRole('region', { name: '共享控制台首页' | '共享控制台 - API 文档' | '共享控制台 - API Keys' })` 后再在该 region 内验证 heading；未改任何运行时代码、未改后端接口、未改 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx` 通过（19 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings）；`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/pages/ApiKeysPage.test.tsx src/App.test.tsx` 通过（56 tests，仅既有 shared-shell / Semi warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/webhooks-route-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/webhooks-route-region-real-api/20260520-104913`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 且不泄漏 `/admin/users`、`/supplier/resources`、`/supplier/offerings`；seeded admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（bridge/fallback/first-loop 用例现直接保护 `共享控制台首页`、`共享控制台 - API 文档`、`共享控制台 - API Keys` 命名 destination region，标题与 proof 范围一致，不再高估为宽泛 route/stub 命中）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，selector 从 test-id 向 named region/heading 收口，与仓库既有 shared-console 语义测试风格一致）；安全/集成 SAFE（纯前端测试语义补强，不触碰 auth/session/menu/backend contract；真实 API 回放再次证明 user/supplier/admin 菜单、API Keys、Webhooks、Docs 与共享首页链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，route-stub proof 向命名 region 收口后更贴近用户可见合同且未新增 async/navigation 脆弱点）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，ApiKeysPage 共享目标页命名 region 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/ApiKeysPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 API Key 运行时代码、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有 `ApiKeysPage` 的 `共享接入桥接` / fallback 合同基础上，把 Webhooks / Docs / 共享首页这三类 route stub proof 从 `findByTestId(...)` 继续收口为命名 shared-console region + heading 合同，并补上 bridge 内 `控制台能力矩阵` heading 护栏，让接入桥接目标页 proof 更贴近真实可访问树。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ApiKeysPage.tsx` / `web/src/pages/ApiKeysPage.test.tsx`，确认根因不是 API Keys 页面 bridge CTA 缺失、不是 fallback route 失效、不是 role-aware Webhook 目标页标题漂移、也不是真实 API 不一致，而只是 focused suite 里 Webhooks / Docs / Dashboard 落地 proof 仍主要依赖 `route-stub-*` test id，尚未直接保护“命中的仍是共享控制台命名 destination region”这一前端合同；同时 bridge 主断言也还没有显式保护 `控制台能力矩阵` heading 继续留在 bridge card 内。随后先在 `ApiKeysPage.test.tsx` 制造 RED：为 `route-stub-webhooks`、`route-stub-docs`、`route-stub-dashboard` 增补 `role="region" + aria-label`，把 bridge 导航断言升级为 `findByRole('region', { name: '共享控制台 - Webhooks' | '共享控制台 - API 文档' | '共享控制台首页' })` 后再校验 heading，并把 fallback 用例标题同步收口为 shared-console home region proof；未改任何运行时代码、未改后端接口、未改 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 通过（14 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings）；`pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/pages/ProfilePage.test.tsx src/pages/SettingsPage.test.tsx src/App.test.tsx` 通过（53 tests，仅既有 shell / Semi warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/api-keys-route-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/api-keys-route-region-real-api/20260520-100233`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/api-keys`、`/webhooks`、`/docs` 且不泄漏 `/admin/users`、`/supplier/resources`、`/supplier/offerings`；seeded admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/api-keys`、`/docs`、`/webhooks` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（ApiKeysPage bridge / fallback 目标页 proof 现直接保护 `共享控制台 - Webhooks / API 文档 / 首页` 命名 region，不再只停留在 route-stub test id 命中，且新增 `控制台能力矩阵` heading 护栏更诚实地绑定 bridge 内语义）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件；命名与断言范围一致，selector 从 test-id 向 region/heading 收口更贴近仓库既有 shared-console 约定）；安全/集成 SAFE（纯前端测试与 route-stub 语义补强，不触碰 auth/session/menu/backend contract；真实 API 回放再次证明 user/supplier/admin 菜单、API Keys、Webhooks、Docs 与共享首页链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新断言比裸 test-id 更接近用户可见合同，没有引入新的 async/routing 脆弱点）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。


### 最新执行检查点（2026-05-20，LoginPage 注册跑道 route-stub 测试命名收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/LoginPage.test.tsx` 这一处已在工作树中的 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改注册接口、不是改 `/auth/menu` 真值、不是改共享控制台落地页运行时代码，而是在既有注册跑道目标页 region 合同已经成型的前提下，把其中 1 条 focused 用例标题从过宽的“login runway / shared-console shell contract”表述收口为更准确的“来自 registration journey 的 shared-console route stubs region-scoped proof”，让测试标题与真实断言范围完全一致。
- 控制器按 `systematic-debugging` + TDD 先回读 `web/src/pages/LoginPage.test.tsx`，确认根因不是 LoginPage 运行时错误、不是 API Keys/Webhooks/Docs 落地错误、不是注册功能缺失、也不是真实 API 不一致，而只是已有用例 `keeps login runway destination stubs inside the same shared-console shell contract after navigating to API Keys, Webhooks, and Docs` 实际断言的是：从 `首轮接入路径` region 点击 3 个 CTA 后，命中 3 个具名 shared-console route stub regions，并校验各 region 内 heading / copy 与负向 shell 文案；因此旧标题中 `login runway` 与过宽的 `same shared-console shell contract` 容易高估测试覆盖面。随后仅最小修改 `web/src/pages/LoginPage.test.tsx`：把该用例标题改为 `keeps the shared-console route stubs region-scoped when opening API Keys, Webhooks, and Docs from the registration journey`，不改任何运行时代码、不改断言内容、不改后端接口或路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（12 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/login-frontslice-real-api/run.sh`，最新产物目录 `.hermes/local-output/login-frontslice-real-api/20260520-093736`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 且不泄漏 `/admin/users`、`/supplier/offerings`；seeded admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（新标题更精确地描述“注册跑道触发的 shared-console route stub region proof”，不再高估为更宽泛的 shell contract）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，且 reviewer 确认新标题与断言范围一致）；安全/集成 SAFE（纯测试命名收口，不触碰 auth/session/menu/backend contract；真实 API 回放再次证明注册、菜单与 shared routes 鉴权链路稳定）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新标题降低了测试名与真实 proof 漂移风险）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 测试语义与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，SupplierOfferingsPage shared-console 目标页 region 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/SupplierOfferingsPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 supplier offerings 运行时代码、不是改 `/auth/menu` 真值、不是新增独立供应商后台，而是在既有供应商供货规则页已经完成深色 single-shell / bridge / capability matrix 的前提下，把 API Keys / Webhooks / Docs / 共享首页这些目标页 proof 继续从 test-id 命中收口为命名 region 合同，保持供应商供给链路与共享接入链路都留在同一套控制台叙事中。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/SupplierOfferingsPage.tsx` / `web/src/pages/SupplierOfferingsPage.test.tsx` 与周边 supplier shared-console 页面，确认根因不是供货规则页 bridge CTA 缺失、不是 fallback route 失效、不是 supplier 菜单漂移、也不是真实 API 不一致，而只是 focused suite 对 API Keys / Webhooks / Docs / dashboard 落地 proof 仍主要依赖 route stub `data-testid`，尚未直接保护“这些落地面仍是共享控制台命名 region，而不是仅测试桩节点存在”这一前端合同。随后先在 `SupplierOfferingsPage.test.tsx` 制造 RED：为 dashboard shared-home stub 增补 `role="region" aria-label="共享控制台首页"`，把两处 fallback 导航断言升级为 `findByRole('region', { name: '共享控制台首页' })`，并把既有 mission-flow 导航用例扩展为同时覆盖 bridge 内的 Webhook / Docs CTA 落地；未改任何运行时代码、未改后端接口、未改 supplier/shared route 真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx` 通过（6 tests，仅既有 React/Semi `findDOMNode` 与表单校验 warnings）；`pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/pages/SupplierResourcesPage.test.tsx src/pages/SupplierSettlementsPage.test.tsx src/App.test.tsx` 通过（50 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/supplier-offerings-shell-real-api/run.sh`，最新产物目录 `.hermes/local-output/supplier-offerings-shell-real-api/20260520-064925`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview` 且普通用户菜单继续暴露 `/api-keys`、`/webhooks`、`/docs` 而不泄漏 `/supplier/offerings`；seeded supplier 使用 `supplier@nexus-mail.local / Supplier123!` 登录后 `/auth/menu`、`/supplier/resources/overview`、`/supplier/resources/offerings`、`/supplier/settlements` 全部返回 `200`，并显式确认 supplier 菜单保留 `/supplier/resources`、`/supplier/offerings`、`/supplier/settlements`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/supplier/offerings`、`/api-keys`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（SupplierOfferings focused 合同现直接保护 supplier 供货规则页通向 `开发者 API 接入工作台 / 供给事件回调工作台 / API 文档与接入控制台 / 共享控制台首页` 的 shared-console region 落地，不再只停留在 test-id 命中）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，未把语义补强误扩散到运行时代码；新增断言与既有 supplier shared-console bridge/fallback 风格一致）；安全/集成 SAFE（纯前端测试与 stub 语义补强，不触碰 auth/session/menu/backend contract；真实 API 回放再次证明 user/supplier/admin 菜单、supplier offerings/resources/settlements 与 shared routes 鉴权链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增命名 region 查询比裸 test-id 更贴近用户可见合同）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，SettingsPage 当前登录会话 region 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/SettingsPage.tsx`、`web/src/pages/SettingsPage.test.tsx` 与 `web/src/App.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 settings runtime route/menu、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有设置页深色 shared-console 工作台里，把“当前登录会话”卡片从 Card title / test-id proof 收口为具名 named region + 真实 heading 合同，继续对齐 Dashboard / Login / AppSidebar / ConsoleLayout 已推进的单壳语义锚点。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/SettingsPage.tsx`、`web/src/pages/SettingsPage.test.tsx` 与 `web/src/App.test.tsx`，确认根因不是设置页内容错误、不是首轮引导 reopen/fallback 失效、不是 shared-console bridge 漂移、也不是真实 API 不一致，而只是 settings session card 仍停留在 `title="当前登录会话"` 与 `getByTestId('settings-session-card')` 层面的弱 proof，尚未直接保护“这是共享控制台中的具名会话 region，而不是仅某个实现容器”这一前端合同。随后先在 `SettingsPage.test.tsx` / `App.test.tsx` 制造 RED：把 session card 查询升级为 `getByRole('region', { name: '当前登录会话' })`，并要求 region 内存在同名 heading；再最小修改 `SettingsPage.tsx`，仅为该卡片补上 `role="region"`、`aria-labelledby="settings-session-card-heading"` 与 `Typography.Title heading={5}`，不改任何后端接口、菜单计算或 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/App.test.tsx` 通过（30 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；更广一轮 `pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/pages/SettingsPage.test.tsx src/components/AppSidebar.test.tsx src/components/ConsoleLayout.test.tsx src/App.test.tsx` 通过（51 tests）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/settings-session-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/settings-session-region-real-api/20260520-062653`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`；supplier/admin seeded 登录后 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 均返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/settings`、`/api-keys`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（settings session card 现直接保护“当前登录会话”命名 region，而不是仅 test-id / Card title 命中，shared-console 单壳语义更诚实）；代码质量 APPROVED（运行时代码仅集中在 1 个卡片容器与 2 个 focused 测试文件，diff 极小且与仓库既有 named-region 模式一致）；安全/集成 SAFE（纯前端语义补强，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明注册、菜单与管理端链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增 region 查询更贴近用户可见合同）；性能/运维 PASS（零运行时代码/部署开销，仅补语义锚点与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，ApiDocsPage 路由目标 named-region 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/ApiDocsPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 `/docs` 运行时代码、不是改 `/auth/menu` 真值、不是新增独立文档后台，而是在既有 docs bridge / continuation lane 已能命中项目市场、API Keys、Webhook、余额中心与共享首页的前提下，把这些目标页 proof 从“仅命中 test-id stub”补强为“命中命名 shared-console region + heading”的可访问合同，继续对齐此前 LoginPage / ProfilePage 已推进的 shared-console 目标页语义锚点。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ApiDocsPage.test.tsx` 与 `web/src/pages/ApiDocsPage.tsx`，确认根因不是 `/docs` 页面 bridge/loop CTA 失效、不是 route 常量漂移、不是 shared-console homepage fallback 错误、也不是真实 API 不一致，而只是 ApiDocs focused suite 对项目市场、余额中心、API Keys、Webhook 四类目标页仍部分停留在 `findByTestId(...)` proof：虽然已经证明点击能命中 stub，但尚未直接保护“命中的仍是共享控制台内的命名目的页 region，而不是仅测试节点存在”这一前端合同。随后先在 `ApiDocsPage.test.tsx` 制造 RED：为项目市场 / 余额中心 / API Keys / Webhooks route stubs 增补 `role="region" + aria-label`，并把 bridge/loop 相关导航断言升级为 `findByRole('region', { name: '共享控制台 - *' })` 后再在该 region 内验证 heading；未改任何运行时代码、未改后端接口、未改 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 通过（6 tests，仅既有 React/Semi `findDOMNode` warning）；`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/pages/LoginPage.test.tsx src/pages/ProfilePage.test.tsx` 通过（27 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/apidocs-route-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/apidocs-route-region-real-api/20260520-053213`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`，并显式确认 `/auth/menu.items` 继续暴露 `/docs`、`/api-keys`、`/webhooks`；seeded admin 使用 `admin@nexus-mail.local / Admin123!` 登录返回 `200`，且 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 均返回 `200`，继续确认 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks` 保留在管理端菜单中；前端 `http://127.0.0.1:5173/`、`/login`、`/docs`、`/api-keys`、`/webhooks` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（docs bridge/loop 目标页 proof 现直接保护 `共享控制台 - 项目市场 / 余额中心 / API Keys / Webhooks` 命名 region，不再只停留在 test-id 命中）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件；review 仅提出可选 helper 去重，未构成阻塞）；安全/集成 SAFE（纯前端测试与 stub 语义补强，不触碰 auth/session/menu/backend contract；真实 API 回放再次证明注册、菜单、docs/API Keys/Webhooks/admin 链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增 region 查询比裸 test-id 更贴近用户可见合同）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，AdminUsersPage shared-console 目标页 region 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/AdminUsersPage.shared-console.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改管理员钱包/争议/结算运行时代码、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有 `AdminUsersPage` shared-console 桥接与 fallback 已能命中 API Keys / Webhooks / Docs / 共享首页的前提下，把这些目标页 proof 从 `findByTestId(...)` 收口为命名 region 合同，继续对齐前序 Login/Profile/ApiDocs/Settings 的单壳目的页语义锚点。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/AdminUsersPage.shared-console.test.tsx`、`web/src/pages/AdminUsersPage.tsx` 与现有 admin shared-console bridge/fallback 结构，确认根因不是 bridge CTA 丢失、不是 fallback route 失效、不是 admin shared routes 漂移、也不是真实 API 不一致，而只是 focused suite 对 API Keys / Webhooks / Docs / Dashboard 落地 proof 仍停留在 `data-testid` route stub 命中，尚未直接保护“命中的仍是共享控制台命名 region，而不是仅某个测试桩节点”这一前端合同。随后先在 `AdminUsersPage.shared-console.test.tsx` 制造 RED：为四个 route stub 增补 `role="region" + aria-label`，并把 bridge / fallback 导航断言升级为 `findByRole('region', { name: ... })` 后再校验 heading；未改任何运行时代码、未改后端接口、未改角色菜单真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx` 通过（5 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web exec vitest run src/pages/AdminUsersPage.shared-console.test.tsx src/pages/AdminUsersPage.test.tsx src/App.test.tsx` 通过（33 tests，仅既有 React/Semi warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/admin-users-shared-shell-real-api/run.sh`，最新产物目录 `.hermes/local-output/admin-users-shared-shell-real-api/20260520-055831`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/api-keys`、`/webhooks`、`/docs` 且不会泄漏 `/admin/users`；seeded admin 使用 `admin@nexus-mail.local / Admin123!` 登录返回 `200`，且 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 均返回 `200`，继续确认 `/admin/users`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs` 保留在管理端菜单中；前端 `http://127.0.0.1:5173/`、`/login`、`/admin/users`、`/api-keys`、`/webhooks`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（AdminUsers bridge / fallback 目标页 proof 现直接保护 `共享控制台 - API Keys / Webhooks / API 文档` 与 `共享控制台首页` 命名 region，不再只停留在 test-id 命中）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，命名与断言范围一致，未误扩散到运行时代码）；安全/集成 SAFE（纯前端测试与 stub 语义补强，不触碰 auth/session/menu/backend contract；真实 API 回放再次证明 admin/users 与 shared routes 的菜单/鉴权链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增 region 查询更贴近用户可见 shared-console 合同）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，ProfilePage 共享接入目标页 region 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/ProfilePage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 Profile 运行时代码、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有 `共享接入桥接` CTA 已能跳转 API Keys / Webhook / Docs 的前提下，把三个目标页 stub 统一补强为命名 `共享控制台 - *` region 合同，让 bridge click proof 不再只停留在 `findByTestId(...)`，同时保留 supplier/admin 的角色化 Webhook 目标页标题断言，继续对齐单一登录后控制台 / new-api 单壳语义与角色差异菜单合同。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ProfilePage.tsx` / `web/src/pages/ProfilePage.test.tsx`，确认根因不是 `/profile` 页面导航错误、不是 shared bridge CTA 缺失、不是 supplier/admin Webhook 角色标题漂移、也不是真实 API 不一致，而只是 Profile focused suite 里的 API Keys / Webhook / Docs 目标页 proof 仍主要依赖 route stub `data-testid`，尚未直接保护“命中的仍是共享控制台目的页 region”这一可访问语义。随后先在 `ProfilePage.test.tsx` 制造 RED：为 API Keys / Webhooks / Docs route stubs 增补 `role="region" + aria-label`，并把 supplier/admin Webhook 目标页断言升级为 `findByRole('region', { name: '共享控制台 - Webhooks' })` 后再校验 `resolveRouteTitle(...)`；同时将两条 webhook 用例标题改为明确“保留角色化目标页标题于 shared-console route region” 的真实合同。未改任何运行时代码、未改后端接口、未改 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 通过（9 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings）；`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/SettingsPage.test.tsx src/pages/LoginPage.test.tsx src/pages/ApiDocsPage.test.tsx` 通过（34 tests，仅既有 React/Semi warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/profile-shared-shell-real-api/run.sh`，最新产物目录 `.hermes/local-output/profile-shared-shell-real-api/20260520-050543`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`，并显式确认 `/auth/menu.items` 继续暴露 `/`、`/profile`、`/api-keys`、`/webhooks`、`/docs`；前端 `http://127.0.0.1:5173/profile`、`/login`、`/api-keys`、`/docs` 均返回 `200`；seeded admin 使用 `admin@nexus-mail.local / Admin123!` 登录返回 `200`，且 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 均返回 `200`，继续确认 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs` 保留在管理端菜单中。
- 本轮五维评审结论：产品/规格 PASS（Profile bridge 目标页 proof 现直接保护 `共享控制台 - API Keys/Webhooks/API 文档` 命名 region，同时 supplier/admin 仍保留角色化 Webhook 目标页标题断言，不再只停留在 test-id 命中）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，且在 reviewer 指出标题/断言不一致后已修正为真实命名与角色合同）；安全/集成 SAFE（纯前端测试与 stub 语义补强，不触碰 auth/session/menu/backend contract；真实 API 回放再次证明注册、菜单、Profile/API Keys/Webhooks/Docs/admin 链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增 region 查询提升了目的页语义锚点而未削弱角色标题覆盖）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，ProfilePage hero region 命名语义收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口当前工作树中尚未提交的 `web/src/pages/ProfilePage.tsx` / `web/src/pages/ProfilePage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 `/auth/menu` 真值、不是新增独立后台、不是推进新的后端接口，而是在既有 Profile hero 已经承载“个人资料中枢”文案的前提下，把该 hero 从 test-id/裸 heading proof 收口为命名 `region + aria-labelledby` 合同，并把相关路由桩断言统一升级为共享控制台目标页 region proof，使 shared-console 单壳语义与 CTA 落地 proof 更贴近真实可访问树。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ProfilePage.tsx`、`web/src/pages/ProfilePage.test.tsx` 与当前 diff，确认根因不是 Profile 导航失效、不是 supplier/admin Webhook 角色标题漂移、不是 settings fallback route 失联、也不是真实 API 不一致，而只是 Profile hero 仍未暴露稳定 landmark，focused suite 对 hero 与 settings/API Keys/Webhooks/Docs 目标页也仍混用 `data-testid` 与弱命名 proof。随后保持最小切片：在 `ProfilePage.tsx` 仅为 hero card 增补 `role="region"`、`aria-labelledby="profile-hero-card-heading"` 与 heading id；在 `ProfilePage.test.tsx` 仅把 hero、API Keys/Webhooks/Docs 目标页与 settings fallback 断言升级为 `find/getByRole('region', { name: ... })` 后再校验对应 heading，不改任何后端接口、角色菜单逻辑或 shared-console route 常量。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 通过（9 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings）；`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/SettingsPage.test.tsx src/pages/LoginPage.test.tsx src/pages/ApiDocsPage.test.tsx` 通过（34 tests，仅既有 warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面复用并重新执行本地忽略脚本 `.hermes/local-output/profile-shared-shell-real-api/run.sh`，最新产物目录 `.hermes/local-output/profile-shared-shell-real-api/20260520-075735`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`，并显式确认普通用户菜单保留 `/`、`/profile`、`/api-keys`、`/webhooks`、`/docs`；前端 `http://127.0.0.1:5173/profile`、`/login`、`/api-keys`、`/docs` 均返回 `200`；seeded admin 登录后 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 返回 `200`，继续确认 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs` 保留在管理端菜单中。脚本 assistant 可见输出中的 token/密码字段被系统脱敏，但本地回放状态码与产物文件均正常，可继续作为后续 shared-console 前端切片的真实 API 基线。
- 本轮五维评审结论：产品/规格 PASS（Profile hero 现在以命名 region 诚实表达“个人资料中枢”这一 shared-console 语义层，而 route-stub/fallback proof 也直接绑定到共享控制台命名目标页，不再主要依赖 test-id）；代码质量 APPROVED（diff 极小，仅集中在 1 个运行时文件与 1 个 focused test 文件，review 仅给出后续可逐步减少 test-id 混用的非阻塞建议）；安全/集成 SAFE（纯前端语义补强，不触碰 auth/session/menu/backend contract，静态扫描无新增风险，真实 API 回放再次证明 Profile/shared routes/admin 菜单链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增 region 查询更贴近真实可访问合同）；性能/运维 PASS（零运行时代码/部署开销，仅补 landmark 语义与 focused 护栏；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，SettingsPage 快捷入口命名 region 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/SettingsPage.tsx` / `web/src/pages/SettingsPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 Settings 菜单过滤、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有 Settings 快捷入口卡片已经稳定承载 API Keys / Webhook / supplier/admin 角色入口的前提下，把该 lane 从 `data-testid` proof 收口为命名 `控制台运行快捷入口` region 合同，继续对齐单一登录后控制台 / new-api 单壳语义与此前 Dashboard / ApiDocs / LoginPage 已推进的 named-region 护栏。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/SettingsPage.tsx` / `web/src/pages/SettingsPage.test.tsx`，确认根因不是 `/settings` 页面运行时错误、不是 shortcut card 过滤失效、不是 bridge / mission lane ownership 漂移、也不是真实 API 不一致，而只是 focused suite 对 shortcut lane 仍主要依赖 `getByTestId('settings-shortcut-cards')`，尚未直接保护“这是用户可见的共享控制台快捷入口 region，而不是仅某个实现容器”这一前端合同。随后先在 `SettingsPage.test.tsx` 制造 RED：将 `keeps settings runtime shortcuts limited...` 用例升级为 `findByRole('region', { name: '控制台运行快捷入口' })`，继续在该 region 内约束仅暴露当前可见的 runtime shortcut cards；再最小修改 `SettingsPage.tsx`，仅为 shortcut lane `Card` 增补 `role="region" + aria-labelledby="settings-shortcut-cards-heading"`，并把可见标题绑定到稳定 id。未改任何后端接口、未改 route 常量、未改 shortcut 计算逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 通过（7 tests，仅既有 React/Semi `findDOMNode` warning）；`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/pages/ProfilePage.test.tsx src/pages/DashboardPage.test.tsx src/pages/LoginPage.test.tsx` 通过（38 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/settings-shortcut-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/settings-shortcut-region-real-api/20260520-044435`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认 `/auth/menu.items` 继续暴露 `/settings`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`，并确认管理端菜单保留 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；前端 `http://127.0.0.1:5173/`、`/login`、`/settings`、`/api-keys`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（shortcut lane 现直接保护“控制台运行快捷入口”命名 region，而不是仅 test-id 容器命中，shared-console 快捷入口合同更诚实）；代码质量 APPROVED（运行时代码仅新增 2 个 ARIA 属性与 1 个 heading id，测试也只是把查询源切换到用户可见语义，diff 极小且与仓库既有 named-region 模式一致）；安全/集成 SAFE（纯前端语义补强，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明注册、菜单与 settings/shared routes 链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新增断言更准确地区分 shortcut lane 的语义边界）；性能/运维 PASS（零运行时代码/部署开销，仅补 landmark 语义与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。