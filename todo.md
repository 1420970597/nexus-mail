# Nexus-Mail 完整开发计划（todo.md）

> **执行模式已切换：** 本文档不再只是规划稿，而是当前仓库的**执行中路线图**。后续提交必须对照阶段目标更新状态，并在阶段完成后进入下一阶段。

## 执行状态看板（持续维护）

### 最新执行检查点（2026-05-22，LoginPage 注册/登录校验契约与真实 API 回放补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理登录/注册入口这一 shared-console 单壳最前置前端契约，而不是提前扩散到新的 Phase 4/5 后端逻辑。目标不是新增后端 auth 接口、不是改 `/auth/menu` 真值、也不是拆第二套注册后台，而是在既有 `LoginPage.tsx` 已具备 register 本地校验与 shared-console 路径引导的前提下，补强 focused 测试对真实后端契约的显式保护：证明登录模式不会泄漏 register-only 字段 / 文案，证明注册成功仍只向后端发送 `email + password` 两个真实受支持字段，并继续保持单一登录后控制台的落点合同。
- 控制器按 `nexus-mail-auth-form-validation-parity` + `systematic-debugging` 先回读 `web/src/pages/LoginPage.tsx`、`web/src/pages/LoginPage.test.tsx`、`web/src/services/auth.ts`、`internal/modules/auth/service.go`、`internal/modules/auth/http.go`，确认根因不是后端缺少注册能力，不是 `/auth/register` 或 `/auth/login` 契约漂移，也不是 shared-console bootstrap 失效，而是当前 focused 套件虽然已覆盖 register 的非法邮箱 / 短密码 / confirm mismatch / 8 位边界，但仍缺少两类显式防回归 proof：其一，登录模式必须保持 login-only，不应出现注册字段或注册侧校验文案；其二，register 成功路径必须继续只调用 `register(email, password)`，不能把 `confirm_password` 之类前端本地字段误当成后端请求合同。随后先在测试侧 RED 并补齐两条 focused 用例：`keeps login mode on real auth login without leaking register-only fields or validation text` 与 `keeps register mode validation local-only and sends only backend-supported email/password fields on successful submit`；运行时代码未修改，因为现有 `LoginPage.tsx` / `auth.ts` 已满足该合同，说明这一轮属于测试合同补强而非新的运行时功能开发。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（14 tests，仅既有 React `act(...)` warnings，未被本轮 diff 放大）；`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（39 tests，Login focused + App shared-shell 集成稳定）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_login_register_validation_replay.sh`，最新产物目录为 `.hermes/local-output/login-register-validation-replay/20260522-221127`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 且不泄漏 `/admin/risk`、`/supplier/resources`、`/supplier/settlements`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 诚实完成的是 LoginPage 注册/登录 focused 合同补强，而不是夸大为新的 auth backend 能力；single login → shared console 的叙事保持不变）；代码质量 APPROVED（子评审指出一次重复 mock 断言过度，控制器已删去 `mock.calls[0]` 的冗余内部实现检查，保留 `toHaveBeenCalledWith(...)` 作为更直接且可维护的合同 proof；其余断言保持 scoped 且与现有 shared-console 测试风格一致）；安全/集成 SAFE（diff 不触碰 token/session/menu/backend route 真值，新增测试与 `auth.ts` 的 `register(email, password)` 及后端 `RegisterInput` / `validateCredentials` 契约一致，静态 added-line 扫描无 secrets / shell / eval / SQL 风险）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新增用例补齐“login-only 不泄漏 register 字段”和“register 只发送后端支持字段”两个高价值前端回归面；既有 React/Semi warnings 未被本轮 diff 放大）；性能/运维 PASS（纯 focused 测试合同补强，不增加请求、状态、effect 或部署负担；本地 replay 脚本与产物保存在忽略目录，便于后续无上下文复查）。

### 最新执行检查点（2026-05-22，API Keys / Webhooks region 合同继续去 test-id 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先选择仍残留实现细节断言、且直接影响普通用户 API 接入 / Webhook 接入主路径体验的 `ApiKeysPage` 与 `WebhooksPage` focused 用例作为下一可提交前端切片，而不是提前扩散到新的 Phase 4/5 后端逻辑。目标不是新增 API Key / Webhook 后端接口，不是改 `/auth/menu` 真值，也不是拆独立接入后台，而是在既有 shared-console/new-api 单壳已经稳定承接“项目市场 → 订单中心 → API Keys → Webhooks → Docs”接入叙事的前提下，把 API Keys 白名单编辑器与 Webhooks 用户引导 / 共享接入桥接里的残留 `data-testid` / 容器实现细节 proof 继续收口到用户可见 region / heading / 文案合同。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ApiKeysPage.tsx`、`web/src/pages/ApiKeysPage.test.tsx`、`web/src/pages/WebhooksPage.tsx`、`web/src/pages/WebhooksPage.test.tsx` 与当前 focused Vitest 输出，确认根因不是 `/api-keys`、`/webhooks` 路由失效，不是真实 `/auth/api-keys`、`/webhooks/endpoints` 契约变化，也不是角色菜单漂移，而是少数 focused 断言仍依赖 `api-keys-whitelist-editor-card`、`webhooks-role-guidance`、`webhooks-shared-console-bridge` 等 test id / 容器节点。随后先在测试侧 RED：把 ApiKeys 三个白名单编辑用例改为 `getByRole('region', { name: '编辑 API Key 白名单' })`；把 Webhooks 用户 guidance 与三组 bridge honest/fallback 用例改为围绕 `开发者 Webhook 接入工作台`、`共享接入桥接` 这些用户可见命名 region 做 scoped proof。GREEN 阶段仅做最小运行时实现：为 `ApiKeysPage` 白名单编辑卡补上 `role="region" + aria-labelledby="api-keys-whitelist-editor-heading"` 与显式 heading；`WebhooksPage` 运行时代码不变，因为已有具名 region 足以承接新的测试合同。未改动 auth/menu、角色路由、API payload、限流、审计或其它后端逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx src/pages/WebhooksPage.test.tsx` 通过（33 tests，仅既有 React/Semi `act(...)` warnings，未被本轮 diff 放大）；随后复跑 `pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_api_keys_webhooks_region_pass_real_api.sh`，最新产物目录为 `.hermes/local-output/api-keys-webhooks-region-pass/20260522-214745`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`；创建 API Key、更新白名单、撤销与审计查询分别返回 `201/200/200/200`，并显式确认普通用户菜单继续暴露 `/`、`/api-keys`、`/webhooks`、`/docs`；Webhook 真实负路径方面，向 `https://hooks.example.com/nexus-frontslice` 创建 endpoint 返回 `400 + webhook URL 域名无法解析`，诚实证明 SSRF/域名解析校验仍生效；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/api-keys`、`/webhooks`、`/docs` 均返回 `200` 且继续提供 SPA shell。由于新注册普通用户无现成 endpoint，test-delivery 正路径本轮按真实数据诚实记录为 `existing_webhook_test_delivery_attempted=false`，未把缺少现成 endpoint 误报成应用故障。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 诚实完成的是 ApiKeys/Webhooks focused 合同继续从 test-id 向具名 region/heading/文案收口，而不是夸大为新的 API Key/Webhook 后端能力；single shared-console / new-api 单壳叙事保持不变）；代码质量 APPROVED（ApiKeys 运行时仅补最小语义锚点，Webhooks 仅切换测试查询方式，focused 断言更贴近用户可见合同且没有引入新的 class/wrapper 耦合；review 仅建议后续继续删除不再需要的遗留 test id）；安全/集成 SAFE（diff 不触碰 auth/menu/session/backend contract，静态 added-line 审视无 secrets / shell / eval / SQL 风险；真实 API 再次证明 API Keys 生命周期、Webhook 域名解析校验、admin/supplier 菜单与共享路由边界稳定）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新断言由实现细节 test id 收口到具名 region/heading/text，更接近无障碍树与用户可见语义；既有 React/Semi warnings 未被本轮 diff 放大）；性能/运维 PASS（仅新增极小前端语义标注，不增加请求、状态、部署或运行态负担；本地 replay 脚本与产物保存在忽略目录，可供后续无上下文复查）。

### 最新执行检查点（2026-05-22，Dashboard/Balance shared-console 测试合同去 test-id 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先选择仍残留 test-id 依赖、但又直接影响普通用户主路径与余额工作台体验的 `DashboardPage.test.tsx` / `BalancePage.test.tsx` 作为下一可提交前端切片，而不是提前扩散到新的 Phase 4/5 后端逻辑。目标不是新增钱包/项目/订单/API Keys 后端接口、不是改 `/auth/menu` 真值、也不是拆独立资金/仪表盘后台，而是在既有 shared-console/new-api 单壳已经稳定承接预算 → 采购 → 履约 → 接入叙事的前提下，把这两组 focused 测试中残留的 `balance-hero-card`、`dashboard-next-step-*` 级别实现细节 proof 继续收口到用户可见 region / heading / CTA 合同，保持角色差异仍由菜单真值驱动、页面仍停留在同一登录后控制台。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/BalancePage.test.tsx`、`web/src/pages/DashboardPage.test.tsx`、对应运行时 `DashboardPage.tsx` / `BalancePage.tsx` 与当前 diff，确认根因不是 `/balance`、`/projects`、`/orders`、`/api-keys` 深链失效，不是真实 `/wallet/overview` / `/projects/inventory` / `/orders/activations` 契约变化，也不是角色菜单漂移，而是少数 focused 断言仍把存在/缺失状态绑定到 test id：Balance supplier/admin hero 仍通过 `findByTestId('balance-hero-card')` 取壳；Dashboard 普通用户 next-step 隐藏态仍通过 `dashboard-next-step-*` / `dashboard-next-step-action-*` 证明；API Keys 集成卡则依赖卡片级 test id 而不是用户真正看到的 CTA。随后先在测试侧 RED：把 Balance 的 supplier/admin hero proof 改为 `findByRole('region', { name: '余额中心' })`；把 Dashboard next-step 缺失态改为围绕按钮级可见性（`查看余额中心` / `前往项目市场` / `查看订单中心` / `打开 API Keys`）断言；API Keys 集成卡则改为以 lane 内真实 CTA `打开 API Keys` 为锚点，利用其所在卡片层级做最小结构定位，并保留“lane 内不应泄漏 Webhook/Docs CTA”的负向断言。运行时代码不做任何改动，保持这一轮只收口前端测试合同。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/BalancePage.test.tsx src/pages/DashboardPage.test.tsx` 通过（17 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web exec vitest run src/pages/BalancePage.test.tsx src/pages/DashboardPage.test.tsx src/App.test.tsx` 通过（42 tests，Balance/Dashboard focused + App shared-shell 集成稳定）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略产物脚本/回放，最新产物目录为 `.hermes/local-output/dashboard-balance-region-real-api/20260522-212253`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/wallet/overview`、`/api/v1/wallet/transactions`、`/api/v1/projects/inventory`、`/api/v1/orders/activations`、`/api/v1/auth/api-keys`，并显式确认普通用户菜单继续暴露 `/`、`/balance`、`/projects`、`/orders`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 且不泄漏 `/admin/risk`、`/supplier/resources`、`/supplier/settlements`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 返回 `200`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/balance`、`/projects`、`/orders`、`/api-keys`、`/settings`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 诚实完成的是 Dashboard/Balance focused 测试合同从 test-id 向 region/CTA 收口，而不是夸大为新的资金/项目/订单后端能力；single shared-console/new-api 单壳叙事保持不变）；代码质量 APPROVED（初版 reviewer 指出 `closest('[class*=semi-card]')` 过度依赖 UI class 名，控制器已改为以真实 `打开 API Keys` CTA 为锚点并仅做最小父层级定位后复跑通过，剩余 diff 集中且易维护）；安全/集成 SAFE（diff 不触碰 auth/menu/session/backend contract，静态 added-line 审视无 secrets / shell / eval / SQL 风险，真实 API 再次证明 user/admin/supplier 菜单与 `/wallet`、`/projects`、`/orders`、`/api-keys` 共享路由契约稳定）；测试/可靠性 PASS（focused/grouped/App Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新断言更偏向用户可见 region/CTA，减少对实现细节 test id 的依赖；既有 React/Semi warnings 未被本轮 diff 放大）；性能/运维 PASS（纯前端测试合同收口，不增加运行时代码、请求、状态或部署负担，仅在忽略目录写入最新 replay 产物，便于后续无上下文复查）。

### 最新执行检查点（2026-05-22，ProjectsPage 采购任务流 named-region 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先选择注册后普通用户主路径里最核心的 `ProjectsPage` 共享采购切片，而不是提前扩散到新的后端 Webhook / API Key / 风控逻辑。目标不是新增库存/订单后端接口、不是改 `/auth/menu` 真值、不是拆独立采购后台，而是在既有项目市场已经承接“采购 → 履约 → 接入”单壳叙事的前提下，把残留依赖 `Card title` / `data-testid` 的采购任务流、下单后下一步与共享控制台回退路径收口为用户可见的 named-region 合同，继续对齐 new-api 风格下的单一登录后控制台。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ProjectsPage.tsx`、`web/src/pages/ProjectsPage.test.tsx` 与当前 diff，确认根因不是 `/projects` 路由失效、不是真实 `/projects/inventory` / `/orders/activations` 契约变化、也不是角色菜单漂移，而是采购任务流仍主要通过 `projects-mission-flow-card` / `projects-guidance-*-card` 等实现细节 proof 暴露，和近期其它 shared-console 页面已经收口到 `role="region" + aria-labelledby` 的合同不一致。随后先在测试侧 RED：把 ProjectsPage focused 用例从 `getByTestId(...)` 改为 `findByRole('region', { name: ... })`，要求直接证明 `采购任务流`、`先挑库存，再下单`、`下单后下一步`、`共享控制台回退路径` 这些用户可见区域存在并维持 scoped CTA；GREEN 阶段仅做最小运行时实现：为 mission-flow 主卡与三张 guidance 卡补上 `role=region + aria-labelledby` 与显式 heading id，同时保留原有采购 → 订单中心 → API Keys / Docs 的共享控制台链路，不改库存加载、订单创建、fallback 解析或 auth/menu 逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx` 通过（15 tests，仅既有 React/Semi `act(...)` warnings，未被本轮 diff 放大）；`pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx src/App.test.tsx` 通过（40 tests，Projects focused + App shared-shell 集成稳定）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_projects_mission_region_real_api.sh`，最新产物目录为 `.hermes/local-output/projects-mission-region-real-api/20260522-205537`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/projects/inventory`、`/api/v1/orders/activations`、`/api/v1/auth/api-keys`，并显式确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/api-keys`、`/docs` 且不泄漏 `/admin/risk`、`/supplier/resources`、`/supplier/settlements`；前端 `http://127.0.0.1:5173/projects`、`/orders`、`/api-keys`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 诚实完成的是 ProjectsPage 采购任务流 / fallback named-region 收口，而不是夸大为新的库存/订单后端能力；single shared-console 采购 → 履约 → 接入叙事保持不变）；代码质量 APPROVED（运行时与 focused 测试改动都集中在同一垂直切片，测试从 test-id-only proof 收口到用户可见语义后更一致、可维护）；安全/集成 SAFE（静态 added-line 审视无 secrets / shell / eval / SQL 风险，CTA 仍受 `hasMenuPath(...)` 与 `resolvePreferredConsoleRoute(...)` 约束，未放宽 auth/menu 权威或引入独立后台跳转）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新断言用 `within(region)` 保持 scoped proof，减少重复按钮文案带来的脆弱性）；性能/运维 PASS（纯前端语义增强，不增加请求、状态或部署负担，仅有极小 DOM/accessibility 标注增量，controller-side 构建与运行态均稳定）。

### 最新执行检查点（2026-05-22，auth bootstrap 深链保留 / 回退合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先选择认证后 bootstrap / deep-link 这一前端共享壳关键接缝作为可提交里程碑，而不是提前扩散到新的 Phase 4/5 后端逻辑。目标不是新增 Webhook / API Key / 风控后端接口，不是改 `/auth/menu` 服务端真值，也不是拆第二套后台，而是把登录后共享控制台在刷新/深链场景下的落点合同收口得更诚实：允许的共享路由深链应在 bootstrap 后继续停留原页，失效 deep link 在共享首页仍可用时回到 `/`，从而避免此前“只有从 `/` 进入才会做 landing 决策”的 root-only 特例。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/App.tsx`、`web/src/App.test.tsx`、`web/src/utils/consoleNavigation.tsx`、`web/src/utils/consoleNavigation.test.tsx` 并复现实有 bootstrap 逻辑，确认根因不是注册功能缺失、不是 `/auth/menu` 菜单漂移、也不是真实 API 契约变化，而是共享控制台 bootstrap 仍只在初始路径等于 `/` 时才调用 landing resolver，导致已授权的 `/api-keys` 等深链刷新后仍依赖偶然路径，而“无效深链如何回退”也缺少清晰 helper 合同。随后按最小 RED→GREEN 切片：先新增 focused tests 证明“允许的共享控制台深链在 bootstrap 后保持原页”“失效 deep link 在共享首页可用时回到 dashboard”，再在运行时代码中新增 `resolveBootstrapConsoleRoute(currentPath, menuItems, role)`，由它统一表达 bootstrap 路径决策；`App.tsx` 则从原来的 root-only `resolvePostAuthLandingRoute(...)` 切到新的 bootstrap resolver，并且仅在 `preferredRoute !== initialPathRef.current` 时导航。这样既不改变 `/auth/menu` 的服务端权威，也不扩散到页面内业务逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/utils/consoleNavigation.test.tsx src/App.test.tsx` 通过（33 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_bootstrap_route_real_api.sh`，最新产物目录为 `.hermes/local-output/bootstrap-route-real-api/20260522-180431`：验证 `GET /healthz` 返回 `200 + {status: ok}`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 且不泄漏 `/admin/risk`、`/supplier/resources`、`/supplier/settlements`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/audit?limit=5` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/api-keys`、`/projects`、`/orders`、`/settings` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（初版 reviewer 曾指出“无效 deep link 回退语义不够诚实”，控制器随后把合同收口为“已授权深链保留、共享首页仍在菜单时回到 dashboard”，并同步修正 helper/test 名称与断言，不再混淆“回首选工作区”与“回 dashboard”）；代码质量 APPROVED（新增 `resolveBootstrapConsoleRoute` 后 bootstrap 逻辑不再散落在 `App.tsx` 特例分支中，focused tests 也直接覆盖 helper 与 App 壳行为；过程中清理了 root-only redirect 的隐式特殊路径）；安全/集成 SAFE（diff 不触碰 auth/session/backend contract，也未放宽菜单权威；受保护深链只有在服务端 `menu.items` 明确允许时才会保留，supplier/admin 路由仍由 `SupplierRoute` / `AdminRoute` 护栏兜底；静态 added-line 扫描无 secrets / shell / eval / SQL 风险）；测试/可靠性 PASS（focused Vitest、web build、Go tests、compose config、真实 API 回放全部通过，且 reviewer 提出的“测试标题与行为不一致”已在控制器侧修复后复跑通过）；性能/运维 PASS（bootstrap 只增加一次轻量 helper 判断，没有引入额外请求或循环重定向，真实回放脚本保存在忽略目录中，可供后续无状态复查）。

### 最新执行检查点（2026-05-22，SupplierSettlementsPage 任务流 region 查询收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理当前工作树里唯一未提交的前端 focused 切片 `web/src/pages/SupplierSettlementsPage.test.tsx`，而不是扩散到新的后端逻辑。目标不是新增供应商结算/报表/争议后端接口、不是改 `/auth/menu` 真值、不是拆独立供应商财务后台，而是在既有 `SupplierSettlementsPage.tsx` 已完成 mission-control / 共享接入桥接 / fallback named-region 收口的前提下，把残留 focused 测试里对 `supplier-settlements-mission-flow` test id 的依赖继续收口到用户可见的 `role="region" + aria-labelledby="supplier-settlements-mission-flow-heading"` 合同，保持供应商仍通过同一登录后的 shared-console 深色单壳进入资源、供货规则、API Keys、Webhook 与 Docs。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/SupplierSettlementsPage.tsx`、`web/src/pages/SupplierSettlementsPage.test.tsx` 与当前 diff，确认根因不是 `/supplier/settlements` 路由失效、不是 supplier 菜单漂移、也不是真实 API 契约变化，而只是三个 focused 导航断言仍通过 `getByTestId('supplier-settlements-mission-flow')` 查询任务流容器，和当前运行时已经提供的命名 region 合同不一致。随后仅做最小测试侧修复：把任务流导航与集成入口隐藏态断言改为 `getByRole('region', { name: '供应商资金任务流' })` 后再做 scoped button proof；运行时代码、finance service、auth/menu、角色边界与 shared-console 路由真值保持完全不变。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx` 通过（8 tests，仅既有 React/Semi `act(...)` / Table warnings，未被本轮 diff 放大）；`pnpm --dir web exec vitest run src/pages/SupplierSettlementsPage.test.tsx src/App.test.tsx` 通过（31 tests，SupplierSettlements focused + App shared-shell 集成稳定）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_supplier_settlements_region_recheck_real_api.sh`，最新产物目录为 `.hermes/local-output/supplier-settlements-region-recheck/20260522-173148`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 且不泄漏 `/admin/risk`、`/supplier/resources`、`/supplier/settlements`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements`、`/api/v1/supplier/reports?limit=5`、`/api/v1/supplier/disputes?limit=5`、`/api/v1/supplier/cost-profiles` 返回 `200` 且 settlement payload 继续包含 `wallet` 与 `entries`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/audit?limit=5` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/supplier/settlements`、`/supplier/resources`、`/api-keys`、`/webhooks`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（本轮诚实完成的是 SupplierSettlementsPage 任务流 region 查询收口，而不是夸大为新的供应商结算后端能力；single shared-console / new-api 单壳叙事保持不变）；代码质量 APPROVED（diff 仅收敛 1 个 focused 测试文件中的 selector，与已存在的运行时语义合同对齐，没有额外引入 wrapper traversal 或复制逻辑）；安全/集成 SAFE（静态 added-line 扫描无 secrets / shell / eval / SQL 风险，diff 不触碰 auth/menu/route 真值；真实 API 复核继续证明 user/supplier/admin 菜单、supplier settlements/overview/reports/disputes/cost-profiles 与 shared routes 鉴权链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，任务流断言从 test-id 收口到命名 region 后更贴近用户可见合同，既有 Semi/React warnings 未被本轮 diff 放大）；性能/运维 PASS（纯 focused 测试合同增强，不增加请求、状态或部署负担；本地 replay 脚本保存于忽略目录并已最新成功回放）。

### 最新执行检查点（2026-05-22，SettingsPage 继续当前角色主路径 named-region 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理设置中心里最核心的 role-aware 后续行动轨道，而不是提前扩散到新的后端 Webhook/API Key/风控逻辑。目标不是新增设置接口、不是改 `/auth/menu` 真值、不是拆第二套独立后台，而是在既有 SettingsPage 已具备深色共享壳、首次使用清单、共享接入桥接、角色差异快捷入口与继续当前角色主路径的前提下，把 continue lane 内部三组角色步骤卡从实现细节 `data-testid` 证明继续收口为用户可见的命名 region 合同，保持“单一登录后控制台、按角色扩展页面/菜单”的 new-api 风格不变。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/SettingsPage.tsx`、`web/src/pages/SettingsPage.test.tsx` 与 `resolveContinueConsoleSteps` 相关逻辑，确认根因不是 `/settings` 路由失效、不是角色菜单漂移、也不是真实 API 契约变化，而是 continue lane 三张步骤卡仍只暴露 `settings-continue-console-step-*` test id，focused tests 只能证明实现节点存在，不能直接证明“项目市场 / 订单中心 / 开发者 API 接入工作台”“域名池运营中枢 / 供应商资源 / 供应商资金与争议指挥台”“供应商管理 / 风控中心 / 审计日志”这些角色后续动作确实以可访问的用户可见区域存在。随后先在测试侧 RED：把 user/supplier/admin 三组 continue lane 断言从 `getByTestId(...)` 改为 `getByRole('region', { name: ... })`，并补上跨角色 region 不应泄漏的负向断言；GREEN 阶段仅做最小运行时实现：为 `continueConsoleSteps.map(...)` 里的每张步骤卡补上 `role=region + aria-labelledby=settings-continue-console-step-<key>-heading`，并让卡片标题显式承载对应 heading id。没有改动导航路径、角色菜单过滤、shared bridge、首次使用清单、auth/session 或后端接口。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 先按预期 RED 失败（Testing Library 无法找到名为“项目市场 / 域名池运营中枢 / 供应商管理”的内部 region，证明测试确实捕获了现有语义缺口），补上运行时 named-region 后转 GREEN 通过（10 tests）；随后 `pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/App.test.tsx` 通过（35 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_settings_continue_lane_real_api.sh`，最新产物目录为 `.hermes/local-output/settings-continue-lane-real-api/20260522-201858`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`，并确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 且不泄漏 `/admin/risk`、`/supplier/resources`、`/supplier/settlements`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/audit?limit=5` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/settings`、`/login`、`/api-keys`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 诚实完成的是 SettingsPage continue-current-role lane 的 named-region 收口，而不是夸大为新的设置后端能力；single shared-console / role-aware menu 叙事保持不变）；代码质量 APPROVED（运行时只新增最小 `role` / `aria-labelledby` / heading id，测试从 `data-testid` 迁移到用户可见语义后更贴近真实合同且没有扩大 diff 面）；安全/集成 SAFE（静态 added-line 扫描无 secrets / shell / eval / SQL 风险，diff 不触碰 auth/session、菜单真值、路由路径或后端契约；真实 API 回放继续证明 user/supplier/admin 菜单与共享路由链路稳定）；测试/可靠性 PASS（先 RED 再 GREEN，focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且 reviewer 认可新的 region 查询比旧 test-id-only 证明更稳健）；性能/运维 PASS（纯前端语义增强，不新增请求、状态、effect 或部署负担；build 仅保留既有 chunk-size warning，本地 replay 脚本保存在忽略目录，可供后续无上下文复查）。

### 最新执行检查点（2026-05-22，LoginPage 注册表单 named-region / 测试合同收口）
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/LoginPage.tsx`、`web/src/pages/LoginPage.test.tsx`、`web/src/App.test.tsx` 与后端 `internal/modules/auth/{model,service,http}.go`，确认根因不是后端缺少注册接口、不是 register payload 契约漂移、也不是深链/bootstrap 回归，而是登录/注册壳新增了第二个与 copy 同名的表单 heading 之后，旧的 page-global `getByRole('heading', ...)` 断言开始同时命中“认证说明 heading + 表单 heading”，从而让 focused/App 集成测试的 proof 重新变脆。随后先在测试侧 RED：把 LoginPage 注册 CTA 切换后的断言改成必须命中 auth shell 内的命名表单 region，并把登录态/App 登录壳断言同步改为从 `登录认证说明` / 新表单 region 内 scoped 查询 heading。GREEN 阶段仅做最小运行时实现：在 `LoginPage.tsx` 的 `tabpanel` 内为真实认证表单补上 `role=region + aria-labelledby=login-auth-form-heading`，并让新的表单 heading 复用现有 `copy.title`；没有改动 `register(email, password)`、`confirm_password` 前端本地校验、session 落盘、导航落点或 `/auth/menu` 相关逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（12 tests，仅既有 React `act(...)` warnings）；`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（37 tests，LoginPage focused + App shared-shell 集成稳定）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_login_register_form_real_api.sh`，最新产物目录为 `.hermes/local-output/login-register-form-real-api/20260522-195120`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`，并确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 且不泄漏 `/admin/risk`、`/supplier/resources`、`/supplier/settlements`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/audit?limit=5` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/api-keys`、`/docs`、`/settings` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（子评审确认本轮诚实完成的是 LoginPage 注册表单 named-region 与测试合同收口，而不是夸大为新的 auth backend 能力；single login → shared console 的叙事保持不变）；代码质量 APPROVED（新表单 region 复用 `copy.title` 而非复制文案，测试改为 scoped region 查询后更清晰、可维护）；安全/集成 SAFE（diff 不触碰 register/login API、token/session、菜单真值与角色边界，静态 added-line 扫描无 secrets / shell / eval / SQL 风险）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且 App/LoginPage 中原本受重复 heading 影响的脆弱断言已收口到命名 region）；性能/运维 PASS（运行时只新增轻量语义 wrapper 与 heading，不增加请求、bootstrap 次数或部署负担；本地 replay 脚本已保存在忽略目录，便于后续继续无上下文复查）。

### 最新执行检查点（2026-05-22，WebhooksPage 当前 endpoint / bridge / integration loop 测试合同继续去 test-id 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先选择仍位于普通用户 API 接入主链路、且 focused 测试仍残留多处实现细节依赖的 `WebhooksPage` 作为下一前端可提交切片，而不是提前扩散到新的 Phase 4/5 后端逻辑。目标不是新增 Webhook 后端接口、不是改 `/auth/menu` 真值、不是放宽角色边界，也不是拆独立接入后台，而是在既有 shared-console/new-api 单壳已经承接 `API Keys → Webhooks → Docs` 接入叙事的前提下，把 `current endpoint`、empty state、bridge、integration loop 与 admin workspace 的部分 test-id / 容器依赖继续收口到用户可见 region / heading / button 合同。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/WebhooksPage.tsx`、`web/src/pages/WebhooksPage.test.tsx`、`web/src/App.test.tsx` 并复现实有 focused 测试，确认根因不是 `/webhooks` 路由失效、不是真实 `/webhooks/endpoints` / `/auth/menu` 契约变化、也不是 shared-console bootstrap 漂移，而是页面运行时虽已提供 `共享接入桥接`、`注册后首轮回调联调建议` 等命名 region，但测试仍混用 `webhooks-current-endpoints-card`、`webhooks-send-test-button-*`、`webhooks-shared-console-bridge`、`webhooks-first-integration-loop` 与多处 route-stub test id；同时 `当前 endpoint` 卡片本身还没有显式 `role=region` 合同。随后先在测试侧 RED：把 Webhooks focused/App 断言改为优先查询 `当前 endpoint` region、`共享接入桥接` region、`注册后首轮回调联调建议` region 与用户可见按钮/文案；GREEN 阶段仅做最小运行时实现：为 `当前 endpoint` 卡片补上 `role="region" + aria-labelledby="webhooks-current-endpoints-heading"`，并删除 `发送测试投递` 按钮的 test id。其余变更只在测试侧完成，未改动 webhook payload、菜单真值、bridge/fallback 逻辑、auth/session 或任何后端接口。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx` 通过（19 tests，仅既有 React/Semi `act(...)` warnings，未被本轮 diff 放大）；`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx src/App.test.tsx` 通过（44 tests，Webhooks focused + App shared-shell 集成稳定）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_webhooks_region_real_api.sh`，最新产物目录为 `.hermes/local-output/webhooks-region-real-api/20260522-224615`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/webhooks/endpoints`、`/api/v1/auth/api-keys`，并显式确认普通用户菜单继续暴露 `/`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 且不泄漏 `/admin/risk`、`/supplier/resources`；创建无效 webhook URL 返回 `400 + webhook URL 域名无法解析`，诚实证明域名解析/SSRF 负路径校验仍生效；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/risk` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/webhooks`、`/api-keys`、`/docs`、`/settings` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（当前 diff 诚实完成的是 WebhooksPage 前端语义/测试合同继续去 test-id 收口，而不是夸大为新的 webhook backend 能力；single shared-console / new-api 单壳叙事保持不变）；代码质量 APPROVED（初版 reviewer 指出 admin metrics 断言被放宽为过于宽泛的 page-global text proof，控制器已回滚到更克制的文本约束并保留 focused/App 实际通过结果；运行时代码只做 1 处最小语义补强）；安全/集成 SAFE（diff 不触碰 auth/menu/role guard/backend contract，删除的 test id 仅限测试定位用途，真实 API 再次证明 user/admin 菜单与 `/webhooks`、`/api-keys`、`/docs` 共享路由边界稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，`current endpoint` / empty state / bridge / integration loop / admin workspace 继续由用户可见 region/heading/button 合同保护，而不是依赖 route/button test id）；性能/运维 PASS（纯前端语义/测试合同收口，不增加请求、状态、部署或 worker 负担；本地 replay 脚本与产物保存在忽略目录，便于后续无上下文复查）。


### 当前总体进度
- Phase 0：**已完成**（开发底座、Docker、多服务骨架已提交）
- Phase 1：**已完成**（身份权限、共享控制台骨架已提交）
- Phase 2：**已完成**（项目/库存/激活订单主链路、管理员项目配置、供应商资源录入已提交）
- Phase 3：**已完成**（已完成 SMTP 会话落盘、metadata 入库、RabbitMQ 解析任务入队、MinIO 原始对象上传，并打通验证码/link 提取回退增强、订单 READY/FINISHED 自动迁移、真实 OAuth2 刷新接入、授权码/App Password/密码型凭证录入与健康状态落库、`env://` secret_ref 解析、IMAP/POP3 实际登录校验、官方邮箱端点限制、Proton Bridge 接入策略与 Postfix 转发样例）
- Phase 4：进行中（已完成用户钱包、冻结余额、成功扣费、超时退款、供应商待结算余额、管理员调账能力与基础结算页面；已完成供应商报表筛选、供货规则维护、管理员争议单显式筛选、供应商域名运营页、管理员供应商运营页与共享控制台深色单壳导航收敛；已完成 Webhooks / 风控 / 审计真实页面收敛与对应真实 API 回放验证；当前前端后续重点继续围绕 shared-console focused 用例去脆弱化、语义锚点收敛与 new-api 风格单壳一致性补强，再回到 Phase 4/5 余下后端能力）
- 2026-05-22：完成 AdminAuditPage 管理员主任务流 / shared-console fallback named-region 收口：运行时为 `admin-audit-mission-flow` 卡补上 `role=region + aria-labelledby=admin-audit-mission-flow-heading` 与显式 heading/说明文案；在共享接入入口被菜单隐藏时，为 `admin-audit-shared-console-fallback` 增补 `role=region + aria-labelledby=admin-audit-shared-console-fallback-heading`，并将标题诚实收口为“返回共享工作台”。focused Vitest 同步改为查询用户可见的 `管理员主任务流` 与 `返回共享工作台` landmarks；控制器已复跑 `pnpm --dir web exec vitest run src/pages/AdminAuditPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并通过本地忽略脚本 `.hermes/local-output/run_admin_audit_region_real_api.sh` 在 `.hermes/local-output/admin-audit-region-real-api/20260522-190259` 真实回放 `healthz`、普通用户 register/auth/menu/dashboard/api-keys/webhooks、supplier `/supplier/resources/overview` `/supplier/settlements` `/supplier/resources/offerings`、admin `/admin/overview` `/admin/audit?limit=5` 与前端 `/admin/audit` `/admin/risk` `/api-keys` `/docs` 等 SPA shell；五维评审均通过，本轮属于 shared-console 前端语义/测试合同增强，不涉及新的后端业务逻辑。
- 2026-05-22：完成 SupplierOfferingsPage `供应商主任务流` named-region 收口：运行时为任务流卡补上 `role=region + aria-labelledby` 与显式 heading id，focused Vitest 改为查询用户可见的 `供应商主任务流` landmark，App 侧 supplier profile→domains 集成断言也收口到目标 hero card；控制器已复跑 `pnpm --dir web exec vitest run src/pages/SupplierOfferingsPage.test.tsx src/App.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check`，并通过本地忽略脚本 `.hermes/local-output/run_supplier_offerings_mission_region_real_api.sh` 在 `.hermes/local-output/supplier-offerings-mission-region/20260522-183622` 真实回放 `healthz`、普通用户 register/auth/menu/dashboard/api-keys/webhooks、supplier `/supplier/resources/overview` `/supplier/settlements` `/supplier/resources/offerings`、admin `/admin/overview` `/admin/audit?limit=5` 与前端 `/supplier/offerings` 等 SPA shell；五维评审均通过，本轮属于 shared-console 前端语义/测试合同增强，不涉及新的后端业务逻辑。
- Phase 5：进行中（已完成管理员高危操作二次确认、OpenAPI + `/docs`、管理员审计查询、管理员概览/风控真实页、API Key 默认 scopes / 白名单 / Redis 全局限流 / 审计、Webhook 异步投递与 delivery 查询、风控规则 CRUD 等里程碑；后续在“前端先行、真实 API 验证、五维评审后提交”的约束下继续推进 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片）
- Phase 6：未开始

### 本机环境现状（2026-04-13 更新）
- `go version`：可用
- `docker version`：可用
- `docker compose version`：可用
- `pnpm -v`：可用
- 本机已验证可绑定 **25 端口**

### 本地参考资料
- 已在项目目录放置竞品调研参考：`docs/reference-local/luckyous-reference.md`
- 该目录已加入本地 Git 排除规则，**仅作为本机开发参考，不推送远程仓库**

### 最新执行检查点（2026-05-22，AdminUsersPage 管理员资金任务流 / fallback named-region 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，选择 `web/src/pages/AdminUsersPage.tsx` 与 `web/src/pages/AdminUsersPage.test.tsx` 作为下一前端 focused slice，而不是继续扩散到新的后端逻辑。目标不是新增管理员钱包/争议/结算后端能力、不是改 `/auth/menu` 真值、不是拆独立 admin 财务后台，而是在既有 AdminUsersPage 已承担钱包调账、供应商待结算确认、争议处理与 shared-console bridge 的前提下，把“管理员主任务流”与 shared-console fallback 从残留的 `Card title` / 非具名卡片 proof 继续收口为 `role="region" + aria-labelledby` 合同，保持管理员仍通过同一登录后的 shared-console 深色单壳进入风控、审计、API Keys、Webhook 与 Docs。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/AdminUsersPage.tsx` 与 `web/src/pages/AdminUsersPage.test.tsx`，确认根因不是 `/admin/users` 路由失效、不是 `/auth/menu` 菜单漂移、也不是真实 API 契约变化，而是页面仍有两处关键任务区块继续依赖裸 `Card title`/`.semi-card` 容器作为主要 proof：一是管理员主任务流卡片尚未显式暴露 named region，二是当 API Keys / Webhook / Docs 未被菜单暴露时，shared-console fallback 卡片虽有 heading 与按钮，却还不是具名 landmark。随后先在测试侧制造并完成 RED→GREEN：为 `AdminUsersPage.test.tsx` 新增“管理员主任务流” region、从任务流导航到 `共享控制台 - 风控中心`、以及“回到共享工作台继续管理员主链路” fallback region 的 focused 合同；运行时代码仅做最小实现，为 mission-flow card 增补 `role="region" + aria-labelledby="admin-users-mission-flow-heading"`，为 shared-console fallback card 增补 `role="region" + aria-labelledby="admin-users-shared-console-fallback-heading"`，并保持钱包调账、供应商待结算确认、争议处理 payload、菜单真值、角色边界与 shared-console 路由真值完全不变。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminUsersPage.test.tsx` 通过（8 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web exec vitest run src/pages/AdminUsersPage.test.tsx src/App.test.tsx` 通过（31 tests，AdminUsers focused + App shared-shell 集成仍稳定）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_admin_users_region_real_api.sh`，最新产物目录为 `.hermes/local-output/admin-users-region-real-api/20260522-143809`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 且不泄漏 `/admin/risk`、`/supplier/resources`、`/supplier/settlements`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/audit?limit=5` 返回 `200`，并确认 admin 菜单继续保留 `/admin/users`、`/admin/suppliers`、`/admin/pricing`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`；前端 `http://127.0.0.1:5173/`、`/login`、`/admin/users`、`/admin/risk`、`/api-keys`、`/webhooks`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（本轮诚实完成的是 AdminUsersPage 管理员任务流 / fallback named-region 收口，而不是夸大为新的 admin 钱包/争议/结算后端能力；single shared-console / new-api 叙事保持不变）；代码质量 APPROVED（diff 极小，集中在 1 个运行时文件与 1 个 focused 测试文件，新增语义锚点与测试合同对齐，没有扩散到服务层/路由层）；安全/集成 SAFE（静态 added-line 扫描无 secrets / shell / eval / SQL 风险，diff 不触碰 auth/menu/route 真值，也未引入权限泄漏；真实 API 再次证明 admin/user/supplier 菜单与共享路由链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新的 named-region/fallback proof 更贴近用户可见合同，既有 Semi/React warning 未被本轮 diff 放大）；性能/运维 PASS（纯前端语义增强，不增加请求、状态或部署负担；build 仅保留既有 chunk-size warning）。

### 最新执行检查点（2026-05-22，AdminRiskPage 风控中心 / 任务流 / fallback named-region 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，选择 `web/src/pages/AdminRiskPage.tsx` 与 `web/src/pages/AdminRiskPage.test.tsx` 作为下一前端 focused slice，而不是继续扩散到新的后端逻辑。目标不是新增风控后端能力、不是改 `/auth/menu` 真值、不是拆独立风控后台，而是在既有风控页已经承担风险信号、规则编辑、审计回放与 shared-console bridge 的前提下，把 hero、风控摘要信号区、管理员主任务流、共享接入桥接与 fallback 从残留的 `Card title` / `data-testid` proof 继续收口为 `role="region" + aria-labelledby` 合同，保持同一登录后的 shared-console 深色单壳进入风控、审计、API Keys、Webhook 与 Docs。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/AdminRiskPage.tsx` 与 `web/src/pages/AdminRiskPage.test.tsx`，确认根因不是路由失效、不是 `/auth/menu` 菜单漂移、也不是真实 API 契约变化，而是页面仍有关键区块继续依赖裸 `Card title` / `data-testid` 作为主要 proof。随后先在测试侧制造并完成 RED→GREEN：把 hero、任务流、fallback、API Keys 路由断言改为 `findByRole('region', { name: ... })`；运行时代码仅做最小实现，为 hero、风控摘要信号、任务流、shared-console fallback 增补 `role="region" + aria-labelledby` 与显式 heading id，不改任何后端接口、菜单真值、角色边界或 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/AdminRiskPage.test.tsx` 通过（6 tests，仅既有 Semi `act(...)` / `Form` 警告，未被本轮 diff 放大）；`pnpm --dir web exec vitest run src/pages/AdminRiskPage.test.tsx src/App.test.tsx` 通过（29 tests，AdminRisk focused + App shared-shell 集成仍稳定）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/run_admin_risk_region_real_api.sh`，最新产物目录为 `.hermes/local-output/admin-risk-region-real-api/20260522-150107`：验证 `GET /healthz` 返回 `200 + {status: ok, redis: ok}`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/api/v1/auth/me`、`/api/v1/auth/menu`、`/api/v1/dashboard/overview`、`/api/v1/auth/api-keys`、`/api/v1/webhooks/endpoints`，并显式确认普通用户菜单继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 且不泄漏 `/admin/risk`、`/supplier/resources`、`/supplier/settlements`；seeded supplier 登录后 `/api/v1/auth/menu`、`/api/v1/supplier/resources/overview`、`/api/v1/supplier/settlements` 返回 `200`；seeded admin 登录后 `/api/v1/auth/menu`、`/api/v1/admin/overview`、`/api/v1/admin/risk`、`/api/v1/admin/audit?limit=5` 返回 `200`，并确认 admin 菜单继续保留 `/admin/users`、`/admin/suppliers`、`/admin/pricing`、`/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；前端 `http://127.0.0.1:5173/`、`/login`、`/admin/risk`、`/admin/audit`、`/admin/users`、`/admin/suppliers`、`/api-keys`、`/webhooks`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（本轮诚实完成的是 AdminRiskPage 风控中心 / 任务流 / fallback named-region 收口，而不是夸大为新的风控后端或新的权限切片；single-shell role-aware 叙事保持不变）；代码质量 APPROVED（diff 极小，集中在 1 个运行时文件与 1 个 focused 测试文件，新增语义锚点与测试合同对齐，没有扩散到服务层/路由层）；安全/集成 SAFE（静态 added-line 扫描无 secrets / shell / eval / SQL 风险，diff 不触碰 auth/menu/route 真值，也未引入权限泄漏；真实 API 再次证明 admin/user/supplier 菜单与共享路由链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新的 named-region/fallback proof 更贴近用户可见合同，既有 Semi/React warning 未被本轮 diff 放大）；性能/运维 PASS（纯前端语义增强，不增加请求、状态或部署负担；build 仅保留既有 chunk-size warning）。
