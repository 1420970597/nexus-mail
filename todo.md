# Nexus-Mail 完整开发计划（todo.md）

> **执行模式已切换：** 本文档不再只是规划稿，而是当前仓库的**执行中路线图**。后续提交必须对照阶段目标更新状态，并在阶段完成后进入下一阶段。

## 执行状态看板（持续维护）

### 当前总体进度
- Phase 0：**已完成**（开发底座、Docker、多服务骨架已提交）
- Phase 1：**已完成**（身份权限、共享控制台骨架已提交）
- Phase 2：**已完成**（项目/库存/激活订单主链路、管理员项目配置、供应商资源录入已提交）
- Phase 3：**已完成**（已完成 SMTP 会话落盘、metadata 入库、RabbitMQ 解析任务入队、MinIO 原始对象上传，并打通验证码/link 提取回退增强、订单 READY/FINISHED 自动迁移、真实 OAuth2 刷新接入、授权码/App Password/密码型凭证录入与健康状态落库、`env://` secret_ref 解析、IMAP/POP3 实际登录校验、官方邮箱端点限制、Proton Bridge 接入策略与 Postfix 转发样例）
- Phase 4：进行中（已完成用户钱包、冻结余额、成功扣费、超时退款、供应商待结算余额、管理员调账能力与基础结算页面；已完成供应商报表筛选、供货规则维护、管理员争议单显式筛选、供应商域名运营页、管理员供应商运营页与共享控制台深色单壳导航收敛；已完成 Webhooks / 风控 / 审计真实页面收敛与对应真实 API 回放验证；当前前端后续重点继续围绕 shared-console focused 用例去脆弱化、语义锚点收敛与 new-api 风格单壳一致性补强，再回到 Phase 4/5 余下后端能力）
- Phase 5：进行中（已完成管理员高危操作二次确认、OpenAPI + `/docs`、管理员审计查询、管理员概览/风控真实页、API Key 默认 scopes / 白名单 / Redis 全局限流 / 审计、Webhook 异步投递与 delivery 查询、风控规则 CRUD 等里程碑；后续在“前端先行、真实 API 验证、五维评审后提交”的约束下继续推进 API Key / Webhook / IP 白名单 / 限流 / 风控 / 审计余下切片）
- Phase 6：未开始

### 本机环境现状（2026-04-13 更新）
- `go version`：可用
- `docker version`：可用
- `docker compose version`：可用
- `pnpm -v`：可用
- 本机已验证可绑定 **25 端口**

### 本地参考资料
- 已在项目目录放置竞品调研参考：`docs/reference-local/luckyous-reference.md`
- 该目录已加入本地 Git 排除规则，**仅作为本机开发参考，不推送远程仓库**


### 最新执行检查点（2026-05-20，ApiDocsPage 路由目标 named-region 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/ApiDocsPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 `/docs` 运行时代码、不是改 `/auth/menu` 真值、不是新增独立文档后台，而是在既有 docs bridge / continuation lane 已能命中项目市场、API Keys、Webhook、余额中心与共享首页的前提下，把这些目标页 proof 从“仅命中 test-id stub”补强为“命中命名 shared-console region + heading”的可访问合同，继续对齐此前 LoginPage / ProfilePage 已推进的 shared-console 目标页语义锚点。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ApiDocsPage.test.tsx` 与 `web/src/pages/ApiDocsPage.tsx`，确认根因不是 `/docs` 页面 bridge/loop CTA 失效、不是 route 常量漂移、不是 shared-console homepage fallback 错误、也不是真实 API 不一致，而只是 ApiDocs focused suite 对项目市场、余额中心、API Keys、Webhook 四类目标页仍部分停留在 `findByTestId(...)` proof：虽然已经证明点击能命中 stub，但尚未直接保护“命中的仍是共享控制台内的命名目的页 region，而不是仅测试节点存在”这一前端合同。随后先在 `ApiDocsPage.test.tsx` 制造 RED：为项目市场 / 余额中心 / API Keys / Webhooks route stubs 增补 `role="region" + aria-label`，并把 bridge/loop 相关导航断言升级为 `findByRole('region', { name: '共享控制台 - *' })` 后再在该 region 内验证 heading；未改任何运行时代码、未改后端接口、未改 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 通过（6 tests，仅既有 React/Semi `findDOMNode` warning）；`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx src/pages/LoginPage.test.tsx src/pages/ProfilePage.test.tsx` 通过（27 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/apidocs-route-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/apidocs-route-region-real-api/20260520-053213`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`，并显式确认 `/auth/menu.items` 继续暴露 `/docs`、`/api-keys`、`/webhooks`；seeded admin 使用 `admin@nexus-mail.local / Admin123!` 登录返回 `200`，且 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 均返回 `200`，继续确认 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks` 保留在管理端菜单中；前端 `http://127.0.0.1:5173/`、`/login`、`/docs`、`/api-keys`、`/webhooks` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（docs bridge/loop 目标页 proof 现直接保护 `共享控制台 - 项目市场 / 余额中心 / API Keys / Webhooks` 命名 region，不再只停留在 test-id 命中）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件；review 仅提出可选 helper 去重，未构成阻塞）；安全/集成 SAFE（纯前端测试与 stub 语义补强，不触碰 auth/session/menu/backend contract；真实 API 回放再次证明注册、菜单、docs/API Keys/Webhooks/admin 链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增 region 查询比裸 test-id 更贴近用户可见合同）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，ProfilePage 共享接入目标页 region 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/ProfilePage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 Profile 运行时代码、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有 `共享接入桥接` CTA 已能跳转 API Keys / Webhook / Docs 的前提下，把三个目标页 stub 统一补强为命名 `共享控制台 - *` region 合同，让 bridge click proof 不再只停留在 `findByTestId(...)`，同时保留 supplier/admin 的角色化 Webhook 目标页标题断言，继续对齐单一登录后控制台 / new-api 单壳语义与角色差异菜单合同。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ProfilePage.tsx` / `web/src/pages/ProfilePage.test.tsx`，确认根因不是 `/profile` 页面导航错误、不是 shared bridge CTA 缺失、不是 supplier/admin Webhook 角色标题漂移、也不是真实 API 不一致，而只是 Profile focused suite 里的 API Keys / Webhook / Docs 目标页 proof 仍主要依赖 route stub `data-testid`，尚未直接保护“命中的仍是共享控制台目的页 region”这一可访问语义。随后先在 `ProfilePage.test.tsx` 制造 RED：为 API Keys / Webhooks / Docs route stubs 增补 `role="region" + aria-label`，并把 supplier/admin Webhook 目标页断言升级为 `findByRole('region', { name: '共享控制台 - Webhooks' })` 后再校验 `resolveRouteTitle(...)`；同时将两条 webhook 用例标题改为明确“保留角色化目标页标题于 shared-console route region” 的真实合同。未改任何运行时代码、未改后端接口、未改 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 通过（9 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings）；`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/SettingsPage.test.tsx src/pages/LoginPage.test.tsx src/pages/ApiDocsPage.test.tsx` 通过（34 tests，仅既有 React/Semi warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/profile-shared-shell-real-api/run.sh`，最新产物目录 `.hermes/local-output/profile-shared-shell-real-api/20260520-050543`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`，并显式确认 `/auth/menu.items` 继续暴露 `/`、`/profile`、`/api-keys`、`/webhooks`、`/docs`；前端 `http://127.0.0.1:5173/profile`、`/login`、`/api-keys`、`/docs` 均返回 `200`；seeded admin 使用 `admin@nexus-mail.local / Admin123!` 登录返回 `200`，且 `/auth/menu`、`/admin/overview`、`/admin/audit?limit=5` 均返回 `200`，继续确认 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs` 保留在管理端菜单中。
- 本轮五维评审结论：产品/规格 PASS（Profile bridge 目标页 proof 现直接保护 `共享控制台 - API Keys/Webhooks/API 文档` 命名 region，同时 supplier/admin 仍保留角色化 Webhook 目标页标题断言，不再只停留在 test-id 命中）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，且在 reviewer 指出标题/断言不一致后已修正为真实命名与角色合同）；安全/集成 SAFE（纯前端测试与 stub 语义补强，不触碰 auth/session/menu/backend contract；真实 API 回放再次证明注册、菜单、Profile/API Keys/Webhooks/Docs/admin 链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增 region 查询提升了目的页语义锚点而未削弱角色标题覆盖）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，SettingsPage 快捷入口命名 region 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/SettingsPage.tsx` / `web/src/pages/SettingsPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 Settings 菜单过滤、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有 Settings 快捷入口卡片已经稳定承载 API Keys / Webhook / supplier/admin 角色入口的前提下，把该 lane 从 `data-testid` proof 收口为命名 `控制台运行快捷入口` region 合同，继续对齐单一登录后控制台 / new-api 单壳语义与此前 Dashboard / ApiDocs / LoginPage 已推进的 named-region 护栏。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/SettingsPage.tsx` / `web/src/pages/SettingsPage.test.tsx`，确认根因不是 `/settings` 页面运行时错误、不是 shortcut card 过滤失效、不是 bridge / mission lane ownership 漂移、也不是真实 API 不一致，而只是 focused suite 对 shortcut lane 仍主要依赖 `getByTestId('settings-shortcut-cards')`，尚未直接保护“这是用户可见的共享控制台快捷入口 region，而不是仅某个实现容器”这一前端合同。随后先在 `SettingsPage.test.tsx` 制造 RED：将 `keeps settings runtime shortcuts limited...` 用例升级为 `findByRole('region', { name: '控制台运行快捷入口' })`，继续在该 region 内约束仅暴露当前可见的 runtime shortcut cards；再最小修改 `SettingsPage.tsx`，仅为 shortcut lane `Card` 增补 `role="region" + aria-labelledby="settings-shortcut-cards-heading"`，并把可见标题绑定到稳定 id。未改任何后端接口、未改 route 常量、未改 shortcut 计算逻辑。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 通过（7 tests，仅既有 React/Semi `findDOMNode` warning）；`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/pages/ProfilePage.test.tsx src/pages/DashboardPage.test.tsx src/pages/LoginPage.test.tsx` 通过（38 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/settings-shortcut-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/settings-shortcut-region-real-api/20260520-044435`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认 `/auth/menu.items` 继续暴露 `/settings`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`，并确认管理端菜单保留 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；前端 `http://127.0.0.1:5173/`、`/login`、`/settings`、`/api-keys`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（shortcut lane 现直接保护“控制台运行快捷入口”命名 region，而不是仅 test-id 容器命中，shared-console 快捷入口合同更诚实）；代码质量 APPROVED（运行时代码仅新增 2 个 ARIA 属性与 1 个 heading id，测试也只是把查询源切换到用户可见语义，diff 极小且与仓库既有 named-region 模式一致）；安全/集成 SAFE（纯前端语义补强，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明注册、菜单与 settings/shared routes 链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新增断言更准确地区分 shortcut lane 的语义边界）；性能/运维 PASS（零运行时代码/部署开销，仅补 landmark 语义与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，SettingsPage 首轮回到共享控制台首页 region 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/SettingsPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 Settings 运行时代码、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有默认用户首轮引导可从设置中心重新回到 `/` 的前提下，把该落地面从 test-id proof 收口为命名 `共享控制台首页` region 合同，继续对齐单一登录后控制台 / new-api 单壳叙事与此前 LoginPage / ApiDocsPage 的首页 handoff 护栏。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/SettingsPage.test.tsx`，确认根因不是 `/settings` 页面运行时错误、不是 first-run reopen 逻辑失效、不是 dashboard 路由缺失、也不是真实 API 不一致，而只是首轮引导回到首页这一 focused proof 仍停留在 `findByTestId('settings-route-stub-shared-home')`，尚未直接保护“命中的是共享控制台首页命名 region，而不是仅某个测试桩节点”这一前端合同。随后先在测试桩层制造 RED：为 `/` route stub 增补 `role="region" aria-label="共享控制台首页"`，并把 `homeRegion` 查询升级为 `findByRole('region', { name: '共享控制台首页' })`；未改任何运行时代码、未改后端接口、未改 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 通过（7 tests，仅既有 React/Semi `findDOMNode` warning）；`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（42 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/settings-home-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/settings-home-region-real-api/20260520-042358`：验证 `GET http://127.0.0.1:8080/healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认 `/auth/menu.items` 继续暴露 `/settings`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`，并确认管理端菜单保留 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；前端 `http://127.0.0.1:5173/`、`/login`、`/settings`、`/api-keys`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（Settings first-run reopen 现直接保护“回到共享控制台首页 region”而不是仅 test-id 命中，单壳落地合同更诚实）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，`findByRole` 比 `findByTestId` 更贴近用户可见语义）；安全/集成 SAFE（纯前端测试与 stub 语义补强，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明注册、菜单与共享首页链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且五维评审未发现新的阻塞问题）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，LoginPage 注册跑道目标页 shared-console 单壳合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先收口 `web/src/pages/LoginPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改注册接口、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有注册跑道 CTA 已可跳转 API Keys / Webhooks / Docs 的前提下，把这些目标页 stub 从“仅证明命中 heading”补强为“继续处于同一共享控制台壳内”的命名 region 合同，继续对齐单一登录后控制台与 new-api 风格单壳叙事。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/LoginPage.tsx`、`web/src/pages/LoginPage.test.tsx` 与 `web/src/utils/consoleNavigation.tsx`，确认根因不是注册功能缺失、不是路由常量漂移、不是 post-register landing 错误、也不是 `/auth/menu` 与真实 API 不一致，而只是现有 focused suite 虽已证明 `首轮接入路径` CTA 能命中 API Keys / Webhooks / Docs route stubs，却尚未直接保护“命中后的目标页仍属于共享控制台单壳而不是第二套后台/独立登录站”这一前端合同。随后先在 `LoginPage.test.tsx` 制造 RED：为三个目标页 stub 增补 `role="region" + aria-label` 与 shared-console continuation copy，并新增 `keeps login runway destination stubs inside the same shared-console shell contract after navigating to API Keys, Webhooks, and Docs` 用例，逐一点击 `先配置 API Keys`、`继续联调 Webhook`、`查看 API 文档`，验证目标 region heading、shared-shell copy、`控制台总览` 缺失以及 `第二套后台/独立登录站` 负向断言；未改任何运行时代码、未改后端接口、未改 shared-console 路由真值。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（12 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx src/App.test.tsx` 通过（35 tests，仅既有 React/Semi warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/login-runway-shared-shell-real-api/run.sh`，最新产物目录 `.hermes/local-output/login-runway-shared-shell-real-api/20260520-035915`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`；并显式确认 `/auth/menu.items` 继续暴露 `/`、`/api-keys`、`/webhooks`、`/docs`、`/settings`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/risk`、`/admin/audit?limit=5`；前端 `http://127.0.0.1:5173/login`、`/api-keys`、`/webhooks`、`/docs` 均返回 `200` 且继续提供 SPA shell。
- 本轮五维评审结论：产品/规格 PASS（新增 focused 合同诚实保护“注册跑道命中的目标页仍属于共享控制台单壳”，不再只停留在 route-hit/heading proof）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，新增 stub region 与断言作用域一致，虽存在三段相似导航断言但不构成当前里程碑阻塞）；安全/集成 SAFE（纯前端测试与 stub 语义补强，不触碰 auth/session/token/backend contract，真实 API 回放再次证明注册、菜单与共享路由链路稳定）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新增断言直接防守目标页 shared-shell continuity 合同）；性能/运维 PASS（零运行时代码/部署开销，仅补 focused 护栏与本地 replay 脚本；构建仅保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-20，SettingsPage 快捷入口可见范围 focused 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/SettingsPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 Settings 运行时代码、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有 `控制台运行快捷入口` lane 合同上继续补强：明确该 shortcut lane 只承载当前真正可见的 runtime shortcut cards，不误承载 docs mission card、共享桥接标题或项目市场 CTA。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/SettingsPage.tsx` / `web/src/pages/SettingsPage.test.tsx`，确认根因不是 `/settings` 页面运行时错误、不是 docs route 缺失、不是 bridge CTA 丢失、也不是真实 API 不一致，而只是 focused suite 尚未单独保护 shortcut lane 的可见范围。随后先在 `SettingsPage.test.tsx` 制造 RED：新增 `keeps settings runtime shortcuts limited to currently visible shared-console route cards` 用例，限定默认用户菜单含 `/profile`、`/api-keys`、`/webhooks`、`/docs`、`/settings` 时，`settings-shortcut-cards` 车道只暴露 `开发者 API 接入工作台` 与 `开发者 Webhook 接入工作台` 这些真实 shortcut cards，并显式防守 `API 文档与接入控制台` heading、`共享路由常驻同一壳`、`共享接入桥接` 以及 `查看 API 文档` / `打开项目市场` CTA 不会误泄漏到该车道；不改任何运行时代码、不改路由常量、不改后端接口。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/pages/ProfilePage.test.tsx src/pages/DashboardPage.test.tsx src/pages/LoginPage.test.tsx` 通过（37 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/settings-shortcut-frontslice-real-api/run.sh`，最新产物目录 `.hermes/local-output/settings-shortcut-frontslice-real-api/20260520-033126`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认 `/auth/menu.items` 继续暴露 `/settings`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`；前端 `http://127.0.0.1:5173/`、`/login`、`/settings`、`/api-keys`、`/docs` 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（shortcut lane 现只证明当前真正可见的共享快捷卡，不再把 docs mission / bridge ownership 混入同一 focused proof）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，断言作用域清晰，且与现有 `within(shortcutLane)` 风格一致）；安全/集成 SAFE（纯前端测试补强，不触碰 auth/session/menu/backend contract，且负向断言仅约束 shortcut lane 自身，不错误声称 docs route 全局不可见）；测试/可靠性 PASS（focused/grouped Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增用例准确防守 shortcut lane 与 mission/bridge lane 的职责边界）；性能/运维 PASS（仅新增少量 focused 断言与本地 replay 脚本，无运行时代码/部署开销增加，本地产物已保存在 `.hermes/local-output/settings-shortcut-frontslice-real-api/20260520-033126` 可供下一轮连续复用）。

### 最新执行检查点（2026-05-20，ProfilePage 共享接入桥接 CTA 语义归位）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先收口 `web/src/pages/ProfilePage.tsx` / `web/src/pages/ProfilePage.test.tsx` 这一处 shared-console 前端切片，而不是扩散到新的后端逻辑。目标不是改 `/auth/menu` 真值、不是改角色落地策略、不是新增独立后台，而是把 `ProfilePage` 的 `共享接入桥接` region 从“错误承载目标工作台 heading”收口为“只承载 bridge 说明与 CTA 动作”的语义合同，并继续把角色差异标题保留在真实目标页/route stub 上。
- 控制器按 TDD + `systematic-debugging` 先回读 `ProfilePage.tsx`、`ProfilePage.test.tsx` 与已通过的 `DashboardPage` bridge contract，确认根因不是 bridge CTA 丢失、不是 `/auth/menu` 漂移、不是角色路由错误，也不是后端/真实 API 不一致，而只是 `ProfilePage` 仍把 `开发者 API 接入工作台`、角色化 Webhook 标题、`API 文档与接入控制台` 这些 destination heading 直接渲染在 `共享接入桥接` region 内，focused tests 也同步过度宣称了这种 ownership。随后先在 `ProfilePage.test.tsx` 制造 RED：把 bridge region 合同改为仅保护 bridge heading、CTA buttons 与 destination headings 缺失，并将 supplier/admin Webhook 用例改为“bridge 只保护 CTA、角色标题在点击后的目标页验证”；再最小修改 `ProfilePage.tsx`，为 capability cards 引入非 heading 的 eyebrow + paragraph copy，把 bridge 内部的 destination `Typography.Title` 改成非 heading 文本，保留按钮文案与导航路径不变；同时删除已不再使用的 `webhooksTitle` 变量与 `useMemo` 依赖噪音，不改任何后端接口或菜单计算。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 通过（9 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx src/pages/DashboardPage.test.tsx` 通过（19 tests，仅既有 React/Semi warnings，未被本轮 diff 放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/profile-bridge-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/profile-bridge-region-real-api/20260520-022330`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认 `/auth/menu.items` 继续暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`，并确认管理端菜单继续保留 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；前端 `http://127.0.0.1:5173/`、`/login`、`/profile`、`/api-keys`、`/docs` 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（bridge region 现仅承载共享接入桥接本体说明与 CTA，不再误冒充目标工作台标题，单一登录后 shared-console/new-api 单壳叙事更诚实）；代码质量 APPROVED（diff 仅集中在 1 个运行时文件与 1 个 focused test 文件，且顺手清理了未使用的 `webhooksTitle` 依赖）；安全/集成 SAFE（CTA 仍只导航到既有 canonical shared routes，菜单可见性继续受 `hasMenuPath` 约束，角色化 Webhook 标题仍在目标页验证，不触碰 auth/session/menu/backend contract）；测试/可靠性 PASS（focused Vitest、grouped rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且 tests 现更准确地区分 bridge ownership 与 destination ownership）；性能/运维 PASS（纯前端语义/copy 归位与 focused test 护栏，无新增网络、轮询或部署负担；本地 replay 脚本产物已保存在 `.hermes/local-output/profile-bridge-region-real-api/20260520-022330` 可供后续连续复用）。

### 最新执行检查点（2026-05-20，Dashboard capability matrix / bridge focused 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口 `web/src/pages/DashboardPage.test.tsx` 这一处 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 Dashboard 运行时代码、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有 Dashboard capability matrix / shared bridge 的 named-region 合同上继续补强，用 focused 断言明确区分“共享接入桥接卡片”与“目标工作台标题”，防止 bridge 区域错误冒充 API Keys / Webhook / Docs 目标页本体。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/DashboardPage.tsx` / `web/src/pages/DashboardPage.test.tsx`，确认根因不是 `/` 页面运行时结构错误、不是 bridge CTA 丢失、不是 capability matrix 文案漂移、不是真实 API 不一致，而只是 focused suite 先前虽然已经保护 `控制台能力矩阵` region 与 `共享接入桥接` region 的正向内容，却尚未直接断言 capability matrix 顶部 `共享壳能力总览` 标签，也未显式防守 bridge 区域错误渲染 `开发者 API 接入工作台` / `开发者 Webhook 接入工作台` / `API 文档与接入控制台` 这些本应属于目标页或其他 mission card 的 heading。随后仅最小修改 `DashboardPage.test.tsx`：在 `matrixScope` 内补上 `共享壳能力总览` 文案断言，并在 `bridgeScope` 内新增三条负向 heading 断言；不改任何运行时代码、不改路由常量、不改后端接口。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/DashboardPage.test.tsx` 通过（10 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/dashboard-capability-bridge-real-api/run.sh`；最新产物目录实际位于 `.hermes/local-output/dashboard-capability-bridge-real-api/20260520-001415`（脚本 stdout 打印路径含重复 `.hermes/` 前缀，但运行产物已落在仓库内单层忽略目录）。该回放验证了 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认 `/auth/menu.items` 暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`，并确认管理端菜单保留 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；前端 `http://127.0.0.1:5173/`、`/login`、`/projects`、`/orders`、`/api-keys`、`/docs` 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（shared / supplier / admin 三类菜单分组继续保持在同一登录后控制台内，新增 focused 断言诚实保护“共享路由不泄漏到扩展分组、扩展分组也不反向冒充共享菜单”的单壳侧栏合同）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，继续复用现有 `data-testid` 分组容器与 `within(...)` 作用域，未扩散到运行时代码）；安全/集成 SAFE（真实 API 回放再次确认普通用户菜单只暴露 shared routes，而管理员菜单同时包含 supplier/admin 扩展路径；前端 focused 断言与 `/auth/menu` 真值保持一致，没有新增独立后台或错误角色泄漏）；测试/可靠性 PASS（`pnpm --dir web exec vitest run src/components/AppSidebar.test.tsx src/App.test.tsx src/components/ProtectedRoute.test.tsx`、`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 均通过；仅保留既有 React/Semi `act(...)` / `findDOMNode` warning，未被本轮 diff 放大）；性能/运维 PASS（纯前端 focused 护栏与本地忽略 real-api 脚本补强，无运行时代码/部署开销增加，本地产物已保存在 `.hermes/local-output/sidebar-role-groups-real-api/20260520-004136` 可供下一轮连续复用）。

### 最新执行检查点（2026-05-19，ProjectsPage 首轮采购路径 named region 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先选择注册后主链路上仍缺少语义锚点的前端切片，而不是扩散到新的后端逻辑。目标不是改库存 API、不是改 `/auth/menu` 真值、不是改角色落地策略、不是新增独立后台，而是在 `ProjectsPage` 现有深色 shared-console/new-api 单壳里，把“注册后首轮采购路径”卡片从普通 Card/test-id proof 收口为具名 region + heading 合同，让注册后的 budget → fulfillment → API onboarding 路径对齐此前 LoginPage / ApiDocsPage / ProfilePage 等页已经推进的语义锚点标准。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ProjectsPage.tsx` 与 `web/src/pages/ProjectsPage.test.tsx`，确认根因不是 `/projects` 页面运行时结构错误、不是项目市场文案漂移、不是订单/API Keys CTA 丢失、不是真实 API 不一致，而只是 `projects-first-run-lane` 仍是普通容器，focused suite 也仍部分依赖 `data-testid` 与裸文本，而没有直接保护 `role="region" + aria-labelledby + heading` 这一 shared-console first-run lane 合同。随后先在 `ProjectsPage.test.tsx` 新增 RED：要求首轮采购路径可通过 `getByRole('region', { name: '采购 → 履约 → 接入' })` 访问，并在 region 内保护 heading、badge、核心 copy、订单/API Keys CTA 与错误 lane 文案缺失；再仅最小修改 `ProjectsPage.tsx`，为 `projects-first-run-lane` 增补 `role="region" aria-labelledby="projects-first-run-lane-heading"`，并把现有 heading 绑定稳定 id。未改后端接口、未改菜单优先级、未改登录注册/单壳路由策略。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProjectsPage.test.tsx` 通过（15 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning，未因本轮 diff 新增）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/projects-first-run-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/projects-first-run-region-real-api/20260519-234612`：验证 `GET /healthz` 返回 `200 + status=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认 `/auth/menu.items` 暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`，并确认管理端菜单保留 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；前端 `http://127.0.0.1:5173/`、`/login`、`/projects`、`/orders`、`/api-keys` 均返回 `200`。期间按 root-cause 仅修正了本地 replay 脚本的 admin seed 邮箱 harness 问题，未触碰应用代码契约。
- 本轮五维评审结论：产品/规格 PASS（注册后主采购路径现在以具名 region + heading 诚实表达“采购 → 履约 → 接入”的 shared-console first-run lane，而不是只停留在卡片视觉层）；代码质量 APPROVED（diff 极小，仅集中在 1 个运行时文件与 1 个 focused test 文件，作用域清晰，且继续沿用现有 named-region 收口模式）；安全/集成 SAFE（纯前端语义与测试收口，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明注册、菜单、dashboard、项目/订单/API Keys/Webhooks/admin 链路稳定）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增断言直接防守 first-run lane 的可访问语义合同）；性能/运维 PASS（仅新增静态 landmark 属性与极小测试护栏，零新增网络/状态开销，本地 replay 脚本位于忽略目录可持续复用且不污染远程仓库）。

### 最新执行检查点（2026-05-19，LoginPage auth-copy named-region 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理登录/注册壳层里仍缺少语义锚点的前端切片，而不是扩散到新的后端逻辑。目标不是改注册 API、不是改 `/auth/menu` 真值、不是新增独立后台，而是在 `LoginPage` 现有深色 shared-console/new-api 单壳里，把 auth shell 顶部 mode-specific copy 从普通容器收口为具名 region，并用 focused 用例直接保护登录/注册两种模式下 copy landmark 与 guidance banner 都不会泄漏错误模式文案。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/LoginPage.tsx` / `web/src/pages/LoginPage.test.tsx`，确认根因不是注册功能缺失、不是 mode 切换失效、不是 `/auth/menu` 漂移、也不是 shared-console post-register landing 错误，而只是 auth shell 顶部 `login-auth-copy-block` 仍停留在普通容器层，focused suite 也只直接保护 guidance banner，尚未把登录/注册说明本身升级为 named region 合同。随后先在 `LoginPage.test.tsx` 新增 RED：要求 login mode 出现 `登录认证说明` region、register mode 出现 `注册认证说明` region，并分别保护正确 heading / helper copy 与错误模式文案不泄漏；再仅最小修改 `LoginPage.tsx`，为 `login-auth-copy-block` 增补 `role="region"` 与按 mode 切换的 `aria-label`。不改后端接口、不改路由常量、不改 session/token 语义。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（11 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/login-auth-copy-real-api/run.sh`，最新产物目录 `.hermes/local-output/login-auth-copy-real-api/20260519-231727`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认 `/auth/menu.items` 暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`，并确认管理端菜单保留 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；前端 `http://127.0.0.1:5173/`、`/login`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 均返回 `200`。期间按 root-cause 仅修正了本地 replay 脚本的 admin seed 邮箱与 admin menu 输出文件命名/断言问题，未触碰应用代码契约。
- 本轮五维评审结论：产品/规格 PASS（auth-copy region 现在诚实表达登录/注册两种模式下的单一 shared-console 身份入口说明，而不是只剩 guidance banner 一层 proof）；代码质量 APPROVED（diff 极小，仅集中在 1 个运行时文件与 1 个 focused test 文件，命名清晰，review 仅建议未来若继续扩展可考虑 `aria-labelledby` 进一步降低 copy drift 风险）；安全/集成 SAFE（纯前端语义与 focused test 收口，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明注册、菜单、dashboard、API Keys、Webhook、admin 概览链路稳定）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增断言直接防守 named auth-copy region 与错误模式 copy 泄漏回归）；性能/运维 PASS（仅新增静态 landmark 属性与极小测试护栏，零新增网络/状态开销，新增 replay 脚本保存在本地忽略目录可持续复用且不污染远程仓库）。

### 最新执行检查点（2026-05-19，bootstrap shell / sidebar loading named-region 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理共享壳层仍未完全语义收口的 bootstrap / menu-loading 前端切片，而不是扩散到新的后端逻辑。目标不是改登录注册 API、不是改 `/auth/menu` 真值、不是改角色落地策略、不是新增独立后台，而是在同一套登录后控制台里，把 post-login bootstrap 恢复壳与侧栏菜单加载态都提升为具名 region + heading 合同，并用 focused 用例直接保护这些 loading landmark。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/App.tsx`、`web/src/components/AppSidebar.tsx` 及对应 focused tests，确认根因不是 auth bootstrap 逻辑错误、不是 `/auth/menu` 集成异常、不是 dashboard 首屏回退错误，而只是两处 shared-shell loading 面板仍停留在普通容器 / test-id 级 proof：`AuthBootstrapShell` 没有稳定可访问 landmark 名称；侧栏 `共享菜单加载中` 卡片也尚未升级为 named region。随后仅最小修改 `web/src/App.tsx` / `web/src/components/AppSidebar.tsx` 与 `web/src/App.test.tsx` / `web/src/components/AppSidebar.test.tsx`：为 bootstrap shell 增补 `section role="region" aria-labelledby` 并把 heading 绑定为 `正在恢复共享控制台`；为侧栏 loading 卡增补 `role="region" aria-labelledby` 并把标题提升为真实 heading；focused tests 则分别验证 named region、heading、文案、loading 期间无目标内容泄漏。未改任何后端接口、未改 landing route 优先级、未改角色菜单真实来源。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx src/components/AppSidebar.test.tsx` 通过（28 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning，未因本轮 diff 新增）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/bootstrap-shell-real-api/run.sh`，最新产物目录 `.hermes/local-output/bootstrap-shell-real-api/20260519-224545`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认 `/auth/menu.items` 暴露 `/`、`/projects`、`/orders`、`/balance`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`，并确认管理端菜单保留 `/admin/risk`、`/admin/audit`、`/api-keys`、`/webhooks`、`/docs`；前端 `http://127.0.0.1:5173/`、`/login`、`/profile`、`/api-keys`、`/webhooks`、`/settings`、`/docs` 均返回 `200`。期间按 root-cause 仅修正了本地 replay 脚本的 health JSON 读取与 admin seed 邮箱 harness 问题，未触碰应用代码契约。
- 本轮五维评审结论：产品/规格 PASS（bootstrap 恢复壳与 shared-menu loading 现在都以具名 region + heading 诚实表达“单一登录后 shared-console 正在恢复/同步”的用户可见合同，而非仅停留在 test-id/card 证明）；代码质量 APPROVED（diff 极小，仅集中在 2 个运行时文件与 2 个 focused test 文件，且沿用仓库既有 named-region 收口风格）；安全/集成 SAFE（纯前端语义与 focused test 收口，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明注册、菜单、dashboard、API Keys、Webhook、admin 概览链路稳定）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新断言明确保护 loading 期间无内容泄漏）；性能/运维 PASS（仅新增语义标签与 heading，零新增网络/状态开销；构建只保留既有 chunk-size warning，未新增运维风险）。

### 最新执行检查点（2026-05-19，App fallback dashboard contract + Profile admin return region 收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，只收口已在工作树中的两个 focused 前端切片，而不是扩散到新的后端逻辑。目标不是改运行时路由、不是改 `/auth/menu` 真值、不是新增独立后台，而是把 `App.test.tsx` 中 supplier 无更高优先级落点场景的合同修正为真实的 dashboard fallback 证明，同时把 `ProfilePage.test.tsx` 管理员 return lane 的 proof 从 card/test-id 风格继续收口为 named region + heading + scoped CTA + 当前描述文案合同。
- 控制器先按 `systematic-debugging` 回读 `web/src/App.test.tsx`、`web/src/pages/ProfilePage.test.tsx` 与相关运行时代码，确认根因不是 shared-console 导航错误、不是 docs/profile 页面运行时异常、不是 `/auth/menu` 与角色菜单漂移，而是两处 focused 用例描述/断言与真实产品合同仍有残留偏差：其一，`App.test.tsx` 旧用例仍把 supplier 仅剩 `/docs` 菜单时的真实落点描述成 docs workspace，而当前产品逻辑实际回退到 dashboard workspace；其二，`ProfilePage.test.tsx` 管理员 settings 可用场景虽然运行时代码早已提供 return lane，但 focused proof 仍停留在 test-id/card 级断言，未直接保护 named region / heading / scoped CTA。基于此，本轮仅最小修改两个 test 文件，不改任何运行时代码或后端接口。
- 控制器验证结果：`pnpm --dir web exec vitest run src/App.test.tsx src/pages/ProfilePage.test.tsx` 通过（31 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/app-profile-front-slice-real-api/run.sh`，最新产物目录 `.hermes/local-output/app-profile-front-slice-real-api/20260519-221653`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认 `/auth/menu.items` 暴露 `/profile`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`；前端 `http://127.0.0.1:5173/`、`/login`、`/profile`、`/api-keys`、`/docs` 均返回 `200`。期间按 root-cause 修正了 replay 脚本最初把非法邮箱误写成仍包含 `@` 的 harness 问题，未触碰应用代码契约。
- 本轮五维评审结论：产品/规格 PASS（App fallback 用例现在诚实保护 dashboard workspace 回退合同，Profile 管理员 return lane 则直接绑定 named region / heading / CTA / 文案，不再误称 docs 落地或停留在弱 card proof）；代码质量 APPROVED（diff 极小，仅集中在 2 个 focused test 文件，且作用域与既有 shared-console 断言风格一致）；安全/集成 SAFE（纯前端测试收口，不触碰 auth/session/menu/backend contract；真实 API 回放再次证明注册、菜单、Profile/API Keys/Webhooks/Docs/admin 链路稳定）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增/修正断言直接防守真实产品合同）；性能/运维 PASS（零运行时代码开销，仅补 focused 护栏；新增 replay 脚本保存在本地忽略目录，可持续复用且不污染远程仓库）。

### 最新执行检查点（2026-05-19，ProfilePage fallback return region focused 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理工作树里唯一可提交的 `web/src/pages/ProfilePage.test.tsx` focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 Profile 运行时路由、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有 `profile-shared-console-return` 已升级为 named region 的前提下，把 user/supplier/admin 三类 fallback return-card 用例一起收口到同一套可访问合同：通过 `role="region" + heading + scoped CTA + 当前文案` 证明 return lane，而不是继续停留在 `data-testid`/单节点文本层面的弱 proof。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ProfilePage.tsx` / `web/src/pages/ProfilePage.test.tsx`，确认根因不是 `/profile` 页面回退逻辑错误、不是 settings fallback 失效、不是共享控制台首页 stub 丢失、也不是真实 API 不一致，而只是 2026-05-19 先前里程碑已把运行时代码升级为 named region + real heading，但 user/supplier/admin fallback focused 用例仍在使用 `getByTestId('profile-shared-console-return')` 与弱文本断言，导致 return-card accessible contract 保护不完整。随后仅最小修改 `ProfilePage.test.tsx`：把三类 fallback case 全部升级为 `getByRole('region', { name: ... })` + `getByRole('heading', { name: ... })` + scoped CTA + 当前描述文案断言，并把“无 return card”场景从 test-id absence 收口为 region absence；不改任何运行时代码、不改路由常量、不改后端接口。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 通过（9 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/profile-fallback-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/profile-fallback-region-real-api/20260519-211033`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认 `/auth/menu.items` 暴露 `/profile`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`；前端 `http://127.0.0.1:5173/`、`/login`、`/profile`、`/api-keys`、`/docs` 均返回 `200`。期间按 root-cause 先后修正了本地 replay 脚本的 health JSON 解析、`/api/v1/auth/*` 路径与 admin seed 邮箱三个 harness 问题，未触碰应用代码契约。
- 本轮五维评审结论：产品/规格 PASS（fallback return-card 用例现在诚实保护 named region / heading / CTA / copy 合同，而不是只证明某个 test id 仍存在）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，断言风格与先前 settings-based region 用例保持一致）；安全/集成 SAFE（纯前端测试收口，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明 Profile/API Keys/Webhooks/Docs/admin 链路稳定）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且“隐藏 return region”场景已从 test-id absence 升级为可访问语义 absence）；性能/运维 PASS（零运行时代码开销，仅补 focused 护栏；新增 replay 脚本保存在本地忽略目录，可持续复用且不污染远程仓库）。

### 最新执行检查点（2026-05-19，ProfilePage 共享工作台返回卡 named-region 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，锁定 ProfilePage 里尚未完成语义收口的 shared-console return card，而不是扩散到新的后端逻辑。目标不是改 `/auth/menu` 真值、不是改角色回落路由策略、不是引入独立后台，而是在既有个人资料 shared-console/new-api 单壳页面基础上，把“返回共享工作台”卡片从 test-id/card-only proof 收口为具名 region + 真正 heading 的可访问合同，并用 focused supplier 用例显式证明 CTA 受限于该 return lane。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ProfilePage.tsx` / `web/src/pages/ProfilePage.test.tsx`，确认根因不是 `/profile` 页面导航错误、不是 supplier fallback 落点错误、不是 `/settings` 不可达、不是真实 API 不一致，而只是 `profile-shared-console-return` 仍缺少稳定语义锚点；现有 focused supplier 用例也还停留在 test-id + button proof，尚未直接保护 named region / heading / scoped CTA 这一 shared-console handoff 合同。随后先在 `ProfilePage.test.tsx` 把 supplier 用例升级为 `getByRole('region', { name: '通过设置中心回到共享控制台' })` + scoped heading/button/description 断言，制造 RED；再仅最小修改 `ProfilePage.tsx`，为 return card 增补 `role="region" aria-labelledby="profile-shared-console-return-heading"`，并把卡片标题从 `Typography.Text` 提升为真实 `Typography.Title heading={6}`。不改 fallback route 计算，不改菜单权限，不改后端接口。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 通过（9 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/profile-return-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/profile-return-region-real-api/20260519-204515`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式确认 `/auth/menu.items` 暴露 `/profile`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`；前端 `http://127.0.0.1:5173/`、`/login`、`/profile`、`/api-keys`、`/docs` 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（return lane 现在以具名 region + heading 诚实表达“通过设置中心回到共享控制台”的单壳 handoff 合同，而不是继续停留在 test-id/card-only 证明）；代码质量 APPROVED（diff 极小，仅集中在 1 个运行时文件与 1 个 focused test 文件，且未引入额外 test id 膨胀或路由逻辑改动）；安全/集成 SAFE（纯前端语义与测试收口，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明注册、菜单、Profile/API Keys/Webhooks/Docs/admin 链路稳定）；测试/可靠性 PASS（focused Vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新增用例直接防守 return lane 的 named-region / scoped-CTA 合同）；性能/运维 PASS（仅新增静态语义属性与 heading，零网络/后端开销，本地 replay 脚本位于忽略目录可持续复用）。

### 最新执行检查点（2026-05-19，SettingsPage 共享接入桥接 named-region 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理工作树里唯一未提交的 `web/src/pages/SettingsPage.test.tsx` focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 Settings 运行时代码、不是改 `/auth/menu` 真值、不是新增独立后台，而是在既有深色 shared-console/new-api 单壳设置页基础上，把 regular-user 用例标题与 bridge proof 收口为“共享接入桥接具名 region 合同”，并显式防守 bridge 区域错误复用 mission-card 工作台 heading 的回归面。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/SettingsPage.tsx` / `web/src/pages/SettingsPage.test.tsx`，确认根因不是 `/settings` 页面运行时结构错误、不是 bridge CTA 丢失、不是真实 API 不一致，而只是 focused 用例标题仍过度强调“整页深色样式”，且 shared bridge 区域只校验了 subheading / copy / CTA，尚未直接证明该 region 不会错误重复渲染 `开发者 API 接入工作台`、`开发者 Webhook 接入工作台`、`API 文档与接入控制台` 这些本应留在 `settings-mission-cards` 的 heading。随后仅最小修改 `web/src/pages/SettingsPage.test.tsx`：重命名该条用例以匹配真实合同，并在 `within(sharedBridge)` 内新增三个负向 heading 断言；不改任何运行时代码、不改路由常量、不改后端接口。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx` 通过（6 tests，仅既有 React/Semi `findDOMNode` warning）；`pnpm --dir web exec vitest run src/pages/SettingsPage.test.tsx src/pages/LoginPage.test.tsx src/components/ConsoleLayout.test.tsx src/components/AppSidebar.test.tsx src/App.test.tsx` 通过（46 tests，仅既有 React `act(...)` / Semi `findDOMNode` warning，未被本轮 diff 新增或放大）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/settings-bridge-real-api/run.sh`，最新产物目录 `.hermes/local-output/settings-bridge-real-api/20260519-201629`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；非法邮箱注册返回 `400 + 请输入有效邮箱`；短密码注册返回 `400 + 密码长度至少为 8 位`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式断言 `/auth/menu.items` 同时暴露 `/settings`、`/api-keys`、`/webhooks`、`/docs`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`；前端 `http://127.0.0.1:5173/`、`/login`、`/settings`、`/api-keys`、`/webhooks`、`/docs` 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（用例标题与断言更诚实地聚焦 shared bridge named-region 合同，而不是把整页视觉描述与 bridge 结构混为一体）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，新增断言作用域明确且与现有 `within(sharedBridge)` 风格一致）；安全/集成 SAFE（纯前端测试收口，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明 settings/API Keys/Webhooks/Docs/shared-console 链路稳定）；测试/可靠性 PASS（focused Vitest、grouped rerun、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新增负向断言直接防守 bridge 区域结构漂移而未弱化原有 navigation proof）；性能/运维 PASS（零运行时代码开销，仅补 focused 护栏；新增 replay 脚本位于本地忽略目录，可连续复用且不污染远程仓库）。

### 最新执行检查点（2026-05-19，WebhooksPage 用户 guidance region 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理工作树里唯一未提交的 `web/src/pages/WebhooksPage.test.tsx` focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 Webhook 异步投递后端、不是改 `/auth/menu` 真值、不是新增独立后台，而是在已完成的 Webhook shared-console/new-api 单壳页面基础上，把 default-user guidance card 的 copy 合同从较细粒度 tips 子容器 proof 收口为整张 guidance region 级别的用户文案合同，继续补强前端先行的 shared-console 质量护栏。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/WebhooksPage.tsx` 与 `web/src/pages/WebhooksPage.test.tsx`，确认根因不是 `/webhooks` 页面运行时内容错误、不是 shared-console bridge / integration loop 漂移、不是真实 API 不一致，而只是 focused suite 里的 `renders canonical user guidance copy without legacy API Keys wording` 仍主要依赖 `webhooks-role-tips` 子容器断言，尚未直接保护整张 guidance card 的 `用户视角` badge 与核心描述文案。随后仅最小修改 `web/src/pages/WebhooksPage.test.tsx`：保留页面级 heading proof，新增对 `webhooks-role-guidance` 容器的 `用户视角` badge 与 `创建账户级回调 endpoint，测试真实投递链路，并跟踪最近 50 条异步 delivery 状态。` 描述文案断言，并把 legacy copy 的负向保护扩大到整个 guidance region；不改运行时代码、不改路由常量、不改后端接口。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/WebhooksPage.test.tsx` 通过（19 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/webhooks-guidance-real-api/run.sh`，最新产物目录 `.hermes/local-output/webhooks-guidance-real-api/20260519-184130`：验证 `GET /healthz` 返回 `200 + status=ok`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/auth/api-keys`、`/webhooks/endpoints`、`/dashboard/overview`；并显式断言 `/auth/menu.items` 同时暴露 `/webhooks` 与 `/api-keys`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`；前端 `http://127.0.0.1:5173/`、`/login`、`/webhooks`、`/api-keys`、`/docs` 均返回 `200`。期间按 root-cause 先后修正本地 replay 脚本的 health 路径、`/auth/menu.items` 取值与 admin 种子邮箱写法这三个 harness 问题，未触碰应用代码契约。
- 本轮五维评审结论：产品/规格 PASS（新增断言诚实收口在 default-user guidance card 的用户视角与核心描述合同，不夸大新的业务能力）；代码质量 APPROVED（diff 极小，仅集中在 1 个 focused test 文件，作用域从 tips 子容器提升到 guidance region 更稳健）；安全/集成 SAFE（纯前端测试加固，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明 Webhook/API Keys/shared-console 菜单链路稳定）；测试/可靠性 PASS（focused vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且负向断言已从 tips 子容器扩大到整张 guidance card）；性能/运维 PASS（零运行时代码开销，仅补 focused 护栏；新增 replay 脚本保存在本地忽略目录，便于后续连续执行且不污染远程仓库）。

### 最新执行检查点（2026-05-19，ApiDocs continuation lane region + fallback CTA 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理工作树里唯一未提交的 `web/src/pages/ApiDocsPage.tsx` / `web/src/pages/ApiDocsPage.test.tsx` focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 `/docs` 后端契约、不是改 `/auth/menu` 真值、不是新增独立文档后台，而是在既有 API Docs dark shared-console 页面基础上，把 docs continuation lane 从普通 Card 内容提升为具名 shared-console region，并补一条 focused 用例显式保护 fallback CTA 只在该 lane 内暴露与回到共享控制台首页的合同。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ApiDocsPage.tsx`、`web/src/pages/ApiDocsPage.test.tsx` 与当前 diff，确认根因不是 `/docs` 页面内容错误、不是 bridge/actions 丢失、不是真实 API 不一致，而只是 docs continuation lane 仍缺少稳定的语义锚点，focused suite 也尚未直接证明 `返回共享工作台` CTA 受限于该 lane 且点击后命中首页 region。随后仅最小修改运行时代码与测试：在 `ApiDocsPage` 为 loop lane 增补 `section role="region" aria-labelledby="docs-shared-console-loop-heading"`，把可见标题提升为真实 heading；在 `ApiDocsPage.test.tsx` 新增 1 条 focused 用例，断言 lane region、核心描述、三个 CTA、legacy 按钮缺失，以及 fallback 点击后命中 `共享控制台首页` region + `控制台总览` heading；不改后端接口、不改路由常量、不拆第二套后台。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiDocsPage.test.tsx` 通过（6 tests，仅既有 React/Semi `findDOMNode` warning）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/apidocs-loop-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/apidocs-loop-region-real-api/20260519-192651`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`，并显式断言 `/auth/menu.items` 同时暴露 `/docs`、`/api-keys`、`/webhooks`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`；前端 `http://127.0.0.1:5173/`、`/login`、`/docs`、`/api-keys` 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（lane region 与 fallback CTA 合同更诚实地表达“文档仍服务同一套共享控制台接入链路”，未过度声称新业务能力）；代码质量 APPROVED（diff 极小，仅集中在 1 个运行时文件与 1 个 focused test 文件，语义锚点与用例作用域一致）；安全/集成 SAFE（仅前端语义与测试补强，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明 docs/API Keys/Webhooks/shared-home 链路稳定）；测试/可靠性 PASS（focused vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新增用例直接防守 fallback CTA 泄漏/失联回归）；性能/运维 PASS（零运行时请求开销，仅新增语义包裹与测试护栏，本地 replay 脚本已保存在忽略目录便于后续连续执行）。

### 最新执行检查点（2026-05-19，ApiKeysPage hero supplier 文本合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理工作树里唯一未提交的 `web/src/pages/ApiKeysPage.test.tsx` focused 前端切片，而不是扩散到新的后端逻辑。目标不是改 API Key 后端行为、不是改 `/auth/menu` 真值、不是改 Webhook / Docs 业务页，而是在已有 `ApiKeysPage` dark shared-console/new-api 单壳页面基础上，把 supplier hero 文本 proof 从较细粒度 `getByText` 节点匹配收口为 hero-card 容器级合同，同时保留页面级 heading 证明，并显式防止 hero 回退到 default-user badge 文案。
- 控制器按 TDD + `systematic-debugging` 先回读 `web/src/pages/ApiKeysPage.test.tsx` 与 `web/src/pages/ApiKeysPage.tsx`，确认根因不是 supplier 角色页面内容错误、不是 shared-console bridge 漂移、不是真实 API 不一致，而只是 focused test 首个 supplier hero 用例仍把 badge / tips 绑定在节点级 `getByText` 上，缺少对 hero-card 整体文案组合与“不得回退到基础接入 badge”这一回归面的直接保护。随后仅最小修改 `web/src/pages/ApiKeysPage.test.tsx`：保留 `开发者 API 接入工作台` heading proof，把 supplier badge 与两条运营 tips 收口为 `api-keys-hero-card` 容器级 `toHaveTextContent` 断言，并新增 `not.toHaveTextContent('共享控制台 · 基础接入')` 负向保护；不改运行时代码、不改路由常量、不改任何后端接口。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ApiKeysPage.test.tsx` 通过（14 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/api-keys-hero-focus-real-api/run.sh`，最新产物目录 `.hermes/local-output/api-keys-hero-focus-real-api/20260519-181450`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`；前端 `http://127.0.0.1:5173/`、`/login`、`/api-keys`、`/docs` 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（supplier hero 断言更诚实地绑定到当前 shared-console/new-api 单壳页面卡片合同，未过度声称新的业务能力）；代码质量 APPROVED（diff 极小，只集中在 1 个 focused test 文件，且 reviewer 提醒后已把负向文案校正为实际 default-user badge `共享控制台 · 基础接入`）；安全/集成 SAFE（纯前端测试收口，不触碰 auth/session/menu/backend contract，真实 API 回放再次证明注册、菜单、dashboard、API Keys 与 Webhook 链路稳定）；测试/可靠性 PASS（focused vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，且新增负向断言直接防守 supplier hero 回退）；性能/运维 PASS（零运行时代码开销，仅补 focused 护栏；现存 build chunk warning 与 React/Semi 测试 warning 均为既有噪音，未被本轮 diff 扩大）。

### 最新执行检查点（2026-05-19，ProfilePage focused 文本断言容器收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先挑选 `web/src/pages/ProfilePage.test.tsx` 中仍依赖较细文本节点匹配的 focused 前端切片，只收口 Profile 首个 mission-control 用例的断言作用域，而不扩散到新的后端能力。目标不是改注册/登录契约、不是改 `/auth/menu` 真值、不是引入独立后台，而是在同一登录后 shared-console/new-api 单壳里，把 hero / role-focus / capability / capability-matrix / role-expansion 这些已有局部卡片的 copy proof 从节点级 `getByText/queryByText` 收口为容器级 `toHaveTextContent`，降低 Semi UI 文本包裹层变化带来的 brittle failure，同时继续保留真实共享控制台入口与角色差异菜单合同。
- 控制器按 TDD + `systematic-debugging` 先从 `ProfilePage.test.tsx` / `ProfilePage.tsx` 回读当前 focused proof，再做最小 RED→GREEN 假设：根因不是 `/profile` 路由缺失、不是 profile runtime 内容错误、不是 `/auth/menu` 漂移、不是 API Keys / Webhook / Docs 按钮落点异常，而只是测试仍把多处稳定 copy 绑定在具体文本节点级匹配上。随后仅最小修改 `web/src/pages/ProfilePage.test.tsx`：保留 `个人资料中枢`、`用户接入焦点`、`共享接入桥接`、`控制台能力矩阵`、`开发者 API 接入工作台` 等关键 heading / button / role proof，不新增任何 runtime test id，不改运行时代码，只把局部卡片中的 copy 正负向断言收口为容器级 `toHaveTextContent` / `not.toHaveTextContent`。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/ProfilePage.test.tsx` 通过（9 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web build`、`go test ./...`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/profile-page-focus-real-api/run.sh`，最新产物目录 `.hermes/local-output/profile-page-focus-real-api/20260519-085545`：验证 `GET /healthz` 返回 `200 + status=ok`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`；seeded admin 登录后可成功访问 `/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`；前端 `http://127.0.0.1:5173/`、`/login`、`/profile`、`/api-keys`、`/docs` 均返回 `200`。另记录：`docker compose up -d --build web api gateway` 本轮因 `Dockerfile.web` 中 `pnpm install --frozen-lockfile` 命中 `ERR_PNPM_IGNORED_BUILDS`（`esbuild@0.21.5` build scripts 未批准）失败，属于镜像环境/构建链阻塞，不影响当前已健康容器上的真实 API 与前端页面验证。
- 本轮五维评审结论：产品/规格 PASS（Profile 继续诚实表达单一登录后 shared-console/new-api 单壳与角色差异菜单，未把测试弱化成纯实现快照）；代码质量 APPROVED（当前 diff 仅集中在 1 个 focused test 文件，容器级文本断言较节点级匹配更稳健）；安全/集成 SAFE（未改 auth/session/menu/backend contract，仍保留 API Keys / Webhook / Docs 导航与 fallback 合同）；测试/可靠性 PASS（focused vitest、build、Go tests、diff-check 与真实 API 回放全部通过，且新断言仍严格限定在局部卡片容器内）；性能/运维 PASS（纯测试去脆弱化，不新增运行负担；唯一阻塞为 Docker 构建环境的 pnpm ignored-builds 配置，已如实记录）。

### 最新执行检查点（2026-05-19，ConsoleLayout 页面摘要 region + footer 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先收口 `web/src/layouts/ConsoleLayout.tsx` / `web/src/components/ConsoleLayout.test.tsx` 这一层共享壳合同，而不是继续扩散到新的后端能力。目标不是改 `/auth/menu` 真值、不是改注册/登录流程、不是引入第二套后台，而是在单一登录后的共享控制台壳里，把页面摘要从普通 `group` 收口为可命名 `region`，并补上显式 `contentinfo` 页脚合同，让 header-summary / main / footer 三层语义在同一壳内更完整，也让 focused test 更贴近真实可访问树。
- 控制器先从现有 diff 与 focused proof 出发核验最小切片：当前运行时代码仅把 `console-layout-header-summary` 从 `role="group"` 提升为 `role="region" aria-label="当前页面摘要"`，并新增 `footer role="contentinfo" aria-label="共享控制台页脚"`；对应 `ConsoleLayout.test.tsx` 也只补上 `getByRole('region', { name: '当前页面摘要' })` 与 `getByRole('contentinfo')` 的合同断言。根因确认不是 shared-console 导航错误、不是 quick actions 漂移、不是登录后路由身份异常，而只是壳层语义仍少一层稳定 landmark/source of truth，需要以最小前端 diff 收口。
- 控制器验证结果：`pnpm --dir web exec vitest run src/components/ConsoleLayout.test.tsx src/pages/LoginPage.test.tsx` 通过（11 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web build`、`go test ./...`、`git diff --check` 全部通过。真实 API 方面新增并修正本地忽略脚本 `.hermes/local-output/real-api-auth-menu/run.sh`，最终回放成功并产出 `.hermes/local-output/real-api-auth-menu/`：`GET /healthz` 返回 `200` 且 `status=ok` / `redis=ok`；非法注册返回 `400`；合法动态注册返回 `201`，bearer token 下 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints` 均 `200`；seeded admin 登录后 `/auth/menu` 与 `/admin/overview` 也均 `200`。期间按 `systematic-debugging` 先后修正了脚本里 curl 参数数组展开错误与邮箱大小写断言过严这两个 harness 根因，未触碰应用代码契约。
- 本轮五维评审结论：产品/规格 PASS（页面摘要与页脚语义更完整，继续诚实表达单一登录后 shared-console/new-api 单壳，而不是另起独立后台）；代码质量 APPROVED（diff 极小，仅集中在共享壳 1 个运行时文件与 1 个 focused test）；安全/集成 SAFE（未改 auth/session/menu/backend contract，真实 API 回放再次证明注册、菜单、dashboard、admin overview 稳定）；测试/可靠性 PASS（focused test、build、Go tests、diff-check 与真实 API 回放全部通过，且新增 proof 直接保护 summary-region 与 footer-contract）；性能/运维 PASS（纯语义与测试合同补强，不新增请求、轮询或部署负担，真实 API 脚本已保存在本地忽略目录供后续连续执行复用）。

### 最新执行检查点（2026-05-19，LoginPage 注册跑道 CTA 作用域加固）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先收口 `web/src/pages/LoginPage.test.tsx` 这一处登录/注册前端 focused 用例，而不是扩散到新的后端逻辑。目标是在注册入口已完成、共享控制台 runway CTA 已存在的前提下，补上一条更精确的前端合同：`首轮接入路径` 区域内的 API Keys / Webhook / Docs 跑道按钮必须由共享 journey region 独占暴露，不能在 `login-auth-shell` 里泄漏出同名重复 CTA，以继续对齐 new-api 风格的单壳接入叙事与单一登录后控制台路线。
- 控制器按最小切片原则直接回读 `web/src/pages/LoginPage.tsx` / `web/src/pages/LoginPage.test.tsx` 与现有 diff，确认根因不是注册功能缺失、不是路由常量漂移、不是 `/auth/menu` 或共享控制台落点错误，而是 focused suite 之前虽然已验证 runway CTA 可导航到 API Keys / Webhooks / Docs，却尚未显式防守“auth shell 内出现同名重复 CTA 导致测试误点/合同失真”这一 shared-console 前端回归面。基于此，仅新增 1 条 focused 测试：先在 `首轮接入路径` region 内断言三个 CTA 存在，再显式断言 `login-auth-shell` 中不存在同名 CTA，最后点击 `先配置 API Keys` 并验证命中 `login-route-stub-api-keys`，不改运行时代码、不改后端契约、不新增独立后台路径。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（10 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web build`、`go test ./...`、`git diff --check` 全部通过。真实 API 方面新增并执行本地忽略脚本 `.hermes/local-output/loginpage-focused-real-api/run.sh`，最新产物目录 `.hermes/local-output/loginpage-focused-real-api/20260519-112137`：验证 `GET /healthz` 返回 `200 + status=ok`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`；seeded admin 登录后可成功访问 `/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`；前端 `http://127.0.0.1:5173/`、`/login`、`/api-keys`、`/docs` 均返回 `200`，继续满足“真实 API 测试、禁止仅冒烟”的本轮约束。
- 本轮五维评审结论：产品/规格 PASS（新测试诚实保护单一注册入口后的 shared-console runway CTA 归属，不把接入按钮错误地绑定到 auth shell）；代码质量 APPROVED（diff 极小、聚焦单一回归面，局部 `within(...)` 作用域明确，只有轻微与既有 API Keys 导航测试重叠但不构成阻塞）；安全/集成 SAFE（纯前端测试加固，不触碰 auth/session/token/backend 合同，并额外防守 CTA 泄漏导致的错误导航证明）；测试/可靠性 PASS（focused vitest、前端 build、Go tests、diff-check 与真实 API 回放全部通过，且独立 reviewer JSON 审核通过）；性能/运维 PASS（零运行时代码开销，仅补前端测试护栏，真实 API 复核脚本保存在本地忽略目录便于后续连续执行）。

### 最新执行检查点（2026-05-19，App 首轮 API Keys 引导卡片 focused 合同收口）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理工作树里唯一未提交的 `web/src/App.test.tsx` focused 前端切片，而不是扩散到新的后端逻辑。目标不是改注册功能、不是改 `/auth/menu` 真值、不是改 API Keys 运行时页面，而是在 App 级首轮 onboarding 合同里，避免把“打开 API Keys” 误写成过宽的跨页 runtime 证明，转而把 proof 收口回 default-user first-run lane 自身的共享控制台卡片合同。
- 控制器按 `systematic-debugging` + TDD 先复现并定位当前回归面：`App.test.tsx` 原断言在注册成功后点击 `打开 API Keys`，再等待 `api-keys-hero-card` 与全局 heading 渲染，导致该用例同时承担 onboarding 卡片合同与目标页 runtime 渲染两种职责；而同文件其他用例已经分别覆盖共享 onboarding lane、登录后深链、独立页面渲染与导航链路。基于此，本轮只做最小测试收口：保留 `API_KEYS_ROUTE=*** '/api-keys'`、注册后 default-user first-run lane 存在、`dashboard-next-step-api-keys` 卡片内继续暴露 `开发者 API 接入工作台` heading 与 `打开 API Keys` CTA，不新增 runtime test id、不改运行时代码、不改后端契约、不引入独立后台。
- 控制器验证结果：先在 focused RED 过程中暴露过一次 `App.test.tsx` 结构被错误 patch 破坏导致的 `Unexpected end of file`，随后按根因最小修复并重新收口到卡片级断言；最终 `pnpm --dir web exec vitest run src/App.test.tsx` 通过（22 tests，仅既有 React/Semi `act(...)` warning），`pnpm --dir web build`、`go test ./...`、`git diff --check` 全部通过。真实 API 方面继续执行本地忽略脚本 `.hermes/local-output/login-register-home-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/login-register-home-region-real-api/20260519-170317`：`GET /healthz` 返回 `200 + status=ok + redis=ok`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`；继续满足“真实 API 测试、禁止仅冒烟”的约束。
- 本轮五维评审结论：产品/规格 PASS（新 diff 诚实把 App 级用例限定在 onboarding lane 内的共享控制台卡片合同，不再把独立页面渲染混入同一 focused proof）；代码质量 APPROVED（diff 极小，仅集中在 1 个 test 文件，作用域更单一，虽与已有卡片断言有轻微重叠但不构成阻塞）；安全/集成 SAFE（纯前端测试收口，不触碰 auth/session/menu/backend contract，真实 API 回放继续证明注册与共享控制台链路稳定）；测试/可靠性 PASS（focused vitest、build、Go tests、diff-check 与真实 API 回放全部通过，且过程中已按 root-cause 修复一次误 patch 造成的语法回归）；性能/运维 PASS（零运行时代码开销，不新增部署负担；`pnpm build` 仍仅保留既有 chunk-size warning，无新增性能/运维回归）。

### 最新执行检查点（2026-05-19，LoginPage 注册后落地首页 region 合同补强）
- 本轮继续严格遵循“前端优先、单一登录后 shared-console/new-api 单壳、真实 API 回放、五维评审后提交”：从当前 `git status`、`todo.md` 与运行态出发，优先处理工作树里唯一未提交的 `web/src/pages/LoginPage.test.tsx` focused 前端切片，而不是扩散到新的后端逻辑。目标是在已完成前端注册功能、共享控制台单壳落地与角色差异菜单基础上，把“注册成功后回到共享控制台首页”的可见合同从纯 route-stub 跳转证明，进一步收口为带命名 landmark 的用户可见首页 region 合同，以保持 new-api 风格单壳叙事与 post-register landing contract 一致。
- 控制器先按 `systematic-debugging` 回读 `LoginPage.test.tsx` / `LoginPage.tsx` 与当前 diff，确认根因不是注册接口缺失、不是密码/email 前端校验漂移、不是 `/auth/menu` 真值问题、也不是共享控制台首页路由不存在，而是 focused suite 之前虽然已验证“注册成功后会跳到 `/`”，却尚未显式证明落地面具备用户可见的首页语义锚点。基于此，本轮仅最小修改测试桩与 focused 用例：为 `/` route stub 增补 `role="region" aria-label="共享控制台首页"`，并新增一条注册后断言，要求 redirect 最终落在命名 region 且其内部继续暴露 `控制台总览` heading；不改运行时代码、不改后端契约、不引入第二套后台。
- 控制器验证结果：`pnpm --dir web exec vitest run src/pages/LoginPage.test.tsx` 通过（11 tests，仅既有 React/Semi `act(...)` / `findDOMNode` warning）；`pnpm --dir web build`、`go test ./...`、`docker compose config`、`git diff --check` 全部通过。真实 API 方面复用并修正本地忽略脚本 `.hermes/local-output/login-register-home-region-real-api/run.sh`，最新产物目录 `.hermes/local-output/login-register-home-region-real-api/20260519-124649`：验证 `GET /healthz` 返回 `200 + status=ok + redis=ok`；动态注册普通用户返回 `201`，bearer token 可成功访问 `/auth/me`、`/auth/menu`、`/dashboard/overview`、`/auth/api-keys`、`/webhooks/endpoints`；seeded admin 登录后可成功访问 `/auth/menu`、`/admin/overview`、`/admin/risk`、`/admin/audit?limit=5`；前端 `http://127.0.0.1:5173/`、`/login`、`/profile`、`/api-keys`、`/docs` 均返回 `200`。
- 本轮五维评审结论：产品/规格 PASS（新增断言诚实保护“注册后回到共享控制台首页”这一单壳可见合同，而不把证明降级为仅内部 stub 导航）；代码质量 APPROVED（diff 极小、仅聚焦 1 个 focused test 文件与对应首页 stub 语义锚点补强）；安全/集成 SAFE（纯测试与 stub 语义增强，不触碰 auth/session/token/backend contract，真实 API 回放再次证明注册/菜单/首页链路稳定）；测试/可靠性 PASS（focused vitest、build、Go tests、compose config、diff-check 与真实 API 回放全部通过，新用例继续保留用户可见 heading proof 而不是只看 test id）；性能/运维 PASS（无运行时代码开销，仅补 landing contract 护栏，复用本地忽略脚本可持续支撑后续连续执行）。